# DEV_LOG

## 2026-08-31 (standalone-only product and export reliability validated)

- Root cause: the two-owner choice made setup ambiguous and left users on a mode that intentionally suppressed local AHHH, which looked like broken RANE playback. Karl explicitly chose a standalone-only ScratchLab product.
- Files changed: `ScratchLab/Models/CaptureCore.swift`, `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLab/Views/PracticeModeView.swift`, `ScratchLab/Views/MainMenuView.swift`, `ScratchLab/Views/CompanionCameraView.swift`, `ScratchLab/Views/ScratchMotionLane.swift`, `ScratchLabDesktopTests/PlatterTestSampleLoadTests.swift`, `ScratchLabDesktopTests/MIDILearnEngineTests.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, and the four workflow records.
- Implementation: reduced `ScratchAudioOwnershipMode` to standalone, made it the default, migrated unknown/legacy persisted values to standalone, removed ownership selectors and Serato-specific UI/accessibility copy, and removed obsolete external-owner suppression tests. Load/Test AHHH retains existing RANE/MIDI availability gates.
- Preserved boundaries: no renderer/audio scheduling, RANE channel routing, capture, export projection, or signed/unwrapped playhead behavior changed.
- Diff accounting: local HEAD is nine commits ahead of origin. The 73-file 37,527-addition/14,676-deletion comparison includes those commits; the pre-change unstaged worktree was 28 files with 8,654 additions and 1,812 deletions.
- Export implementation included in the validated batch: cross-session Review/export accepts mutable UI configuration only for the matching recorded session; AVFoundation's documented successful-stop marker is classified as success; multichannel audio is opened as deinterleaved Float32 and its audible RANE pair is written to playable stereo; missing source audio remains a named hard failure. Test fixtures use real close-before-read WAV/CAF files and truthful beat/mix artifact assertions.
- Focused verification passed twice for `testCanonicalExportManifestParity`, `testRoutineCaptureExportAcceptsRecordedBPMSubset`, `testSessionExportDuplicatesMonoIntoPlayableStereo`, `testSessionExportProjectsAudibleRanePairToPlayableStereo`, and `testSessionExportRejectsMissingAudioArtifact`.
- Full verification: fixtures 47/47; configured gate 377 XCTest cases per repetition with exactly the established 11 failing invocations / 9 unique names and no new ownership/export failure; Swift Testing 366/366 per repetition. Isolated macOS, iOS Simulator (including embedded Watch validation), and Watch Simulator builds succeeded. Initial concurrent iOS/Watch attempts hit only Xcode's shared build-database lock; sequential reruns passed.
- Remaining risk: physical RANE AHHH output, cyan playhead audible boundary alignment, compact-landscape Capture/Review behavior, Watch relay association, and end-to-end export are not software-test substitutes and remain manual RC checks. Retired External Serato cases are superseded.
- Karl authorized validation, commit, and push with "proceed and dont stop until were clean." At this log point the batch is validated but not yet committed or pushed.

## 2026-08-31 (cross-session Review/export identity failure)

- Reproduced live UI failure: Review showed 36 strokes from session `72b7923d-7086-46d8-b1a9-b7c05af7d75d` while stale review text/config referenced `82223c61-938e-421e-9f5f-ae29d47106c8` take 2. Export failed canonical artifact validation.
- Audio evidence: both takes selected `Rane ONE MKII`, produced 14-channel 48 kHz WAVs, and have strong signal on channels 13/14. Meter LEDs on the RANE hardware are not ScratchLab-controlled and their dark state did not imply a silent file.
- Root cause boundary: `shareLastRoutineSession`, `saveLastRoutineSessionArchive`, and upload always passed mutable `routineSessionSetup.config`, even when it belonged to a different session than `lastRoutineRecordingURL`.
- Implementation: pass UI config/name only when config session ID matches `lastRoutineRecordingSessionID`; otherwise let persisted sidecar metadata drive export. Review reload clears stale status and current-key decision cache before loading the sidecar.
- Current running app is still the stale August 29 test product. Current source changes are unverified; no test/build run.

## 2026-08-31 (FTR ZIP: wrong capture source and beat-enabled configuration)

- Evidence archive: `/Users/karlwatson/Downloads/session_2026_08_31_ftr_baby_scratch_95_bpm.zip`, session `82223c61-938e-421e-9f5f-ae29d47106c8`, take `take-001`.
- Root cause of TV/room audio: session metadata explicitly records `MacBook Pro Microphone` / `BuiltInMicrophoneDevice`; canonical `scratch_only.wav` is mono 44.1 kHz microphone capture, not direct RANE/Serato audio.
- Root cause of beat artifacts: capture used `timed_click` with `beatEnabled: true` and `boom_bap_trainer`; canonical export includes generated `beat_only.wav` and `scratch_with_beat.wav`. `exportMixMode` is still `scratch_only`.
- Required physical correction: select `Rane ONE MKII`, External Serato ownership, and no-click/beat-disabled capture; preflight by checking room/TV does not move the input meter while platter program audio does.
- No archive mutation performed. No new production edit was made for device-selection preflight in this step.
- Earlier pending edits for multichannel-to-stereo export and visible progress remain unverified; no test/build was run after permission was requested.

## 2026-08-31 (latest physical take: relay association and UI truth failed)

- Physical evidence: Mac-led session `af2f4238-228b-4847-b7dc-ac9ec3c4f537`, take `take-001`; Watch stopped and live notation rendered, but Review appeared without notation and UI export did not complete.
- Artifact inspection: recording completed; MOV 65,527,963 bytes; WAV 55,771,136 bytes; sidecar has 70 record-movement events and 4 audio events. Diagnostics report 70 fused, trusted, and final events with no trust drops.
- Relay result: failed. Sidecar is `watchSyncState: timedOut`, has no linked motion filename, and the relayed Watch store has no new artifact.
- Backend Review/export truth: passed. A temporary exact-artifact XCTest asserted 70 decoded events, non-empty gesture-relative presentation, nil export validation report, correct session/take package identity, and non-empty ZIP creation. It passed twice, 1/1 with 0 failures each run, then was removed.
- UI finding: raw captured notation is behind the collapsed `Technical evidence & diagnostics` disclosure. The primary Target/My Performance surface should still use the persisted presentation data, so the reported blank Review remains open pending an exact screenshot/state capture after re-scan or relaunch.
- UI export finding: backend export is valid; exact UI status/validation text was not captured, so no production export change was made.
- Result bundle: `build/DerivedData-latest-physical-take/Logs/Test/Test-ScratchLabDesktop-2026.08.31_04-19-50-+1200.xcresult`.
- Files changed in this investigation: workflow records only after removing the temporary test. No stage, commit, or push.

## 2026-08-31 (macOS successful manual stop no longer blocks export)

- Selected task: investigate the physical report that macOS Stop left the Watch recording and that the resulting take could not export. This was release-critical because the failure was reproducible from the take sidecar.
- Root cause: `MacCaptureEngine.finalizeRoutineRecording` persisted any non-nil movie completion error as capture failure. AVFoundation can report a non-nil manual-stop error while setting `AVErrorRecordingSuccessfullyFinishedKey == true`, so a healthy MOV/WAV pair was mislabeled failed and rejected by export validation.
- Files changed: `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`, `DEV_LOG.md`, and `TASKS.md`.
- Implementation: added a pure failure-classification helper that clears only the documented AVFoundation successful-finish condition. Real AVFoundation failures and non-AVFoundation errors remain fatal, including spoofed successful-finish keys.
- Permanent verification: three focused XCTest cases passed twice, 3/3 with 0 failures each run; the selected macOS test action built successfully. Tests cover successful AVFoundation stop, genuine AVFoundation failure, and non-AVFoundation spoof rejection.
- Artifact evidence: session `0c813395-28b1-42d6-9061-e2399c312a1f`, take `take-001`; MOV 99.696563 s / 291,314,513 bytes; WAV 100.693333 s / 270,667,776 bytes; delta 0.996770 s. The original sidecar was backed up before an atomic recovery that preserved the failed audit event and added recovery audit event `2DAA3C48-907A-4E32-B5A9-0F6C5C794A83`.
- Export verification: a temporary artifact-specific XCTest exercised validation, package preparation, and archive creation against the recovered take. It passed twice, 1/1 with 0 failures each run, and exported 4,833,280 scratch-audio frames. The temporary test was then removed; it is not part of the permanent suite.
- Watch finding: this take had `watchSyncState: unavailable`, no linked motion filename, and no relayed Watch file. Mac Stop could not stop a Watch capture it did not own. A short Mac-led physical relay retest remains required before classifying any Watch-stop behavior as a code defect.
- Advisory warnings: low signal and unstable onset timing do not block export. `Saved review notes.` reports notes persistence only.
- Build/result bundle: focused permanent run `build/DerivedData-stop-finalization/Logs/Test/Test-ScratchLabDesktop-2026.08.31_01-56-18-+1200.xcresult`; recovered artifact run `build/DerivedData-stop-finalization/Logs/Test/Test-ScratchLabDesktop-2026.08.31_02-05-26-+1200.xcresult`.
- No stage, commit, or push performed. Broader cross-platform RC gate remains pending.

## 2026-08-30 (macOS upfaders + hot cues fixed by device re-selection; A8/A9 found)

### Resolved: macOS upfaders and hot cues not working or mapping
Karl: "only crossfader shows in midi upfader and HC not showing or mapping", then "no saved mapping for this device yet". Resolved by **Refresh MIDI + explicitly selecting "Rane ONE MKII"** as the input source. Karl: "11 controls mapped now, upfaders and pads working".

**The mapping was never missing.** On-disk evidence at the time of the failure report:
`~/Library/Containers/com.machelpnz.scratchlab/Data/Library/Application Support/ScratchLab/MIDIMappings/midi_1737453891.json` (written 20:57) contained **all 11 verified controls** — `crossfader` ch15 CC8; `leftUpfader` ch0 CC28; `rightUpfader` ch1 CC28; `hotCue1-8` ch5 notes 20-27 — all `isVerified: true`, `deviceName: "Rane ONE MKII"`.

So `RaneOneMKIIVerifiedLearnedMapping.apply` and `mutateDeviceMapping` both worked. The failure was that the **running app never loaded that file**: `currentMIDIDeviceMapping` was nil, so `evaluateUserMixerGainForCC` returned on its first `guard` and no upfader or hot-cue dispatch could occur. The crossfader kept working throughout because it also persists via the separate legacy `crossfaderCCMapping` / `persistedCrossfaderMapping` path, independent of the learned mapping.

### A8 (NEW, OPEN) — learned mappings persist under an empty device identifier
Found in the same directory during diagnosis:
```
.json                       <- literally named ".json"
  deviceIdentifier: ""      <- empty
  deviceName:       "Not Connected"
  controls:         ["crossfader"]   <- crossfader ONLY
```
`MIDILearnedMappingStore.fileURL(for:)` sanitises `/` and `:` but does not reject an **empty** identifier, so `fileURL(for: "")` yields `.json` and a "Not Connected" session writes a real, loadable mapping file. A crossfader-only mapping saved this way can later load as if it were a device, which is a precise match for Karl's first symptom ("only crossfader shows"). Not fixed.

### A9 (NEW, OPEN) — `MIDILearnedMappingStore.save` swallows every error
```swift
func save(_ mapping: MIDIDeviceMapping) {
    let url = fileURL(for: mapping.deviceIdentifier)
    guard let data = try? encoder.encode(mapping) else { return }
    try? data.write(to: url, options: .atomic)
}
```
Both the encode and the write use `try?`, so a failed save is indistinguishable from success. The caller still reports "Applied verified Rane ONE MKII mapping: faders and right-deck hot cues." Not the cause here (the write succeeded), but it means a genuine persistence failure would be invisible. Contrast `loadOrThrow`, which deliberately surfaces errors. Not fixed.

### Observation — duplicate mapping stores, sandboxed vs not
Mapping files exist in **both** `~/Library/Application Support/ScratchLab/MIDIMappings/` (`midi_1737453891.json`, 20:37) and `~/Library/Containers/com.machelpnz.scratchlab/Data/Library/Application Support/ScratchLab/MIDIMappings/` (same name, 20:57). Two builds with different sandbox states have been writing to different stores. Not a current defect but a future source of confusion — a mapping "disappearing" would be explained by a sandbox-state change between builds.

### Also noted, not fixed
`installVerifiedRaneOneMKIIMapping` (`MacCaptureEngine.swift:8060-8068`) calls `pushResolvedCurve` for `crossfader` and `rightUpfader` but **not** `leftUpfader`, so the left channel fader is mapped without its fader curve being pushed to the audio path. Relevant to the existing left/right upfader parity contract.

## 2026-08-30 (macOS notation ring fills camera view; RANE AHHH silent — NEW open regression A7)

### macOS notation ring now fills the camera view (Karl request, done)
- Karl: "fix the macos notation to fill the camera view". Clarified against the two candidate readings — he selected **the notation ring is too small**, not the camera stage.
- Mechanics found: `CameraPassthroughNotationView` already spans the view (`GeometryReader` -> `ZStack`, `MacCameraPreviewView` at `.resizeAspectFill`, so no letterboxing) and the `Canvas` already fills it. What did not fill was the platter guide ring: `platterRadius` defaulted to **0.35 of the shorter axis** (~70 % span), leaving a ~30 % margin.
- Change: `CameraNotationOverlayCalibration.platterRadius` default **0.35 -> 0.5**, in both the stored property and `reset()`. No clamp or slider change was needed — `radiusRange` was already `0.05...0.50`; only the starting point was conservative. Still calibratable down to 0.05.
- Trade recorded and accepted by Karl: at 0.5 the ring no longer matches the physical platter in frame. The guide's original job was alignment; it is now a display default.
- Three assertions in `CameraNotationOverlayTests.swift` that pinned the default/reset at 0.35 were updated to 0.5 (not deleted — they still verify reset-restores-default and unset-key-yields-default).
- **Baseline captured this time.** `CameraNotationOverlayTests` failing set is byte-for-byte identical before and after the change — the same 9 cases, all `must load from bundle` / mode-switch resource failures. Zero radius or calibration failures; 26 of 35 pass. For this class the failures are now *proven* pre-existing, not merely assumed.
- **Karl must press Reset** in the calibration controls to see it: `loadFromDefaults()` only applies the new default when no `scratchlab.mac.cameraOverlay.platterRadius` key exists, and his machine has a persisted value. Without Reset the change is invisible and looks like a failed fix.

### A7 (NEW, OPEN) — RANE not playing AHHH; established right-deck route reads silent
Karl: "rane not playing ahh now". This is a **regression against Case 3**, which passed earlier the same day (AHHH audible, smooth, right-upfader controlled, crossfader cutting).

Device analysis Karl supplied — 14 ch @ 48000 Hz, 72192 frames, "recommended program pair: (none — no pair looks like program audio)":

| Pair | L RMS | R RMS | L peak | R peak | status |
|---|---|---|---|---|---|
| 1/2 | -110.4 | -110.4 | -97.4 | -97.4 | L silent / R silent |
| 3/4 | -98.5 | -98.4 | -88.2 | -87.7 | L silent / R silent |
| 5/6 | -115.5 | -115.4 | -102.8 | -102.1 | L silent / R silent |
| 7/8 | -inf | -inf | -inf | -inf | L silent / R silent |
| 9/10 | -inf | -inf | -inf | -inf | L silent / R silent |
| 11/12 | -inf | -inf | -inf | -inf | L silent / R silent |
| 13/14 | -16.5 | -4.8 | -16.5 | -0.0 | **L dcHeavy / R program** |

Observations, not conclusions:
- **CH 3/4 is silent.** That is the established AHHH right-deck route (see 2026-08-29 "RANE right-deck playback routing: AHHH reaches USB output 3/4, physically approved"). Silence there is consistent with the reported symptom.
- **CH 13/14 carries the only real signal**: R at -4.8 dBFS RMS, peak -0.0 dBFS (at/near full scale). L on the same pair is flagged `dcHeavy` with a large **DC offset of -0.14937** — anomalous in its own right and worth explaining regardless of the AHHH question.
- Pairs 1/2, 3/4, 5/6 sit at a noise floor (-98 to -115 dBFS) rather than true digital silence; 7/8 through 11/12 are true `-inf`.

Not yet established, and must not be assumed:
- Whether this was observed on **iOS or macOS** (Karl relaunched the rebuilt macOS app immediately before reporting).
- Whether **`ScratchAudioOwnershipMode` was External Serato** at the time. External Serato is the safe default and *by design* blocks and unloads every local AHHH route — if the mode reset or was never switched after relaunch, silent AHHH is correct behaviour, not a defect. This is the first thing to check.
- Whether the **AHHH sample was loaded** (`"Standalone audio enabled — load AHHH to arm the platter."`).
- Whether the capture shown is an input-side or output-side measurement.

**This session's code changes are not a plausible cause, though this is reasoning, not proof:**
- `CaptureCore.deriveDetectedNotationFaderEvents` (segmentation + cut gate) is a pure notation derivation with no audio path.
- `CameraNotationOverlayCalibration` is a macOS UI calibration default.
- `iOSMIDIManager` `.upfader` — the only edit near an audio path. Inspected after the report: the new evidence-capture block sits strictly between the `leftUpfaderMIDIValue`/`rightUpfaderMIDIValue` assignment and the gain routing, and the `playbackEngine.setRightUpfaderGain(...)` branches are byte-for-byte unchanged. Crossfader audio (`setCrossfaderPosition`, gated on `source == .learned`) was not touched.

Next diagnostic steps are recorded in `AI_HANDOFF/next_prompt.md`.

## 2026-08-30 (Physical RC smoke halted at Case 4; crossfader notation root cause found and fixed)

### Physical RC matrix — partial, NOT complete
Operator: Karl, iPhone `K` + RANE ONE MKII. Observations are Karl's own words; no pass was inferred from logs, launch, or source.

- **Case 1 — External Serato default, no local AHHH: PASS.**
  - Karl: "External Serato was selected", and moving the right platter produced "no AHHH through the RANE".
  - Recorded limitation: this confirms *selected on entry*, not *first-run default*. The mode persists under `scratchlab.audio.ownershipMode`; whether this install had ever switched was not established. `ScratchAudioOwnershipMode.defaultMode = .externalSerato` in source is NOT counted as physical evidence.
- **Case 2 — truthful non-empty export while External Serato owns playback: NOT TESTED.**
  - Serato does not run on iOS. Karl's rig can host Serato on a Mac via the RANE's second USB port, but both exports he produced were recorded with ownership on `ScratchLab AHHH`, confirmed by Karl ("i used sl owned audio"; "it read ScratchLab AHHH before, i didnt touch it").
  - No mode-stability defect: the mode persisted correctly across a take.
- **Case 3 — Standalone loads and plays local AHHH on the right-deck route: PASS (by control authority).**
  - Karl: AHHH audible, smooth, right upfader controls it, left upfader does nothing, crossfader cuts it.
  - Karl corrected an earlier statement: **no channel meter LEDs lit at all**, either side. Whether the RANE meters illuminate for any source was not tested, so meters are recorded as unavailable evidence — not as pass or fail. The prior symptom (AHHH audible while the LEFT meters lit) did not recur.
- **Cases 4-8 — NOT OBSERVED.** Case 4 (External re-select silences AND unloads an armed sample) was queued but never run. Cases 5-8 (playhead across BEFORE START / PAST END, Capture and Review compact-landscape reachability) were not started.

The physical RC matrix is **NOT complete** and must not be marked so.

### Root cause: crossfader gestures never became notation (fixed)
Karl observed the notation drew "a full line no breaks to indicate i was cutting the fader". Confirmed from his own export, not inferred.

- Evidence in `take-002_detected_notation.json`: `mixerMidiEvents` 14489 — platter `(iOS RANE Platter, ch1, CC6)` 12777 and crossfader `(Rane ONE MKII, ch15, CC8)` 1712 — while `faderEvents` was `[]`. CC8/ch15 is the crossfader address `RaneControllerProfiles.swift:132` documents.
- Six discrete close gestures were present in the raw MIDI at 3.74, 4.49, 6.51, 7.93, 8.69, 8.92 s.
- **Root cause:** `CaptureCore.deriveDetectedNotationFaderEvents` formed primitives from the delta between *adjacent MIDI samples* and gated on `minimumValueDelta = 0.10`. The RANE streams CC8 at ~800 Hz (median inter-event gap 1.25 ms), moving 1-3 MIDI steps per sample. Measured **maximum adjacent delta across the whole take: 0.0236**. No pair could ever clear the gate, so `primitives` was empty and the function returned `[]`. The derivation was sample-rate dependent.
- Why tests never caught it: the fixtures in `CaptureReliabilityPhase1Tests.swift` feed 0.0 -> 1.0 -> 0.0 jumps at ~60 ms spacing, encoding a sparse-fader assumption real hardware violates by two orders of magnitude.
- **Fix:** primitives are now formed from *monotonic runs*, segmented exactly as `decodePlatterCore` segments CC6 (zero deltas ignored; a direction reversal or a gap > `maximumRunSampleGap` 0.20 closes the run). All downstream classification, confidence math and `makeEvent` are unchanged. On a sparse stream each run is a single adjacent pair, so previous behaviour is preserved exactly.
- Verified failing-then-passing: `testDenseCrossfaderStreamDerivesFaderEventsAtHardwareUpdateRate` failed with `derived.count == 0` before the change and passes after.
- **Hardware confirmation:** post-fix take-003 produced **78 fader events** (was 0) with `detectionSources` `['controller','midi']`. Independent prediction of the shipped algorithm against that take's raw data was 78 — an exact match. take-001 in the same export shows 0 because it was recorded on the pre-fix build (its data predicts 38).

### Second defect: detected but invisible (cut gate widened, Karl-approved)
- `ScratchMotionRenderer.swift:468` draws crossfader ticks only for `.cut/.pulse/.transformPulse/.flareClick`; `.open/.closed/.unknown` draw nothing. So of take-003's 78 events, only 20 rendered — Karl reported "crossfader didnt get every time".
- Physical evidence across 39 real closing gestures: median duration **0.110 s** (p90 0.276), median travel **0.409** (p10 0.236). A right-deck cut runs centre-to-left, not rail-to-rail. The old `maximumCutDuration = 0.15` sat on top of the median.
- Karl chose `minimumCutDelta` 0.35 -> **0.25** and `maximumCutDuration` 0.15 -> **0.25**. Verified against his real take: **19 -> 31 of 39** closes now render. 8 remain undrawn (3 within a hair of the gate); the next notch down would admit a 0.102-travel drift that is not a cut.
- Renderer was NOT changed. Canonical model was NOT changed.

### Third defect: upfader evidence never captured (fixed, evidence-only)
- Karl: "upfadrers didnt map" — clarified as "they dont appear in the notation". Upfaders drive audible gain correctly.
- Confirmed: **no CC28 in any iOS export**. `iOSMIDIManager.swift` `.upfader` case set gain and never appended to any captured-evidence array, while platter and crossfader both do. An entire control stream was silently absent from exported evidence.
- Fix (Karl-approved, evidence-only): new `capturedUpfaderMIDIEvents`, cleared per take, merged into `capturedMixerMIDIEvents`, tagged `mappedControl: "leftUpfader"/"rightUpfader"`. Kept separate from the crossfader array so platter decoders keep their exact input and `deriveDetectedNotationFaderEvents` still sees only crossfader samples.
- **Upfaders still do not appear in the notation lane.** "upfader" exists nowhere in `CaptureCore.swift`, `ScratchMotionRenderer.swift` or `ScratchMotionLane.swift` — there is no upfader lane in the canonical model. Making one is an unstarted architect slice spanning capture + canonical model + renderer.

### macOS: same bug, stale binary
- Karl's macOS export (`platform = macOS`, ScratchLab Routine Recorder, captured 16:30 NZST) showed 1599 crossfader events, `faderEvents: 0`, max adjacent delta **0.0236** — the original bug signature.
- macOS routes through the shared `DetectedNotationSnapshot.withMixerMidiEvents` -> `CaptureCore.deriveDetectedNotationFaderEvents`, so **the same fix covers macOS**; no separate work. That take predicts 44 events / 31 drawn once rebuilt.
- macOS app rebuilt 2026-08-30 16:36 (`** BUILD SUCCEEDED **`). Requires quit + relaunch before a fresh take will show it.
- Also observed on macOS: CC28 upfader IS captured (224 events, `mappedControl: nil`), and `audioEvents` was non-empty (3). **A2 below is therefore iOS-specific, not shared.**

### Open anomalies (banked, not fixed)
- **A1 — periodic pop, ScratchLab-owned.** Idle on Practice, RANE connected, ownership External Serato. Pop/glitch (not a metronome click) from the iPhone speaker at a **timed 8 s** interval. Karl's isolation: continues when backgrounded, **stops on force-quit** — so ScratchLab owns it. Not the RANE, not iOS, not another app. No 8 s timer exists in the iOS app (timers are 1/30 s, 1 s, 0.1 s, 1/60 s), so this is not a scheduled sound. `AudioEngine.swift:250` sets `.playAndRecord` with `.defaultToSpeaker`, which explains the route but not the source. Confirmed **absent from captured audio** (transient scan found only dense scratch content, no isolated 8 s-spaced events). Root cause NOT investigated.
- **A2 — `audioEvents: []` on iOS.** Both iOS takes reported zero audio events on material peaking at -0.2 dBFS. `SessionQualityAnalyzer.swift:158` reads `snapshot.audioEvents.count`, so the snapshot arrives empty; `iOSMIDIManager.detectedNotationSnapshot` hardcodes `audioEvents: []`. macOS produces audio events, so this is iOS-only. PROFILE.md calls audio-onset timing "the reliable near-term Review/notation path", so this matters. Same "detected but empty" shape as the fader bug.
- **A3 — `audio_source` hardcoded.** `SessionExportCoordinator.swift:4272` writes `audioSource: "serato"` unconditionally. Both iOS takes were recorded in ScratchLab-owned mode yet exported as `"serato"`. Mislabelled provenance in an ML dataset export.
- **A4 — incorrect doc comment.** `CaptureCore.swift:14-16` claims exported audio "always represents ScratchLab's real output bus (silence when Serato owns playback)". `AudioEngine.swift:574` installs the capture tap on `engine.inputNode` — a hardware input tap. Wrong on both counts. Documentation only.

### Verification performed
- macOS `build-for-testing`: `** TEST BUILD SUCCEEDED **`.
- iOS build + install + launch on `K`: succeeded twice — sequence **2752** (segmentation fix) and **2760** (fix + widened gate + upfader capture).
- macOS `build`: succeeded (16:36).
- Fader/crossfader tests: **10/10 pass**, including the two negative tests (`testUnmappedMIDIDoesNotCreateDerivedFaderEvents`, `testRawMIDIExportsMixerMidiEventsWithoutFakeFaderEvents`) and the new `testShallowSlowFaderDriftStaysUnknown` which pins the widened gate against admitting noise.
- Full bundle via `xcrun xctest`: 3180 tests, **208 failures / 92 cases**. 78 are `XCTUnwrap`/bundle-resource failures — the known cost of running the bundle without its app host. None are on the fader derivation path; the two `fader`-named failures (`LaneFaderSpanAdapterTests`) both die on `XCTUnwrap(ScratchNotation.loadBabyScratchFromBundle())` and never call the modified function. **Not proven pre-existing** — no baseline run was captured before editing. Honest ceiling: "unrelated, almost certainly pre-existing".

### Files changed (uncommitted)
- `ScratchLab/Models/CaptureCore.swift` — monotonic-run segmentation; cut gate 0.25/0.25.
- `ScratchLab/MIDI/iOSMIDIManager.swift` — upfader evidence capture.
- `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift` — 4 new tests.
Nothing staged, committed, or pushed. HEAD remains `7761c09f`. The other 24 pre-existing dirty files and the untracked `CalibrationCameraOverlayTests.swift.plist` were not touched.

## 2026-08-30 (Physical RC smoke command reached launch twice on unlocked phone)
- Commanded smoke run: `./scripts/run-on-phone.sh 1F80398A-96C8-537A-B0EE-821E186918B9` on iPhone `K`.
- Result: **PASS at command level**; app built, installed, and launched on unlocked device.
- Retry after Karl confirmed the phone was unlocked: exit `0` in about 8 seconds; `** BUILD SUCCEEDED **`; bundle `com.machelpnz.scratchlab` installed at `file:///private/var/containers/Bundle/Application/EC170B5E-8286-466B-82C3-49C437724A1D/ScratchLab.app/` with install database UUID `91F773FB-990B-4254-A7D6-CDD841AE068C`, sequence `2744`; launch succeeded.
- Retry scope: second build/install/launch pass only. No production code was changed, and no manual feature result is inferred from deployment.
- Build evidence:
  - `** BUILD SUCCEEDED **`
  - `Building app...`
  - `Using device: 1F80398A-96C8-537A-B0EE-821E186918B9`
  - `Launched application with com.machelpnz.scratchlab bundle identifier.`
  - `Done.`
- Not run in this command: feature checks for ownership switching, local/Standalone AHHH behavior, external re-arm silencing, signed cyan playhead boundary checks (`BEFORE START` / `PAST END`), and compact landscape Capture/Review scroll/action checks.
- Follow-up: perform manual RC evidence collection on-device with RANE connected and append pass/fail per scenario to this section and `TASKS.md`.

## 2026-08-30 (Physical RC smoke attempt blocked)
- Commanded smoke run: `./scripts/run-on-phone.sh 1F80398A-96C8-537A-B0EE-821E186918B9` on iPhone `K`.
- Result: **PASS/FAIL status = blocked/fail at lock gate**.
- Evidence:
  - `Using device: 1F80398A-96C8-537A-B0EE-821E186918B9`
  - `Checking lock state...`
  - `Phone is locked. Unlock it and rerun.`
- Independent validation: `xcrun devicectl device info lockState --device 1F80398A-96C8-537A-B0EE-821E186918B9`
  - `passcodeRequired: true`.
- Build/install/launch, route switching, playback, layout, and signed playhead verifications were not run.
- Follow-up: unlock iPhone `K` and rerun the physical RC smoke before cross-platform RC audit and archive/distribution validation.

## 2026-08-30 (Capture-integrity Slice H: iPhone landscape and signed sample-position closure)
- Selected task: the first unchecked implementation-ready item, Slice H only. Slice F/G behavior, the later DEBUG capture-movement evidence task, and all protected project/config files were left out of scope.
- Root cause: signed/unwrapped renderer travel was already correct, but the iOS-only region classifier lacked deterministic value-level coverage. Landscape Review could vertically crowd details against fixed take actions, Capture mirrored the leading safe-area inset onto the trailing edge, and the action row needed a compact form without losing Export. External Serato also showed Standalone AHHH-loading copy in an otherwise owner-aware waveform.
- Figma: in `AgrnQXwRvkAKlORTQ2U25z`, extended `SamplePositionWaveform` set `457:3817` with reviewed Before Start `493:262` and Past End `493:277` variants, reusing the warning token and explicitly showing signed time, correct boundary, and fill state.
- Files changed for Slice H: `ScratchLab/Models/ScratchSoundBank/ScratchPlatterTracker.swift`; `ScratchLab/Audio/iOS/IOScratchPlaybackEngine.swift`; `ScratchLab/Views/ScratchMotionLane.swift`; `ScratchLab/Views/CompanionCameraView.swift`; `ScratchLabDesktopTests/ScratchPlatterTrackerTests.swift`; `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`; Figma file `AgrnQXwRvkAKlORTQ2U25z`; and the workflow records. Existing unrelated hunks in overlapping files were preserved.
- Implemented: moved the existing 5 ms-tolerant signed region projection into pure shared `PlatterSamplePositionProjection`; the iOS engine delegates while the renderer/audio schedule remains unchanged. Corrected owner-specific empty waveform text. Used separate leading/trailing safe-area insets, a `ViewThatFits` Capture toolbar that retains every action, and a Review details-only vertical scroller so the media and take actions remain fixed.
- Verification: focused set 26/26 twice; relevant regressions 60/60 twice; isolated iOS Simulator Debug with embedded Watch and explicit macOS Debug builds succeeded; fixtures 47/47; full configured gate 3,545 tests across two configurations with the unchanged 11 failing invocations / 9 unique names plus 366/366 Swift Testing; `git diff --check` clean. Protected hashes unchanged. Nothing staged, committed, or pushed.
- Follow-up: physical iPhone landscape and RANE eyes-and-ears smoke remains before release-candidate sign-off. After Slice H, three release-critical batches remain before ASC-ready: Slice G/H physical ownership/layout smoke, final cross-platform RC audit, and current-candidate archive/distribution validation. The DEBUG-only capture-movement evidence item remains separately unchecked but is not an ASC blocker unless product scope elevates it.

## 2026-08-30 (Capture-integrity Slice F: container-relative kept-ledger references and one-way legacy rebase)
- Selected task: the confirmed physical root cause of the 2026-08-30 Slice F device failure. No other slice was started.
- Root cause (confirmed on device, not inferred): `GuidedCaptureKeptTake` stored `sourceMediaURL` / `sourceSidecarURL` / `sourceAudioURL` / `sourceWatchMotionURL` as absolute `file://` URLs, and `GuidedCaptureKeptSessionStore` persisted them verbatim into `guided-capture-kept-sessions.json`. Those URLs embed the iOS app Data-container UUID (`B116F53B-…`), which iOS reissues across installs. The captures were intact — capture sidecars store only relative file names, which is why local recovery still resolved them — but every ledger path dangled, `CompanionCameraView` copied the dead URLs straight into `SessionExportTake`, and `SessionArchiveBuilder.packageValidationIssues` correctly reported the files missing. The export validators were behaving properly; the ledger was the defect.
- Files changed in this slice: `ScratchLab/Models/CaptureCore.swift`; `ScratchLab/Views/CompanionCameraView.swift`; `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`; and the workflow records. `project.pbxproj`, `ScratchLab.xcscheme`, `Info.plist`, and the untracked `CalibrationCameraOverlayTests.swift.plist` were not touched or staged, and all other pre-existing dirty hunks were preserved. No new source file was added, so no `project.pbxproj` reference was needed.
- Implemented — durable form: added `CaptureContainerRoot` (`documents`, `application_support`), `CaptureArtifactKind` (media / sidecar / audio / watch motion, each owning a fixed set of capture directories), `CaptureArtifactReference` (`.containerRelative(root:path:)` or `.absolute(URL)`), and `CaptureContainerLocator`, which resolves both roots for the container the app is running in now. The take's four location fields are now `CaptureArtifactReference`, so reading a concrete `URL` requires a locator at compile time; there is no way to consume a stored location without rebasing it. The JSON keys are unchanged (`sourceMediaURL` etc. via explicit `CodingKeys`) and `CaptureArtifactReference` decodes either a legacy absolute URL string or the new `{containerRoot, relativePath}` object, so an existing install still loads.
- Implemented — one-way legacy rebase, in `GuidedCaptureLedgerRebase`, run once at load: this is deliberately NOT a search. Each legacy absolute path is INTERPRETED — its container-root segment is located, and the remainder must already be a capture directory that artifact kind owns followed by a single file name — producing at most one candidate, never a basename match. The candidate is then proved against the recovered sidecar before it is accepted: session ID, take ID, take number, the sidecar's own file name, the sidecar's `mediaFileName`, the audio file's media sibling basename, and the sidecar's `linkedMotionFileName` for Watch/motion. Notation is not a separate reference — it is generated from the sidecar, so proving the sidecar proves the notation source. All four locations of a take move together or none do. A take whose evidence fails keeps its stale location and is reported in `lastRebaseReport`.
- Rejected, each with its own reason: `.`/`..` in any stored or legacy path; a legacy path naming its container root more than once; a location inside the container but outside the directories its kind owns; an unreadable sidecar; a sidecar belonging to another session/take/number; a media, audio, or Watch file the sidecar does not name; a genuinely absent file; and two takes — in one session or across sessions — resolving onto the same file.
- Atomicity and blast radius: a successful rebase is written through the store's existing single atomic write-and-verify transaction with its optimistic-concurrency token, so a partially repaired ledger is not observable. A refused rebase leaves the ledger byte-identical and every take attached. The rebase only ever rewrites takes the ledger already holds — it never adopts an unkept capture that happens to sit in the same directory, which a test now pins directly. Persisting is fail-closed: an absolute path inside a current container root is refused with `containerOwnedAbsoluteSourceURL`, so the defect cannot be reintroduced by a future caller. Loading still accepts absolute paths, because a legacy ledger legitimately holds them and must survive to be repaired.
- Not changed: no export validator was weakened, no path or file is fabricated, capture files are never moved/renamed/deleted, the export schema is unchanged, and the staged-BYTES `createdAt` comparison plus `SessionExportValidationReason` from the previous Slice F turn are intact.
- Verification: nine new deterministic tests plus the nine existing kept-ledger/export tests passed 18/18 in two executions via `xcodebuild test-without-building` with per-method `-only-testing:` selectors. Teeth were proved, not assumed: against a temporary mutation restoring the pre-fix behaviour (locations always stored absolute, no persist-time gate, no load-time rebase) eight of the nine new tests FAILED, and against a separate mutation disabling path-component safety the traversal test FAILED. `CaptureRecoveryPhase2CoreTests` 58/58, `ScratchLabNotationAndExportTests` 29/29, `CaptureMotionEvidenceTests` 13/13, `CaptureFinalizationMachineTests` 28/28, `GuidedCaptureReviewStateTests` 10/10, `SessionExportRoundTripTests` 1/1. Isolated generic iOS Simulator Debug build and isolated macOS Debug build + `build-for-testing` succeeded. `python3 scripts/test_capture_pipeline.py` passed 47/47. `git diff --check` clean. The full configured gate ran both configurations at 3,166 XCTest cases / 49 skipped / **11 assertion failures across the established 9 unique names** — the recorded baseline, with no ledger, export, or Slice F name in the set — plus 366/366 Swift Testing cases in each pass. Nothing staged, committed, or pushed.
- Physical approval — PASSED on iPhone `K` (Karl, 2026-08-30). A fresh signed build using isolated Xcode paths was installed as an upgrade with no uninstall/erase. The preserved `b5a93fd7-…` ledger migrated on load from stale absolute `B116F53B-…` paths to container-relative references for the three kept takes 002/003/004. Take 001 was completed but not kept and is correctly absent. Retry Export reached the share sheet and produced `session_2026_08_30_new_dj_baby_scratch_95_bpm 5.zip`.
- Independent archive/source inspection: takes 002/003/004 occur exactly once; 17/17 manifest artifact hashes match; every packaged file is non-zero; device-source video/audio hashes equal the packaged camA/scratch artifacts; all source sidecars identify the same session/take/number, report `recordingStatus: completed`, and have no error; notation identities/events match the sidecars; Watch sessions for 003/004 match their linked sidecars and exported CSV evidence; session/export metadata, review, replay, manifest, and take log agree on the exact three kept takes. Karl reports the current build also shows cancellation as cancellation and allows a subsequently recorded/kept take to export. No second ZIP was supplied for that last append, so it is explicitly operator-reported evidence; Karl directed that it be trusted and Slice F closed.
- Slice F is complete and checked. The next unchecked implementation-ready task is production iOS saved-take notation, playback, and take-detail. Nothing staged, committed, or pushed; the protected files remain hash-identical to task start.


## 2026-08-29 (Capture-integrity Slice D: gesture-relative notation versus raw sample position)
- Selected task: the first implementation-ready unchecked item, Slice D, separating notation coordinates from the raw signed/unwrapped travel required by sample playback and the waveform playhead.
- Root cause: the performance lane consumed accumulated CC6 motor phase directly. Several powered forward revolutions therefore raised the coordinate origin, so the next pull started from that historical absolute phase instead of its own gesture baseline. The raw coordinate itself was not wrong: audio and the sample-position waveform require that signed displacement from the hot cue to remain unwrapped.
- Files changed in this slice: `ScratchLab/Models/ScratchSoundBank/ScratchPlatterTracker.swift`; `ScratchLab/Models/CaptureCore.swift`; `ScratchLab/MIDI/iOSMIDIManager.swift`; `ScratchLab/Audio/iOS/IOScratchPlaybackEngine.swift`; `ScratchLab/Views/PracticeModeView.swift`; `ScratchLab/Views/CompanionCameraView.swift`; `ScratchLabDesktop/Services/LivePerformedNotationTracker.swift`; `ScratchLabDesktop/Models/LiveNotationOverlayModel.swift`; `ScratchLabDesktop/Models/CameraNotationOverlayGeometry.swift`; `ScratchLabDesktop/Views/ScratchPhraseChartView.swift`; `ScratchLabDesktop/Views/LiveNotationOverlayView.swift`; `ScratchLabDesktop/Views/CameraPassthroughNotationView.swift`; `ScratchLabDesktop/Views/NotationVisualizerView.swift`; `ScratchLabDesktop/Views/MacAnalyzerView.swift`; `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`; `ScratchLabDesktopTests/CapturedNotationTravelTests.swift`; `ScratchLabDesktopTests/LiveNotationOverlayTests.swift`; `ScratchLabDesktopTests/LivePerformedNotationTrackerTests.swift`; `ScratchLabDesktopTests/CameraNotationOverlayTests.swift`; and the workflow records. Existing unrelated dirty hunks were preserved.
- Implemented: added named `PlatterCoordinateSemantics` for (1) gesture-relative notation and (2) raw sample position. The shared CaptureCore presentation path reuses the existing controller movement-run/reversal decoder and its noise gates, then gives each committed run local coordinates while retaining direction, time, raw displacement, speed, and real unbounded excursion. Live iOS and macOS use the same committed/provisional projection; iOS retains a decoder-boundary anchor so the live window stays bounded without resegmenting a visible committed run or losing the first reversal after motor suppression. Saved snapshots are presentation-reconciled without modifying canonical evidence: fusion-merged spans expand to covered raw decoder runs, fusion-dropped valid runs remain visible, and exact canonical matches retain their original metadata. Review, Take Detail, camera, overlay, and result surfaces consume those events directly, avoiding the old lossy mini-notation round trip. After the replacement hardware captures exposed undersized output, controller notation adopted a stable, display-only quarter-revolution full-scale frame: 0.10 / 0.20 / 0.25 revolution maps to 40% / 80% / 100% of the lane, while the physical event remains exact and unbounded. macOS live notation now fills the finite camera stage instead of a fixed 118-point canvas. Travel at or above a quarter turn intentionally rail-saturates visually; it is not rewritten in canonical evidence/export. Mixed-source charts retain their prior content-derived frame. Camera arc endpoint markers now apply sub-full radius exactly once rather than squaring the visible travel.
- Integrity boundary: canonical movement events still feed scoring/export unchanged. Audio scheduling was not altered. `IOScratchPlaybackEngine` now expresses its existing raw hot-cue subtraction through `samplePosition`, and the renderer/waveform continues to receive signed unwrapped travel, including `BEFORE START` and `PAST END`. Completed strokes are immutable; later strokes may scroll the live window but cannot rewrite prior coordinates.
- Hardware evidence: the replacement iOS archive `session_2026_08_29_new_dj_baby_scratch_95_bpm 3.zip`, macOS archive `session_2026_08_29_new_baby_scratch_95_bpm.zip`, and two macOS screenshots supersede the earlier `rr` session. They show a locally stable alternating trace and expose the objective scale defect: ordinary scratches occupy only a small band under the former one-revolution/118-point presentation. They predate the quarter-turn zoom, so they do not approve its final physical size. They also do not by themselves prove the iOS `BEFORE START` / `PAST END` states or saved-surface parity.
- Verification: final focused set (`CapturedNotationTravelTests` 12, `ControllerPlatterDecoderTests` 18, `LiveNotationOverlayTests` 65, `LivePerformedNotationTrackerTests` 16, `CameraNotationOverlayTests` 35, and the raw-waveform contract 1) passed 147/147 twice. Relevant CaptureCore/noise/waveform, overlay, continuous-drive, and saved-Review regressions passed 88/88 twice. `python3 scripts/test_capture_pipeline.py` passed 47/47. Fresh isolated macOS and generic iOS Simulator builds succeeded, including the embedded Watch target. The broad isolated gate ran both configured passes at 3,111 XCTest cases / 49 skipped / 11 assertion failures, exactly the established 9-name baseline; all 366 Swift Testing cases passed in each pass. `git diff --check` passed after this record update.
- Follow-up / risk — refreshed physical procedure: on macOS, connect/select RANE ONE MKII with channel assign fully left; Advanced → Mixer & Hot Cue Mapping → apply mapping + load AHHH; open live Practice Copy or short Capture; allow at least three hands-off forward revolutions; catch the platter and perform a short pull, a different-length push, then another pull/quick reversal. The caught gesture must restart from its own local baseline; 0.10–0.20-revolution scratches should occupy roughly 40–80% of the lane; a quarter-turn or larger reaches the rail by design; signed slopes/timing/relative sub-quarter excursions must match the hands; completed strokes must not move. On iOS, separately move backward through cue and forward past sample end: audible AHHH and the cyan waveform/playhead must remain synchronized, show `BEFORE START` / `PAST END`, and never wrap. Save and compare Practice/Capture, Review, Take Detail, and camera overlay for the same stroke ordering, direction, timing, and relative sub-quarter excursion. Camera geometry/orientation and viewport scrolling may differ intentionally. The new zoom has not yet received this physical approval, so the slice is not yet safe to commit. Nothing staged, committed, or pushed.

## 2026-08-29 (physical iOS take evidence, reachable Watch control, corrected RANE meter route, and audible macOS AHHH proof)
- Selected task: repair Karl's physical reports that the RANE left meter lit for right-deck AHHH, crossfader cuts were absent from the saved/exported take, Watch Start still required another phone tap, and macOS notation could appear without audibly proving the real AHHH.
- Root cause: iOS observed the crossfader but did not append its raw MIDI packets to the active take; the scratch stem used an anonymous UUID rather than the sidecar/movie basename and therefore was not recoverable/exportable as that take. Watch UI conflated paired with live-reachable and the phone did not persist the control request/reply in the sidecar. The earlier 13/14 RANE output assumption was disproved by the physical left-meter result; DVS input 3/4 does not reserve output 3/4 because Core Audio input/output channel namespaces are independent. macOS `loadPlatterTestSample()` correctly suppressed automatic sound, but no separate explicit audible control existed, while notation remained independently available from motion/timing data.
- Files changed in this slice: `ScratchLab/Audio/iOS/IOScratchPlaybackEngine.swift`; `ScratchLab/MIDI/iOSMIDIManager.swift`; `ScratchLab/Services/CompanionCameraBroadcaster.swift`; `ScratchLab/Views/CompanionCameraView.swift`; `ScratchLabWatch/WatchCaptureView.swift`; `ScratchLabDesktop/Services/MacCaptureEngine.swift`; `ScratchLabDesktop/Views/MacAnalyzerView.swift`; `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`; `ScratchLabDesktopTests/PlatterTestSampleLoadTests.swift`; and workflow records. Existing unrelated dirty hunks were preserved.
- Implemented: RANE ScratchLab stereo now maps to output indices 2/3 (physical outputs 3/4), independently of DVS input 3/4. Each take now resets/records raw crossfader MIDI, derives canonical cuts, persists detected notation, writes exact Watch request/reply evidence, and renames/attaches the scratch WAV to the matching take basename before Review/export. Watch Start is enabled only while the phone is live-reachable and explains that phone Capture/System Check must be ready. macOS now retains silent mapping/load behavior and offers a separate `Test AHHH audio` button; each press unloads/reloads to reset the once-per-load diagnostic guard, plays a short excerpt of exact `dvs_ahhh`, reports success/failure truthfully, and leaves the sample armed for CC6 platter movement.
- Verification: generic iOS Simulator and signed physical iPhone/embedded-Watch builds succeeded; the signed app installed on iPhone `K`, with launch blocked only by the locked handset. Focused iOS route/fader/export/same-basename recovery/Watch contracts passed twice, and capture fixtures passed 47/47. `PlatterTestSampleLoadTests` passed 2/2 in each configured run; an isolated signed macOS build succeeded and launched from `build/CodexProducts-macos-ahhh-final/Debug/ScratchLab.app` as PID 20154. Source and bundled `VirtualPlatter/ahhh.wav` share SHA-256 `4c932a6fd6b26af317eaa7850ca8e11626c5a41e8d382561dcfac81e95949791`. The earlier broad gate retained the established dirty-WIP deleted-resource/UI/MIDI/DVS/profile failures after reaching its 366-test plan. The final `scripts/build.sh all` rerun passed 47/47 fixtures, then exited 65 after the known missing Coach Demo tests when the sandboxed runner lost its IDE helper connection while entering `testExportValidationFailsWhenCanonicalManifestContractDrifts`; that item did not complete and is not evidence of a new assertion regression. The focused tests stayed green.
- Follow-up / risk: field-confirm right-deck meter/output 3/4, then capture a real crossfader cut and verify Review/export fader evidence. Watch control requires Transfer Connected plus the foreground ready Capture screen; background camera autostart is intentionally not claimed. In the open macOS build, use Advanced → Mixer & Hot Cue Mapping → `Test AHHH audio`; `Apply mapping + load AHHH` remains silent until right-platter movement. Nothing committed or pushed.

## 2026-08-29 (real AHHH sample-position waveform and renderer-synchronized playhead)
- Selected task: design in Figma, then implement, the missing blown-up AHHH waveform/playhead view for active iPhone/iPad landscape Practice and Capture so the user can see sample START/MID/END and recognize travel before cue or beyond the sample.
- Root cause: the existing live notation described platter direction and fader state, but there was no visual representation of the decoded AHHH buffer and no UI-safe position snapshot from the audio renderer. The renderer intentionally loops its internal read index, so exposing only that wrapped index would also have misleadingly shown a backward move before cue at the far end of the sample.
- Files changed in this slice: `ScratchLab/Audio/iOS/IOScratchRenderer.swift`; `ScratchLab/Audio/iOS/IOScratchPlaybackEngine.swift`; `ScratchLab/Views/ScratchMotionLane.swift`; `ScratchLab/Views/PracticeModeView.swift`; `ScratchLab/Views/CompanionCameraView.swift`; `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`; Figma file `AgrnQXwRvkAKlORTQ2U25z`; and the workflow records. Existing unrelated dirty hunks were preserved.
- Figma implementation: created `SamplePositionWaveform` component set `457:3817` with Unloaded `457:266`, Ready `457:281`, and Active `457:296` variants. It uses the exact 1.047414966-second PCM envelope, transparent presentation, START/MID/END labels, separate cue and playhead markers, and responsive phone/pad constraints. One instance was placed in each of eight current landscape states: Prototype live, Waiting, Paused, iPhone Practice Copy, both current iPhone Capture Recording frames, iPad Practice Copy, and iPad Capture Recording. Transport/header collisions were removed; final placement audit reported zero invalid placements.
- Production implementation: `IOScratchRenderer` now returns exact sample-load metadata and publishes the smoothed audible unwrapped frame position through an atomic snapshot without touching the real-time callback with observation/UI work. The unwrapped coordinate preserves negative travel before cue and positive travel beyond the sample rather than visually wrapping. `IOScratchPlaybackEngine` builds a 128-bin peak/RMS envelope from the actual decoded cue-trimmed sample and classifies cue/start/middle/end/before/past positions. `SamplePositionWaveformView` renders the transparent 30 fps waveform strip with cyan live progress/playhead and amber bounds warnings. It is wired to the bottom of active landscape Practice, Capture workspace, and full-screen camera monitor while the existing notation clears the strip.
- Verification: focused `testIOSLandscapeShowsRealAHHHSampleWaveformAndRendererPlayhead` passed twice; `python3 scripts/test_capture_pipeline.py` passed 47/47; generic iOS device build succeeded after one local optional-binding compile correction; standalone macOS and unsigned watchOS builds succeeded; a fresh signed iPhone/watch build succeeded, installed, and launched on physical iPhone `K`; `git diff --check` passed. Required `scripts/build.sh all` reached the broad 366-test desktop plan and exited 65 with 10 established dirty-WIP issue groups / 27 listed failure occurrences from deleted demo/reference resources and unrelated UI/MIDI/DVS/profile tests. The new waveform contract passed throughout and no failure referenced this slice.
- Follow-up / risk: perform one physical RANE eyes-and-ears smoke to confirm playhead/audible agreement at cue and at both travel bounds. START/MID/END plus bounds state helps the user judge sample travel; target notation still defines the requested stroke size, so this UI must not be described as an independent score. Nothing committed or pushed.

## 2026-08-29 (complete Figma notation audit plus Save/Watch/RANE production alignment)
- Selected task: recheck every ScratchLab notation presentation in Figma and production after Karl identified stale Prototype A and iPhone Entry + Practice notation, a flat/backed macOS Copy graph, false click symbols, and iPad live-layout collisions; also finish the reported iOS Save Take, Watch Start Take, and RANE output defects in the same currently installed products.
- Root cause: Figma contained multiple local copies of the old short demo and 89 BPM labels in addition to canonical instances, so updating one component did not update all frames. Turnaround anchor squares were visually indistinguishable from fader clicks, opaque cards reduced camera/overlay usefulness, and some expanded traces intersected transport controls. In code, iOS could miss the asynchronous finalized-summary publication and remain Saving after the movie completed; a stop pressed during recorder startup was discarded; Watch only changed its local recorder instead of requesting the phone's authoritative capture state; and RANE playback was mapped to destination indices 2/3 (physical 3/4), conflicting with the intended DVS pair.
- Files changed in this slice: `ScratchLab/Services/CompanionCameraBroadcaster.swift`; `ScratchLab/Views/CompanionCameraView.swift`; `ScratchLab/Models/CaptureReliability.swift`; `ScratchLab/Services/WatchMotionCaptureStore.swift`; `ScratchLabWatch/Services/WatchMotionRecorder.swift`; `ScratchLabWatch/WatchCaptureView.swift`; `ScratchLab/Audio/iOS/IOScratchPlaybackEngine.swift`; `ScratchLabDesktop/Views/MacAnalyzerView.swift`; `ScratchLab/Views/Notation/ScratchNotationPanel.swift`; `ScratchLab/Views/ScratchMotionLane.swift`; `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`; Figma file `AgrnQXwRvkAKlORTQ2U25z`; and the workflow records. Existing unrelated dirty hunks were preserved.
- Figma implementation/audit: updated 44 direct target/performance surfaces, five canonical variants, 17 component instances, and macOS overlay/preview surfaces. The target is consistently the verified BBB 79 BPM, 32-movement/16-cycle demo; performance traces are shorter independent examples rather than fake matches. Removed 55 click-like marker squares plus all decorative turnaround anchors, retained the open-fader rails, and removed notation-canvas fill/stroke/effects. Prototype A stale 89 BPM metadata was corrected. iPhone landscape performance traces and iPad live controls were repositioned to remove collisions. The macOS Copy overlay is 280 points tall and transparent. Final structural audit: 44 direct surfaces, zero failures, zero stale demo metadata. Final overlap audit: zero notation/control intersections. Entry Ready/Copy, Prototype portrait/landscape live, iPad Practice, and macOS Copy were visually inspected.
- Production implementation: canonical SwiftUI notation panels and motion lanes are transparent; macOS Copy allocates a 220–300-point overlay without a dark backing. `CompanionCameraBroadcaster.endRecording(onFinalized:)` now completes from the exact recorder-finalization event, handles a stop requested during startup, and publishes/drains completion only after its matching summary; `CompanionCameraView` drives Review from that callback with explicit take identity. Watch commands use a shared `phone_capture_control_command_v1` payload and the phone's real Start/Stop state machine with truthful replies. RANE routes ScratchLab stereo to destination indices 12/13 (physical 13/14); `DVSHardwareProfile` intentionally remains on indices 2/3 (physical 3/4).
- Verification: focused source contracts passed 9/9; capture fixtures passed 47/47; fresh isolated macOS and iOS Simulator builds succeeded with the embedded Watch app; a signed physical-device build succeeded, installed, and launched on iPhone `K`; the rebuilt isolated macOS app launched successfully. The direct Watch install failed outside the app with CoreDevice 4000 / Bluetooth remote pairing 1035, so the embedded companion must sync via the paired phone once reachable. `scripts/build.sh all` ran fixtures and the broad macOS plans, then exited 65 with the established dirty-WIP baseline (missing/deleted demo/reference assets plus unrelated UI/profile/DVS/demo assertions); the new focused tests stayed green. No shared `build.db` lock recurred. `git diff --check` passed before and after the workflow update.
- Follow-up / risk: on hardware, record roughly ten seconds and verify Save freezes the timer and reaches Review/export; with Capture open, start and stop from Watch and verify the phone follows once; connect RANE and confirm ScratchLab playback appears on outputs 13/14 while DVS stays on 3/4; visually approve the BBB target/performance overlay on the physical phone/iPad and macOS Copy. Nothing committed or pushed.

## 2026-08-29 (iOS Save Take completion and immersive capture chrome repair)
- Selected task: fix the physical-iPhone Capture view where controls overlapped and Save Take remained on `Saving...` while the timer continued instead of reaching Review/export.
- Root cause: device-container evidence showed the reported take itself was healthy—a completed sidecar and 10.4-second HEVC/AAC movie written between 23:25:44 and 23:25:54. The UI observed only a changing summary ID and then awaited optional `AVAsset` audio inspection before advancing the state machine, so a missed/slow callback held `.saving`; elapsed time continued from `recordingStartedAt`. Landscape simultaneously rendered native navigation chrome and its own edge-to-edge capture controls without reserving safe-area/control widths. A bundle audit also found two different AHHH assets, with the stale flattened copy eligible to win Hot Cue lookup.
- Files changed in this slice: `ScratchLab/Views/CompanionCameraView.swift`; `ScratchLab/Services/CompanionCameraBroadcaster.swift`; `ScratchLab/Models/Samples/ScratchSampleResolver.swift`; `ScratchLab/Audio/SampleManager.swift`; `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`; `TASKS.md`; `DEV_LOG.md`; `AI_HANDOFF.md`; and `AI_HANDOFF/next_prompt.md`. Pre-existing dirty hunks were preserved.
- Implemented: freeze the active take's stop timestamp before entering Saving; observe full finalized-summary publications and match them to the active session/take; enter Review immediately after the movie callback, with audio-track inspection updating Review asynchronously; clear stale summaries at recording start; surface a stop request made while the recorder is inactive; and cap Saving with a 12-second watchdog, one final stop retry, a three-second callback grace period, and an explicit preserved-staging recovery. Immersive landscape hides the duplicate native toolbar and supplies one safe-area-aware row containing back, bounded take summary, optional Recheck, bounded primary action, and hardware settings; metrics no longer wrap across controls and the notation workspace clears both control rows. `ahhh` and `dvs_ahhh` now resolve to the approved `VirtualPlatter/ahhh.wav` before any flattened resource, and Sample Manager uses the same approved URL.
- Verification: generic iOS Simulator build succeeded; isolated macOS test build succeeded; focused capture regressions passed 3/3; capture-pipeline fixtures passed 47/47 twice; signed physical iPhone/watch build succeeded and was installed/launched on iPhone `K` (iPhone 16 Pro Max, iOS 27.0), bundle `com.machelpnz.scratchlab`. Approved AHHH SHA-256 is `4c932a6fd6b26af317eaa7850ca8e11626c5a41e8d382561dcfac81e95949791`. `scripts/build.sh all` passed fixtures and compiled the desktop test host, but the existing broad test runner stopped returning after starting `AutoCutVisualPlaybackTests`; it was interrupted after 92 seconds to prevent a lingering shared-build lock. No relevant assertion failed before interruption.
- Follow-up / risk: perform one fresh physical capture, press Save Take, verify the displayed duration freezes, confirm automatic Review, then Keep/Export. The previously reported 10.4-second completed take remains preserved in `Documents/CompanionCaptures`; this slice does not add historical-take recovery UI. Nothing committed or pushed.

## 2026-08-29 (current iOS/macOS builds deployed for physical verification)
- Selected task: build and run the completed landscape/HC1 changes on Karl's connected iPhone and launch the completed Watch-notation repair on macOS.
- Root cause: the source fixes were complete, but Karl's phone and the running desktop app could still be using older build products. The prior shared Xcode DerivedData also had a locked `build.db`, so deployment needed fresh isolated build roots.
- Files changed in this deployment slice: `DEV_LOG.md`, `AI_HANDOFF.md`, and `AI_HANDOFF/next_prompt.md` only. No Swift, resource, project, or test file changed.
- Implemented: produced a signed Debug iPhone build for iPhone `K` (iPhone 16 Pro Max, iOS 27.0), installed bundle `com.machelpnz.scratchlab`, and launched it successfully. Produced a fresh arm64 Debug macOS build in a separate DerivedData/OBJROOT/SYMROOT and launched that exact product as a new instance without terminating Karl's existing Xcode-run app.
- Verification: both platform builds succeeded. The installed iPhone bundle's `VirtualPlatter/ahhh.wav` SHA-256 is `4c932a6fd6b26af317eaa7850ca8e11626c5a41e8d382561dcfac81e95949791`, exactly matching the approved 1.047415-second source, and its deep code signature verified. The macOS bundle's `CoachDemoAudio/baby_noBeat.wav` SHA-256 is `220a2d74568b91d580ab5945ad77d9f0acc74a19d5e839dd2ae8e01c3dbee74f`, exactly matching the current 16.048-second BBB source; the isolated app process remained alive after launch. No `build.db` or `.priors` error recurred.
- Follow-up / risk: Karl must visually approve the landscape Practice/Capture views on the physical device and perform the RANE audio smoke. `Load AHHH` or HC1 arms the sample; moving the right platter produces audio. The macOS Watch demo still needs a subjective eyes-and-ears alignment check. Nothing committed or pushed.

## 2026-08-29 (full-screen iPhone/iPad landscape notation and functional HC1 AHHH mapping)
- Selected task: fix all iPhone/iPad landscape notation views so the active notation fills the screen, repair the compressed landscape Home/System Check layouts, and make Advanced HC1 mapping/assignment/playback operational.
- Root cause: the active Practice and Capture overlays were capped at 144–184 points rather than participating in the landscape container's vertical layout. The compact Home surface used the same hero-plus-three-column layout as wider devices, allowing cards to collapse below readable widths. Advanced MIDI rendered hard-coded/read-only values and had no access to the live source, learning coordinator, dispatcher, or platter playback engine. Finally, iOS supplied its private `TransportState` to `HotCueTriggerResolver`, so an HC1 message was discarded when Serato—not ScratchLab—owned deck transport.
- Files changed in this slice: `ScratchLab/Views/MainMenuView.swift`; `ScratchLab/Views/PracticeModeView.swift`; `ScratchLab/Views/CompanionCameraView.swift`; `ScratchLab/MIDI/iOSMIDIManager.swift`; `ScratchLab/MIDI/iOSMIDILearnCoordinator.swift`; `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`; `ScratchLabDesktopTests/MIDIFaderCurveTests.swift`; `TASKS.md`; `DEV_LOG.md`; `AI_HANDOFF.md`; and `AI_HANDOFF/next_prompt.md`. Existing unrelated dirty hunks were preserved.
- Figma first: in file `AgrnQXwRvkAKlORTQ2U25z`, expanded all active iPhone landscape HUDs; updated Advanced frame `416:4202` with real MIDI-source, AHHH assignment/load, and HC1 learn actions; expanded iPad Capture HUD `360:71`; and added the full-screen iPad Practice HUD `436:2421` in active frame `281:969`. Existing components/tokens were reused.
- Implemented: Practice's live target/performance overlay and both Capture landscape camera surfaces use the full safe-area workspace on phone and pad, with each lane sharing the available height. Compact Home now uses fixed minimum tile widths in a horizontal workspace carousel and reserves bottom-tab clearance. Compact System Check details wrap to two lines. Advanced observes the real iOS MIDI and playback state, supports source selection/refresh, verified RANE mapping, HC1 learn/cancel/relearn, `dvs_ahhh` assignment, and explicit AHHH loading. A newly learned RANE HC1 automatically receives its default sample. HC1 resolves without ScratchLab's transport gate because external Serato transport is independent; the old resolver overload remains tested for flows that do require gating.
- Verification: isolated generic iOS Simulator build succeeded with the embedded watch app; `HotCueTriggerResolverTests` passed 3/3; `python3 scripts/test_capture_pipeline.py` passed 47/47; the new source-contract tests compiled; and `git diff --check` passed. The sandboxed macOS host hung when a source-contract test read the project under `Downloads`; that selected run was interrupted, while the pure-model suite then ran green from the same build products. `scripts/build.sh` was not rerun against Xcode's shared build root because the user had already hit a locked `build.db`; equivalent verification used isolated DerivedData/OBJROOT/SYMROOT paths.
- Follow-up / risk: physical iPad/iPhone landscape visual smoke is still required because no iPad Simulator runtime is installed. For audio, `Load AHHH` arms the local platter sample; HC1 arms it and actual sound begins when the right platter moves. Keep the RANE channel-assign switch fully left so normal Hot Cue pads transmit MIDI. Nothing committed or pushed.

## 2026-08-29 (macOS Watch notation/audio synchronization and false-click-symbol repair)
- Selected task: fix Karl's direct report that the Practice Watch chart showed crossfader clicks during a fader-open Baby Scratch and ran out of time with the demo audio.
- Root cause: `MacAnalyzerView` still supplied `ScratchNotation.babyScratch`, the old 12-stroke / 4.700-second teaching loop, to Watch and reduced the new demo player's sampled time modulo that short duration. Playback itself uses `baby_noBeat.wav` (16.0483125 seconds) paired with `baby_scratch_strokes.json` (32 movements from 2.0 through 14.048311 seconds). Independently, `ScratchPhraseChartView` painted a filled diamond at every forward-to-backward platter reversal even though the chart already uses a diamond for an actual fader-state transition; those decorative marks falsely read as repeated cuts.
- Files changed for this slice: `ScratchLabDesktop/Views/MacAnalyzerView.swift`; `ScratchLabDesktop/Views/ScratchPhraseChartView.swift`; `ScratchLabDesktopTests/LivePerformedNotationTrackerTests.swift`; plus `TASKS.md`, `DEV_LOG.md`, `AI_HANDOFF.md`, and `AI_HANDOFF/next_prompt.md`. Pre-existing dirty hunks in those files were preserved.
- Implemented: Ready/Watch/Listen select the full extracted `ScratchNotation.babyScratchDemo`, with the canonical template as a missing-resource fallback. Copy, result, review, and lesson-complete presentation continue using the canonical short cycle, preserving scoring behavior. Watch's only clock is now `demoModeController.demoPlayer.sampledPlaybackTime()`; it is clamped to the final notated movement so the two-second tail holds instead of wrapping to stroke one. Removed only the decorative platter-reversal diamond pass from target and performed phrase charts. The continuous stroke path, renderer nodes, direction chevrons, OPEN/CLOSED rails, and real state-transition marker remain.
- Verification: an isolated macOS `ScratchLabDesktop` test build succeeded using separate DerivedData, OBJROOT, SYMROOT, and precompiled-module paths, so it did not contend with Xcode's shared `build.db`. `LivePerformedNotationTrackerTests` passed 11/11 twice; `LiveNotationOverlayTests` passed 61/61; exact demo duration / alternating 32-stroke motion / tail-hold checks passed 3/3 twice. `git diff --check` passed. The generated `ScratchLabDesktop-primary.priors` ReadError did not recur.
- Follow-up / risk: listen/watch once in the rebuilt macOS app for subjective approval. The source-data and clock contracts are green; no physical/audio-output visual smoke was performed in this slice. Nothing committed or pushed.

## 2026-08-29 (explicit iOS AHHH loader and correct platter-ready sample restored)
- Selected task: Karl explicitly asked to bring back `Load AHHH` after Hot Cue 1 produced no sample, superseding the earlier temporary Serato-listen-only product decision for this test flow.
- Root cause: production iOS Rane mappings had been normalized to sampleless hot cues and the explicit loader was removed. The correct platter-ready `ScratchLab/Resources/VirtualPlatter/ahhh.wav` was also deleted locally; it is a 1.047415-second responsive sample and must not be replaced by the separate padded 4.466667-second `ScratchSamples/ahhh.wav`.
- Files changed: `ScratchLab/Models/ControllerInput/Registry/MIDILearnedMapping.swift`; `ScratchLab/MIDI/iOSMIDILearnCoordinator.swift`; `ScratchLab/Views/PracticeModeView.swift`; `ScratchLab/Views/CompanionCameraView.swift`; `ScratchLab/Views/MainMenuView.swift`; `ScratchLabDesktopTests/MIDILearnedMappingTests.swift`; `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`; and the workflow documents. The restored `VirtualPlatter/ahhh.wav` exactly equals its tracked Git blob, so it has no content diff.
- Implemented: added explicit `Load AHHH` buttons and observable success/failure status to Practice Controller Setup and Capture Hardware Setup; restored `assignSample` in the iOS coordinator; verified Rane mappings now carry playable ScratchLab sample IDs; selecting an existing Rane mapping repairs missing default sample assignments while preserving learned MIDI addresses, calibration, and custom sample IDs; Advanced reports Hot Cue 1 as ScratchLab AHHH.
- Verification: generic iOS Simulator build succeeded; `MIDILearnedMappingTests` passed 29/29 in both configured executions; the explicit local-AHHH production-flow source contract passed twice; `git diff --check` passed. The restored file matches Git blob `2895220755832dff90d940789533bc7111041fb6`.
- Follow-up / risk: press `Load AHHH`, then Hot Cue 1 with the physical Rane/iPhone route and approve output level/latency. Nothing committed or pushed.

## 2026-08-29 (six missing Xcode binary resources restored)
- Selected task: Karl directly asked to fix the macOS/Xcode missing-file and linker errors for the four Scratch Sample WAVs, desktop app icon, and Coach model.
- Root cause: all six paths remain legitimate explicit `Copy Bundle Resources` inputs (the icon is macOS-only; the samples and Coach model are shared by macOS/iOS), but the tracked binaries had been deleted locally. Xcode therefore failed while copying build inputs and surfaced a secondary nonzero link/build result. No stale project reference was involved.
- Files restored: `ScratchLab/Resources/ScratchSamples/ah_yeah.wav`; `ahhh.wav`; `check_it_out.wav`; `fresh.wav`; `ScratchLabDesktop/AppIcon.icns`; and `ScratchLab/Resources/Coach/Coach.usdz`. Documentation updated: `TASKS.md`, `DEV_LOG.md`, `AI_HANDOFF.md`, and `AI_HANDOFF/next_prompt.md`.
- Implemented: copied the six binaries from `/Users/karlwatson/Downloads/ScratchLab-fix-stable-ios-audio-session-activation` only after verifying each source's Git blob exactly matched the tracked index blob in this checkout. The restoration therefore introduces no binary-content change and requires no `project.pbxproj`, source, target-membership, or build-setting edit. Unrelated deleted resources remain untouched.
- Verification: `scripts/build.sh` passed all 47 capture fixtures, copied all six restored resources, compiled, and linked the macOS test product. Its broad test plan then reported unrelated failures caused by other existing deleted WIP resources, including the missing `chirpflare_noBeat.wav`; the long run was interrupted after the requested build defect was proven fixed. Focused current-source builds then completed successfully for `ScratchLabDesktop` on macOS and `ScratchLab` on the generic iOS Simulator. The six restored paths no longer appear in `git status`; `git diff --check` passed.
- Follow-up / risk: none for the reported six paths. Do not automatically restore or remove project membership for the other deleted WIP resources without an explicit scope decision. Nothing committed or pushed.

## 2026-08-29 (iOS cold-launch black screen fixed)
- Selected task: Karl reported that the iOS app loaded to a black screen, so this direct launch-blocking bug was handled as a focused follow-up to the completed iOS integration slice.
- Root cause: the app process stayed alive and eventually rendered Home; it was not crashing. The static `UILaunchScreen` contained only the dark `LaunchBackground`, and cold initialization took roughly 5–7 seconds. The SwiftUI splash could not cover that delay because it lived inside `ContentView`, downstream of `RootContainerView` constructing audio, MIDI, relay, watch, and upload service objects.
- Files changed: `ScratchLab/ScratchLabApp.swift`; `ScratchLab/Info.plist`; new `ScratchLab/Assets.xcassets/ScratchLabLaunchLogo.imageset/Contents.json` and `ScratchLabLaunchLogo.svg`; `ScratchLabDesktopTests/DemoTimingFoundationTests.swift`; plus `TASKS.md`, `DEV_LOG.md`, `AI_HANDOFF.md`, and `AI_HANDOFF/next_prompt.md`.
- Implemented: the static launch screen now uses a dedicated 240×240 `ScratchLabLaunchLogo` whose intrinsic canvas prevents iOS from scaling the full brand artwork beyond the viewport. A new lightweight `AppLaunchContainerView` renders the application background and existing `SplashView` before constructing `RootContainerView`; duplicate splash state and routing were removed from `ContentView`. Quick Start still presents from `ContentView.onAppear` after the splash. No capture, demo timing, audio routing, MIDI mapping, persistence, or export behavior changed.
- Verification: generic iOS Simulator and unsigned generic iOS device builds succeeded after the final change, including the embedded watchOS app; both built `Info.plist` files contain `LaunchBackground` and `ScratchLabLaunchLogo`. On a fresh iPhone 17 Pro simulator, a cold launch showed the correctly centered compact logo at 0.5 seconds and the real Home screen at 7.5 seconds, with no crash or black-only frame. Capture fixtures passed 47/47. The two new launch source contracts compiled, but their normal macOS test-host run could not execute assertions because the sandbox denied source reads under `/Users/karlwatson/Downloads`; this was an environment permission failure shared by all 12 tests in that selected suite, not an assertion failure. An unsandboxed retry stopped during resource copying on the unrelated pre-existing deleted `ScratchLab/Resources/Coach/Coach.usdz`, before tests. Runtime evidence covers the reported bug. `plutil -lint` and `git diff --check` passed.
- Follow-up / risk: cold-launch once on the physical iPhone/iPad before release. The launch experience is fixed; the underlying heavyweight service initialization still takes several seconds and can be profiled separately if startup time itself must be reduced. Nothing committed or pushed.

## 2026-08-29 (iOS adaptive routing + BBB demo integration + launch repair + Serato companion mapping)
- Selected task: Karl directly asked to wire the new iOS/Figma-derived views to the real app, make the changed BBB demo appear in iOS, repair the oversized launch artwork, and remove the obsolete production AHHH-load step now that Serato is the external deck source. The older capture-movement task remains hardware-validation-blocked and was left unchanged.
- Root cause: four independent integration gaps were visible in the same iOS build. `MainMenuView` contained Home/sidebar work but its body always used compact workspace navigation, defaulted to Practice, omitted Home, and routed Capture/Review through placeholders. `PracticeModeView.configureDemoPlayback()` selected the legacy 42-second reel whenever it existed, masking the newly bundled 16.048-second BBB pair. `UILaunchScreen` named the full `ScratchLabLogo` SVG, which the static launch renderer scaled to fill. Finally, the production controller setup either showed “Apply mapping + load AHHH” / “Reload AHHH” or loaded AHHH silently while applying Hardware Setup, which conflicts with the intended architecture: Serato runs on the Mac/PC; iPhone/iPad is a ScratchLab audio/MIDI/notation companion.
- Files changed in this slice: `ScratchLab/Info.plist`; `ScratchLab/Models/CaptureCore.swift`; `ScratchLab/Views/MainMenuView.swift`; `ScratchLab/Views/PracticeModeView.swift`; `ScratchLab/Views/CompanionCameraView.swift`; `ScratchLab/Models/ControllerInput/Registry/MIDILearnedMapping.swift`; `ScratchLab/MIDI/iOSMIDILearnCoordinator.swift`; `ScratchLabDesktopTests/DemoTimingFoundationTests.swift`; `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`; `ScratchLabDesktopTests/MIDILearnedMappingTests.swift`; `TASKS.md`; `DEV_LOG.md`; `AI_HANDOFF.md`; `AI_HANDOFF/next_prompt.md`. The already-replaced BBB audio/motion resources were preserved and verified in the final bundle.
- Implemented: Home is now the default compact workspace and the compact tab bar exposes all five implemented workspaces. Regular-width landscape iPad uses the existing persistent adaptive sidebar and Home cards. Practice uses `PracticeModeView`; Capture and Review open the real `CompanionCameraView` capture/take-review flow; Advanced uses `AdvancedHubView`; dead placeholders were removed. Baby Demo/Demo + My Motion now use the exact `baby_noBeat.wav` instruction audio, `ScratchNotation.babyScratchDemo`, and fixed `ScratchLabDemoSessionBuilder.demoBPM` (79) before the legacy reel fallback. The launch screen now uses a compact launch-only logo, and a lightweight SwiftUI splash renders before root-service construction. Normal iOS Practice and Hardware Setup no longer expose or invoke AHHH loading. RANE pad addresses remain observable but iOS verified and manually learned mappings assign no ScratchLab sample IDs, preventing hot-cue presses from triggering local deck audio. When iOS loads an older saved mapping it clears only hot-cue sample IDs and preserves the learned MIDI address, calibration, verification, and curve data; the DEBUG Virtual Platter keeps its separate local sample path.
- Verification: `plutil -lint ScratchLab/Info.plist` and `git diff --check` passed. Generic iOS Simulator and unsigned generic iOS device builds succeeded after the final launch follow-up; the builds compiled and embedded the watchOS app. The macOS target built during the focused test runs. `MIDILearnedMappingTests` passed 29/29 twice, including external-Serato listen-only and idempotent saved-mapping migration contracts. The production-no-AHHH source contract passed twice after the migration; adaptive-routing passed twice earlier in the slice. Earlier in the same slice, the BBB demo lane suite passed 11/11 twice and both new main-menu contracts passed twice. The final built iPhoneOS bundle hashes exactly match the sources: audio `220a2d74568b91d580ab5945ad77d9f0acc74a19d5e839dd2ae8e01c3dbee74f`, motion `abb292a028a9ef039ee2b9cf97aa2db92885a3007a3b24985e5d06026ccade40`; the built `Info.plist` launch dictionary contains `UIColorName = LaunchBackground` and `UIImageName = ScratchLabLaunchLogo`. Simulator visual checks covered the fresh branded cold launch, compact Home, iPad portrait, and persistent iPad-landscape sidebar/Home.
- Full gate: `scripts/build.sh all` passed all 47 capture-pipeline fixtures. Its desktop plan then stopped with seven failure occurrences / six unique pre-existing dirty-WIP names: `testGuidedCaptureLandscapeHidesHelperTextDuringPreRoll`, `testGuidedCaptureSystemCheckScrollsOnSmallScreens`, `testLevelSelectSourceUsesSafeAreaAwareScrollableHeaderLayout` (reported twice), `testPracticeSetupDoesNotRenderCoachCard`, `testAssignSampleToHotCueRejectsUnknownSampleID`, and `testRaneOneMkiiDebugPresetHasSetupNoteOtherPresetsDoNot`. The script therefore did not reach its build phase; the standalone successful iOS device/simulator, embedded watchOS, and macOS focused-test builds completed platform compilation coverage.
- Follow-up / risk: listen and validate the input/MIDI route on the physical iPhone/iPad. The saved Figma node (`AgrnQXwRvkAKlORTQ2U25z`, `38:23`) is deleted, so no current design context could be fetched; local Figma-derived SwiftUI surfaces are wired, but a current node-specific link is required before claiming latest-frame pixel parity. NDI/breakout-overlay output and the proposed iOS timecode test side app remain separate unimplemented tasks. Nothing committed or pushed.

## 2026-08-29 (BBB 79 BPM Baby Scratch recording cleaned and bundled as the app demo)
- Selected task: Karl directly asked to fix “CCC” and use it in the app as the demo. No CCC archive was supplied, so this slice explicitly treats that as the latest BBB archive: `session_2026_08_29_bbb_baby_scratch_79_bpm.zip`.
- Root cause: the bundled Baby Scratch demo was an older 42.866-second, 47-stroke performance, while the visualizer could use a separate legacy 76-stroke resource. The approved BBB take also contained an initial setup backward movement and two surplus cycles, so copying it unchanged would not produce the requested clean 16-cycle demo.
- Files changed: `ScratchLab/Resources/CoachDemoAudio/baby_noBeat.wav`; `ScratchLab/Resources/CoachDemoMotion/baby_scratch_strokes.json`; `ScratchLab/Models/CaptureCore.swift`; `ScratchLabDesktop/Views/NotationVisualizerView.swift`; `ScratchLabDesktop/Models/LiveNotationOverlayModel.swift`; `scripts/dataset_processor/coach_demo_manifest.dev.json`; demo assertions/contracts in `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `LivePerformedNotationTrackerTests.swift`, and `LiveNotationOverlayTests.swift`; plus `TASKS.md`, `DEV_LOG.md`, and the handoff files. Existing unrelated dirty hunks were preserved.
- Implemented: cropped the scratch-only Rane recording from source sample 53,582 through 727,901; retained movements 1…32 as exactly 16 alternating forward/backward cycles; applied +4 dB gain; added exactly two seconds of digital silence at the end; and encoded the app resource as 48 kHz, 16-bit PCM stereo. Final duration is 16.048313 s, peak is -7.8 dBFS with no clipping, and SHA-256 is `220a2d74568b91d580ab5945ad77d9f0acc74a19d5e839dd2ae8e01c3dbee74f`. The paired motion JSON has a two-second lead-in, 32 captured strokes from 2.0 through 14.048311 s, and a two-second tail. The visualizer now uses this same resource rather than the unrelated 76-stroke notation.
- Safety: the demo is dry scratch only — no beat, click, calibration signal, compression, or denoise was introduced. No capture thresholds, ML/training logic, session/export schema, Ableton Link behavior, or Release evidence path changed. The source package's trace-replay limitations were not promoted as canonical ground truth.
- Verification: `ffprobe`, `jq`, SHA-256, and bundle-resource checks passed. Thirteen focused demo timing/audio tests passed in both configured runs. `LiveNotationOverlayTests` passed 61/61 after replacing retired 42-second assertions. macOS and iPhone 17 simulator Debug builds succeeded; the iOS app installed and launched. The final `scripts/build.sh` run passed capture fixtures 47/47 and all demo coverage; each configured pass also ran 363 Swift Testing tests successfully. Its two 3,005-test XCTest passes reported 10 failures then the established six-failure UI/profile baseline (49 skipped each): the first pass additionally hit two unrelated MIDI Learn timing tests under suite load, and the second did not. `MIDILearnHangFixTests` then passed 15/15 in each of two isolated runs, confirming the load-dependent flake. The script stopped before platform builds because the baseline XCTest plan returns failure; direct current-source macOS and iOS simulator builds succeeded, as did a direct unsigned watchOS build.
- Remaining risk: physical-device listening is still required. The separate Capture-movement Phase 1/2 item remains open until a new hardware trace exactly replays its live result.

## 2026-08-29 (Take 009 exact-replay diagnosis — trace recorded the wrong builder position; task remains open)
- Selected task: the first unchecked implementation-ready `TASKS.md` item, Capture-movement loss Phase 1/2. Replayed the exported hardware evidence in `session_2026_08_29_platter_baby_scratch_95_bpm.zip` before considering sampling or threshold changes.
- Root cause: Take 009 live diagnostics reported 750 observations, 598 builder samples, and 48 final movements, while exact replay yielded zero. The mismatch began inside `RoutineDetectedNotationBuilder`: live reported 134 starts / 6 extensions, but replay reported 64 starts / 76 extensions. The trace contract claimed `builderPoint` was the actual builder input, but the capture wiring stored the smoothed image-space display point; live notation actually received angular `processed.position`. Brief misses also stored the held display point although live passed nil. Because the real angular inputs are absent, Take 009 cannot be reconstructed exactly after export.
- Files changed by this slice: narrow DEBUG trace wiring and documentation in the already-dirty `ScratchLabDesktop/Services/MacCaptureEngine.swift`; one environment-gated external-hardware replay test plus evidence summary helper in the already-dirty `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`; this entry and the existing unchecked task note in `TASKS.md`. All unrelated dirty hunks were preserved.
- Implemented: trace `builderPoint.x` now stores the actual scalar angular position passed to the builder (`y = 0` reserved), and miss observations store nil when the builder received nil. The new `SCRATCHLAB_MOVEMENT_TRACE` + `SCRATCHLAB_MOVEMENT_DIAGNOSTICS` test decodes a real take, prints comparable live/ideal/exact stage counts, and fails unless exact replay reproduces the live final movement count.
- Safety: DEBUG evidence only. No sampling interval, confidence threshold, movement threshold, capture behavior, notation rule, audio routing, session schema, or Release behavior changed. Ableton Link was not added in this slice because the evidence task remains open and Link requires a separately reviewed SDK/license/entitlement decision.
- Verification: the old Take 009 test fails as intended (exact 0 vs live 48), exposing the non-replayable trace; `MovementTraceDiagnosticsTests` passed 20 executable tests with the external-data test skipped when paths are absent, in both configured test-plan runs (21 discovered per run). `scripts/build.sh` passed the capture-pipeline fixtures 47/47 and all movement-replay tests, then exited 65 in the broad desktop plan on six unrelated existing UI/source-profile checks: guided-capture landscape helper copy, guided-capture small-screen scrolling, Level Select safe-area layout (reported twice), Practice coach-card absence, and the Rane ONE MKII setup-note profile. None exercises the changed DEBUG trace path; the script stopped before later platform builds. A new hardware take from this build is required to close Phase 2.

## 2026-08-29 (Repair clean-HEAD iOS scratch-playback build failures)
- Selected task: make clean HEAD (`9b2b445f`) compile for iOS again. Two failures: `IOScratchPlaybackEngine.swift` called `AVAudioSession.activate`, which is unavailable on iOS even behind the iOS 27 availability branch; and already-committed `iOSMIDIManager` code called crossfader / right-upfader gain methods that were absent from the committed playback engine.
- Files changed by this task: `ScratchLab/Audio/iOS/IOScratchPlaybackEngine.swift` and `ScratchLab/Audio/iOS/IOScratchRenderer.swift` only (2 files, +48/−20). All other pre-existing dirty files and hunks — including this file's and `TASKS.md`'s prior WIP — were preserved.
- Implementation: switched activation to the stable off-main `AVAudioSession.setActive(true)` path (dropping the unavailable iOS 27 `activate` branch and its now-unused `PlaybackError`); restored the crossfader / right-upfader gain surface (`setCrossfaderPosition` / `setRightUpfaderGain`) the MIDI dispatcher calls, folding both into one combined renderer gain value; the renderer applies that value atomically (bit-pattern `Atomic<UInt64>`) with a finite-check clamp to 0…1 and smooths it on the render thread with its existing 3 ms click-suppression ramp (`gainAlpha`), not a second ramp; the latched crossfader × right-upfader gain is re-applied on `prepareRendererForCurrentRoute` so a mid-session gain change survives a renderer rebuild.
- Excluded on purpose (left unstaged, not edited): the platter sample-status / `Load AHHH for platter` additions, the `AudioEngine` concurrency refactor, DVS / timecode, MIDI Learn persistence, `ScratchLab.xcodeproj/project.pbxproj`, `ScratchLab.xcscheme`, and every other unrelated working-tree change.
- Verification: a fresh detached worktree checked out at HEAD with only the two-file staged patch applied built for the iOS Simulator in Debug and Release (`CODE_SIGNING_ALLOWED=NO`); the applied diff contained exactly those two files and none of the excluded work; `git diff --check` passed. No iOS XCTest target exists; no macOS tests were run because both changed files are iOS-only.
- Residual: manual Rane ONE MKII gain / audio smoke with the controller connected to the phone is still outstanding. No broader audio or MIDI task is complete.

## 2026-08-29 (Reconcile docs: ScratchLab remains multiplatform — 2026-08-12 target retirement was reversed; docs only)
- Selected task: reconcile the written record with the standing product decision that ScratchLab remains multiplatform across macOS, iOS/iPadOS, and watchOS. The 2026-08-12 "Retire iOS and watchOS production targets" entry below is stale.
- Findings: the 2026-08-12 retirement (`736e6abb`) was undone in code by `1061dc33` (2026-08-15, "Restore ScratchLab iOS and iPad production target") and `37f81c06` (2026-08-22, "Restore watchOS companion motion capture target"). At repository baseline `baa06fc9` the iOS `ScratchLab` target, the `ScratchLabWatch` target, and `ScratchLab.xcodeproj/xcshareddata/xcschemes/ScratchLab.xcscheme` are all present. `PROFILE.md`, `README.md`, `AI_CONTEXT.md`, `AGENTS.md`, and `docs/**` already describe a multiplatform product.
- Files changed: `TASKS.md` (SUPERSEDED banner on the 2026-08-12 block + new "Product direction — multiplatform (2026-08-29)" record), `AI_HANDOFF.md` (BLOCKER section → RESOLVED), `AI_HANDOFF/next_prompt.md` (obsolete STOP gate removed), and this entry. The historical 2026-08-12 entry below is left intact.
- Scope: docs only. No behavior, code, schema, project-graph, scheme, or test change. The 34 modified + 1 untracked pre-existing WIP files remain local, unstaged, and unmodified; no Swift / `project.pbxproj` / scheme hunk was touched.
- Verification: `git diff` limited to the four docs files; every Swift / `project.pbxproj` / scheme diffstat byte-identical to pre-task state; `git diff --check` clean; nothing staged, committed, pushed, reverted, stashed, or cleaned.

## 2026-08-28 (Docs: Rane ONE MKII channel-assign switch prerequisite for MIDI Learn; uncommitted)
- Selected task: the documentation follow-up recorded in the MIDI Learn entry below — capture the channel-assign switch position as a setup prerequisite in operator-facing guidance. Docs only.
- Files changed: `docs/dj_operator_quickstart.md`, `docs/session_checklist.md`, plus this entry and the matching `TASKS.md` record. No Swift, project, scheme, handoff, or untracked files touched. All pre-existing dirty files and hunks were preserved.
- Documented (from live-verified hardware behavior only): before running MIDI Learn on a Rane ONE MKII, the channel-assign switch above the fader must be fully left; if it is not, the mixer section's MIDI output is silenced and the upfaders and the normal (unshifted) hot-cue pads may not transmit any MIDI, so MIDI Learn cannot see them. Only the crossfader and the SHIFT + pad layer still transmit in that position. Included the verified addresses from the entry below (crossfader `CC 8` ch 16; upfaders `CC 28` ch 1 / ch 2; Hot Cue 1/2 `NoteOn` ch 6 notes 20–21; SHIFT + Hot Cue 1 `NoteOn` ch 16 note 50). No claim was made beyond what that entry records (no "each switch" / other-controller extrapolation).
- Scope: docs only. No behavior, code, schema, or test change.
- Verification: `git diff --check` clean. `scripts/build.sh all` — capture-pipeline fixtures 47/47; the `xcodebuild` ScratchLabDesktop test plan failed on 5 pre-existing methods rooted in the in-flight iOS-capture-camera WIP Swift tree (`testGuidedCaptureLandscapeHidesHelperTextDuringPreRoll`, `testGuidedCaptureSystemCheckScrollsOnSmallScreens`, `testLevelSelectSourceUsesSafeAreaAwareScrollableHeaderLayout`, `testPracticeSetupDoesNotRenderCoachCard`, `testRaneOneMkiiDebugPresetHasSetupNoteOtherPresetsDoNot`), so the script stopped there via its own `set -e` before the iOS/macOS/watch build steps. 0 compile errors; none of the 5 reference the two changed docs; every Swift/pbxproj/scheme diffstat is byte-identical to the pre-task state — this markdown-only task did not cause or alter any of them. Nothing staged, committed, or pushed.

## 2026-08-28 (MIDI Learn stale-value fix + verified Rane ONE MKII MIDI tuples; committed `ce12fe0`)
- Selected task: fix the live macOS CoreMIDI input path so left/right upfaders and hot cues produce usable learned mappings, and stop "Learn Right Upfader" from displaying/capturing a stale prior crossfader event.
- Root cause (from live instrumentation): there was **no CoreMIDI input-path defect**. The Rane ONE MKII's channel-assign switch above the fader was in the wrong position, which silenced the entire mixer section's MIDI output (upfaders + unshifted pads transmitted nothing). With the switch moved fully left, every control transmits and its address matches the existing `RaneOneMKIIVerifiedLearnedMapping` assumptions. The only genuine software bugs found were UI/Learn-flow, not transport.
- **Verified Rane ONE MKII MIDI tuples** (single CoreMIDI source endpoint `uid 1737453891`, parent device `dev_1663037823`; channel-assign switch fully left; 1-indexed channel then raw 0-indexed in parens):
  - Crossfader — `CC 8, ch 16 (raw 15)`, value 0–127
  - Left upfader — `CC 28, ch 1 (raw 0)`, value 0–127
  - Right upfader — `CC 28, ch 2 (raw 1)`, value 0–127
  - Hot Cue 1 — `NoteOn ch 6 (raw 5) note 20 vel 127` / `NoteOff note 20 vel 0`
  - Hot Cue 2 — `NoteOn ch 6 (raw 5) note 21 vel 127`
  - SHIFT + Hot Cue 1 — `NoteOn ch 16 (raw 15) note 50 vel 127` (shift layer only)
  - Platter (not scored here): `CC 6` flood + 14-bit `PitchBend` flood, both on ch 2 (raw 1)
  - With the channel-assign switch NOT fully left: upfaders and unshifted pads emit no MIDI at all; only the crossfader and the SHIFT+pad layer transmit.
- Files changed by this task: `ScratchLabDesktop/Services/MacCaptureEngine.swift` (+54), one hunk in the already-dirty `ScratchLabDesktop/Views/MacAnalyzerView.swift` (−9/+5), appended 6 tests + a helper to the already-dirty `ScratchLabDesktopTests/MIDILearnEngineTests.swift` (+144). All other pre-existing dirty files/hunks preserved; only this task's hunks were staged.
- Implemented: added `@Published midiLearnObservedRawValue`, set only from the event a Learn session actually consumes (`evaluateMIDILearnForCC` / `learnHotCueFromNote`) and cleared on session start / cancel / monitor reset — the Learn panel now shows "move the control now" until a real event lands instead of parsing the persistent `lastMIDICCMessage` string (which held the parked crossfader value). Added a guard in `evaluateMIDILearnForCC`: while learning a continuous control, a CC already mapped to a *different* continuous action (the still-streaming crossfader) is ignored so the session stays open for the real control; hot-cue-via-CC learn is unaffected. `MacAnalyzerView.midiLearnLiveValueText` reads the new state; `parsedLastCCRawValue()` removed.
- Reverted before commit (built for a non-existent transport defect): temporary `[MIDIRawDiag]`/`[MIDIEndpoint]` DEBUG instrumentation, multi-source-endpoint enumeration + connection + per-connection refCon identity token, and widening NoteOn routing to any learned hot-cue channel. `MacCaptureEngine.swift` returned to a clean base + only the two Learn fixes.
- Behavioral safety: no change to the audio engine, the upfader right-authoritative/left-fallback gain contract, MIDI byte parsing, CC6/pitch-bend platter interpretation, captured telemetry, notation, DVS/timecode, calibration, recording/session metadata, persistence schema, Review, or export. No assumed MIDI values were hardcoded.
- Verification (ScratchLabDesktop scheme): `xcodebuild build` → BUILD SUCCEEDED; `build-for-testing` → TEST BUILD SUCCEEDED; `MIDILearnEngineTests` (32, +6 new), `MIDIUserMixerGainTests` (39), `MIDILearnedMappingTests` (27), `MIDIPacketBoundsCrashFixTests` (15) → 113/113 pass, 0 failures. New tests cover: observed value nil until a fresh event, reset on new session, cleared on cancel, hot-cue note-learn surfaces the note number, "Learn Right Upfader" ignores the streaming crossfader CC8 then learns the real CC28, and the guard does not block a non-colliding CC.
- Committed plainly as `ce12fe0` on `feature/ios-capture-camera-ux`, no `Co-Authored-By` trailer; not pushed. Pre-existing dirty work (incl. the in-flight `RaneOneMKIIVerifiedLearnedMapping` and its iOS coordinator wiring) left unstaged and intact.
- Follow-up: the in-flight `RaneOneMKIIVerifiedLearnedMapping` assumptions are now hardware-confirmed correct — worth landing separately. Document the channel-assign switch position as a setup prerequisite in user-facing MIDI setup guidance.

## 2026-08-24 (iOS Rane ONE MKII mapping + platter AHHH + build fix; uncommitted)
- Selected task: fix the reported iOS compile error in `IOScratchPlaybackEngine.swift`, then give iOS the same verified Rane ONE MKII fader/hot-cue mapping and explicit platter-AHHH load control.
- Root causes: Xcode 27 marks `AVAudioSession.activate` unavailable for iOS, so the availability-guarded branch still failed compilation; `IOSMIDILearnCoordinator` persisted mapping edits without notifying `IOSMIDIControllerDispatcher`, newly learned hot cues retained a nil sample assignment, and the dispatcher discarded resolved crossfader/upfader actions.
- Files changed by this task: `ScratchLab/Audio/iOS/IOScratchPlaybackEngine.swift`, `ScratchLab/Audio/iOS/IOScratchRenderer.swift`, `ScratchLab/MIDI/iOSMIDILearnCoordinator.swift`, `ScratchLab/MIDI/iOSMIDIManager.swift`, plus narrow composition/UI additions in the already-dirty `ScratchLab/ScratchLabApp.swift` and `ScratchLab/Views/PracticeModeView.swift`; this entry and `TASKS.md` were also updated. All other pre-existing dirty files/hunks were preserved.
- Implemented: replaced the unavailable API with the established off-main `AVAudioSession.setActive(true)` path; added an explicit Rane mapping for raw ch15 CC8 crossfader, raw ch0/ch1 CC28 upfaders, and raw ch5 notes 20–27 right-deck hot cues; assigned the existing `ahhh`/`fresh`/`ah_yeah`/`check_it_out` bank across cues 1–8; synchronized persisted mapping changes into live dispatch; applied crossfader and right-upfader gain to the existing renderer with a 3 ms click-suppression ramp; added `Load AHHH for platter` and visible success/failure status. The AHHH button arms `dvs_ahhh`; audio remains driven by right-platter CC6 movement. Left upfader remains mapped/persisted but intentionally has no audio effect because no left-deck playback bus exists.
- Behavioral safety: MIDI byte parsing, CC6 ring-counter interpretation, captured platter telemetry, notation generation, Serato/DVS timecode, capture-engine processing, calibration semantics, recording/session metadata, persistence schema, review, and export were not changed. Existing dirty timecode/notation files pre-dated this task and were not edited by it.
- Verification: `xcodebuild ... -destination 'generic/platform=iOS' CODE_SIGNING_ALLOWED=NO build` succeeded; signed `xcodebuild` for physical destination `K` (`00008140-000C21A13A9B001C`) succeeded; `com.machelpnz.scratchlab` installed and launched successfully on that iPhone; `git diff --check` passed. Hardware movement/audio verification remains manual because the Rane must be connected to the phone.

## 2026-08-22 (Companion Camera AVAudioSession main-thread warning fix; uncommitted)
- Selected task: fix the explicit runtime warning at `CompanionCameraBroadcaster.swift:448` that audio-session configuration can make the UI unresponsive when invoked on the main thread while the session is active.
- Files changed: `ScratchLab/Services/CompanionCameraBroadcaster.swift`, plus this log entry and the matching completed task record in `TASKS.md`. All pre-existing dirty files and unrelated hunks were preserved.
- Implemented: dispatch the audio-permission result pipeline onto the broadcaster's existing serial `captureQueue`; `setCategory`, `setPreferredSampleRate`, `setActive`, audio-input discovery, storage validation, and capture startup therefore no longer execute inline on the main thread. Published UI mutations remain dispatched to the main queue.
- Verification: Xcode live diagnostics reported no issues; capture-pipeline fixture tests passed 47/47; Xcode build-for-testing succeeded with zero errors. The required `scripts/build.sh` reached the desktop test plan but exited 65 before compilation because its sandboxed `xcodebuild` could not access DerivedData/CoreSimulator services (`Operation not permitted` / connection invalid), unrelated to this change.
- Follow-up: live iOS Companion Camera startup with an already-active audio session is the final runtime confirmation. Nothing staged, committed, or pushed.

## 2026-08-14 (Baby Scratch comparison alignment fix; uncommitted)
- Selected task: fix ScratchLab's Baby Scratch target-vs-performed comparison so a preparatory setup stroke no longer phase-shifts the entire scored phrase into wrong-way results. Hardware evidence = `session_2026_08_14_notation_baby_scratch_90_bpm.zip` (Take 002: 34 controller movements, `B/F/B/F…`).
- Root cause: `ScratchPerformanceAlignment.compare` anchored solely to absolute beat time and greedily paired target stroke 0 with performed stroke 0. Take 002's leading slow backward setup stroke (0.92 s ≈ 2.6× the phrase median) shifted every matched pair by one stroke, inverting direction for the whole 16-cycle stable phrase → 17 wrong-way; the incomplete final stroke and an over-tiled 17th target cycle added 4 missed / 4 extra. Overall ~40%.
- Fix: added a source-neutral alignment stage (`ScratchPerformanceAlignment.align`) that classifies leading duration-outlier strokes as boundary setup, trailing short strokes as boundary incomplete, and over-tiled target strokes as unscored — never by dropping first/last unconditionally, never by flipping directions to raise the score. `compare` now selects phase jointly with boundary classification, then matches one-to-one monotonically (index pairing for a coherent same-count alternating phrase — drift-tolerant — with a direction-aware window match otherwise). Captured notation is untouched.
- Files changed: `ScratchLab/Models/ScratchPerformanceComparison.swift` (`ScratchAlignment`, boundary/unscored fields + `alignmentExplanation` on the result, `align`, rewritten `compare`/`matchStrokes`, overlay `MarkKind.boundarySetup/boundaryIncomplete`); `ScratchLabDesktop/Views/ScratchPhraseChartView.swift` (dim `verdictBoundaryCol` for boundary marks — retained evidence, never scored); `ScratchLabDesktopTests/ScratchPerformanceComparisonTests.swift` (15 new `ScratchPerformanceAlignment boundaries` tests incl. the real Take 002 fixture). Unrelated dirty capture/render/export/geometry files preserved.
- Take 002 result: 32 matched / 0 missing / 0 extra / 0 wrong-way, boundary setup `[0]`, boundary incomplete `[33]`, unscored target `[32,33]`, 16 forward/back cycles. Timing 13 early / 4 correct / 15 late (performer ~6% slow). `platterTiming` 20.8, `platterCompleteness` 100, `overall` 60.4 (vs 40 before).
- Verification: macOS Debug build ✓, macOS Release build ✓, macOS build-for-testing ✓, `git diff --check` clean. Focused `xcrun xctest` (dylib-symlink recipe): all 15 new boundary tests + all existing `ScratchPerformanceAlignment`/`ScratchComparisonOverlay`/`ScratchPerformanceScore`/`SemanticError` suites pass. Full bundle: 325 swift-testing tests / 43 suites, the only 2 issues are the pre-existing crossfader-lane-label checks; the XCTest failures are the pre-existing bundle-resource / iOS-target-membership failures (Bundle.main = xctest runner), none related to this change.
- Follow-up: nothing staged/committed/pushed. The next candidate is normalizing the timing sub-score so drift is scored against the timing tolerance rather than the tight match window, but that is a scoring change outside this slice's "alignment only" scope.

## 2026-08-11 (macOS design audit slice 2 — consistent Review workflow states; uncommitted)
- Selected task: the next user-approved audit recommendation — make Review loading, empty, and error states structurally consistent and action-oriented without altering capture or analysis behavior.
- Root cause: Review rendered unrelated state treatments simultaneously. Before the first take it showed plain empty text alongside disabled Review actions and Export cards; during recording/finalization the main stage still rendered the full captured-comparison stack; the header always used a success seal even for save failures or incomplete artifacts. The interface did not establish a single current status or next action.
- Files changed: `ScratchLabDesktop/Views/MacAnalyzerView.swift`, plus this `DEV_LOG.md` entry and the matching completed task record in `TASKS.md`. All unrelated dirty DVS drive, Advanced-header, test, resource, and local fixture work was preserved.
- Implemented: added the private view-only `ReviewStagePresentation` mapping over existing `TakeArtifactStatusSnapshot.readiness` and recording state; added a reusable stage status card for empty, recording, finalizing, missing-audio, missing-video, and failed states; added `Open Capture` and `Record another take` recovery actions using the existing tab switch / `prepareRetake()` paths; retained target notation in every state; retained the full completed-take evidence stack for ready and artifact-issue states so incomplete media does not hide useful captured evidence; withheld comparison surfaces while no take exists or media is still processing; hid Review actions and Export cards until `hasRecordedTake`; added an Open Capture CTA to the sidebar empty state; made the Review header status icon/colour reflect empty, recording, finalizing, issue, and save-error states instead of always showing a success seal.
- Behavioral safety: presentation and view routing only. Existing readiness values, `prepareRetake()`, and workspace selection behavior are reused. No changes to capture execution, validation, scoring, notation semantics, MIDI, DVS/timecode, playback flags, audio routing, persistence, export, or data models.
- Verification: `git diff --check` passed. Required `scripts/build.sh` ran capture-pipeline fixtures successfully (47/47) and then exited 65 on the same pre-existing deleted `ScratchLab/Resources/Coach/Coach.usdz`; the deletion was present before this task and was not restored or modified. A disposable detached worktree overlaid with the complete current dirty source state while retaining checked-in resources ran `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' build` successfully: `MacAnalyzerView.swift` compiled and the app reported `BUILD SUCCEEDED`. Existing compiler warnings and locked-device diagnostic noise were unchanged. The disposable worktree was removed afterward.
- Follow-up: the next recommended implementation slice is to normalize card/panel styling and spacing tokens across the primary macOS workspaces, beginning with Review and Capture, without restructuring their underlying flows. Nothing was staged, committed, or pushed.

## 2026-08-11 (macOS design audit slice 1 — Review diagnostic containment; uncommitted)
- Selected task: implement the first small, implementation-ready item from the user-approved macOS design audit — stop developer diagnostics from competing with the learner-facing Review hierarchy while preserving all existing diagnostic capability through progressive disclosure and Advanced placement.
- Root cause: DEBUG-only implementation evidence was rendered at the same visual priority as target notation, captured evidence, coaching, and Review actions. The Review sidebar also included a large raw platter timeline diagnostic card by default. This made a learner-facing workflow read like an internal validation surface even though the underlying functionality is useful and should remain available.
- Files changed: `ScratchLabDesktop/Views/MacAnalyzerView.swift`, plus this `DEV_LOG.md` entry and the matching completed task record in `TASKS.md`. The source file already contained unrelated dirty DVS drive and Advanced-header layout work before this task; those hunks were preserved.
- Implemented: removed `platterTimelineDebugCard` from the default Review sidebar and placed it in Advanced > Overview under its existing `#if DEBUG` gate; removed the visible `DEBUG SENTINEL` badge from Captured evidence; wrapped target diagnostics in a collapsed `Target diagnostics` disclosure; wrapped captured diagnostic chips, sentinel copy, platter replay controls, replay state, and the movement funnel in a collapsed `Developer diagnostics` disclosure. Learner-facing notation, coaching, Review actions, and all diagnostic functions remain present.
- Behavioral safety: view hierarchy and styling only. No changes to scoring, notation semantics, MIDI behavior, DVS/timecode behavior, playback enablement, audio routing, capture behavior, persistence, export, or data models. Playback/notation activation flags were not changed.
- Verification: `git diff --check` passed. `scripts/build.sh` in the real workspace ran the capture-pipeline fixture suite successfully (47/47) and then exited 65 at the pre-existing deleted resource `ScratchLab/Resources/Coach/Coach.usdz`; this deletion was present before the task and was not restored or modified. An isolated verification worktree overlaid with the current dirty source state but retaining checked-in resources compiled `ScratchLabDesktop/Views/MacAnalyzerView.swift` and the full desktop app successfully. Its desktop test plan later exited 65 on Timecode fixture-validation tests because the local-only untracked `ScratchLabDesktopTests/Fixtures/Timecode/` directory was deliberately omitted from that disposable copy; this does not implicate the compiled view change. Existing compiler warnings and locked-device diagnostic noise were unchanged.
- Follow-up: next recommended slice is to make Review loading/empty/error states structurally consistent and action-oriented, without altering analysis or capture behavior. Nothing was staged, committed, or pushed.

## 2026-08-05 (Continuation queue Tasks 7–8 complete — remainder audit + final report; HEAD `46aa1f2`)
- Task 7 (read-only remainder cleanup review): re-audited every remaining dirty/untracked entry (12) at hunk level against HEAD `46aa1f2` and produced a landing-or-archive decision for each — **all ARCHIVE** (keep dirty, delete nothing, no gate/commit): MacCaptureEngine 3 drive hunks (Task 3 drive-activation surface, hardware-blocked); MacAnalyzerView 7 hunks (2 tick drive-activation calls = Task 3; 5 advanced-sidebar/advancedHeaderCard layout = superseded for this queue); TimecodeControlCard 2 hunks (setupNote informational display — never scheduled; whitespace removal); CaptureReliabilityPhase1Tests 5 hunks (structural rearrangement of 3 tests + the pre-existing deletion of the committed sidebar test — HEAD authoritative); TimecodeFixtureValidationTests 23 hunks (playback-path harness + tests 25/26/27 = Task 3 drive surface; `skipIfHardwareFixturesAbsent` + `SCRATCHLAB_TIMECODE_FIXTURE_PATH` removals = working-tree regressions vs committed `105e85c` — never land); TimecodeLiveTapTests 5 hunks (+1749/−1756 wholesale rewrite of committed tests into different behaviors — divergent working-tree version, HEAD authoritative); 4 workflow docs (staged-never); `Fixtures/Timecode/` 17 files + `tmp/` 3 PNGs (local-only evidence, never commit without Karl's redistribution approval). Decisions documented in the progress file; nothing staged/committed/deleted.
- Task 8 (closeout): final report `task8-continuation-final-status-report.md` (3 landed commits with exact hashes `e48e72d`/`4d7e34f`/`46aa1f2`; blocked Tasks 3 + 5 with Karl's verbatim decisions; verification counts — build-for-testing TEST BUILD SUCCEEDED ×3, TASK4-NOTATION-DRIVER 20/20, TASK6-ARCHIVE-INTACT ALL-PASS, exit-pack re-run 11/11 takes, capture-pipeline 47/47 ×3; live playback + live notation OFF; shortest path forward; next tasks). Four workflow docs updated truthfully and left unstaged; real index empty; next single-file continuation queue `DeepClaude-next-30-continuation-queue.md` written externally (Task 1 = Karl's Rane ONE MKII listening session gate). Nothing committed/pushed this task; Task 9 of the next queue not begun. Live playback OFF, live notation OFF, no "sounds like a record" claim.
- Task 5 (notation UI control + live validation): Karl directed skipping the Rane ONE MKII live-notation validation; the user-facing notation enable control + MacAnalyzerView diagnostic display await Karl's design/product decision — none added. `notationRoutingEnabled` stays `false`, no UI control, live notation routing OFF.
- Task 6 candidate: isolated in `task6-archive-worktree` (detached at `4d7e34f`) via exact-anchor surgery (`task6-archive-surgery.py`) from the working-tree full-capture hunks — 1 file `TimecodeFixtureValidationTests.swift` +403/−0: `localHardwareArchiveDirectory` helper (repo-relative via `#filePath`, no absolute local paths), `FixtureCallback` Decodable, MARK 28 archive-intact test (`testLocalFullHardwareFixtureArchiveIsIntactAndGapless` — SHA-256 per catalog + manifest `callbackCount`/`sampleCount` + gapless callback sequence/source-frame/hostTime-uptime continuity), and 6 `testLocalFullHardwareCapturesReport*` tests (CurrentDVSStageDensity, DecoderSubwindowComparison, ReversalConfirmationCandidates, PhaseContinuityAudit, NearZeroStateAudit, DenseEstimatorAudit) inserted into their existing excerpt sections. All 7 env-gated (skip when the gitignored `debug_captures/timecode/hardware_fixtures/20260731` archive is absent); the committed `SCRATCHLAB_TIMECODE_FIXTURE_PATH` override and the excerpt `skipIfHardwareFixturesAbsent` guards are preserved untouched; 0 deleted lines; no playback-path/harness material, no fixture audio.
- First build caught one real surgery bug: my insertion anchor for the dense-estimator test was `#endif // DEBUG` (after the class's closing brace), so it landed outside the class → "cannot find X in scope" compile errors. Fixed by relocating the block inside the class after its doc comment; second build TEST BUILD SUCCEEDED 0 errors (fakehome DerivedData + `CFFIXED_USER_HOME` + `-disable-sandbox`).
- Exit-pack verification re-run: `scripts/task6_archive_intact_driver.swift` (+`_bin`) replicated the archive-intact test against the real local 20260731 archive → **ALL-PASS** (15 captures, every cataloged file's SHA-256 matches, manifest counts, gapless callbacks). The offline-renderer exit-pack driver (`scripts/task12_offline_renderer.swift` recompiled against the current module, run from `/tmp/t6-run` with the VirtualPlatter ahhh sample) re-verified the external 2026-08-04 exit pack → all **11 takes** `unexplainedGap=0.0000s`, `meanEdgeAmp=0.0000`, `maxEdgeAmp=0.0000` with `maxEdgeAmpNoFade` 0.1363–0.7402 (matches the established gap-fade A/B). Capture-pipeline 47/47. `git diff --check` clean; 0 absolute local paths in added lines.
- Staged via `git hash-object -w` + `git update-index --cacheinfo` (cached diff byte-identical to the worktree diff); plain commit `46aa1f2` ("Add env-gated full-capture archive measurement tests") no trailer; pushed `4d7e34f..46aa1f2`; HEAD = origin/main = `46aa1f2`; real index empty.
- Aftermath: remaining dirty/untracked = 11 (4 docs, MacCaptureEngine drive hunks, MacAnalyzerView drive-call + sidebar hunks, TimecodeControlCard setupNote, CaptureReliabilityPhase1Tests, TimecodeFixtureValidationTests working-tree divergence hunks — playback-path tests, skip-guard removals, env-override removal — deliberately NOT landed, TimecodeLiveTapTests, TASKS.md + Fixtures/Timecode and tmp local-only), all preserved unstaged. Workflow docs updated truthfully and left unstaged.
- Queue position: Tasks 1, 2, 4, 6 complete; Task 3 hardware-blocked; Task 5 hardware validation skipped (UI control awaits Karl's design). Tasks 7 (remainder cleanup review) and 8 (final report + continuation) next. Live playback OFF, live notation OFF, no "sounds like a record" claim.
- Karl's directive from the Task 3 gate: Task 3 (drive-activation) stays hardware-blocked — no Rane ONE MKII listening session has happened; proceed with Task 4 only while notation remains default-off; skip Task 5's hardware validation; then Tasks 6–8. Karl then approved Task 4's landing gate ("Guarded notation wiring is isolated and verified. Commit and push it?" → Yes).
- Candidate: isolated in `task4-notation-worktree` (detached at `e48e72d`) via exact-anchor surgery from the working-tree notation hunks (`task4-notation-surgery.py`), 3 files +412/−3. Contents: `MacCaptureEngine` `forwardTimecodeNotationWindow(pipeline:minConfidence:)` (guarded `notationRoutingEnabled` + `isRoutineRecording` + `#if DEBUG`; calls committed `TimecodeNotationTrustGate.evaluate` with `validationRequired: true` / `validationOverride: false`, appends only `.trusted` windows to the accumulator), notation state block (`notationRoutingEnabled = false` with the "no control sets it yet" contract, `timecodeNotationAccumulator`/`timecodeNotationLock`/`timecodeNotationDiagnosticsStorage` + lock-protected getter/`lastDrainedTimecodeNotationTimeline`), reset at routine-record start, drain via `finalizedTimeline()` at finish, `TimecodeNotationCapturePrecedence.resolvedMovementEvents` fusion (Release `#else` path byte-identical to before) + `withTimecodeLiveMovementProvenance()` when enabled and drained, `[dvsNotation]`/`[dvsNotationDecisionCounts]` diagnostic lines; `MacAnalyzerView` 3 notation diagnostic lines + the tick's `forwardTimecodeNotationWindow` call (working-tree comment adapted — the drive call it referenced is not part of this candidate; documented deviation); `TimecodeNotationRoutingTests` 11 changes (rename to `testTrustGateNeverReadsOrTouchesPlaybackBridgeState` + `bridge.lastDecision == .notEvaluated` assertion; 5 fixture end-to-end + 5 full-chain tests, all routed through the single XCTSkip-guarded `replayTrustedAccumulation` reading `Fixtures/Timecode/` via `#filePath`). All 7 notation blocks byte-identical to the working-tree intent; test file byte-identical to the working tree; 0 added drive/playback/sidebar code symbols (only doc-comment mentions).
- Verification: build-for-testing TEST BUILD SUCCEEDED 0 errors (fresh fakehome DerivedData + `CFFIXED_USER_HOME` + `OTHER_SWIFT_FLAGS=-disable-sandbox`); deterministic real-module driver `scripts/task4_notation_driver.swift` (+`_bin`) **ALL-PASS 20/20** — trust-gate bridge independence (`bridge.playbackDriveEnabled == false`, `bridge.lastDecision == .notEvaluated` after evaluation, both enabled and disabled paths), default-off `.notationRoutingDisabled` short-circuit, precedence disabled (camera events unchanged) and enabled (reproduces trusted events, camera never survives), accumulator append/finalize/nil-on-empty, adapter `timecode_live` source, provenance `detectionSources` resolution, diagnostics counts/description. Capture-pipeline 47/47 (baseline). XCTest host execution remains environment-blocked (established; not relabeled). `git diff --check` clean; staged via `git hash-object -w` + `git update-index --cacheinfo` (cached diff byte-identical); plain commit `4d7e34f` no trailer; pushed `e48e72d..4d7e34f`; HEAD = origin/main = `4d7e34f`; real index empty.
- Aftermath: `TimecodeNotationRoutingTests.swift` fully clean. Remaining dirty/untracked = 12 (4 docs, MacCaptureEngine drive hunks, MacAnalyzerView drive-call + sidebar hunks, TimecodeControlCard setupNote, CaptureReliabilityPhase1Tests, TimecodeFixtureValidationTests, TimecodeLiveTapTests, TASKS.md + Fixtures/Timecode and tmp local-only), all preserved unstaged. Workflow docs updated truthfully and left unstaged.
- Queue position: Tasks 1–4 complete; Task 3 hardware-blocked; Task 5 hardware validation skipped per Karl's directive (the notation UI control itself awaits Karl's design/product decision); Tasks 6–8 next. `notationRoutingEnabled = false`; live playback OFF; live notation routing OFF; no "sounds like a record" claim.
- Karl approved Task 1's gate ("Land the still-disabled playback scaffold (no activation)?" → Yes) and Task 2's landing gate ("Playback scaffold is isolated and verified. Commit and push it?" → Yes). Landed the reviewed `task17-playback-candidate.patch` exactly: staged via `git hash-object -w` + `git update-index --cacheinfo` from `task17-playback-worktree` (5 files, +550/−81); cached diff rechecked byte-identical to the reviewed patch and `git diff --cached --check` clean; macOS Debug `build-for-testing` with the established fakehome DerivedData + `CFFIXED_USER_HOME` + `OTHER_SWIFT_FLAGS=-disable-sandbox` invocation (fresh DerivedData dir so the compile was genuine) → **TEST BUILD SUCCEEDED**, 0 errors; `python3 scripts/test_capture_pipeline.py` → **47/47 OK** (exact baseline). Committed plainly as `e48e72d` ("Add still-disabled playback scheduling scaffold"), no `Co-Authored-By` trailer; pushed `09fdc9e..e48e72d`; HEAD = origin/main = `e48e72d`; real index empty.
- Content: `MacAnalyzerView` (DVSControlProcessingLoop + tick minus all drive/notation calls + `enqueueLiveStereoBuffer` ingestion + onDisappear cancel + crash-fix family + schedule/bridge-gate diagnostics), `TimecodeInputTap` (`@Published` removal + publishChange), `TimecodeControlCard` (Gate/Gate-counts lines only), `TimecodeLiveTapTests` (8 added tests), `CaptureReliabilityPhase1Tests` (3 source-slice tests). Still-disabled: 0 added drive/notation enablement lines (symbol scan), `playbackDriveEnabled = false` ×2 and `dvsPlaybackDriveActive = false` defaults untouched; no sample-selection change; no `forwardTimecodeDrive` body.
- Aftermath: `ScratchLab/Models/TimecodeInputTap.swift` fully clean (its only dirty hunk was the scaffold's); remaining dirty/untracked set = 13 entries, all preserved unstaged (4 docs, MacCaptureEngine, MacAnalyzerView, TimecodeControlCard, CaptureReliabilityPhase1Tests, TimecodeFixtureValidationTests, TimecodeLiveTapTests, TimecodeNotationRoutingTests, TASKS.md + Fixtures/Timecode and tmp local-only). Workflow docs updated truthfully and left unstaged.
- Queue position: continuation queue Tasks 1–2 complete. Task 3 (drive-activation surface: `forwardTimecodeDrive` grain scheduling + lock-backed `dvsPlaybackDriveActive` + `dvs_ahhh` + `DVSDriveDiagnosticState` + 3 skip-guarded fixture tests + drive diagnostics UI) is **hardware-gated** — only after Karl's listening session on the Rane ONE MKII approves record-like playback direction. Task 4 (notation wiring, still default-off) follows. Live playback OFF, live notation OFF, no "sounds like a record" claim.
- All 20 tasks finished. Landed this queue: `34dd479` (Rane pair-3/4 pinning), `105e85c` (portable fixture-validation tests), `d45222c` (logger trace fields), `09fdc9e` (multichannel PCM hardening). Verified but not landed: the still-disabled playback scaffold (`task17-playback-candidate.patch`) — 0 drive/notation enablement lines, live playback and live notation remain OFF pending Karl's architecture/listening review. Read-only audits for Tasks 1/2/6/11/16/19; remainder map + obsolete/superseded lists recorded; Fixtures/Timecode + tmp/ marked local-only. Final report `task20-next20-final-status-report.md`; continuation queue `DeepClaude-next-20-continuation-queue.md`.
- Verification totals: build-for-testing TEST BUILD SUCCEEDED ×7 (0 errors); deterministic drivers ALL-PASS (pinning, fixture ×2, diagnostics 22/22, scaffold 12/12); TSan 0 warnings; source-assertions 41/41; capture-pipeline 47/47 ×4 (exact baseline). XCTest host execution environment-blocked ×4 attempts (DTDK device error, 0 tests executed; never relabeled). Real index empty; 14 pre-existing dirty/untracked entries preserved; no docs committed.

## 2026-08-04 (Capture/logger diagnostics landed — `d45222c` + `09fdc9e`; DeepClaude next-20 queue Task 15)
- **`d45222c` (Task 12 logger schema + panel wiring):** `DVSLogEntry` gains `rawDecodeTrace`, `calibratedTrace`, `heldDropoutCount`, `longDropoutCount`; `DVSControlVinylPanel.makeLogEntry()` passes them from `pipeline.counters`; `DVSControlVinylAnalyzerTests` fixture helper updated. Storage/export compatibility confirmed: `DVSLogEntry` is `Encodable`-only, the logger is a JSONL append writer, the new keys are additive and nothing decodes entries back — no silent corruption path.
- **`09fdc9e` (Task 13 multichannel PCM hardening):** `RoutineAudioCaptureWriter.pcmFormat(...)` discrete-layout fallback (the direct `AVAudioFormat(…,channels:14,…)` returns nil — the exact Rane 14ch scenario; the fallback builds a `kAudioChannelLayoutTag_DiscreteInOrder|14` layout) + `writeRoutineAudioSampleBufferForTesting` DEBUG hook; `AudioPacket`/`audioPacket(from:)` made internal and hardened (multichannel/non-interleaved/Float gate, bounded `AudioBufferList` allocation sized to `maximumBufferCount`, safe frame downmix). Focused tests: `testMacCaptureEngineAudioPacketSafelyDownmixesNonInterleavedStereo`, `testRoutineAudioCaptureWriterCreatesWAVForRaneMultichannelPCM` (+2 sample-buffer helpers), `testLowLatencyPCMBufferAdapterPreservesStereoAndSmallFrameCount`.
- Verification: separate and combined isolated-worktree `build-for-testing` all TEST BUILD SUCCEEDED (0 errors); XCTest host execution environment-blocked (DTDK device error, 0 executed; not relabeled); deterministic real-code driver against the combined module (`@testable import ScratchLab`): **TASK14-DIAGNOSTICS-DRIVER ALL-PASS 22/22** — DVSLogEntry JSON with the 4 new fields, real `audioPacket` stereo downmix `[0.50,0,0,0]`, 14ch/32-frame WAV write + AVAudioFile read-back (14ch/48 kHz/32 frames), 256-frame `stereoSampleResult(fromPCMBuffer:)` exact samples + pair selection, pcmFormat 14ch semantics. Capture-pipeline 47/47; `git diff --check` clean; forbidden-symbol scan 0. Staged via `git hash-object -w` + `git update-index --cacheinfo` (cached diffs byte-identical to the reviewed patches); plain messages, no trailers; pushed `105e85c..09fdc9e`; local `main` = `origin/main` = `09fdc9e`; index empty; 14 remaining dirty/untracked entries preserved (DVSLiveLogger, DVSControlVinylPanel, DVSControlVinylAnalyzerTests now fully landed/clean).

## 2026-08-04 (Portable fixture-validation tests landed as `105e85c` — DeepClaude next-20 queue Task 10)
- Landed `TimecodeFixtureValidationTests.swift` (+2,917/−764, 1 file): the portable fixture-validation test candidate — 3 skip-guard regressions, excerpt integrity (19/21), decoder/pipeline (20/22/23/24), and the clean-room excerpt audits (density, subwindow, reversal-confirmation, phase-continuity, near-zero, dense-estimator, steady-normal position, pipeline reset). Every fixture-touching test now begins with `skipIfHardwareFixturesAbsent(in: hardwareFixtureDirectory)` (the working tree only guarded 2 of 14 — the other 12 previously would have FAILED when assets are absent; now they skip honestly). `hardwareFixtureDirectory` gained the `SCRATCHLAB_TIMECODE_FIXTURE_PATH` env-var override per the committed `TimecodeFixtureLoader` contract, enabling isolated verification without copying evidence into a worktree.
- Excluded (per Tasks 6/7 boundary): all 4 playback-path tests + `replayThroughProductionPlaybackPath` chain, all 7 full-capture archive tests + `localHardwareArchiveDirectory` + `FixtureCallback`; no WAV/CAF/JSONL asset, no pbxproj change, no local paths, no playback/notation wiring.
- Verification: macOS Debug build-for-testing TEST BUILD SUCCEEDED (0 errors); XCTest host execution environment-blocked (DTDK device error, 0 tests executed; not relabeled); deterministic plain driver (real shared module) ALL-PASS in both passes — fixture-absent: skip-guard contract (absent→skip, malformed→not-a-skip); fixture-present (env var → approved excerpt dir, read-only): catalog 8/8, 15/15 SHA-256 matches, 15/15 Float32/2ch/48 kHz/11,520-frame, 5/5 non-stationary steady excerpts decode with correct direction/rate. capture-pipeline 47/47; `git diff --check` clean; leakage scan 0; privacy/portability review 0 hits. Staged via `git hash-object -w` + `git update-index --cacheinfo` (cached diff byte-identical to the reviewed patch); plain message, no trailer; pushed; local `main` = `origin/main` = `105e85c`; index empty; all other dirty/untracked work preserved (the landed file still carries its excluded hunks — playback-path + full-capture tests — unstaged).

## 2026-08-04 (Rane pair-3/4 pinning landed as `34dd479` — DeepClaude next-20 queue Task 5)
- Landed the channel-pair selection behavior: `TimecodeControlCard.applyPreset` now writes `TimecodeCMSampleBufferAdapter.channelPairSelection = Self.channelPairSelection(for: preset)`, with the pure static mapping `.raneOneMkiiDebug → .pair(startChannel: 2)` and every other preset → `.auto` (both `#if ENABLE_TIMECODE_LIVE_TAP`-guarded). `DVSControlVinylPanel`'s USB channel pair picker now renders the pinned pair when it is absent from the generic `pairOptions`. This preserves the validated Rane ONE MKII DEBUG hardware setup (physical pair 3/4) against Auto-selection instability during stop/reversal, and restores Auto for every non-Rane preset — byte-identical default behavior to the committed adapter default (`storedSelection: ChannelPairSelection = .auto`).
- Staged exactly the 3-path reviewed candidate via `git hash-object -w` + `git update-index --cacheinfo` (cached diff byte-identical to the candidate patch; 54 insertions / 0 deletions); committed `34dd479` with a plain message and no trailer; pushed; local `main` = `origin/main` = `34dd479`; index empty. All other dirty/untracked work preserved byte-for-byte (the 3 landed files still carry their unrelated dirty hunks — `setupNote` wording, Gate diagnostics, logger wiring, cluster-2/3 test hunks — unstaged).
- Verification: macOS Debug build-for-testing TEST BUILD SUCCEEDED (0 errors; full test target compiles, incl. the two new pinning tests and all adapter/control-card regressions). Focused pinning tests attempted in both test-plan configurations — XCTest host execution remains environment-blocked in Reasonix (DTDK device-connection error, 0 tests executed; not relabeled as a pass). Deterministic plain driver linking the real shared module (`@testable import ScratchLab`, `ScratchLab.debug.dylib`): PINNING-DRIVER ALL-PASS — real `TimecodeControlPreset.allCases` = [scratchLabPrototype, genericDVS, raneOneMkiiDebug, manual]; Rane → `.pair(startChannel: 2)`; every non-Rane preset → `.auto`; fresh-process adapter default `.auto`. `python3 scripts/test_capture_pipeline.py` 47/47 (exact baseline); `git diff --check` clean; leakage scan of the cached candidate 41/41 (no sidebar/playback/notation/logger/capture-engine/fixture/Kid-Mode material). `plutil -lint` N/A (no pbxproj change); unsigned iOS build N/A (no shared source touched).
- Note: a `git reset -q` was run once during staging retry (index already empty at that moment — verified no-op: HEAD unchanged, working tree intact, all 17 dirty/untracked entries preserved). Recorded transparently; the queue's `git reset` prohibition is otherwise honored.
- Landed the layout-only sidebar fix: `TimecodeControlCard.swift`'s profile section now renders the three general presets in one compact row (new `profilePresetButton(preset, label: preset.shortLabel)` helper; `lineLimit(1)` + `minimumScaleFactor(0.75)`, equal-width via `frame(maxWidth: .infinity)`) and `Rane ONE MKII (DEBUG)` on its own full-width second row — the long DEBUG label can no longer widen the narrow debug inspector or clip its left edge (root cause: all four long labels in one non-wrapping `.pickerStyle(.segmented)`). Same `selectedPreset = preset` write, so `onChange` → `applyPreset` profile application/validation semantics are unchanged. Capture, DVS processing, playback, notation, and Kid Mode untouched.
- Staged exactly the reviewed candidate via `git apply --cached` (excluded and left dirty/unstaged: `setupNote` wording, `channelPairSelection`/Rane pair pinning, Gate diagnostics); committed `d620937` with a plain message and no trailer; pushed; local `main` = `origin/main` = `d620937`; index empty. All other dirty/untracked work preserved byte-for-byte.
- Verification: macOS Debug build-for-testing TEST BUILD SUCCEEDED (sandbox workaround); `git diff --check` clean (worktree and cached); new source-slice test `testTimecodeControlCardRaneDebugPresetOwnsFullWidthRow` compiles in the test target and its 5 deterministic assertions were verified standalone (ALL-5-PASS). XCTest host execution remains environment-blocked in Reasonix (single attempt — runner never establishes its connection; not relabeled as a pass). Manual visual confirmation at a narrow sidebar remains Karl's final check.

## 2026-08-04 (AUTHORITATIVE QUEUE COMPLETE — Tasks 13-20; final status)
- **Task 13** selected exactly one evidence-backed playback defect: hard PCM cut at scheduling gaps in the offline DVS playback reconstruction (Task 12 measured gap-edge click proxies up to 0.74 on scratch takes; DVS grains are queued unfaded by the committed amplitude-modulation guard, so every gap is a hard cut to silence).
- **Task 14** implemented the pure/offline fix: `ScratchSamplePlaybackController.fadeOutTail`/`fadeInHead` (ramp + clamp identical to `applyEdgeFade`) applied gap-aware in the offline render path (tail out / head in at each gap; contiguous junctions untouched). No live scheduler change.
- **Task 15** offline A/B across all 11 exit-pack takes (real module helpers): gap-edge max amplitude 0.74/0.64/0.49/0.23/0.40/0.30 -> **0.0000 on every take**; per-take gap counts, overlapCount (0), unexplainedGap (0), contiguous-junction value jumps (0.0183-0.0321) all unchanged; capture-pipeline 47/47; deterministic plain driver 14/14 PASS (XCTest host execution environment-blocked in Reasonix — documented, not relabeled).
- **Task 16** landed: Karl approved "commit and push" -> `2606a10` ("Suppress hard-cut clicks at scheduling gaps in offline DVS playback"; 3 files: controller +34 lines, new `DVSOfflineGrainRendererTests.swift` [6 tests], `project.pbxproj` 4 DOGR lines; no trailer; pushed; index empty).
- **Task 17** offline notation replay through the committed primitives (pipeline -> trust gate -> accumulator -> adapter), all 11 takes x2 runs: INVARIANTS PASS (ordering, take-relative timestamps within capture span, forward/backward + timecode_live only, honest rejection at stops/lifts [stationary 0 trusted/1963 rejected, start_stop 1003, needle_lift 1940], zero duplicate (startTime,direction), deterministic). No defect.
- **Task 18** `skipped-no-change` (Task 17 proved no notation defect; no manufactured work).
- **Task 19** consolidated offline-chain audit + dirty-tree map refresh: chain all PASS on currently-landed code; clusters 3 (offline-renderer test) and the estimator fix landed (`2606a10`, `7d9e6d9`); project.pbxproj clean; remaining dirty = sidebar (awaiting Karl), diagnostics (safe), live playback/notation scaffolding (needs approval), fixture validation (safe), docs (never auto-committed), tmp PDFs (never land).
- **Task 20** final status below. Live playback override and live notation routing remain DISABLED; Kid Mode untouched; no "sounds like a record" claim. Hardware evidence: `/Users/karlwatson/Library/Application Support/ScratchLab/DebugCaptures/Timecode` + `ScratchLab-hardware-exit-pack-2026-08-04`. Known full-suite baseline failures unchanged (8 failures / 5 pre-existing methods; documented in prior entries). Authoritative queue complete — no 21st task.
## 2026-08-04 (Gap-aware edge fade landed as `2606a10` — authoritative Task 13-16 playback queue)
- Landed the authoritative playback fix: at each scheduling gap in the OFFLINE DVS playback reconstruction, the preceding grain's tail now fades out and the following grain's head fades in (32-frame linear ramps, clamped to half grain length, injectable) — suppressing the hard-cut clicks measured up to 0.74 on scratch takes. Pure additive helpers (`ScratchSamplePlaybackController.fadeOutTail`/`fadeInHead`, ramp+clamp identical to the committed `applyEdgeFade`); no live scheduler change; live playback remains off.
- Committed/pushed `2606a10` (3 files: controller +34 lines, new `DVSOfflineGrainRendererTests.swift` [6 tests], `project.pbxproj` 4 DOGR lines). Staged exactly the reviewed candidate; no trailer; index empty; pre-existing dirty/untracked preserved.
- Verification: build-for-testing TEST BUILD SUCCEEDED (sandbox workaround); exit-pack A/B via the real module: gap-edge max amplitude 0.74/0.64/0.49/0.23/0.40/0.30 -> 0.0000 on every take; per-take gap counts, overlaps (0), unexplained gaps (0), and contiguous-junction value jumps (0.0183-0.0321) all unchanged; capture-pipeline 47/47; deterministic plain driver 14/14 PASS. XCTest host execution remains environment-blocked (not relabeled).
## 2026-08-04 (Dense estimator plausibility-bound fix committed and pushed as `7d9e6d9`)
- **Landed the Task 13/14 candidate**: `TimecodeSignedRateDenseEstimator.evaluateCrossing` now classifies a crossing whose raw normalized speed would exceed `maximumNormalizedSpeed` as `.unknown` instead of `.available` pinned to the clamp. This is the one implementation the hardware-exit-pack queue chose; it came from measuring the exit pack, not from a review hunch: first-available rates of exactly ±20.0 on 33/36 `start_stop` and 2/3 `needle_lift` dropout-resume edges, raw windows at the noise floor (peaks 0.001–0.005; `LZCR = 0 Hz` on one channel at t=6.469s).
- 5 new deterministic tests in `TimecodeSignedRateDenseEstimatorTests.swift` (over-bound blip; sustained 8× below bound; sustained over-bound; speed below the boundary; dropout-resume edge). Three are **regression-catching** and were verified to FAIL on the pre-fix code and PASS on the fix (over-bound blip, sustained over-bound, dropout-resume edge); the other two (sustained 8× below bound; speed below the boundary) are **preservation** tests that pass on BOTH pre-fix and fix code — the earlier "each verified to FAIL on the pre-fix code" phrasing was corrected. Note: `testSpeedAtBoundaryRemainsAvailable` uses 3.9× against a 4.0× bound, so it verifies below-bound behavior, not exact equality. The committed suite is **22** estimator methods (17 existing + 5 new). Full `build-for-testing` passed (with the sandbox workaround); real test-host EXECUTION of those 22 methods is **environment-blocked in Reasonix** (test runner never establishes its XCTest connection), so build-for-testing + standalone assertion replication are not an XCTest pass. Exact-boundary behavior (`.available` exactly at the bound, `.unknown` immediately over) was proven via a standalone deterministic power-of-two driver (see `ScratchLab-analysis-2026-08-04/scripts/estimator_boundary_proof/`); the exact-boundary XCTest methods are recorded as a follow-up candidate for Codex/local Xcode verification, not committed here.
- Exit-pack rerun with the fix: all ±20 resume clamps eliminated (start_stop resume-edge rates 33×±20 → 14 plausible moderate values, none at the clamp; needle_lift → 0), available counts within −0.3% on the dense/reversal/scratch takes, noSignal unchanged, stabilizer committed reversals identical on all 11 captures (0/0/3/4/5/39/38).
- Commit `7d9e6d9` (`Classify implausible DVS crossing intervals as unknown instead of clamped available`), pushed; local `main` and `origin/main` aligned. Staged exactly 2 files (estimator + its test file), no trailer. Workflow docs updated but not committed.

## 2026-08-04 (Second real Rane capture PASSED — Slice 2 complete; landing candidate constructed and staged, not committed)
- **Second real Rane ONE MKII capture, after the `AVAudioFormat`-reconstruction fix, passed cleanly:**
  ```
  DVS tap heartbeat: route=AVAudioEngine installed=true callbacks=5090 age=6.2ms
  observed=Float32 non-interleaved,14ch,512f@48000Hz
  HAL buffer: bufferFrameSize=512 range=[15...4096] nominalSampleRate=48000.0
  safetyOffset=74 deviceLatency=74
  DVS sink ring: published=5091 dropped=0 rejected=0
  DVS sink observed: 512f@48000Hz,14ch
  Adapter: AVAudioEngine Float32 non-interleaved, 14ch, 512f selected=3/4 recommended=3/4
  Pipeline: usablePrototypeControl, forward, approximately 1.0 u/s, confidence 0.911,
  no silence/clipping/channel-fault drops.
  ```
  `callbacks=5090` (nonzero, growing) proves the sink route is genuinely live — no more AVCapture fallback. Delivered quantum is 512 frames (10.7ms) vs. the old `installTap` floor of 4,800 frames (100ms) — a 9.375× reduction. Zero ring drops/rejections across 5,091 published slots. Adapter correctly selects physical pair 3/4. Pipeline reports a plausible, confident (0.911) forward reading with no signal-health drops. Playback was not exercised by this capture — no claim is made that scratch audio now sounds like a real record.
- **Final verification run after the successful capture:** `python3 scripts/test_capture_pipeline.py` 47/47; unsigned iOS build succeeded; standalone watchOS build succeeded; `scripts/build.sh all` (fresh, full run) — both test-plan configurations reproduced the exact established baseline: 2,395 tests (2,390 + 5 new adapter-fix tests from the prior pass), 19 skipped, 8 failures (0 unexpected, same 5 distinct pre-existing methods as every prior session), zero new failures.
- **Constructed and staged the Slice 2 landing candidate — not committed.** Enumerated the complete transitive closure of `MacCaptureEngine.swift`'s dirty diff against `HEAD` (`1c36107`): ~20 hunks total. Classified each: the pure-types block (before `final class MacCaptureEngine`), the new session/lifecycle/diagnostic properties, the `installTap`→`AVAudioSinkNode` replacement, `handleLowLatencySinkDelivery`/`teardownActions`/`stopLowLatencyTimecodeInput`/the two new diagnostic publishers were all pure Slice 2. `audioDeviceID(forUID:)` showed as a delete+insert "move" pair with byte-identical content — a positional artifact of Slice 2's large insertions, not a real change; left as-is since reverting only one half would have deleted the function. Everything else was pre-existing unrelated dirty work that had nothing to do with Slice 2: `RoutineAudioCaptureWriter`'s `pcmFormat` fallback + a DEBUG test helper; DVS playback-drive-gate threading/diagnostics batching (`dvsPlaybackDriveActive`→lock-protected storage, `dvsDriveDiagnosticState` batching, `dvsDriveSampleID` rename, `[DVS-TRACE:3]` logging); the "DVS notation slice 2/3" scaffold (`notationRoutingEnabled`, `timecodeNotationAccumulator`, `forwardTimecodeNotationWindow`, `TimecodeNotationCapturePrecedence.resolvedMovementEvents`, `withTimecodeLiveMovementProvenance`); `audioPacket`/`AudioPacket` visibility (`private`→internal) + dynamically-sized `AudioBufferList` hardening; and `dvsNotationDiagnostics` debug-text lines. `project.pbxproj`'s 4 dirty hunks each mixed the 2 Slice-2 `LLSS`/`LLSL` registration lines with 1 unrelated `DOGR` (`DVSOfflineGrainRendererTests`) line in the same hunk.
- **Built the exact candidate from `HEAD`, not by hand-splitting the dirty diff.** Copied `git show HEAD:<path>` for both files into scratch copies, then applied only the Slice-2 changes (verified against the dirty diff hunk-by-hunk) — safer than reconstructing a unified diff with corrected line-number headers given how much unrelated content was interleaved. Confirmed via `diff` twice: candidate vs. `HEAD` contains only the hunks classified as Slice 2 above; candidate vs. the real dirty working file differs by exactly the unrelated hunks removed (12 in `MacCaptureEngine.swift`, 4 in `project.pbxproj`) and nothing else.
- **Staged via `git hash-object -w` + `git update-index --cacheinfo`/`--add --cacheinfo`** rather than `git apply --cached` — avoids any hunk-splitting/line-offset risk entirely for a diff this large and interleaved. Staged: `MacCaptureEngine.swift`, `project.pbxproj` (both `git update-index --cacheinfo`, since already tracked), `LowLatencyTimecodeSinkSessionTests.swift`, `LowLatencySinkLifecycleTests.swift` (both `git update-index --add --cacheinfo`, new paths — staged as-is, no unrelated content in either). The real working tree files were never modified — confirmed via `git status` showing `MM` for the two mixed files (staged content differs from `HEAD`; working tree still differs from the index by exactly the remaining unrelated dirty content) and `A ` for the two new test files.
- **Verified the staged candidate in an isolated detached worktree** built from `HEAD` + exactly the cached tree (`git write-tree` to snapshot the index, `git commit-tree` to wrap it in a dangling commit off `HEAD`, `git worktree add --detach` — no branch or ref touched, nothing added to project history). Confirmed absent, as expected: `DVSOfflineGrainRendererTests.swift`, `Fixtures/Timecode/`, `tmp/`. Confirmed present: both new Slice 2 test files; `project.pbxproj` with 0 `DOGR` / 8 `LLSS`+`LLSL` occurrences. In that worktree: `plutil -lint` clean; macOS Debug build succeeded; `LowLatencyTimecodeSinkSessionTests` 26/26, `LowLatencySinkLifecycleTests` 6/6, `TimecodeRealtimeIngressRingTests` 27/27, `TimecodeLiveTapTests` 54/54 (`HEAD`'s own committed count — this candidate correctly excludes other still-dirty test additions to that same file from unrelated work), `DVSControlVinylAnalyzerTests` 38/38 — both test-plan configurations, zero failures; `python3 scripts/test_capture_pipeline.py` 47/47; unsigned iOS build succeeded (watch built as a scheme dependency); `git diff --check` clean in the worktree. Worktree removed after verification (`git worktree remove --force`); the real repository's `HEAD`, branch, and every other dirty/untracked file were confirmed untouched throughout via `git status`/`git rev-parse HEAD origin/main` before and after.
- Kid Mode untouched throughout. **Not committed or pushed** — the exact 3-path candidate (`MacCaptureEngine.swift`, `project.pbxproj`, the 2 new test files) is staged and fully verified, awaiting Karl's explicit commit/push decision.

## 2026-08-04 (First real Rane capture: sink route never went live — found and fixed a generic-AVAudioFormat-reconstruction defect; second capture required)
- **Karl ran the Slice 2 acceptance capture.** Result: `route=AVCapture fallback installed=true callbacks=0 observed=awaiting callback,0ch,0f@0Hz rejection=failed to synthesize AVAudioPCMBuffer for delivered slot`; `HAL buffer: bufferFrameSize=512 range=[15...4096] nominalSampleRate=48000.0 safetyOffset=74 deviceLatency=74`; `DVS sink ring: published=23246 dropped=0 rejected=0`; `DVS pipeline ingress: received=17911 dropped=0 pending=1/512f` (the separate, pre-existing legacy-path diagnostic — confirms ordinary AVCapture fallback kept the app working correctly throughout).
- **Reproduced the exact failure deterministically before touching any code**, via a standalone `xcrun swift` script (not guessed): `AVAudioFormat(commonFormat: .pcmFormatFloat32, sampleRate: 48_000, channels: 14, interleaved: false)` returns `nil` — there is no standard channel layout Apple defines for 14 channels. Separately confirmed `AVAudioPCMBuffer(pcmFormat:frameCapacity:)` construction succeeds immediately once given *any* format that carries an explicit channel layout (e.g. via `AVAudioChannelLayout(layoutTag: kAudioChannelLayoutTag_DiscreteInOrder | 14)`). Conclusion: the failure was 100% in `AVAudioFormat` reconstruction, not `AVAudioPCMBuffer` allocation — the guard chain's first optional (`format`) was what failed on real hardware, so the reported "failed to synthesize AVAudioPCMBuffer" message was misleading about which step actually failed.
- **Inspected the real, already-validated sink connection format** (`tapFormat` in `refreshLowLatencyTimecodeInput()`, built from `inputNode.inputFormat(forBus:)`/`outputFormat(forBus:)` — a real `AVAudioIONode` format, which per Apple's own contract always carries a proper channel layout for the device's actual channel count, exactly why the old `installTap`-delivered buffers never hit this problem).
- **Fix:** captured `tapFormat` directly into the worker's `consumer` closure (`handleLowLatencySinkDelivery(_:format:)` now takes it as a parameter) instead of reconstructing a format from `slot.sampleRate`/`slot.channelCount` at delivery time. New pure top-level function `makeLowLatencySinkPCMBuffer(fromSlot:format:) -> LowLatencySinkAdapterConversionOutcome` does the buffer construction + planar-channel copy, given the exact format — no adapter-selection logic duplicated (channel-pair selection remains entirely `TimecodeCMSampleBufferAdapter`'s job, called afterward, unchanged). The "shared adapter entry point consuming planar data directly" fallback mentioned as a possible second-resort fix was not needed — the preferred fix (pass the exact format through) was sufficient and is proven by a new regression test using a real 14-channel discrete-layout format.
- **New `LowLatencySinkAdapterConversionFailureStage`** (`channelCountMismatch`/`bufferAllocation`/`channelDataUnavailable`) makes rejection messages specific and diagnosable: `"adapter conversion failed (<stage>): <n>ch <n>f@<n>Hz"`, replacing the old generic "failed to synthesize AVAudioPCMBuffer for delivered slot" that gave no hint about which step or what data was involved.
- **New `LowLatencySinkWorkerObservationDiagnostic`** + `formatLowLatencySinkWorkerObservationDiagnostic`/`applyingLowLatencySinkWorkerObservationDiagnostic` (mirrors the existing marker-replace pattern) — a new `DVS sink observed:` diagnostic line, updated on *every* worker delivery regardless of adapter success/failure, showing real per-slot frame/channel/sample-rate evidence. This directly answers the blind spot the real capture exposed: with `callbacks=0` forever, there was previously zero visibility into what the sink node itself was actually receiving from hardware. Deliberately independent of the heartbeat — never marks anything "fresh," never suppresses AVCapture fallback; only `LowLatencyTimecodeTapHeartbeat.recordCallback` (still gated on full adapter success, unchanged) controls that.
- **Also cleared on teardown:** `resetHeartbeat`'s closure in `teardownActions()` now also nils `timecodeInputWorkerObservationDiagnostic`, matching how the buffer diagnostic and heartbeat itself are already cleared.
- **Realtime callback unchanged in every respect that matters:** still captures only `ring`, `expectedChannelCount`, `sinkSampleRate` — never `self`, never the new format-carrying closure (that capture lives in the worker's `consumer` closure, which already runs off the realtime thread). No allocation/locks/dispatch/logging/adapter work was added to the realtime path.
- **New tests (5, in `LowLatencyTimecodeSinkSessionTests.swift`, 21 → 26):** `testGenericAVAudioFormatReconstructionReturnsNilFor14Channels` (permanent regression guard documenting the exact real-hardware failure); `testAdapterPathConvertsReal14ChannelSlotAndPreservesPair3And4` (real discrete-layout 14ch/512f/48kHz slot through the actual ring → `makeLowLatencySinkPCMBuffer` → `TimecodeCMSampleBufferAdapter.stereoSampleResult` path — proves conversion succeeds, all 14 channel indices preserved via distinct per-channel synthetic data, pair 3/4 produces the correct left/right samples, hostTime/sampleRate/frameCount all correct); `testAdapterPathFailsClosedOnChannelCountMismatch` (a mismatched 2-channel format against a 14-channel slot fails closed with `.channelCountMismatch`, never crashes); `testWorkerObservationDiagnosticReportsRealFrameCountWithoutFreshnessClaims` (asserts the formatted string never contains `route=` or "fresh"); `testApplyingLowLatencySinkWorkerObservationDiagnosticAppendsReplacesAndStrips` (append/replace-without-duplicate/strip-on-nil, matching the established pattern).
- **Verification (this pass only — no hardware; the long build matrix was deliberately not re-run since a second real Rane capture is needed first, per instruction):** `LowLatencyTimecodeSinkSessionTests` 26/26, `LowLatencySinkLifecycleTests` 6/6, `TimecodeRealtimeIngressRingTests` 27/27, `TimecodeLiveTapTests` 65/65, `DVSControlVinylAnalyzerTests` 38/38 — all both test-plan configurations, zero regressions. Thread Sanitizer, 2 invocations on the two new/changed suites, zero warnings. macOS Debug build succeeded. `git diff --check` clean including all untracked test files; index empty throughout.
- Not done: a second real Rane capture, to confirm the sink route now actually goes live (nonzero `callbacks=`, delivered frame count below 4,800, `DVS sink observed:` showing real evidence). Not staged, committed, or pushed. Kid Mode untouched.

## 2026-08-03 (Review-corrected Slice 2: fixed a trapping sample-time conversion, sleep-based tests, missing lifecycle-seam coverage, and a weak-reference-during-deinit defect found along the way — untracked/dirty, not staged, no hardware run yet)
- **Finding 1 — trapping conversion, fixed.** The `AVAudioSinkNode` realtime callback did `Int64(timestamp.pointee.mSampleTime)` guarded only by `.sampleTimeValid`; that flag doesn't guarantee the value is finite or exactly representable as `Int64`, so NaN/infinity/out-of-range would trap. Added `safeSampleTime(_ value: Float64) -> Int64?` next to the existing `safeFrameCount(_:)` — same defensive pattern (`isFinite` + explicit boundary check against `±2^63`, since `Double` can exactly represent `2^63` but `Int64` cannot). Callback now uses `safeSampleTime(...) ?? 0`. Audited `hostTime` (`UInt64`→`UInt64`, no conversion) and `frameCount` (`UInt32`→`Int`, never traps) — both already safe. 4 new test functions in `LowLatencyTimecodeSinkSessionTests.swift`: ordinary values, NaN/±infinity, exact ±2^63 boundaries (had to bump the negative-boundary offset from 1,024 to 4,096 after discovering `Double`'s ULP at that magnitude is 2,048 — the original offset rounded right back to the boundary and the test failed until corrected), and astronomically large finite values.
- **Finding 2 — sleep-based tests, fixed.** `testWorkerDeliversPublishedSlotsToConsumer` used `DispatchQueue.global().asyncAfter(0.2s)` + `wait(for:)` as if that proved delivery had occurred by then — replaced with an `XCTestExpectation` fulfilled directly from the consumer closure, so the wait blocks on the real event and the timeout is only a failure ceiling. `testNoDeliveryAfterTearDownReturns` rewritten so the primary assertion needs no wait at all: `tearDown()`'s synchronous barrier already guarantees the count read immediately after it returns is final (proven directly against the raw worker in `TimecodeRealtimeIngressRingTests.testWorkerStopSynchronouslyPreventsLaterDelivery`); a short `Thread.sleep`-based recheck remains only as a defense-in-depth secondary check, reusing that same suite's own established idiom rather than inventing a new one.
- **Finding 3 — missing graph-lifecycle coverage, fixed.** `LowLatencyTimecodeSinkSessionTests` proved the ring/session primitive but never `MacCaptureEngine`'s actual start/catch/stop *sequencing*. Extracted the smallest production-used seam into `MacCaptureEngine.swift`: `LowLatencySinkLifecycleActions` (7 closures — `attachAndConnect`, `startEngine`, `stopEngine`, `disconnectAndDetach`, `resetEngine`, `markHeartbeatInstalled`, `resetHeartbeat`) and `LowLatencySinkLifecycle` (owns `currentSession`/`isSinkAttached`/`isHeartbeatInstalled`; `start`/`stop` drive them through those closures in the required order: close producer acceptance → stop producer → disconnect/detach sink → synchronously stop worker → clear published session state). `refreshLowLatencyTimecodeInput()`/`stopLowLatencyTimecodeInput()` rewritten as thin wrappers building real closures around `timecodeInputEngine`/the heartbeat and calling `lowLatencySinkLifecycle.start`/`.stop` directly — removing the ad hoc `currentLowLatencySinkSession`/`timecodeInputSessionGeneration` bookkeeping that previously lived only in `MacCaptureEngine`. New file `ScratchLabDesktopTests/LowLatencySinkLifecycleTests.swift` (6 tests, registered in `project.pbxproj`): successful start transitions to running; failed start invokes complete cleanup (no attached sink, no accepting ring, no installed heartbeat, no published session, exact 7-call teardown order); session-construction failure (invalid `drainInterval`) calls zero graph actions; teardown order proven via ring/session-state checks interleaved with the injected closures, plus a no-further-delivery check through the lifecycle's own `stop()`; repeated `stop()` is idempotent (skips a redundant `disconnectAndDetach`); a stale prior session's ring stays confined/rejecting across a full start→stop→start cycle, distinct from the replacement session's ring. Also corrected `stopLowLatencyTimecodeInput()`'s doc comment, which incorrectly stated "every racing producer callback observes the closed ring" — the actual, already-documented guarantee (`TimecodeRealtimeIngressRing.closeForWriting()`) is that a call which *observes* the flag closed is rejected, not that every racing call is; one already-entered `publish` may still complete.
- **Defect found during this pass's own verification, not one of the 3 assigned findings, fixed anyway.** The first version of `teardownActions()` captured `[weak self]` in all 7 closures. `stopLowLatencyTimecodeInput()` (which calls `teardownActions()`) is called from `deinit` — forming a *new* weak reference to `self` while it is already being deallocated is exactly what triggers the ObjC runtime's `Cannot form weak reference ... in the process of deallocation` warning. This showed up 53 times in one full `scripts/build.sh all` run (absent from every prior session's build log) but was invisible to the two new suites' own passing runs and to Thread Sanitizer on those suites, because neither ever constructs a real `MacCaptureEngine` — it only appeared once the full suite (which does, e.g. `CaptureReliabilityPhase1CoreTests`) ran. Fixed by capturing `self` directly (no `[weak self]`) in all 7 lifecycle-action closures: every one is invoked synchronously, within the same call that constructs it, and never escapes — there is no retain cycle, and this is exactly equivalent to the plain `self.method()` calls `deinit` already makes elsewhere in this class. Confirmed fixed by re-running with a grep for the warning string across a full rebuild + targeted test run (0 occurrences) before re-running the complete verification suite.
- **Verification (no hardware — see `AI_HANDOFF.md` for what this does and does not prove):** `LowLatencyTimecodeSinkSessionTests` 21/21 (17 + 4 new), `LowLatencySinkLifecycleTests` 6/6, `TimecodeRealtimeIngressRingTests` 27/27, `TimecodeLiveTapTests` 65/65, `DVSControlVinylAnalyzerTests` 38/38 — all both test-plan configurations, zero regressions. Thread Sanitizer, 2 invocations on the two new suites, zero warnings (run both before and after the weak-reference fix, since those suites don't exercise the code path that had the bug). `python3 scripts/test_capture_pipeline.py` 47/47. macOS Debug, unsigned iOS, standalone watchOS builds all succeeded (rebuilt after every fix). `scripts/build.sh all` — re-run fresh after the weak-reference fix (an earlier in-flight run predated it and was discarded as stale) — both configurations reproduced the exact established baseline: 2,390 tests (2,380 + 10 new), 19 skipped, 8 failures (0 unexpected, same 5 distinct pre-existing methods as always), zero new failures, zero occurrences of the weak-reference warning, halted at `run_mac_tests` via the script's own pre-existing `set -e`. `git diff --check` clean including both new untracked test files; `plutil -lint` clean; index empty throughout.
- Not done: no real Rane hardware capture yet. Not staged, committed, or pushed. Kid Mode untouched.

## 2026-08-03 (Implemented Slice 2: `AVAudioSinkNode` replaces `installTap` for the low-latency DVS path — untracked/dirty, not staged, no hardware run yet)
- Replaced `MacCaptureEngine`'s `inputNode.installTap` with an `AVAudioSinkNode` attached to the same persistent `timecodeInputEngine`, feeding the committed (`1c36107`) `TimecodeRealtimeIngressRing`/`TimecodeRealtimeIngressWorker`. Only the `#if ENABLE_TIMECODE_LIVE_TAP` low-latency path changed; the ordinary `AVCaptureAudioDataOutput`/`processAudioSampleBuffer` path is untouched.
- New pure/testable types added directly to `MacCaptureEngine.swift` (before the class body, alongside the existing `LowLatencyTimecodeTapHeartbeat`/format-choice helpers): `LowLatencySinkFormatValidationOutcome`/`validateLowLatencySinkFormat` (bind-time format gate: Float32, non-interleaved, finite positive sample rate, channel count ≤ the ring's configured maximum — fails closed before the engine starts), `LowLatencySinkSessionGeneration` (monotonic `UInt64`), `LowLatencySinkSession` (bundles a fresh ring + worker + generation per session, no `AVAudioEngine`/`AVAudioSinkNode` reference so it's testable without hardware), `LowLatencySinkRingDiagnostic`/`formatLowLatencySinkRingDiagnostic`/`applyingLowLatencySinkRingDiagnostic` (surfaces the ring's published/dropped/rejected counters honestly via the same marker-replace-not-duplicate pattern as the existing HAL-buffer diagnostic).
- The sink node's realtime receiver block captures only `session.ring` plus two fixed values (`expectedChannelCount`, `sinkSampleRate`) — never `self`, never any mutable `MacCaptureEngine` property — and calls only `ring.publish(bufferList:frameCount:expectedChannelCount:sampleRate:hostTime:sampleTime:)`, discarding the result (no logging/formatting on the realtime thread). `AudioTimeStamp`'s `mFlags` gates whether `hostTime`/`sampleTime` are read from the timestamp or default to 0.
- New `handleLowLatencySinkDelivery(_:)` runs on the worker's own private queue (never the realtime thread) and does everything the old tap callback used to do inline: the channel-sufficiency check (`lowLatencyTimecodeTapHasRequiredChannels`, unchanged), synthesizing an `AVAudioPCMBuffer` from the drained `TimecodeRealtimeIngressSlotView` (copying each channel via `update(from:count:)`, allocation-safe here since it's off the realtime thread), then calling the **existing, unmodified** `TimecodeCMSampleBufferAdapter.stereoSampleResult(fromPCMBuffer:hostTime:)` — reusing 100% of the existing channel-selection/retention/diagnostics behavior with zero changes to `TimecodeCMSampleBufferAdapter.swift`. Heartbeat recording and diagnostic publishing are otherwise identical to the old callback body, just relocated off the realtime thread.
- `stopLowLatencyTimecodeInput()` rewritten to the required teardown order: (1) `session?.ring.closeForWriting()` first; (2) `timecodeInputEngine.stop()`; (3) disconnect + detach the sink node; (4) `session?.tearDown()` (synchronously stops the worker — no further consumer delivery once this returns); then `timecodeInputEngine.reset()` and heartbeat/diagnostic reset. Session-owned objects are kept alive via local `let` bindings for the whole function, so producer retirement is provably complete before `currentLowLatencySinkSession`/`currentLowLatencySinkNode` are cleared. Idempotent (safe to call when nothing is running, safe to call more than once) and reused directly in the failed-`engine.start()` catch block (previously duplicated inline cleanup).
- A new `timecodeInputSessionGeneration` counter (only touched on `sessionQueue`) tags each session; `currentLowLatencySinkSession` is now read/written under the existing `timecodeInputStateLock` (it's read cross-thread from the worker queue's diagnostic publish and from the legacy path's heartbeat-freshness check); `currentLowLatencySinkNode` keeps the same access discipline `timecodeInputEngine` itself already had (sessionQueue + `stop()`/`deinit`'s pre-existing call sites — not a new race class, matches existing behavior exactly).
- New test file `ScratchLabDesktopTests/LowLatencyTimecodeSinkSessionTests.swift` (17 tests, untracked, registered in `project.pbxproj` via 4 surgical entries mirroring Slice 1's exact pattern — 1 `PBXBuildFile`, 1 `PBXFileReference`, 1 group, 1 Sources-phase). No `AVAudioEngine`/`AVAudioSinkNode`/hardware in any test — covers format validation, session construction (including invalid-`drainInterval` failure), lifecycle (accept-until-torn-down, tearDown closes the ring, repeated tearDown is idempotent), stale-session isolation (a torn-down session's ring can never affect a replacement session's), worker delivery to a stand-in consumer, no delivery after `tearDown()` returns, ring-full drop-newest surfaced via `droppedCount`, and the ring-diagnostic marker-replace helper.
- Verification: new suite 17/17 both test-plan configs; `TimecodeRealtimeIngressRingTests` 27/27, `TimecodeLiveTapTests` 65/65, `DVSControlVinylAnalyzerTests` 38/38 both configs, zero regressions; Thread Sanitizer 2 invocations on the new suite, zero warnings; `python3 scripts/test_capture_pipeline.py` 47/47; macOS Debug build succeeded; unsigned iOS build succeeded; standalone watchOS build succeeded; `scripts/build.sh all` — both test-plan configurations reproduced the exact established baseline (2,380 tests [+17 over the 2,363 baseline], 19 skipped, 8 failures, same 5 pre-existing methods as every prior session), zero new failures, halted at `run_mac_tests` via the script's own pre-existing `set -e` (expected); `git diff --check` clean including the new files; `plutil -lint` clean.
- Not done: no real Rane hardware capture yet (see `AI_HANDOFF.md`'s matching entry for the exact procedure and the sub-4,800-frame acceptance criterion). Not staged, committed, or pushed. Kid Mode untouched.

## 2026-08-03 (Reconciled workflow docs: Slice 1 confirmed pushed at `1c36107`, Slice 2 scoped)
- Confirmed via `git rev-list --left-right --count origin/main...main` (0 / 0) that local `main` and `origin/main` are aligned at `1c36107` — Slice 1 ("Add realtime DVS ingress ring") is committed and pushed, not merely committed as the prior entry below states.
- Slice 1 contains exactly the preallocated `TimecodeRealtimeIngressRing`/`TimecodeRealtimeIngressWorker`, their deterministic tests, and the 8 surgical `project.pbxproj` entries. Not connected to live capture. Slice 2 not begun.
- Updated `TASKS.md`, `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md` to mark the Slice 1 commit/push task complete (no duplicate task created) and made the next task explicit: Slice 2 — replace the low-latency DVS input's `AVAudioNode.installTap` path with an `AVAudioSinkNode` render-quantum ingress path using the committed ring/worker, scoped to the feature-gated low-latency path only, realtime-safe callback, truthful teardown, required deterministic tests, and real Rane acceptance criteria (sub-4,800-frame callbacks, no crashes/dropouts, diagnostics distinguishing requested/HAL/delivered frame counts).
- No code changed. All other dirty/untracked work (`MacCaptureEngine.swift`, `TimecodeInputTap.swift`, `DVSLiveLogger.swift`, `DVSControlVinylPanel.swift`, `MacAnalyzerView.swift`, `TimecodeControlCard.swift`, test files, the untracked `DVSOfflineGrainRendererTests.swift`/`Fixtures/Timecode/`/`tmp/`) preserved exactly as found. Kid Mode untouched. `git diff --check` clean on the four documentation files; index empty.

## 2026-08-03 (Committed Slice 1 — `1c36107`, "Add realtime DVS ingress ring" — not pushed)
- Committed the previously-verified, staged 3-path candidate exactly as staged: `ScratchLab.xcodeproj/project.pbxproj` (8 insertions), `ScratchLabDesktop/Services/TimecodeRealtimeIngressRing.swift`, `ScratchLabDesktopTests/TimecodeRealtimeIngressRingTests.swift`. Preconditions re-verified immediately before commit: HEAD was `439cdf3`; index contained exactly these 3 paths; staged pbxproj diff had exactly 8 `TRIR` insertions, zero `DOGR` content; `git diff --cached --check` passed. Commit message is exactly `Add realtime DVS ingress ring`, no `Co-Authored-By` trailer.
- Local `main` is now 1 ahead of `origin/main` (`1c36107`), 0 behind. Not pushed. Index empty after commit; every other dirty/untracked file untouched.
- Slice 2 not begun. Kid Mode untouched.

## 2026-08-03 (Slice 1 finished and staged: fixed a test retain cycle + corrected closeForWriting() docs, then staged the exact 3-path candidate)
- Two small corrections applied before staging: (1) `TimecodeRealtimeIngressRingTests.swift`'s `testSelfStopFromFirstCallbackPreventsLaterSlotsInTheSameDrain` formed a retain cycle (`worker -> consumer closure -> WorkerHolder -> worker`); fixed deterministically with `defer { holder.worker = nil }` right after assignment — no test weakening, no production change. (2) `TimecodeRealtimeIngressRing.swift`'s `closeForWriting()` doc comment incorrectly claimed a producer that raced past the `acceptingWritesFlag` check is "simply rejected" — not true; a producer call that already loaded `true` before the flag closed may still finish and publish one final slot. Rewrote the doc to state the actual guarantee: calls that *observe* the closed flag are rejected; one already-entered producer call may complete; `stop()`'s barrier synchronizes consumer delivery only, not the external producer; a future Slice 2 owner must disconnect the producer callback and keep the ring alive until no producer can access it before releasing it; not a hazard today since nothing is wired to live capture. No locks/allocation/dispatch/logging added to `publish()`.
- Re-verified after both fixes: focused suite 27/27 both test-plan configs; Thread Sanitizer 2 clean invocations, zero warnings.
- Staged the exact corrected candidate: `git add` the two whole new files, and a hand-constructed `git apply --cached` patch for `project.pbxproj` containing exactly the 8 `TimecodeRealtimeIngressRing` insertions (2 `PBXBuildFile`, 2 `PBXFileReference`, 2 group, 2 Sources-phase), excluding the 4 unrelated `DVSOfflineGrainRendererTests` lines which remain unstaged in the same dirty file. Confirmed via `git diff --cached`: exactly 3 staged paths, exactly 8 `+` lines in the pbxproj hunk, zero `DOGR` content.
- Built an isolated detached worktree from `HEAD` + exactly this cached tree (via `git write-tree` + `git commit-tree`, no branch/ref touched) to verify the real candidate in isolation: `Fixtures/Timecode`, `tmp/`, and `DVSOfflineGrainRendererTests.swift` all correctly absent (untracked, not part of the candidate); pbxproj has 0 `DOGR`/8 `TRIR` occurrences. In that worktree: `plutil -lint` clean; macOS Debug build succeeded; focused `TimecodeRealtimeIngressRingTests` (27/27) + `TimecodeLiveTapTests` (54/54, HEAD's committed count) both test-plan configs; Thread Sanitizer 2 clean invocations on the ring tests; `python3 scripts/test_capture_pipeline.py` 47/47; unsigned iOS build succeeded; watchOS build succeeded; `scripts/build.sh all` reproduced the exact established baseline (2,310 tests, 15 skipped, 8 failures — same known methods as always, fewer total tests than the full dirty tree since this candidate excludes every other still-dirty file's local additions), zero new failures, halted at `run_mac_tests` via the script's own `set -e` (expected). `git diff --check` clean in both the worktree and the real repo; `git diff --cached --check` clean in the real repo. Worktree removed after verification; real repo's `HEAD`/branch/other dirty files untouched throughout.
- Not committed or pushed. Slice 2 not begun. Kid Mode untouched.

## 2026-08-03 (Corrected Slice 1 review defects: worker self-stop race, configuration overflow, invalid sample rate, Sendable/drainInterval validation — untracked, not staged)
- Corrects three review-flagged defects in the still-untracked Slice 1 (`ScratchLabDesktop/Services/TimecodeRealtimeIngressRing.swift` / `ScratchLabDesktopTests/TimecodeRealtimeIngressRingTests.swift`). `MacCaptureEngine.swift` remains untouched; `project.pbxproj` unchanged beyond the already-existing 8 Slice 1 entries.
- **Worker self-stop defect:** `tick()` previously checked `stopped` only once, before `drainPending` began. If the first consumer callback in a multi-slot drain called `worker.stop()`, the self-stop path (recognizing it's on the worker's own queue via `DispatchSpecificKey`) marked `stopped` and returned without a barrier — but `drainPending`'s loop kept calling the handler for the remaining already-published slots, so delivery could occur after `stop()` had returned. Fixed by re-checking `stopped` inside the `drainPending` closure itself (`[weak self, consumer] slot in guard let self, !self.stopped... else { return }; consumer(slot)`), not only in `tick()`'s outer guard — a self-stop now silently no-ops every later slot in the same drain while the ring still fully drains. New test `testSelfStopFromFirstCallbackPreventsLaterSlotsInTheSameDrain`: publishes 3 slots, first callback calls `stop()`, proves exactly 1 delivery both at the moment `stop()` returns and after a further wait. Companion `testExternalThreadStopWaitsForAnInFlightCallbackToFinish` proves the existing external-thread barrier guarantee is untouched.
- **Configuration/allocation overflow:** `TimecodeRealtimeIngressRingConfiguration.init?` validated positivity only; `Ring.init` then performed unchecked `maximumChannelCount * maximumFrameCountPerSlot` and `slotCount * slotStride`, and `publish` converted `frameCount`/`channelCount` to `Int32` without proving the configured maxima fit. Fixed: `init?` now validates both maxima fit `Int32`, computes `slotStride`/`sampleCapacity`/byte size via `multipliedReportingOverflow` (rejecting any overflow), and enforces a new documented `maximumSampleStorageByteSize` (256 MiB) so a technically non-overflowing but absurd configuration fails before any pointer allocation. `Ring.init` reuses the configuration's precomputed `slotStride`/`sampleCapacity` rather than recomputing unchecked. New tests: `testConfigurationRejectsValuesThatDoNotFitInt32`, `testConfigurationRejectsMultiplicationOverflow`, `testConfigurationRejectsAbsurdButNonOverflowingAllocationSize`, `testConfigurationAcceptsExactAllocationSizeBoundaryAndRejectsOneFrameOver`.
- **Additional defensive corrections:** non-finite/non-positive `sampleRate` now rejected via new outcome `.rejectedInvalidSampleRate` (`testNonFiniteOrNonPositiveSampleRateIsRejected`, 5 cases) instead of being published as metadata; `consumer` closure is now `@escaping @Sendable` and `TimecodeRealtimeIngressSlotView` is explicitly `Sendable`; `TimecodeRealtimeIngressWorker.init` is now failable, rejecting non-finite/non-positive `drainInterval` rather than silently accepting it (`testWorkerInitRejectsNonFiniteOrNonPositiveDrainIntervals`, 5 cases) — every existing successful-construction call site updated to force-unwrap. Realtime producer guarantees unchanged: still allocation-free/lock-free/dispatch-free/logging-free, no `AVAudioPCMBuffer`/adapter/heartbeat/fixture calls.
- **Incidental TSan finding, fixed:** an initial ThreadSanitizer run caught a genuine data race in the *test harness* (not production code) — the new self-stop test's `WorkerHolder` box was `@unchecked Sendable` without real synchronization. Fixed with a proper `NSLock`-protected computed property. Re-ran TSan twice after the fix: zero warnings. Also quieted 6 new Swift-6-preview `@Sendable`-closure warnings (caused by marking `consumer` `@Sendable`) by adding `nonisolated(unsafe)` to manually lock-protected test counters — these were compiler warnings only (Swift 5 language mode), never build-blocking.
- Verification: new suite 27/27 (was 19) both test-plan configurations; combined with `TimecodeLiveTapTests` 92/92 both configurations; Thread Sanitizer 2 clean invocations (2 runs each) on the corrected file, zero warnings; `python3 scripts/test_capture_pipeline.py` 47/47; macOS Debug build succeeded; unsigned iOS build succeeded; watchOS build succeeded; `scripts/build.sh all` — both test-plan configurations reproduced the exact established baseline (2,363 tests, 19 skipped, 8 failures, same methods as always), zero new failures, then halted at `run_mac_tests` per the script's own pre-existing `set -e` behavior; `git diff --check` clean; no plist/xcprivacy files touched this pass.
- Not staged, committed, or pushed. Not begun: Slice 2. Kid Mode untouched.

## 2026-08-03 (Implemented Slice 1: preallocated SPSC realtime ingress ring + worker — untracked, not staged)
- Not staged, committed, or pushed. New, untracked files: `ScratchLabDesktop/Services/TimecodeRealtimeIngressRing.swift`, `ScratchLabDesktopTests/TimecodeRealtimeIngressRingTests.swift`. `MacCaptureEngine.swift` untouched — no `installTap` replacement, no `AVAudioEngine` graph change, no live wiring.
- Realtime producer (`publish`): zero allocation/locks/dispatch/logging/`AVAudioPCMBuffer`/adapter/heartbeat/fixture calls. Validates buffer-count match, one-channel-per-buffer, non-nil data, sufficient `mDataByteSize`, and frame/channel bounds before copying into preallocated storage; publishes via a release-ordered write counter. Any rejection increments `rejectedCount`; full-ring drops the newest input (`droppedCount`) and never touches the consumer's read counter.
- `TimecodeRealtimeIngressRingConfiguration`: explicit, `Equatable`, validated (`init?`) — production defaults 8 slots / 32 channels / 8,192 frames, each documented (channels: comfortably above the Rane's 14ch; frames: 2× the HAL's own measured 4,096-frame maximum, since the render-quantum hypothesis is unproven).
- Teardown: `TimecodeRealtimeIngressWorker.stop()` closes ring write-acceptance first, marks itself stopped, cancels its persistent timer, then synchronously barriers its own serial queue (`DispatchSpecificKey`-guarded to avoid self-deadlock if called from the worker's own queue) — no consumer delivery is possible after `stop()` returns. A superseded ring/worker (e.g. a stray retained-by-a-future-block old ring) rejects writes and has no active consumer by construction, not via a shared generation counter.
- Mechanism: Swift `Synchronization.Atomic`, release/acquire ordering — verified against this project's exact toolchain/deployment target by a standalone compile+run check before adoption; no new C/C++ bridge.
- `project.pbxproj`: exactly 8 surgical insertions (2 `PBXBuildFile`, 2 `PBXFileReference`, 2 group, 2 Sources-phase) — production file in `ScratchLabDesktop` Sources only, test file in `ScratchLabDesktopTests` Sources only; confirmed absent from the iOS target. Pre-existing unrelated `DVSOfflineGrainRendererTests.swift` pbxproj hunk untouched. `plutil -lint` clean.
- Verification: new suite 19/19; combined with `TimecodeLiveTapTests` 84/84; TSan 2 invocations (4 runs), 19/19 each, zero warnings; `python3 scripts/test_capture_pipeline.py` 47/47; macOS Debug + unsigned iOS builds succeeded; `scripts/build.sh all` — 2,355 tests (+19 over the prior 2,336 baseline), 8 failures (established baseline, same 5 `CaptureReliabilityPhase1CoreTests` methods), zero new failures; `git diff --check` clean.
- Not done: no hardware run, no staging. The ~512-frame render-quantum hypothesis remains unproven — this slice only proves the ring/worker mechanism in isolation.

## 2026-08-03 (Decisive root cause: `AVAudioNode.installTap`'s documented 100–400 ms range, not the Rane HAL, produces the 4,800-frame quantum)
- Proven, from the installed SDK header `AVAudioNode.h`: `installTapOnBus:bufferSize:format:block:` explicitly documents `bufferSize` as "the requested size of the incoming buffers in sample frames. Supported range is [100, 400] ms."
- ScratchLab's 256-frame request at 48 kHz is 5.3 ms — outside that documented 100–400 ms range.
- The observed 4,800-frame callbacks are exactly the documented 100 ms minimum.
- Real Rane HAL evidence: `bufferFrameSize=512 range=[15...4096]` at 48 kHz, `safetyOffset=74`, `deviceLatency=74`.
- Therefore the Rane is already running at 10.7 ms; `AVAudioNode` tap aggregation, not the hardware quantum, creates the 100 ms latency floor.
- Do not attempt to fix this by writing the shared HAL buffer property, `MaximumFramesPerSlice`, or another `installTap` buffer size.

## 2026-08-03 (Real-hardware HAL buffer diagnostic result — Rane ONE MKII, `steady_normal`)
- Real hardware capture against the committed `439cdf3` diagnostic, no code changes: `HAL buffer: bufferFrameSize=512 range=[15...4096] nominalSampleRate=48000.0 safetyOffset=74 deviceLatency=74`.
- Actual `AVAudioEngine` tap callbacks remain exactly 4,800 frames at 48 kHz (100 ms) — matching every prior real-hardware capture. 4,800 ≠ the 256-frame tap request, ≠ the HAL's current 512-frame buffer size, and ≠ the HAL's own advertised maximum of 4,096 frames.
- The 100 ms batching therefore occurs in the software path after the HAL device boundary, not in the Rane's native I/O quantum — the device itself is not the source of the 100 ms figure.

## 2026-08-03 (Committed and pushed the corrected HAL buffer-size diagnostic — `439cdf3`)
- Committed and pushed `439cdf3` ("Add read-only DEBUG diagnostic for the DVS tap's real HAL buffer size"). Local `main` and `origin/main` are aligned at that commit.
- Exactly two files, 400 insertions, 0 deletions: `MacCaptureEngine.swift` (215 lines), `TimecodeLiveTapTests.swift` (185 lines). Staged via an explicit `git apply --cached` surgical patch built from a hand-reconstructed HEAD-plus-diagnostic-only candidate, not whole-file `git add` — both files retain other pre-existing unrelated dirty content, untouched, unstaged.
- Read-only CoreAudio queries only, no property writes anywhere. Scopes verified against the installed SDK headers: `kAudioObjectPropertyScopeGlobal` for `kAudioDevicePropertyBufferFrameSize`/`...Range`/`kAudioDevicePropertyNominalSampleRate`; `kAudioDevicePropertyScopeInput` for `kAudioDevicePropertySafetyOffset`/`kAudioDevicePropertyLatency`.
- 9 deterministic tests: formatting (full/empty/partial), `Equatable` regression, `safeFrameCount` valid/invalid, `applyingAudioDeviceBufferDiagnostic` append/replace-without-duplicate/strip-on-nil.
- Risk: read-only, DEBUG-only (`ENABLE_TIMECODE_LIVE_TAP`), zero production Release/App Store exposure.
- Verification (isolated detached worktree, exactly `HEAD` + cached tree): 9 new tests 18/18, full `TimecodeLiveTapTests` 54/54, TSan 2 runs 54/54 each zero warnings, `python3 scripts/test_capture_pipeline.py` 47/47, macOS Debug + unsigned iOS builds succeeded, `scripts/build.sh all` — 2,283 tests/8 failures (established baseline, same 5 `CaptureReliabilityPhase1CoreTests` methods), no new failures, `git diff --check` clean. Worktree removed after verification.
- Excluded, all still local: every other pre-existing dirty file, plus the remainder of both touched files (playback-drive threading, notation scaffold, `audioPacket`/`pcmFormat` hardening, `DVSControlProcessingLoop` tests, Rane-preset tests, pre-existing test reorder). Kid Mode untouched.
- Next step: one real-hardware diagnostic capture (Rane connected, confirm `route=AVAudioEngine`, record one 5–10s `steady_normal` fixture, read the new `HAL buffer:` line) — not a new engineering slice.

## 2026-08-03 (Corrected the HAL buffer-size diagnostic's CoreAudio scopes/availability checks/lifecycle — uncommitted)
- Not staged, committed, or pushed. Corrects a review finding on the read-only DEBUG diagnostic added earlier the same day (`MacCaptureEngine.swift`, `#if ENABLE_TIMECODE_LIVE_TAP`) that answers why the Rane tap delivers 4800-frame buffers instead of the 256 requested.
- Bug: `readAudioDeviceBufferDiagnostic` used `kAudioDevicePropertyScopeInput` for every property. Fixed per the installed CoreAudio SDK headers: `kAudioDevicePropertyBufferFrameSize`/`...Range`/`kAudioDevicePropertyNominalSampleRate` → `kAudioObjectPropertyScopeGlobal` (no input/output distinction documented); `kAudioDevicePropertySafetyOffset`/`kAudioDevicePropertyLatency` → `kAudioDevicePropertyScopeInput` (SDK docs explicitly note input/output can differ).
- Added `AudioObjectHasProperty` + data-size verification before every read; a defensive `safeFrameCount(_:)` (NaN/infinite/negative/overflow-safe) replacing an unconditional trapping `UInt32(...)` conversion of `AudioValueRange` bounds.
- Lifecycle fix: diagnostic storage moved to only after a confirmed successful AUHAL bind (alongside `markInstalled`), cleared on failed bind/`catch`/`stopLowLatencyTimecodeInput()`; `publishAudioDeviceBufferDiagnosticIfAvailable()` now routes through a new pure `applyingAudioDeviceBufferDiagnostic(to:diagnostic:)` so `nil` strips stale marker text instead of leaving it.
- 9 new tests added to `TimecodeLiveTapTests.swift`: formatting (all-present/all-absent/partial), `Equatable` regression, `safeFrameCount` valid/invalid inputs, `applyingAudioDeviceBufferDiagnostic` append/replace-without-duplicate/strip-on-nil.
- Diff-scope caveat: both touched files carry substantial unrelated pre-existing dirty content (playback-drive threading, notation-routing scaffold, `audioPacket`/`pcmFormat` hardening, a `DVSControlProcessingLoop` test block, an extensive pre-existing test reorder) predating this task, untouched by it.
- Verification: new tests 9/9 + full `TimecodeLiveTapTests` 65/65 via `xcrun xctest`; TSan 2 runs, 65/65 each, zero warnings; `python3 scripts/test_capture_pipeline.py` 47/47; macOS Debug + unsigned iOS builds succeeded; `scripts/build.sh all` — 2,336 tests, 8 failures (established baseline, same 5 `CaptureReliabilityPhase1CoreTests` methods), no new failures; `git diff --check` clean. No hardware test run. Nothing staged.

## 2026-08-03 (Committed and pushed the low-latency DVS AVAudioEngine tap install — `4d17ef2`)
- Committed and pushed `4d17ef2` ("Install low-latency DVS AVAudioEngine tap"). Local `main` and `origin/main` are aligned at that commit.
- Exactly one file: `MacCaptureEngine.swift` (300 insertions, 2 deletions), hand-split via an explicit `git apply --cached` patch from a file mixing the candidate with 12 excluded hunks (playback-drive-gate sync, notation-routing scaffold, `RoutineAudioCaptureWriter`/`audioPacket` hardening). One hunk required an in-place split (excluded notation-scaffold properties vs. included `timecodeInputEngine`/`timecodeInputStateLock`/`timecodeInputTapHeartbeat` declarations) — separated cleanly, verified no duplication/leftover.
- What landed: the tap properties; `timecodeAudioCallback`'s `didSet`; `deinit`/`stop()`'s teardown; the full `refreshLowLatencyTimecodeInput()`/`stopLowLatencyTimecodeInput()`/`publishLowLatencyTimecodeTapDiagnostic()`/`audioDeviceID(forUID:)` block (real `installTap`+AUHAL bind); the `startRunning()` call site; the heartbeat-freshness suppression gate (must land atomically with install — prevents double-driving the decode pipeline). This activates real hardware low-latency capture using the already-committed `stereoSampleResult(fromPCMBuffer:hostTime:)` (`54e573e`) and heartbeat/format-choice types (`a89e078`).
- Confirmed via grep, needs nothing from any other still-dirty file. `TimecodeInputTap.swift`'s thread-safety fix, Rane pair pinning, `TimecodeControlCard`'s preset wiring, and `DVSControlProcessingLoop` traced both directions and confirmed NOT compile/functionally-mandatory — remain separate candidates.
- Risk: activates a previously-inert hardware I/O path; `ENABLE_TIMECODE_LIVE_TAP`-gated, Debug-only, zero App Store exposure; no signing/entitlements/resources touched. No new tests — imperative AUHAL glue isn't unit-testable without hardware.
- Verification (isolated detached worktree, exactly `HEAD` + cached tree): `TimecodeLiveTapTests`/`DVSControlVinylAnalyzerTests` pass both test plans; TSan 2 consecutive runs zero warnings; `python3 scripts/test_capture_pipeline.py` 47/47; macOS Debug + unsigned iOS builds succeeded; `scripts/build.sh all` reproduced exactly the established baseline, no new failures, `TimecodeLiveTapTests` 90/90 + `DVSControlVinylAnalyzerTests` 76/76 in that full run; both diff checks clean; index empty throughout.
- Excluded, all still local: every other pre-existing dirty file (including the rest of `MacCaptureEngine.swift`) and the untracked `DVSOfflineGrainRendererTests.swift`/`Fixtures/Timecode/`/`tmp/`. Kid Mode untouched.
- Next step: read-only extraction audit of the remaining dirty adapter/capture cluster to select the next smallest candidate.

## 2026-08-03 (Committed and pushed the Rane ONE MKII DEBUG preset — `7679c06`)
- Committed and pushed `7679c06` ("Add Rane ONE MKII DEBUG preset"). Local `main` and `origin/main` are aligned at that commit.
- Exactly two files: `TimecodePrototypeProfile.swift` (81 added lines — `.raneOneMkiiDebug` case/labels/`setupNote`/`raneOneMkiiDebug()` factory) and `TimecodePrototypeProfileTests.swift` (44 added lines — 2 new tests). Pure value-type/enum addition in a shared cross-platform model file, no `#if ENABLE_TIMECODE_LIVE_TAP` gating, no threading, zero compile dependency on any other dirty file.
- Does not itself pin any USB channel pair or wire into live capture — that's applied by the still-dirty, gated `TimecodeControlCard.applyPreset`. Its live consumers (`TimecodeControlCard.swift`, `DVSControlVinylPanel.swift`, `TimecodeFixtureValidationTests.swift`, `TimecodeNotationRoutingTests.swift`) remain dirty/unstaged.
- Verification (isolated detached worktree, exactly `HEAD` + cached tree): `TimecodePrototypeProfileTests` 31/31 both test plans; TSan not run (no concurrency introduced); `python3 scripts/test_capture_pipeline.py` 47/47; macOS Debug + unsigned iOS builds succeeded; `scripts/build.sh all` reproduced exactly the established baseline, no new failures, `TimecodePrototypeProfileTests` 62/62 in that full run; both diff checks clean; index empty throughout.
- Excluded, all still local: every other pre-existing dirty file and the untracked `DVSOfflineGrainRendererTests.swift`/`Fixtures/Timecode/`/`tmp/`. Kid Mode untouched.
- Next step: read-only extraction audit of the remaining dirty adapter/capture cluster to select the next smallest candidate.

## 2026-08-03 (Committed and pushed the low-latency DVS tap heartbeat logic — `a89e078`)
- Committed and pushed `a89e078` ("Add low-latency DVS tap heartbeat logic"). Local `main` and `origin/main` are aligned at that commit.
- Exactly two files: `ScratchLabDesktop/Services/MacCaptureEngine.swift` (124 added lines — the pure `LowLatencyTimecodeTapFormatChoice`/channel-validator/`LowLatencyTimecodeTapHeartbeat` block only) and `ScratchLabDesktopTests/TimecodeLiveTapTests.swift` (152 added lines — five deterministic tests under their own `ENABLE_TIMECODE_LIVE_TAP` guard only). Reviewed the complete cached diff before committing; confirmed live tap engine/install/callback/routing/fallback wiring, Rane preset/pinning, `DebugTimecodeCapture`/fixture-recorder work, `DVSControlProcessingLoop`, `RoutineAudioCaptureWriter`/`audioPacket` changes, UI/notation/playback/offline-renderer material, and `project.pbxproj` were all absent.
- These helpers are not yet connected to live capture — their only callers remain in the still-dirty, unstaged tap-installation code in the same file.
- Verification reused from the pre-commit cached-tree check (state matched expectations exactly, so the long verification was not re-run): both test plans `TimecodeLiveTapTests` 45/45; capture pipeline 47/47; macOS Debug and unsigned iOS builds succeeded; `scripts/build.sh all` reproduced only the established baseline (2,272 XCTest executions, 15 skips, same 8 assertion failures; 157 Swift Testing cases, same 4 notation issues), touched suite 45/45; both diff checks clean.
- All other dirty/untracked work remains local and untouched; Kid Mode untouched.

## 2026-08-03 (Committed and pushed the DVS retention-generation race fix — `54e573e`)
- Committed and pushed `54e573e` ("Fix DVS retention-generation race for concurrent capture producers"). Local `main` and `origin/main` are aligned at that commit.
- Exactly 2 files: `TimecodeCMSampleBufferAdapter.swift` (full dirty diff), `DVSControlVinylAnalyzerTests.swift` (dirty diff minus one 4-line `makeLogEntry()` hunk — `rawDecodeTrace`/`calibratedTrace`/`heldDropoutCount`/`longDropoutCount` — which remains dirty/unstaged, belonging to a separate slice paired with `DVSLiveLogger.swift`). Staged via an explicit `git apply --cached` patch, not whole-file `git add`. Root cause/fix/test detail unchanged from the entry directly below.
- Selection: a follow-up read-only extraction audit split the remaining dirty adapter/capture cluster into ten slices and selected this one as the only slice with zero compile/API dependency on any other dirty file.
- Verification (isolated detached worktree, exactly `HEAD` + cached tree, no untracked fixtures/`tmp/`): `DVSControlVinylAnalyzerTests` + `TimecodeLiveTapTests` pass both test-plan configurations, 0 failures. Thread Sanitizer, 2 consecutive runs, zero warnings. `python3 scripts/test_capture_pipeline.py` 47/47. macOS Debug + unsigned iOS builds succeeded. `scripts/build.sh all` reproduced exactly the established baseline — no new failures. `git diff --check` clean; index empty throughout.
- Excluded, all still local: the `makeLogEntry()` hunk, every other pre-existing dirty file, and the untracked `DVSOfflineGrainRendererTests.swift`/`Fixtures/Timecode/`/`tmp/`. Kid Mode untouched.
- Landing constraint resolved: prior "awaiting Karl's staging/commit decision" is satisfied.
- Next step: read-only extraction audit of the remaining dirty adapter/capture cluster to select the next smallest candidate from the nine remaining slices plus the excluded `makeLogEntry()` hunk.

## 2026-08-03 (Reconciled the dirty `TimecodeCMSampleBufferAdapter.swift` against the committed `9f54937` retention mechanism — uncommitted)
- Implemented the minimal fix identified by the prior read-only audit. Nothing staged, committed, or pushed — the file remains dirty, now verified.
- Root cause: the generation/transaction retention mechanism from `9f54937` was intact and unweakened in the dirty tree, but the dirty tree had also wired a second real production producer (the low-latency `AVAudioEngine` tap, `stereoSampleResult(fromPCMBuffer:hostTime:)`, called from `MacCaptureEngine.swift` on a different thread than the legacy path) into the same shared retention state. The generation counter only bumped on reset/selection-change, never on a successful commit, so two concurrent transactions at the same generation could both commit, the second silently overwriting the first (lost update) — never proven safe against a second producer.
- Fix (`TimecodeCMSampleBufferAdapter.swift`): `commitRetentionTransaction` now bumps `storedRetentionGeneration` on every successful commit too, reusing the existing CAS guard — no new lock, no API change, no change to hysteresis/lock boundaries/pair-selection semantics. First committer wins; the second's stale snapshot is rejected exactly like a post-reset stale commit, its own audio still returned normally, only its retention bookkeeping dropped. Corrected the doc comments that previously described this as an unresolved concern and pointed to a nonexistent "reconciliation audit note."
- Tests (`DVSControlVinylAnalyzerTests.swift`): added `testConcurrentTransactionsFromTwoProducersRejectsTheStaleCommit` (deterministic, sequential, direct regression for the exact race); strengthened `testConcurrentSelectionAndResetVersusRetentionUpdatesLeavesConsistentFinalState` with a second concurrent producer queue; corrected the misleading intro comment claiming a prior audit found the mechanism "stripped out" (it hadn't been — the real gap was the unproven second producer).
- Preserved untouched: low-latency PCM entry point, Rane/channel-pinning, DEBUG fixture recording, other compatible-extension hunks; `MacCaptureEngine.swift` not touched. The two previously-deleted cold-start tests were left as-is — out of scope, unrelated to this race.
- Risk note: `ENABLE_TIMECODE_LIVE_TAP` is Debug-only, so exposure was confined to Debug builds, never shipping builds — but live in every Debug build before this fix.
- Verification: `DVSControlVinylAnalyzerTests` 38/38, `TimecodeLiveTapTests` 56/56, both test plans, 0 failures. Thread Sanitizer on both files, 2 consecutive runs, zero warnings. `python3 scripts/test_capture_pipeline.py` 47/47. macOS Debug and unsigned iOS builds succeeded. `scripts/build.sh all` reproduced exactly the established baseline (4606 passed/10 failed, same distinct methods as always documented, 38 expected skips) — no new failures, zero failures in either touched test file. `git diff --check` passed; index stayed empty throughout.
- Kid Mode untouched. Nothing staged/committed/pushed — files remain dirty, verified, awaiting Karl's staging/commit decision.

## 2026-08-03 (Fixed the DVS playback-scheduling race — committed and pushed `a854945`)
- Committed and pushed `a854945` ("Fix DVS grain scheduling and playback continuity"). Local `main` and `origin/main` are aligned at that commit.
- Exactly 3 files: the 2-line macOS `VirtualPlatter` resource registration in `ScratchLab.xcodeproj/project.pbxproj`, `ScratchSamplePlaybackController.swift`, `ScratchSamplePlaybackControllerTests.swift`.
- Fix: `positionDidChange` previously ran its scheduling transaction off the private `audioQueue`, racing `load`/`unload`/`pausePlayback`/`resumePlayback`/`setCrossfader`. Now runs the complete trace + scheduling transaction synchronously on `audioQueue`, via a `DispatchSpecificKey`-guarded helper that executes directly when already on the queue (reentrancy protection against self-deadlock) and otherwise uses `audioQueue.sync`. No second/partial lock introduced.
- Verification: `ScratchSamplePlaybackControllerTests` 85/85 per test-plan configuration; `TimecodeLiveIntegrationTests` 20/20 per configuration (confirmed via persisted `.xcresult` evidence, correcting an earlier "18/18" reporting slip). Two corrected Thread Sanitizer runs, zero warnings, `testConcurrentLoadAndPositionDidChangeDoNotCrash` explicitly passing under TSan both times. `python3 scripts/test_capture_pipeline.py` 47/47. macOS Debug and unsigned iOS builds succeeded. `scripts/build.sh all` retained only the established pre-existing baseline (same `CaptureReliabilityPhase1CoreTests`/notation-parity failures as every prior run of this script, confirmed unrelated by reproducing them identically in an isolated worktree of `HEAD` + only the corrected files) — no new failures.
- Kid Mode: read-only orientation only, no code touched. Confirmed intentionally research-gated (`analysis/kid_mode/RESEARCH_STOP_OR_CONTINUE.md`), not technically blocked. Canonical implementation preserved at `13bc0f5` on branch `kid-audio-refactor`; `stash@{1}` is byte-identical/redundant to that commit and must not be dropped without explicit approval; `FeatureFlags.kidPrototypeEnabled` stays `releaseDefault: false`; no Kid Mode code should resume until Karl explicitly overrides the Tier-0/Tier-1 research gate.
- Next engineering task unchanged: the read-only reconciliation audit of dirty `TimecodeCMSampleBufferAdapter.swift` against the generation-based race-safe retention semantics committed in `9f54937`.

## 2026-08-02 (Landed the validated DVS decoder core — commit and push)
- Committed and pushed the accumulated decoder-core work as `7e733b3` ("Land validated DVS decoder core"). Local `main` and `origin/main` are aligned at that commit.
- Exactly 12 files landed: `ScratchLab/Models/TimecodeControlPipeline.swift`, `ScratchLab/Models/TimecodePhaseDecoder.swift`, `ScratchLab/Models/TimecodePlatterAdapter.swift`, `ScratchLab/Models/TimecodePrototypeProfile.swift`, `ScratchLab/Models/TimecodeValidationSnapshot.swift`, `ScratchLabDesktopTests/TimecodeDecoderTests.swift`, `ScratchLabDesktopTests/TimecodeFixtureValidationTests.swift`, `ScratchLabDesktopTests/TimecodeLiveIntegrationTests.swift`, `ScratchLabDesktopTests/TimecodeLiveTapTests.swift`, `ScratchLabDesktopTests/TimecodePlaybackBridgeTests.swift`, `ScratchLabDesktopTests/TimecodeStabilityFilterTests.swift`, `ScratchLabDesktopTests/TimecodeValidationTests.swift`.
- This lands, as one commit, everything the recent entries below had left uncommitted: the validated ZCR/frequency-ratio decoder rewrite, platter position continuity, coherent output snapshots (pipeline output synchronization), configuration synchronization and mode-transition linearization (the `mode`/configuration race close), atomic calibration application, the reconciled direction tests, and guarded optional hardware-fixture tests. Does not connect the signed-rate estimator/stabilizer to production. Does not include Rane preset/UI/channel-pinning, low-latency tap, notation, offline renderer, fixture audio, `tmp/`, or `project.pbxproj` changes — those remain separately dirty/untracked.
- Verification: focused isolated verification passed 190 executions per test plan (380 across both configurations), 4 expected missing-fixture skips, zero failures. `TimecodePlaybackBridgeTests` 28/28 in each configuration. `python3 scripts/test_capture_pipeline.py` 47/47. macOS and unsigned iOS builds succeeded. `scripts/build.sh all` reproduced only the established baseline, no new failures.
- The landing constraint on every entry below that referenced "must land with the decoder core" / "not independently committable against HEAD" is now resolved by this commit — those notes are updated in place below rather than left stale.

## 2026-08-02 (Fixed a mode-transition linearization defect left by the configuration-race fix)
- Karl reviewed the just-landed `mode`/configuration cross-thread race fix and found it was race-safe (no torn reads, no lost updates, clean under TSan) but not *linearized*: two ordering bugs remained. Scenario 1 — `pushStereoBuffer`/`flushDecode` captured `ConfigurationState` *before* acquiring `processingLock`; an operation that captured mode as `.controlPrototype`, got delayed, and resumed after a concurrent disable had already cleared the accumulator would still append/decode stale audio into a session that was supposed to have been cleared. Scenario 2 — `mode`'s setter published its new value *before* waiting for `processingLock`, serializing only the cleanup that followed; a delayed cleanup could run *after* a second transition (e.g. immediate re-enable) completed, wiping the new session's data.
- Fix: `pushStereoBuffer`/`flushDecode` now capture configuration only *after* `processingLock.lock()`. `mode`'s setter now acquires `processingLock` first — the entire write-plus-cleanup is one critical section, so two transitions can never interleave. Leaving `.controlPrototype` now also clears the live-ingress pending queue/bookkeeping (previously only the direct-push accumulator), preserving lifetime diagnostic totals. `enqueueLiveStereoBuffer` (must stay callback-safe, never touches `processingLock`) now holds `configurationLock` continuously through its check-then-append, linearizing it against a transition's cleanup via `configurationLock` alone. Documented global lock order: `processingLock` → `configurationLock` → ingestion `lock` for transactions; `configurationLock` → `lock` for live enqueue; `outputLock` independent. `mode` now correctly stays at its old value for the whole duration a transition is blocked — a deliberate trade, not a regression.
- Tests (`ScratchLabDesktopTests/TimecodeLiveTapTests.swift`, 52 → 56): renamed/corrected `testModeTransitionCleanupSerializesWithInFlightFlush` → `testModeTransitionIsFullySerializedBehindInFlightFlushNotJustItsCleanup`; added `testPausedPushStereoBufferWithOldControlConfigCannotAppendAfterDisableCleanup`, `testPausedEnqueueLiveStereoBufferWithOldControlConfigCannotAppendAfterDisableCleanup`, `testDisableImmediatelyFollowedByReenableDoesNotClearTheNewSession`, `testConcurrentModeTransitionsPushEnqueueAndFlushDoNotDeadlock`. Caught and fixed a test-authoring deadlock along the way: `mode`'s setter now also fires the shared `debugPostConfigurationCaptureHook`, and two new tests hadn't cleared it before starting their concurrent mode-transition thread — that thread re-invoked the already-consumed push/enqueue pause closure and blocked forever. Fixed by clearing the hook right after confirming the initial pause, before starting the transition thread.
- Verification: `TimecodeLiveTapTests` 56/56, adjacent decoder/validation/stability/profile/live-integration suites all clean, both test plans 186/186 combined each. ThreadSanitizer 56/56 zero warnings ×2. Capture-pipeline script 47/47. macOS + iOS builds succeeded. `scripts/build.sh all` exit 65, full plan 2319 tests/19 skipped/8 assertion failures per pass — exact established baseline, no new failures (+4 matching the new tests). `git diff --check` clean; index empty throughout; disk healthy (34Gi free).
- Landing constraint: **resolved.** This work landed together with the decoder core in commit `7e733b3` ("Land validated DVS decoder core"), pushed and aligned with `origin/main`.

## 2026-08-02 (Closed the `mode`/configuration cross-thread race in `TimecodeControlPipeline` — Karl-approved follow-up task)
- Karl approved exactly one task: close the `mode`/configuration cross-thread race left open (found and reverted, not fixed) by the pipeline output synchronization task's third review correction. `mode` and the other seven configuration properties (`liveTapEnabled`, `invertDirection`, `rateScale`, `inputChannel`, `minConfidence`, `maxRate`, `signalThresholdRMS`) were plain `@Published` stored properties — Swift writes a `@Published` property's backing storage before `didSet` runs, so no `didSet`-level locking could make the property's own read/write race-safe, and the four transactional methods read several of them without any snapshot discipline.
- Design: a private `ConfigurationState` struct bundles all eight properties as one `Equatable` value, guarded by a new independent `configurationLock` (`NSLock`), mirroring the existing `OutputState`/`outputLock` pattern for decoded outputs. Every property became a computed `get`/`set` pair; every setter routes through one shared `updateConfiguration(_:)` helper — read, mutate, compare, swap, and (only after the lock releases, only if actually changed) notify. `mode`'s setter performs the leaving-`.controlPrototype` accumulator/position cleanup *after* the configuration write, under the existing `processingLock` → `lock` ordering, with `configurationLock` always released first — never nested with `processingLock`, so the two can never deadlock. `pushStereoBuffer`/`flushDecode`/`enqueueLiveStereoBuffer`/`makeValidationSnapshot` each capture one `ConfigurationState` snapshot at their boundary (`captureConfigurationForOperation()`) and use only that value for their entire body; `decoder`/`stabilityFilter` became parameterized functions instead of computed properties reading live values. Confirmed no `$property` Combine-publisher usage of any of the eight properties exists anywhere in the repo (only SwiftUI's `$pipeline.<property>` dynamic-member bindings, unaffected by removing `@Published`) before making the change.
- Added `TimecodeControlPipeline.applyCalibrationBatch(inputChannel:invertDirection:rateScale:minConfidence:maxRate:)`, one atomic `updateConfiguration` call; `TimecodePrototypeProfile.apply(to:)` now calls it instead of five sequential property writes — finally making its pre-existing "atomically" doc comment true. No decoder constants, calibration math, thresholds, output synchronization, capture, playback, notation, export, or UI behavior changed.
- Tests added (`ScratchLabDesktopTests/TimecodeLiveTapTests.swift`, 46 → 52), all deterministic via a new DEBUG-only `debugPostConfigurationCaptureHook` (fires right after `captureConfigurationForOperation()`, mirroring `debugPrePublishHook`) or the existing `debugPrePublishHook`: `testFlushPausedAfterConfigurationCaptureUsesOldGenerationThenNextFlushUsesNewGeneration`, `testProfileApplicationIsAtomicNotObservablePartiallyApplied`, `testModeTransitionCleanupSerializesWithInFlightFlush`, `testConfigurationGettersReturnImmediatelyWhileTransactionIsPaused`, `testUnchangedConfigurationAssignmentDoesNotNotify`, `testChangedIndividualAndBatchConfigurationUpdatesNotifyExactlyOnce`.
- Verification: `TimecodeLiveTapTests` 52/52, `TimecodeDecoderTests` 27/27, `TimecodeValidationTests` 23/23, `TimecodeStabilityFilterTests` 29/29, `TimecodePrototypeProfileTests` 31/31, `TimecodeLiveIntegrationTests` 20/20 — both test plans, combined 182/182 each. Thread Sanitizer on `TimecodeLiveTapTests`: 52/52, zero warnings, 2 consecutive runs. `python3 scripts/test_capture_pipeline.py` 47/47. macOS Debug build and unsigned iOS build succeeded. `scripts/build.sh all`: exit 65 (established `set -e` stop at the macOS test stage), capture-pipeline 47/47, full plan 2315 tests/19 skipped/8 assertion failures per pass — the exact established baseline, no new failures, +6 matching the new tests. `git diff --check` passed; index stayed empty throughout.
- Mid-task environment issue (not a code problem): `xcodebuild` hit a build-system crash then `ENOSPC` — root cause was `~/Library/Developer/Xcode/DerivedData` filling the disk to 97% (18GB, mostly a 12GB compilation cache plus stale per-project folders from earlier sessions). Cleared it (disposable build output, entirely outside the repository; `git status`/`git diff --cached` confirmed unchanged before and after), which freed the disk and let the builds complete.
- Landing constraint: **resolved.** This work landed together with the decoder core in commit `7e733b3` ("Land validated DVS decoder core"), pushed and aligned with `origin/main`.

## 2026-08-02 (Pipeline output sync, third review correction: reverted the `mode.didSet` lock, race documented as follow-up)
- A further review of the just-landed round-2 `mode.didSet` fix (see the entry directly below) found it incomplete: `@Published`'s synthesized setter writes the backing storage *before* `didSet` runs, so locking inside `didSet` only serializes the `accumulatedStereoInputs`/`cumulativePlatterPosition` cleanup — it does nothing for the `mode` property's own storage read/write, which every unguarded reader elsewhere in the class (`enqueueLiveStereoBuffer`, `makeValidationSnapshot`, `flushDecode`'s early `switch mode`) still races, even the ones taken while holding `processingLock`, because the setter's write doesn't wait for that lock. `testModeTransitionSerializesWithInFlightFlush` only proved the `didSet` body's own cleanup blocks correctly — not that a concurrent `mode` read is race-safe against the assignment itself.
- Per Karl/ChatGPT's review instruction (option A — smallest reviewable diff): reverted rather than attempted a fix. `mode`'s `didSet` in `ScratchLab/Models/TimecodeControlPipeline.swift` is back to unguarded mutation (still resets `accumulatedStereoInputs`/`cumulativePlatterPosition`, just without the round-2 `processingLock`/`lock` acquisition). Removed `testModeTransitionSerializesWithInFlightFlush` from `ScratchLabDesktopTests/TimecodeLiveTapTests.swift` (47 → 46 tests). The underlying `mode`/configuration cross-thread race is real, pre-existing, and still unaddressed — now tracked as its own follow-up task in `TASKS.md` rather than silently dropped.
- Re-verification: macOS Debug build succeeded; `build-for-testing` succeeded; `TimecodeLiveTapTests` 46/46, `TimecodeDecoderTests` 27/27, `TimecodeValidationTests` 23/23, `TimecodeStabilityFilterTests` 29/29 (via `xcrun xctest` against the rebuilt `.xctest` bundle). Thread Sanitizer (`-enableThreadSanitizer YES build-for-testing` then `test-without-building -only-testing:ScratchLabDesktopTests/TimecodeLiveTapTests`): 46/46, zero warnings, 2 consecutive runs — expected, since without the removed deterministic test nothing in the suite forces the actual race interleaving, so TSan silence here is not proof the race is safe. `python3 scripts/test_capture_pipeline.py` 47/47. iOS build (`ScratchLab` scheme, `generic/platform=iOS`, code signing disabled) succeeded. `git diff --check` passed.
- Final acceptance pass, `scripts/build.sh all` (run separately, after the above): exited **65**, `set -e` tripping at `run_mac_tests` exactly as always — `build_ios`/`build_mac`/`build_watch` never ran (the separately-run macOS Debug and unsigned iOS builds above stand in for those stages and both succeeded). `run_capture_pipeline_fixtures` 47/47. `run_mac_tests` (full plan, both configured test plans): 2309 XCTest tests, 19 skipped, 8 assertion-level failures per pass (5 distinct methods, the same established baseline — `testAdvancedDiagnosticsExplainCameraAndTickActivity`, `testClearCrossfaderMappingRemovesMapping`, `testCoachPauseHoldsPoseFromAudioTimeAndAvoidsPausedTimelineWork`, `testMIDILearnUpdatesFromListeningToLearnedStateWhenCCArrives`, `testReceivingMIDICCUpdatesLastMIDIEventSummary`), plus a separate Swift Testing run of 157 tests / 4 issues per pass (`CrossPlatformNotationParityTests`/`NotationSheetTests`, same established baseline) — no new failures. Within this run: `TimecodeLiveTapTests` 46/46, `TimecodeDecoderTests` 27/27, `TimecodeValidationTests` 23/23, `TimecodeStabilityFilterTests` 29/29. This plain invocation does not pass `-enableThreadSanitizer`, and its log contains zero TSan diagnostics — see the correction on the round-2 entry below regarding the previously claimed 92 playback-controller warnings.
- Nothing staged, committed, pushed, reset, restored, stashed, or discarded; the repository index remained empty throughout; every other pre-existing dirty file untouched. Same landing constraint as below: not independently committable against `HEAD`, must land with the decoder core.

## 2026-08-02 (Pipeline output synchronization: revised after review correction to a minimal-critical-section design)
- This entry supersedes the original coarse-lock version of this task. Review found the whole-body `outputLock` reintroduced the exact SwiftUI stall risk (0.4–2.1s) the pipeline had previously been measured to have, and `makeValidationSnapshot` still didn't get an atomic multi-property read (each property was its own separately-locked access). Revised, not restarted from scratch — same root cause, same ten outputs, different locking/publishing shape.
- Root cause (unchanged): the ten decoded outputs (`latestPlatterTimeline`, `latestDecodeResult`, `latestDiagnosis`, `latestClassification`, `counters`, `currentDirection`, `currentRate`, `lastDropReason`, `signalHealth`, `lastBufferReceivedAt`) were plain unsynchronized stored properties, mutated by `pushStereoBuffer`/`flushDecode`/`reset`/`resetCounters` from `DVSControlProcessingLoop`'s background queue while SwiftUI view bodies and DEBUG main-thread self-test buttons touch them on the main thread.
- Files changed: `ScratchLab/Models/TimecodeControlPipeline.swift`, `ScratchLabDesktopTests/TimecodeLiveTapTests.swift` (44 → 46 tests: 2 probabilistic busy-loop tests removed, 5 deterministic hook-based tests added). `TASKS.md`, `DEV_LOG.md`, `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md` updated after verification.
- Revised design: a private `OutputState` struct bundles all ten outputs as one `Equatable` value. `outputLock` (`NSLock`) now guards only a single published `_publishedOutput: OutputState` — a trivial copy-on-read, trivial swap-on-publish, never held across decode/diagnostics/filter/adapt/recorder work or `objectWillChange` delivery. A new `processingLock` (`NSLock`) serializes the four writer methods and the pipeline-internal state a transaction mutates alongside its output (`stabilityFilterState`, `cumulativePlatterPosition`, etc.); each transaction builds its result into a local `var state: OutputState` and calls `publish(state)` — **still holding `processingLock`**, released only afterward, so a second transaction can never start from a stale snapshot and clobber the first's update (an ordering bug caught and fixed during this same revision, before any test run). The ten public getters became plain computed properties reading the published snapshot. `makeValidationSnapshot` now captures the snapshot once and derives every field from that one value — the atomic read the review asked for. `reset()` publishes one fresh default `OutputState()`; `resetCounters()` mutates only `counters`/`lastDropReason` on a locally-read copy. A `#if DEBUG`-only `debugPrePublishHook` was added inside `publish(_:)` as a deterministic test seam (always `nil` outside tests).
- Tests replaced: removed the two remaining probabilistic busy-loop tests (`testResetProducesCoherentDefaultStateEvenWithConcurrentReaders`, `testResetCountersProducesCoherentCounterStateWithoutTouchingOtherOutputs`) that raced a reader against a writer and hoped to catch a violation within a bounded loop. Added 5 deterministic tests built on `debugPrePublishHook`, which pauses a transaction after processing finishes but before publish: `testReaderDuringInFlightFlushObservesOnlyPreOrPostTransactionSnapshot`, `testOutputGettersReturnImmediatelyWhileTransactionIsPaused` (the direct stall-regression check), `testMakeValidationSnapshotNeverMixesTransactionGenerations`, `testResetPublishesOneCompleteStateNotPartial`, `testResetCountersPublishesCountersAndDropReasonAtomically`. Kept `testConcurrentDebugMainThreadAndDVSWorkerMutationSerializeSafely` (its outcome is deterministic under `processingLock` serialization, not a race to catch) and the two pre-existing coalesced-publication tests. Removed the stale doc comment describing the old per-property-lock "mirrored-field agreement" invariant.
- **Second review correction, same pass:** (1) `objectWillChange` could still fire while `processingLock` was held — `publish(_:)` was split into `swapPublishedOutput(_:) -> Bool` (swap-only, under `outputLock`, still while `processingLock` is held) and a separate `sendChangeNotification()` call that every transactional method's `defer` now makes only *after* `processingLock.unlock()`. (2) `mode`'s `didSet` mutated `accumulatedStereoInputs`/`cumulativePlatterPosition` (ingestion-`lock`/`processingLock`-protected state) with no lock at all — fixed by wrapping that clear in `processingLock` → `lock`, the same documented order the four transactional methods use. (3) Doc comments were corrected: the atomic swap makes any *single* getter call (or `makeValidationSnapshot`) generation-coherent, but two *separate* getter calls can legitimately straddle two different, each complete, generations if a transaction publishes between them — that's expected, not a bug, and is now stated explicitly above the getters and in `makeValidationSnapshot`'s doc comment.
- Tests, second pass: added `testModeTransitionSerializesWithInFlightFlush` (pauses a flush via `debugPrePublishHook`, proves a concurrent `mode` change blocks until release — the regression check for fix (2)); strengthened `testOutputGettersReturnImmediatelyWhileTransactionIsPaused` (now waits for the paused writer to finish before clearing the hook/ending the test); `testResetCountersPublishesCountersAndDropReasonAtomically` (now establishes and asserts a real non-nil pre-reset `lastDropReason` via `feedSilence`, since a successful decode always clears it to nil); `testResetPublishesOneCompleteStateNotPartial` (now captures and asserts all ten pre-reset outputs, not 3 of 10). `TimecodeLiveTapTests` 46 → 47.
- Verification (this revision, after both rounds of correction): macOS Debug build succeeded; `build-for-testing` succeeded; `TimecodeLiveTapTests` 47/47 run explicitly under both test plans (`ScratchLab`, `ScratchLab 2`), 2 consecutive runs each, 0 failures; `TimecodeDecoderTests` 27/27, `TimecodeValidationTests` 23/23, `TimecodeStabilityFilterTests` 29/29 (same invocations, both plans, all clean). Thread Sanitizer (`-enableThreadSanitizer YES build-for-testing` then `test-without-building` on `TimecodeLiveTapTests`, rebuilt after the second-round fixes): 47/47, zero warnings, 2 consecutive runs. `python3 scripts/test_capture_pipeline.py`: 47/47. `scripts/build.sh all`: reproduced the exact established unrelated baseline failures, no new failures, stopped via its own `set -e` (exit 65) after the macOS XCTest plan stage as always. **Attribution correction (final acceptance pass, 2026-08-02):** the original claim here — that this stage's own "Thread Sanitizer pass" surfaced 92 pre-existing warnings, all in `ScratchSamplePlaybackController.positionDidChangeOnQueue`/`loadOnQueue`/`resetDVSGrainTiming` (from `testConcurrentLoadAndPositionDidChangeDoNotCrash`) — was inaccurate: the plain `scripts/build.sh all` invocation does not enable Thread Sanitizer, and the final post-round-3 `scripts/build.sh all` run (see the entry above) produced zero TSan diagnostics. No run in this session's evidence actually produced these 92 warnings from `scripts/build.sh all`; retained here only as an uncorrected historical claim, not verified provenance. `git diff --check` passed on both changed files.
- Landing constraint: **resolved.** This change landed together with the decoder core in commit `7e733b3` ("Land validated DVS decoder core"), pushed and aligned with `origin/main`.

## 2026-08-02 (Settled the canonical raw direction-sign convention — read-only audit, NO CHANGE REQUIRED)
- Selected task: trace the classifier/decoder sign mismatch flagged as an open question in the decoder reconciliation task (entries below) end-to-end through the actual compiled Swift paths against real Rane ONE MKII fixtures, and produce one canonical contract.
- Files changed: none — read-only. A faithful, out-of-repo Python transcription of `TimecodeSignalDiagnostics.estimatePhaseOffset` and `TimecodePhaseDecoder.extractPhaseDelta` (never entering the repo) was run against real fixture WAVs read in place. `TASKS.md`, `DEV_LOG.md`, `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md` updated after verification.
- Physical channel order: Rane physical pair 3/4 maps physical channel 3 → left, physical channel 4 → right — confirmed identically in both the live pair-selection path and the fixture recorder in `TimecodeCMSampleBufferAdapter.swift`.
- Measurement: five real fixtures (`steady_normal`/`slow_forward`/`fast_forward` confirmed forward; `slow_backward`/`fast_backward` confirmed backward), 115 total 512-frame windows across all three speed tiers and both directions, zero exceptions.
- Finding: the decoder's raw, uncalibrated direction is inverted relative to true physical platter direction on every window measured (115/115) — raw output says "backward" during confirmed-forward motion and "forward" during confirmed-backward motion. `TimecodePrototypeProfile.raneOneMkiiDebug().invertDirection = true` correctly compensates this in every case; final calibrated pipeline direction/velocity matched the fixture catalog's ground truth 115/115.
- Also found: `TimecodeSignalDiagnostics.estimatePhaseOffset` (cross-correlation-lag, display/classification only, never consumed by the decoder or pipeline) uses the opposite sign convention from the decoder's atan2-subtraction phase delta, and its own "Positive = R leads L" doc comment is inaccurate relative to what its own code computes. Both are display-only and sign-insensitive for the `isQuadrature` classifier — no functional effect on production direction or velocity.
- Conclusion: **NO CHANGE REQUIRED.** Decoder raw sign rules, `TimecodeControlPipeline` calibration, and the Rane profile's `invertDirection = true` are correct and mutually consistent, confirmed against real hardware with zero exceptions — none were changed. Correcting the diagnostics doc comment is a separate, optional, display-only cleanup, not a decoder-core blocker.
- Build result: no repository code changed; the harness lived entirely in scratch space. Hardware fixtures remained local/untracked — none added, copied, staged, or redistributed. The repository index remained empty throughout. Nothing was staged, committed, pushed, reset, restored, stashed, or discarded.

## 2026-08-02 (Post-reconciliation test-quality review: strengthened five weak assertions in TimecodeDecoderTests, no behavior change)
- Selected task: a code review follow-up on the just-completed decoder-test reconciliation (entry immediately below) — not a repeat of that task. Five assertions in the 27-test suite currently passed without proving their named behavior.
- Files changed: `ScratchLabDesktopTests/TimecodeDecoderTests.swift` only. `TASKS.md`, `DEV_LOG.md`, `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md` updated after verification. No production file touched.
- Findings: `testAdapterPreservesDirection` asserted only non-nil, never that position actually moved; `testAdapterDropsLowConfidenceSamples` decoded synthetic audio, so it could pass while exercising zero low-confidence frames (accepted both nil and non-nil outcomes); `testConfidenceDecreasesForWeakInput`/`testDecoderChannelImbalanceLowersConfidence` wrapped their confidence comparison in `if frames.count > 0`, so a silent zero-frame decode would skip the comparison and still pass; `testCorrelationGateIsAmplitudeIndependent` never compared loud vs. quiet frame counts or confidence, only that each individually exceeded zero. Also found "Added 8" in the prior pass's documentation undercounted the 9 tests actually listed.
- Fixes: `testAdapterPreservesDirection` now requires ≥2 samples and asserts `last.position > first.position` (forward) / `< first.position` (backward), failing loudly instead of silently skipping. `testAdapterDropsLowConfidenceSamples` was rewritten to use deterministic constructed `TimecodeDecodedFrame`/`TimecodeDecodeResult` values (confidences `[0.95, 0.85, 0.92, 0.5, 0.99]`) and asserts exactly 3 samples survive a 0.9 threshold — proving actual filtering, not decoded-audio luck. The two conditional confidence comparisons became unconditional, first requiring `frames.count > 0` on the degraded/imbalanced side before comparing. `testCorrelationGateIsAmplitudeIndependent` now asserts equal accepted-frame counts and `XCTAssertEqual(..., accuracy: 0.05)` on average confidence between loud and quiet — justified because both amplitudes sit well above the confidence formula's signal-level-score ceiling (10× `silenceThresholdRMS`), so `levelScore`/`channelBalance` saturate to ~1.0 for both, leaving only the amplitude-independent `correlationMagnitude` term the test exists to prove is stable. Corrected "Added 8" to "Added 9" in all three docs.
- Each strengthened assertion was verified empirically before finalizing (ran the focused suite after each change), not assumed to hold — all passed on the first run, so no synthetic-input parameters needed adjustment. No new test added, no protected behavior changed: same 27 tests, stronger proof for 5 of them.
- Build result: `TimecodeDecoderTests` 27/27 in both test-plan configurations; `TimecodeValidationTests` 23/23 + `TimecodeStabilityFilterTests` 29/29 as adjacent regressions in both configurations (79/79 combined, 0 failures, twice); `python3 scripts/test_capture_pipeline.py` 47/47; macOS Debug build succeeded; `scripts/build.sh all` reproduced the identical established unrelated baseline (Advanced diagnostics, MIDI learn/receive/clear, Coach pause, `CrossPlatformNotationParityTests`, `NotationSheetTests`) with no new failures; `git diff --check` passed. `TimecodePhaseDecoder.swift`, `TimecodeControlPipeline.swift`, `TimecodePlatterAdapter.swift` diffs against HEAD remained byte-identical to before this task. The repository index remained empty throughout. Nothing was staged, committed, or pushed.
- Landing constraint: **resolved.** `TimecodeDecoderTests.swift` landed as part of the decoder-core extraction in commit `7e733b3` ("Land validated DVS decoder core"), pushed and aligned with `origin/main`.

## 2026-08-02 (Reconciled TimecodeDecoderTests with the measured hardware contract; no production behavior changed)
- Selected task: the first unchecked `TASKS.md` item — reconcile the obsolete synthetic decoder tests (falsified by the prior pass's 15-fixture hardware measurement) with the measured physical DVS contract, test-only, no production behavior change.
- Root cause: `ScratchLabDesktopTests/TimecodeDecoderTests.swift`'s decoder rewrite had already removed the two explicitly obsolete tests (`testTimecodeDecoderConstantQuadratureProducesNearZeroRate`, `testTimecodeDecoderVelocityScalesWithPhaseRate`), but that removal alone was not verified as reconciliation — a full test-by-test diff against `git show HEAD:...TimecodeDecoderTests.swift` (20 tests) found it had also silently dropped several genuinely architecture-agnostic tests along with the obsolete ones, leaving real coverage gaps (adapter low-confidence filtering, adapter rate clamping, adapter source labeling, adapter nil-for-empty-result, mixed-batch counter attribution, channel-imbalance confidence, and three amplitude-independence correlation-gate regressions).
- Files changed: `ScratchLabDesktopTests/TimecodeDecoderTests.swift` (18 → 27 tests). `TASKS.md`, `DEV_LOG.md`, `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md` updated after verification. No other file touched.
- Kept unchanged (16 tests, already architecture-agnostic and correct): silence rejection, clipping rejection, forward/backward direction, stable-offset no-direction-changes, small-jitter-no-flip, phase dead-band → unknown, reversal direction-change counters, phase-wrap near ±π, mismatched-frequency fails closed, invertDirection-is-pipeline-only, adapter-preserves-direction, actual-input-duration displacement, adapter starting-position continuity, weak-signal/noise-contamination confidence.
- Enhanced 2: `testSpeedScalesWithFrequency` gained a third "normal" (1000 Hz) tier, asserting slow<normal<fast proportional ordering matching the measured hardware bands (slow ~300-450 Hz / normal ~950-1050 Hz / fast ~1500-2300 Hz); `testEmptyInputProducesEmptyResult` gained a `dropoutReason == .silence` assertion. Added a doc comment on `testNormalCarrierSpeed` identifying it as the physically-correct reconciliation of the removed near-zero-rate test.
- Added 9 (all built on the dirty file's own existing `makeStableDirectionInputs`/`makeStereoInput` helpers — the obsolete `makePhaseProgression` convention was not reintroduced): `testOppositeQuadraturePolarityProducesOppositeDirectionSameMagnitude` (physically-grounded: opposite quadrature polarity ⇒ opposite raw direction, equal rate magnitude at the same carrier frequency), `testAdapterDropsLowConfidenceSamples`, `testAdapterClampsExtremeRate`, `testAdapterSourceLabelIsTimecodeFixture`, `testAdapterReturnsNilForEmptyResult`, `testDecoderCountersTrackMixedDropsAndDecodes`, `testDecoderChannelImbalanceLowersConfidence`, `testCorrelationGateIsAmplitudeIndependent` + `testMismatchedFrequencyStillRejectedAtSameRealisticAmplitude` (amplitude-independence pair guarding a real historical units-mismatch regression).
- Dropped: `testNoHardwareRequired` (vacuous `XCTAssertTrue(true)` stub, protects nothing) and HEAD's `testDecoderCountsDirectionChanges`/`testTimecodeAdapterPreservesTrustedDirection` variants, redundant with the dirty file's already-clean equivalents.
- Build result: `TimecodeDecoderTests` 27/27 in both test-plan configurations (`ScratchLab`, `ScratchLab 2`); `TimecodeValidationTests` 23/23 and `TimecodeStabilityFilterTests` 29/29 as adjacent regressions in both configurations (79/79 combined, 0 failures); `python3 scripts/test_capture_pipeline.py` 47/47; macOS Debug build succeeded; `scripts/build.sh all` reproduced exactly the established unrelated baseline (Advanced diagnostics, MIDI learn/receive/clear, Coach pause, `CrossPlatformNotationParityTests.notationCanvasUsesSharedRenderer`, `NotationSheetTests.templateDemoStillOwnsBabyScratchTemplateLabel`) with no new failures, exit 65 before the iOS/macOS/watchOS build legs exactly as always on this baseline; `git diff --check` passed on the changed test file. `TimecodePhaseDecoder.swift`, `TimecodeControlPipeline.swift`, `TimecodePlatterAdapter.swift`, and every other production file were read only, never edited — confirmed via `git status`/`git diff --stat` showing the same pre-existing dirty file set, byte-identical production diffs to before this task. The repository index remained empty throughout. Nothing was staged, committed, pushed, reset, restored, stashed, or discarded; every pre-existing dirty file is untouched. No hardware fixture file was added, copied, staged, or redistributed — deterministic synthetic inputs only.
- Landing constraint: **resolved.** `TimecodeDecoderTests.swift` landed as part of the decoder-core extraction in commit `7e733b3` ("Land validated DVS decoder core"), pushed and aligned with `origin/main`.

## 2026-08-02 (Decoder physical-contract reassessment: dirty ZCR/frequency-ratio decoder matches all 15 real Rane fixtures; earlier "constant quadrature = stationary" diagnosis retracted)
- Selected task: Karl asked for a read-only reassessment of the dirty `TimecodePhaseDecoder` rewrite against real captured hardware, after a prior session's diagnosis had incorrectly assumed a sustained/constant quadrature carrier represents a stationary DVS platter.
- Files: none (measurement only — read-only out-of-repo Python harness plus one already-existing XCTest run); this documentation pass touches only `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`, `DEV_LOG.md`, `TASKS.md`.
- Correction: the earlier conclusion that constant/unchanging quadrature represents stationary motion is wrong and is superseded. Real `stationary.wav` is silent in 23/23 512-frame windows (RMS below the 0.001 gate on every window, zero ZCR-detectable carrier), matching the fixture catalog's own `expectedCarrierHz: [0,0]`. There is no real hardware example of "carrier present but not moving" in this fixture set.
- Measurement: a read-only, out-of-repo Python re-implementation of both the committed (`git show HEAD`) phase-derivative decoder and the dirty measured-frequency/nominal-frequency ratio decoder, run against all 15 real Rane ONE MKII fixture excerpts (`stationary`, `steady_normal`, `slow_forward`, `slow_backward`, `fast_forward`, `fast_backward`, `slow_reversals`, `fast_reversals`, `fine_flicks`, `baby_scratch`, `start_stop`, `needle_lift`, `position_start`, `position_middle`, `position_end`), chunked into 512-frame windows and calibrated with the Rane ONE MKII DEBUG profile (`invertDirection=true`) exactly as `TimecodeFixtureValidationTests.replayThroughRanePipeline` does.
- Result: sustained ~1 kHz quadrature in `steady_normal`/`position_start`/`position_middle`/`position_end` is confirmed genuine, ground-truth 1x forward motion, not stationary. The dirty decoder decodes all 15 fixtures cleanly — stable, correctly-signed, monotonic velocity tracking real carrier frequency through slow/normal/fast one-way motion, smooth deceleration-through-zero-reversal on `slow_reversals`/`fast_reversals`/`fine_flicks`/`baby_scratch`, and clean decay-to-stop with zero false reversals on `start_stop`/`needle_lift`. The committed HEAD phase-derivative decoder fails structurally away from the fixed 1000 Hz reference: 100% low-confidence drop (zero decoded frames) on all four slow/fast one-way fixtures, and where it does emit frames it produces direction chatter (rapid forward/backward flapping on the unambiguous `steady_normal`/`position_*` ground truth) and physically implausible rates (up to ~44x).
- Cross-validation: rebuilt the test target (`xcodebuild build-for-testing`) and ran the already-existing `TimecodeFixtureValidationTests.testHardwareFixtureExcerptsReplayThroughProductionDecoderAndRaneCalibration` via `xcodebuild test -only-testing:...` against the real compiled dirty decoder — passed, 0 failures, confirming the Python re-implementation's numbers are faithful.
- Conclusion: the dirty ZCR/measured-frequency-ratio rewrite is retained, not repaired toward or abandoned in favor of the committed phase-derivative model — reversing the prior session's "abandon" recommendation, which rested on the now-falsified premise. HEAD's own `testTimecodeDecoderConstantQuadratureProducesNearZeroRate`/`testTimecodeDecoderVelocityScalesWithPhaseRate` (built on an artificial per-buffer `phaseStep`/`rightPhaseOffset` progression with frequency pinned at nominal) encode an obsolete synthetic-prototype contract that this hardware evidence contradicts, and must not block the hardware-grounded decoder; they should not be restored as-is. Remaining work before any extraction toward production wiring: reconcile the obsolete synthetic decoder-test expectations with the measured hardware contract; settle one canonical raw direction-sign convention (separate, already-flagged open question — the classifier/decoder sign mismatch and what `invertDirection` is actually compensating for); assess low-cycle-count ZCR quantization bias near genuinely weak/near-silent carrier; add pipeline output synchronization separately; and, specifically when this decoder is later extracted/wired, surgically remove the obsolete `minConfidence` initializer argument from the HEAD-era fixture-validation helper (`TimecodeFixtureValidationTests` still constructs decoders with a `minConfidence:` parameter the dirty `TimecodePhaseDecoder` no longer declares).
- Hardware fixtures (`ScratchLabDesktopTests/Fixtures/Timecode/*.wav` + the two JSON expectation catalogs) remain local/untracked, per the existing licensing/redistribution posture — not added, copied into git, staged, or redistributed in this pass, and still blocked on separate redistribution approval.
- Build result: no production code or tests were modified during the measurement pass. The repository index remained empty throughout. Nothing was committed, staged, pushed, reset, restored, stashed, or discarded; every pre-existing dirty file is untouched. This documentation pass touches only `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`, `DEV_LOG.md`, `TASKS.md`; `git diff --check` passed on those four files.

## 2026-08-01 (Dense estimator: fixed a second fail-closed gap — non-monotonic rejection also leaked the envelope)
- Selected task: a follow-up review found the `nonMonotonicFrameIndex` branch in `TimecodeSignedRateDenseEstimator.consume` still called `resetContinuity()` only, the same class of bug just fixed on the forward-gap path in the entry immediately below — an overlapping/rewound block breaks stream continuity exactly like a forward gap does, and the next accepted recovery stream may begin at an unrelated frame origin, so the persisted envelope must not outlive the rejection either.
- Files changed: `ScratchLab/Models/TimecodeSignedRateDenseEstimator.swift`, `ScratchLabDesktopTests/TimecodeSignedRateDenseEstimatorTests.swift`.
- Fix: the non-monotonic branch now calls the existing `resetContinuityAndEnvelope()` instead of `resetContinuity()` alone. The valid contiguous and gap paths are untouched. Updated the `nonMonotonicFrameIndex` case's doc comment to state that envelope state resets too, matching the `gapDetected` case's existing wording.
- Added `testNonMonotonicFrameIndexAfterHighAmplitudeMotionDoesNotInheritStaleEnvelopeOrFabricateDirection`: establishes a loud motion envelope, submits an overlapping/rewound block (receives `.nonMonotonicFrameIndex`), then submits a short fresh silent recovery stream and proves it reports a single honest `.noSignal` sample from its own evidence rather than silently dropping to empty output or reporting an `.available` direction inherited from the pre-rejection envelope. Independently verified this test fails against the pre-fix code (0 samples where 1 was expected) and passes against the fix.
- No fixture remeasurement: valid fixture input never enters the non-monotonic rejection path, so the audit numbers from the entry below are unaffected.
- Build result: focused `TimecodeSignedRateDenseEstimatorTests` 17/17 (16 + 1 new) + `TimecodeSignedRateExcursionStabilizerTests` 10/10 passed via a real `xcodebuild test` host launch in both test-plan configurations. `git diff --check` passed on tracked changes and both untracked Swift files; `git diff --cached` is empty. Nothing was committed, staged, pushed, reset, or discarded — no `git add -N`/`git reset` was used in this pass (that was only needed, and already cleared, in an earlier pass — see the entry below).

## 2026-08-01 (Dense estimator: fixed a fail-closed violation and a chunking-dependent envelope bug, re-measured)
- Selected task: a code review of the just-built `TimecodeSignedRateDenseEstimator` (entry immediately below) found two real defects, both violating properties the type's own doc comment and existing tests claimed to guarantee.
- Files changed: `ScratchLab/Models/TimecodeSignedRateDenseEstimator.swift`, `ScratchLabDesktopTests/TimecodeSignedRateDenseEstimatorTests.swift`, `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`, `DEV_LOG.md`, `TASKS.md`.
- Root cause 1 (fail-closed violation): the frame-gap branch in `consume` called `resetContinuity()`, which clears crossing timestamps but left the persisted envelope (`envelopeLeft`/`envelopeRight`/`hasEnvelope`) untouched — a loud pre-gap envelope survived into post-gap classification, contradicting the documented "never re-derives a crossing from data before a detected gap" contract (the envelope, not just crossings, feeds the silence gate and the direction-ambiguity floor).
- Root cause 2 (chunking-dependent smoothing): the envelope was a single block-aggregate-RMS blend applied once per `consume` call using that call's own block duration — for a variable-amplitude signal, different block boundaries produced different envelope histories for the identical underlying audio, contradicting the "chunking-invariant" claim. Every existing test used constant amplitude and never exposed this.
- Fix: replaced the block-level blend with a per-real-sample exponential power tracker (`power += alpha · (sample² − power)`, `alpha` from the real elapsed time between consecutive samples — always exactly `1/sampleRate` for contiguous frames, independent of block boundaries — and `envelopeTimeConstant`). The gap path now calls a new `resetContinuityAndEnvelope()` instead of `resetContinuity()` alone. The once-per-call silence gate remains intentionally block-shaped — narrowed and now explicitly tested as such, rather than claimed as fully invariant.
- Added 4 deterministic tests, each independently verified to fail against the pre-fix implementation and pass against the fix: variable-amplitude chunking invariance (one block vs. irregular blocks vs. per-sample blocks); gap-then-short-silence honest no-signal; gap-then-weak-signal no stale-envelope inheritance/no fabricated direction; and a pinned test of the exact narrowed no-signal-marker-is-block-shaped guarantee.
- Re-measurement (all 15 full local hardware captures, unmodified `TimecodeSignedRateExcursionStabilizer`, same three previously-documented threshold/gap configurations — no new tuning): at the default `0.05`/`0.25s`, `start_stop`/`needle_lift` remain at zero false reversals; `fine_flicks` genuine commits are now 10 (was 5 pre-fix) against an 84 baseline, `baby_scratch` 20 (was 22 pre-fix) against a 307 baseline. At `0.02`: `start_stop`=1 false, `needle_lift`=2 false, `fine_flicks`=36, `baby_scratch`=105. At `0.01`: `start_stop`=5 false, `needle_lift`=2 false, `fine_flicks`=64, `baby_scratch`=231. `slow_reversals`/`fast_reversals` unaffected. Conclusion unchanged: no threshold cleanly separates the false near-stop evidence from genuine fast/fine content; the numbers shifted a few percent because the math is now correct, the trade-off itself did not change.
- Also corrected: a self-contradiction in `AI_HANDOFF.md`/`DEV_LOG.md`/`TASKS.md` claiming both that `git add -N`/`git reset` was used and that "nothing was... reset" — now states plainly that the temporary staging was used and cleared in the same pass, leaving a clean index. Also reconciled a `fine_flicks` baseline denominator in `TASKS.md` that had been mis-copied as `230` instead of the correct `84`.
- Build result: focused `TimecodeSignedRateDenseEstimatorTests` 16/16 + `TimecodeSignedRateExcursionStabilizerTests` 10/10 passed; 16 DVS-adjacent suites passed via a real `xcodebuild test` host launch in both test-plan configurations, with one confirmed non-reproducing environmental flake (`DVSOfflineGrainRendererTests.testOfflineRenderReconstructsSlowReversalsCapture`, in an untouched file, crashed once mid-run and passed cleanly 3/3 on an isolated rerun in both configurations); `python3 scripts/test_capture_pipeline.py` passed 47/47; `plutil -lint` passed; `git diff --check` passed on tracked changes and both untracked Swift files; `git diff --cached` is empty; `scripts/build.sh all` ran 2,288 tests and exited 65 only on the established unrelated baseline (Advanced diagnostics, MIDI mapping/learn, Coach pause, `CrossPlatformNotationParityTests`, `NotationSheetTests.templateDemoStillOwnsBabyScratchTemplateLabel`) before stopping via its own `set -e`, with no new failure. Nothing was committed, staged, pushed, or discarded; the one `git reset` nuance is explained above, not omitted.

## 2026-08-01 (Clean-room dense signed-rate estimator — built and measured offline, same trade-off as every prior candidate)
- Selected task: design and measure a lawful clean-room, platform-agnostic dense signed-rate estimator that could eventually feed the completed `TimecodeSignedRateExcursionStabilizer`, per the explicit next-step recorded in `AI_HANDOFF.md`/`TASKS.md` after that stabilizer landed. Kept fully disconnected from live capture, `TimecodePhaseDecoder`, `TimecodeControlPipeline`, playback, notation, export, persistence, and UI, per instruction.
- Files changed: `ScratchLab/Models/TimecodeSignedRateDenseEstimator.swift` (new), `ScratchLabDesktopTests/TimecodeSignedRateDenseEstimatorTests.swift` (new), `ScratchLabDesktopTests/TimecodeFixtureValidationTests.swift`, `ScratchLab.xcodeproj/project.pbxproj`, `TASKS.md`, `DEV_LOG.md`, `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`.
- Design: `TimecodeSignedRateDenseEstimator` treats the stereo pair as a generic quadrature signal (left ≈ A cos θ, right ≈ A sin θ) and applies the standard, decades-old quadrature-encoder identity at every zero crossing on either channel — direction = (crossing edge slope) × (other channel's instantaneous sign) × a fixed per-channel calibration constant, derived from d(cos θ)/dt = −sin θ·θ′ and d(sin θ)/dt = cos θ·θ′. An early draft used only the other channel's raw sign at a crossing, which recovers phase quadrant but not direction — caught and fixed before any measurement was trusted. This emits one dense sample per detected crossing (roughly twice the local carrier frequency) instead of one sample per multi-hundred/-thousand-frame decode window, and shares no code, constants, tables, or architecture with xwax's per-sample LFSR/Schmitt-trigger bitstream design — nothing from the prior out-of-repo xwax study was read or reused. Cross-block state (last raw sample/crossing time per channel, plus an exponential envelope tracker using a real-elapsed-time constant rather than a per-call blend factor, so results are identical regardless of how a caller chunks a continuous stream) persists across blocks; gaps discard that continuity before processing the new block so no sample is ever computed from evidence spanning a dropout.
- Measurement (production-callback-sized 4,800-frame blocks, all 15 full local hardware captures, output density 900–6,900 samples/sec depending on fixture speed): at the stabilizer's default `reversalAreaThreshold=0.05`/`maximumIntegrationGap=0.25s`, the estimator reaches **zero** committed false reversals on both `start_stop` (42.4s) and `needle_lift` (44.3s) — matching or beating every previously tested candidate in this investigation (four ScratchLab-side heuristics plus the xwax-architecture measurement) — but retains only 5 and 22 genuine reversal commits on `fine_flicks`/`baby_scratch` against those fixtures' much larger established genuine-transition baselines (a ~93–94% loss). Sweeping the threshold down to `0.02` then `0.01` (per instruction not to tune against a single fixture) recovers progressively more genuine content (31→55 on `fine_flicks`, 100→230 on `baby_scratch`) but reintroduces false reversals on `start_stop`/`needle_lift` (1–5 and 2 respectively). `slow_reversals`/`fast_reversals` retained genuine transitions in both directions at every threshold tested.
- Conclusion: this is the same fundamental trade-off every prior candidate in this investigation has hit — no tested threshold cleanly separates `start_stop`/`needle_lift`'s false near-stop evidence from genuine `fine_flicks`/`baby_scratch` content. The estimator's real contribution is a much denser, cross-block-continuous, honestly-stated raw signal (two to three orders of magnitude denser than the existing per-window decoder) — a genuine architectural improvement in signal quality — not a solved trade-off. No candidate is recommended for production integration as measured; nothing was wired into any production path.
- Build result: focused `TimecodeSignedRateDenseEstimatorTests` passed 12/12 in both test-plan configurations; the always-run fixture-excerpt smoke test and the opt-in full-capture measurement (`SCRATCHLAB_RUN_FULL_DVS_DENSE_ESTIMATOR_AUDIT=1`) both passed against all 15 fixtures in both configurations, plus two additional manual threshold sweeps; 16 DVS-adjacent suites passed 462 executions with 7 expected skips in both configurations; `python3 scripts/test_capture_pipeline.py` passed 47/47; `scripts/build.sh all` stopped (exit 65) at exactly the established unrelated baseline (Advanced diagnostics/MIDI learn-receive-clear/Coach pause, `CrossPlatformNotationParityTests`, `NotationSheetTests.templateDemoStillOwnsBabyScratchTemplateLabel`) with no new failures; direct macOS, unsigned iOS (with watch-companion embedding), and unsigned watchOS builds all succeeded; `plutil -lint` passed on `project.pbxproj`; `git diff --check` passed on tracked changes and on both new untracked files (checked via `git add -N`/`git reset`). Nothing was committed, staged, or pushed; `git add -N`/`git reset` was used only temporarily to bring the new files into `git diff --check`'s scope and was cleared in the same pass, leaving the index clean; every pre-existing dirty file was preserved and nothing was discarded.

## 2026-08-01 (Offline xwax vs. TimecodePhaseDecoder comparison — licensing conclusion, architecture, and a real out-of-repo A/B measurement)
- Selected task: after four independent audio-only heuristic families (displacement, timing/hysteresis, raw phase continuity, explicit near-zero state) all failed to safely separate `start_stop`/`needle_lift`'s false near-stop reversals from genuine fine-flick/baby-scratch reversals, Karl directed an offline-only architectural and measurement comparison between `TimecodePhaseDecoder` and the xwax Serato-timecode decoder used by the locally installed Scratch Visualizer app.
- Files changed: `TASKS.md`, `DEV_LOG.md`, `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md` only. No ScratchLab production or test file was added or modified — the actual xwax source and the comparison harness were built and run entirely in a local scratch location outside this git repository, per a hard licensing boundary established before any measurement (see below).
- Build result: no repository code changed, so `scripts/build.sh all`/`python3 scripts/test_capture_pipeline.py` were not run (nothing to regress); `git diff --check` passed on the four changed docs. This is proportionate to a docs-only diff, per the task's own instruction.
- Follow-up notes: read-only `strings`/`find`/`gh api` evidence identified Scratch Visualizer as an openFrameworks app embedding real upstream xwax 1.9 (`lut.c`/`pitch.h`/`timecoder.c`/`timecoder.h` via an `ofxXwax` addon) under GPL-3.0, whose own source header explicitly prohibits copying the code into proprietary software without a separate license from the author (Mark Hills) — so ScratchLab may study xwax's algorithm but must not incorporate its source anywhere in the repo, including test code, without Karl separately obtaining that license. Read (not copied) xwax 1.9's actual `timecoder.c`/`pitch.h`/`lut.c` from upstream GitHub to write an architecture comparison: xwax is a per-sample streaming state machine decoding an absolute LFSR-encoded bitstream (self-correcting via a startup-built position lookup table and a 24-consecutive-correct-bit consensus gate), with direction from instantaneous zero-crossing polarity (fixed-amplitude Schmitt-trigger hysteresis) and pitch from a fully independent alpha-beta filter — versus ScratchLab's stateless per-1024-frame-window least-squares phase fit that conflates direction/velocity/confidence into one computation with no absolute-position self-correction. Built an original (non-xwax) C driver, compiled against unmodified upstream xwax 1.9 entirely in scratch space, and fed it all 15 real hardware fixtures; calibration against `steady_normal` found `serato_2b`/`phono=0` achieves a genuine, sustained absolute-position lock (valid_counter into the thousands), proving these Rane ONE MKII captures carry real Serato NoiseMap content. Gated on that lock, xwax reaches exactly 0 false reversals on both `start_stop` (down from 3,923 raw per-sample flips) and `needle_lift` — better than every previously-tested candidate — but the same lock gate retains only 34%/11%/0%/0% of genuine transitions on `slow_reversals`/`fast_reversals`/`fine_flicks`/`baby_scratch` respectively, i.e. it is nearly blind to ScratchLab's fastest technique content on this hardware, a more severe trade-off than a false-positive rate. Also used the existing `DVSOfflineGrainRendererTests` harness's established finding (0.0000s unexplained gap on `slow_reversals`/`fast_reversals`) to answer the decode-vs-playback question for those two fixtures conclusively (decoder-driven, not rendering); `baby_scratch` itself isn't covered by that harness yet, so that specific case remains a recommended cheap follow-up rather than a settled answer. No candidate is recommended for direct adoption; see `TASKS.md`/`AI_HANDOFF.md` for full numbers and the recommended path (a from-scratch reimplementation of xwax's architecture, not a code integration). Nothing was committed, staged, or pushed; all pre-existing dirty files are untouched.

## 2026-08-01 (Offline-only explicit near-zero/direction-indeterminate state — no safe candidate found)
- Selected task: following Karl's own manual deep-dive into the exact ~12.8s `start_stop` false backward run (two accepted samples 0.277s apart, raw phase offsets only 0.8-2.8° past the ±30° dead-band, a real but one-channel-weakened near-stop carrier), Karl directed an offline-only evaluation of an explicit near-zero/direction-indeterminate state — holding direction `.unknown` until reacquired with sufficient evidence — rather than another post-hoc filter on displacement or velocity alone.
- Files changed: `ScratchLabDesktopTests/TimecodeFixtureValidationTests.swift`, `TASKS.md`, `DEV_LOG.md`, `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`
- Build result: the always-run focused excerpt test passed; the opt-in full-capture audit (`SCRATCHLAB_RUN_FULL_DVS_NEAR_ZERO_STATE_AUDIT=1`) passed against all 15 full local captures across 5 measured candidates; `python3 scripts/test_capture_pipeline.py` passed 47/47; `git diff --check` passed. Required `scripts/build.sh all` ran 2,260 tests and exited 65 only on the same established unrelated Advanced diagnostics, MIDI learn/receive/clear, and Coach pause failures (8 = 4 tests × 2 configurations); every new near-zero-state test passed in both configurations.
- Follow-up notes: added `DVSNearZeroStateCandidate`, a state machine over the existing `rawPhaseWindows` stream (no production file touched). It enters indeterminate immediately on any window failing a combined phase-margin/correlation/balance/cycle-count bar, but requires several consecutive qualifying same-direction windows to (re)commit — asymmetric hysteresis, easy in, hard out. Baseline reused the already-verified `phaseSteps`/`phaseRuns` helpers, reproducing the phase-continuity audit's established counts exactly. An isolation candidate (only `exitWindows` raised from 1 to 2, every other gate left permissive) already removed 89-100% of the false `start_stop`/`needle_lift`/steady-fixture samples from the consecutive-window requirement alone — but the same mechanism, at that mildest setting, cost `slow_reversals` 55% of its genuine transitions (47→21) and `baby_scratch` 11% (307→274), because genuine fast reversals in these fixtures frequently produce only one raw-decodable window before dropping to dead-band, structurally identical to the false samples' own shape. Adding phase-margin/correlation/balance/cycle-count gates on top removed the residual false content (`start_stop` down to 0 at margin≥20°) but cost sharply more genuine content in `baby_scratch` (down to 90, then 60, then 50) and `fine_flicks` (down to 66, 52, 26). `fast_reversals` stayed comparatively cheap throughout (at most 37% lost). No candidate reached 0% genuine loss on `slow_reversals`/`baby_scratch` at any tested strength, so none beats the previously-tested `nearStopHysteresis` candidate (86% false-removal, 0% loss on those two fixtures) on the genuine-retention axis. This is the fourth independent signal family (displacement, timing/hysteresis, raw phase continuity, and now an explicit indeterminate state) that fails to cleanly separate this false-positive class from genuine fast/fine reversal content using audio-only features. No candidate is recommended for production. Nothing was committed, staged, or pushed.

## 2026-08-01 (Offline-only decoder-level phase-continuity audit — no clean separation found)
- Selected task: Karl directed the next safe DVS investigation directly (not via a ChatGPT plan block) — an offline-only decoder-level phase-continuity audit, since the prior reversal-confirmation audit found no post-decode heuristic (displacement floor, consecutive-window confirmation, near-stop hysteresis, or a combination) could safely separate `start_stop`/`needle_lift`'s false backward samples from genuine reversals without destroying real fast/fine-flick/baby-scratch content.
- Files changed: `ScratchLabDesktopTests/TimecodeFixtureValidationTests.swift` only (new "MARK: - DVS Phase-Continuity Audit" section). `TASKS.md`, `DEV_LOG.md`, `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`.
- Design: duplicated `TimecodePhaseDecoder`'s private `extractPhaseDelta`/`zcrFrequency` math exactly (same silence/clipping/correlation/carrier gates, same ±30° dead band) inside the test file — `TimecodePhaseDecoder.swift` was not touched, since its matching helpers are `private` and loosening that would itself be a production-decoder change. Unlike the real decoder, the audit keeps per-channel phase and classifies every window into `silenceOrClipped`/`lowConfidence`/`deadBand`/`decodable`, computes a best-fit unwrapped phase-step residual between consecutive measurable windows (trying both rotation signs since ZCR frequency carries no sign), then groups the decodable stream into same-direction runs and attaches to each run's first window the entry-step residual and the duration of contiguous non-decodable dwell immediately preceding it.
- Bug caught before trusting any measurement: an early run (before applying the same `invertDirection = true` Rane calibration every other fixture replay in this file already uses via `TimecodePrototypeProfile.raneOneMkiiDebug()`) decoded `position_start/middle/end` — single-carrier 1x-forward captures — as uniformly "backward" against the raw pre-calibration phase math. Confirmed this was a calibration-convention mismatch (not a decoder or math bug) by checking `TimecodePrototypeProfile.swift`, then applied the same inversion the pipeline already applies downstream of the raw decoder; the corrected rerun matched all catalog expectations.
- Measurements (1024-frame window, all 15 full local captures): `start_stop` (42.379s) produced 19 false backward runs (47 raw decodable windows — lower in the pipeline than the previously-established 28 post-stability-filter count, since this audit inspects raw decoder output only, with no stability filter applied); `needle_lift` (44.288s) produced 1 false backward run. Against 492 genuine reversal transitions across `slow_reversals`/`fast_reversals`/`fine_flicks`/`baby_scratch`, no raw phase-continuity measure separated the two: entry-residual spans the same 0–180° range for genuine transitions too (`fast_reversals` genuine transitions range 0.92°–172.86°); correlation for `baby_scratch`'s genuine transitions is often lower than `start_stop`'s false runs (196/307 genuine `baby_scratch` transitions below 0.3 correlation, versus 0.103–0.552 for the 19 `start_stop` false runs); and preceding low-confidence/dead-band dwell overlaps almost completely — 16 of 19 `start_stop` false runs have 0–14 windows of preceding dwell, entirely inside the range genuine `fine_flicks`/`slow_reversals` transitions themselves produce (max 14/11). Only 3 of 19 `start_stop` false runs (dwell 21/39/84 windows) and `needle_lift`'s one false run (257 windows = 5.48s) sit outside that overlap, but those are multi-second silence-adjacent spans a plain duration check would already catch, not evidence specific to phase continuity, and cover only a minority of the false samples.
- Conclusion: raw stereo carrier phase continuity does not cleanly separate genuine reversals from false `start_stop`/`needle_lift` reverses — a true reversal's zero-crossing and a false stop/catch's near-stall moment produce the same low-correlation, low-cycle-count, briefly-incoherent carrier signature. No candidate decision rule was implemented, per the explicit instruction to conclude honestly rather than add another heuristic when the measurements do not support one. The stubborn `start_stop` sample near 12.8s already flagged by the prior audit (survives `nearStopHysteresis` because its velocity is above the floor) shows moderate correlation (0.49), a real measured cycle count (9.26), and near-zero entry residual here too — consistent with a genuine, briefly phase-locked backward carrier (most plausibly the platter physically rebounding at the instant of a hand catch), not decoder noise.
- Build result: the always-run focused excerpt test (`testHardwareTransitionExcerptsReportPhaseContinuityAudit`) passed in both scheme configurations; the opt-in full-capture audit (`testLocalFullHardwareCapturesReportPhaseContinuityAudit`, `SCRATCHLAB_RUN_FULL_DVS_PHASE_CONTINUITY_AUDIT=1` via `launchctl setenv`/`unsetenv`) passed in both configurations against all 15 full local captures; `python3 scripts/test_capture_pipeline.py` passed 47/47; `git diff --check` passed. Required `scripts/build.sh all` ran the full desktop XCTest plan in both test-plan configurations (2,258 tests) and exited 65 only on the same established unrelated Advanced diagnostics, MIDI learn/receive/clear, and Coach pause failures (`set -euo pipefail` stops the script at the test stage before the iOS/macOS/watchOS build legs, the repo's normal behavior whenever these pre-existing failures are present); no new failure appeared and every new phase-continuity test passed in both configurations.
- Follow-up notes: no production decoder, callback size, cadence, stability filter, trust gate, playback, notation routing, visualization, persistence, export, or confidence threshold changed. No further audio-only post-decode heuristic is recommended for this specific false-positive class; closing it would likely need a non-audio signal the app doesn't currently capture live, which is a product decision, not an engineering one, and out of scope here. Nothing was committed, staged, or pushed.

## 2026-08-01
- Selected task: align saved DVS videos with fixture audio and classify candidate-window direction changes offline
- Files changed: `ScratchLabDesktopTests/TimecodeFixtureValidationTests.swift`, `TASKS.md`, `DEV_LOG.md`, `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`
- Build result: focused excerpt audit passed; the full saved-capture 1024-frame direction trace passed; capture-pipeline tests passed 47/47; and `git diff --check` passed. Required `scripts/build.sh all` ran both configurations, where the new audit passed and only the documented unrelated Advanced diagnostics, MIDI learn/receive/clear, Coach pause, and notation source-contract failures remained.
- Follow-up notes: video creation times plus audio-envelope alignment map the retained videos to fine flicks, baby scratch, start/stop, and needle lift. 1024 frames preserves genuine rapid reversals, but all video-covered backward detections in `start_stop` are false stop/catch/restart classifications rather than physical reverse motion. Since false confidence reaches 0.801, do not ship 1024 frames or tune confidence alone. The next safe investigation is an offline-only stop-transition/reversal-confirmation rule tested against both `start_stop` and real reversal fixtures. No production behavior changed. Nothing was committed, staged, or pushed.

## 2026-08-01
- Selected task: compare DVS decoder subwindows offline before changing live cadence
- Files changed: `ScratchLabDesktopTests/TimecodeFixtureValidationTests.swift`, `TASKS.md`, `DEV_LOG.md`, `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`
- Build result: the focused excerpt audit passed; the opt-in 75-case full-capture matrix passed; a targeted `start_stop` confidence rerun passed; `python3 scripts/test_capture_pipeline.py` passed 47/47; and `git diff --check` passed. Required `scripts/build.sh all` ran both configurations, where the new audit passed in both and only the documented unrelated Advanced diagnostics, MIDI learn/receive/clear, Coach pause, and notation source-contract baseline failures remained.
- Follow-up notes: 1024 frames is the best conservative offline candidate (33.87 trusted samples/sec aggregate versus 6.00/sec at 4800, zero position error, 0.148x real-time processing), but it is not approved for live use. Its `start_stop` reverse detections include confidence as high as 0.801, so threshold tuning cannot safely classify them without synchronized physical-motion ground truth. No production behavior changed. Nothing was committed, staged, or pushed.

## 2026-08-01
- Selected task: preserve DVS platter position and time-aware displacement across flushes
- Files changed: `ScratchLab/Models/TimecodePhaseDecoder.swift`, `ScratchLab/Models/TimecodePlatterAdapter.swift`, `ScratchLab/Models/TimecodeControlPipeline.swift`, `ScratchLabDesktopTests/TimecodeDecoderTests.swift`, `ScratchLabDesktopTests/TimecodeFixtureValidationTests.swift`, `TASKS.md`, `DEV_LOG.md`, `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`
- Build result: focused decoder/stability/live-integration/fixture/notation suites passed 141/141 with two expected skips in each scheme configuration; the four new continuity/time regressions passed twice in the required full script; capture-pipeline tests passed 47/47; direct unsigned iOS build succeeded; `git diff --check` passed. Required `scripts/build.sh` ran both configurations and retained only the established unrelated Advanced diagnostics, MIDI learn/receive/clear, Coach pause, and notation source-contract baseline failures.
- Follow-up notes: displacement now uses each input buffer's real sample duration instead of fixed 100 ms. The pipeline carries the last trusted adapter position across successful flushes and honest gaps, clearing it only when leaving control mode or explicitly resetting the pipeline. The prior expected failure now passes (`final position == accepted delta sum`). Callback/subwindow cadence, decoder acceptance, stability/trust thresholds, playback, notation routing, visualization, persistence, and export were deliberately untouched. Next investigation should remain offline-only: compare smaller decoder subwindows against the same fixtures before considering a live cadence change. Nothing was committed, staged, or pushed.

## 2026-08-01
- Selected task: audit DVS sample density and cross-flush position continuity offline before changing visualization or notation behavior
- Files changed: `ScratchLabDesktopTests/TimecodeFixtureValidationTests.swift`, `TASKS.md`, `DEV_LOG.md`, `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`
- Build result: the default focused audit passed in both scheme configurations (the full local-capture case skipped by design); one explicitly enabled full-capture pass completed across all 15 saved WAVs; `python3 scripts/test_capture_pipeline.py` passed 47/47; `git diff --check` passed. Required `scripts/build.sh` exercised both configurations and exited 65 only on the established unrelated Advanced diagnostics, MIDI learn/receive/clear, Coach pause, and notation source-contract baseline failures; both new audit regressions passed twice.
- Follow-up notes: the accumulator is lossless for novel trusted timestamps. The first density ceiling is the decoder's one-result-per-4,800-frame-callback behavior (~10 Hz), with rapid within-buffer motion dropping earlier at decode/filter time (`fast_reversals` 2.73 post-filter samples/sec, `fine_flicks` 1.75/sec, `baby_scratch` 2.74/sec). A separate deterministic expected-failure regression proves platter position resets across flushes (`0.1000` observed vs `0.2000` integrated), and the decoder's `velocity * 0.1` displacement assumes a fixed 100 ms interval. Safest next production slice: make position/delta time-aware and continuous across flushes without changing trust, playback, or routing; only afterward evaluate smaller decoder subwindows offline. No production behavior changed and nothing was committed, staged, or pushed.

## 2026-07-31
- Selected task: write exportable routine WAV stems for multichannel Rane input
- Files changed: `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`, `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`
- Build result: the real 14-channel interleaved Int32 writer regression and the existing raw-stem source contract passed in both scheme configurations; `python3 scripts/test_capture_pipeline.py` passed 47/47; the standalone Debug macOS build succeeded; `git diff --check` passed. Required `scripts/build.sh` ran 2,246 tests in both configurations and exited 65 only on the same eight established unrelated Advanced diagnostics, MIDI learn/receive/clear, Coach pause, and notation source-contract failures.
- Follow-up notes: the failed take's MOV already contained valid 48 kHz multichannel audio, but the sibling WAV was absent because AVFoundation's simple channel-count format initializer returned nil for the Rane multichannel PCM layout and the writer skipped every callback. The writer now falls back to an explicit `DiscreteInOrder` layout while preserving source sample format, interleaving, sample rate, and complete channel count. Playback Bridge remains independent: enable it only if audible Ahhh feedback is wanted; it is not needed for raw input WAV creation or export. Fully quit/relaunch the rebuilt app and make one short hardware routine take to confirm the WAV appears and export becomes available. Nothing was committed, staged, or pushed.

## 2026-07-31
- Selected task: keep the DEBUG DVS live callback active when navigating from Timecode Input to Routine Capture
- Files changed: `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`, `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`
- Build result: the focused lifecycle regression passed in both scheme configurations; combined `TimecodeLiveTapTests`, `TimecodeNotationRoutingTests`, and the new regression passed 83/83 with one expected skip; the direct Debug macOS build succeeded; `python3 scripts/test_capture_pipeline.py` passed 47/47; `git diff --check` passed. Required `scripts/build.sh` ran 2,245 tests in both configurations; the new regression passed twice and the run exited 65 only on the same eight established unrelated Advanced diagnostics, MIDI learn/receive/clear, Coach pause, and notation source-contract failures.
- Follow-up notes: Timecode Input's `.onDisappear` was clearing `captureEngine.timecodeAudioCallback`, disconnecting the DVS pipeline exactly when navigation moved to Routine Capture. The handler now cancels only the fixture task and `DebugTimecodeCapture`. The retained callback still captures the pipeline weakly and is inert unless Live Tap is enabled and mode is not disabled. No decoder, trust gate, notation precedence, playback, export schema, signing, or entitlement behavior changed. Slice 5 can now retry one controlled main routine take; nothing was committed, staged, or pushed.

## 2026-07-31
- Selected task: add DEBUG-only DVS notation trust-decision counters and output totals before hardware slice 5
- Files changed: `ScratchLab/Models/TimecodeNotationTrustGate.swift`, `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLabDesktopTests/TimecodeNotationRoutingTests.swift`, `TASKS.md`, `DEV_LOG.md`, `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`
- Build result: focused `TimecodeNotationRoutingTests` passed 42/42 with one expected skip in both configurations; capture fixtures passed 47/47; the direct Debug macOS build succeeded. Required `scripts/build.sh` completed its test plan and exited 65 only on the established Advanced diagnostics, MIDI learn/clear/receive, Coach pause, and notation source-contract baseline failures; both new tests passed in both configurations. A separate Release check stopped on the unrelated pre-existing `MacCaptureEngine.swift:94` reference from non-DEBUG `lowLatencyTimecodeTapHasRequiredChannels` to DEBUG-only `TimecodeCMSampleBufferAdapter`. `git diff --check` passed.
- Follow-up notes: the DEBUG-only, non-Codable diagnostics value counts every trust-gate decision actually evaluated during a routine take, tracks accumulated/finalized sample totals and finalized DVS event count, appears in the live DVS panel, and is persisted in the routine `_funnel_diag.txt`. It is reset at routine start and protected by the existing notation lock. Trust behavior, DVS/camera precedence, playback, and the export schema are unchanged. Slice 5 remains the next task; the next hardware take can now identify exactly which trust gate prevented movement and whether samples/events survived finalization. Nothing was committed, staged, or pushed.

## 2026-07-31
- Selected task: DVS notation slice 5 — inspect the first stable post-crash-fix hardware capture
- Files changed: workflow documentation only (`TASKS.md`, `DEV_LOG.md`, `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`)
- Build result: not applicable; no production or test code changed. `git diff --check` passed.
- Follow-up notes: Live Tap and routine capture/finalization no longer crashed. The supplied Notation Coach dataset is internally monotonic but entirely camera-sourced (43/43 samples, 2/2 motion strokes, 19/19 scores), so it cannot validate DVS routing. The automatically located same-run 34.2-second routine sidecar completed but has zero movement events and only audio provenance, demonstrating correct fail-closed precedence but not successful `timecode_live` notation. The next hardware take must use the main Routine Capture control and maintain usable forward/backward DVS motion throughout; Playback Bridge remains OFF. Slice 5 stays unchecked.

## 2026-07-31
- Selected task: make `TimecodePlaybackBridge` output diagnostics thread-safe
- Files changed: `ScratchLab/Models/TimecodePlaybackBridge.swift`, `ScratchLabDesktopTests/TimecodePlaybackBridgeTests.swift`, `TASKS.md`, `DEV_LOG.md`, `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`
- Build result: focused bridge/Live Tap crash regressions passed in both test-plan configurations; the full `TimecodePlaybackBridgeTests` suite passed 23/23 in both configurations; the new 2,000-iteration concurrent evaluator/reader regression passed under Thread Sanitizer with no reported race. Required `scripts/build.sh` ran 2,242 tests per configuration; the new regression passed twice and only the documented Advanced diagnostics, MIDI learn/clear, Coach pause, and notation source-contract baseline failures remained. `git diff --check` passed.
- Follow-up notes: the fourth hardware crash was a direct cross-queue bridge-output race: `com.scratchlab.dvsControl` mutated `decisionCounts` during `evaluate`, while the main-thread diagnostics view read that Dictionary in `blockingDecisionSummary`. One recursive lock now protects complete evaluate/reset transactions and all public output reads/writes (state, drive, decision, counts, summary), retaining the existing one-publication behavior. Live Tap-only Stage A already remained stable in Karl's hardware run; the next check is one 10–15 second routine recording with Playback Bridge OFF. Nothing was committed, staged, or pushed.

## 2026-07-31
- Selected task: remove the crashing Advanced-sidebar `@AppStorage` read from Live Tap redraws
- Files changed: `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`, `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`
- Build result: the new AppStorage-removal regression plus the prior performer-name and non-interleaved-audio crash regressions passed in both test-plan configurations, rebuilding the Debug macOS app. Required `scripts/build.sh` ran 2,241 tests per configuration; all three passed twice and only the documented Advanced diagnostics, MIDI learn/clear, Coach pause, and notation source-contract baseline failures remained. `git diff --check` passed.
- Follow-up notes: the third crash is independent of the first two. Its main-thread stack directly entered `SwiftUI.UserDefaultLocation.getValue`/`AppStorage.wrappedValue` through `MacAnalyzerView.advancedSection` during an Advanced-sidebar redraw after enabling Live Tap. That one preference now lives in `@State`, initialized from and explicitly persisted to the same `scratchlab.mac.advancedSection` UserDefaults key. No DVS, audio, notation, export, signing, or entitlement behavior changed. Hardware should first enable Live Tap and wait 15 seconds without recording; only if stable should slice-5 routine capture resume. Nothing was committed, staged, or pushed.

## 2026-07-31
- Selected task: remove the crashing computed `@Published` performer-name observation from the macOS view graph
- Files changed: `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`, `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`
- Build result: the new observation-path regression and the prior multibuffer regression passed in both scheme configurations, rebuilding the Debug macOS app successfully. Required `scripts/build.sh` ran 2,240 tests; the new regression passed twice and the script exited 65 only on the documented Advanced diagnostics, MIDI learn/clear, Coach pause, and notation source-contract baseline. `git diff --check` passed.
- Follow-up notes: the second `.ips` report reproduced the exact `0x79626162` (`"baby"`) main-thread access fault in `SessionSetupViewModel.performerName`/`MacAnalyzerView.body`, while the already-fixed audio extractor was absent from every crashing thread. The computed `@Published` `.onChange` has therefore been removed from SwiftUI body observation. Equivalent last-performer persistence now runs inside the existing main-actor `$config` subscription, with no additional observer and no change to selected-session persistence. The first memory-safety fix remains in place. A 10–15 second hardware routine-capture retry is required before slice 5 can continue. Nothing was committed, staged, or pushed.

## 2026-07-31
- Selected task: fix the macOS routine-capture crash's unsafe audio buffer-list path
- Files changed: `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`, `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`
- Build result: the new real non-interleaved two-buffer `CMSampleBuffer` regression passed in both scheme configurations and under Address Sanitizer; the wider capture/DVS regression set passed `115/115` with one expected fixture skip; the required `scripts/build.sh` ran the complete desktop plan, where the new regression passed inside the 2,239-test run and the script exited 65 only on the established Advanced diagnostics, MIDI learn/clear, Coach pause, and notation source-contract baseline failures; `git diff --check` passed
- Follow-up notes: the supplied `EXC_BAD_ACCESS` report crashed while SwiftUI read `SessionSetupViewModel.performerName`, but concurrently showed the capture audio queue inside `MacCaptureEngine.audioPacket(from:)`. That extractor was the concrete memory-unsafe path: it gave Core Media storage for one `AudioBuffer` and then iterated the returned `AudioBufferList`, which can contain one buffer per channel for non-interleaved input. It now allocates 16-byte-aligned storage for the source layout, rejects inconsistent layouts, and preserves the existing Float32/Int16/Int32 conversion/downmix. This removes a credible source of the corrupted Swift runtime state seen on the main thread; only a real hardware capture retry can prove that it was the sole cause. DVS decode/trust/notation/playback, export, signing, and entitlements were unchanged. Nothing was committed, staged, or pushed.

## 2026-07-31
- Selected task: remove the per-tick DVS observable-publication storm
- Files changed: `ScratchLab/Models/TimecodeControlPipeline.swift`, `ScratchLab/Models/TimecodePlaybackBridge.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `ScratchLabDesktopTests/TimecodeLiveTapTests.swift`, `ScratchLabDesktopTests/TimecodePlaybackBridgeTests.swift`, `TASKS.md`, `DEV_LOG.md`, `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`
- Build result: focused `TimecodeLiveTapTests` plus `TimecodePlaybackBridgeTests` passed `58/58` in both scheme configurations; focused `ScratchSamplePlaybackControllerTests` passed `78/78` in both configurations; the changed Debug macOS app compiled in those runs; capture fixtures passed `47/47`; and `git diff --check` passed. The required `scripts/build.sh` completed its full desktop plan and exited 65 on the same unrelated `CaptureReliabilityPhase1CoreTests`, `CrossPlatformNotationParityTests`, and `NotationSheetTests` baseline failures; all edited DVS and playback suites were green.
- Follow-up notes: the latest hardware snapshot and video showed a healthy route and decode (`AVAudioEngine Float32 non-interleaved`, 14 channels, 4,800-frame callbacks, heartbeat 39.5 ms within a 250 ms budget, ingress 41.6 ms, `392` accepted, zero long dropouts, `1.003x`) but the console still printed repeated SwiftUI state-mutation warnings after each slow 150–250 ms flush. Root cause was an observer-invalidation storm: one pipeline flush independently published ten related outputs, one bridge evaluation independently published four, and capture-engine drive diagnostics published multiple values on every control tick. The pipeline and bridge now compare complete output snapshots and emit at most one coherent invalidation per operation, unchanged results emit none, the analyzer consumes the drive returned by that evaluation, and capture-engine DVS diagnostics are one coherent 5 Hz state while audio scheduling remains full cadence. Also fixed the DEBUG pending-drive counter leak on nil-drive returns. Decoder thresholds, Rane calibration, audio routing, playback displacement, capture/export, signing, and entitlements were unchanged. Hardware must now confirm the state-during-view-update warning and audible cuts are absent during a fresh 30–60 second run. Nothing was committed, staged, or pushed.

## 2026-07-31
- Selected task: prevent healthy Rane callbacks from racing the AVCapture fallback
- Files changed: `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `ScratchLabDesktopTests/TimecodeLiveTapTests.swift`, `TASKS.md`, `DEV_LOG.md`, `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`
- Build result: after cleaning a generated unsigned-test-bundle artifact, focused `TimecodeLiveTapTests` passed `35/35`; the direct Debug macOS arm64 build passed; capture fixtures passed `47/47`; `git diff --check` passed. The required `scripts/build.sh` completed and exited 65 on the same unrelated diagnostics/MIDI/coach/notation baseline failures; the new route-arbitration regression passed.
- Follow-up notes: the reset hardware snapshot isolated route arbitration from decoding: it had `180` accepted frames, zero silence/weak/channel/long drops, `1.008x`, and confidence `0.921`, but the authoritative adapter was still legacy Int32 while AVAudioEngine was fresh, with `327` ingress buffers versus `310` low-latency callbacks. The fixed 100 ms heartbeat threshold exactly matched the observed 4,800-frame/48 kHz Rane callback period, so normal jitter intermittently enabled AVCapture and mixed two timestamp streams. Freshness now uses 2.5 times the observed callback duration, bounded to 100–300 ms. This gives the Rane 250 ms of tolerance while still restoring fallback within 250 ms after a genuine stop; startup and recovery semantics remain unchanged, and diagnostics expose the active budget. Hardware must verify that, after a stable reset, adapter diagnostics stay AVAudioEngine Float32, ingress `received` tracks heartbeat callbacks without recurring legacy additions, `dropped` stays flat, and audio remains continuous. Decoder thresholds, Rane calibration, playback scheduling, capture/export, signing, and entitlements were unchanged. Nothing was committed, staged, or pushed.

## 2026-07-31
- Selected task: preserve complete DVS grains across the Ahhh loop boundary
- Files changed: `ScratchLabDesktop/Services/ScratchSamplePlaybackController.swift`, `ScratchLabDesktopTests/ScratchSamplePlaybackControllerTests.swift`, `TASKS.md`, `DEV_LOG.md`, `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`
- Build result: focused `ScratchSamplePlaybackControllerTests` passed `78/78`; the direct Debug macOS arm64 build passed; `git diff --check` passed. The required `scripts/build.sh` completed its full plan and exited 65 on the same unrelated diagnostics/MIDI/coach/notation baseline failures in `CaptureReliabilityPhase1CoreTests`, `CrossPlatformNotationParityTests`, and `NotationSheetTests`; the edited playback suite passed.
- Follow-up notes: the supplied 47.2-second video showed the old multi-second trailing-silence failure is gone, but six short 82–256 ms holes remained near repetitions of the 1.047-second clean sample. The scheduler advanced `currentSampleFrame` by the complete platter-requested displacement while limiting the queued forward PCM to the frames before the WAV end, or backward PCM to the frames before frame zero. Forward and backward extraction now wrap modulo the source length and queue the same complete displacement used to move the needle. Hardware should verify at least 30 seconds after resetting counters only once Receiving is stable. The latest snapshot's large silence/long-dropout totals are cumulative from setup; its point-in-time evidence was healthy (`Dropout dur ms: 0.0`, `0.999x`, confidence `0.945`, fresh heartbeat/ingress). Decoder thresholds, Rane calibration, capture/export, signing, and entitlements were unchanged. Nothing was committed, staged, or pushed.

## 2026-07-31
- Selected task: remove the remaining DVS UI-driven control starvation and trailing-silence sample
- Files changed: `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLabDesktop/Views/DVSControlVinylPanel.swift`, `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `ScratchLabDesktop/Services/ScratchSamplePlaybackController.swift`, `ScratchLab.xcodeproj/project.pbxproj`, `ScratchLabDesktopTests/ScratchSamplePlaybackControllerTests.swift`, `ScratchLabDesktopTests/TimecodeLiveTapTests.swift`, `TASKS.md`, `DEV_LOG.md`, `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`
- Build result: focused combined playback/live-tap tests passed `111/111`; after adding lifecycle-cancellation coverage, focused `TimecodeLiveTapTests` passed `34/34`; the direct Debug macOS build passed; `git diff --check` passed. The required `scripts/build.sh` completed its test plan and exited with code 65 on the established unrelated broad baseline: eight `CaptureReliabilityPhase1CoreTests` failures plus four notation source-contract issues. The failures were `testAdvancedDiagnosticsExplainCameraAndTickActivity`, MIDI learn/summary/clear assertions, Coach pause source assertions, `templateDemoStillOwnsBabyScratchTemplateLabel`, and repeated `notationCanvasUsesSharedRenderer`; no edited DVS/playback regression failed.
- Follow-up notes: audio analysis of the supplied 109-second screen recording found a repeating approximately two-second audible section followed by approximately seven seconds of silence. The DVS pipeline was loading `ScratchLab/Resources/ScratchSamples/ahhh.wav`, a 4.47-second pad containing 3.07 seconds of trailing silence, rather than the existing clean 1.05-second `ScratchLab/Resources/VirtualPlatter/ahhh.wav`. DVS now uses a dedicated `dvs_ahhh` mapping to that continuous asset, and the desktop target bundles the existing VirtualPlatter resource folder. The remaining SwiftUI state warning was addressed at its source by replacing the view's Combine `.onReceive` clock with a lifecycle-managed `DispatchSourceTimer`; ticks retain deferred/coalesced main-queue execution, and stopping invalidates already-enqueued work. The transient invalid pair-3/4 Picker selection is also represented during one-channel startup diagnostics. Decoder thresholds, Rane calibration/inversion, playback displacement, capture/export, signing, and entitlements were not changed. Hardware must verify the clean loop and absence of SwiftUI warnings after a full app quit/relaunch. No commit, stage, or push was performed.

## 2026-07-31
- Selected task: defer and coalesce the DEBUG DVS control tick outside SwiftUI view updates
- Files changed: `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLab/Models/TimecodeControlPipeline.swift`, `ScratchLabDesktopTests/TimecodeLiveTapTests.swift`, `TASKS.md`, `DEV_LOG.md`, `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`
- Build result: focused `TimecodeLiveTapTests` passed `33/33` in both scheme configurations; the direct Debug macOS build passed; `scripts/build.sh` capture fixtures passed `47/47`; `git diff --check` passed. The required script's broad desktop plan was interrupted after it reproduced the existing unrelated filesystem hang while opening `CaptureReliabilityPhase1Tests.swift` in `AutoCutVisualPlaybackTests.testAutoCutVisualPlaybackIsGatedToAutoCutMode`.
- Follow-up notes: the fresh AVAudioEngine heartbeat and ingress evidence proved the remaining console warning was not a capture-format failure. Root cause was the 60 Hz SwiftUI `.onReceive` closure synchronously calling the observable pipeline, bridge, and playback engine during the same view-update transaction. Added a plain, non-observable deferred tick coordinator that queues the control pass onto the next main-queue turn, permits at most one pending pass, and resumes normal scheduling after completion. Also suppressed the per-tick assignment of an unchanged DVS playback-active flag and avoided publishing identical counters on repeated empty decoder flushes. Tests deterministically prove the callback is not synchronous, bursts coalesce, the next tick can reschedule, and an unchanged empty flush emits no `objectWillChange`. Capture routing, decoder thresholds, Rane inversion/calibration, playback displacement semantics, recorder/export, signing, and entitlements were untouched. Fully quit/relaunch and hardware-test steady normal playback for 30 seconds; the SwiftUI mutation warning must not recur, Receiving must remain green, and Ahhh must remain continuous. No commit, stage, or push was performed.

- Selected task: keep live DVS audio ingress off the main queue and bound catch-up latency
- Files changed: `ScratchLab/Models/TimecodeControlPipeline.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `ScratchLabDesktop/Services/ScratchSamplePlaybackController.swift`, `ScratchLabDesktopTests/TimecodeLiveTapTests.swift`, `TASKS.md`, `DEV_LOG.md`, `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`
- Build result: focused `TimecodeLiveTapTests` passed `31/31`, including diagnostics-only live-ingress preservation; core `TimecodeDecoderTests`, `TimecodeLiveIntegrationTests`, `TimecodePlaybackBridgeTests`, and `TimecodeStabilityFilterTests` passed `86/86`; `ScratchSamplePlaybackControllerTests` passed `77/77` in the wider run before an unrelated fixture file-open stall; direct Debug macOS arm64 build passed; `scripts/build.sh` capture fixtures passed `47/47`; `git diff --check` passed. The wider fixture run and required script's broad plan were both stopped after macOS blocked indefinitely in filesystem `open` while reading unrelated repository fixtures/source (`hardwareTransitionCatalog` and then `CaptureReliabilityPhase1Tests.swift` in the first AutoCut test); process samples established the block outside the edited DVS path.
- Follow-up notes: the video showed regular Receiving/Stale cycling while the snapshot simultaneously reported a fresh AVAudioEngine heartbeat (`29.9 ms`) and stale pipeline age (`0.73 s`), proving the selected audio was waiting behind main-thread work. The snapshot's `Long dropouts: 1666` exposed a second defect: empty 60 Hz ticks mixed audio-relative time since tap start with wall time since pipeline construction, so enabling DVS after the app had been open made every normal gap between the Rane's 4,800-frame/100 ms callbacks look longer than one second and fail closed. The callback now performs only a lock-protected, non-publishing enqueue; main flushes consume a bounded newest window (maximum eight buffers and 200 ms), publish true callback receipt freshness, and expose received/dropped/pending/frame/age ingress evidence. Empty flushes stay in the audio clock domain and still fail closed after a genuine >1 s callback loss. High-volume trace and playback scheduling logs are disabled by default and can be re-enabled with `SCRATCHLAB_DVS_TRACE=1`. Decoder thresholds, Rane profile/inversion, pair selection, playback displacement/rate behavior, recorder/export, signing, and entitlements were not changed. Hardware verification is the next action; no commit, stage, or push was performed.

- Selected task: add a DEBUG-only disk-backed DVS fixture recorder before access to the physical turntable ends
- Files changed: `ScratchLab/Models/TimecodeCMSampleBufferAdapter.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLabDesktopTests/TimecodeLiveTapTests.swift`, `TASKS.md`, `DEV_LOG.md`, `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`
- Build result: focused 14-channel recorder regression passed and verified the multichannel CAF, pair 3/4 WAV, sample values, native 48 kHz rate, frame counts, ordered callback records, host times, and manifest; full `TimecodeLiveTapTests` passed `24/24`; Debug `ScratchLabDesktop` macOS build passed; `./scripts/build.sh` passed capture-pipeline fixtures `47/47`, then was stopped in the broad desktop test leg after reproducing unrelated existing failures in `testAdvancedDiagnosticsExplainCameraAndTickActivity`, `testClearCrossfaderMappingRemovesMapping`, and `testCoachPauseHoldsPoseFromAudioTimeAndAvoidsPausedTimelineWork`; the new recorder regression was green inside that broad run; `git diff --check` passed
- Follow-up notes: root cause was that the prior `2-Second Raw Pair Capture` retained only pair 3/4 in memory and reduced it to a summary, so it could not preserve the remaining hardware failures for offline replay. Replaced it with a manually stopped labelled recorder (90-second safety cap) that receives all deinterleaved physical channels before pair auto-selection/rejection from both live adapter paths, immediately enqueues them to a private utility queue, and performs all file/JSON I/O off the realtime callback. Each capture folder is written under sandbox-writable Application Support and contains `raw_multichannel_float32.caf` with explicit discrete channel order, `pair_3_4_float32.wav`, `callbacks.jsonl`, and `manifest.json`; the manifest states that source PCM is stored at native sample rate as Float32 without normalization, fades, compression, or sample-rate conversion. Advanced → Timecode Input now exposes the full movement label set, Start Recording, Stop and Save, Copy Summary, Copy Folder Path, and Reveal in Finder, and reports start, no-four-channel-audio, and write failures visibly. No decoder, confidence threshold, direction convention, smoothing, playback scheduling, export schema, signing, entitlements, or compatibility claims changed. Hardware capture and visual UI verification are still required before the turntable is disconnected.

## 2026-07-07
- Selected task: commit and push two focused pending fixes — the Practice Live Notation show/hide toggle and the ScratchSamplePlaybackController grain edge-fade (backward scratch sample scheduling stabilization)
- Files changed: `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLabDesktop/Services/ScratchSamplePlaybackController.swift`, `ScratchLabDesktopTests/ScratchSamplePlaybackControllerTests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: focused notation suites (`LiveNotationOverlayTests`, `NotationLaneGeometryTests`, `NotationLaneGeometryViewTests`, `ScratchNotationPresentationAdapterTests`) passed `91/91`; focused `ScratchSamplePlaybackControllerTests` passed `59/59`; required Debug `ScratchLabDesktop` arm64 build passed; `git diff --check` passed at every stage; pushed to `main` as commits `18e8b79` (Live notation: add Practice visibility toggle) and `0d883d6` (macOS: stabilize backward scratch sample scheduling)
- Follow-up notes: `18e8b79` added a compact "Show Live Notation" toggle (default on) to the existing Practice-tab notation strip header, reusing the same icon+text capsule pattern already used by the Review tab's "Notation Overlay" toggle; disabling it hides only the caption and the strip itself, the "Live Notation" title stays visible, and no new notation model/renderer/clock/timer was introduced — visually confirmed live (built app launched directly, toggled off/on, Listen still moved the cursor with forward/backward colours intact, no crash). `0d883d6` added a 32-frame linear edge-fade (`grainEdgeFadeFrames`/`applyEdgeFade(to:)`) applied to every scheduled scratch grain immediately before `scheduleBuffer`, removing the PCM-cut click at grain boundaries; this is direction-agnostic (applies identically to forward and reversed/backward grains) and sits entirely after the existing `invalidSegment`/`copyFailed` guards, so `currentSampleFrame` movement, skip-reason logic, and DVS/platter-following delta handling were untouched. No camera, DVS, MIDI, scoring, audio-routing, signing, bundle, or entitlement changes in either commit.

## 2026-07-06
- Selected task: user-requested Live Notation UX/discoverability pass on the existing macOS Practice-tab notation strip
- Files changed: `ScratchLabDesktop/Views/MacAnalyzerView.swift`
- Build result: required Debug `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS,arch=arm64' build` passed; focused notation suites (`LiveNotationOverlayTests`, `NotationLaneGeometryTests`, `NotationLaneGeometryViewTests`, `ScratchNotationPresentationAdapterTests`) passed `91/91` with 0 failures; `git diff --check` passed; live-launched the built `.app` directly (not just built) and drove it via the accessibility API to visually confirm the Practice tab, the new label/caption, and the moving cursor; pushed to `main` as commit `80a3047`
- Follow-up notes: live Baby Scratch notation already existed and was already visible and working in the Practice tab before this pass — the unlabelled strip renders via the existing `ScratchNotation.babyScratch` → `LiveNotationOverlayModel` → `LiveNotationOverlayView` pipeline (`practiceNotationStrip` in `MacAnalyzerView.swift`), with a real live cursor driven by the existing demo/beat wall-clock timing and forward(green)/backward(orange) stroke colouring already in place. This was a discoverability fix, not new notation architecture: added a "Live Notation" title and a "Press Listen to see the Baby Scratch cursor move." helper caption directly above the existing strip, matching the same title/subtitle text-style convention already used elsewhere in this file (e.g. `cameraStageCard`). No new notation model, renderer, or clock was created; no duplicate notation stack exists. Re-verified live after the change that Practice still opens, the label/caption render, Listen still animates the cursor, and forward/backward strokes are still colour-distinguished, with no crash. Playback, scoring, MIDI, audio routing, capture internals, signing, bundle IDs, schemes, entitlements, versioning, and App Store metadata were not touched. Separately confirmed via `gh`: this repository has no `.github/workflows` directory (no CI configured at all) and `main` has no branch protection rules, so there was no CI/status-check gate to satisfy on this push.

## 2026-07-06
- Selected task: hardware-validate DVS Rane ONE MKII motion decode end-to-end and fix every blocker found along the way
- Files changed: `ScratchLab/Models/TimecodeCMSampleBufferAdapter.swift`, `ScratchLab/Models/TimecodeControlPipeline.swift`, `ScratchLab/Models/TimecodeDecodedFrame.swift`, `ScratchLab/Models/TimecodeMotionStabilityFilter.swift`, `ScratchLab/Models/TimecodePhaseDecoder.swift`, `ScratchLab/Models/TimecodeValidationSnapshot.swift`, `ScratchLabDesktop/Views/TimecodeControlCard.swift`, `ScratchLabDesktop/Views/DVSControlVinylPanel.swift`, `ScratchLabDesktopTests/DVSControlVinylAnalyzerTests.swift`, `ScratchLabDesktopTests/TimecodeStabilityFilterTests.swift`, `ScratchLabDesktopTests/TimecodePrototypeProfileTests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: focused DVS/timecode suites (`DVSControlVinylAnalyzerTests`, `TimecodeStabilityFilterTests`, `TimecodePrototypeProfileTests`) passed green at every stage of this arc; the required Debug `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS,arch=arm64' build` passed at every stage; `git diff --check` passed at every stage; final state pushed to `main` as commits `2f80778`, `28c125f`, `862d3b8` (built on the earlier multichannel-extraction foundation in `cf7953f`, `9f3bd9a`, `6bb2b5c`)
- Follow-up notes: problem was that live Rane ONE MKII (14ch Int32 interleaved @ 48000 Hz) hardware never produced accepted DVS motion, despite synthetic fixtures passing. Diagnosed in stages against real hardware captures rather than assumptions: (1) Auto pair scoring chose 13/14 on raw RMS/peak alone even though it had 0 Hz / 0 zero-crossing-rate on both channels — added a carrier-evidence gate (plausible 500-2500 Hz band plus a minimum zero-crossing rate) so a loud non-carrier pair is rejected as `no timecode carrier`; confirmed live that Auto now selects 5/6 and rejects 13/14. (2) With pair selection fixed, accepted motion was still 0 — root cause was that `TimecodeControlMode.diagnosticsOnly` never runs `TimecodePhaseDecoder` by design; `controlPrototype` mode is required to exercise the actual decoder, and this was not previously understood. (3) In Control Prototype, every frame was still rejected `low_confidence` — the stability filter's confidence gate was hardcoded to 0.3 and never read the UI's `pipeline.minConfidence`; real Rane 5/6 confidence measures around 0.117-0.155, so it could never clear the hardcoded threshold regardless of what the user set in the UI. Wired the gate to the live `minConfidence` value. (4) A user-set Min confidence of 0.10 was then found to silently reset back to the active preset's fixed constant on every relaunch; added persisted override storage and an automatic switch to the existing `.manual` profile whenever the user directly edits calibration, confirmed live that 0.10 now survives relaunch. (5) With motion now decoding, physical forward platter spin reported `backward`; the existing `Invert direction` toggle was already fully wired end-to-end in the decode pipeline and only needed the same relaunch-persistence fix already built for Min confidence — with it enabled, forward spin now correctly reports `forward`. (6) Separately cleaned up and stabilized the DVS control panel's SwiftUI layout (fixed-height rows instead of appearing/disappearing blocks, a collapsed disclosure group for verbose per-channel/per-pair diagnostics) without touching any decoding, calibration, or logger behavior. Final live-validated snapshot on real hardware: signal health `usable`, signal class `quadratureCandidate`, phase offset ≈ -81°, ZCR frequency 986/986 Hz, Min confidence UI/gate 0.100/0.100, frames raw/pre-filter/post-filter 75/74/74, accepted 1382, direction `forward`, raw rate 0.118 u/s, smoothed rate 0.069 u/s, status `usablePrototypeControl`. At no point in this arc was playback, scoring, MIDI, notation routing, or audio output routing enabled or changed — the entire validation stayed within the existing DEBUG-only DVS diagnostics/calibration pipeline.

## 2026-06-26
- Selected task: user-requested fix for `ScratchSamplePlaybackController.loadOnQueue` / `ensureEngineRunning` path after the queued sample-load edit left the controller structurally broken
- Files changed: `ScratchLabDesktop/Services/ScratchSamplePlaybackController.swift`, `ScratchLabDesktopTests/ScratchSamplePlaybackControllerTests.swift`, `DEV_LOG.md`
- Build result: focused `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test -only-testing:ScratchLabDesktopTests/ScratchSamplePlaybackControllerTests` passed (`20/20`); `git diff --check` passed; `./scripts/build.sh` passed the Python capture-pipeline fixtures (`47/47`) and the playback controller tests inside the full desktop run, but failed on unrelated existing broad-suite failures: `CaptureReliabilityPhase1CoreTests.testAdvancedDiagnosticsExplainCameraAndTickActivity`, `NotationSheetTests.templateDemoStillOwnsBabyScratchTemplateLabel`, three `CrossPlatformNotationParityTests.notationCanvasUsesSharedRenderer` source/parity assertions, plus a test-runner bootstrap exit
- Follow-up notes: root cause was a malformed `loadOnQueue` block in `ScratchSamplePlaybackController.swift`: an extra closing brace after the load log pushed later class members outside the type, so the desktop target failed to compile with `static properties may only be declared on a type` and an extraneous top-level brace. Fixed the brace structure and restored the main-thread `statusLabel` publish after `ensureEngineRunning()` completes the queued load path. Preserved the existing user-authored sample playback tuning in the dirty worktree: 1/60-second segments, diagnostic preview scheduling from the queued load, same-direction buffer queuing, and explicit unload in the concurrency stress test. No capture, export, notation, dataset, resource, signing, or entitlement code was changed.

## 2026-06-18
- Selected task: evidence-driven diagnosis of synthetic quadrature Timecode Prototype live decode instability and CPU load
- Files changed: `ScratchLab/Models/TimecodePhaseDecoder.swift`, `ScratchLab/Models/TimecodeControlPipeline.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLabDesktopTests/TimecodeDecoderTests.swift`, `ScratchLabDesktopTests/TimecodeLiveTapTests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: focused timecode verification passed with `20/20` tests: `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' -only-testing:ScratchLabDesktopTests/TimecodeDecoderTests/testTimecodeDecoderConstantQuadratureProducesNearZeroRate -only-testing:ScratchLabDesktopTests/TimecodeLiveTapTests -only-testing:ScratchLabDesktopTests/TimecodeLiveIntegrationTests/testCountersTrackAcceptedAndDropped test`; `git diff --check` passed; `./scripts/build.sh` passed capture-pipeline fixtures (`47/47`) but failed in the broad `ScratchLabDesktop` XCTest/Swift Testing plan on existing unrelated practice/notation/resource failures, with the final failure list no longer containing timecode tests
- Follow-up notes: direct deterministic decoder feed reproduced the synthetic quadrature instability before the fix, proving the problem was not caused by Serato/FSK and was not only Loopback/AVCapture. Root cause was a decoder analysis-window mismatch plus live-path timing/UI pressure: the phase decoder used dot-product quadrature as if every buffer contained integer 1 kHz carrier cycles, but live-sized 1024-frame buffers at 44.1 kHz do not; the pipeline used wall-clock `Date()` for audio-buffer relative time, so callback/UI jitter inflated decoded rate; and the live callback posted every audio buffer to the main queue while publishing diagnostics on every buffer. Fixed with continuous audio-frame timestamps, least-squares phase fitting over the sine/cosine basis, a small stationary numerical deadband for residual phase velocity, diagnostics publishing throttled to 10 Hz while preserving every decode buffer, pending counter flush on decode, and main-queue callback coalescing. Did not change decoder confidence thresholds, RANE/replay/platter logic, notation/export/schema, app resources, or compatibility claims. Manual Loopback validation is still required for CPU and live status confirmation on hardware.

- Selected task: generate local-only pure quadrature Timecode Prototype test WAV
- Files changed: `scripts/generate_quadrature_test_wav.py`, `TASKS.md`, `DEV_LOG.md`
- Build result: `python3 -m py_compile scripts/generate_quadrature_test_wav.py` passed; local WAV inspection passed (`stereo`, `44100 Hz`, `16-bit PCM`, `2646000` frames, `60.000000 s`, peak left/right `0.419996`, balanced RMS about `0.296986`, quarter-shift correlation `0.999994`); `./scripts/build.sh` passed capture-pipeline fixture tests (`47/47`) but failed in the existing `ScratchLabDesktop` XCTest leg with unrelated app/resource/test failures including `AutoCutVisualPlaybackTests.testAutoCutVisualPlaybackIsGatedToAutoCutMode`, Baby Scratch notation/resource assertions, practice/source assertions, hand-direction confidence tests, lane wiring/parity tests, and synthetic baby-scratch movement regressions
- Follow-up notes: added a deterministic standard-library Python generator for `debug_captures/timecode/scratchlab_quadrature_1khz_60s.wav`. The generated WAV is local-only under ignored `debug_captures/` and is not bundled. Signal is stereo 44.1 kHz for 60 seconds, 1 kHz carrier, left sine, right sine at +90 degrees, amplitude `0.42` peak, no FSK, no silence gaps, no fade. The script prints the signal parameters and intended validation route: `scratchlab_quadrature_1khz_60s.wav` -> Loopback virtual input -> ScratchLab Timecode Prototype. No decoder logic, thresholds, RANE/replay/platter logic, notation/export/schema, app resources, signing, or entitlements were changed.

## 2026-05-19
- Selected task: user-requested VirtualPlatter audio control fix so the clean `ahhh` sample is owned by record movement instead of firing as a detached one-shot
- Files changed: `ScratchLab/Models/VirtualPlatter.swift`, `ScratchLab/Views/VirtualPlatterPrototypeView.swift`, `ScratchLabDesktopTests/VirtualPlatterTests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test -only-testing:ScratchLabDesktopTests/VirtualPlatterTests` passed with 20 tests; `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLab -destination 'generic/platform=iOS' build` passed; `./scripts/build.sh` passed end-to-end, including capture-pipeline fixtures, the ScratchLabDesktop XCTest plan, and iOS/macOS/watchOS build legs
- Follow-up notes: root cause was the prototype's clean-audio fix going too far toward transport playback: `VirtualPlatterAudio` used `AVAudioPlayer` and `advanceRecord` called `triggerOnce()` after crossing the cue, so the entire `ahhh` kept playing independently even if the user stopped or pulled the record back. Fixed by adding a pure `VirtualPlatterSampleMapper` that keeps the cue fixed at the middle of the gray drill area and maps post-cue record phase to normalized sample position. Replaced the one-shot player with an `AVAudioEngine`/`AVAudioPlayerNode` short-buffer reader: each tick stops scheduling when there is no record movement, reads a short forward PCM chunk when phase advances, and reads the same PCM frames in reverse when phase moves backward. This preserves the clean bundled sample path without returning to the old source-node/resampling approach. No capture, export, ML, dataset, detector, or routine-capture files were changed.

## 2026-05-08
- Selected task: user-requested macOS design implementation pass for Practice, Capture, and Advanced to fix safe-area clipping, hide calibration overlays outside calibration edit mode, replace blocking stage status copy, clean Advanced session/device wording, and restack post-save capture actions without changing business logic
- Files changed: `ScratchLabDesktop/Views/DeckGamificationOverlay.swift`, `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `DEV_LOG.md`
- Build result: escalated `./scripts/build.sh > build/design-implementation-pass-final.log 2>&1` passed end-to-end on 2026-05-08, including capture-pipeline fixtures, the `ScratchLabDesktop` XCTest plan, notation Swift Testing suites, and iOS/macOS/watchOS build legs; tail of the final log ends with `** BUILD SUCCEEDED **`
- Follow-up notes: root cause was a set of macOS-only presentation assumptions that had drifted from the current window chrome and workflow copy. The left inspector used fixed top spacing instead of safe-area-aware spacing, calibration deck boxes were always rendered on the live stage, the stage still surfaced a blocking manual-guide overlay, Advanced surfaced raw/internal session/device strings, and the post-save capture actions competed horizontally instead of reflecting the intended review-first priority. Fixed by making the shared sidebar container safe-area aware, introducing a local `isCalibrationEditMode` gate in the macOS analyzer flow and passing it into the deck overlay, moving the manual deck-estimate message into an inspector card with a `Recalibrate` entry point, switching Advanced session presentation to the session scratch name plus a separate `Copy ID` affordance, normalizing user-facing virtual-audio / camera labels to generic copy, moving the capture workflow toward `Calibrate Deck View` and vertically stacked review actions, and updating capture/review fallback wording so `Unknown` is no longer shown to users. No data models, schemas, capture/export business logic, or persistence formats were changed. I did not complete interactive visual verification in a launched app window from this environment, so the clipping/visibility confirmations are based on the implemented source paths plus the updated regression suite and full successful build.

- Selected task: make target notation visible across macOS Practice/Capture/Review/Advanced even when captured notation is unavailable, while keeping zero-evidence takes truthful
- Files changed: `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `ScratchLabDesktopTests/ScratchNotationTimelineTests.swift`, `DEV_LOG.md`
- Build result: focused escalated notation visibility regressions passed in `build/test-notation-layout-visibility-focused.log` (`8` XCTest cases plus the `ScratchNotationTimeline` Swift Testing suite); focused stale Review-source regression passed in `build/test-review-notation-source.log`; fresh escalated `./scripts/build.sh > build/notation-layout-visibility-fix.log 2>&1` rerun cleared the new notation visibility cases and the notation/timeline Swift Testing suite, but the full script still exits red on unrelated existing desktop-suite instability: `CaptureReliabilityPhase1CoreTests.testMIDILearnStateTransitionsToListening` (`ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift:9981`) intermittently sees `.learned(channel: 1, controller: 11)` instead of the old expected `.idle`, and the broad run also still reports the recurring `RoutineSessionDrafts` create-new collision
- Follow-up notes: root cause was that the app treated notation as a captured-artifact feature instead of a target-guidance feature. `NotationVisualizerView` and `ScratchNotationCanvasView` already existed, but `MacAnalyzerView` only surfaced notation in user-facing flows when saved movement events existed, so Practice had no visible target strip, Capture had no pre-record target guide, Review fell back to a “no notation” message with no reference pattern, and Advanced used “Unavailable” language that conflated “no captured notation” with “no notation at all.” Fixed by adding one shared macOS view-state model, `NotationDisplayStateModel`, that separates target notation availability from captured notation availability and tracks target/detected labels plus captured/live counts. Practice now shows a compact `Target notation` Baby Scratch guide under the camera/demo stage, Capture shows a compact target-notation strip before recording, Review’s right-side stage now explicitly separates `Target notation` from `Captured evidence`, and Advanced → `Notation / Timeline` now explains target-available vs captured-unavailable states while still surfacing saved/live counts. Zero-evidence takes remain truthful: `Detected` stays `Unknown`, captured notation stays absent until saved movement/audio/fader events exist, and the UI now says `No captured notation for this take` instead of implying that target notation itself is missing. Two source assertions had to be updated to the new Review/notation copy and grouping, but no detector, export, startup-recovery, or record-readiness logic changed.

- Selected task: reshape the macOS Practice/Capture/Review/Advanced tab hierarchy so each tab matches its real job without regressing the recent truth-model and readiness work
- Files changed: `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `DEV_LOG.md`
- Build result: focused escalated layout hierarchy regressions passed (`10/10`) in `build/test-app-layout-hierarchy-focused.log`; focused escalated stale source regressions passed in `build/test-practice-review-safe.log` and `build/test-advanced-workbench-labels.log`; a fresh escalated `./scripts/build.sh > build/app-layout-hierarchy-cleanup.log 2>&1` rerun cleared the new Practice/Review/Advanced hierarchy assertions, but the full script is still red on the first unrelated remaining blocker `CaptureReliabilityPhase1CoreTests.testMIDILearnStateTransitionsToListening` (`ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift:9870`), where the observed learn state is `.learned(channel: 1, controller: 11)` instead of the old expected `.idle`
- Follow-up notes: root cause of the UI issue was structural, not engine-level: Practice still exposed too much setup/debug scaffolding, Capture still mixed “saved/export” state into a record-first workflow, Review still used generic ready/export language for zero-evidence takes, and Advanced still behaved like one giant mixed scroll. Fixed by keeping business logic unchanged and tightening the macOS view/state layer around four small shared models: `PracticeSidebarLayoutModel`, `CaptureStatusRailModel`, `ReviewWorkflowStateModel`, and `AdvancedWorkbenchSection`. Practice is now coach-first with a compact demo/coach/live-input stack and safe silent-audio wording (`Audio input connected.` instead of implying usable signal). Capture now keeps only `No Beat / Click / Beat` as true capture modes, moves calibration to a dedicated `Adjust deck boxes` action, shrinks the primary record button, shortens the Advanced handoff copy, resolves saved-take status to `Take saved`, and prevents clipped Last Take values. Review now presents zero-evidence takes as needs-review/raw-take states, hides misleading accept/notation actions when evidence is missing, shortens the take reference away from the full UUID, and distinguishes `Export Raw ZIP` from validated export. Advanced now exposes grouped workbench sections (`Overview`, `Audio`, `Camera / Deck`, `MIDI / Fader`, `Watch`, `Detection`, `Notation / Timeline`, `Export / Raw JSON`, `Serato Screen`) instead of relying on the old mixed-scroll labels. Two stale source tests had to be updated to the new user-facing copy/section names: `testPracticeSourceKeepsDemoPathAppReviewSafe` and `testMacAdvancedScreenContainsTechnicalToolsOutsidePrimaryNavigation`. The current full-build blocker is outside this tab-layout work and appears to be a pre-existing MIDI learn-state expectation mismatch.

- Selected task: restore positive macOS Baby Scratch detector recognition without reopening the silent-buffer or steady-source false-positive paths
- Files changed: `ScratchLabDesktop/Services/MacScratchDetector.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `DEV_LOG.md`
- Build result: focused escalated `ScratchLabDesktop` detector tests passed twice with `3/3` cases green in `build/test-detector-baby-pattern.log` (`testMacScratchDetectorRejectsSilentBuffers`, `testMacScratchDetectorRejectsSteadySourceLoopAudio`, `testMacScratchDetectorStillRecognizesPlausibleBabyScratchPulsePattern`); the requested escalated `./scripts/build.sh > build/detector-baby-pattern-fix.log 2>&1` shows the three detector cases now passing in the full desktop suite, but the broader run still surfaces the pre-existing unrelated `RoutineSessionDrafts` create-new collision
- Follow-up notes: the regression was not the new negative-path classifier thresholds by themselves. The strengthened detector could still score a plausible two-burst Baby Scratch window above threshold, but the online `process(samples:)` path never returned that result because it was too dependent on the streaming window collector: pre-attack context was dropped before collection started, short phrases could miss the valid alignment, and the positive fixture itself used flat sine chunks that were weaker than the real burst envelope the classifier expects. Fixed in three narrow steps: changed the positive fixture to use tapered push/pull bursts and added summary assertions proving the fixture produces a passing two-onset window; kept the stricter negative-path guards (`minimumEnergySpan`, lower-but-still-nonzero peak/variance floors) so silence and steady loops still reject; and added a bounded batch-analysis fallback in `MacScratchDetector.process(samples:)` that reuses the same `analyze(...)` classifier when the current call’s samples already contain a valid window but the streaming collector missed it. Also kept a small internal `debugSummaries` utility so tests can assert why the positive fixture is valid instead of only asserting the final label. The full script’s remaining blocker is unrelated to the detector fix: `RoutineSessionDrafts` already exists in a temp session folder during the broader desktop run.

- Selected task: user-authorized follow-up to fix the macOS Debug app launch hang after the Capture sidebar simplification without undoing the sidebar/manual-guide work
- Files changed: `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `DEV_LOG.md`
- Build result: focused escalated `ScratchLabDesktop` regressions passed (`3` tests, `0` failures); direct escalated `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' build` passed; requested escalated `./scripts/build.sh > build/app-open-hang-fix.log 2>&1` still fails on unrelated existing suite blockers: `CaptureReliabilityPhase1CoreTests.testMacScratchDetectorStillRecognizesPlausibleBabyScratchPulsePattern` and the pre-existing `RoutineSessionDrafts` create-new collision
- Follow-up notes: the launch hang was not caused by the new Capture sidebar value models. The saved sample showed `MacCaptureEngine.configureInitialState()` doing synchronous routine-capture recovery/audit scanning on the main thread during init, including `StagedCaptureRecoveryManager.recoverRecordingDirectory`, `latestCompletedRoutineCapture`, and take-audit decoding. Fixed by replacing the inline `rescanRoutineCaptures()` startup call with `scheduleRoutineCaptureRescan()`, moving the recovery/latest-capture/artifact-status scan into a background `Task.detached(priority: .utility)`, and applying the restored status/last-take state back on the main actor once the scan completes. Sidebar simplification remains in place, manual guide drag logic remains unchanged, the direct macOS app build succeeds, and the rebuilt Debug app now launches via `open -n .../ScratchLab.app` and remains running several seconds later (`pgrep -fl ScratchLab`).

- Selected task: user-authorized follow-up to make macOS Capture record readiness independent of pre-roll external audio while keeping invalid targets blocked
- Files changed: `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `DEV_LOG.md`
- Build result: focused escalated `ScratchLabDesktop` record-readiness tests passed (`7` tests, `0` failures); requested `./scripts/build.sh > build/record-readiness-fix.log 2>&1` currently hits an unrelated existing blocker in the broad suite: `CaptureReliabilityPhase1Tests.testMacScratchDetectorStillRecognizesPlausibleBabyScratchPulsePattern` still fails because `MacScratchDetector` returns `nil` for the synthetic positive case
- Follow-up notes: re-audited the Capture button path and confirmed the production record gate is `routineCountInBeat != nil || (!captureEngine.isRoutineRecording && !canStartCaptureRecording)`, where `canStartCaptureRecording` comes from metadata validity, camera selection, and `captureAudioStatus.recordingBlockMessage`. The readiness side was already correctly decoupled from silent live audio for connected inputs, so this pass tightened the regression coverage around the actual acceptance cases: Click, Beat, and No Beat all remain start-allowed with a valid target plus silent connected input; invalid `NM` / `.unknown` targets still block regardless of audio level. Also corrected a downstream diagnostics truthfulness bug where `RoutineAudioDiagnosticsSnapshot.hasRecentAudioActivity` had been populated from `status.isRecordable`, which now means “device connected” rather than “signal present.” The broad full-build blocker is unrelated to Record enablement and remains the earlier Baby Scratch positive detector regression.

- Selected task: user-authorized override to decouple macOS Capture record readiness from pre-roll external audio and stop false Baby Scratch promotion from silent/source-only input
- Files changed: `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `ScratchLabDesktop/Services/MacScratchDetector.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLab/Services/SessionExportCoordinator.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `DEV_LOG.md`
- Build result: focused escalated `ScratchLabDesktop` tests for readiness + negative detector cases passed (`6` tests, `0` failures); the added positive regression `testMacScratchDetectorStillRecognizesPlausibleBabyScratchPulsePattern` still fails because the current raw audio detector does not yet prove a clean synthetic baby pattern; the earlier saved full-build log still reports the unrelated `RoutineSessionDrafts` create-new collision as the broad-suite blocker, and I did not keep waiting indefinitely on another full `./scripts/build.sh`
- Follow-up notes: root cause split in two places. Capture still treated `noSignal` as a hard record blocker even when the selected input was connected, which is wrong for Click/Beat workflows where the app starts timing after Record. Separately, macOS detection still promoted any non-nil `MacScratchDetector` Baby Scratch result straight into `lastScratchDetection`, so audio-only transients could leak into Last Take labels even with silent or non-scratch buffers. Fixed the readiness side by making connected-but-silent input recordable with a warning (`Ready to record — no external audio detected yet.`) and by surfacing `signal silent` diagnostics instead of falsely implying audio is ready. Fixed the false-positive path by making `MacScratchDetector` stricter on onset count / waveform variance and by requiring recent alternating hand-motion evidence in `MacCaptureEngine` before any Baby Scratch result is promoted into UI/review state. Review/Last Take now carry a separate `targetLabel` from the saved sidecar so Target and Detected do not collapse together, and zero-detection takes render `No scratch detected`. Remaining blocker: I have not yet proven the positive Baby Scratch path end-to-end because the new synthetic detector regression still returns `nil`, so I am not marking this task complete or claiming full success yet.

- Selected task: user-authorized override to persist last completed routine diagnostics and make Advanced distinguish live buffers from below-threshold audio
- Files changed: `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' build` passed; `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test` passed with `377` XCTest cases and `35` Swift Testing cases; `python3 scripts/test_capture_pipeline.py` passed (`47/47`); `git diff --check` passed
- Follow-up notes: root cause was that Advanced diagnostics only reflected live in-flight routine state. Once recording stopped, `MacCaptureEngine` cleared the published movement/audio diagnostics, so Review/Advanced had nothing truthful to show and rendered zero-like fallback values instead. The audio status path also treated recent activity and signal percentage as the same concept, so a routed feed could show nonzero peak/RMS while still formatting as `Signal 0%`, which hid the real case: buffers were arriving but the detector threshold had not been met. Fixed by adding persisted `lastCompletedRoutineMovementDiagnostics` / `lastCompletedRoutineAudioDiagnostics`, teaching `MacAnalyzerView` to render those snapshots when live capture is inactive, showing `No capture diagnostics yet` when there has been no routine capture, and extending the audio snapshot with `hasRecentBufferFlow`, `hasRecentAudioActivity`, and `signalThresholdMet` so the UI can distinguish no buffers, below-threshold input, and true signal. Also updated the source regression that still expected the older `audioSignalStatusText` plumbing after the richer diagnostics UI replaced it.

- Selected task: user-authorized override to surface visible Advanced movement/audio diagnostics and diagnose why routine capture reported `Signal: No signal` with only one audio `silenceGap` while `Serato Virtual Audio` was selected
- Files changed: `ScratchLab/Models/ScratchMotionAnalysis.swift`, `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' build` passed; `python3 scripts/test_capture_pipeline.py` passed (`47/47`); `git diff --check` passed; the full `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test` suite is still running, and the newly added diagnostics/audio regressions are already passing inside that run
- Follow-up notes: root cause split in two places. First, the new movement diagnostics path was still effectively developer-only because the counters and rows were gated behind `#if DEBUG`, so Advanced diagnostics could show only the old audio/camera rows in non-DEBUG desktop builds. Second, routine audio diagnostics had no way to explain whether `No signal` meant no buffers, stale buffers, or very low-level routed audio, and `ScratchAudioNotationDetector` still used fixed absolute activity floors that are too strict for some virtual-audio feeds, which can collapse a real routed signal into one long `silenceGap`. Fixed by promoting movement diagnostics publishing out of compile-time DEBUG gating, adding visible `Movement Diagnostics` / `Audio Diagnostics` sections in `MacAnalyzerView`, publishing selected-device / attached-input / buffer / event / peak / rms / no-signal-reason snapshots from `MacCaptureEngine`, and making `ScratchAudioNotationDetector` use adaptive activity/release floors so low-level routed bursts still register without inventing events from silence. Existing direction normalization, truthful movement rendering, and audio-only behavior remain intact.

- Selected task: user-authorized override for diagnosing why manual baby scratch capture still collapsed to one `recordMovementEvent`
- Files changed: `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' build` passed; `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test` passed with `374` XCTest cases and `35` Swift Testing cases; `python3 scripts/test_capture_pipeline.py` passed (`47/47`); `git diff --check` passed
- Follow-up notes: measured loss was still upstream of notation rendering. Real routine capture was sampling hand pose at only `0.12s`, which is too sparse for repeated baby-scratch reversals, and the saved snapshot path was still discarding truthful normalized motion unless it passed the stricter trust/fusion gate used to promote notation from `partial` to `detected`. Fixed by reducing routine-recording hand-pose cadence to `0.04s`, preserving the earlier forward/back normalization helper, saving normalized `candidateMovementEvents` directly into `recordMovementEvents` while keeping trust-gated motion only for `notationSource` confidence, and adding DEBUG-only movement diagnostics that count frames, hand observations, direction changes, builder samples, raw/normalized/fused/final movement events, and reason-specific drops. Surfaced the summary inside the existing macOS Performance Diagnostics card and updated regressions so low-confidence motion still drops cleanly, off-timing truthful motion remains visible in partial notation, alternating builder output survives, and audio-only captures remain movement-free.

- Selected task: user-authorized override for improving captured record movement stroke detection sensitivity
- Files changed: `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' build` passed; focused movement regression tests passed in isolated DerivedData (`testRoutineDetectedNotationBuilderEmitsMultipleAlternatingMovementEventsForBabyScratchPhrase`, `testRoutineDetectedNotationBuilderSplitsDirectionChangesInsteadOfMergingStrokes`, `testRoutineDetectedNotationBuilderKeepsNormalBabyScratchRepetitionsAboveSuppressionThresholds`, `testRoutineDetectedNotationBuilderRejectsTinyJitterWithoutCreatingMovementEvents`); `python3 scripts/test_capture_pipeline.py` passed (`47/47`); `git diff --check` passed; the first non-escalated full `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test` rerun was blocked by sandbox permission errors writing Xcode's default `XCBuildData`, and an escalated rerun of the same command is actively progressing through the full suite with the new movement tests already passing
- Follow-up notes: root cause was in `RoutineDetectedNotationBuilder`, not notation drawing. The old builder only observed hand position when `HandMotionState` changed, so a real baby-scratch stroke was often measured from its start to the delayed commit point of the next opposite direction instead of to the furthest excursion reached during that stroke. Those collapsed deltas then failed `RoutineNotationEventNormalizer` thresholds, which explained why long audio phrases still exported only 1-3 movement events. Fixed by having `MacCaptureEngine` feed every published routine-capture hand observation into the builder, tracking each active stroke's latest and furthest confirmed position/time, finishing the stroke at its furthest excursion when direction changes or capture ends, and then starting the next direction from the new committed sample. Preserved the earlier `normalizedRecordDirection(forCameraSpaceDirection:)` source mapping, slightly lowered `minStrokeDuration`, `minPositionDelta`, and `minSpeedForStroke` to keep shorter truthful baby-scratch passes, and added regressions for alternating multi-stroke phrases, direction-change splitting, repetition survival, jitter rejection, and source wiring to `recordObservation`.

- Selected task: Fix routine capture forward/back direction inversion at the detection source of truth
- Files changed: `ScratchLabDesktop/Services/HandDirectionTracker.swift`, `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `ScratchLabDesktopTests/NotationSheetTests.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' build` passed; the first direct `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test` attempt was invalid because it overlapped another Xcode job and hit a locked build database; `python3 scripts/test_capture_pipeline.py` passed (`47/47`); `git diff --check` passed; clean reruns of the desktop test suite via `./scripts/build.sh` and `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' -derivedDataPath build/DerivedData-direction-fix test` were started, and the new direction regressions were already passing during those runs when output collection moved on
- Follow-up notes: root cause was not in the notation renderer. `CapturedNotationDisplayView` already draws whatever `recordMovementEvents.direction` says, and export persists those same strings. The inversion came earlier: `HandDirectionTracker` reports raw unmirrored camera-space X motion, while `MacCaptureEngine` had been mapping positive-X directly to semantic record `forward` and negative-X directly to `backward`. Manual capture proved that assumption was reversed for the real routine-capture setup, so live guidance, saved notation events, and captured notation previews all faithfully showed the wrong semantic direction. Fixed by adding a single `normalizedRecordDirection(forCameraSpaceDirection:)` helper in `MacCaptureEngine` and routing tracker output through it before publishing `HandMotionState`, keeping the correction at the detection source rather than swapping labels later. Added regressions for the normalization helper, source-level use of that helper, forward/back preservation in detected previews, renderer reliance on event-direction strings, continued audio-only honesty when no movement exists, and the existing no-CXL-string requirement.

- Selected task: Fix captured audio-only notation review UI
- Files changed: `ScratchLabDesktop/Views/NotationVisualizerView.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLabDesktopTests/NotationSheetTests.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' build` passed; the first direct `xcodebuild ... test` attempt was invalid because it overlapped another Xcode job and hit a locked build database, then a clean rerun with `-derivedDataPath build/DerivedData-notation-audio-only-rerun` passed with `367/367` XCTest plus `33/33` Swift Testing notation tests; `python3 scripts/test_capture_pipeline.py` passed (`47/47`); full `./scripts/build.sh` passed end-to-end after updating the stale source assertion for the new review copy; `git diff --check` passed
- Follow-up notes: root cause was that captured partial notation still assumed the user should wait for movement confirmation, so Review and Advanced either hid the saved snapshot behind text-only fallback copy or rendered it as a thin low-contrast audio lane that was not useful to inspect. Fixed by keeping captured/template mode separation intact, preserving real detected record strokes whenever `recordMovementEvents` exist, and treating audio-only captured takes honestly: Review now titles the stage `Captured Notation`, the captured partial state says `Audio-only notation`, `Movement direction was not detected for this take.`, and `Record movement not detected`, the captured renderer filters demo/template source labels out of captured mode, exposes a readable count strip with real movement/audio/fader/MIDI counts, uses a stronger time grid, and expands the partial audio lane to a larger rounded-block timeline while still showing `No fader data` when appropriate. Added source/model regressions for the new captured partial wording, captured/template label separation, and the review-preview source expectation that had to be updated from the older `Direction pending video/motion confirmation` copy.

## 2026-05-06
- Selected task: user-requested fix for shared Capture/Practice audio input selection so Capture auto-selects Serato Virtual Audio without visiting Practice
- Files changed: `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `DEV_LOG.md`
- Build result: `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' build` passed; `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test` passed with `347/347` XCTest plus `13/13` Swift Testing notation timeline tests; `python3 scripts/test_capture_pipeline.py` passed (`45/45`); `git diff --check` passed; `./scripts/build.sh` was started and advanced through the capture-pipeline suite plus the embedded `ScratchLabDesktop` XCTest leg, but it did not reach a final shell exit inside this turn before output collection moved on
- Follow-up notes: root cause was that macOS device discovery already persisted a shared `selectedAudioDeviceUniqueID`, but refresh logic only fell back to the first discovered input when the saved device was missing and Capture never proactively re-applied a Serato-first policy on entry. That left Capture dependent on whichever route Practice had last touched, and when Serato Virtual Audio appeared later it would not win automatically unless the user visited Practice or manually changed the picker. Fixed by adding a shared audio-selection policy in `MacCaptureEngine` that distinguishes explicit user choice from automatic fallback, preserves a valid manual selection, otherwise prefers exact `Serato Virtual Audio`, then other Serato-like devices, then the prior valid route, system default input, and finally the first available input. Capture now calls that policy on appear, when the Capture tab becomes active, and when the app re-enters the foreground on Capture. Both Practice and Advanced audio pickers now route through the same explicit-selection setter, Capture exposes its own shared audio picker plus `Use Serato Audio` action, input status shows the selected device name directly through the shared status line, missing-input state now reads as `Audio Missing`, and routine sidecar/export metadata still records the actual selected audio device name and unique ID at record start. Added policy regressions for Serato preference, Serato-like fallback, preserved explicit choice, missing-device fallback, default/first fallback, and source regressions for Capture auto-selection, selected-device display, Serato action visibility, and metadata wiring.

- Selected task: improve real detected-notation event quality by filtering jittery motion segments before Review/export
- Files changed: `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `DEV_LOG.md`
- Build result: `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' build` passed; `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test` passed (`322/322` XCTest plus `13/13` Swift Testing notation timeline tests); `./scripts/build.sh` passed the capture-pipeline fixture suite (`45/45`), the full `ScratchLabDesktop` XCTest plan (`322/322` plus `13/13` Swift Testing notation timeline tests), and the iOS/watchOS build legs; `git diff --check` passed
- Follow-up notes: the previous audio+video notation fusion path was still classifying weak hand jitter as directional strokes because the routine notation builder only enforced a minimum duration, defaulted low-speed movement to `slowDrag/slowPullDrag`, and never merged adjacent same-direction fragments. Added a shared `RoutineNotationEventNormalizer` with named tuning thresholds for minimum stroke duration, position delta, speed, merge gap, and fast/slow classification boundaries. Provisional motion events are now saved with neutral `hold` classification first, then normalized in one place: tiny deltas and near-zero speeds are dropped, same-direction fragments with small gaps are merged, motion overlapping real audio gets a confidence lift, unmatched motion is downgraded, and only meaningful motion is promoted to `fastPush/normalPush/slowDrag` or `fastPull/normalPull/slowPullDrag`. This keeps audio-only captures in `partial` notation instead of faking detected strokes when everything directional filters out. Added regressions for dropping near-zero noise, merging adjacent forward segments, classifying forward/backward speed buckets correctly, falling back to `partial` when motion is only jitter, recomputing detected notation confidence from the filtered movement set, and requiring a minimum raw motion confidence before an audio overlap can upgrade a segment into trusted fused directional notation. Follow-up verification for the honesty threshold change: `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' build` passed again; `python3 scripts/test_capture_pipeline.py` passed again (`45/45`); focused notation fusion tests passed for low-confidence partial fallback, trusted fused detection, and filtered-confidence detection; `git diff --check` passed again. A full `xcodebuild ... test` rerun was started after the fix and the only failure it had exposed earlier was the now-fixed low-confidence directional fusion regression.

## 2026-05-06
- Selected task: add audio-derived routine notation detection and fuse it with saved take/export notation snapshots
- Files changed: `ScratchLab/Models/ScratchMotionAnalysis.swift`, `ScratchLab/Models/CaptureCore.swift`, `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `ScratchLab/Services/SessionExportCoordinator.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `DEV_LOG.md`
- Build result: `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' build` passed; `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test` passed with `317/317` XCTest plus `13/13` Swift Testing notation timeline tests; `./scripts/build.sh` passed the capture-pipeline fixture suite (`45/45`) and advanced through the repeated `ScratchLabDesktop` XCTest leg before failing in a later local test-host run when the launched app lost its IDE helper connection (`Failed to establish connection to the IDE`), which appears to be an environment/test-runner issue rather than a code assertion or compile failure; `git diff --check` passed
- Follow-up notes: the previous notation export fix only persisted hand-direction-derived movement events, which meant a take with clear scratch audio but weak hand tracking still exported `notationSource: unavailable`. Added a shared `ScratchAudioNotationDetector` that segments routine-capture audio into burst/gap candidates with timing, level, confidence, and event-kind metadata, then fed that detector from live routine audio buffers without touching the raw audio stem. Extended the saved take notation snapshot with `audioEvents` and `detectionSources`, and added a small fusion stage in `MacCaptureEngine` that uses audio as the primary timing source while promoting overlapping hand-motion segments into fused `recordMovementEvents` with direction when video agrees. Export now supports `notationSource: detected`, `partial`, and `unavailable`, includes `audioEvents` plus `detectionSources` in `take-XXX_detected_notation.json`, keeps `notationConfidence` nil only when no notation exists at all, and no longer reports a take as unavailable when audio-only notation was detected. Review now shows a real mini notation preview only for saved directional movement events, and otherwise surfaces an honest partial-audio message when only audio events were captured. Added regressions for audio burst/gap detection, partial notation export semantics, saved sidecar persistence of audio events, and the review source guard for partial notation messaging.

- Selected task: user-requested P0 fix for Practice launch crash from invalid audio percent display
- Files changed: `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' build` passed; `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test` passed with 273 XCTest cases and 13 Swift Testing notation timeline tests; `./scripts/build.sh` remains blocked by local environment because Xcode cannot find the iOS 26.4 platform for the `ScratchLab` scheme's `generic/platform=iOS` build leg
- Follow-up notes: root cause was a launch-time display trap, not a camera/Vision crash. `MacCaptureEngine.formattedAudioPercent` converted `audioLevel` with raw `Int(audioLevel * 100)`, so any non-finite startup value could crash the Practice header on the main thread before live input had settled. Fixed by adding a safe clamped formatter, a published-audio readiness flag, a Practice-specific audio status string for `Choose input` / `Audio Missing` / real percentages, and sanitizing the audio publish path so invalid levels never propagate back into the UI. The existing capture/vision gate still limits heavy processing to active live input, recording, CXL recording, or performer-monitor demand.

- Selected task: user-requested P0/P1 fix for Advanced raw JSON hang, cloud-save export reliability, and notation layout verification
- Files changed: `ScratchLab/Services/StagingInspector.swift`, `ScratchLab/Views/StagingInspectorView.swift`, `ScratchLab/Services/SessionExportCoordinator.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLabDesktop/Views/NotationVisualizerView.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' build` passed; `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test` passed with 268 XCTest cases and 13 Swift Testing notation timeline tests; `./scripts/build.sh` is still blocked by local environment because Xcode cannot find the iOS 26.4 platform for the `ScratchLab` scheme's `generic/platform=iOS` build leg, but the capture-pipeline fixture tests and full `ScratchLabDesktop` XCTest leg inside the script passed before that platform failure
- Follow-up notes: root cause was split across three places. The Advanced `Raw JSON/sidecar inspection` button was accidentally wired to the heavy staging inspector sheet, which refreshes staged-session/quarantine state and can feel like a hang when opened from that label. The export flow already built ZIPs in a temp directory, but the final user-selected save step needed cloud-folder-friendly write coordination and a save-specific failure message for Drive/iCloud-style destinations. The main layout routing was already `Practice / Capture / Review / Advanced`; the “old notation view” report maps to the Advanced Notation Lab being the technical notation surface, so this pass relabels it more clearly instead of changing primary navigation.

- Selected task: user-requested layout polish for Practice / Capture / Review / Advanced
- Files changed: `ScratchLab/Models/CaptureCore.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLabDesktop/Views/NotationVisualizerView.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `scripts/test_capture_pipeline.py`, `TASKS.md`, `DEV_LOG.md`
- Build result: requested `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' build` passed; requested `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test` passed with 261 XCTest cases and 13 Swift Testing notation timeline tests; `./scripts/build.sh` passed end-to-end with 36 capture-pipeline fixture tests, the desktop test plan, and iOS, macOS, and watchOS build legs
- Follow-up notes: root cause was that the first Practice/Capture/Review/Advanced refactor kept a few placeholder-style states and shared validation rules that were still tuned for metadata completeness rather than fast capture flow. The macOS Capture path still surfaced performer-name/BPM validation, fallback session naming still leaked `Unknown` into blank titles, mixer readiness could go green without real mixer-like hardware, Review always had a mini notation strip available even without real take data, and Advanced diagnostics exposed activity without clearly labeling whether it came from Live Input or retained diagnostic timing. Fixed the shared macOS routine setup to allow blank performer/session metadata, suppress `.unknown` in user-facing session names, auto-create routine sessions before Record, resolve blank metadata to `Untitled Session`, `Unknown Performer`, and timestamped `CXL Take` labels, gate mixer ready state on actual mixer-style hardware detection, hide Review notation previews until real captured events/detections exist, and reset notation diagnostics to idle when playback is inactive so the tick-rate card only stays busy when a visible reason exists. Practice copy now stays demo-focused and App Review-safe, while Advanced keeps the technical diagnostics/tooling path. No bundle IDs, signing, export settings, App Store metadata, screenshots, or resources were changed.

- Selected task: user-requested P0 fix for Coach Pause correctness and paused CPU work
- Files changed: `ScratchLab/Audio/AudioEngine.swift`, `ScratchLab/Models/CaptureCore.swift`, `ScratchLab/Services/SessionExportCoordinator.swift`, `ScratchLab/Views/ScratchCoachViews.swift`, `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLabDesktop/Views/NotationVisualizerView.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: `xcodebuild -list` passed; requested `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' build` passed; requested `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test` passed with 256 XCTest cases and 13 Swift Testing notation timeline tests; `./scripts/build.sh` passed end-to-end with capture-pipeline fixture tests, the desktop test plan, and iOS, macOS, and watchOS build legs; the requested residual source scan was reviewed; `git diff --check` passed
- Follow-up notes: root cause was that Coach and Notation shared the Baby Scratch coordinator inconsistently: UI could say paused while work still advanced from periodic timers, TimelineView frames, demo auto-replay, or capture analysis. Fixed by making `BabyScratchDemoPlaybackCoordinator.playbackState` the single playback truth; making pause hold current audio time on the same audio player that play/replay start; preventing auto-replay outside `.playing`; making Notation ticking a cheap no-op while paused/stopped; and making the Baby Scratch coach pose come directly from coordinator audio time rather than wall-clock or buffered smoothing. Added signposts and Advanced-only diagnostics so the remaining real bottleneck can be identified in Instruments without user-facing debug HUDs. Camera/deck/audio processing is now gated to active recording/live-monitor states rather than running as default idle work. No bundle IDs, signing, App Store metadata, screenshots, export settings, app identity, notation resources, or dataset architecture were changed. Remaining risk is empirical performance: manual Instruments profiling should confirm which active path dominates when the app is intentionally playing, listening, or recording.

## 2026-05-05
- Selected task: refactor ScratchLab Desktop layout into Practice, Capture, Review, and Advanced
- Files changed: `ScratchLab/Models/CaptureCore.swift`, `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' build` passed; `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test` passed with 252 XCTest cases and 13 Swift Testing notation timeline tests; `./scripts/build.sh` passed with 36 capture-pipeline fixture tests, the desktop test plan, and iOS, macOS, and watchOS build legs; `git diff --check` passed
- Follow-up notes: the app had exposed too many internal technical surfaces as primary navigation, making normal practice, App Review, and CXL recording compete with analyzer, notation, calibration, dataset, and diagnostic tooling. The desktop shell now has four clear modes: Practice for coach/demo use, Capture for CXL recording, Review for label acceptance/correction and ZIP export, and Advanced for notation/analyzer/diagnostics/calibration/export-manifest/timing tools. Capture now uses target and timing-mode choices with readiness cards and one main Record/Stop action; Review preserves raw capture media while writing accept/correct/unknown label decisions into the existing local recording sidecar with a `label_reviewed` audit event so the decision travels with the exported ZIP. No bundle IDs, signing, App Store metadata, export/upload settings, build numbers, or app identity were changed.

## 2026-05-05
- Selected task: user-requested P0 fix for Baby Scratch Notation Lab / Coach runtime playback regression
- Files changed: `ScratchLab/Models/CaptureCore.swift`, `ScratchLab/Views/ScratchCoachViews.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLabDesktop/Views/NotationVisualizerView.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: initial requested `xcodebuild -scheme ScratchLabDesktop -destination 'platform=macOS' test` was blocked by sandboxed DerivedData / `.xcresult` writes, then passed after rerunning with permission; requested `xcodebuild -scheme ScratchLabDesktop -destination 'platform=macOS' build` passed; `./scripts/build.sh` passed end-to-end with 36 capture-pipeline fixture tests, the full `ScratchLabDesktop` XCTest plan at 247 tests and 0 failures plus 13 Swift Testing notation timeline tests, and iOS, macOS, and watchOS build legs; `git diff --check` passed
- Follow-up notes: root cause was that the shared Baby Scratch coordinator existed but did not guarantee runtime configuration before playback, while Notation Lab set its own `isPlaying` before the audio player had actually started. That left the master clock at zero, so the WAV was silent and the playhead/strokes did not move. The Coach path also still allowed raw-player actions and buffered sample-derived Baby Scratch animation, so it could skip configuration and remain visibly late. Fixed by adding `configureBabyScratchIfNeeded()`, `playBabyScratch()`, `pause()`, `stop()`, `replayBabyScratch()`, readiness, and error state to `BabyScratchDemoPlaybackCoordinator`; making play/replay fail closed when audio cannot start; wiring Notation Lab and macOS Coach through the coordinator; updating Notation ticking to read `demo.currentAudioTime`; and sampling Baby Scratch rig pose directly from `BabyScratchDemoPlaybackCoordinator.coachPose(for:)` with no buffered smoothing path for Baby playback. Added regressions for bundled audio existence/duration, coordinator configuration and failure behavior, phrase/silence mapping, active pose movement, silence hold, source-level single coordinator ownership, and Coach button wiring. No bundle IDs, signing, app identity, export settings, App Store metadata, or external assets were changed.

## 2026-05-02
- Selected task: user-requested fix for lower scroll-down ScratchLab Coach Rig Baby Scratch perspective geometry
- Files changed: `ScratchLab/Views/ScratchCoachViews.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: focused lower-rig perspective/timing regressions passed with 12 selected tests and 0 failures; `./scripts/build.sh` passed end-to-end with 36 capture-pipeline fixture tests, the full `ScratchLabDesktop` XCTest plan at 186 tests and 0 failures, iOS/macOS builds, and the watchOS build; regenerated `scripts/__pycache__` was removed after the build; `git diff --check` passed
- Follow-up notes: root cause was that the previous lower-rig pass fixed cadence but still used direct viewer/screen angles for coach-authored clock positions. That rendered Baby Scratch as viewer `9 -> 11` hand motion and `12 -> 2` sticker motion, while the front-facing perspective needs a 180-degree clock conversion: coach `9 -> 11` becomes viewer `3 -> 5`, and coach `12 -> 2` becomes viewer `6 -> 8`. Fixed `ScratchCoachRigGeometry` with explicit coach-space constants plus `frontFacingViewerHour(coachHour:)` and `viewerAngleDegrees(coachHour:)`, replaced the record sticker dot with a radial `Capsule` line marker that sits inside the rotating record group, and moved the static tone arm away from the viewer `3 -> 5` hand-motion area to top-left/top-safe offsets (`pivot -0.62/-1.18`, `stylus -0.10/-0.58`). Existing Baby Scratch timing stayed unchanged at `strokeDuration 0.14s`, `holdDuration 0.08s`, `cycleDuration 0.44s`, and `babyScratchDemoTempoMultiplier 2.5`. Crossfader remains centered/open below volume faders, the left/crossfader hand remains fixed, and no live capture, export/upload, version/build number, bundle ID, signing, or deployment-target code was changed.

- Selected task: user-requested fix for the lower scroll-down ScratchLab Coach Rig Baby Scratch geometry and timing against the bundled demo audio
- Files changed: `ScratchLab/Models/CaptureCore.swift`, `ScratchLab/Views/ScratchCoachViews.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: focused lower-rig/demo timing regressions passed with 14 selected tests and 0 failures; the first full `./scripts/build.sh` exposed one stale repeatability assertion still sampling the old slow-cycle timestamp, that assertion was updated to the new active burst timestamp and passed as a focused test, then `./scripts/build.sh` passed end-to-end with 36 capture-pipeline fixture tests, the full `ScratchLabDesktop` XCTest plan at 186 tests and 0 failures, iOS/macOS builds, and the watchOS build; regenerated `scripts/__pycache__` was removed after the build; `git diff --check` passed
- Follow-up notes: root cause was that the previous lower-rig fix reached the correct shared `ScratchCoachRigView`, but Baby Scratch timing still used an old `1.20s` visual-cycle reference/long segment interpretation and the rig still needed explicit angular hand/sticker mapping for the faster visual model. Fixed the shared motion pattern with `strokeDuration 0.14s`, `holdDuration 0.08s`, `cycleDuration 0.44s`, `babyScratchDemoTempoMultiplier 2.5`, `babyScratchDemoPhaseOffset 0`, and `60deg` record/sticker travel; fixed the lower rig geometry with a 9 o'clock to 11 o'clock record-hand path (`180deg -> 240deg`) and a 12 o'clock to 2 o'clock sticker path (`-90deg -> -30deg`) that applies the same relative offsets for left and right deck centers. The exact lower component remains `ScratchCoachRigView` in `ScratchLab/Views/ScratchCoachViews.swift`, rendered by lower scrollable Coach cards in `PracticeModeView` and `MacAnalyzerView`; `MainMenuView` still supplies its Try Demo custom provider path. Tone arm remains static and right/top battle-safe, Baby Scratch crossfader remains centered/open below volume faders, and the left/crossfader hand remains fixed. Live capture, export/upload, version/build numbers, bundle IDs, signing, and deployment targets were not changed.

- Selected task: user-requested fix for the lower scroll-down ScratchLab Coach Rig geometry and Baby Scratch motion mapping
- Files changed: `ScratchLab/Models/CaptureCore.swift`, `ScratchLab/Views/ScratchCoachViews.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: focused lower-rig regressions passed with 9 selected tests and 0 failures; `./scripts/build.sh` passed end-to-end, including capture-pipeline fixtures, the full `ScratchLabDesktop` XCTest plan, iOS/macOS app builds, and the watchOS build; regenerated `scripts/__pycache__` was removed after the build; `git diff --check` passed
- Follow-up notes: root cause was that the previous timing fix reached the shared lower `ScratchCoachRigView`, but the rig still had incomplete Baby Scratch geometry and idle-state guards. The exact lower component is `ScratchCoachRigView` in `ScratchLab/Views/ScratchCoachViews.swift`, rendered through `ScratchCoachCardContent` by the lower scrollable Coach cards in `PracticeModeView` and `MacAnalyzerView`; `MainMenuView` still supplies a custom Try Demo animation provider. Fixed the shared lower rig with hard constants for the fixed right-side tone arm (`pivotOffset 1.42/-0.92`, `stylusOffset 0.72/-0.26`), 9 o'clock record hand (`-0.92/0`), 12 o'clock rotating record sticker (`0/-0.72`), fixed Baby left-hand pose (`0.50/0.66`), centered/open Baby crossfader (`0.5`), and mixer ordering (`volumeFaderYRatio 0.46`, `crossfaderYRatio 0.80`). Baby Scratch now holds still with the fader centered/open during lower-rig pause, silence, and profile-loading fallback; active Baby motion only moves the record/platter/right hand. Live capture, export, upload, bundle IDs, version/build numbers, signing, and deployment targets were not changed.

## 2026-05-02
- Selected task: user-requested macOS App Store archive path fix for `ScratchLabDesktop` version `1.0.1` build `18`
- Files changed: `ScratchLabDesktop/Info.plist`, `TASKS.md`, `DEV_LOG.md`
- Build result: initial `git status --short` showed existing dirty `ScratchLab.xcodeproj/project.pbxproj` and `ScratchLab/Info.plist`; `ScratchLabDesktop` Release build settings reported `PRODUCT_BUNDLE_IDENTIFIER = com.machelpnz.scratchlab`, `MARKETING_VERSION = 1.0.1`, `CURRENT_PROJECT_VERSION = 18`, `INFOPLIST_FILE = ScratchLabDesktop/Info.plist`, and `SKIP_INSTALL = NO`; the iOS `ScratchLab` Release target still reported `IPHONEOS_DEPLOYMENT_TARGET = 18.0`; `rm -rf scripts/__pycache__` left no regenerated Python cache; `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -configuration Release -destination 'generic/platform=macOS' -archivePath /Users/karlwatson/Library/Developer/Xcode/Archives/2026-05-02/ScratchLabDesktop-1.0.1-18-111830.xcarchive archive` passed with `** ARCHIVE SUCCEEDED **`
- Follow-up notes: root cause was that the macOS target build settings had already moved to build `18`, but `ScratchLabDesktop/Info.plist` still hard-coded `CFBundleVersion` to `15`, so the archive metadata could remain stale even though the target setting looked correct. Fixed only the desktop plist by changing `CFBundleVersion` to `$(CURRENT_PROJECT_VERSION)`. Verified the resulting archive at `/Users/karlwatson/Library/Developer/Xcode/Archives/2026-05-02/ScratchLabDesktop-1.0.1-18-111830.xcarchive`: archive `Info.plist` reports `ApplicationPath = Applications/ScratchLab.app`, `CFBundleIdentifier = com.machelpnz.scratchlab`, `CFBundleShortVersionString = 1.0.1`, `CFBundleVersion = 18`, and architectures `x86_64`/`arm64`; the processed app plist reports bundle ID `com.machelpnz.scratchlab`, version `1.0.1`, build `18`, and category `public.app-category.music`. No archive was deleted and no upload was performed. Note: archive signing identity is currently `Apple Development: Karl Watson (5C7ZHKGE7L)`; Xcode Organizer/App Store Connect distribution can re-sign during the explicit upload/export step.

- Selected task: user-requested fix for the iOS 16 AVAsset track-loading deprecation warning in `SessionExportCoordinator`
- Files changed: `ScratchLab/Services/SessionExportCoordinator.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: focused `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testSessionExportCoordinatorUsesAsyncAVAssetTrackLoading` passed; `/usr/libexec/PlistBuddy -c "Print :CFBundleShortVersionString" ScratchLab/Info.plist` returned `1.0.1`; `/usr/libexec/PlistBuddy -c "Print :CFBundleVersion" ScratchLab/Info.plist` returned `16`; requested `grep -Rni "tracks(withMediaType:)" ScratchLab ScratchLabDesktop` returned no matches; `rg -n "\\.tracks\\(withMediaType:" ScratchLab ScratchLabDesktop` returned no matches; `./scripts/build.sh` passed end-to-end, including capture-pipeline fixtures, the full `ScratchLabDesktop` XCTest plan, and iOS/macOS/watchOS builds; regenerated `scripts/__pycache__` was deleted after the build; `git diff --check` passed
- Follow-up notes: root cause was that `extractLinearPCMAudio` still synchronously called `asset.tracks(withMediaType: .audio).first`, which is deprecated on iOS 16 and later, while the broader archive/package creation path is intentionally synchronous. Fixed with the smallest API change by reusing the existing `runAsyncProbe` bridge around `try await asset.loadTracks(withMediaType: .audio)`, preserving the same media type, first-track selection, missing-audio error, PCM conversion, export naming, and archive behavior. Added a source regression that fails if the deprecated selector returns in `SessionExportCoordinator.swift` and confirms both audio and video track probes use `loadTracks`. `ScratchLab.xcodeproj/project.pbxproj` was already dirty with the intentional `1.0.1` / `16` project version bump before this task; this change did not edit deployment targets. The iOS app target Debug/Release settings remain `IPHONEOS_DEPLOYMENT_TARGET = 18.0`; two project-level default `26.0` entries are unchanged from `HEAD`.

- Selected task: user-requested App Review demo mode that completes without external hardware
- Files changed: `ScratchLab/Views/MainMenuView.swift`, `ScratchLab/Models/CaptureCore.swift`, `ScratchLab/Services/SessionExportCoordinator.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: focused `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testDemoModeProducesFeedbackWithoutHardware -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testDemoModeExportSucceeds` passed; direct `xcodebuild -project ScratchLab.xcodeproj -target ScratchLab -configuration Debug -destination 'generic/platform=iOS' CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO build` passed; `./scripts/build.sh` passed end-to-end, including capture-pipeline fixtures, the full `ScratchLabDesktop` XCTest plan, and iOS/macOS/watchOS builds; generated `scripts/__pycache__` was deleted after the build; `git diff --check` passed
- Follow-up notes: root cause was that App Review could still land in live practice or capture paths where real input setup is expected before meaningful feedback/export can be demonstrated; on macOS, the analyzer also started camera/microphone capture immediately on first appearance. Fixed by adding a first-launch-visible `Try Demo` entry to the iOS main menu, adding the same bundled-data demo card/stage to the macOS first workspace, making macOS live input an explicit `Start Live Input` action, auto-loading `CoachDemoAudio/baby_noBeat.wav`, playing it through the existing local coach demo player, and deriving a smoothed motion proxy from that same WAV into `ScratchMotionAnalyzer` so the feedback card moves from Listening into Balanced/Unbalanced states without camera, watch, or mic startup. Demo export now builds a temporary `demo_mode` `SessionExportPackage` with the bundled WAV, a generated deck-view MOV, safe local sidecar metadata, no watch capture, and the existing Scratch Only ZIP archive pipeline. Real live capture remains available after the explicit live-input action.

- Selected task: user-requested removal of external training dataset references from the App Review build
- Files changed: `ScratchLab/Audio/AudioEngine.swift`, `ScratchLabDesktop/Services/MacScratchDetector.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: focused `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testMacScratchDetectorTrainingLookupReturnsEmptyWithoutBundledTrainingLibrary -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testTrainingAudioLookupSourcesUseFallbackWhenBundledLibraryIsMissing -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testTrainingAudioLookupSourcesGateTrainingPathsOutOfRelease` passed; requested `grep -RniE "cxl|reference_pro|reference_champ|reference_beginner" ScratchLab` reported only binary matches in `ScratchLab/Resources/Coach/Happy Idle.usdz` and `ScratchLab/Resources/Coach/Coach.usdz`; text/runtime source scans over `ScratchLab/Audio` and `ScratchLabDesktop/Services` show no external dataset path names; `./scripts/build.sh` passed end-to-end, including capture-pipeline fixtures, the full `ScratchLabDesktop` XCTest plan, and iOS/macOS/watchOS builds; regenerated `scripts/__pycache__` was deleted after the build; `git diff --check` passed
- Follow-up notes: root cause was that optional baby-scratch training calibration still looked for a runtime training-library folder even though release builds must not reference external scratch datasets. Fixed by adding `babyTrainingFolderPath` in both `ScratchPatternMatcher` and `MacScratchDetector`: Debug can look under the neutral `internal_training/baby_scratch`, while Release returns `nil`; `bundledBabyTrainingFiles(in:)` now returns `[]` immediately when that path is nil. With no training files, both analyzers fall back to their built-in default baby profile, so audio engine startup, scratch detection, Practice mode, and Coach demo playback remain independent of training assets.

- Selected task: user-requested cleanup of accidental Python cache tracking and P0-2 placeholder tutorial video review risk
- Files changed: `.gitignore`, `ScratchLab/Audio/BackingTrackManager.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: focused `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testPracticeModeSourceDoesNotExposePlaceholderTutorialEntryPoints -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testScratchDefinitionsDoNotExposeTutorialVideoAssetReferencesInRelease -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testPracticeModeSourceKeepsLaunchFlowWithoutTutorialAssets -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testPythonBytecodeCachesAreIgnoredAndUntracked` passed; requested `grep -Rni "TutorialOverlayView\|tutorialVideoName\|showingTutorial\|play.circle.fill" ScratchLab ScratchLabDesktop ScratchLab.xcodeproj` returned no matches; `git ls-files 'scripts/__pycache__/*' '*.pyc'` returned no tracked cache files; `./scripts/build.sh` passed end-to-end, including capture-pipeline fixtures, the full `ScratchLabDesktop` XCTest plan, and iOS/macOS/watchOS builds; regenerated `scripts/__pycache__` was deleted after the build; `git diff --check` passed
- Follow-up notes: root cause was that a generated Python cache file had previously slipped into source control and App Review needed a clean guarantee that placeholder tutorial-video UI cannot be reached in release. The cache path was already absent from the current index, so no `git rm --cached` path was available; kept the CoachInstructions regression tests intact, added an explicit Python cache ignore header for the existing `__pycache__/` and `*.pyc` rules, and removed the regenerated working-tree cache directory. The P0-2 audit found `PracticeModeView.swift` and `Scratch.swift` already free of `TutorialOverlayView`, `tutorialVideoName`, and `showingTutorial`; the only requested-grep hit was an unrelated legacy backing-track `play.circle.fill`, which now uses `play.fill`/`pause.fill` so the review grep is clean. Practice and coach setup flow still launches through `SessionSetupOverlay`, `ScratchCoachCard`, and `startSession()` without tutorial assets.

- Selected task: user-requested P0-1 removal of coach instruction provenance metadata from bundled resources
- Files changed: `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: focused `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testCoachInstructionsResourceFolderIsBundledForIOSAndMacTargets -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testCoachInstructionResourcesContainNoProvenanceMetadataAndDecode -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testHostedDesktopBundleContainsCoachInstructionResources` passed; requested `grep -RniE "sourceReference|rightsStatus|reviewStatus|sourceRoot|sourceMKV|makemkv|MakeMKV|qbert|QBERT|Qbert|cxl|CXL" ScratchLab/Resources/CoachInstructions ScratchLab.xcodeproj` returned no matches; `./scripts/build.sh` passed end-to-end, including capture-pipeline fixtures, the full `ScratchLabDesktop` XCTest plan, and iOS/macOS/watchOS builds; `git diff --check` passed
- Follow-up notes: root cause was that App Review needed an explicit regression guard proving bundled CoachInstructions resources cannot ship source/provenance/review metadata. The bundled JSON was already clean, so no JSON fields or runtime coach playback behavior changed. Fields verified absent: `sourceReference`, `rightsStatus`, `reviewStatus`, `sourceRoot`, `sourceMKV`, `makemkv`/`MakeMKV`, `qbert` variants, and `cxl`/`CXL`. Added coverage for source resource decode, forbidden metadata scans, hosted macOS bundle loading/decoding, and iOS/macOS project resource-phase inclusion. CoachInstructions remains bundled.

- Selected task: user-requested removal of Game Center App Review risk for the current release
- Files changed: `ScratchLab/Info.plist`, `ScratchLab/Services/ProgressManager.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: focused `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testLocalProgressUpdatesWithGameCenterDisabledByDefault -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testGameCenterDashboardIsDisabledForAppReviewBuilds -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testGameCenterLeaderboardCodeIsDebugAndFeatureFlagGated` passed; `xcodebuild -project ScratchLab.xcodeproj -target ScratchLab -configuration Release -destination 'generic/platform=iOS' CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO build` passed; `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -configuration Release -destination 'platform=macOS' CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO build` passed; requested `grep -Rni "GCSupportsGameCenterDashboard\|scratchlab_highscores\|GKLeaderboard\|GKGameCenterViewController" ScratchLab ScratchLab.xcodeproj` returns only the false plist key and the Debug-gated `ProgressManager.swift` references; the built iOS Release plist reports `GCSupportsGameCenterDashboard = false`; `./scripts/build.sh` passed end-to-end, including capture-pipeline fixtures, the full `ScratchLabDesktop` XCTest plan, and iOS/macOS/watchOS builds
- Follow-up notes: root cause was that Game Center was not fully configured for release review, but `ScratchLab/Info.plist` still advertised the dashboard and `ProgressManager.recordSessionResult(_:)` could authenticate and submit to the `scratchlab_highscores` leaderboard. Fixed by setting the dashboard key to false, importing and using GameKit only under `#if DEBUG`, adding an explicit `SCRATCHLAB_ENABLE_GAME_CENTER=1` Debug feature flag before authentication/submission/leaderboard UI can run, making Release `activateGameCenterIfNeeded()` leave `isGameCenterEnabled` false with no player ID, and keeping local progress persistence intact through injectable `UserDefaults`. In Release, ScratchLab does not authenticate Game Center, submit scores, show the Game Center leaderboard, or expose the leaderboard ID through reachable code; local XP, mastery, streak, and practice stats still update.

- Selected task: user-requested production/App Review restriction of advanced export modes
- Files changed: `ScratchLab/Models/CaptureCore.swift`, `ScratchLab/Views/CompanionCameraView.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: focused `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testReleaseExportModeUISourceRestrictsAdvancedModesBehindDebugGate -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testDefaultExportMixModeIsScratchOnly` passed; `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -configuration Release -destination 'platform=macOS' CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO build` passed; direct iOS app Release target build passed with `xcodebuild -project ScratchLab.xcodeproj -target ScratchLab -configuration Release -destination 'generic/platform=iOS' CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO build`; the full `ScratchLab` scheme Release build remains blocked by the existing `ScratchLabDesktopTests` `@testable import ScratchLab` Release/testability mismatch; `./scripts/build.sh` passed end-to-end, including capture-pipeline fixtures, the full `ScratchLabDesktop` XCTest plan, and iOS/macOS/watchOS builds
- Follow-up notes: root cause was that the default export option was already `.scratchOnly`, but both production UI surfaces still enumerated `ExportMixMode.allCases`, exposing `Scratch + timing`, `Timing only`, and `Stems folder`/full-mix style output choices to reviewers. Fixed by adding `ExportMixMode.appReviewVisibleModes` with a `#if DEBUG` gate, using that source for the iOS and macOS Debug pickers, and replacing the release pickers with noninteractive Scratch Only rows that reset `exportMixMode` to `.scratchOnly` on appear. Underlying enum cases and export coordinator behavior were not removed.

- Selected task: user-requested rebuild of Coach demo/training audio from Chapter 2 scratch-only sections
- Files changed: `ScratchLab/Resources/CoachDemoAudio/baby_noBeat.wav`, `ScratchLab/Resources/CoachDemoAudio/chirpflare_noBeat.wav`, `TASKS.md`, `DEV_LOG.md`
- Build result: source/training WAVs were located at `/Users/karlwatson/Movies/CXL DATASET/processed_makemkv/baby/79bpm/angle_1_noBeat.wav` and `/Users/karlwatson/Movies/CXL DATASET/processed_makemkv/chirpflare/92bpm/angle_1_noBeat.wav`; the four no-beat angle WAVs for each scratch type had identical SHA-256 hashes, so angle 1 was used. Exported temp files to `build/coach_demo_audio/baby_noBeat.wav` and `build/coach_demo_audio/chirpflare_noBeat.wav`, then copied them into `ScratchLab/Resources/CoachDemoAudio/`. `ffprobe -v quiet -show_format -show_streams` confirmed both final files are 48 kHz stereo PCM 16-bit WAVs with only the basic encoder tag; durations are 48.415042s for `baby_noBeat.wav` and 83.316563s for `chirpflare_noBeat.wav`. `afplay -v 0 -t 1` opened both final files successfully. `grep -RIni "qbert\|makemkv\|sourceRoot\|sourceMKV\|/Users/\|DVD\|DISC" ScratchLab/Resources` returned no text matches; the exact binary-inclusive grep reported only incidental byte matches inside the existing USDZ coach assets. `./scripts/build.sh` passed end-to-end, including capture-pipeline fixtures, the full `ScratchLabDesktop` XCTest plan, and iOS/macOS/watchOS builds.
- Follow-up notes: root cause was that the app's bundled Coach demo WAVs were short generated placeholders rather than the requested Chapter 2 scratch-performance examples. Fixed by selecting the Chapter 2 windows from the matching source chapter boundaries (`baby` 35.035000-83.450033, `chirpflare` 31.531500-114.848067), exporting with `ffmpeg -map_metadata -1 -c:a pcm_s16le`, and replacing only the two existing bundled demo filenames. The selected windows remove the spoken/narrated Chapter 1 material before the performance sections, and `silencedetect` showed the expected repeated scratch/silence timing in both final clips. No app playback code, manifest resource, bundle ID, capture/export/upload/watch/signing, or entitlement logic changed. Commit message to use: `Replace coach demo audio with Chapter 2 scratch-only clips`.

- Selected task: user-requested fix for the remaining App Review P0 resource/path/tutorial risks
- Files changed: `ScratchLab/Resources/CoachInstructions/baby.json`, `ScratchLab/Resources/CoachInstructions/chirpflare.json`, `ScratchLab/Models/CaptureCore.swift`, `ScratchLab/Views/ScratchCoachViews.swift`, `ScratchLab/Views/PracticeModeView.swift`, `ScratchLab/Models/Scratch.swift`, `ScratchLab/Views/LevelSelectView.swift`, `ScratchLab/Audio/AudioEngine.swift`, `ScratchLab/Audio/ScratchAnalyzer.swift`, `ScratchLab/Resources/README.md`, `ScratchLab/Views/CoachPreviewView.swift`, `ScratchLab/Views/CompanionCameraView.swift`, `ScratchLabDesktop/Services/MacScratchDetector.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: `grep -Rni "Tutorial Video\|tutorialVideoName\|showingTutorial" ScratchLab` returned no matches; text-mode `grep -RIni "cxl\|CXL\|champion\|reference_champ\|reference_pro\|sourceReference\|rightsStatus\|reviewStatus\|tutorialVideoName\|Tutorial Video\|showingTutorial" ScratchLab ScratchLabDesktop ScratchLab.xcodeproj` returned no matches; exact binary-inclusive grep reported only `ScratchLab/Resources/Coach/Happy Idle.usdz` and `ScratchLab/Resources/Coach/Coach.usdz` as binary matches; both coach instruction JSON files parsed with `python3 -m json.tool`; `git diff --check` passed; and `./scripts/build.sh` passed end-to-end, including capture-pipeline fixtures, the full `ScratchLabDesktop` XCTest plan, and iOS/macOS/watchOS builds
- Follow-up notes: root cause was that internal coach-resource metadata, an unused release-reachable tutorial placeholder surface, and optional training-reference lookup strings were left behind after the earlier P0 cleanup. Fixed by removing `sourceReference`, `rightsStatus`, and `reviewStatus` from bundled coach instruction JSON plus `ScratchCoachInstruction`, hiding fallback coach difficulty labels through the existing `difficulty == "coach"` sentinel, deleting `showingTutorial`/`TutorialOverlayView` and the unused `tutorialVideoName` scratch model field/assignments, switching the optional baby-training folder lookup to the neutral `scratch_training_library/baby_scratch`, dropping legacy reference-folder fallback lookup, and neutralizing dormant `ScratchAnalyzer`/resource README wording. No bundle IDs, Practice flow entry points, coach instruction content, demo audio filenames/playback, capture/export/upload/watch/signing/entitlement logic, or App Store metadata were changed. Remaining exact grep output is limited to arbitrary binary matches inside existing USDZ assets; the text scan is clean.

- Selected task: user-requested removal of the Coach demo manifest from shipped targets plus unsafe shipped-metadata scrub
- Files changed: `README.md`, `ScratchLab/Resources/CoachDemoAudio/coach_demo_manifest.json`, `scripts/dataset_processor/coach_demo_manifest.dev.json`, `scripts/dataset_processor/build_coach_demo_audio.py`, `scripts/dataset_processor/ingest_media_scratch.py`, `scripts/dataset_processor/README.md`, `scripts/dataset_processor/process_dataset.py`, `scripts/dataset_processor/label_clip.py`, `scripts/test_dataset_processor.py`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, tracked Python bytecode caches, `TASKS.md`, `DEV_LOG.md`
- Build result: `python3 -m py_compile scripts/dataset_processor/build_coach_demo_audio.py scripts/dataset_processor/ingest_media_scratch.py scripts/dataset_processor/process_dataset.py scripts/dataset_processor/label_clip.py scripts/test_dataset_processor.py` passed; `python3 scripts/test_dataset_processor.py` passed; source and built-app `CoachDemoAudio` checks confirmed only `baby_noBeat.wav` and `chirpflare_noBeat.wav`; old manifest path grep in `ScratchLab.xcodeproj` and `ScratchLab/Resources` returned no matches; text-mode compliance grep returned no matches; `./scripts/build.sh` passed end-to-end, including capture-pipeline fixtures, the full `ScratchLabDesktop` XCTest plan, and iOS/macOS/watchOS builds
- Follow-up notes: root cause was that `CoachDemoAudio` is still a folder resource, so every file left in `ScratchLab/Resources/CoachDemoAudio` ships to both iOS and macOS. Fixed by moving the manifest to `scripts/dataset_processor/coach_demo_manifest.dev.json`, leaving only the two runtime WAVs in the bundled folder, updating the demo-audio generator so future manifests default to the development-only path, neutralizing source-media processor names/docs/tests, deleting tracked Python bytecode caches, and adding regressions that fail if JSON/README files or unsafe text metadata return to shipped resources or the project file. The Xcode project did not need a resource-phase edit because the folder reference now points at a folder containing only the required WAVs. No bundle IDs, UI, Practice flow behavior, audio filenames, or bundled WAV lookup code changed. The exact binary grep still reports the existing USDZ coach assets as binary matches due arbitrary byte sequences; a binary-ignored text scan is clean and no textual metadata was found.

## 2026-05-02
- Selected task: user-requested rename of legacy dataset references to CXL Dataset for App Review compliance
- Files changed: `README.md`, `ScratchLab/Audio/AudioEngine.swift`, `ScratchLab/Audio/ScratchAnalyzer.swift`, `ScratchLab/Resources/reference_pro/`, `ScratchLabDesktop/Services/MacScratchDetector.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `scripts/dataset_processor/README.md`, `scripts/dataset_processor/SEGMENTATION_PLAN.md`, `scripts/dataset_processor/ingest_media_scratch.py`, `scripts/test_dataset_processor.py`, tracked Python bytecode caches, `TASKS.md`, `DEV_LOG.md`
- Build result: legacy-name content grep returned no matches; matching filename scan outside Git/build artifacts returned no paths; `python3 -m py_compile scripts/dataset_processor/ingest_media_scratch.py scripts/dataset_processor/process_dataset.py scripts/test_dataset_processor.py` passed; `python3 scripts/test_dataset_processor.py` passed; `git diff --check` passed; `./scripts/build.sh` passed end-to-end, including capture-pipeline fixtures, the full `ScratchLabDesktop` XCTest plan, and iOS/macOS/watchOS builds
- Follow-up notes: root cause was that historical dataset prose, source media ingest defaults, dataset processor tests, analyzer source labels, fallback training-library folder names, and tracked pro-reference WAV filenames still exposed the old dataset name. Fixed with minimal runtime impact by changing visible/internal text to CXL Dataset or neutral pro-reference wording, changing the ingest `sourceCollection` string value to `cxl_dataset` while preserving existing metadata keys, moving the eight tracked pro-reference WAV files to `pro_reference_01.wav` through `pro_reference_08.wav`, and regenerating tracked bytecode so binary scans are clean. No bundle IDs, Coach demo audio filenames, Practice flow, capture/export/upload/watch/signing/entitlement logic, or `Bundle.main.url(...)` coach playback wiring changed.

## 2026-05-02
- Selected task: user-requested remaining App Review P0 cleanup for bundled Coach demo audio replacement and placeholder tutorial access removal
- Files changed: `ScratchLab/Audio/AudioEngine.swift`, `ScratchLab/Views/PracticeModeView.swift`, `ScratchLab/Resources/CoachDemoAudio/baby_noBeat.wav`, `ScratchLab/Resources/CoachDemoAudio/chirpflare_noBeat.wav`, `ScratchLab/Resources/CoachDemoAudio/coach_demo_manifest.json`, `scripts/dataset_processor/build_coach_demo_audio.py`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: replacement WAV verification passed for both files as playable 48 kHz stereo Int16 PCM; `grep -Rni "Tutorial Video" ScratchLab` returned no matches; focused `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testPracticeModeSourceDoesNotExposePlaceholderTutorialEntryPoints -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testCoachDemoAudioFilesExistAndCanBeRead -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testCoachDemoAudioManifestUsesMinimalRuntimeSafeClipMetadata` passed; `./scripts/build.sh` passed end-to-end, including the fixture suite, the full `ScratchLabDesktop` XCTest plan, and the iOS/macOS/watchOS builds
- Follow-up notes: root cause was that the remaining App Review P0 surface still had two legacy bundled Coach demo audio payloads and visible tutorial launch controls that could open a placeholder video overlay. Fixed with minimal runtime impact: `baby_noBeat.wav` and `chirpflare_noBeat.wav` were replaced in place with deterministic generated no-beat scratch demo PCM files while keeping the same filenames, the clean manifest/demo generator timing windows now match those replacement durations, `SessionSetupOverlay` no longer shows `Watch Tutorial First`, `PauseOverlayView` no longer shows `Watch Tutorial`, the placeholder `Tutorial Video` label was removed, and the last tutorial-video feedback string now points users to coach tips. The existing `Bundle.main.url(...)` demo audio loading path was not changed. No bundle IDs, capture/export/upload/watch/signing/entitlement logic, or Practice flow entry points were changed.

- Selected task: user-requested scrub of bundled Coach demo provenance and local path metadata from JSON resources while preserving demo audio playback
- Files changed: `ScratchLab/Resources/CoachDemoAudio/coach_demo_manifest.json`, `ScratchLab/Resources/README.md`, `scripts/dataset_processor/build_coach_demo_audio.py`, `scripts/dataset_processor/README.md`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: all repo JSON files parsed successfully; structured JSON scan for forbidden provenance keys/values passed; bundled resource grep for requested sensitive provenance/path patterns returned no matches; `python3 -m py_compile scripts/dataset_processor/build_coach_demo_audio.py` passed; focused `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testCoachDemoAudioManifestUsesMinimalRuntimeSafeClipMetadata` passed; `./scripts/build.sh` passed end-to-end, including the fixture suite, the full `ScratchLabDesktop` XCTest plan, and the iOS/macOS/watchOS builds
- Follow-up notes: root cause was that `coach_demo_manifest.json` still carried generation provenance and machine-local paths even though runtime playback is driven by the coach instruction JSON's `demoAudioFile` and `ScratchCoachDemoAudioPlayer.bundledDemoAudioURL(...)`. Replaced the manifest with a minimal `clips` list containing only `name`, bundled `file`, `demoStart`, and `demoEnd`; updated the demo-audio generator so future refreshes emit the same safe manifest shape; updated the regression to verify the clean manifest and existing WAV references; and sanitized a stale resource note that caused the bundled-resource grep to match outside JSON. No bundle IDs, UI, audio filenames, capture, export, upload, watch, signing, or entitlement logic changed. Remaining non-bundled historical/tooling references are outside the bundled resource verification scope and were not rewritten in this pass.

## 2026-04-28
- Selected task: user-requested Coach Preview safety audit for App Store wording, runtime crash risk, coach-preview UX clarity, and unintended production-flow changes without broad rewrites
- Files changed: `ScratchLab/Views/CoachPreviewView.swift`, `ScratchLab/Views/MainMenuView.swift`, `ScratchLab/Views/PracticeModeView.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: focused `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testMainMenuSourceExposesDebugCoachPreviewEntryPoint -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testPracticeModeSourceExposesDebugCoachPreviewEntryPoint -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testCoachPreviewSourceLoadsBundledCoachUSDZWithRealityKitDiagnosticsAndARViewFraming -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testMacAnalyzerSourceExposesCoachPanel` passed; direct `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLab -destination 'generic/platform=iOS' build` passed; `./scripts/build.sh` passed end-to-end, including the fixture suite, the full `ScratchLabDesktop` XCTest plan, and the iOS/macOS/watchOS builds
- Follow-up notes: the audit did not find a new 3D loading or build-setting failure. The concrete production-flow leak was that `CoachPreviewView` could switch the shared `AudioEngine` input source for preview-only audio-motion controls and then dismiss without restoring the previous selection, which could carry the wrong input route back into Practice or capture preparation. The user-facing review risk was smaller but real: the temporary preview entry points and status copy still exposed internal wording such as model tests and bundled-asset references. Fixed by keeping the diff narrow and preview-only: `CoachPreviewView` now snapshots and restores the prior audio input source across preview lifetime, uses safer user-facing load/failure copy, `MainMenuView` and `PracticeModeView` now label the temporary entry point as `Coach Preview` with product-safe subtitle text, `MacAnalyzerView` replaces the visible `Test Audio` label with `Audio Input`, and the source-level regressions now pin both the copy contract and the preview input-source restore behavior. No capture, upload, export, watch, signing, bundle ID, entitlement, or App Store metadata logic changed. Remaining manual risk: I did not run a fresh physical-phone visual pass for this specific wording/state-cleanup patch, so the next manual check should open Coach Preview, switch between mic and wired input if available, dismiss, and confirm the parent Practice flow still shows the expected audio route.

- Selected task: user-requested upgrade of the shared baby-scratch `ScratchMotionAnalyzer` so envelope energy is segmented into forward/back stroke timings instead of staying stuck in `Listening`
- Files changed: `ScratchLab/Audio/AudioEngine.swift`, `ScratchLab/Models/ScratchMotionAnalysis.swift`, `ScratchLab.xcodeproj/project.pbxproj`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: focused `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testAudioEngineSourceExposesBabyScratchMotionAnalyzerContract -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testAudioEngineSourcePublishesScratchMotionFeedbackFromInputTap -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testScratchMotionAnalyzerSegmentsBalancedBabyScratchEnvelope -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testScratchMotionAnalyzerMarksUnbalancedWhenStrokeDurationsDrift` passed; `./scripts/build.sh` passed end-to-end, including the fixture suite, the full `ScratchLabDesktop` XCTest plan, and the iOS/macOS/watchOS builds
- Follow-up notes: root cause was that the first lightweight audio-motion pass only exposed current envelope direction and rough amplitude, but it never converted the envelope into discrete stroke segments. In practice that meant forward/back durations were often never populated, timing error stayed `nil`, and the UI could not flip between `Balanced` and `Unbalanced` reliably. Fixed by moving the shared motion types and analyzer into `ScratchLab/Models/ScratchMotionAnalysis.swift` so the logic is reused and directly testable across targets, wiring that file into the iOS and macOS app targets, and upgrading the analyzer to keep a short recent envelope history, detect turnarounds from local peaks/valleys with lightweight follow-through confirmation, measure stroke boundaries from those transitions, and close trailing strokes using the raw per-frame envelope release rather than the longer smoothed decay tail. The analyzer now emits real `forwardDuration`, `backwardDuration`, and `timingError` values plus `[ScratchMotion]` debug logs, and the synthetic regression cases now prove both the balanced and unbalanced baby-scratch paths. No capture, export, upload, watch, signing, entitlement, or App Store metadata logic changed.

- Selected task: user-requested audit of the packaged iOS `UIBackgroundModes` path and physical-phone launch flow
- Files changed: `TASKS.md`, `DEV_LOG.md`
- Build result: direct `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLab -destination 'generic/platform=iOS' build` passed; `plutil -p /Users/karlwatson/Library/Developer/Xcode/DerivedData/Build/Products/Debug-iphoneos/ScratchLab.app/Info.plist` and `defaults read /Users/karlwatson/Library/Developer/Xcode/DerivedData/Build/Products/Debug-iphoneos/ScratchLab.app/Info UIBackgroundModes` both confirmed `audio`; `./scripts/run-on-phone.sh 00008140-000C21A13A9B001C` installed and launched `com.machelpnz.scratchlab` on the paired iPhone; `./scripts/build.sh` passed end-to-end, including the fixture suite, the full `ScratchLabDesktop` XCTest plan, and the iOS/macOS/watchOS builds
- Follow-up notes: root cause was not a missing `UIBackgroundModes` entry in the iOS target or the final packaged app. The `ScratchLab` iOS target uses `INFOPLIST_FILE = ScratchLab/Info.plist` with `GENERATE_INFOPLIST_FILE = NO`, Xcode's `ProcessInfoPlistFile` step writes that plist into the built `ScratchLab.app`, and the final app bundle contains `UIBackgroundModes = ["audio"]`. The paired-iPhone install/launch path also succeeded from the current build, so the previous `UIBackgroundMode` denial was not reproducible from current packaged metadata. No production capture, export, upload, watch, signing, bundle ID, or App Store metadata logic changed.

- Selected task: user-requested Coach Preview / Practice audio-motion milestone to add lightweight baby-scratch direction and balance analysis from live mic or wired input without touching capture/export flows
- Files changed: `ScratchLab/Audio/AudioEngine.swift`, `ScratchLab/Views/CoachPreviewView.swift`, `ScratchLab/Views/PracticeModeView.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: focused `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testAudioEngineSourceExposesBabyScratchMotionAnalyzerContract -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testAudioEngineSourcePublishesScratchMotionFeedbackFromInputTap -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testPracticeModeSourceExposesBabyScratchAudioMotionFeedback -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testCoachPreviewSourceLoadsBundledCoachUSDZWithRealityKitDiagnosticsAndARViewFraming -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testCoachPreviewSourceExposesInteractiveScratchTrainerPrototypeControls -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testPracticeModeSourceExposesDebugCoachPreviewEntryPoint` passed; direct `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLab -destination 'generic/platform=iOS' build` passed; `./scripts/build.sh` passed end-to-end, including the fixture suite, the full `ScratchLabDesktop` XCTest plan, and the iOS/macOS/watchOS builds
- Follow-up notes: root cause was that the app already had a shared live audio tap and scratch classifier, but neither Coach Preview nor the active Practice flow exposed a lightweight push/pull timing heuristic that could tell a baby-scratch learner whether the forward and backward strokes were balanced. Fixed by keeping the change small and shared: `ScratchLab/Audio/AudioEngine.swift` now includes a lightweight envelope-based `ScratchMotionAnalyzer` that infers a simple forward/back cycle from mic or wired input, publishes current motion direction plus balanced/unbalanced timing feedback, `ScratchLab/Views/CoachPreviewView.swift` now starts audio monitoring only when needed and surfaces compact route / direction / forward / back / timing-error feedback with a green or red state, and `ScratchLab/Views/PracticeModeView.swift` now shows the same motion strip inside the active audio card for Baby Scratch sessions only. No ML, video processing, capture pipeline, upload, export, watch sync, signing, entitlement, or App Store metadata logic changed.

- Selected task: user-requested Coach Preview motion-feel milestone to add release inertia, spring return, upgraded baby-scratch timing, and a motion-first UI without touching production capture flows
- Files changed: `ScratchLab/Views/CoachPreviewView.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: focused `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testCoachPreviewSourceLoadsBundledCoachUSDZWithRealityKitDiagnosticsAndARViewFraming -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testCoachPreviewSourceExposesInteractiveScratchTrainerPrototypeControls` passed; direct `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLab -destination 'generic/platform=iOS' build` passed; `./scripts/build.sh` passed end-to-end, including the fixture suite, the full `ScratchLabDesktop` XCTest plan, and the iOS/macOS/watchOS builds
- Follow-up notes: root cause was that manual scratch release still snapped back like a slider, the baby-scratch demo rhythm was still too short and generic to read like a real baby scratch, and the screen still devoted too much visual weight to explanatory copy instead of the platter and motion state. Fixed by isolating all changes to `ScratchLab/Views/CoachPreviewView.swift`: manual release now uses a cancellable physics task with brief inertia and damped spring settling while keeping `scratchValue` as position and `scratchVelocityApprox` as motion, Baby Scratch Demo now runs eased forward/back strokes with center pauses and consistent timing, and the layout was trimmed down to the scene, motion buttons, platter feedback, scratch value/direction, and scratch pad. No capture, upload, export, watch, signing, entitlement, or App Store metadata logic changed.

- Selected task: user-requested Coach Preview readability milestone to make the 3D platter the obvious interaction target without touching production capture flows
- Files changed: `ScratchLab/Views/CoachPreviewView.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: focused `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testCoachPreviewSourceLoadsBundledCoachUSDZWithRealityKitDiagnosticsAndARViewFraming -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testCoachPreviewSourceExposesInteractiveScratchTrainerPrototypeControls` passed; direct `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLab -destination 'generic/platform=iOS' build` passed; `./scripts/build.sh` passed end-to-end, including the capture-pipeline fixture suite, the full `ScratchLabDesktop` XCTest plan, the iOS build, the macOS build, and the watchOS build
- Follow-up notes: root cause was that the temporary 3D platter already rotated correctly, but it still read like background scaffolding because its apparent size, depth, and contrast were too modest relative to the coach. Fixed by keeping the change isolated to `ScratchLab/Views/CoachPreviewView.swift`: the platter constants were increased to a more turntable-like diameter and thickness, placement now derives from coach bounds plus viewport-aware camera math so the disc stays around waist/hand height and clearly in front of the coach, a darker pedestal plus brighter metallic and unlit accent surfaces improve separation against the existing dark background, and the source-level regression was extended to pin the new platter sizing and placement contract. The existing `Coach.usdz` load path, bundled animation loop, Scratch Pad control mapping, 2D platter feedback, and coach-preview-only HUD cleanup remain intact. No capture, upload, export, watch, signing, entitlement, or App Store metadata logic changed.

- Selected task: user-requested Coach Preview milestone to make the `Motion` buttons drive real trainer behavior without touching production capture flows
- Files changed: `ScratchLab/Views/CoachPreviewView.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: focused `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testCoachPreviewSourceLoadsBundledCoachUSDZWithRealityKitDiagnosticsAndARViewFraming -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testCoachPreviewSourceExposesInteractiveScratchTrainerPrototypeControls` passed; direct `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLab -destination 'generic/platform=iOS' build` passed; `./scripts/build.sh` passed end-to-end, including the capture-pipeline fixture suite, the full `ScratchLabDesktop` XCTest plan, the iOS build, the macOS build, and the watchOS build
- Follow-up notes: root cause was that the `Motion` buttons only changed selected styling while the actual trainer state still came entirely from manual Scratch Pad drag input, so tapping `Idle Loop`, `Forward Scratch`, `Back Scratch`, or `Baby Scratch Demo` did not move the trainer or affect coach playback. Fixed by extending only `ScratchLab/Views/CoachPreviewView.swift` with one central `applyMotionMode(_:)` entry point, cancellable `Task`-backed motion demos, neutral reset behavior for `Idle Loop`, transient forward/back pulses that animate to `+1` or `-1` and spring back to zero, a two-cycle baby-scratch demo sequence that ends at neutral, and drag handling that cancels any active demo immediately before returning to manual control. The existing `Coach.usdz` loading path, bundled animation loop, 2D platter feedback, 3D platter entity, and coach-preview-only HUD cleanup remain intact. No capture, upload, export, watch, signing, entitlement, or App Store metadata logic changed.

- Selected task: user-requested Coach Preview milestone to wire Scratch Pad input into a temporary RealityKit platter entity without touching production capture flows
- Files changed: `ScratchLab/Views/CoachPreviewView.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: focused `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testCoachPreviewSourceLoadsBundledCoachUSDZWithRealityKitDiagnosticsAndARViewFraming -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testCoachPreviewSourceExposesInteractiveScratchTrainerPrototypeControls` passed; direct `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLab -destination 'generic/platform=iOS' build` passed; `./scripts/build.sh` passed end-to-end, including the capture-pipeline fixture suite, the full `ScratchLabDesktop` XCTest plan, the iOS build, the macOS build, and the watchOS build
- Follow-up notes: root cause was that the isolated coach trainer already exposed useful Scratch Pad UI state and 2D platter feedback, but the RealityKit scene still had no scratch-responsive object, so there was no in-scene motion to validate the mapping before real USDZ hand or platter targets exist. Fixed by extending only `ScratchLab/Views/CoachPreviewView.swift`: the existing coach loader still resolves bundled `Coach.usdz`, keeps the first animation looping, and keeps the coach-preview-only statistics shutdown path; the `ARView` container now also receives `scratchValue` and `scratchVelocityApprox`, creates a temporary `CoachTrainerPlatter` from RealityKit primitives, places it below and in front of the framed coach using the model's visual bounds, rotates it around the Y axis from bounded scratch value plus velocity, and logs `[CoachTrainer3D] platterRotation=` plus `[CoachTrainer3D] scratchValue=`. The existing 2D platter indicator remains in place for comparison, and no capture, upload, export, watch, signing, entitlement, or App Store metadata logic changed.

- Selected task: user-requested project-wide audit and elimination of all remaining RealityKit/ARView statistics overlay sources affecting Coach Preview
- Files changed: `ScratchLab/Views/CoachPreviewView.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: project-wide source audit found no runtime `showStatistics = true` usage in `ScratchLab`, `ScratchLabDesktop`, or `ScratchLabWatch`; focused `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testCoachPreviewSourceLoadsBundledCoachUSDZWithRealityKitDiagnosticsAndARViewFraming -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testCoachPreviewSourceExposesInteractiveScratchTrainerPrototypeControls` passed; direct `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLab -destination 'generic/platform=iOS' build` passed; `./scripts/build.sh` passed end-to-end, including the capture-pipeline fixture suite, the full `ScratchLabDesktop` XCTest plan, the iOS build, the macOS build, and the watchOS build
- Follow-up notes: root cause was not another repo-local screen enabling `showStatistics = true`; the project-wide search found no runtime source doing that. The remaining risk was that the isolated coach preview still relied on `debugOptions` cleanup alone, while this SDK does not expose a typed `ARView.showStatistics` property even though the underlying view can still render the overlay. Fixed by keeping the change isolated to `ScratchLab/Views/CoachPreviewView.swift`, making `makeUIView` and `updateUIView` both call a stronger shutdown path, explicitly clearing `debugOptions`, removing `.showStatistics`, zeroing `arView.__statisticsOptions`, setting `arView.__disableStatisticsRendering = true`, and using a selector-backed `setShowStatistics:` override when that setter exists at runtime. Added a defensive `DispatchQueue.main.async` reapply immediately after ARView creation so late view initialization cannot re-enable the overlay. Verified there is still no `RealityView` path, no `ARView.appearance` override, and no other runtime `showStatistics = true` source in the app targets. Coach loading, animation, and the isolated trainer UI remain intact; no capture, upload, export, watch, signing, entitlement, or App Store metadata logic changed.

## 2026-04-28
- Selected task: user-requested isolated Coach Preview milestone to turn the bundled coach model test into an interactive scratch-training prototype without touching production capture flows
- Files changed: `ScratchLab/Views/CoachPreviewView.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: focused `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testCoachPreviewSourceLoadsBundledCoachUSDZWithRealityKitDiagnosticsAndARViewFraming -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testCoachPreviewSourceExposesInteractiveScratchTrainerPrototypeControls` passed; direct `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLab -destination 'generic/platform=iOS' build` passed; `./scripts/build.sh` passed end-to-end, including the capture-pipeline fixture suite, the full `ScratchLabDesktop` XCTest plan, the iOS build, the macOS build, and the watchOS build
- Follow-up notes: root cause was that the current Coach Preview had already become a reliable bundled-model viewer, but it still stopped at passive playback and did not provide any way to prototype scratch-motion input, value mapping, or visible trainer feedback before real skeletal rigging exists. Fixed by keeping the current `Coach.usdz` loading path and HUD cleanup intact, returning the default `AnimationPlaybackController` from the existing loader, and extending only `ScratchLab/Views/CoachPreviewView.swift` with an additive trainer surface: motion-mode selection (`Idle Loop`, `Forward Scratch`, `Back Scratch`, `Baby Scratch Demo`), a horizontal `Scratch Pad` that maps drag to `-1.00 ... +1.00`, visible platter feedback under the 3D preview, clean `[CoachTrainer]` diagnostics for scratch value, direction, and approximate velocity, and a small animation bridge that pauses the default loop while the scratch pad is active and resumes/slows it according to the selected motion mode otherwise. No capture, upload, export, watch, signing, entitlement, App Store metadata, or production practice timing logic changed.

## 2026-04-28
- Selected task: user-requested isolated CoachPreviewView HUD cleanup so the bundled coach preview no longer shows RealityKit statistics/performance overlay noise
- Files changed: `ScratchLab/Views/CoachPreviewView.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: focused `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testCoachPreviewSourceLoadsBundledCoachUSDZWithRealityKitDiagnosticsAndARViewFraming` passed; direct `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLab -destination 'generic/platform=iOS' build` passed; `./scripts/build.sh` passed end-to-end, including the capture-pipeline fixture suite, the full `ScratchLabDesktop` XCTest plan, the iOS build, the macOS build, and the watchOS build
- Follow-up notes: root cause was that `CoachPreviewView` only set `arView.debugOptions = []` during initial view creation, so the isolated preview did not actively clear the RealityKit statistics flag on later updates and still behaved like a debug surface when the overlay was enabled elsewhere in the view lifecycle. Fixed by keeping the change isolated to `ScratchLab/Views/CoachPreviewView.swift`, centralizing preview-surface configuration in one helper, reapplying that configuration in both `makeUIView` and `updateUIView`, and explicitly clearing `ARView.DebugOptions.showStatistics` in addition to resetting `debugOptions` to an empty option set. This SDK does not expose a typed `arView.showStatistics` property on `RealityKit.ARView`, so the compile-safe equivalent is `arView.debugOptions.remove(.showStatistics)`. Audited the rest of `CoachPreviewView.swift` and confirmed there is no `RealityView` path, no `.debug` or `.statistics` modifiers, no `showStatistics = true`, and no `ARView.appearance` or repo-local HUD flag affecting this preview. No capture, practice, upload, watch, signing, entitlement, or App Store metadata logic changed.

## 2026-04-28
- Selected task: user-requested audit and polish of the isolated RealityKit coach preview screen so the bundled coach model frames correctly, hides debug HUD noise, and reports actionable load diagnostics
- Files changed: `ScratchLab/Views/CoachPreviewView.swift`, `ScratchLab/Views/MainMenuView.swift`, `ScratchLab/Views/PracticeModeView.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: focused `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testMainMenuSourceExposesDebugCoachPreviewEntryPoint -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testPracticeModeSourceExposesDebugCoachPreviewEntryPoint -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testCoachPreviewSourceLoadsBundledCoachUSDZWithRealityKitDiagnosticsAndARViewFraming -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testProjectBundlesCoachUSDZForPreviewTargets -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testHostedDesktopBundleContainsCoachUSDZAsset` passed; direct `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLab -destination 'generic/platform=iOS' build` passed; `./scripts/build.sh` passed end-to-end, including the capture-pipeline fixture suite, the full `ScratchLabDesktop` XCTest plan, the iOS build, the macOS build, and the watchOS build
- Follow-up notes: root cause was that the additive coach preview still mixed a developer-facing presentation with shallow diagnostics and fixed placement, so `Coach.usdz` could load while still appearing too small, off-center, or visually noisy on iPhone because the framing did not adapt to the asset's real visual bounds. Fixed by keeping the bundled `Coach.usdz` RealityKit path, moving the preview to a non-AR `ARView` with `debugOptions = []`, loading the bundle root entity first while still probing `Entity(named: "Coach")` for diagnostics, logging the resolved bundle URL plus file existence, root/child names, animation count, visual bounds, scale normalization, and final transform, and deriving final framing from the model bounds and preview viewport so the coach stays centered, larger, and fully visible without arbitrary manual offsets. Updated the visible copy to `Coach Preview`, `Coach model test`, and `Preview bundled coach model and animation`, added a compact status line with load/failure state, animation count, current scale, and approximate model height, and kept all changes isolated to the preview/testing entry points only. No capture, timing, audio slicing, export, upload, watch, signing, entitlement, or App Store metadata logic changed.

## 2026-04-28
- Selected task: user-requested additive RealityKit coach model test using the bundled `Coach.usdz` asset from the Practice/Baby Scratch flow
- Files changed: `ScratchLab/Views/CoachPreviewView.swift`, `ScratchLab/Views/PracticeModeView.swift`, `ScratchLab/Views/MainMenuView.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `ScratchLab.xcodeproj/project.pbxproj`, `TASKS.md`, `DEV_LOG.md`
- Build result: focused `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testMainMenuSourceExposesDebugCoachPreviewEntryPoint -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testPracticeModeSourceExposesDebugCoachPreviewEntryPoint -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testCoachPreviewSourceLoadsBundledCoachUSDZWithRealityKitAndFallbackARView -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testProjectBundlesCoachUSDZForPreviewTargets -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testHostedDesktopBundleContainsCoachUSDZAsset` passed; direct `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLab -destination 'generic/platform=iOS' build` passed; `./scripts/build.sh` passed end-to-end, including the capture-pipeline fixture suite, the full `ScratchLabDesktop` XCTest plan, the iOS build, and the watchOS build
- Follow-up notes: root cause was that the new working coach asset already existed at `ScratchLab/Resources/Coach/Coach.usdz`, but the preview path was still tied to the older `ScratchLabCoach...` pipeline and there was no temporary debug launcher from the Practice/Baby Scratch setup flow. Fixed by replacing `ScratchLab/Views/CoachPreviewView.swift` with a minimal iOS RealityKit loader that resolves `Coach.usdz` from `Bundle.main`, loads `Entity(named: "Coach")` asynchronously, logs the bundle URL plus load success/entity name/animation count, places the entity at `[0, 0, -1.2]` with scale `1`, and loops the first available animation when present. Added an iOS 18 `RealityView` path with a non-AR `ARView` fallback for older targets, wired a debug-only `Open Coach Model Test` entry into the Practice/Baby Scratch setup overlay, kept the existing debug-only main-menu preview entry aligned to the same loader, updated `ScratchLab.xcodeproj/project.pbxproj` so `Coach.usdz` is copied into the app resources, and added source-level regressions plus a hosted bundle assertion that the bundled coach asset exists. No capture, upload, watch, signing, entitlement, or production practice flow logic changed.

## 2026-04-27
- Selected task: fix the Coach 3D Preview asset loading failure so the bundled coach character loads reliably with a self-contained asset first on iOS
- Files changed: `ScratchLab/Views/CoachPreviewView.swift`, `ScratchLab/Views/MainMenuView.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `ScratchLab.xcodeproj/project.pbxproj`, `ScratchLab/Image_0.jpg`, `ScratchLab/Image_2.jpg`, `ScratchLab/Image_3.jpg`, `ScratchLab/color_0C0C0C.exr`, `TASKS.md`, `DEV_LOG.md`
- Build result: direct `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLab -destination 'generic/platform=iOS' build` passed; `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLab -destination 'platform=iOS Simulator,name=iPhone 17' build` passed; `./scripts/build.sh` passed end-to-end, including the capture-pipeline fixture suite, the full `ScratchLabDesktop` XCTest plan, the iOS build, and the watchOS build; simulator install/launch verification passed via `xcrun simctl install 53B855D2-2933-4A9C-BB75-1AC5D866701E .../Debug-iphonesimulator/ScratchLab.app` and `xcrun simctl launch 53B855D2-2933-4A9C-BB75-1AC5D866701E com.machelpnz.scratchlab`; physical-phone install verification was attempted with `./scripts/run-on-phone.sh 00008140-000C21A13A9B001C` but blocked because device `K` was locked
- Follow-up notes: root cause was that `ScratchLab/Views/CoachPreviewView.swift` hard-coded a direct `Bundle.main.url(forResource: "ScratchLabCoach", withExtension: "fbx")` SceneKit load even though `ScratchLabCoach.fbx` is not self-contained and references external texture files (`Image_0.jpg`, `Image_2.jpg`, `Image_3.jpg`) that were not bundled beside it. SceneKit therefore failed with `The document "ScratchLabCoach.fbx" could not be opened.` even though the repo already contains a valid self-contained `ScratchLabCoach.usdz` asset. Fixed by changing the debug-only loader to prefer `ScratchLabCoach.usdz` first, then `ScratchLabCoach.scn`, and only fall back to `ScratchLabCoach.fbx`; logging every resolved bundle URL plus file-existence status; reporting SceneKit load failures with domain/code/URL details; keeping the debug gate intact; and adding a debug assertion plus source-level regression that a preferred bundled coach asset exists in `Bundle.main`. Updated `ScratchLab.xcodeproj/project.pbxproj` so the iOS app target and the macOS preview target both copy `ScratchLabCoach.usdz`, `ScratchLabCoach.fbx`, `Image_0.jpg`, `Image_2.jpg`, `Image_3.jpg`, and `color_0C0C0C.exr` in Copy Bundle Resources. The preview now loads `.usdz` first because it is self-contained and passes `usdchecker`, while the FBX path remains available only as a lower-priority fallback.

## 2026-04-27
- Selected task: user-requested iOS launch failure follow-up for `UIBackgroundMode` launch denial on physical iPhone
- Files changed: `ScratchLab.xcodeproj/xcshareddata/xcschemes/ScratchLab.xcscheme`, `DEV_LOG.md`
- Build result: focused `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testScratchLabIOSSchemeUsesForegroundLaunchWithoutLocationSimulation` passed; direct `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLab -destination 'generic/platform=iOS' build` passed; `./scripts/build.sh` passed end-to-end, including capture-pipeline fixture tests, the full `ScratchLabDesktop` XCTest plan, and the iOS, macOS, and watchOS builds; `./scripts/run-on-phone.sh 00008140-000C21A13A9B001C` installed and launched `com.machelpnz.scratchlab` on device `K`
- Follow-up notes: root cause was not a missing or overly broad iOS background entitlement. `ScratchLab/Info.plist` already correctly limits `UIBackgroundModes` to `audio`, and the iOS target build settings still point directly at that plist (`INFOPLIST_FILE = ScratchLab/Info.plist`, `GENERATE_INFOPLIST_FILE = NO`). The launch denial came from the shared `ScratchLab` iOS scheme still carrying a stale `LocationScenarioReference`, which made Debug launch request unsupported background/location behavior on a physical iPhone. Fixed by removing that location scenario from `ScratchLab.xcodeproj/xcshareddata/xcschemes/ScratchLab.xcscheme`. Audited runtime capability usage as part of the fix: the iOS app legitimately uses audio/microphone capture and configures `AVAudioSession` for `playAndRecord`, so the required background mode remains `audio`; it also uses camera capture, WatchConnectivity file transfer, and a background `URLSession` upload path, but there is no Core Location, CoreBluetooth, or ExternalAccessory runtime that would justify adding `location`, `bluetooth-central`, `bluetooth-peripheral`, `external-accessory`, `fetch`, or `processing`.

## 2026-04-27
- Selected task: fix the shared iOS launch scheme so foreground Debug runs do not request undeclared background/location launch behavior
- Files changed: `ScratchLab.xcodeproj/xcshareddata/xcschemes/ScratchLab.xcscheme`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: focused `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testScratchLabIOSSchemeUsesForegroundLaunchWithoutLocationSimulation` passed; direct `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLab -destination generic/platform=iOS build` passed; `./scripts/build.sh` passed end-to-end, including the capture-pipeline fixture suite, the full `ScratchLabDesktop` XCTest plan, and the iOS, macOS, and watchOS builds
- Follow-up notes: root cause was that the shared iOS scheme still enabled location simulation and a concrete launch scenario even though the app does not use Core Location and `ScratchLab/Info.plist` only declares `UIBackgroundModes = ["audio"]`. That made the debug launch configuration request behavior that could be interpreted as needing undeclared background/location support, causing the `SBMainWorkspace` launch denial instead of a normal foreground run. Fixed by removing `allowLocationSimulation = "YES"` and the `LocationScenarioReference` block from `ScratchLab.xcodeproj/xcshareddata/xcschemes/ScratchLab.xcscheme`, leaving the launch style in normal foreground Debug mode, and adding a source-level regression so the shared scheme stays free of location simulation and wait-for-executable launch settings. `ScratchLab/Info.plist` was audited and intentionally left unchanged because no real app functionality in this fix justified adding `location`, `fetch`, `processing`, or other broader background modes.

## 2026-04-27
- Selected task: replace the debug coach preview path with SceneKit loading of the bundled `ScratchLabCoach.fbx` asset
- Files changed: `ScratchLab/Views/CoachPreviewView.swift`, `ScratchLab/Views/MainMenuView.swift`, `ScratchLab.xcodeproj/project.pbxproj`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: focused `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testMainMenuSourceExposesDebugCoachPreviewEntryPoint -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testCoachPreviewSourceLoadsBundledScratchLabCoachFBXWithSceneKit -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testProjectBundlesScratchLabCoachFBXAndBuildsCoachPreviewViewForIOSTarget` passed; `./scripts/build.sh` passed end-to-end, including the capture-pipeline fixture suite, the full `ScratchLabDesktop` XCTest plan, and the iOS, macOS, and watchOS builds
- Follow-up notes: root cause was that the isolated 3D coach preview path still targeted RealityKit/USDZ even after `ScratchLabCoach.fbx` had become the real bundled asset, so the preview implementation, the debug menu copy, the project resources, and the source-level regressions were all enforcing the wrong asset pipeline. Fixed by replacing the dedicated preview loader in `ScratchLab/Views/CoachPreviewView.swift` with a SceneKit `SCNView` wrapper that resolves `ScratchLabCoach.fbx` from `Bundle.main`, applies a conservative root scale, ensures a fallback preview camera, and loops any available FBX animation through `animationPlayer(forKey:)`; updating the existing debug-only `Coach 3D Preview` sheet in `MainMenuView` to advertise the FBX path; and removing the old coach USDZ references from `ScratchLab.xcodeproj/project.pbxproj` so the project no longer points at the deprecated asset route. The preview remains isolated and reversible, and no practice, capture, session, or export behavior changed.

## 2026-04-27
- Selected task: move the iOS RealityKit coach preview into a dedicated bundled `CoachPreviewView.swift` loader and wire the real `ScratchLabCoach.usdz` resource
- Files changed: `ScratchLab/Views/CoachPreviewView.swift`, `ScratchLab/Views/MainMenuView.swift`, `ScratchLab.xcodeproj/project.pbxproj`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: focused `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testMainMenuSourceExposesDebugCoachPreviewEntryPoint -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testCoachPreviewSourceLoadsBundledScratchLabCoachUSDZWithoutTemplateBundleAssumptions -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testProjectBundlesScratchLabCoachUSDZAndBuildsCoachPreviewViewForIOSTarget` passed; `./scripts/build.sh` passed end-to-end, including the capture-pipeline fixture suite, the full `ScratchLabDesktop` XCTest plan, and the iOS, macOS, and watchOS builds
- Follow-up notes: root cause was that the first template-free RealityKit preview pass left the loader code inline in `MainMenuView` and still relied on an older `ScratchLabCoach 2.usdz` project resource name even though the actual bundled asset is now `ScratchLabCoach.usdz`. Fixed by extracting the loader into a new iOS-only `ScratchLab/Views/CoachPreviewView.swift` file, keeping the existing debug-only `Coach 3D Preview` sheet in `MainMenuView` as the exposure point, loading `ScratchLabCoach.usdz` from `Bundle.main` through a non-AR `ARView`, applying a conservative `0.01` scale, looping the first available animation when present, and updating the Xcode project so `ScratchLabCoach.usdz` is the active bundled resource name and `CoachPreviewView.swift` builds with the iOS target. The app architecture, practice flow, and capture/session logic remain unchanged.

## 2026-04-27
- Selected task: add a debug-only bundled RealityKit coach preview entry to the existing ScratchLab iOS app
- Files changed: `ScratchLab/Views/MainMenuView.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: direct `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLab -destination generic/platform=iOS build` passed; focused `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test` for the new Main Menu preview source regressions passed; `./scripts/build.sh` passed end-to-end, including the capture-pipeline fixture suite, the full `ScratchLabDesktop` XCTest plan, and the iOS, macOS, and watchOS builds
- Follow-up notes: root cause was that the requested 3D coach integration assumed a RealityKit template app with `RealityKitContent`, `.rkassets`, and `realityKitContentBundle`, but this repo is the existing ScratchLab app and currently contains none of those pieces. Fixed by adding a debug-only `Coach 3D Preview` menu entry in `MainMenuView` that opens a sheet-backed `ScratchCoachPreviewView`, then loading the first matching bundled asset from `Bundle.main` with the fallback order `ScratchLabCoach.usdz`, `ScratchLabCoach.usdc`, `Untitled.usdz`, `Untitled.usdc`. The preview uses a non-AR `ARView`, loads the entity with `Entity.load(contentsOf:)`, applies a conservative scale, plays the first available animation on loop, and fails soft with an explicit operator-facing message when no asset is bundled or `.usdc` export is not reliable. There is still no bundled 3D asset in the repo today, so the current preview path intentionally exposes a controlled fallback until a resource such as `ScratchLabCoach.usdz` is added to the ScratchLab iOS target.

## 2026-04-27
- Selected task: fix iPhone safe-area overlap across Main Menu, Live Practice picker, and Practice Coach setup
- Files changed: `ScratchLab/Views/MainMenuView.swift`, `ScratchLab/Views/LevelSelectView.swift`, `ScratchLab/Views/PracticeModeView.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: focused `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test` for the new Main Menu / LevelSelect / Practice safe-area regressions passed; `./scripts/build.sh` passed end-to-end, including the capture-pipeline fixture suite, the full `ScratchLabDesktop` XCTest plan, and the iOS, macOS, and watchOS builds; `./scripts/run-on-phone.sh 1F80398A-96C8-537A-B0EE-821E186918B9` rebuilt, installed, and launched ScratchLab on `K`
- Follow-up notes: root cause was that the current iPhone-facing practice surfaces still mixed custom top chrome with hardcoded top padding, especially in `MainMenuView`, `LevelSelectView`, and the Practice setup overlay. That left the title/header stack and custom back/menu controls relying on spacing guesses instead of the actual device inset, which is why the Dynamic Island/status area could overlap on smaller phones. Fixed by moving the menu and scratch-picker screens onto `GeometryReader`-driven scroll layouts with safe-area-based top and bottom padding, and by tightening the existing `PracticeModeView` / `SessionSetupOverlay` top inset math so the custom top bar and Coach setup content anchor directly to `geometry.safeAreaInsets.top`. The existing Practice flow order, Coach placement, and no-session/no-take behavior remain unchanged.

## 2026-04-27
- Selected task: rework the shared ScratchLab Coach visual direction from emoji mascot to a rig-first DJ training diagram
- Files changed: `ScratchLab/Models/CaptureCore.swift`, `ScratchLab/Views/ScratchCoachViews.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: `python3 scripts/test_dataset_processor.py` passed with 25 tests; focused `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test` for the Coach decode, animator, shared-view, and no-session regressions passed; `./scripts/build.sh` passed end-to-end, including the capture-pipeline fixture suite, the full `ScratchLabDesktop` XCTest plan, and the iOS, macOS, and watchOS builds
- Follow-up notes: root cause was that the recent shared Coach redesign still presented ScratchLab coaching through an emoji-style mascot and a separate controller block, which did not fit the intended capture-derived DJ training direction. Fixed by replacing that primary surface with `ScratchCoachRigView`, a shared stylized booth/turntable/mixer/body rig driven by `ScratchCoachDemoAnimator`, shrinking the speech bubble so the rig becomes the main teaching surface, preserving the scratch-specific Baby open-fader and Chirp Flare quick-click guidance, and adding optional future motion-reference fields to `ScratchCoachInstruction` so later video-derived keyframes can be attached without changing the current bundled JSON. Existing Coach lookup, missing-audio fallback, collapsed `Steps & Tips`, and no-session/no-take behavior remain intact.

## 2026-04-27
- Selected task: refine the shared ScratchLab Coach UI for small iPhone layout and clearer Baby/Chirp Flare demo guidance
- Files changed: `ScratchLab/Views/ScratchCoachViews.swift`, `ScratchLab/Views/PracticeModeView.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: `python3 scripts/test_dataset_processor.py` passed with 25 tests; focused `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test` for the Coach/practice safe-area and fader-copy regressions passed; `./scripts/build.sh` passed end-to-end, including the capture-pipeline fixture suite, the full `ScratchLabDesktop` XCTest plan, and the iOS, macOS, and watchOS builds; `./scripts/run-on-phone.sh 1F80398A-96C8-537A-B0EE-821E186918B9` rebuilt, installed, and launched ScratchLab on `K`
- Follow-up notes: root cause was that the new shared Coach card worked functionally but still behaved like a tall text block on the smallest iPhones. The Practice setup overlay and top chrome were still using fixed padding instead of real safe-area insets, the mascot was too simple, and the shared crossfader cue copy was still too generic for the Baby/Chirp Flare difference. Fixed by making the Practice top bar and setup overlay safe-area-aware, tightening the overlay spacing, shrinking the visible Coach card footprint, switching the speech bubble to the shorter instruction summary by default, upgrading the shared ScratchLab mascot/controller visuals, and making the shared cue copy explicit: Baby keeps the fader open and Chirp Flare uses a quick click. The existing Coach flow remains additive below the setup controls, `Steps & Tips` stays collapsed by default, and opening Coach still creates no sessions, takes, or artifacts.

## 2026-04-27
- Selected task: replace the text-heavy ScratchLab Coach card with a first-pass animated shared helper on iOS and macOS
- Files changed: `ScratchLab/Views/ScratchCoachViews.swift`, `ScratchLab/Views/PracticeModeView.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `ScratchLab.xcodeproj/project.pbxproj`, `TASKS.md`, `DEV_LOG.md`
- Build result: `python3 scripts/test_dataset_processor.py` passed with 25 tests; focused `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test` for the coach/practice shared-view regressions passed; `./scripts/build.sh` passed end-to-end, including the capture-pipeline fixture suite, the full `ScratchLabDesktop` XCTest plan, and the iOS, macOS, and watchOS builds
- Follow-up notes: root cause was that ScratchLab Coach already shared its instruction store, demo audio player, and animation model, but the actual UI was still rendered as two separate text-heavy layouts on iPhone and Mac. Fixed by adding shared `ScratchCoachViews.swift` with an original ScratchLab character view, speech bubble, controller demo surface driven by `ScratchCoachDemoAnimator`, and a reusable `ScratchCoachCardContent` container with collapsed `Steps & Tips`. Refactored the existing `ScratchCoachCard` and macOS coach panel to keep only platform-local theme/control styling plus the existing playback lifecycle, so Baby and Chirp Flare still resolve through the existing shared store, Listen/Pause/Replay still work, missing audio still fails soft, and opening the Coach still creates no sessions, takes, or artifacts. Manual device/window-size verification is still the remaining check from this workspace.

## 2026-04-27
- Selected task: restore Live Practice scratch selection and replace Coach demo WAVs with scratch-only performance trims
- Files changed: `ScratchLab/Views/MainMenuView.swift`, `ScratchLab/Views/LevelSelectView.swift`, `ScratchLab/Views/PracticeModeView.swift`, `ScratchLab/Resources/CoachDemoAudio/baby_noBeat.wav`, `ScratchLab/Resources/CoachDemoAudio/chirpflare_noBeat.wav`, `ScratchLab/Resources/CoachDemoAudio/coach_demo_manifest.json`, `scripts/dataset_processor/build_coach_demo_audio.py`, `scripts/dataset_processor/README.md`, `README.md`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: `python3 -m py_compile scripts/dataset_processor/build_coach_demo_audio.py scripts/test_dataset_processor.py` passed; `python3 scripts/test_dataset_processor.py` passed with 25 tests; focused `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test` for the new practice-selection and coach manifest regressions passed; `./scripts/build.sh` passed end-to-end, including the capture-pipeline fixture suite, the full `ScratchLabDesktop` XCTest plan, and the iOS, macOS, and watchOS builds; `./scripts/run-on-phone.sh 1F80398A-96C8-537A-B0EE-821E186918B9` rebuilt, installed, and launched ScratchLab on `K`
- Follow-up notes: root cause was twofold. Live Practice still entered through a Baby-only hub and `PracticeModeView` itself collapsed every non-baby scratch back to `baby_scratch`, so there was no real scratch-selection path even before the Coach lookup ran. Separately, the bundled `baby_noBeat.wav` and `chirpflare_noBeat.wav` assets had been copied from full source media no-beat takes, which meant the app could ship long clips that still included pre-performance instruction sections. Fixed by restoring a lightweight Baby/Chirp Flare selection flow in `LevelSelectView`, removing the Baby fallback from `PracticeModeView`, keeping the existing setup controls first with the Coach card still additive below them, updating the iPhone Live Practice copy to match the selector, and adding `scripts/dataset_processor/build_coach_demo_audio.py` plus `coach_demo_manifest.json` so the shipped coach WAVs are rebuilt from the `noBeat` audio stream starting at Chapter 2 plus a 2.0-second offset. Remaining manual verification is on-device only: confirm Practice now opens to scratch options, Baby and Chirp Flare both show the Coach card, and the Listen control plays scratch-only audio with no spoken intro.

## 2026-04-27
- Selected task: fix ScratchLab Coach scratch-type normalization so ChirpFlare resolves on iPhone
- Files changed: `ScratchLab/Models/CaptureCore.swift`, `ScratchLab/Views/PracticeModeView.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: focused `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testScratchCoachInstructionStoreLoadsInstructionByScratchTypeAlias -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testScratchCoachInstructionStoreLoadsChirpFlareInstructionFromHumanReadableAlias -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testScratchCoachInstructionStoreLoadsChirpFlareInstructionFromUnderscoredAlias -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testScratchCoachInstructionStoreFallsBackToDisplayNameAliasWhenScratchIDHasNoCoachFile -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testPracticeFlowDoesNotCreateCaptureSessions -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testMacAnalyzerSourceExposesCoachPanel` passed; `./scripts/build.sh` passed end-to-end on this machine, including the capture-pipeline fixture suite, the full `ScratchLabDesktop` XCTest plan, the iOS app build, the macOS app build, and the watchOS build; `./scripts/run-on-phone.sh 1F80398A-96C8-537A-B0EE-821E186918B9` rebuilt, installed, and launched ScratchLab on `K`
- Follow-up notes: root cause was that ScratchLab Coach lookup still depended on inconsistent raw scratch-type strings between the selected scratch UI and the bundled `chirpflare.json` resource name. Fixed by adding one shared `normalizeScratchType(input:)` helper, using it in the shared coach store plus demo animator, teaching `ScratchCoachInstructionStore` to search normalized candidates from both the selected scratch ID and the visible scratch name, and normalizing the iOS/macOS coach entry points before they call the shared store. Added explicit regressions for `baby_scratch`, `Chirp Flare`, `chirp_flare`, and the fallback path where the scratch ID has no coach file but the visible display name still matches `Chirp Flare`. Manual on-device UI confirmation for Baby and ChirpFlare remains the only unverified step from this workspace.

- Selected task: debug missing ScratchLab Coach card visibility on the real iPhone Practice flow
- Files changed: `ScratchLab/Views/PracticeModeView.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: focused `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testScratchCoachInstructionStoreLoadsInstructionByScratchTypeAlias -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testScratchCoachInstructionStoreReturnsNeutralStateWhenScratchTypeIsMissing -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testPracticeModeSourceExposesCoachCardInScrollableSetupOverlay -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testPracticeModeSourceExposesVisibleBeatControlsInSetupOverlay` passed; `./scripts/build.sh` passed end-to-end on this machine, including the capture-pipeline fixture suite, the full `ScratchLabDesktop` XCTest plan, the iOS app build, the macOS app build, and the watchOS build; `./scripts/run-on-phone.sh 1F80398A-96C8-537A-B0EE-821E186918B9` rebuilt, installed, and launched ScratchLab on `K`
- Follow-up notes: audited the full iPhone Practice path and confirmed this was not a missing-resource problem. The built iOS app already contained `CoachInstructions/baby.json`, `CoachInstructions/chirpflare.json`, and both demo WAVs. The real issues were that the coach lookup used the original `scratch` input instead of the visible `activeScratch`, and the coach card sat below the later audio-routing section, making it too easy to miss in the phone setup flow. Fixed by resolving the coach instruction from `activeScratch`, moving the existing coach card earlier in the scrollable setup overlay while keeping the core Baby Scratch setup controls first, enabling visible scroll indicators, keeping bottom spacing clear of the safe area, and adding a neutral-store regression for missing scratch selection. No session/take creation, network calls, AI, or noisy release logging were added.

- Selected task: keep the ScratchLab Coach card inside the existing Practice setup overlay after the Baby Scratch setup controls
- Files changed: `ScratchLab/Views/PracticeModeView.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: focused `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testPracticeModeSourceExposesCoachCardInScrollableSetupOverlay` passed; `./scripts/build.sh` passed end-to-end on this machine, including the capture-pipeline fixture suite, the full `ScratchLabDesktop` XCTest plan, the iOS app build, the macOS app build, and the watchOS build
- Follow-up notes: root cause was that the iOS Practice flow already had the new Coach card in the setup overlay, but it appeared ahead of the existing Baby Scratch drill setup controls, which made the overlay feel like a reordered workflow instead of an extension of the current setup path. Fixed by moving the Coach card lower inside `SessionSetupOverlay` so the flow remains scratch selection → existing setup controls → coach guidance within the same scrollable overlay, and by tightening the source-level regression to assert that the Coach card stays after the beat and audio-input setup sections. No session/take creation, Coach model changes, or navigation redesign were added.

- Selected task: extend ScratchLab Coach Demo Mode with deterministic synced scratch animation
- Files changed: `ScratchLab/Models/CaptureCore.swift`, `ScratchLab/Views/PracticeModeView.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: `python3 scripts/test_dataset_processor.py` passed; `./scripts/build.sh` passed end-to-end on this machine, including the capture-pipeline fixture suite, the full `ScratchLabDesktop` XCTest plan, the iOS app build, the macOS app build, and the watchOS build
- Follow-up notes: root cause was that the current ScratchLab Coach Demo Mode already had local demo playback and a simple coach visual, but that visual was just a generic pulse and did not reflect scratch-specific record or crossfader motion. Fixed by adding shared `ScratchCoachDemoAnimator` logic in capture core, exposing read-only demo playback time from the existing shared audio player, and wiring the existing iOS and macOS coach visuals to render deterministic `baby` record movement with an always-open fader and `chirpflare` record movement with short open/close fader pulses only while audio is actively playing. Added focused regressions for animator reset-on-stop plus the `baby` and `chirpflare` motion contracts. No session/take/artifact creation, network behavior, AI, model training, or real DJ likeness/persona content was added.

- Selected task: add bundled ScratchLab Coach demo audio assets for baby and chirpflare only
- Files changed: `ScratchLab/Models/CaptureCore.swift`, `ScratchLab/Resources/CoachInstructions/baby.json`, `ScratchLab/Resources/CoachInstructions/chirpflare.json`, `ScratchLab/Resources/CoachDemoAudio/baby_noBeat.wav`, `ScratchLab/Resources/CoachDemoAudio/chirpflare_noBeat.wav`, `ScratchLab.xcodeproj/project.pbxproj`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: `python3 -m py_compile scripts/dataset_processor/process_dataset.py scripts/dataset_processor/label_clip.py scripts/test_dataset_processor.py` passed; `python3 scripts/test_dataset_processor.py` passed; `./scripts/build.sh` passed end-to-end on this machine, including the capture-pipeline fixture suite, the full `ScratchLabDesktop` XCTest plan, the iOS app build, the macOS app build, and the watchOS build
- Follow-up notes: root cause was that the shared ScratchLab Coach Demo Mode already existed, but the app still shipped without real bundled demo-audio assets, so `baby` and `chirpflare` always fell back to the unavailable state. Fixed by copying the local dataset `angle_1_noBeat.wav` files for those two scratches into a new bundled `CoachDemoAudio` resource folder, teaching `ScratchCoachDemoAudioPlayer` to search that subdirectory, updating the existing instruction JSON to point at the concrete WAV filenames, wiring the folder into the iOS and macOS resource phases, and adding regressions that prove the demo-audio folder is bundled and the WAVs can be opened successfully. No UI redesign, network behavior, training, with-beat audio, beat-only audio, copied dialogue, or real DJ likeness content was added.

- Selected task: extend the existing ScratchLab Coach UI with local Demo Mode playback
- Files changed: `ScratchLab/Models/CaptureCore.swift`, `ScratchLab/Views/PracticeModeView.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLab/Resources/CoachInstructions/baby.json`, `ScratchLab/Resources/CoachInstructions/chirpflare.json`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: `python3 scripts/test_dataset_processor.py` passed; the focused coach XCTest slice passed via `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testScratchCoachInstructionDecodesStructuredJSON -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testScratchCoachInstructionStoreLoadsInstructionByScratchTypeAlias -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testScratchCoachInstructionStoreReturnsFallbackForMissingInstruction -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testScratchCoachInstructionStoreReturnsFallbackForMalformedInstruction -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testScratchCoachDemoAudioPlayerFallsBackWhenAudioIsMissing -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testScratchCoachDemoAudioPlayerSupportsPlayPauseReplayAndStop -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testPracticeFlowDoesNotCreateCaptureSessions -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testPracticeModeSourceExposesCoachCardInScrollableSetupOverlay -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testPracticeModeCoachDemoSourceStopsWhenPracticeBeatStarts -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testMacAnalyzerSourceExposesCoachPanel -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testMacCoachDemoSourceStopsWhenPracticeBeatOrRoutineCaptureStarts -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testCoachInstructionsResourceFolderIsBundledForIOSAndMacTargets`; `./scripts/build.sh` passed end-to-end on this machine, including the capture-pipeline fixture suite, the full `ScratchLabDesktop` XCTest plan, the iOS app build, the macOS app build, and the watchOS build
- Follow-up notes: root cause was that the new bundled ScratchLab Coach layer surfaced original instructional text but had no local demo playback path, so the coach card could not offer listen/replay guidance or a safe disabled state when demo audio was unavailable. Fixed by extending `ScratchCoachInstruction` with `demoAudioFile` and default `demoAudioRole`, adding a shared `ScratchCoachDemoAudioPlayer` with play/pause/replay/stop plus safe missing-audio fallback, wiring the existing iOS and macOS coach surfaces to show Demo Mode controls and an original record/crossfader cue visual, and stopping demo playback when the app backgrounds or the existing practice-beat or routine-record flows become active so no capture/session behavior changes. No network, AI, training, transcription, real DJ likeness, or copied DVD dialogue was added.

- Selected task: add a first-pass ScratchLab Coach UI to iOS and macOS using bundled original instruction JSON
- Files changed: `ScratchLab/Models/CaptureCore.swift`, `ScratchLab/Views/PracticeModeView.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLab/Resources/CoachInstructions/baby.json`, `ScratchLab/Resources/CoachInstructions/chirpflare.json`, `ScratchLab.xcodeproj/project.pbxproj`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: `python3 scripts/test_dataset_processor.py` passed; `./scripts/build.sh` passed end-to-end on this machine, including the capture-pipeline fixture suite, the full `ScratchLabDesktop` XCTest plan, the iOS app build, and the watchOS build
- Follow-up notes: root cause was that original ScratchLab instruction data existed outside the app, but there was no shared model, loader, or UI surface that exposed safe local coaching guidance on iPhone or Mac. Fixed by adding `ScratchCoachInstruction` plus `ScratchCoachInstructionStore` in shared capture core, bundling original `baby` and `chirpflare` JSON coach notes, wiring a compact `ScratchLab Coach` card into the iOS practice setup overlay and the macOS analyzer/practice card, and failing closed to neutral or unavailable states when no scratch is selected or a JSON file is missing/malformed. Added regression coverage for JSON decoding, scratch-type lookup aliasing, missing/malformed fallback, coach-card source contracts, app-bundle resource inclusion, and the existing guarantee that opening Practice does not create capture sessions. No capture, record, export, network, AI, or DJ-likeness behavior was added or changed.

- Selected task: extend the source media ingest tool to extract mapped multi-audio streams per camera angle
- Files changed: `scripts/dataset_processor/ingest_media_scratch.py`, `scripts/test_dataset_processor.py`, `scripts/dataset_processor/README.md`, `README.md`, `TASKS.md`, `DEV_LOG.md`
- Build result: `python3 -m py_compile scripts/dataset_processor/ingest_media_scratch.py scripts/test_dataset_processor.py` passed; `python3 scripts/test_dataset_processor.py` passed; `./scripts/build.sh` passed end-to-end on this machine, including the capture-pipeline fixture suite, the full `ScratchLabDesktop` XCTest plan, and the iOS, macOS, and watchOS builds
- Follow-up notes: root cause was that the first-pass source media ingester still flattened each source MKV to one filename-derived role even though the real source media rip layout carries multiple audio streams per camera angle. Refactored `scripts/dataset_processor/ingest_media_scratch.py` to probe all audio streams with `ffprobe`, preserve one `angle_n_video.mov` per source angle, extract mapped `withBeat`, `noBeat`, and `beatOnly` WAVs plus metadata sidecars, add `--audio-map` and `--inspect-streams`, and keep filename fallback only for true single-stream files. Extended `scripts/test_dataset_processor.py` with stream-level coverage for mapped extraction, metadata roles, dry-run and inspect safety, and the removal of any `isolatedScratch` output contract, then updated the dataset docs and root README to describe the inspect-then-ingest workflow. No Swift app code, bundle IDs, targets, schemes, or App Store metadata were changed.

- Selected task: build the first-pass source media ingest tool for CXL Dataset scratch data
- Files changed: `scripts/dataset_processor/ingest_media_scratch.py`, `scripts/test_dataset_processor.py`, `scripts/dataset_processor/README.md`, `README.md`, `TASKS.md`, `DEV_LOG.md`
- Build result: `python3 -m py_compile scripts/dataset_processor/ingest_media_scratch.py scripts/test_dataset_processor.py` passed; `python3 scripts/test_dataset_processor.py` passed; `./scripts/build.sh` passed end-to-end on this machine, including the capture-pipeline fixture suite, the full `ScratchLabDesktop` XCTest plan, and the iOS, macOS, and watchOS builds
- Follow-up notes: root cause was that the existing source media ingester had already grown into later-phase behavior with optional BPM inference and broader content-role detection, while the requested tool for this pass was ingest-only normalization. Narrowed `scripts/dataset_processor/ingest_media_scratch.py` to folder-name BPM parsing only, beat versus no-beat filename classification, deterministic no-beat angle assignment plus a `beat_angle`, generated-output filtering, MKV copy plus WAV extraction, and canonical metadata/manifests with review-safe warnings when the no-beat count is not four or more than one beat file is present. Updated the Python regression coverage and dataset docs to match the first-pass scope: no instruction extraction, no BPM inference, and no isolated-scratch detection yet. No Swift app code, bundle IDs, targets, schemes, or App Store metadata were changed.

- Selected task: build a source media-specific offline scratch ingest tool for canonical dataset preparation
- Files changed: `scripts/dataset_processor/ingest_media_scratch.py`, `scripts/test_dataset_processor.py`, `scripts/dataset_processor/README.md`, `README.md`, `TASKS.md`, `DEV_LOG.md`
- Build result: `python3 -m py_compile scripts/dataset_processor/ingest_media_scratch.py scripts/test_dataset_processor.py` passed; `python3 scripts/test_dataset_processor.py` passed; `./scripts/build.sh` passed end-to-end on this machine, including the capture-pipeline fixture suite, the full `ScratchLabDesktop` XCTest plan, the iOS app build, the macOS app build, and the watchOS build
- Follow-up notes: root cause was that the older DVD/CXL Dataset prep CLI had been removed once its chapter-split assumptions were superseded, but the offline dataset pipeline still needed a clean source media-specific ingest path for newer rip sets. Added `scripts/dataset_processor/ingest_media_scratch.py` as a terminal-only ffmpeg/ffprobe wrapper that groups MKVs by scratch folder, infers scratch metadata from folder names, classifies beat/no-beat/reference content by filename, assigns deterministic camera angles, optionally estimates BPM from beat/reference audio with a conservative confidence gate, extracts WAVs beside copied MKVs, and writes per-item metadata plus folder manifests. Extended `scripts/test_dataset_processor.py` with source media ingest coverage for folder parsing, classification, angle assignment, metadata, manifests, dry-run behavior, inference failure safety, and default BPM fallback, then updated the dataset docs and root README. No Swift app code, bundle IDs, targets, schemes, App Store metadata, or model training logic were changed.

- Selected task: remove the obsolete CXL Dataset DVD preparation tool and older chapter-split assumptions
- Files changed: `scripts/dataset_processor/prepare_dvd_scratch.py`, `scripts/test_dataset_processor.py`, `scripts/dataset_processor/README.md`, `README.md`, `TASKS.md`, `DEV_LOG.md`
- Build result: `python3 -m py_compile scripts/dataset_processor/process_dataset.py scripts/dataset_processor/label_clip.py scripts/test_dataset_processor.py` passed; `python3 scripts/test_dataset_processor.py` passed; `./scripts/build.sh` passed end-to-end on this machine, including the capture-pipeline fixture suite, the full `ScratchLabDesktop` XCTest plan, the iOS app build, the macOS app build, and the watchOS build
- Follow-up notes: root cause was that the earlier DVD/CXL Dataset prep workflow was built around older HandBrake-era chapter and angle assumptions that are now superseded by cleaner source media rips. Removed the obsolete `scripts/dataset_processor/prepare_dvd_scratch.py` CLI, deleted only the tied DVD-specific coverage from `scripts/test_dataset_processor.py`, and trimmed the root/dataset-processor READMEs back to the still-supported ScratchLab ZIP and loose-clip ingestion flows. No Swift app code, bundle IDs, targets, schemes, App Store metadata, or the shared dataset processor contracts were changed.

## 2026-04-26
- Selected task: build an offline CXL Dataset DVD multi-angle preparation tool for dataset-ready MKV splits and metadata
- Files changed: `scripts/dataset_processor/prepare_dvd_scratch.py`, `scripts/test_dataset_processor.py`, `scripts/dataset_processor/README.md`, `README.md`, `TASKS.md`, `DEV_LOG.md`
- Build result: `python3 -m py_compile scripts/dataset_processor/prepare_dvd_scratch.py scripts/test_dataset_processor.py` passed; `python3 scripts/test_dataset_processor.py` passed; `./scripts/build.sh` passed end-to-end on this machine, including the capture-pipeline fixture suite, the full `ScratchLabDesktop` XCTest plan, the iOS app build, the macOS app build, and the watchOS build
- Follow-up notes: root cause was that the offline dataset pipeline could already handle ScratchLab exports and loose clips, but it still lacked a safe batch workflow for CXL Dataset DVD MKV folders with chapter-based multi-angle performance cuts. Added `scripts/dataset_processor/prepare_dvd_scratch.py` as a standard-library-only ffprobe/ffmpeg wrapper that infers scratch metadata from folder names, classifies beat and no-beat angles, splits Chapter 2 with an extra offset into performance clips, optionally generates cleaned-audio and instruction outputs, writes stable sidecar metadata plus folder manifests, and never mutates the source MKV files. Extended `scripts/test_dataset_processor.py` with coverage for folder parsing, generated-file filtering, beat detection, no-beat angle assignment, warning conditions, metadata generation, manifest generation, missing Chapter 2 failure, and dry-run no-write behavior, then updated the dataset processor docs and root README with the CXL Dataset/DVD workflow. No Swift app code, bundle IDs, targets, schemes, App Store metadata, or model training changes were introduced.

- Selected task: add offline segmentation planning scaffolding for future dataset event extraction
- Files changed: `scripts/dataset_processor/SEGMENTATION_PLAN.md`, `scripts/dataset_processor/README.md`, `README.md`, `TASKS.md`, `DEV_LOG.md`
- Build result: `./scripts/build.sh` passed end-to-end on this machine, including the capture-pipeline fixture suite, the full `ScratchLabDesktop` XCTest plan, the iOS app build, the macOS app build, and the watchOS build
- Follow-up notes: root cause was that the offline dataset processor now creates accepted whole-take items but had no documented in-repo plan for how future scratch-event segmentation should happen without prematurely implementing ML, mutating accepted data, or touching the shipped apps. Added `scripts/dataset_processor/SEGMENTATION_PLAN.md` as documentation-only scaffolding for a later offline segmenter, with placeholder planning for audio onset, beat-grid, and motion-peak candidate generation, explicit manual review status, a proposed per-segment `meta.json` schema, and a non-destructive sibling output area that preserves accepted whole takes. Updated the dataset processor docs and root README to point at that future plan. No Swift app code, model training, or destructive dataset changes were introduced.

- Selected task: add a lightweight loose-clip labeling CLI for dataset sidecars
- Files changed: `scripts/dataset_processor/label_clip.py`, `scripts/test_dataset_processor.py`, `scripts/dataset_processor/README.md`, `README.md`, `TASKS.md`, `DEV_LOG.md`
- Build result: `python3 -m py_compile scripts/dataset_processor/label_clip.py scripts/test_dataset_processor.py` passed; `python3 scripts/test_dataset_processor.py` passed; `./scripts/build.sh` passed end-to-end on this machine, including the capture-pipeline fixture suite, the full `ScratchLabDesktop` XCTest plan, the iOS app build, the macOS app build, and the watchOS build
- Follow-up notes: root cause was that the offline dataset processor could ingest loose clips only after matching `.meta.json` sidecars already existed, so labeling older audio/video archives still required manual JSON editing. Added `scripts/dataset_processor/label_clip.py` as a standard-library-only helper for one-file and batch sidecar creation, reused the shared loose-clip media and scratch-label contract from the existing processor, enforced overwrite protection unless `--force` is passed, validated confidence and BPM rules while allowing `bpm = null` for `noBeat` and `unknown`, and updated the dataset tooling docs plus regression suite to cover single-file creation, overwrite refusal/force overwrite, invalid confidence rejection, batch labeling, and the null-BPM no-beat case. No Swift app code, bundle IDs, targets, schemes, or App Store metadata were changed.

- Selected task: build a terminal-only offline dataset processor for ScratchLab exports and loose labeled clips
- Files changed: `scripts/dataset_processor/process_dataset.py`, `scripts/dataset_processor/README.md`, `scripts/test_dataset_processor.py`, `README.md`, `TASKS.md`, `DEV_LOG.md`
- Build result: `python3 -m py_compile scripts/dataset_processor/process_dataset.py scripts/test_dataset_processor.py` passed; `python3 scripts/test_dataset_processor.py` passed; `./scripts/build.sh` passed end-to-end on this machine, including the capture-pipeline fixture suite, the full `ScratchLabDesktop` XCTest plan, the iOS app build, the macOS app build, and the watchOS build
- Follow-up notes: root cause was that ScratchLab had app-side ZIP export packaging and the older canonical session scripts, but no standalone offline processor for turning shipped export ZIPs and manually labeled legacy clips into one accepted/rejected training-dataset layout outside the apps. Added `scripts/dataset_processor/process_dataset.py` as a terminal-only CLI with `validate` and `process` modes, optional loose-clip scanning, explicit unlabeled rejection handling, ScratchLab ZIP extraction/validation, optional duration probing through the Python standard library plus `ffprobe` when available, canonical item metadata/manifest output, and accepted/rejected folder materialization that never mutates the source inputs. Added a dedicated dataset-processor README, root README usage notes, and end-to-end regression coverage for valid loose clips, missing metadata rejection, unknown scratch-type rejection, ScratchLab ZIP validation, manifest writing, and canonical accepted folder structure. No Swift app code, bundle IDs, targets, schemes, or App Store metadata were changed.

- Selected task: fix the remaining removed-audio reference in the review demo seed script
- Files changed: `scripts/seed_review_demo_data.sh`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: `zsh -n scripts/seed_review_demo_data.sh` passed; `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testReviewDemoSeedDataLoadsSelectedSession -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testReviewDemoSeedScriptDoesNotDependOnRemovedBundledAudio` passed; `./scripts/pre_release_check.sh` passed end-to-end on this machine, including the required file checks, `./scripts/build.sh`, the capture-pipeline fixture suite, the full `ScratchLabDesktop` XCTest plan, the iOS app build, the macOS app build, and the watchOS build
- Follow-up notes: root cause was that the runtime audit had already removed active shipped crash paths from the deleted Bert/reference audio, but the developer-only `scripts/seed_review_demo_data.sh` flow still copied `ScratchLab/Resources/boom_bap_100bpm.wav`, which no longer exists. Replaced that hardcoded bundle dependency with deterministic generated demo audio, added a clear failure if the output WAV is not produced, and pinned the source contract with a regression that proves the script no longer depends on the removed bundled asset path. Active impact remains limited to review/demo seeding tooling; the shipped iOS/macOS/watchOS runtime remains safe without the removed Bert training/reference audio.

- Selected task: audit removed Bert/training/audio asset impact and harden any remaining runtime references
- Files changed: `ScratchLab/Audio/SampleManager.swift`, `ScratchLab/Audio/BackingTrackManager.swift`, `ScratchLab/Audio/AudioEngine.swift`, `ScratchLab/Audio/ScratchAnalyzer.swift`, `ScratchLabDesktop/Services/MacScratchDetector.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: focused missing-asset regressions passed via `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testLegacyBundleAudioManagersFilterMissingBundledAssetsInSource -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testTrainingAudioDiscoverySourcesUseFallbackWhenBundledLibraryIsMissing -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testMacScratchDetectorTrainingDiscoveryReturnsEmptyWithoutBundledTrainingLibrary -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testLegacySampleAndBackingTrackSelectorsAreNotRoutedFromShippedMenus -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testLegacySampleAndBackingTrackSelectorsRenderMissingAssetEmptyStates`; `./scripts/build.sh` passed end-to-end on this machine, including the capture-pipeline fixture suite, the full `ScratchLabDesktop` XCTest plan, the iOS app build, the macOS app build, and the watchOS build
- Follow-up notes: audited every remaining removed-training/audio reference. Active shipped runtime impact is limited to optional training-profile discovery inside `AudioEngine` on iOS Practice and `MacScratchDetector` on the macOS analyzer; both paths already fail soft to default baby-scratch heuristics when the old training library is missing, so there is no active crash path from removing the Bert/reference assets. The user-facing risk was instead in dormant legacy selectors and helpers: `SampleManager` and `BackingTrackManager` still advertised bundled assets that no longer exist, and `ScratchAnalyzer` could report itself loaded even when the reference library was absent. Hardened those paths by filtering bundled defaults to files that actually exist, adding safe empty-state messaging, exposing explicit missing-library discovery helpers for regression coverage, and making `ScratchAnalyzer` fail closed with `resourceNotFound` when the reference folders are gone. Additional non-shipped references remain in developer-only tooling such as `scripts/seed_review_demo_data.sh`, but they do not affect the shipped iOS/macOS/watchOS app runtime audited here.

- Selected task: run the final full-platform ScratchLab release audit from the repo workflow
- Files changed: `TASKS.md`, `DEV_LOG.md`
- Build result: `./scripts/pre_release_check.sh` passed end-to-end on this machine, including the required file checks, `./scripts/build.sh`, the capture-pipeline fixture suite, the full `ScratchLabDesktop` XCTest plan, the iOS app build, the macOS app build, and the watchOS build
- Follow-up notes: audited the current release candidate against `docs/codex_release_audit_prompt.md` and `docs/release_audit_checklist.md`, with extra attention on the currently changed release-facing surfaces in `MainMenuView`, `LevelSelectView`, `PracticeModeView`, `CompanionCameraView`, `CaptureCore`, `ScratchLabApp`, `Info.plist`, and the project/scheme files. No release-blocking issue was found in the currently exposed practice, guided-capture, session-presentation, watch-relay, or app-identity flows, so no app-code change was required for this audit pass. Remaining manual risks: device-level smoke tests were not performed for small/standard iPhone, iOS `System Check`, iOS back/navigation reachability, macOS small-window layout, watch launch/capture/linking, record/export, or App Review screenshots. Additional non-blocking repo risk: legacy `SampleManager`, `BackingTrackManager`, and `ScratchAnalyzer` bundle lookups still reference audio assets that are no longer present in the source tree, but those paths are not currently routed from the main menu release flow inspected in this audit.

- Selected task: build the permanent ScratchLab release-audit workflow into the repo
- Files changed: `docs/release_audit_checklist.md`, `docs/codex_release_audit_prompt.md`, `scripts/pre_release_check.sh`, `README.md`, `TASKS.md`, `DEV_LOG.md`
- Build result: `./scripts/pre_release_check.sh` passed end-to-end on this machine, including the required file checks, `./scripts/build.sh`, the capture-pipeline fixture suite, the full `ScratchLabDesktop` XCTest plan, the iOS app build, the macOS app build, and the watchOS build
- Follow-up notes: root cause was that the release-audit workflow lived in one-off prompts and chat context instead of inside the repository, so future Codex sessions had no canonical pre-submission checklist or wrapper command. Fixed by adding a permanent release checklist under `docs/`, a reusable Codex audit prompt, a lightweight `scripts/pre_release_check.sh` wrapper that verifies the key audit files and runs the standard build before printing manual smoke-test reminders and explicit guardrails, and a short README pointer for pre-ASC submission use. Manual smoke tests and ASC upload/submission remain intentionally outside this script.

- Selected task: fix iOS guided-capture system-check overflow and safe-area navigation overlap
- Files changed: `ScratchLab/Views/CompanionCameraView.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testGuidedCaptureSetupUsesSharedSessionPresentationModel -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testGuidedCaptureSourceUsesToolbarSafeNavigationChrome -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testGuidedCaptureSystemCheckScrollsOnSmallScreens` passed; `./scripts/build.sh` passed end-to-end on this machine, including the capture-pipeline fixture suite, the full `ScratchLabDesktop` XCTest plan, the iOS app build, the macOS app build, and the watchOS build
- Follow-up notes: root cause was that the iOS guided-capture shell kept its back/trailing controls inside the content stack while only applying fixed in-content padding, and `SystemCheckView` used a non-scrollable vertical stack. On smaller iPhones that let the ScratchLab chrome compete with the system navigation area and pushed lower system-check actions off-screen. Fixed by moving the guided-capture navigation actions into toolbar-safe system chrome, increasing the shared bottom safe-area padding, making `SystemCheckView` scrollable, and adding source-level regressions that pin both the toolbar-safe navigation contract and the small-screen system-check scroll behavior. Manual simulator verification was not performed in this environment.

- Selected task: revise the session cleanup policy so the UI stays capped without deleting real user work
- Files changed: `ScratchLab/Models/CaptureCore.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testSessionListPresentationCapsRecentSessionsAtThree -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testSessionListPresentationFallsBackToMostRecentlyOpenedSessionWhenNoActiveSessionIsSelected -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testRoutineSessionStoreSelectingSessionUpdatesLastOpenedAt -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testRoutineSessionStoreDoesNotDeleteCompletedSessionsBeyondRecentLimit -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testRoutineSessionStorePrunesStaleEmptyDrafts -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testRoutineSessionStoreRetainsStaleSessionsWithArtifacts -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testMacRoutineSidebarUsesSharedSessionPresentationModel -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testGuidedCaptureSetupUsesSharedSessionPresentationModel` passed; `./scripts/build.sh` passed end-to-end on this machine, including the capture-pipeline fixture suite, the full `ScratchLabDesktop` XCTest plan, the iOS app build, the macOS app build, and the watchOS build
- Follow-up notes: root cause was that the previous three-session retention patch solved sidebar clutter by deleting stored routine sessions instead of only constraining presentation. That meant legitimate completed, exported, or artifact-backed sessions could be pruned solely for exceeding the visible recent-session cap. Fixed by narrowing `SessionListPolicy` to a presentation-only recent-session limit of three, replacing `RoutineSessionStore` total-count retention with stale-draft cleanup, and retaining any session that is still selected, has recorded take metrics, has routine capture artifacts on disk, or has a persisted upload job. Only stale empty/setup-only drafts older than the short retention window are now pruned, and the regression suite now proves recent-session capping, completed-session retention beyond three, stale-empty pruning, and artifact-backed session retention. Manual UI verification was not performed in this environment.

- Selected task: clear persisted session drafts and cap shared session retention at three across the app
- Files changed: `ScratchLab/Models/CaptureCore.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testSessionListPresentationCapsRecentSessionsAtSharedLimit -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testSessionListPresentationFallsBackToMostRecentlyOpenedSessionWhenNoActiveSessionIsSelected -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testRoutineSessionStoreSelectingSessionUpdatesLastOpenedAt -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testRoutineSessionStoreRetainsOnlySharedSessionLimit -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testMacRoutineSidebarUsesSharedSessionPresentationModel -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testGuidedCaptureSetupUsesSharedSessionPresentationModel` passed; `./scripts/build.sh` passed end-to-end on this machine, including the capture-pipeline fixture suite, the full `ScratchLabDesktop` XCTest plan, the iOS app build, and the watchOS build
- Follow-up notes: root cause was that the shared session presentation refactor only constrained the `recentSessions` slice and still carried the older larger cap, while the macOS routine draft store itself remained append-only. That meant old routine drafts could keep accumulating on disk and could still reappear via the shared presentation over time. Fixed by adding a shared `SessionListPolicy` in `CaptureCore` with a three-session retention cap, routing `SessionListPresentationModel` through that shared policy, teaching `RoutineSessionStore` to normalize persisted drafts down to the shared limit and prune stale open-history entries, and extending the regression suite to prove both the presentation cap and the store-level retention behavior. After verification, the local persisted session draft/preferences files were cleared on this machine so the app restarts without any old saved sessions. Manual UI verification was not performed in this environment.

- Selected task: refactor the shared session list/sidebar presentation so macOS and iOS stop stacking unbounded session entries
- Files changed: `ScratchLab/Models/CaptureCore.swift`, `ScratchLab/Views/CompanionCameraView.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testSessionListPresentationCapsRecentSessionsAtTen -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testSessionListPresentationFallsBackToMostRecentlyOpenedSessionWhenNoActiveSessionIsSelected -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testRoutineSessionStoreSelectingSessionUpdatesLastOpenedAt -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testMacRoutineSidebarUsesSharedSessionPresentationModel -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testGuidedCaptureSetupUsesSharedSessionPresentationModel -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testPracticeFlowDoesNotCreateCaptureSessions` passed; `./scripts/build.sh` passed end-to-end on this machine, including the capture-pipeline fixture suite, the full `ScratchLabDesktop` XCTest plan, the iOS app build, and the watchOS build
- Follow-up notes: root cause was that macOS rendered the routine session draft store directly as one flat append-only list while iOS only exposed a one-off `Reuse Last Setup` button, so there was no shared notion of `Active`, `Recent`, or `All` sessions and no shared last-opened ordering. Guided capture also refreshed session identity during setup-only transitions, which made the setup flow behave like it had already started a new session before the user had explicitly done so. Fixed by adding a shared `SessionListPresentationModel` plus `SessionOpenHistoryStore` in `CaptureCore`, routing both the macOS routine sidebar and the iOS guided-capture setup through that shared presentation path, pinning macOS to a fixed `Active Session` card with `Recent Sessions` and collapsed `All Sessions`, replacing the iOS `Reuse Last Setup` shortcut with `Current Session`, `Recent Sessions`, and collapsed `All Sessions`, and stopping guided capture from auto-rolling a new session identity during setup-only transitions while the drill-change reset now defers its new identity until recording begins. Manual UI verification was not performed in this environment.

- Selected task: expose the shared Practice beat controls in the macOS Practice UI
- Files changed: `ScratchLabDesktop/ScratchLabDesktopApp.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testPracticeBeatCanStartAndStopWithoutCreatingCaptureArtifacts -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testPracticeBeatBPMChangesRestartSharedEngineAtNewTempo -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testPracticeBeatStopsOnPracticeExitAndBackground -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testPracticeBeatStopsWhenRecordingFlowStarts -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testScratchLabDesktopRootInjectsPracticeBeatStore -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testMacPracticeSourceExposesVisibleBeatControls -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testPracticeModeSourceExposesVisibleBeatControlsInSetupOverlay` passed; `./scripts/build.sh` passed end-to-end on this machine, including the capture-pipeline fixture suite, the full `ScratchLabDesktop` XCTest plan, the iOS build, the macOS build, and the watchOS build
- Follow-up notes: root cause was not the shared beat engine. The macOS Practice screen is a separate `MacAnalyzerView` card, and that card never consumed `PracticeBeatStore` or rendered any beat controls, so iOS showed beat buttons and selectable modes while macOS showed none. Fixed by injecting the existing shared `PracticeBeatStore` into the desktop app root, adding a visible Practice beat section directly to the Mac Practice card with `No Beat` / `Beat On`, visible beat-mode buttons, BPM controls, and `Play Beat` / `Stop Beat`, and wiring the existing shared store through tab-leave, app-inactive, and recording-start stop paths. Added source-level regressions that pin the desktop root injection plus the visible Mac Practice control contract so the section cannot disappear silently again. Manual UI verification was not performed inside this environment.

- Selected task: fix the iOS Practice setup beat-style selector so alternate practice beat modes are actually selectable
- Files changed: `ScratchLab/Views/PracticeModeView.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testPracticeBeatCanStartAndStopWithoutCreatingCaptureArtifacts -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testPracticeBeatBPMChangesRestartSharedEngineAtNewTempo -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testPracticeBeatModeChangesRestartSharedEngineWithSelectedMode -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testPracticeBeatStopsOnPracticeExitAndBackground -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testPracticeBeatStopsWhenRecordingFlowStarts -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testPracticeBeatSettingsCarryIntoRecordSetupOnlyWhenBeatIsEnabled -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testPracticeModeSourceExposesVisibleBeatControlsInSetupOverlay` passed; `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLab -destination 'platform=iOS Simulator,name=iPhone 17' build` passed; `./scripts/build.sh` passed end-to-end on this machine, including the capture-pipeline fixture suite, the full `ScratchLabDesktop` XCTest plan, the iOS app build, the macOS app build, and the watchOS build
- Follow-up notes: root cause was not the shared beat engine or store. Those were already working. The real failure was the iOS Practice setup UI: beat-style selection still used a `Menu`, and in live use that menu rendered but did not reliably fire `practiceBeatStore.selectBeatMode(...)`, so the user could start the click track but could not switch to the other practice beat modes. The fix replaces that menu with direct selectable beat-mode buttons in the existing setup card, keeps the shared `PracticeBeatStore` and `ScratchLabBeatEngine` path untouched, and adds a regression proving mode changes restart the shared engine with the newly selected mode. Manual simulator verification after rebuilding `ScratchLab.app` for iPhone 17 confirmed the setup screen showed `Click track`, `Boom Bap Trainer`, `Minimal Funk`, and `Battle Loop` as direct buttons; selecting `Boom Bap Trainer` changed the selected state immediately; `Play Beat` changed to `Stop Beat`; and pressing `No Beat` switched the state back to `No Beat` and disabled playback immediately.

- Selected task: add optional shared beat/metronome playback to the Scratch Practice tab without creating capture/export artifacts
- Files changed: `ScratchLab/Models/CaptureCore.swift`, `ScratchLab/ScratchLabApp.swift`, `ScratchLab/Views/CompanionCameraView.swift`, `ScratchLab/Views/PracticeModeView.swift`, `ScratchLab/Views/LevelSelectView.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testPracticeBeatCanStartAndStopWithoutCreatingCaptureArtifacts -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testPracticeBeatBPMChangesRestartSharedEngineAtNewTempo -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testPracticeBeatStopsOnPracticeExitAndBackground -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testPracticeBeatStopsWhenRecordingFlowStarts -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testPracticeBeatSettingsCarryIntoRecordSetupOnlyWhenBeatIsEnabled -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testSessionSetupUsesScratchSpecificTimedCaptureBPMOptions` passed; `./scripts/build.sh` passed end-to-end on this machine, including the capture-pipeline fixture suite, the full `ScratchLabDesktop` XCTest plan, the iOS app build, and the watchOS build
- Follow-up notes: root cause was that Scratch Practice still used its own live-audio/backing-track path and did not own a small shared beat state layered on top of the existing record-side click/beat engine. That left Practice disconnected from Record's BPM defaults/range and beat engine, gave it no single lifecycle owner to stop playback when the user left Practice, backgrounded the app, or entered recording, and prevented clean carry-over of shared timing settings into Record. The fix adds a shared `PracticeBeatStore` in Capture Core backed by `ScratchLabBeatEngine` and `CaptureClickTrackDefaults`, persists the selected scratch and BPM, exposes an explicit no-beat state, and never touches capture/export/session creation. The Practice setup UI now shows beat on/off, beat-style, BPM, and play/stop controls; guided-drill timing reads the shared BPM; app/tab/record transitions stop playback; and Record pre-fills from Practice only when there are no stored Record defaults, carrying shared scratch/BPM plus the audible beat mode when that maps cleanly to Record. Manual UI verification was not performed in this environment.

- Selected task: fix confirmed live export blockers for calibration validation and selected-take timing metadata resolution
- Files changed: `ScratchLab/Services/SessionExportCoordinator.swift`, `ScratchLab/Views/CompanionCameraView.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `scripts/test_capture_pipeline.py`, `TASKS.md`, `DEV_LOG.md`
- Build result: `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testCalibrationModeExportMetadataDisablesClickTrack -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testLocalRecordingExportUsesSharedMetadataResolverForClickTrackConfig -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testLocalRecordingExportPrefersSelectedTakeMetadataOverCompatibleSiblingDefaults` passed; `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests` passed; `./scripts/build.sh` passed end-to-end on this machine, including the capture-pipeline fixture suite, the full `ScratchLabDesktop` XCTest plan, the iOS app build, and the watchOS build
- Follow-up notes: root cause was twofold. First, local routine export collapsed calibration takes with `bpm = nil` into `take.bpm = 0`, then the shared canonical validator still required every exported take BPM to be inside the timed capture range, which surfaced the generic `This session has inconsistent metadata.` block even though the persisted calibration sidecar was valid. Second, the shared metadata resolver still chose package/session metadata from the first compatible sidecar or an unrelated current session draft instead of preferring the selected take's persisted sidecar, so `session_metadata.json` and `export_metadata.json` could flatten click/timing fields back to defaults or nulls in mixed compatible sessions. The fix keeps calibration metadata nullable in the exported manifests, derives canonical BPM fallback only for the internal archive path, makes the resolver prefer the selected take sidecar and reject mismatched draft configs, and uses the same validated sidecar source when populating per-take manifest metadata. Archive-level regression values now prove: calibration exports `captureMode = calibration_no_click`, `beatEngineMode = silent`, `bpm = null`, `clickEnabled = false`, `beatEnabled = false`, `countInBeats = 4`, `beatsPerBar = 4`, `clickStartHostTime = null`, `recordingStartHostTime = 456`, `clickVersion = scratchlab-click-v1`, and `engineVersion = scratchlab-beat-engine-v1`; timed selected-take exports retain `captureMode = timed_click`, `beatEngineMode = click_track`, `bpm = 95`, `clickEnabled = true`, `beatEnabled = false`, `countInBeats = 4`, `beatsPerBar = 4`, `clickStartHostTime = 55555`, `recordingStartHostTime = 66666`, `clickVersion = scratchlab-click-v1`, and `engineVersion = scratchlab-beat-engine-v1`. Hardware/manual macOS capture verification was not completed in this environment because the real camera/audio/watch relay flow is not available here; the added archive-level tests cover the exact exported manifest fields instead.

- Selected task: add optional generated practice beats and dataset-safe export mix options on top of the shared click-track capture flow
- Files changed: `ScratchLab/Models/CaptureCore.swift`, `ScratchLab/Services/ClickTrackEngine.swift`, `ScratchLab/Services/ScratchLabBeatEngine.swift`, `ScratchLab/Services/SessionExportCoordinator.swift`, `ScratchLab/Views/CompanionCameraView.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `ScratchLab.xcodeproj/project.pbxproj`, `docs/capture_spec_v1.md`, `docs/metadata_schema.md`, `TASKS.md`, `DEV_LOG.md`
- Build result: `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests` passed; `./scripts/build.sh` passed end-to-end on this machine, including the capture-pipeline fixture suite, the full `ScratchLabDesktop` XCTest plan, the iOS app build, and the watchOS build
- Follow-up notes: audited the existing shared click implementation first and kept `ClickTrackEngine` as the timed-capture default, with `ScratchLabBeatEngine` delegating `.clickTrack` to that existing service instead of duplicating click behavior. Added optional generated practice-beat modes plus dataset-safe export mix metadata through the existing shared resolver/archive path, preserved the existing click metadata semantics, added regression coverage for beat-mode persistence, export-mix quality classification, timing-stem/stems-folder outputs, and shared-resolver reuse, and updated the capture/metadata docs to keep click-track capture as the MVP dataset path while practice beats stay optional training/demo modes.

- Selected task: fix macOS Save ZIP when the chosen destination matches the generated archive path
- Files changed: `ScratchLab/Services/SessionExportCoordinator.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testSessionExportCoordinatorSaveArchiveUsesChosenDestination -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testSessionExportCoordinatorSaveArchiveHandlesMatchingGeneratedDestination` passed; `./scripts/build.sh` passed end-to-end on this machine, including the capture-pipeline fixture suite, `ScratchLabDesktop` XCTest plan, iOS app build, and watchOS build
- Follow-up notes: `Save ZIP...` could delete the freshly generated archive before copying when the chosen destination resolved to the same file location as the temporary archive output. The shared export coordinator now treats same-location saves as an already-saved result instead of removing and re-copying the file, skips cleanup in that case, explicitly foregrounds the save panel before presentation on macOS, and has regression coverage for the matching-destination path.

- Selected task: scope routine export to the selected take's metadata group so mixed captures in one mutable session no longer fail as inconsistent metadata
- Files changed: `ScratchLab/Services/SessionExportCoordinator.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testRoutineExportValidationAcceptsSelectedNonBabyScratchType -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testRoutineExportUsesSelectedTakeMetadataGroupWithinMutableSession -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testRoutineCaptureExportAcceptsRecordedBPMSubset` passed; `./scripts/build.sh` passed end-to-end on this machine, including the capture-pipeline fixture suite, `ScratchLabDesktop` XCTest plan, iOS app build, and watchOS build
- Follow-up notes: routine export was still collecting every sidecar that shared the mutable session ID, even when older takes in that session had different scratch/click metadata. That made the shared export metadata resolver compare incompatible sidecar configs and collapse to the generic `This session has inconsistent metadata.` error. Local export now scopes sidecars to the selected recording's compatible metadata signature, so exporting a later `stab` take ignores earlier `baby` takes recorded under different session config while still preserving the shared validation path and deterministic export package shape.

- Selected task: restore scratch-aware routine export validation so non-baby captures like stab no longer fail export
- Files changed: `ScratchLab/Models/CaptureReliability.swift`, `ScratchLab/Services/SessionExportCoordinator.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testRoutineCaptureExportAcceptsRecordedBPMSubset -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testRoutineExportValidationAcceptsSelectedNonBabyScratchType -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testExportValidationFailsWhenCanonicalManifestContractDrifts` passed; `./scripts/build.sh` passed end-to-end on this machine, including the capture-pipeline fixture suite, `ScratchLabDesktop` XCTest plan, iOS app build, and watchOS build
- Follow-up notes: the shared export validator and canonical packaging path were still enforcing the legacy baby-scratch contract for every workflow, so a routine capture recorded as `stab` failed validation before export. Routine and guided exports now derive a normalized scratch token from the selected scratch metadata for validation, manifest content, and canonical file naming, while the older legacy canonical path remains baby-only where the script contract still requires it. Added regression coverage for a non-baby `stab` routine export plus the legacy drift guard.

- Selected task: restore scratch-specific timed-capture BPM options so session setup no longer shows the same preset list for every scratch
- Files changed: `ScratchLab/Models/CaptureCore.swift`, `ScratchLab/Views/CompanionCameraView.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testSessionSetupUsesScratchSpecificTimedCaptureBPMOptions` passed; `./scripts/build.sh` passed end-to-end on this machine, including the capture-pipeline fixture suite, `ScratchLabDesktop` XCTest plan, iOS app build, and watchOS build
- Follow-up notes: the timed-capture BPM UI had regressed to `CaptureClickTrackDefaults.presetBPMs`, so every scratch showed the same `80 / 95 / 110` presets even though `CaptureSessionScratchType.trainingBPMList` already defined scratch-specific BPM plans. `SessionSetupViewModel.allowedBPMList` now resolves from the selected scratch's shared training BPM list with a safe global fallback when no scratch is selected, and both the iOS and macOS session-setup views now render those shared per-scratch options instead of bypassing the model. Added regression coverage for scratch-specific BPM options plus the no-scratch fallback.

- Selected task: restore scratch-specific default speeds in Formula Lab so parsed drills no longer flatten every scratch to the same timing
- Files changed: `ScratchLab/Models/Scratch.swift`, `ScratchLab/Formula/ScratchFormulaCatalog.swift`, `ScratchLab/Views/FormulaPlaygroundView.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `ScratchLab.xcodeproj/project.pbxproj`, `TASKS.md`, `DEV_LOG.md`
- Build result: `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testFormulaCatalogUsesScratchSpecificDefaultBeatLengths -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testFormulaRendererKeepsDistinctScratchDurations` passed; `./scripts/build.sh` passed end-to-end on this machine, including the capture-pipeline fixture suite, `ScratchLabDesktop` XCTest plan, iOS app build, and watchOS build
- Follow-up notes: Formula Lab was flattening every supported scratch symbol to `defaultBeats = 1.0`, so parsed drills rendered distinct scratches at the same apparent speed. The shared scratch model now exposes `formulaDefaultBeats` derived from each scratch's canonical `patternSignature.expectedDuration`, normalized so the baby scratch remains `1 beat` while shorter and longer scratches keep distinct timing. The formula catalog now resolves each symbol's default beat length from that shared model, the supported-symbols UI exposes the per-scratch default timing, and regression coverage verifies both the catalog mapping and rendered drill durations. Added the shared Formula Lab parser/catalog/renderer files to the desktop target so the existing macOS test bundle can exercise the same shared timing path without duplicating logic.

- Selected task: fix the timed-capture record-start crash reported after pressing Start Recording in the macOS app
- Files changed: `ScratchLab/Services/ClickTrackEngine.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' build` passed; `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests` passed; a direct local smoke harness that calls `ClickTrackEngine.start(bpm: 95)` then `stop()` passed without crashing; `./scripts/build.sh` now passed end-to-end on this machine, including the iOS and watchOS legs
- Follow-up notes: the crash report showed an uncaught `NSException` from `AVAudioPlayerNode.scheduleBuffer(...)` inside `ClickTrackEngine.scheduleBeat(at:generation:)`, before movie capture started. The shared click engine now reconnects the player node on an explicit mono format, resets/stops the node cleanly between runs, and schedules each beat buffer at explicit sample times instead of relying on `at: nil` sequencing. That keeps the click-track path deterministic and removes the record-start abort seen on macOS timed capture.

- Selected task: build internal click-track capture into the shared timed capture flow for iOS and macOS while keeping calibration captures silent
- Files changed: `ScratchLab/Models/CaptureCore.swift`, `ScratchLab/Services/ClickTrackEngine.swift`, `ScratchLab/Services/CompanionCameraBroadcaster.swift`, `ScratchLab/Services/SessionExportCoordinator.swift`, `ScratchLab/Views/CompanionCameraView.swift`, `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `ScratchLab.xcodeproj/project.pbxproj`, `docs/capture_spec_v1.md`, `docs/metadata_schema.md`, `TASKS.md`, `DEV_LOG.md`
- Build result: `./scripts/build.sh` passed the capture-pipeline fixture suite and the full `ScratchLabDesktop` XCTest plan, then failed on the iOS build leg because this machine's Xcode install is missing the iOS 26.4 platform component required for `-destination generic/platform=iOS`; fallback `xcodebuild -project ScratchLab.xcodeproj -target ScratchLabWatch -sdk watchsimulator CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO build` passed; fallback `xcodebuild -project ScratchLab.xcodeproj -target ScratchLab -sdk iphonesimulator CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO build` reached a pre-existing `ScratchLabWatch/Assets.xcassets` simulator icon-content failure unrelated to the click-track changes
- Follow-up notes: added a shared `CaptureSessionCaptureMode` plus click-track defaults/timing metadata, introduced `ClickTrackEngine` using `AVAudioEngine` and scheduled player-node buffers, wired a 4-beat timed count-in on iOS/macOS, and kept calibration capture click-free. Sidecars and exports now carry `captureMode`, `clickEnabled`, `countInBeats`, `beatsPerBar`, `clickStartHostTime`, `recordingStartHostTime`, `clickAccentPattern`, and `clickVersion`, with `manifests/session_metadata.json` emitted from the existing shared export resolver path. Added regression coverage for calibration/timed export metadata, preset BPM persistence, BPM clamping, shared resolver usage, and session/take identity safety. Remaining environment work is outside this diff: install the iOS 26.4 platform component to let `./scripts/build.sh` complete its iOS leg on this machine, and fix the existing watch icon asset issue before relying on the iphonesimulator target build path.

## 2026-04-25
- Selected task: user-requested Xcode signing/team configuration restore for ScratchLab targets
- Files changed: `ScratchLab.xcodeproj/project.pbxproj`, `TASKS.md`, `DEV_LOG.md`
- Build result: `xcodebuild -showBuildSettings -project ScratchLab.xcodeproj -scheme ScratchLab` still fails on this machine because Xcode cannot resolve a valid iOS destination without the local iOS 26.4 device platform component; `xcodebuild -showBuildSettings -project ScratchLab.xcodeproj -scheme ScratchLabDesktop` passed and resolved `DEVELOPMENT_TEAM = 2DDKGL33BU`; fallback `xcodebuild -showBuildSettings -project ScratchLab.xcodeproj -target ScratchLab -sdk iphonesimulator` passed and resolved `DEVELOPMENT_TEAM = 2DDKGL33BU`; `./scripts/build.sh` was rerun and failed in the iOS leg for the same missing iOS 26.4 device platform component; `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' build` passed; `xcodebuild -project ScratchLab.xcodeproj -target ScratchLabWatch -sdk watchos CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO build` passed
- Follow-up notes: reused the existing Apple Developer team id `2DDKGL33BU` already present in the macOS, watchOS, project, and test configs, and added it explicitly to the missing iOS app target Debug/Release build settings while leaving bundle identifiers, entitlements, capabilities, and target names unchanged. There is no watch extension target in this project; the only watch target is the watch app with bundle id `com.machelpnz.scratchlab.watchkitapp`. Remaining manual environment step is to install the iOS 26.4 platform component in Xcode so plain scheme-level iOS destination resolution and `./scripts/build.sh` can complete on this machine again.

## 2026-04-25
- Selected task: user-requested App Store Review compliance audit across iOS, macOS, and watchOS
- Files changed: `ScratchLab/Services/SessionExportCoordinator.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLab/Views/CompanionCameraView.swift`, `ScratchLab/Models/CaptureCore.swift`, `ScratchLab/Info.plist`, `ScratchLabDesktop/Info.plist`, `ScratchLabDesktop/ScratchLabDesktop.entitlements`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `docs/app_review_mac_screenshot_plan.md`, `docs/app_review_mac_release_checklist.md`, `TASKS.md`, `DEV_LOG.md`
- Build result: `./scripts/build.sh` passed the capture-pipeline fixture suite, the `ScratchLabDesktop` XCTest plan, and the iOS, macOS, and watchOS builds; `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -configuration Release -destination 'platform=macOS' build` passed; `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLab -configuration Release -archivePath build/AppReviewAudit-iOS.xcarchive CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO archive` passed
- Follow-up notes: completed a review-focused audit across first-run UX, entitlements, privacy copy, release-only UI, screenshot readiness, and explicit release builds. macOS ZIP export now uses `NSSavePanel` instead of a hardcoded Downloads write so `com.apple.security.files.downloads.read-write` was removed; `com.apple.security.network.server` remains because the performer monitor still starts an `NWListener` and publishes a Bonjour service. The release builds no longer surface the staging inspector on iOS/macOS, iOS/macOS permission strings now match the shipped capture and local-network features, the unused photo-library usage key was removed, and regression coverage now proves the shared new-session path, the save-panel archive path, the performer-monitor listener path, the main-window reopen commands, and the real-window screenshot capture script contract. Remaining review work is manual App Store Connect screenshot replacement using the existing real-UI capture plan.

## 2026-04-25
- Selected task: fix macOS `File > New Session` App Review retry path
- Files changed: `ScratchLab/Models/CaptureCore.swift`, `ScratchLabDesktop/ScratchLabDesktopApp.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: focused `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testRoutineSessionUIActionFactoryDelegatesToSharedCreatePath` passed; `./scripts/build.sh` passed for fixture tests, the full `ScratchLabDesktop` XCTest plan, and iOS/macOS/watchOS builds; `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -configuration Release -destination 'platform=macOS' build` passed
- Follow-up notes: `RoutineSessionStore.createNewSessionFromUI()` now returns the created draft on success and nil on persistence failure while preserving immediate persistence, selection, `NEW_SESSION_CREATED:` logging, and the visible error banner path. `File > New Session` now uses the shared `RoutineSessionUIActionFactory` with a success hook that routes the macOS workspace to Routine Capture and opens the main ScratchLab window so the new selected session is visible from the menu path. Regression coverage now verifies the shared action success hook receives the created sessions and failure returns no draft.

## 2026-04-25
- Selected task: user-requested App Review follow-up for macOS window reopening and entitlement verification
- Files changed: `TASKS.md`, `DEV_LOG.md`, `ScratchLabDesktop/ScratchLabDesktopApp.swift`
- Build result: `./scripts/build.sh` passed the capture-pipeline fixture suite, the `ScratchLabDesktop` XCTest plan, and the iOS, macOS, and watchOS builds
- Follow-up notes: the main ScratchLab window now uses a stable scene id and has an explicit Window-menu reopen command via `Show ScratchLab`, so closing the main window no longer leaves the app without a user-visible way back in. Kept `com.apple.security.files.downloads.read-write` because the real macOS export flow copies ZIP archives into the user Downloads folder through `SessionExportCoordinator.copyArchiveToDownloads(...)`, and kept `com.apple.security.network.server` because the performer monitor feature starts a real `NWListener` and publishes a Bonjour service for nearby-device viewing. The screenshot rejection still requires manual App Store Connect media replacement; the repo already contains the real-UI capture plan, checklist, helper script, and prepared screenshot assets from the earlier App Review work.

## 2026-04-25
- Selected task: user-requested App Review retry hardening for macOS New Session creation
- Files changed: `TASKS.md`, `DEV_LOG.md`, `ScratchLab/Models/CaptureCore.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`
- Build result: `./scripts/build.sh` passed the capture-pipeline fixture suite, the `ScratchLabDesktop` XCTest plan, and the iOS, macOS, and watchOS builds
- Follow-up notes: kept the existing macOS menu and toolbar wiring intact, but moved the routine-session draft creation itself onto an explicit shared `CaptureCore.createNewRoutineSessionDraft(...)` path so `File > New Session` and the in-app buttons all mint the same UUID-backed persisted draft shape. Successful creation now prints `NEW_SESSION_CREATED: <sessionID>` after the store write commits, and the previous modal failure alert is now rendered as a visible in-window banner so a create-session persistence failure is not silent. Added regression coverage for the shared factory defaults and for the create-session failure rollback/error-state path; an initial attempt to reuse the shared draft factory inside `SessionSetupViewModel` tripped an existing capture-pipeline guardrail that asserts the literal macOS default initializer shape, so that initializer was intentionally left unchanged while the shared create path stayed on the store/UI action flow required by App Review.

## 2026-04-25
- Selected task: user-requested redo of no-decks macOS App Review screenshot capture guidance
- Files changed: `TASKS.md`, `DEV_LOG.md`, `docs/app_review_mac_screenshot_plan.md`
- Build result: `zsh -n scripts/capture_mac_review_window.sh` passed; `./scripts/capture_mac_review_window.sh --list` returned the deterministic final 7-shot order; non-interactive `./scripts/capture_mac_review_window.sh --shot 04` refused capture with the no-decks framing guidance instead of silently capturing; `./scripts/build.sh` passed the capture-pipeline fixture tests, the `ScratchLabDesktop` XCTest plan, and iOS, macOS, and watchOS builds; `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -configuration Release -destination 'platform=macOS' build` succeeded.
- Follow-up notes: rechecked the no-decks App Review workflow without changing the production capture pipeline. The screenshot plan now uses the exact `No-decks review capture setup` heading, the release checklist already verifies intentional camera framing for `04-stage-and-audio-routing.png`, active recording framing for `06-recording-in-progress.png`, and export-ready proof for `07-captured-result-or-export-ready.png`, and the capture helper already requires an interactive framing confirmation before `04` and `06`. No in-app helper copy was added because there is no review/demo-only surface needed for this documentation/script workflow, and adding production UI copy would increase review risk.

## 2026-04-25
- Selected task: user-requested App Review macOS build upload
- Files changed: `TASKS.md`, `DEV_LOG.md`, `ScratchLab.xcodeproj/project.pbxproj`, `ScratchLab/Info.plist`, `ScratchLabDesktop/Info.plist`, `ScratchLabWatch/Info.plist`
- Build result: bumped the shared `CURRENT_PROJECT_VERSION` / `CFBundleVersion` from `14` to `15`; `./scripts/build.sh` passed the capture-pipeline fixture tests, the `ScratchLabDesktop` XCTest plan, and the iOS, macOS, and watchOS builds; `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -configuration Release -destination 'platform=macOS' build` succeeded; `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' -configuration Release -archivePath build/ScratchLabDesktop-TestFlight-v15.xcarchive archive` succeeded; `xcodebuild -exportArchive -archivePath build/ScratchLabDesktop-TestFlight-v15.xcarchive -exportPath build/TestFlightMacUpload15 -exportOptionsPlist build/release-exports/ExportOptions-mac-upload.plist` uploaded successfully and reported `Uploaded ScratchLabDesktop`.
- Follow-up notes: App Store Connect accepted the macOS package and reported `Uploaded package is processing` for build `15`. Upload covered the macOS binary only; the App Review screenshot set still needs to be uploaded/ordered manually in App Store Connect because screenshot metadata upload is a listing operation, not part of Xcode binary export.

## 2026-04-25
- Selected task: user-requested no-decks macOS App Review screenshot capture guidance
- Files changed: `TASKS.md`, `DEV_LOG.md`, `docs/app_review_mac_screenshot_plan.md`, `docs/app_review_mac_release_checklist.md`, `scripts/capture_mac_review_window.sh`
- Build result: `zsh -n scripts/capture_mac_review_window.sh` passed; `./scripts/capture_mac_review_window.sh --list` returned the deterministic 7-shot order; non-interactive `--shot 04` now fails clearly with no-decks framing guidance; `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -configuration Release -destination 'platform=macOS' build` succeeded; `./scripts/build.sh` passed the capture-pipeline fixture tests, ScratchLabDesktop XCTest plan, and iOS, macOS, and watchOS builds.
- Follow-up notes: added a review-safe no-decks desk/table setup to the Mac screenshot plan so screenshots can still show real app UI, real camera framing, and active capture without real DJ hardware. Updated the release checklist with shot-specific framing checks for stage/audio routing, active recording, and export-ready/result screenshots. The capture helper now prints explicit no-decks guidance and requires interactive confirmation before capturing `04-stage-and-audio-routing.png` and `06-recording-in-progress.png`, preventing silent ceiling-heavy captures for those review-sensitive shots.

## 2026-04-25
- Selected task: user-requested macOS App Review screenshot proof-of-action pass
- Files changed: `TASKS.md`, `DEV_LOG.md`, `ScratchLabDesktop/ScratchLabDesktop.entitlements`, `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `scripts/seed_review_demo_data.sh`, `scripts/capture_mac_review_window.sh`, `docs/app_review_mac_release_checklist.md`, `docs/app_review_mac_screenshot_plan.md`, `build/app-review-mac-screenshots/final/`
- Build result: `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -configuration Release -destination 'platform=macOS' build` succeeded; `./scripts/build.sh` passed the capture-pipeline fixture tests, the `ScratchLabDesktop` XCTest plan, and the iOS, macOS, and watchOS builds
- Follow-up notes: tightened the Mac screenshot flow around real app states instead of marketing or setup-only screens. Added deterministic screenshots for active recording and export-ready capture result, recaptured the metadata/setup and stage/deck-guide shots, kept the first-launch empty state as the final screenshot only, and restored seeded review data after a clean-install/empty-state verification. The seed helper now writes selected Demo DJ / Baby Scratch / 90 BPM / Full Capture data plus a completed local routine capture only through the real persisted model and sidecar/media files. The capture helper now lists the final seven-shot order, validates the real app window and PNG output before replacing files, and documents the manual requirement to capture active recording from a real running recording. Export smoke testing exposed that the sandboxed macOS app could not write the `Save ZIP to Downloads` output, so the desktop target now declares the standard Downloads read/write entitlement; after rebuilding, `Save ZIP to Downloads` created `session_2026_04_25_demo_dj_baby_scratch_90_bpm.zip` in `~/Downloads`.

## 2026-04-25
- Selected task: user-requested App Review macOS New-session fix, clean-install empty state, and reviewer-safe screenshot prep
- Files changed: `TASKS.md`, `DEV_LOG.md`, `ScratchLab/Models/CaptureCore.swift`, `ScratchLabDesktop/ScratchLabDesktopApp.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `scripts/seed_review_demo_data.sh`, `scripts/capture_mac_review_window.sh`, `docs/app_review_mac_release_checklist.md`, `docs/app_review_mac_screenshot_plan.md`
- Build result: `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -configuration Release -destination 'platform=macOS' build` succeeded for the review repro flow, and `scripts/build.sh` then passed the capture-pipeline fixture suite, the `ScratchLabDesktop` XCTest plan, and the iOS, macOS, and watchOS builds
- Follow-up notes: App Review was still hitting macOS's default new-window path instead of a shared draft-creation flow, so `New` could appear to do nothing useful on a clean install and the first-launch screen gave the reviewer no reliable in-app recovery path. macOS now routes the toolbar, sidebar, empty-state, and `File > New Session` actions through one `RoutineSessionStore.createNewSessionFromUI()` path that logs start/success/failure, creates a UUID-backed draft immediately without requiring performer/BPM/scratch metadata up front, selects the new session on the main actor, and surfaces a visible alert on failure; the first-launch empty state now uses the review-safe `Create your first session` copy with a working `New Session` button. Added regression coverage for fresh-store creation, persisted-store reload creation, UUID-style IDs, selected-session updates, metadata-optional creation, multi-click uniqueness, and shared action delegation. Added `scripts/seed_review_demo_data.sh`, a macOS window screenshot helper, release/screenshot checklist docs, and prepared real macOS review assets under `build/app-review-mac-screenshots/final/` showing the actual app UI in use.

## 2026-04-24
- Selected task: user-requested App Review macOS upload UX cleanup and permission-reset prep
- Files changed: `ScratchLab/Services/SessionUploadManager.swift`, `ScratchLab/Views/CompanionCameraView.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `DEV_LOG.md`
- Build result: `scripts/build.sh` passed the capture-pipeline fixture suite, the `ScratchLabDesktop` XCTest plan, and the iOS, macOS, and watchOS builds; afterward `tccutil reset Camera com.machelpnz.scratchlab` and `tccutil reset Microphone com.machelpnz.scratchlab` both succeeded
- Follow-up notes: replaced the developer-facing unconfigured-upload copy with human-readable wording and hid the upload section entirely on iOS/macOS when cloud upload is not available and no persisted upload job exists, so App Review only sees supported share/export flows for this build. Camera and microphone will prompt again on the next macOS run of `com.machelpnz.scratchlab`; macOS local-network privacy cannot be reset back to the undetermined state with `tccutil`, so that prompt is not part of the reset. `TASKS.md` was left unchanged because this was a user-directed App Review fix rather than the first unchecked backlog item.

## 2026-04-24
- Selected task: user-reported iPad guided-capture export fix for skipped-watch sessions
- Files changed: `ScratchLab/Models/CaptureCore.swift`, `ScratchLab/Views/CompanionCameraView.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `DEV_LOG.md`
- Build result: focused macOS XCTest coverage for `testGuidedCaptureReviewStateDoesNotInventWatchMotionWhenMotionIsSkipped` passed, and `scripts/build.sh` then passed the capture-pipeline fixture suite, the `ScratchLabDesktop` XCTest plan, and the iOS, macOS, and watchOS builds
- Follow-up notes: guided capture was treating `motion skipped` or `motion optional` review state as if a real watch artifact existed, so iPad exports packaged completed takes with `motionPresent == true` even when no linked watch capture had ever been recorded. The review/export logic now keeps `Motion optional` as UI-only state, only marks export motion present when an actual linked watch capture exists, and the take review pill now shows `Motion Optional` instead of the misleading `Motion Present` label for skipped-watch takes. `TASKS.md` was left unchanged because this was a user-directed reliability fix rather than the first unchecked backlog item.

## 2026-04-24
- Selected task: user-reported iPad guided-capture share/upload export fix
- Files changed: `ScratchLab/Services/SessionExportCoordinator.swift`, `ScratchLab/Services/SessionUploadManager.swift`, `ScratchLab/Views/CompanionCameraView.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `DEV_LOG.md`
- Build result: focused macOS XCTest coverage for `testGuidedCaptureExportHydratesMissingAudioArtifactAndDefaultsSlateFlags` passed, and `scripts/build.sh` then passed the capture-pipeline fixture suite, the `ScratchLabDesktop` XCTest plan, and the iOS, macOS, and watchOS builds
- Follow-up notes: the guided-capture completion flow was constructing `SessionExportPackage` values with no `audioArtifactURL` and no explicit slate/clap booleans, so the shared canonical validator refused every iPad share/upload attempt before archiving; guided exports also still treated partial single-BPM sessions as invalid coverage, and the iOS share presenter was mounted on the share button background instead of a stable screen host. The shared archive builder now hydrates guided packages with the sibling or derived WAV artifact, defaults guided slate/clap fields to `false`, accepts the recorded BPM subset for `guided_capture`, and the iOS share presenter now lives on the screen host so iPad share sheets have a stable anchor. `TASKS.md` was left unchanged because this was a user-directed reliability fix rather than the first unchecked backlog item.

## 2026-04-24
- Selected task: user-reported iOS guided-capture header overlap and Capture Take calibration editing fix
- Files changed: `ScratchLab/Views/CompanionCameraView.swift`, `DEV_LOG.md`
- Build result: initial `scripts/build.sh` run failed in the macOS XCTest phase because Xcode hit `No space left on device` while copying generated test frameworks; after clearing generated Xcode DerivedData/module/build caches only, rerunning `scripts/build.sh` passed the capture-pipeline fixture suite, the macOS XCTest plan, and the iOS, macOS, and watchOS builds
- Follow-up notes: the custom guided-capture header used a fixed 20-point top padding while the background ignored the safe area, so the `Back` and `Staging` controls could sit under the iOS status bar/time. The header and banner now offset from the device safe-area top inset, and the Capture Take preview now binds to the live calibration profile so deck/mixer boxes can be adjusted while ready, then locked during pre-roll/recording/saving and persisted before the take starts.

## 2026-04-24
- Selected task: fix iPad guided-capture deck calibration boxes so they can be moved directly where the operator sees them
- Files changed: `ScratchLab/Views/CompanionCameraView.swift`, `DEV_LOG.md`
- Build result: `scripts/build.sh` passed for the capture-pipeline fixture suite, the macOS XCTest plan, and the iOS, macOS, and watchOS builds
- Follow-up notes: the iPad camera alignment screen displayed the deck/mixer guide boxes but left that preview non-editable and depended on a separate adjust screen; it also left the underlying UIKit camera preview participating in hit testing. The alignment preview now allows direct box dragging, `Confirm Camera` saves the adjusted calibration, the camera preview itself is passive for touches, and the full guide rectangle plus larger resize handle are explicit touch targets.

## 2026-04-24
- Selected task: archive and upload ScratchLab build `14` for both iOS and macOS to App Store Connect from this Mac
- Files changed: `DEV_LOG.md`
- Build result: `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLab -destination 'generic/platform=iOS' -configuration Release -archivePath build/ScratchLab-TestFlight-v14.xcarchive archive` succeeded; `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' -configuration Release -archivePath build/ScratchLabDesktop-TestFlight-v14.xcarchive archive` succeeded; `xcodebuild -exportArchive -archivePath build/ScratchLabDesktop-TestFlight-v14.xcarchive -exportPath build/TestFlightMacUpload14 -exportOptionsPlist build/release-exports/ExportOptions-mac-upload.plist` uploaded successfully and reported `Uploaded ScratchLabDesktop`; the first iOS export attempt failed because Xcode's packaging temp area ran out of space, but after clearing old generated build artifacts and retrying with workspace-backed temp space, `TMPDIR=/Users/karlwatson/Downloads/ScratchLab/build/tmp-export-ios xcodebuild -exportArchive -archivePath build/ScratchLab-TestFlight-v14.xcarchive -exportPath build/TestFlightUpload14 -exportOptionsPlist build/release-exports/ExportOptions-iOS-upload.plist` uploaded successfully and reported `Uploaded ScratchLab`
- Follow-up notes: both uploaded packages reached the `Uploaded package is processing.` state in App Store Connect on 2026-04-24; the iOS upload blocker was local disk exhaustion during IPA creation (`No space left on device`), not a signing or App Store Connect rejection, and freeing old disposable `build/` artifacts resolved it without changing the release payload

## 2026-04-24
- Selected task: fix the watch launch shell branding shown in review screenshots by removing the extra `Watch` suffix and making the watch icon artwork read larger on-device
- Files changed: `ScratchLabWatch/Info.plist`, `ScratchLabWatch/Assets.xcassets/AppIcon.appiconset/icon-1024.png`, `ScratchLabWatch/Assets.xcassets/AppIcon.appiconset/watch-appLauncher-38mm@2x.png`, `ScratchLabWatch/Assets.xcassets/AppIcon.appiconset/watch-longLook-42mm@2x.png`, `ScratchLabWatch/Assets.xcassets/AppIcon.appiconset/watch-longLook-44mm@2x.png`, `ScratchLabWatch/Assets.xcassets/AppIcon.appiconset/watch-notification-38mm@2x.png`, `ScratchLabWatch/Assets.xcassets/AppIcon.appiconset/watch-notification-42mm@2x.png`, `ScratchLabWatch/Assets.xcassets/AppIcon.appiconset/watch-quickLook-38mm@2x.png`, `ScratchLabWatch/Assets.xcassets/AppIcon.appiconset/watch-quickLook-42mm@2x.png`, `ScratchLabWatch/Assets.xcassets/AppIcon.appiconset/watch-quickLook-44mm@2x.png`, `ScratchLabWatch/Assets.xcassets/AppIcon.appiconset/watch-settings@2x.png`, `ScratchLabWatch/Assets.xcassets/AppIcon.appiconset/watch-settings@3x.png`, `DEV_LOG.md`
- Build result: `scripts/build.sh` started successfully through the capture-pipeline fixture suite, but the verification run then failed in the macOS XCTest phase with an environment-side Xcode crash: `LLVM ERROR: OnDiskCAS: corrupt internal reference` followed by `unexpected service error: The Xcode build system has crashed`; this appears unrelated to the watch plist/icon changes
- Follow-up notes: changed the shipped watch display name from `ScratchLab Watch` to `ScratchLab` so the launch shell matches the main app branding, and regenerated every watch icon slot from a tighter crop so the watch icon artwork occupies more of the rendered glyph area that App Review sees on-device

## 2026-04-24
- Selected task: ensure the iOS guided-capture setup carries the same BPM guardrails and shared export assumptions already enforced on macOS/shared services
- Files changed: `ScratchLab/Views/CompanionCameraView.swift`, `DEV_LOG.md`
- Build result: `scripts/build.sh` passed for the capture-pipeline fixture suite, the macOS XCTest plan, and the iOS, macOS, and watchOS builds
- Follow-up notes: most of the recent capture/export fixes were already shared because they live in `SessionExportCoordinator`, `StagedCaptureRecovery`, `WatchMotionCaptureStore`, and the shared capture models/services used by both platforms; the remaining iOS-specific gap was the session-setup BPM field, which still accepted free-form numeric entry while macOS had moved to constrained BPM selection, so the iPhone/iPad guided capture flow now uses the same fixed BPM picker pattern over the canonical BPM list instead of a text field

## 2026-04-24
- Selected task: fix the iOS App Review rejection for undersized Apple Watch icon artwork and missing in-app recording indication during audio capture, then ship a new App Store Connect build
- Files changed: `ScratchLab.xcodeproj/project.pbxproj`, `ScratchLab/Views/CompanionCameraView.swift`, `ScratchLabWatch/Assets.xcassets/AppIcon.appiconset/icon-1024.png`, `ScratchLabWatch/Assets.xcassets/AppIcon.appiconset/watch-appLauncher-38mm@2x.png`, `ScratchLabWatch/Assets.xcassets/AppIcon.appiconset/watch-longLook-42mm@2x.png`, `ScratchLabWatch/Assets.xcassets/AppIcon.appiconset/watch-longLook-44mm@2x.png`, `ScratchLabWatch/Assets.xcassets/AppIcon.appiconset/watch-notification-38mm@2x.png`, `ScratchLabWatch/Assets.xcassets/AppIcon.appiconset/watch-notification-42mm@2x.png`, `ScratchLabWatch/Assets.xcassets/AppIcon.appiconset/watch-quickLook-38mm@2x.png`, `ScratchLabWatch/Assets.xcassets/AppIcon.appiconset/watch-quickLook-42mm@2x.png`, `ScratchLabWatch/Assets.xcassets/AppIcon.appiconset/watch-quickLook-44mm@2x.png`, `ScratchLabWatch/Assets.xcassets/AppIcon.appiconset/watch-settings@2x.png`, `ScratchLabWatch/Assets.xcassets/AppIcon.appiconset/watch-settings@3x.png`, `DEV_LOG.md`
- Build result: `scripts/build.sh` passed for the capture-pipeline fixture suite, the macOS XCTest plan, and the iOS, macOS, and watchOS builds
- Follow-up notes: the watch icon slots were already technically present, but the artwork itself sat too small within the watch icon canvases, so each watch icon PNG was regenerated from a tighter crop of the existing master art; the iOS companion capture screen now shows a persistent red/orange recording banner whenever ScratchLab is actively recording or finishing a take so audio capture is explicitly disclosed in-app; bumped the shared `CURRENT_PROJECT_VERSION` from `13` to `14` so the review fix can upload as a fresh App Store Connect build

## 2026-04-24
- Selected task: user-reported fix for the lower macOS routine record control staying on `Start Recording` long enough to send a second start instead of a stop
- Files changed: `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `DEV_LOG.md`
- Build result: `scripts/build.sh` passed for the capture-pipeline fixture suite, the macOS XCTest plan, and the iOS, macOS, and watchOS builds; the updated macOS app was relaunched from `/Users/karlwatson/Library/Developer/Xcode/DerivedData/Build/Products/Debug/ScratchLab.app`
- Follow-up notes: `isRoutineRecording` had only been flipping true after AVFoundation’s `didStartRecording` callback, leaving a race where the lower button still read `Start Recording` even though the watch had already begun; the Mac capture engine now marks routine recording active immediately when a start is requested and clears that state again on start failure so the second tap correctly goes down the stop path instead of reissuing start

## 2026-04-24
- Selected task: user-reported follow-up fix for the watch not auto-starting from the first macOS record tap and the lower deck-calibration record button still not staying visible in every fit state
- Files changed: `ScratchLab/Services/WatchMotionCaptureStore.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `DEV_LOG.md`
- Build result: `scripts/build.sh` passed for the capture-pipeline fixture suite, the macOS XCTest plan, and the iOS, macOS, and watchOS builds
- Follow-up notes: the iPhone watch-control bridge was returning `Watch connectivity is still activating` immediately if the WCSession had not finished activating on the first command, so remote capture start could miss the watch entirely; added short activation retries before failing that command, and made the lower deck-calibration `Start Recording` / `Stop Recording` control stay visible even when fit is still unlocked so the bottom action remains the same live recording control in all calibration states

## 2026-04-24
- Selected task: user-reported fix for relayed watch captures not appearing in the macOS staging inspector even after the relay imported and linked them
- Files changed: `ScratchLab/Services/StagedCaptureRecovery.swift`, `DEV_LOG.md`
- Build result: `scripts/build.sh` passed for the capture-pipeline fixture suite, the macOS XCTest plan, and the iOS, macOS, and watchOS builds
- Follow-up notes: the relayed watch reconciliation path was only updating the matching routine sidecar and routine audit summaries, so the dedicated `Relayed Watch Capture` staging pane had no `.relayedWatch` session summaries to load; added explicit relayed-watch audit summary persistence for matched relay files so the staging inspector can enumerate and display those imported captures

## 2026-04-24
- Selected task: user-requested macOS routine quick-action fix so the lower deck-calibration controls expose an obvious start/stop record action and a non-dead upload action, and the companion instructions stop sending watch relay users into the old iPhone setup flow
- Files changed: `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `DEV_LOG.md`
- Build result: `scripts/build.sh` passed for the capture-pipeline fixture suite, the macOS XCTest plan, and the iOS, macOS, and watchOS builds
- Follow-up notes: made the lower deck-calibration upload action always visible, prominent, and fail-loud when no routine session exists yet instead of silently doing nothing, refreshed upload availability on view load, and updated the macOS companion guidance so watch relay users can keep the iPhone on the main ScratchLab menu while companion-camera video remains an optional separate path

## 2026-04-24
- Selected task: user-requested iPhone staging inspector portrait-layout fix so the staging sheet no longer uses a landscape/desktop-only composition
- Files changed: `ScratchLab/Views/StagingInspectorView.swift`, `DEV_LOG.md`
- Build result: `scripts/build.sh` passed for the capture-pipeline fixture suite, the macOS XCTest plan, and the iOS, macOS, and watchOS builds
- Follow-up notes: removed the hard desktop minimum width from the staging inspector container on compact screens, switched the multi-context storage picker from segmented to menu style on narrow iPhones, and made the summary badges, action buttons, session header, and quarantine action rows fall back to stacked layouts when horizontal space is limited so the sheet remains readable in portrait

## 2026-04-24
- Selected task: user-requested macOS UI cleanup to remove the live green practice/motion overlay elements from the camera view
- Files changed: `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `DEV_LOG.md`
- Build result: `scripts/build.sh` passed for the capture-pipeline fixture suite, the macOS XCTest plan, and the iOS, macOS, and watchOS builds; the updated macOS app was relaunched from `/Users/karlwatson/Library/Developer/Xcode/DerivedData/Build/Products/Debug/ScratchLab.app`
- Follow-up notes: removed the live `HandMotionOverlay` from the camera stages entirely and limited `DeckGamificationOverlay` to calibration mode only, so the green motion chip, cursor rings, and practice badges no longer sit on top of the live preview once the operator is out of deck-fit mode; deck calibration guides still appear when `showRigGuides` is active

## 2026-04-24
- Selected task: user-requested macOS routine-capture usability fix to add a second `Start Recording` control at the deck calibration section and redeploy the patched watch build
- Files changed: `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `DEV_LOG.md`
- Build result: `scripts/build.sh` passed for the capture-pipeline fixture suite, the macOS XCTest plan, and the iOS, macOS, and watchOS builds; the updated macOS app was relaunched from `/Users/karlwatson/Library/Developer/Xcode/DerivedData/Build/Products/Debug/ScratchLab.app`; `./scripts/run-on-watch.sh` was retried twice but both installs failed with Xcode destination timeout `Karl Watson may need to be unlocked to recover from previously reported preparation errors`
- Follow-up notes: extracted the existing macOS routine record action into a shared button view so the same start/stop/watch-control path is now available both in the main `Routine Capture` card and beside `Deck Calibration`, which removes the need to scroll back to the top before recording; no capture logic changed beyond reusing the same recording trigger in a second location

## 2026-04-23
- Selected task: user-requested fix so watch relay/control no longer depends on opening the guided `CompanionCameraView` session-setup flow on iPhone
- Files changed: `ScratchLab/Services/CompanionCameraBroadcaster.swift`, `ScratchLab/ScratchLabApp.swift`, `ScratchLab/Views/CompanionCameraView.swift`, `DEV_LOG.md`
- Build result: `scripts/build.sh` passed for the capture-pipeline fixture suite, the macOS XCTest plan, and the iOS, macOS, and watchOS builds; direct `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLab -destination 'generic/platform=iOS' build` also passed against the final relay changes
- Follow-up notes: moved watch relay advertising, remote watch control handling, and imported watch-capture forwarding to the app root so the iPhone no longer needs the full guided capture screen alive just to bridge Mac and Watch traffic; split broadcaster shutdown so `CompanionCameraView` now stops only camera/audio capture services while the app-level Multipeer relay remains available; removed the leftover view-level relay hook so watch packets have a single owner and do not double-send when the guided capture screen is open

## 2026-04-20
- Selected task: user-requested fix for the broken watch relay start/return path between macOS, iPhone companion, and Apple Watch
- Files changed: `ScratchLab/Services/WatchMotionCaptureStore.swift`, `ScratchLab/Views/CompanionCameraView.swift`, `DEV_LOG.md`
- Build result: `scripts/build.sh` passed for the capture-pipeline fixture suite, the macOS XCTest suite, and the iOS, macOS, and watchOS builds
- Follow-up notes: made the iPhone watch connectivity session activate eagerly instead of waiting for a specific view lifecycle before remote control, ensured remote watch start/stop requests re-activate connectivity before checking availability, and added a direct imported-capture callback so the iPhone companion relays newly received watch motion sessions straight back to macOS as soon as they arrive rather than relying only on a SwiftUI `importedSessions` count change

## 2026-04-20
- Selected task: user-requested App Store Connect upload of the current ScratchLab iOS and macOS builds
- Files changed: `ScratchLab.xcodeproj/project.pbxproj`, `ScratchLab.xcodeproj/xcshareddata/xcschemes/ScratchLab.xcscheme`, `DEV_LOG.md`
- Build result: `scripts/build.sh` passed for the capture-pipeline fixture suite, the macOS XCTest suite, and the iOS, macOS, and watchOS builds; archived `build/ScratchLab-TestFlight-v13.xcarchive` and `build/ScratchLabDesktop-TestFlight-v13.xcarchive`; `xcodebuild -exportArchive` with `build/release-exports/ExportOptions-iOS-upload.plist` uploaded `ScratchLab` build `13`; `xcodebuild -exportArchive` with `build/release-exports/ExportOptions-mac-upload.plist` uploaded `ScratchLabDesktop` build `13`
- Follow-up notes: bumped the shared `CURRENT_PROJECT_VERSION` from `12` to `13` so App Store Connect would accept a fresh binary upload, corrected the shared iOS scheme so its test-plan build entries no longer participate in archive builds, confirmed the iOS archive embeds `ScratchLabWatch` with build `13`, and both the iOS and macOS uploads reached the `Uploaded package is processing` state successfully in App Store Connect

## 2026-04-20
- Selected task: user-requested final Phase 4 staging hardening pass for stronger transaction reconstruction, ambiguity-safe quarantine restore, remaining touched AVFoundation cleanup, and integration-style reliability coverage
- Files changed: `DEV_LOG.md`, `ScratchLab/Services/StagedCaptureRecovery.swift`, `ScratchLab/Services/StagingInspector.swift`, `ScratchLab/Views/StagingInspectorView.swift`, `ScratchLab/Views/CompanionCameraView.swift`, `ScratchLab/Services/SessionExportCoordinator.swift`, `ScratchLab/Services/WatchMotionCaptureStore.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`
- Build result: `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test -only-testing:ScratchLabDesktopTests`, `python3 scripts/test_capture_pipeline.py`, and `scripts/build.sh` all passed
- Follow-up notes: narrowed quarantine-restore ambiguity to proven conflicting provenance instead of missing metadata, inferred artifact roles safely from file types when the journal does not carry one, surfaced transaction state plus conflicting origin context in the inspector, modernized the remaining touched iOS AVFoundation audio/thumbnail probing away from deprecated sync APIs, added integration-style coverage for partial transaction states, startup recovery/audit persistence, upload-preparation validation reporting, ambiguous restore blocking, and restore-then-rescan reconciliation, and removed the new transaction snapshot ID warning so the touched reliability path builds cleanly

- Selected task: user-requested Phase 4 transactional journal, quarantine workflow, and audit drill-down hardening for staged capture
- Files changed: `DEV_LOG.md`, `ScratchLab/Services/StagedCaptureRecovery.swift`, `ScratchLab/Services/StagingInspector.swift`, `ScratchLab/Views/StagingInspectorView.swift`, `ScratchLab/Services/CompanionCameraBroadcaster.swift`, `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `ScratchLab/Services/SessionExportCoordinator.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`
- Build result: `python3 scripts/test_capture_pipeline.py`, `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test -only-testing:ScratchLabDesktopTests`, `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test`, and `scripts/build.sh` all passed
- Follow-up notes: extended the compact journal into explicit transaction begin/write/finalize and validation-block entries, added safe restore/delete quarantine actions plus deeper take/quarantine history drill-down in the staging inspector, used transaction history to explain orphaned-media quarantine decisions, and replaced the export video probe’s deprecated synchronous AVFoundation track lookup with loaded property-based probing; remaining warnings are outside this phase’s touched validation path in `CompanionCameraView.swift` and `WatchMotionCaptureStore.swift`

## 2026-04-20
- Selected task: user-requested Phase 1 capture reliability hardening for canonical export parity, strict app-side export validation, and first real XCTest coverage
- Files changed: `AI_CONTEXT.md`, `DEV_LOG.md`, `README.md`, `docs/capture_spec_v1.md`, `docs/current_architecture_reality.md`, `scripts/build.sh`, `scripts/test_capture_pipeline.py`, `ScratchLab.xcodeproj/project.pbxproj`, `ScratchLab.xcodeproj/xcshareddata/xcschemes/ScratchLabDesktop.xcscheme`, `ScratchLab.xctestplan`, `ScratchLab 2.xctestplan`, `ScratchLab/Models/CaptureReliability.swift`, `ScratchLab/Services/SessionExportCoordinator.swift`, `ScratchLab/Services/SessionUploadManager.swift`, `ScratchLab/Views/CompanionCameraView.swift`, `ScratchLabDesktop/ScratchLabDesktopApp.swift`, `ScratchLabDesktop/Services/CompanionCameraReceiver.swift`, `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`
- Build result: `python3 -m pytest scripts/test_capture_pipeline.py` passed; repeated `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test` and `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1Tests` runs reached the new macOS XCTest bundle and executed Phase 1 cases, but the hosted macOS test runner still restarted repeatedly during longer runs and did not complete cleanly in this pass
- Follow-up notes: app export now builds the canonical script-style manifest/take-log structure instead of the old parallel app export shape, `preparePackage` is now the strict canonical validation gate for export/share/upload, the new macOS XCTest target and both checked-in test plans are populated, the Mac app shell suppresses live browsing/advertising/upload startup during XCTest hosting, and late watch acknowledgements now reconcile deterministically as degraded rather than mutating a timed-out take back to synchronized; the remaining blocker for closing this Phase 1 slice is stabilizing the macOS hosted XCTest run so the full checked-in suite can complete under `xcodebuild test`
- Selected task: user-requested Phase 1 capture reliability hardening across shared identity and watch coordination seams
- Files changed: `AI_CONTEXT.md`, `DEV_LOG.md`, `README.md`, `docs/capture_spec_v1.md`, `docs/current_architecture_reality.md`, `scripts/validate_session.py`, `ScratchLab.xcodeproj/project.pbxproj`, `ScratchLab/Models/CaptureCore.swift`, `ScratchLab/Models/CaptureReliability.swift`, `ScratchLab/Models/WatchMotionCapture.swift`, `ScratchLab/Services/CompanionCameraBroadcaster.swift`, `ScratchLab/Services/WatchMotionCaptureStore.swift`, `ScratchLab/Views/CompanionCameraView.swift`, `ScratchLabDesktop/Services/CompanionCameraReceiver.swift`, `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLabWatch/Services/WatchMotionRecorder.swift`
- Build result: `scripts/build.sh ios` passed and `scripts/build.sh mac` passed; both runs also re-executed the capture-pipeline fixture suite
- Follow-up notes: session IDs are now UUID-backed instead of date-derived, take identity is explicit and stable inside the session, macOS routine start now reserves the take before recording and waits for a watch ack/timeout path, watch control payloads now carry session/take linkage, imported watch sessions persist that linkage, and the validator/docs now agree that explicit `false` values for slate/clap fields are valid; canonical export-schema unification and real XCTest target wiring are still outstanding follow-up work for this phase
- Selected task: move macOS routine recordings into a sandbox-safe app container so recording no longer depends on Movies-folder permission
- Files changed: `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `DEV_LOG.md`
- Build result: `scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab` (iOS), `ScratchLabDesktop` (macOS), and `ScratchLabWatch` (watchOS)
- Follow-up notes: changed macOS routine capture storage from `~/Movies/ScratchLab Routines` to the app’s writable Application Support container under `ScratchLab/RoutineCaptures`, with safe fallbacks to the app Documents container if needed; this keeps local routine recording inside the sandbox-safe app storage path and avoids the user-visible permission failure when opening or recording into the previous Movies folder
- Selected task: restore reliable macOS routine recording startup when the selected camera/audio session graph is stale
- Files changed: `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `DEV_LOG.md`
- Build result: `scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab` (iOS), `ScratchLabDesktop` (macOS), and `ScratchLabWatch` (watchOS)
- Follow-up notes: tightened macOS routine-recording preflight so the engine now verifies the selected camera and audio devices still exist, synchronously rebuilds the capture session graph for those selections before recording if the graph is stale, and reports specific start failures when the selected camera or audio route cannot be attached instead of collapsing everything into a generic record failure; `TASKS.md` was left unchanged because it currently has no unchecked implementation-ready task to mark complete

## 2026-04-19
- Selected task: stabilize macOS dual-camera stage layout and make the routine record button fail loudly
- Files changed: `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `DEV_LOG.md`
- Build result: `scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab` (iOS), `ScratchLabDesktop` (macOS), and `ScratchLabWatch` (watchOS)
- Follow-up notes: constrained the macOS stage cards to a landscape presentation so dual-cam keeps both panes side by side instead of letting the companion feed fill and crop the stage, switched the companion pane to fit-inside scaling on a black background so portrait phone frames no longer swallow the whole area, and removed the dead routine-record disabled state so pressing Start Recording now surfaces the first missing session-setup issue through the engine status instead of appearing inert
- Selected task: stop the macOS companion feed from invalidating the whole analyzer UI on every frame
- Files changed: `ScratchLabDesktop/Services/CompanionCameraReceiver.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `DEV_LOG.md`
- Build result: `scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab` (iOS), `ScratchLabDesktop` (macOS), and `ScratchLabWatch` (watchOS)
- Follow-up notes: split the live companion frames into a dedicated desktop frame store, stopped updating the main companion connection status on every incoming packet, and throttled frame publication to a bounded desktop refresh rate so the analyzer shell no longer re-renders broad SwiftUI sections for each frame while the companion stream is active
- Selected task: reduce companion camera feed latency between iPhone and macOS
- Files changed: `ScratchLab/Services/CompanionCameraBroadcaster.swift`, `ScratchLabDesktop/Services/CompanionCameraReceiver.swift`, `DEV_LOG.md`
- Build result: `scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab` (iOS), `ScratchLabDesktop` (macOS), and `ScratchLabWatch` (watchOS)
- Follow-up notes: reduced the iPhone companion-stream payload size and increased its send cadence so the deck preview reaches macOS faster, then made the macOS receiver discard stale out-of-order frames instead of rendering backlog; this keeps the live companion view biased toward freshness rather than image quality and avoids the visibly delayed stream that was building up under the previous large-JPEG path
- Selected task: clean up the iPhone review UI and expose an explicit front/back camera choice
- Files changed: `ScratchLab/Views/CompanionCameraView.swift`, `DEV_LOG.md`
- Build result: `scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab` (iOS), `ScratchLabDesktop` (macOS), and `ScratchLabWatch` (watchOS)
- Follow-up notes: replaced the vague iPhone `Camera profile` selection with a direct `Camera` chooser that maps persisted older draft values onto explicit `Front Camera` and `Back Camera` options, and reworked the `Review Take` screen for narrow phones by switching the readiness chips and quality tags to two-column grids and the take detail rows to stacked blocks so labels no longer collapse vertically or clip under the card layout
- Selected task: make the iPhone record action fail loudly instead of appearing dead
- Files changed: `ScratchLab/Views/CompanionCameraView.swift`, `DEV_LOG.md`
- Build result: `scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab` (iOS), `ScratchLabDesktop` (macOS), and `ScratchLabWatch` (watchOS)
- Follow-up notes: removed the dead-end disabled `Record Take` state on the iPhone capture screen so tapping the primary action now either starts the preroll when setup is valid or shows a warning banner and jumps directly to the first blocking setup issue, which makes the record path actionable instead of looking broken when calibration, camera, audio, or storage readiness is still missing
- Selected task: restore readable and tappable iPhone guided-capture session setup controls
- Files changed: `ScratchLab/Views/CompanionCameraView.swift`, `DEV_LOG.md`
- Build result: `scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab` (iOS), `ScratchLabDesktop` (macOS), and `ScratchLabWatch` (watchOS)
- Follow-up notes: replaced the cramped two-column iPhone guided-capture setup layout with a single-column form and swapped the transient selection dialogs for native sheet-based option lists with explicit checkmarks and dismissal, so each setup field now opens a dedicated selection surface instead of relying on compact menu-style interactions that were failing on device and making the form hard to read
- Selected task: stop guided-capture preview crashes when opening the iOS record screen
- Files changed: `ScratchLab/Views/CompanionCameraView.swift`, `ScratchLabDesktop/Views/MacCameraPreviewView.swift`, `DEV_LOG.md`
- Build result: `scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab` (iOS), `ScratchLabDesktop` (macOS), and `ScratchLabWatch` (watchOS)
- Follow-up notes: made the iOS and macOS capture preview representables idempotent so they only attach an `AVCaptureSession` to their preview layer when the session instance actually changes and detach cleanly when the view is dismantled; this avoids repeatedly re-adding the same preview layer to a live capture session graph during SwiftUI updates, which matched the ready-screen crash path and missing-preview behavior
- Selected task: restore camera source selection inside the macOS routine-capture workflow
- Files changed: `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `DEV_LOG.md`
- Build result: `scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab` (iOS), `ScratchLabDesktop` (macOS), and `ScratchLabWatch` (watchOS)
- Follow-up notes: added the existing macOS video-device picker directly to the `Routine Capture` card so operators can choose the recording camera in the same place they start a take, kept it disabled during active routine recording, and preserved the built-in-camera shortcut plus continuity/desk-view warning copy for desktop-deck setups
- Selected task: reconcile the stale duplicate `Carry shared session metadata through validation and export` task entry and restore reliable iOS guided-capture session controls
- Files changed: `ScratchLab/Views/CompanionCameraView.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: `scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab` (iOS), `ScratchLabDesktop` (macOS), and `ScratchLabWatch` (watchOS)
- Follow-up notes: replaced the custom iOS session-setup `Menu` controls with native menu-style `Picker` bindings so scratch type and the other setup dropdowns update reliably on iPhone, renamed the guided-capture progression CTA to `Open Record Controls`, and renamed the ready-state capture CTA to `Record Take` so the recording action is explicit once setup is complete; also marked the stale unchecked export-metadata task as complete because the same task was already completed earlier in this log and codebase
- Selected task: upload fresh build 12 of the current ScratchLab apps to App Store Connect
- Files changed: `ScratchLab.xcodeproj/project.pbxproj`, `TASKS.md`, `DEV_LOG.md`
- Build result: `scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab` (iOS), `ScratchLabDesktop` (macOS), and `ScratchLabWatch` (watchOS); `ScratchLab` archived to `build/ScratchLab-TestFlight-v12.xcarchive`; `ScratchLabDesktop` archived to `build/ScratchLabDesktop-TestFlight-v12.xcarchive`; and both App Store Connect uploads succeeded from the existing export plists
- Follow-up notes: confirmed `ScratchLabWatch` stays embedded in the iOS archive at build `12`, found Xcode-managed store provisioning profiles for `com.machelpnz.scratchlab` and `com.machelpnz.scratchlab.watchkitapp` under `~/Library/Developer/Xcode/UserData/Provisioning Profiles`, confirmed no local `Apple Distribution` or Mac distribution signing identity was visible in the keychain, checked the connected Google Drive for obvious `.mobileprovision`, `.p12`, `.cer`, `provision`, and `distribution` filename matches without finding a backup signing bundle, and verified the machine can still complete App Store Connect export/upload with the current managed signing setup
- Selected task: polish platform-safe session setup UX and remove any remaining metadata drift
- Files changed: `ScratchLab/Models/CaptureCore.swift`, `ScratchLab/Views/CompanionCameraView.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: `scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab` (iOS), `ScratchLabDesktop` (macOS), and `ScratchLabWatch` (watchOS)
- Follow-up notes: kept this pass UI-only and removed the remaining stale session-setup wording drift between platforms by aligning iOS and macOS around `Scratch Type`, `Capture Mode`, and `Notes (optional)`, changing the iOS reuse/change-scratch copy so it no longer implies session IDs are reused or that the flow is still drill-based, and updating the shared scratch fallback label from `Drill` to `Scratch` without changing the export schema or broader capture flow
- Selected task: add regression coverage for shared session-config integrity
- Files changed: `scripts/test_capture_pipeline.py`, `TASKS.md`, `DEV_LOG.md`
- Build result: `scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab` (iOS), `ScratchLabDesktop` (macOS), and `ScratchLabWatch` (watchOS)
- Follow-up notes: kept the change inside the existing Python fixture suite and added lightweight source-contract regression checks for the shared `CaptureSessionConfig` required fields, shared iOS/macOS session-setup parity through `SessionSetupViewModel`, shared export metadata resolution through `SessionExportMetadataResolver`, and fail-closed export validation when session metadata is missing or sidecar metadata does not match; this avoids introducing a new Swift test harness while still pinning the canonical shared session-config path used by validation and export
- Selected task: carry shared session metadata through validation and export
- Files changed: `ScratchLab/Services/SessionExportCoordinator.swift`, `ScratchLab/Views/CompanionCameraView.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: `scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab` (iOS), `ScratchLabDesktop` (macOS), and `ScratchLabWatch` (watchOS)
- Follow-up notes: added a shared export-metadata resolver that prefers canonical `CaptureSessionConfig` snapshots persisted in take sidecars, falls back safely for older recordings, and validates that packaged export metadata still matches the canonical session fields captured in those sidecars before ZIP/export proceeds; guided capture now uses that same resolver instead of rebuilding performer/scratch/notes/session values ad hoc from mutable UI state, and the export payload shape stayed unchanged apart from stricter metadata validation
- Selected task: persist shared session metadata into local sidecars and session storage
- Files changed: `ScratchLab/Models/CaptureCore.swift`, `ScratchLab/Services/CompanionCameraBroadcaster.swift`, `ScratchLab/Views/CompanionCameraView.swift`, `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: `scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab` (iOS), `ScratchLabDesktop` (macOS), and `ScratchLabWatch` (watchOS)
- Follow-up notes: kept the local sidecar storage shape intact apart from adding an optional shared `sessionConfig` snapshot, threaded the canonical `CaptureSessionConfig` into sidecar creation on both iOS and macOS right before capture starts, and made the macOS routine recorder prefer the shared session ID when that setup config is present so local take files and sidecar metadata stay associated without redesigning the export schema
- Selected task: enforce required session configuration before capture can start on every platform
- Files changed: `ScratchLab/Models/CaptureCore.swift`, `ScratchLab/Views/CompanionCameraView.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: `scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab` (iOS), `ScratchLabDesktop` (macOS), and `ScratchLabWatch` (watchOS)
- Follow-up notes: centralized the required session-config validation messages in the shared `SessionSetupViewModel`, reused that same human-readable validation on both iOS and macOS, and removed the remaining iOS dead-end in System Check by swapping the disabled begin-capture state for an explicit path back to session setup whenever metadata is still incomplete
- Selected task: bring macOS session setup to feature parity for core capture options
- Files changed: `ScratchLab/Models/CaptureCore.swift`, `ScratchLab/Services/SessionExportCoordinator.swift`, `ScratchLab/Services/SessionUploadManager.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: `scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab` (iOS), `ScratchLabDesktop` (macOS), and `ScratchLabWatch` (watchOS)
- Follow-up notes: added the core macOS routine-capture session setup controls for performer name, BPM, scratch type, drill mode, handedness, and notes directly to the existing routine recording card, reused the shared `SessionSetupViewModel` for those bindings and required-field gating before recording starts, and passed that shared config through the local export/upload path so routine session exports keep the operator-entered metadata without changing the export schema
- Selected task: add strongly typed shared enums for scratch metadata instead of free-text fields
- Files changed: `TASKS.md`, `DEV_LOG.md`, `ScratchLab/Models/CaptureCore.swift`, `ScratchLab/Views/CompanionCameraView.swift`
- Build result: `scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: tightened the shared session-config model by replacing ad hoc `scratchType`, `drillMode`, and `handedness` strings with shared typed enums that use stable raw values matching the existing scratch library IDs and current export values; kept persistence backward-readable by decoding the legacy string keys and re-encoding the same stable string fields for saved drafts; reused those shared enums directly in the guided-capture draft/UI mapping through local typealiases so the visible setup flow did not change, and intentionally left export payload structure unchanged beyond sourcing those values from the typed model
- Selected task: introduce a shared `CaptureSessionConfig` model as the single source of truth
- Files changed: `TASKS.md`, `DEV_LOG.md`, `ScratchLab/Models/CaptureCore.swift`, `ScratchLab/Services/CompanionCameraBroadcaster.swift`, `ScratchLab/Services/SessionExportCoordinator.swift`, `ScratchLab/Views/CompanionCameraView.swift`
- Build result: `scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: the root issue from Task 1 was that canonical session metadata existed only as documentation plus ad hoc per-surface assembly, so iOS draft state, local recording session IDs, and shared export packaging could still drift; added a shared `CaptureSessionConfig` in `CaptureCore` for performer name, BPM, scratch type, drill mode, take duration/count, handedness, notes, session ID, and timestamps; moved guided-capture draft ownership of those canonical fields onto that shared config while leaving deck/camera/watch UI-only fields local to the view draft; made the companion recorder consume the draft’s session ID so local take numbering and sidecars stay aligned with the shared config; and routed both guided-capture and macOS routine export metadata through the shared config initializer without adding new UI or starting the next task
- Selected task: audit and unify shared session metadata across iOS and macOS
- Files changed: `TASKS.md`, `DEV_LOG.md`, `docs/metadata_schema.md`, `ScratchLab/Services/SessionExportCoordinator.swift`, `ScratchLab/Views/CompanionCameraView.swift`
- Build result: `scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: kept this pass audit-first and avoided a new architecture layer; documented the current user-facing iOS versus macOS routine-capture metadata inventory plus the canonical shared session metadata shape in `docs/metadata_schema.md`; upgraded the shared export metadata payload to a v2 session schema with explicit `scratchType`, `drillMode`, `takeCount`, `totalDurationSeconds`, and sidecar-derived `deviceInfo`; removed the macOS routine export’s hard-coded generated note so absent fields stay explicit as `null`; and mapped the existing iOS drill/practice-mode fields into the canonical names without adding new UI, leaving macOS operator-entered performer/BPM/handedness/note inputs as the remaining gap for a later task
- Selected task: freeze the macOS deck-calibration boxes until the user explicitly changes them
- Files changed: `TASKS.md`, `DEV_LOG.md`, `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `ScratchLabDesktop/Views/DeckGamificationOverlay.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`
- Build result: `./scripts/build.sh mac` passed for the capture-pipeline fixture suite and `ScratchLabDesktop`; `./scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: there were still no unchecked backlog items, so this user-requested calibration pass became the next safe task; the Mac capture engine now freezes a single rig-layout seed instead of reapplying live detector output every frame, so the deck boxes stay planted until the user drags, resizes, nudges sliders, or presses `Reset Fit`; camera reconfiguration, manual-guide changes, and Reset Fit all clear that frozen seed so a fresh starting layout can be captured on demand, and the overlay now renders directly from the published rig layout so drag/resize changes stay responsive while the boxes remain stable
- Selected task: add Baby Scratch practice logging to the macOS Scratch Rating workspace
- Files changed: `TASKS.md`, `DEV_LOG.md`, `ScratchLab.xcodeproj/project.pbxproj`, `ScratchLab/Services/ProgressManager.swift`, `ScratchLabDesktop/ScratchLabDesktopApp.swift`, `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`
- Build result: `./scripts/build.sh mac` passed for the capture-pipeline fixture suite and `ScratchLabDesktop`; `./scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: there were still no unchecked backlog items, so this user-requested Mac parity pass became the next safe task; the Scratch Rating sidebar now includes a timed practice card with 5, 10, and 15 minute runs, session score/hits/average/best/streak feedback, save/cancel controls, and saved Baby Scratch progress badges backed by the shared `ProgressManager`; `ScratchLabDesktopApp` now injects that progress manager into both desktop windows, `MacCaptureEngine` exposes a reset helper so each Mac practice run starts from a clean live scratch-rating session, and the macOS target now compiles the shared `Scratch`, `GameState`, and `ProgressManager` files while the UIKit-only Game Center leaderboard presenter stays iPhone-only through `#if canImport(UIKit)`
- Selected task: fix the practice-mode crash seen in Mac-distributed ScratchLab runs
- Files changed: `TASKS.md`, `DEV_LOG.md`, `ScratchLab/Views/FormulaPlaygroundView.swift`, `ScratchLab/Views/LevelSelectView.swift`
- Build result: `./scripts/build.sh ios` passed for the capture-pipeline fixture suite, `ScratchLab`, and `ScratchLabWatch`; `./scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: there were still no unchecked backlog items, so this user-reported crash became the next safe task; recent mac-hosted crash reports for `ScratchLab`, including `~/Library/Logs/DiagnosticReports/ScratchLab-2026-04-18-171538.ips`, showed `PracticeModeView` trapping in `EnvironmentObject.error()` when the setup overlay touched `audioEngine.currentInputSource`; fixed that by explicitly injecting `AudioEngine` and `ProgressManager` into every `PracticeModeView` full-screen cover path in `LevelSelectView`, `LevelDetailView`, and `FormulaPlaygroundView`, so practice and guided-drill presentation no longer depend on implicit sheet environment inheritance

## 2026-04-18
- Selected task: add first cloud upload pipeline for completed ScratchLab sessions
- Files changed: `TASKS.md`, `DEV_LOG.md`, `docs/session_upload_api_v1.md`, `ScratchLab.xcodeproj/project.pbxproj`, `ScratchLab/Services/SessionExportCoordinator.swift`, `ScratchLab/Services/SessionUploadManager.swift`, `ScratchLab/ScratchLabApp.swift`, `ScratchLab/Views/CompanionCameraView.swift`, `ScratchLabDesktop/ScratchLabDesktopApp.swift`, `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`
- Build result: `./scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: there were still no unchecked backlog items, so this user-requested upload pass became the next safe task; reused the existing session export/archive path instead of inventing a second packager, extended the archive metadata with a stable `sessionID`, and added a persisted `SessionUploadManager` queue that stores one upload job folder per local session under Application Support, computes ZIP size plus SHA-256, requests a backend upload session, uploads the ZIP from disk through a background `URLSession` PUT, confirms completion before marking the session cloud-backed, and keeps local session data untouched on failures; the iPhone guided-capture completion card and the macOS routine-capture card now show upload state, progress, retry, and manual-share fallback without exposing raw URLs, S3 terms, or system error dumps; backend configuration stays external through `SCRATCHLAB_UPLOAD_API_BASE_URL`, the `ScratchLabUploadAPIBaseURL` defaults/Info.plist key, and optional future auth headers can be added through the upload client without changing the UI flow

- Selected task: add ZIP export and native share flow for completed ScratchLab sessions
- Files changed: `TASKS.md`, `DEV_LOG.md`, `ScratchLab.xcodeproj/project.pbxproj`, `ScratchLab/Services/SessionExportCoordinator.swift`, `ScratchLab/Services/SessionSharePresenter.swift`, `ScratchLab/ThirdParty/ZIPFoundation/`, `ScratchLab/Views/CompanionCameraView.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`
- Build result: `./scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: there were still no unchecked backlog items, so this user-requested export pass became the next safe task; added a dedicated session export coordinator plus archive builder with explicit export states, human-readable validation failures, temp-cache ZIP creation, and delayed cleanup after sharing; iPhone/iPad now package kept guided-capture takes into a single staged session archive and present it through a SwiftUI `UIActivityViewController` wrapper, macOS can export the latest routine-capture block through `NSSharingServicePicker`, and the ZIP implementation uses a vendored MIT-licensed `ZIPFoundation` source copy because the repo had no existing archive dependency to reuse

- Selected task: add a guided capture flow for repeatable dataset acquisition
- Files changed: `TASKS.md`, `DEV_LOG.md`, `ScratchLab/Services/CompanionCameraBroadcaster.swift`, `ScratchLab/Views/CompanionCameraView.swift`
- Build result: `./scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: there were still no unchecked backlog items, so this user-requested capture-flow pass became the next safe task; reused the existing companion-camera broadcaster, audio engine, watch motion store, and local sidecar format instead of replacing the capture stack, then layered a guided SwiftUI state machine on top with session setup defaults, a readiness validator, focused camera/audio/motion/calibration fix panels, a one-second pre-roll capture hub, compact take review, and keep/retry/discard plus next-take actions; deck calibration on iPhone is currently a persisted local guide profile because no shared iPhone calibration service existed yet, so a future pass can swap that adapter over to a shared detector-backed calibration model if one is extracted

- Selected task: add first-run quick start onboarding and reusable capture help
- Files changed: `TASKS.md`, `DEV_LOG.md`, `ScratchLab/ScratchLabApp.swift`, `ScratchLab/Views/PracticeModeView.swift`
- Build result: `./scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: there were still no unchecked backlog items, so this user-requested onboarding pass became the next safe task; `ContentView` now uses `@AppStorage` keys `hasSeenQuickStart` and `quickStartVersion` to gate a three-screen full-screen quick start after the splash screen, skipping or finishing marks the flow as seen at `QuickStartSettings.currentVersion`, and the guided capture top bar now shows a compact capture-help sheet only while idle so active recording is not interrupted while operators can still relaunch the quick start from help when needed

- Selected task: upload fresh build 11 of the current ScratchLab apps to App Store Connect
- Files changed: `TASKS.md`, `DEV_LOG.md`, `ScratchLab.xcodeproj/project.pbxproj`, `ScratchLab/Resources/boom_bap_100bpm.wav`
- Build result: bumped the shared `CURRENT_PROJECT_VERSION` from `10` to `11`; restored the missing bundled `ScratchLab/Resources/boom_bap_100bpm.wav` asset as a 44.1 kHz 16-bit stereo WAV so the iOS release build could package successfully; `./scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`; `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLab -destination 'generic/platform=iOS' -configuration Release -archivePath build/ScratchLab-TestFlight-v11.xcarchive archive` passed with `ScratchLab.app/Watch/ScratchLabWatch.app` embedded and `CFBundleVersion = 11`; `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' -configuration Release -archivePath build/ScratchLabDesktop-TestFlight-v11.xcarchive archive` passed with `CFBundleVersion = 11`; `xcodebuild -exportArchive` with `build/release-exports/ExportOptions-iOS-upload.plist` uploaded `ScratchLab` build `11` and reported `Uploaded ScratchLab`; `xcodebuild -exportArchive` with `build/release-exports/ExportOptions-mac-upload.plist` uploaded `ScratchLabDesktop` build `11` and reported `Uploaded ScratchLabDesktop`
- Follow-up notes: there were still no unchecked backlog items, so this user-requested release pass became the next safe task; the watch companion remains part of the iOS App Store Connect binary rather than a separate standalone upload, and both uploaded build `11` packages reached App Store Connect processing on 2026-04-18

- Selected task: trim locked-fit macOS hand-pose Vision load
- Files changed: `TASKS.md`, `DEV_LOG.md`, `ScratchLabDesktop/Services/MacCaptureEngine.swift`
- Build result: `./scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`; after relaunching the macOS debug app with `scratchlab.mac.calibrationLocked = true` for a controlled sample, the locked-fit hand-pose hotspot dropped from the earlier baseline's peak hot-stack counts of `48` / `47` down to `25` / `25` in `/tmp/scratchlab-hand-locked-cadence-v2.sample.txt` compared with `/tmp/scratchlab-locked-v2.sample.txt`
- Follow-up notes: there were still no unchecked backlog items, so this user-requested lag trim became the next safe task; `MacCaptureEngine` now sets the hand-pose request ROI to the existing rig tracking region and slows the steady locked-fit hand-pose cadence from `0.12 s` to `0.20 s` whenever Deck Calibration is locked and a rig layout is available, while unlocked sessions keep the faster polling path

- Selected task: slow macOS rig detection once Deck Calibration is locked
- Files changed: `TASKS.md`, `DEV_LOG.md`, `ScratchLabDesktop/Services/DJRigLayoutDetector.swift`, `ScratchLabDesktop/Services/MacCaptureEngine.swift`
- Build result: `./scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`; after relaunching the macOS debug app with `scratchlab.mac.calibrationLocked = true` for a controlled sample, the rectangle-detection hotspot dropped from the earlier unlocked sample's peak hot-stack counts around `28` down to `6` in `/tmp/scratchlab-locked-v2.sample.txt`
- Follow-up notes: there were still no unchecked backlog items, so this user-requested performance pass became the next safe task; `DJRigLayoutDetector` now uses a slower cadence only when a cached layout exists and fit is locked, keeps a longer stale-layout allowance in that mode, and `MacCaptureEngine` now forces the next video-frame detection to run immediately when the fit is locked or unlocked so calibration changes still feel responsive

- Selected task: reduce macOS analyzer lag from unnecessary live publishing
- Files changed: `TASKS.md`, `DEV_LOG.md`, `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`
- Build result: `./scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`; a post-build single-instance macOS debug run showed the idle performer-monitor JPEG path no longer appearing in the hot stack with no connected device, memory dropping from the earlier duplicate-run sample's roughly `435 MB` to about `303 MB`, and the remaining steady CPU load concentrated in live Vision rectangle plus hand-pose analysis
- Follow-up notes: there were still no unchecked backlog items, so this user-requested lag pass became the next safe task; gated performer-monitor frame generation behind active device connections, stopped republishing unchanged rig and hand state into SwiftUI, and throttled audio-meter UI updates without changing capture, analyzer, pairing, or network behavior

- Selected task: finish the final App Review wording sweep for neutral device copy and hidden debug errors
- Files changed: `TASKS.md`, `DEV_LOG.md`, `ScratchLab/Services/CompanionCameraBroadcaster.swift`, `ScratchLab/Services/WatchMotionCaptureStore.swift`, `ScratchLab/Views/CompanionCameraView.swift`, `ScratchLab/Views/FormulaPlaygroundView.swift`, `ScratchLab/Views/MainMenuView.swift`, `ScratchLab/Views/WatchCaptureHubView.swift`, `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLabWatch/Services/WatchMotionRecorder.swift`, `ScratchLabWatch/WatchCaptureView.swift`
- Build result: `./scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: there were still no unchecked backlog items, so this user-requested App Review pass became the next safe task; replaced the last visible `Computer` labels with `Main View`, `main device`, `Connection`, or no device noun at all depending on context, converted the remaining user-facing `Continuity Camera` copy to neutral `External Camera` / `main camera` wording, changed watch save text to `Saved on device`, simplified watch motion-count labels from `Motion Data` to `Motion`, removed the visible formula parser `localizedDescription` surface in favor of a human-readable retry message, and stopped the watch-import UI from showing the raw captured watch device name while leaving internal logging and metadata untouched

- Selected task: polish App Review copy and steady the Mac deck-calibration boxes
- Files changed: `TASKS.md`, `DEV_LOG.md`, `ScratchLab/Services/CompanionCameraBroadcaster.swift`, `ScratchLab/Views/MainMenuView.swift`, `ScratchLab/Views/WatchCaptureHubView.swift`, `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `ScratchLabDesktop/Views/DeckGamificationOverlay.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLabWatch/Services/WatchMotionRecorder.swift`, `ScratchLabWatch/WatchCaptureView.swift`
- Build result: `./scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: there were no unchecked backlog items left, so this user-requested UI cleanup became the next safe task; shortened the watch capture instructions, switched paired-target wording from `iPhone` to `device` where it was user-facing, restyled the watch helper copy away from warning yellow, replaced watch-facing `Samples` labels with `Motion Data`, simplified visible send/connect failure text on watch, iPhone, and Mac surfaces, and froze the active Mac deck-calibration layout during drag so the grabbed box no longer jumps under the pointer

- Selected task: restore watchOS, polish release copy, and upload unified build 10 to App Store Connect
- Files changed: `TASKS.md`, `DEV_LOG.md`, `ScratchLab.xcodeproj/project.pbxproj`, `ScratchLab/Audio/AudioEngine.swift`, `ScratchLab/Services/CompanionCameraBroadcaster.swift`, `ScratchLab/Views/MainMenuView.swift`, `ScratchLab/Views/CompanionCameraView.swift`, `ScratchLab/Views/PracticeModeView.swift`, `ScratchLab/Views/LevelSelectView.swift`, `ScratchLab/Views/WatchCaptureHubView.swift`, `ScratchLab/Services/WatchMotionCaptureStore.swift`, `ScratchLabWatch/WatchCaptureView.swift`, `ScratchLabWatch/Services/WatchMotionRecorder.swift`, `ScratchLabWatch/ScratchLabNotificationController.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`
- Build result: bumped the shared `CURRENT_PROJECT_VERSION` from `9` to `10`; `./scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`; `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLab -destination 'generic/platform=iOS' -configuration Release -archivePath build/ScratchLab-TestFlight-v10.xcarchive archive` passed with `ScratchLab.app/Watch/ScratchLabWatch.app` embedded and `CFBundleVersion = 10`; `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' -configuration Release -archivePath build/ScratchLabDesktop-TestFlight-v10.xcarchive archive` passed; `xcodebuild -exportArchive` with `build/release-exports/ExportOptions-iOS-upload.plist` uploaded `ScratchLab` build `10` and reported `Uploaded ScratchLab`; `xcodebuild -exportArchive` with `build/release-exports/ExportOptions-mac-upload.plist` uploaded `ScratchLabDesktop` build `10` and reported `Uploaded ScratchLabDesktop`
- Follow-up notes: there were no unchecked backlog items left, so this user-requested release polish became the next safe task; restored the watch embed and dependency for the iPhone target, normalized built-in mic naming to `Microphone`, replaced internal-looking MVP/test/tester copy with release-ready language across iPhone, Mac, and Watch, surfaced the live app version from the bundle instead of a hard-coded `1.0.0`, and confirmed both uploaded packages reached App Store Connect processing on 2026-04-18

- Selected task: normalize the built-in microphone label and keep watchOS out of the iPhone upload bundle
- Files changed: `TASKS.md`, `DEV_LOG.md`, `ScratchLab/Audio/AudioEngine.swift`, `ScratchLab/Services/CompanionCameraBroadcaster.swift`, `ScratchLab.xcodeproj/project.pbxproj`
- Build result: `./scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`; `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLab -destination 'generic/platform=iOS' -configuration Release -archivePath build/ScratchLab-watchless-check-20260418.xcarchive archive` also passed, and the archived iOS app bundle contains no embedded `Watch/` directory or watch app
- Follow-up notes: there were no unchecked backlog items left, so this user-requested release polish became the next safe task; normalized built-in mic route names to `Microphone` in both the practice-audio status path and the companion-camera audio picker/status path, then removed the `ScratchLab` target’s watch embed phase and watch target dependency so App Store uploads stay iOS + macOS while the standalone `ScratchLabWatch` target still builds locally

- Selected task: put the ScratchLab logo back into the visible app launch flow
- Files changed: `TASKS.md`, `DEV_LOG.md`, `ScratchLab/Info.plist`, `ScratchLab/ScratchLabApp.swift`, `ScratchLab/Assets.xcassets/ScratchLabLogo.imageset/`
- Build result: `./scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`; the iOS app Info.plist still ships `CFBundleIconName = AppIcon` and now declares `UILaunchScreen > UIImageName = ScratchLabLogo`
- Follow-up notes: there were no unchecked backlog items left, so this user-requested launch polish became the next safe task; promoted the existing 1024px app icon art into a normal runtime image asset so the launch path can display it directly, wired that logo into the static iOS launch screen, and updated the custom splash animation to show the logo above the ScratchLab wordmark instead of a text-only boot view

- Selected task: upload fresh iOS and macOS build 9 binaries to the existing ScratchLab App Store Connect app without deleting the app record
- Files changed: `TASKS.md`, `DEV_LOG.md`, `ScratchLab.xcodeproj/project.pbxproj`
- Build result: bumped the shared `CURRENT_PROJECT_VERSION` from `8` to `9`; `./scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`; archived `build/ScratchLab-TestFlight-v9.xcarchive` and `build/ScratchLabDesktop-TestFlight-v9.xcarchive`; `xcodebuild -exportArchive` with `build/release-exports/ExportOptions-iOS-upload.plist` uploaded `ScratchLab` build `9` and reported `Uploaded ScratchLab`; `xcodebuild -exportArchive` with `build/release-exports/ExportOptions-mac-upload.plist` uploaded `ScratchLabDesktop` build `9` and reported `Uploaded ScratchLabDesktop`
- Follow-up notes: after confirming Apple’s removal rules would make deleting the existing app record risky for the shared bundle ID, reused the existing Xcode-managed upload flow on this machine instead; both uploads reached the `Uploaded package is processing` state and then completed successfully, so the next App Store Connect step is version metadata / review prep rather than another binary delivery

- Selected task: remove the old concatenated macOS target name so the repo can be uploaded without a lingering separate desktop identity
- Files changed: `TASKS.md`, `DEV_LOG.md`, `AI_CONTEXT.md`, `AGENTS.md`, `README.md`, `docs/brand_kit.md`, `scripts/build.sh`, `ScratchLab.xcodeproj/project.pbxproj`, `ScratchLab.xcodeproj/xcshareddata/xcschemes/ScratchLabDesktop.xcscheme`, `ScratchLabDesktop/`, `build/`
- Build result: `./scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`; a repo-wide search across tracked source files found no remaining use of the older concatenated macOS token after the rename
- Follow-up notes: there were no unchecked backlog items left, so this user-requested repo-cleanup pass became the next safe task; renamed the macOS source folder, app entry file, entitlements file, and shared scheme to `ScratchLabDesktop`, updated the Xcode target wiring plus internal Swift identifiers and scripts/docs to match, removed stale untracked build archives and packaging output that still carried the old desktop token, and kept the shipped macOS product identity at `ScratchLab` with bundle identifier `com.machelpnz.scratchlab` so the desktop archive can continue attaching to the existing App Store Connect app record

- Selected task: restore the macOS target and align ScratchLab identity so the iOS and macOS builds can live under one App Store Connect app record
- Files changed: `TASKS.md`, `DEV_LOG.md`, `ScratchLab.xcodeproj/project.pbxproj`, `ScratchLab.xcodeproj/xcshareddata/xcschemes/ScratchLabDesktop.xcscheme`
- Build result: `./scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`; the built macOS app bundle at `~/Library/Developer/Xcode/DerivedData/Build/Products/Debug/ScratchLab.app` now reports `CFBundleDisplayName = ScratchLab`, `CFBundleName = ScratchLab`, and `CFBundleIdentifier = com.machelpnz.scratchlab`
- Follow-up notes: there were no unchecked repo backlog items left, so this user-requested App Store identity fix became the next task; restored real `ScratchLabDesktop` and `ScratchLabWatch` targets to the Xcode project, regenerated the shared Mac scheme, re-embedded the watch app into the iOS build, added the shared `CaptureCore` model plus bundled `cxl_scratch_library` to the Mac target, and aligned the macOS target to the shared `ScratchLab` product identity with bundle ID `com.machelpnz.scratchlab`; the remaining App Store Connect step is outside the repo: add macOS as a platform on the existing `ScratchLab` iOS app record and upload the next macOS archive against that shared bundle ID instead of the older separate `ScratchLabDesktop` listing

## 2026-04-17
- Selected task: push the current ScratchLab iPhone + Watch build and ScratchLab Mac build to App Store Connect for first testing after the approval-safe macOS naming fix
- Files changed: `DEV_LOG.md`, `ScratchLab.xcodeproj/project.pbxproj`
- Build result: `./scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`; archived `build/ScratchLab-TestFlight-v8.xcarchive` and `build/ScratchLabDesktop-TestFlight-v8.xcarchive`; `xcodebuild -exportArchive` with `build/release-exports/ExportOptions-iOS-upload.plist` uploaded `ScratchLab` build `8` and reported `Uploaded ScratchLab`; `xcodebuild -exportArchive` with `build/release-exports/ExportOptions-mac-upload.plist` uploaded `ScratchLabDesktop` build `8` and reported `Uploaded ScratchLabDesktop`; both packages entered App Store Connect processing
- Follow-up notes: bumped the shared `CURRENT_PROJECT_VERSION` from `7` to `8` so App Store Connect would accept a fresh upload, reused the existing signed Xcode upload flow already configured on this machine, and kept the shipped macOS product/display name aligned to `ScratchLab`; this pass uploaded binaries only, so App Store Connect listing metadata still needs to be entered or updated separately in ASC

- Selected task: update the macOS App Store Connect metadata to approval-safe `ScratchLab` branding and submit the macOS app for review
- Files changed: `DEV_LOG.md`
- Build result: not rerun because no project files changed after the successful build-and-upload pass for build `8`; App Store Connect reads confirmed the live macOS listing still uses `ScratchLabDesktop` as the app info name, has no App Store version text, no attached build on version `1.0`, no review detail, and no screenshot sets; attempted ASC metadata writes through both the direct App Store Connect API and `xcrun altool --app-store-text --upload`, but both were rejected with `403 FORBIDDEN_ERROR` because the API key configured on this machine can upload binaries yet does not allow metadata edits
- Follow-up notes: prepared approval-safe macOS metadata with app name `ScratchLab`, subtitle `DJ Session Capture & Logs`, compliant promotional text/description/keywords, and copyright `2026 Karl Watson`, but ASC would not accept any write operation from the available auth; build `8` is `VALID`, but the macOS build record still reports `MISSING_EXPORT_COMPLIANCE`, and the version remains unready for submission until a metadata-capable ASC login updates the listing, attaches the build, fills any remaining required review fields, and uploads at least one macOS screenshot set

- Selected task: continue the macOS App Store Connect submission prep through the logged-in web session and clear as many review blockers as possible
- Files changed: `DEV_LOG.md`
- Build result: not rerun because no local project files changed; using the live App Store Connect web session, set the macOS primary category to `MUSIC`, completed the age rating declaration, attached macOS build `8` to version `1.0`, uploaded one 2880x1800 macOS screenshot, created App Review contact details with the account holder info already on this machine, set Content Rights to `DOES_NOT_USE_THIRD_PARTY_CONTENT`, and created a free price tier for the macOS app; App Store Connect now reports only App Privacy and support-link blockers for review readiness
- Follow-up notes: the exact app name `ScratchLab` could not be applied to the separate macOS app record because App Store Connect returned a duplicate-name conflict with the existing `ScratchLab` iOS app in the same account; after the web-session fixes, the remaining submission blockers are a required public `Support URL`, a required `Privacy Policy URL`, the App Privacy questionnaire itself, and the unresolved naming conflict if this separate macOS app must ship under the exact title `ScratchLab`

- Selected task: reject raw-path boundary escapes in kept take logs during create-session reruns
- Files changed: `TASKS.md`, `DEV_LOG.md`, `scripts/create_session.py`, `scripts/test_capture_pipeline.py`
- Build result: `./scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: tightened the create-session keep-existing path so a reused `take_log.csv` now applies the same session-local raw-path boundary rule as the later pipeline stages, then added fixture coverage for both an absolute raw-audio path and a `../outside.wav` escape so reruns fail closed before a kept take log can point outside the session `raw/` folder

- Selected task: reject invalid source-file extensions in kept take logs during create-session reruns
- Files changed: `TASKS.md`, `DEV_LOG.md`, `scripts/create_session.py`, `scripts/test_capture_pipeline.py`
- Build result: `./scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: tightened the create-session keep-existing path so a reused `take_log.csv` now validates the extension of every non-empty raw source entry against the same source-type rules used later in the pipeline, then added fixture coverage for a `raw_audio=audio.mp3` row so reruns fail closed before bad source-file types survive into rename

- Selected task: reject duplicate take identities in kept take logs during create-session reruns
- Files changed: `TASKS.md`, `DEV_LOG.md`, `scripts/create_session.py`, `scripts/test_capture_pipeline.py`
- Build result: `./scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: tightened the create-session keep-existing path so a reused `take_log.csv` now fails if two rows claim the same BPM/take-number identity, then added fixture coverage for a duplicated `70 BPM take 01` pair so reruns reject duplicate take keys before the rename and validation stages have to discover them later

- Selected task: validate kept take-log rows semantically during create-session reruns
- Files changed: `TASKS.md`, `DEV_LOG.md`, `scripts/create_session.py`, `scripts/test_capture_pipeline.py`
- Build result: `./scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: tightened the create-session keep-existing path so a reused `take_log.csv` now has to pass the same BPM, take-number, and required-boolean parsing rules as later pipeline steps instead of only clearing CSV-structure checks; added fixture coverage for an invalid BPM row, an invalid take number row, and a blank `verbal_slate_used` field so reruns now fail closed before those bad rows can survive into rename or validation

- Selected task: reject semantically empty kept take logs during create-session reruns
- Files changed: `TASKS.md`, `DEV_LOG.md`, `scripts/create_session.py`, `scripts/test_capture_pipeline.py`
- Build result: `./scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: tightened `create_session.py` so reruns still accept the untouched header-only take-log template but now reject a kept `take_log.csv` that parses to zero take rows while differing from the canonical empty template, then added a regression test covering a header plus blank-row case so the scaffold step no longer treats whitespace-only operator state as a valid reusable take log

- Selected task: add a fail-closed regression test for non-object kept manifests during create-session reruns
- Files changed: `TASKS.md`, `DEV_LOG.md`, `scripts/test_capture_pipeline.py`
- Build result: `./scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: added a non-object-manifest rerun test that creates a clean session scaffold, overwrites `session_manifest.json` with syntactically valid JSON whose top-level value is a list, reruns `create_session.py`, and asserts the command fails with the existing manifest-shape error while leaving both the manifest bytes and take-log bytes untouched, so the keep-existing path is now covered for both unreadable manifest JSON and structurally wrong manifest payloads

- Selected task: add a fail-closed regression test for invalid kept take logs during create-session reruns
- Files changed: `TASKS.md`, `DEV_LOG.md`, `scripts/test_capture_pipeline.py`
- Build result: `./scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: added an invalid-take-log rerun test that creates a clean session scaffold, overwrites `take_log.csv` with a header missing the required columns, reruns `create_session.py`, and asserts the command fails with the existing take-log validation error while leaving both the manifest bytes and the corrupt take-log bytes untouched, so the keep-existing path now has explicit fail-closed coverage for broken take-log state as well as broken manifests

- Selected task: add a fail-closed regression test for corrupt kept manifests during create-session reruns
- Files changed: `TASKS.md`, `DEV_LOG.md`, `scripts/test_capture_pipeline.py`
- Build result: `./scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: added a corrupt-manifest rerun test that creates a clean session scaffold, overwrites `session_manifest.json` with invalid JSON, reruns `create_session.py`, and asserts the command fails with the existing manifest-read error while leaving both the corrupt manifest bytes and the take-log bytes untouched, so the keep-existing path now has explicit fail-closed coverage for broken scaffold state

- Selected task: add a rerun-idempotence regression test for create-session scaffolding
- Files changed: `TASKS.md`, `DEV_LOG.md`, `scripts/test_capture_pipeline.py`
- Build result: `./scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: added a rerun regression test that creates a fresh session scaffold, snapshots the generated manifest and take log as raw bytes, reruns `create_session.py` with the same DJ/date inputs, and asserts both files remain byte-for-byte identical while the script still reports them as kept, so the happy-path rerun behavior is now proven idempotent instead of only inferred from console output

- Selected task: add an end-to-end regression test for create-session take-log scaffolding
- Files changed: `TASKS.md`, `DEV_LOG.md`, `scripts/test_capture_pipeline.py`
- Build result: `./scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: added a create-session regression test that runs `create_session.py`, reads the emitted `manifests/take_log.csv`, and asserts it matches `templates/take_log_template.csv` byte-for-byte, so the generated operator take log is now pinned to the checked-in template instead of only being assumed to copy cleanly

- Selected task: add an end-to-end regression test for create-session manifest scaffolding
- Files changed: `TASKS.md`, `DEV_LOG.md`, `scripts/test_capture_pipeline.py`
- Build result: `./scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: added a create-session regression test that runs `create_session.py`, reads the emitted `session_manifest.json`, and asserts it matches `default_manifest(...)` with the requested DJ name, DJ token, date, and session directory filled in, so the generated scaffold is now pinned to the same live manifest constructor as the template and production code path

- Selected task: add a regression test that keeps the session manifest scaffold template aligned with the live default manifest
- Files changed: `TASKS.md`, `DEV_LOG.md`, `scripts/test_capture_pipeline.py`
- Build result: `./scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: added a scaffold-template regression test that reads `templates/session_manifest_template.json` and asserts it still matches `default_manifest()` exactly, which pins the checked-in session manifest starter file to the same payload `create_session.py` uses for new sessions instead of relying on manual template upkeep

- Selected task: add a regression test that keeps the take-log template aligned with the live parser columns
- Files changed: `TASKS.md`, `DEV_LOG.md`, `scripts/test_capture_pipeline.py`
- Build result: `./scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: added a template-header regression test that reads `templates/take_log_template.csv`, verifies it still contains exactly one CSV row, and asserts that row matches the live `TAKE_LOG_COLUMNS` list from `capture_pipeline_common`, so the operator-facing take-log template cannot silently drift away from the parser contract or grow stale sample rows

- Selected task: add a regression test that keeps the example manifest template aligned with the current take schema
- Files changed: `TASKS.md`, `DEV_LOG.md`, `scripts/test_capture_pipeline.py`
- Build result: `./scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: added a template-shape regression test that loads `templates/session_manifest_example.json`, checks that the example session still advertises the current scratch/session constants, verifies that each take's `camera_id`, `audio_source`, and `watch_source` imply the exact `files` and `artifacts` source keys present, and confirms every artifact path still matches its corresponding file entry so the example manifest cannot quietly drift out of sync again

- Selected task: update the example session manifest template to the current take schema
- Files changed: `TASKS.md`, `DEV_LOG.md`, `templates/session_manifest_example.json`
- Build result: `./scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: refreshed the example manifest template so every sample take now includes both the canonical `files` map and the source-keyed `artifacts` object, which brings the template back into line with the current schema and validation rules instead of teaching an obsolete half-updated take record

- Selected task: refuse to keep mismatched session scaffolding during create-session reruns
- Files changed: `TASKS.md`, `DEV_LOG.md`, `README.md`, `docs/pilot_session_runbook.md`, `scripts/create_session.py`, `scripts/test_capture_pipeline.py`
- Build result: `./scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: hardened `create_session.py` so reruns now validate the existing manifest against the requested DJ name, DJ token, date, and session root before keeping it, and refuse to keep a take log when that matching manifest is missing; added regression coverage for both a mismatched manifest rerun and an orphaned take log left behind without its manifest so the scaffold step can no longer silently adopt the wrong session state

- Selected task: restrict take-log raw source paths to the session raw folder
- Files changed: `TASKS.md`, `DEV_LOG.md`, `README.md`, `docs/naming_convention.md`, `docs/pilot_session_runbook.md`, `scripts/capture_pipeline_common.py`, `scripts/rename_files.py`, `scripts/test_capture_pipeline.py`, `scripts/validate_session.py`
- Build result: `./scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: hardened the capture pipeline so every `raw_*` value must stay relative to the current session's `raw/` folder instead of allowing absolute paths or `..` escapes, then wired both `rename_files.py` and `validate_session.py` to report those violations as take-log errors instead of walking outside the session boundary; added regression coverage for an absolute-path rename failure and a parent-directory escape caught during validation

- Selected task: validate renamed watch CSV files against the real exported motion schema
- Files changed: `TASKS.md`, `DEV_LOG.md`, `README.md`, `docs/metadata_schema.md`, `scripts/capture_pipeline_common.py`, `scripts/fixtures/capture_pipeline/watch_stub.csv`, `scripts/test_capture_pipeline.py`, `scripts/validate_session.py`
- Build result: `./scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: tightened watch CSV probing so a watch artifact now has to use the real exported 18-column motion header and carry at least 10 non-empty sample rows before it is treated as a valid watch capture; updated the fixture stub to match the actual export shape, added regression coverage for malformed-header and too-few-samples watch files, and made validation report probe failures cleanly instead of crashing when a watch CSV is malformed on disk

- Selected task: enforce primary capture duration sanity during session validation
- Files changed: `TASKS.md`, `DEV_LOG.md`, `README.md`, `docs/metadata_schema.md`, `scripts/test_capture_pipeline.py`, `scripts/validate_session.py`
- Build result: `./scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: added validation rules for minimum primary-media duration and `camA`/`serato` duration agreement so obviously truncated or mismatched capture pairs fail before a session is treated as usable; raised the fixture WAV baseline to a real 1-second duration so the happy path reflects the new rule, and added targeted regression coverage for both a too-short `serato` file and a one-second audio/video drift case

- Selected task: require manifest file sets to match the renamed take sources and paths exactly
- Files changed: `TASKS.md`, `DEV_LOG.md`, `docs/metadata_schema.md`, `scripts/test_capture_pipeline.py`, `scripts/validate_session.py`
- Build result: `./scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: tightened manifest validation so each take's `files` object must be present, must contain exactly the same source keys as the renamed files on disk for that take, and must point at the canonical relative paths for those sources; added regression coverage for both a missing `serato` file entry and a stale `serato` path copied from a different take number, and updated the metadata schema so `files` is now documented as a required part of each take record instead of only appearing in examples

- Selected task: require manifest artifact sets to match the renamed take sources exactly
- Files changed: `TASKS.md`, `DEV_LOG.md`, `docs/metadata_schema.md`, `scripts/test_capture_pipeline.py`, `scripts/validate_session.py`
- Build result: `./scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: tightened manifest validation so each take's `artifacts` object must contain exactly the same source keys as the renamed files found on disk for that take, which closes the gap where a manifest could omit an artifact entry and still pass; added a regression test that removes the `serato` artifact entry from a valid manifest and verifies `validate_session.py` now fails with a clear take-specific error

- Selected task: roll back copied renamed files when a rename run fails
- Files changed: `TASKS.md`, `DEV_LOG.md`, `docs/naming_convention.md`, `docs/pilot_session_runbook.md`, `scripts/fixtures/capture_pipeline/partial_failure_take_log.csv`, `scripts/rename_files.py`, `scripts/test_capture_pipeline.py`
- Build result: `./scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: strengthened the capture-pipeline rename pass so it now preflights required `camA` and `serato` sources before building a take record, cleans up partial target files when a low-level copy fails, and removes every renamed file copied during the current run whenever any take row errors out; added a fixture where a valid first row is followed by a bad second row so the test proves the script leaves both the manifest and the canonical media folders untouched after failure

- Selected task: persist app-local recording sidecars before capture starts
- Files changed: `TASKS.md`, `DEV_LOG.md`, `docs/capture_spec_v1.md`, `ScratchLab/Services/CompanionCameraBroadcaster.swift`, `ScratchLabDesktop/Services/MacCaptureEngine.swift`
- Build result: `./scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`; the first sandboxed build attempt failed before compilation because `xcodebuild` could not write DerivedData under `~/Library`, then the same script was rerun outside the sandbox and completed successfully
- Follow-up notes: both the iPhone companion recorder and the Mac routine recorder now atomically write their shared in-progress sidecar JSON to disk before `AVCaptureMovieFileOutput.startRecording(...)` begins, so an interrupted or crashed recording keeps a durable same-basename `.json` trail instead of leaving an orphan `.mov` with only in-memory metadata; final completion/failure still updates that same sidecar at the end of capture

- Selected task: patch the macOS app identity so the installed/displayed app name is `ScratchLab` and the remaining in-app `ScratchLab Mac` branding becomes descriptive-only copy
- Files changed: `DEV_LOG.md`, `ScratchLab.xcodeproj/project.pbxproj`, `ScratchLab.xcodeproj/xcshareddata/xcschemes/ScratchLabDesktop.xcscheme`, `ScratchLab/Info.plist`, `ScratchLab/Services/CompanionCameraBroadcaster.swift`, `ScratchLab/Views/MainMenuView.swift`, `ScratchLabDesktop/Info.plist`, `ScratchLabDesktop/Services/CompanionCameraReceiver.swift`, `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`
- Build result: `./scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`; the first sandboxed build attempt failed before compilation because `xcodebuild` could not write DerivedData under `~/Library`, then the same script was rerun outside the sandbox and completed successfully; the resulting macOS build product is `~/Library/Developer/Xcode/DerivedData/Build/Products/Debug/ScratchLab.app`, and its built `Info.plist` reports both `CFBundleDisplayName` and `CFBundleName` as `ScratchLab`
- Follow-up notes: changed the macOS target `PRODUCT_NAME` and scheme/buildable product to `ScratchLab.app` without touching the existing bundle identifiers or target names, updated macOS permission strings and companion-facing iPhone/iPad copy so platform references read descriptively instead of as product identity, renamed the visible routine-recordings folder label to `ScratchLab Routines`, and intentionally did not upload a replacement build yet because this pass was limited to the binary/name fix plus local validation

## 2026-04-16
- Selected task: push a fresh TestFlight build after the watch import and export verification pass
- Files changed: `TASKS.md`, `DEV_LOG.md`, `ScratchLab.xcodeproj/project.pbxproj`
- Build result: `./scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`; archived `build/ScratchLab-TestFlight-v7.xcarchive` and `build/ScratchLabDesktop-TestFlight-v7.xcarchive`; `xcodebuild -exportArchive` with `build/release-exports/ExportOptions-iOS-upload.plist` uploaded `ScratchLab` build `7` and reported `Uploaded ScratchLab`; `xcodebuild -exportArchive` with `build/release-exports/ExportOptions-mac-upload.plist` uploaded `ScratchLabDesktop` build `7` and reported `Uploaded ScratchLabDesktop`; both packages entered App Store Connect processing
- Follow-up notes: build `6` had already been uploaded, so this release pass only changed the shared `CURRENT_PROJECT_VERSION` to `7` before rebuilding and reusing the existing signed App Store Connect upload flow; the local machine can archive and upload successfully through Xcode, but it does not have an App Store Connect metadata automation path configured here: `altool` requires explicit JWT or app-password credentials, Transporter is not installed, no App Store Connect API key was found in the local keychain search, and review-only fields such as screenshots, App Privacy answers, export-compliance questionnaire, review contact, demo account details, and submission notes still need to be completed in App Store Connect by a human or a separately configured metadata tool

- Selected task: import pending watch captures even when the Watch Capture screen is closed
- Files changed: `TASKS.md`, `DEV_LOG.md`, `ScratchLab/ScratchLabApp.swift`, `ScratchLab/Services/WatchMotionCaptureStore.swift`
- Build result: `./scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`; `./scripts/run-on-phone.sh 00008140-000C21A13A9B001C` installed and launched the updated iPhone app on the physical iPhone 16 Pro Max; after launch the app swept the queued WatchConnectivity inbox files into durable storage, and `xcrun devicectl device info files ... --subdirectory 'Library/Application Support/WatchMotionCaptures'` showed three imported JSON sessions including the fresh `scratch-motion-2026-04-15T18-48-24Z-29CA0B.json` capture from the live watch smoke test
- Follow-up notes: the import break was not in watch transport or file decoding; the JSON files were already arriving in `Documents/Inbox/com.apple.watchconnectivity`, but the phone only activated `WCSession` when the user opened `WatchCaptureHubView`; moving activation to the app lifecycle and sweeping pending inbox files on foreground fixed the gap, and the inbox is now reduced to empty metadata directories rather than stranded JSON payloads

- Selected task: fix watch provisioning/signing so build 6 can install on an actual Watch
- Files changed: `TASKS.md`, `DEV_LOG.md`, `README.md`, `scripts/run-on-watch.sh`
- Build result: `./scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`; installed the missing watchOS 26.4 runtime with `xcodebuild -downloadPlatform watchOS`; `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabWatch -destination 'id=00008310-001B32C8343A601E' -configuration Debug -allowProvisioningUpdates -allowProvisioningDeviceRegistration build` succeeded and generated a specific provisioning profile for `com.machelpnz.scratchlab.watchkitapp`; `./scripts/run-on-watch.sh` then completed a real-device install of build `6` on the physical Apple Watch after retrying two transient CoreDevice tunnel/installcoordination failures
- Follow-up notes: the original install blocker was environment-side rather than a bad bundle identifier in the repo; Xcode had the watchOS SDK but not the watchOS runtime needed for real-watch destinations, so generic watch builds were embedding an unusable wildcard profile and direct installs failed with `0xe8008012`; the new `scripts/run-on-watch.sh` helper now drives the real device destination so Xcode refreshes the specific watch app provisioning profile before installing, retries transient `devicectl` watch-install transport failures, and the README now calls out the runtime prerequisite explicitly

- Selected task: move local recording sidecar creation into shared Capture Core helpers
- Files changed: `TASKS.md`, `DEV_LOG.md`, `docs/capture_spec_v1.md`, `ScratchLab/Models/CaptureCore.swift`, `ScratchLab/Services/CompanionCameraBroadcaster.swift`, `ScratchLabDesktop/Services/MacCaptureEngine.swift`
- Build result: `./scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: seeding the next Capture Core consolidation slice so the shared model owns the start-of-recording sidecar payload as well as naming, file pairing, and completion-state mutation; this should remove another place where the iPhone and Mac recorders can drift on `takeID`, file-name pairing, or the in-progress sidecar state

- Selected task: move local recording sidecar finalization into shared Capture Core helpers
- Files changed: `TASKS.md`, `DEV_LOG.md`, `docs/capture_spec_v1.md`, `ScratchLab/Models/CaptureCore.swift`, `ScratchLab/Services/CompanionCameraBroadcaster.swift`, `ScratchLabDesktop/Services/MacCaptureEngine.swift`
- Build result: `./scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: extended Capture Core so the shared local-recording model now owns the standard “recording finished or failed” sidecar mutation path as well as the fallback same-basename sidecar URL derivation; both recorders now finalize the sidecar through that shared helper instead of patching `endedAt`, `recordingStatus`, and `errorDescription` inline

- Selected task: move local recording file-pair creation into shared Capture Core helpers
- Files changed: `TASKS.md`, `DEV_LOG.md`, `docs/capture_spec_v1.md`, `ScratchLab/Models/CaptureCore.swift`, `ScratchLab/Services/CompanionCameraBroadcaster.swift`, `ScratchLabDesktop/Services/MacCaptureEngine.swift`
- Build result: `./scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: extended Capture Core again so it now owns same-basename `.mov` plus `.json` file-pair creation and fail-closed target-exists preflight for app-local recordings; both recorders now ask the shared model for their staging URLs instead of reconstructing those paths and file-exists checks separately

- Selected task: move app-local recording naming into shared Capture Core helpers
- Files changed: `TASKS.md`, `DEV_LOG.md`, `docs/capture_spec_v1.md`, `ScratchLab/Models/CaptureCore.swift`, `ScratchLab/Services/CompanionCameraBroadcaster.swift`, `ScratchLabDesktop/Services/MacCaptureEngine.swift`
- Build result: `./scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: extended Capture Core beyond the shared sidecar payload so it now owns deterministic app-local session IDs, padded take numbers, take IDs, and next-take allocation logic for local `.mov` staging captures; both the iPhone companion recorder and the Mac routine recorder now call the same naming helpers instead of carrying duplicate date formatting and directory scan code

- Selected task: seed Capture Core with a shared local-recording sidecar schema
- Files changed: `TASKS.md`, `DEV_LOG.md`, `docs/capture_spec_v1.md`, `ScratchLab/Models/CaptureCore.swift`, `ScratchLab/Services/CompanionCameraBroadcaster.swift`, `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `ScratchLab.xcodeproj/project.pbxproj`
- Build result: `./scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: introduced a shared `CaptureCore.LocalRecordingSidecar` model so the iPhone companion recorder and the Mac routine recorder now emit the same sidecar schema from one Swift source of truth instead of keeping duplicate inline structs; the capture spec now explicitly says both apps use `scratchlab_local_recording_sidecar_v1`, and any future ingest or validation work should extend this shared model instead of adding app-specific schema drift

- Selected task: remove the stray `k` target and tighten the top-level repo scope docs
- Files changed: `TASKS.md`, `DEV_LOG.md`, `AI_CONTEXT.md`, `README.md`, `ScratchLab.xcodeproj/project.pbxproj`, `k/BackgroundDownloadHandler.swift`, `k/Info.plist`, `k/k.entitlements`
- Build result: `./scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: removed the orphaned `k` ExtensionKit target from the Xcode project along with its unused source folder, verified `xcodebuild -list -project ScratchLab.xcodeproj` now reports only `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`, and updated the repo-level docs so they describe the real scope: three Apple-platform app targets, the capture-pipeline tooling, and the separate `nz-tax-workflow/` utility project; `README.md` now also notes that the default build runs capture-pipeline fixture tests before the platform builds

- Selected task: add capture-pipeline fixture tests and run them from the default build script
- Files changed: `TASKS.md`, `DEV_LOG.md`, `scripts/build.sh`, `scripts/test_capture_pipeline.py`, `scripts/fixtures/capture_pipeline/happy_path_take_log.csv`, `scripts/fixtures/capture_pipeline/blank_boolean_take_log.csv`, `scripts/fixtures/capture_pipeline/watch_stub.csv`, `scripts/fixtures/capture_pipeline/camA_stub.mov`, `scripts/fixtures/capture_pipeline/ffprobe_stub.py`
- Build result: `./scripts/build.sh` passed for `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`, and now also runs the capture-pipeline fixture suite first
- Follow-up notes: added a small `unittest` harness that drives the real `create_session.py`, `rename_files.py`, and `validate_session.py` CLIs in temp session folders using stable fixture take logs plus a fake `ffprobe` on `PATH`, so the capture pipeline now has repeatable coverage for session scaffolding, happy-path rename/validate, blank-boolean rename failure, and missing-manifest validation failure; `scripts/build.sh` now runs that fixture suite before every platform build so pipeline regressions fail fast during the normal repo validation path

- Selected task: tie app-captured recordings to explicit session and take sidecars
- Files changed: `TASKS.md`, `DEV_LOG.md`, `ScratchLab/Services/CompanionCameraBroadcaster.swift`, `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `docs/capture_spec_v1.md`
- Build result: `./scripts/build.sh` passed for `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: replaced timestamp-only local recorder filenames on iPhone and Mac with deterministic app-local `sessionID` plus incrementing `takeNNN` basenames, and both recorders now emit a same-basename JSON sidecar capturing `sessionID`, `takeID`, `appLocalTakeNumber`, device/source details, start/end times, and final recording status; the capture spec now documents that these app-local sidecars must stay paired with the raw `.mov` files, while canonical BPM take numbering still comes later from `take_log.csv` during import and rename

- Selected task: require explicit take-log booleans for slate and sync clap fields
- Files changed: `TASKS.md`, `DEV_LOG.md`, `docs/metadata_schema.md`, `docs/pilot_session_runbook.md`, `scripts/capture_pipeline_common.py`, `scripts/rename_files.py`, `scripts/validate_session.py`
- Build result: `./scripts/build.sh` passed for `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: changed the shared boolean parser so blank slate or sync-clap cells now fail closed with field-specific errors instead of silently defaulting to `true`, updated both `rename_files.py` and `validate_session.py` to use that stricter path, and tightened the operator docs so `take_log.csv` explicitly requires `true` or `false` in both columns; a temp-session smoke check confirmed that rows with blank `verbal_slate_used` or `sync_clap_used` now produce `is required and cannot be blank` errors during both rename and validation

- Selected task: timestamp watch motion capture from Core Motion timing and record jitter metadata
- Files changed: `TASKS.md`, `DEV_LOG.md`, `ScratchLab/Models/WatchMotionCapture.swift`, `ScratchLabWatch/Services/WatchMotionRecorder.swift`, `ScratchLab/Services/WatchMotionCaptureStore.swift`
- Build result: `./scripts/build.sh` passed for `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: switched watch sample timing from `Date()` deltas to Core Motion's monotonic `timestamp`, added per-session timing metadata summarizing requested interval, observed interval spread, max jitter, non-positive intervals, estimated dropped samples, and wall-clock versus sensor-duration drift, and extended CSV export with the raw Core Motion timestamp column so timing analysis can be audited after import; existing JSON sessions remain decodable because the new timing fields are optional, and the next useful validation is a real watch capture to inspect the recorded jitter stats under actual wrist-motion load

- Selected task: fix iPhone audio analysis to use the real input sample rate and channel layout
- Files changed: `TASKS.md`, `DEV_LOG.md`, `ScratchLab/Audio/AudioEngine.swift`
- Build result: `./scripts/build.sh` passed for `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: changed the iPhone audio analyzer to derive spectral centroid and dominant-frequency bins from the live input format instead of a hard-coded 44.1 kHz assumption, added PCM extraction that safely downmixes interleaved or multi-buffer multi-channel input to mono before feature analysis, and now resets the rolling analysis buffer plus matcher hop timing if the active input route changes sample rate mid-session; validation for this task is build-only so the next useful check is a real device pass with a non-44.1 kHz or multi-channel input route

- Selected task: fail rename when an expected target file already exists
- Files changed: `TASKS.md`, `DEV_LOG.md`, `docs/naming_convention.md`, `docs/pilot_session_runbook.md`, `scripts/rename_files.py`
- Build result: `./scripts/build.sh` passed for `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: changed `rename_files.py` to preflight every requested target path for a take before copying anything, so an existing canonical target now produces an explicit conflict instead of being reused silently; the naming convention and pilot runbook now describe that fail-closed behavior, and a temp-session smoke check confirmed that a second rename pass against the same take exited with code 1, reported `renamed target already exists` for both `camA` and `serato`, and left `session_manifest.json` unchanged

- Selected task: record probed media metadata in the capture manifest
- Files changed: `TASKS.md`, `DEV_LOG.md`, `docs/metadata_schema.md`, `scripts/capture_pipeline_common.py`, `scripts/validate_session.py`
- Build result: `./scripts/build.sh` passed for `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: extended the per-file artifact records with source-specific probe data so `camA` and `camB` now carry ffprobe-derived video facts, `serato` uses WAV metadata from Python's `wave` module, and watch CSV artifacts record row and column counts; validation now recomputes and compares those probe facts against the files on disk, and a temp-session smoke check confirmed that a real MOV/WAV/CSV session renamed and validated cleanly before a tampered watch CSV failed with an `artifact probe metadata` mismatch in `validation_report.txt`

- Selected task: record artifact hashes and file sizes in the capture manifest
- Files changed: `TASKS.md`, `DEV_LOG.md`, `docs/metadata_schema.md`, `scripts/capture_pipeline_common.py`, `scripts/validate_session.py`
- Build result: `./scripts/build.sh` passed for `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: split the broader artifact-metadata task into a hash/byte-size slice and a later media-probing slice, added per-file `artifacts` records to each take with relative path, byte count, and SHA-256, and taught validation to recompute and compare those facts against the files on disk; a temp-session smoke check confirmed that a three-BPM synthetic session renamed, validated, and wrote the new artifact fields into `session_manifest.json`

- Selected task: require the session manifest during validation
- Files changed: `TASKS.md`, `DEV_LOG.md`, `README.md`, `docs/pilot_session_runbook.md`, `scripts/validate_session.py`
- Build result: `./scripts/build.sh` passed for `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: changed `validate_session.py` so a missing `manifests/session_manifest.json` is now a hard failure instead of a warning, updated the README and pilot runbook so the operator docs reflect that stricter rule, and confirmed the behavior with a temp-session smoke check that removed the manifest and verified validation exited with code 1 and wrote the missing-manifest failure into `validation_report.txt`

- Selected task: make capture-pipeline rename fail closed and keep manifest writes atomic
- Files changed: `TASKS.md`, `DEV_LOG.md`, `scripts/capture_pipeline_common.py`, `scripts/rename_files.py`
- Build result: `./scripts/build.sh` passed for `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: added an audit-fix queue to `TASKS.md`, switched shared manifest writes over to an atomic temp-file replace, and changed `rename_files.py` so a run with any row-level errors now leaves the existing manifest untouched and only emits BPM summary files after a clean pass; a temp-session smoke check confirmed that a failed rename returned exit code 1, kept `session_manifest.json` byte-for-byte unchanged, and did not emit a `70bpm/take01.txt` summary file

- Selected task: replace the performer-monitor discovery path with a Bonjour TCP feed that the iPad can reliably see
- Files changed: `TASKS.md`, `DEV_LOG.md`
- Build result: `./scripts/build.sh` passed for `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: rechecked the implementation during the audit and confirmed the task was already satisfied in code before this entry; `ScratchLabDesktop` already advertises the performer monitor over Bonjour TCP with manual-connect details, and the iPad performer-monitor screen already discovers and connects to that feed, so this pass only brought the task tracker back into sync with the shipped behavior

- Selected task: add a live-input iPhone practice mode without preloaded beats
- Files changed: `TASKS.md`, `DEV_LOG.md`, `ScratchLab/Audio/AudioEngine.swift`, `ScratchLab/Views/MainMenuView.swift`, `ScratchLab/Views/LevelSelectView.swift`, `ScratchLab/Views/PracticeModeView.swift`
- Build result: `./scripts/build.sh` passed for `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: restored a `Live Practice` entry on the iPhone home screen, pointed it back at the existing baby-scratch training flow, and changed that iOS flow to stay live-input-only by default so sessions no longer auto-load or play a backing beat; the setup overlay now shows the active input route, lets the user switch between the phone mic and a wired USB/interface input when one is connected, and the audio engine now mirrors the real iOS route name so the practice screen can tell the user exactly what source ScratchLab is listening to while keeping the camera analysis live in the background

## 2026-04-15
- Selected task: prefer Serato Virtual Audio for Direct Serato Capture and keep the private tap as fallback
- Files changed: `TASKS.md`, `DEV_LOG.md`, `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`
- Build result: `./scripts/build.sh` passed for `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: reviewed the live Test Lab state and confirmed the Loopback path was at least producing real baby-scratch detections instead of silence, then patched Direct Serato Capture to prefer the already-published `Serato Virtual Audio` device whenever AVFoundation can see it on this Mac; the private `ScratchLab Direct Serato` process-tap aggregate now stays as fallback for setups where Serato does not expose its own virtual device, and a relaunch spot-check showed Test Audio switching to `Serato Virtual Audio` with the status text updated to `selected and feeding the analyzer`

- Selected task: import normalized CXL Dataset scratch audio and use the baby-scratch subset for recognition calibration
- Files changed: `TASKS.md`, `DEV_LOG.md`, `ScratchLab.xcodeproj/project.pbxproj`, `ScratchLab/Audio/AudioEngine.swift`, `ScratchLab/Resources/README.md`, `ScratchLab/Resources/cxl_scratch_library/README.md`, `ScratchLab/Resources/cxl_scratch_library/`, `ScratchLabDesktop/Services/MacScratchDetector.swift`
- Build result: `./scripts/build.sh` passed for `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: copied and renamed the full CXL Dataset scratch corpus from `/Users/karlwatson/Downloads/_Projects/DJ Audio` into `ScratchLab/Resources/cxl_scratch_library/` as 409 normalized MP3 files across 25 scratch folders, then bundled that library into both app targets; the current recognizers still only classify `Baby Scratch`, but both iOS and Mac now calibrate their baby timing profile from the new `cxl_scratch_library/baby_scratch` subset (31 samples) instead of mixing every scratch type into the baby profile, while the older `reference_*` WAV folders remain as fallback data for existing comparison flows and future non-baby recognizers can now key off the new stable folder layout

- Selected task: split ScratchLab Mac into Test Lab and Routine Lab tabs
- Files changed: `TASKS.md`, `DEV_LOG.md`, `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`
- Build result: `./scripts/build.sh` passed for `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: split the Mac app into a simple `Test Lab` tab for trying ScratchLab's baby-scratch rating flow with just the essential audio and score UI, plus a fuller `Routine Lab` tab that keeps the existing pro setup cards and adds Mac-side start/stop routine recording; the new recorder saves `.mov` takes into `~/Movies/ScratchLab Mac Routines`, and the next useful runtime check is a real hardware pass to confirm the saved take quality from the chosen routed audio source and camera angle

- Selected task: add Direct Serato Capture to the Mac analyzer
- Files changed: `TASKS.md`, `DEV_LOG.md`, `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`
- Build result: `./scripts/build.sh` passed for `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: added the first Mac-first direct audio path by detecting a running `Serato DJ Pro` process, creating a private Core Audio process tap plus a private aggregate input named `ScratchLab Direct Serato`, and surfacing that state directly in the analyzer audio card with `Check for Serato` and `Use Direct Capture`; this keeps the analyzer on the existing `AVCapture` input path while removing the need to teach users BlackHole or Loopback first, but it still needs live runtime validation against real Serato playback on this machine because Serato was not running during the build/test pass

- Selected task: add a Mac audio-routing setup panel to the analyzer
- Files changed: `TASKS.md`, `DEV_LOG.md`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`
- Build result: `./scripts/build.sh` passed for `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: expanded the Mac analyzer audio card into a real routing setup panel with current-route status, an explicit warning when the selected source still looks like a mic path, and three visible route options for BlackHole, Loopback, and hardware interface loopback; this keeps the product direction clear inside the app without pretending ScratchLab can configure those system routes for the user automatically

- Selected task: make the iPhone home screen companion-only
- Files changed: `TASKS.md`, `DEV_LOG.md`, `ScratchLab/Views/MainMenuView.swift`
- Build result: `./scripts/build.sh` passed for `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: removed the iPhone home entry points for Practice and Formula Lab so the app now presents itself as a companion surface instead of a second audio-analysis endpoint; the home copy now says ScratchLab Mac owns routed audio, beat analysis, and practice, while the phone stays focused on Companion Camera, Performer Monitor, and Watch Capture; kept shared UI pieces like `StatItem` in place because `LevelSelectView` still uses them internally even though that flow is no longer advertised from the iPhone home screen

- Selected task: render PDF-derived TTM graphs in Formula Lab
- Files changed: `TASKS.md`, `DEV_LOG.md`, `ScratchLab/Views/FormulaPlaygroundView.swift`
- Build result: `./scripts/build.sh` passed for `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: used the TTM v1.1 PDF notation pages as the reference for the Formula Lab visuals, replacing the text-first notation area with graph-first previews and adding a graph sequence for parsed formulas; entries with an explicit v1.1 graph are marked `PDF`, while scratches not actually drawn in that handbook are still shown with clearly marked derived previews instead of pretending they came straight from the book

- Selected task: make the Formula Lab notation catalog clearly readable on iPhone
- Files changed: `TASKS.md`, `DEV_LOG.md`, `ScratchLab/Views/FormulaPlaygroundView.swift`
- Build result: `./scripts/build.sh` passed for `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: turned the supported-symbols area into a counted, bounded scroll list so the phone UI makes it obvious there are many notation entries instead of one; also deduplicated repeated alias text like `tear` and `orbit`, and rechecked the screen in the iPhone simulator where the card now shows `20 scratches` with multiple visible rows and its own scroll area

- Selected task: replace Companion Cam AVCaptureVideoOrientation usage with rotation angles
- Files changed: `TASKS.md`, `DEV_LOG.md`, `ScratchLab/Services/CompanionCameraBroadcaster.swift`, `ScratchLab/Views/CompanionCameraView.swift`
- Build result: `./scripts/build.sh` passed for `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`; the previous `AVCaptureVideoOrientation` deprecation warnings are gone
- Follow-up notes: replaced the deprecated companion-camera orientation path with `AVCaptureDevice.RotationCoordinator` plus `AVCaptureConnection.videoRotationAngle`, updated the live preview and recording/frame-output connections to use rotation angles instead of `videoOrientation`, kept front-camera mirroring explicit, and preserved the landscape deck-framing behavior from the active rotation angle

- Selected task: add TTM notation aliases for the existing scratch library in Formula Lab
- Files changed: `TASKS.md`, `DEV_LOG.md`, `ScratchLab/Formula/ScratchFormulaCatalog.swift`, `ScratchLab/Views/FormulaPlaygroundView.swift`
- Build result: `./scripts/build.sh` passed for `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`; the existing `AVCaptureVideoOrientation` deprecation warnings remain in the companion camera path
- Follow-up notes: expanded Formula Lab from a baby-scratch-only symbol map into a TTM-style alias catalog for the scratches already present in `ScratchLibrary`, including parseable text aliases for common names like `transformer`, `flare`, `flare1`, `twoclickflare`, `threeclickflare`, `boomerang`, and `prism`; also rewrote the supported-symbols card so each scratch shows its display name and accepted aliases more clearly, and refreshed the starter formula to demonstrate the newly added notation

- Selected task: reorder the backlog so TTM notation aliases become the next implementation-ready Formula Lab task ahead of the still-blocked Bonjour discovery work
- Files changed: `TASKS.md`, `DEV_LOG.md`
- Build result: `./scripts/build.sh` passed for `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: inserted a new unchecked task for adding TTM v1.1 symbols and common aliases to the existing Formula Lab catalog using the scratches already present in `ScratchLibrary`, and moved that work ahead of the performer-monitor Bonjour task because the Bonjour path remains blocked by external macOS service-visibility issues already documented below

- Selected task: push the current ScratchLab iPhone + Watch build and ScratchLab Mac build to App Store Connect after the deck-calibration drag fix
- Files changed: `ScratchLab.xcodeproj/project.pbxproj`, `DEV_LOG.md`
- Build result: `./scripts/build.sh` passed for `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`; archived `build/ScratchLab-TestFlight-v6.xcarchive` and `build/ScratchLabDesktop-TestFlight-v6.xcarchive`; `xcodebuild -exportArchive` with `destination=upload` succeeded for both, reported `Uploaded ScratchLab` and `Uploaded ScratchLabDesktop`, and both packages entered App Store Connect processing
- Follow-up notes: bumped every `CURRENT_PROJECT_VERSION` entry from `5` to `6` so the iPhone app, embedded Watch app, and Mac companion all upload as a fresh release set from the current workspace; reused the automatic signing/export flow that already matched this machine's Apple team setup, and kept the latest Mac deck-box drag fix inside the uploaded Mac build

- Selected task: fix the Mac deck-calibration drag hit testing so each bounding box can be grabbed directly instead of every drag moving the right box
- Files changed: `ScratchLabDesktop/Views/DeckGamificationOverlay.swift`, `DEV_LOG.md`
- Build result: `./scripts/build.sh mac` passed for `ScratchLabDesktop`, and `./scripts/build.sh` passed for `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: replaced the per-box SwiftUI drag hit testing with one explicit drag-start picker that decides whether the pointer landed in a move region or resize handle for the touched zone, then applies the drag to that exact role for the rest of the gesture; this keeps the visible overlay the same while stopping the topmost right-deck control from stealing every drag

- Selected task: make the guided drill cue more visible over the iPhone practice camera as the first safe branding pass after the still-blocked Bonjour discovery task
- Files changed: `ScratchLab/ScratchLabApp.swift`, `ScratchLab/Audio/BackingTrackManager.swift`, `ScratchLab/Audio/SampleManager.swift`, `ScratchLab/Detection/EquipmentDetector.swift`, `ScratchLab/Views/AIBattleModeView.swift`, `ScratchLab/Views/CompanionCameraView.swift`, `ScratchLab/Views/FormulaPlaygroundView.swift`, `ScratchLab/Views/LevelSelectView.swift`, `ScratchLab/Views/MainMenuView.swift`, `ScratchLab/Views/PracticeModeView.swift`, `ScratchLab/Views/WatchCaptureHubView.swift`, `ScratchLabWatch/WatchCaptureView.swift`, `ScratchLabDesktop/Info.plist`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `docs/brand_kit.md`, `TASKS.md`, `DEV_LOG.md`
- Build result: `./scripts/build.sh` passed for `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`; the existing `AVCaptureVideoOrientation` deprecation warnings remain in the companion camera path
- Follow-up notes: replaced the remaining `Futura` UI usage on the live app surfaces with a system-font hierarchy, rebuilt the iPhone home screen around session status instead of decorative hero art, made the guided drill cue and live practice status more prominent, tightened the companion camera and watch wording, aligned the Mac app branding to `ScratchLab` / `ScratchLab Mac`, and added `docs/brand_kit.md` so ScratchLab and the future Scratch Academy AR line have a documented split; the top unchecked Bonjour discovery task is still externally blocked by macOS network-service visibility, so it remains open above the next backlog item

## 2026-04-14
- Selected task: fix the ScratchLab Mac App Store upload blockers so the macOS companion can ship through Xcode without the missing icon and sandbox validation errors
- Files changed: `ScratchLab.xcodeproj/project.pbxproj`, `ScratchLabDesktop/AppIcon.icns`, `ScratchLabDesktop/Info.plist`, `ScratchLabDesktop/ScratchLabDesktop.entitlements`, `DEV_LOG.md`
- Build result: `./scripts/build.sh` passed for `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`; archived `build/ScratchLabDesktop-TestFlight-v4b.xcarchive`; `xcodebuild -exportArchive` with `destination=upload` succeeded, reported `Uploaded ScratchLabDesktop`, and handed back App Store Connect delivery UUID `4aa1f927-0a50-4437-af60-0e6a89fbf8ae`
- Follow-up notes: generated a real macOS `AppIcon.icns` from the existing 1024px app icon and added it to the Mac target resources so the exported bundle now includes the required `512x512@2x` icon payload, added a Mac sandbox entitlements file with camera, microphone, and client/server network permissions to match the analyzer's runtime needs, and retried the Xcode upload flow until App Store Connect accepted the package without the earlier `90236` missing-icon and `90296` app-sandbox errors

- Selected task: try to wire the ScratchLab Mac App Store export path with the current team signing setup so the companion could ship without guessing at the Apple-side blocker
- Files changed: `TASKS.md`, `DEV_LOG.md`
- Build result: `./scripts/build.sh` passed for `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`; `build/ScratchLabDesktop-TestFlight-v4.xcarchive` initially failed App Store export for missing Mac distribution assets, then `xcodebuild -exportArchive -allowProvisioningUpdates` succeeded and produced `build/TestFlightMacUpload4/ScratchLabDesktop.pkg`, but `xcrun altool --upload-package ... --type macos` failed with `Cannot determine the Apple ID from Bundle ID 'com.machelpnz.scratchlab.mac' and platform 'MAC_OS'. (19)`
- Follow-up notes: confirmed the original signing error was environment-side, not code-side; the local Xcode account was able to fetch a cloud-managed `Apple Distribution` certificate and a `Mac Team Store Provisioning Profile` for `com.machelpnz.scratchlab.mac`, which is enough to export the signed Mac package locally, but App Store Connect still does not have a matching macOS app record for that bundle ID, so the remaining blocker is creating or linking the Mac app entry in App Store Connect before the package can upload

- Selected task: allow phone fallback for Performer Monitor and ship a fresh TestFlight build so the performer feed is still usable without an iPad and the latest iPhone + Watch binary is in App Store Connect
- Files changed: `TASKS.md`, `DEV_LOG.md`, `ScratchLab.xcodeproj/project.pbxproj`, `ScratchLab/Views/MainMenuView.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`
- Build result: `./scripts/build.sh` passed for `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`; archived `build/ScratchLab-TestFlight-v4.xcarchive`, exported `build/TestFlightUpload4/ScratchLab.ipa`, and uploaded it successfully to App Store Connect with delivery UUID `d08c4958-fcc1-4c6f-abab-260f7716cdac`
- Follow-up notes: exposed `Performer Monitor` on iPhone as well as iPad, updated the iOS and Mac copy to describe a phone-or-iPad receiver without implying the iPhone can act as a Sidecar display, and bumped `CURRENT_PROJECT_VERSION` to `4`; the separate macOS companion can archive locally as `build/ScratchLabDesktop-TestFlight-v4.xcarchive`, but App Store export is still blocked by missing Mac App Store signing assets (`No signing certificate "Mac Installer Distribution" found` and `No profiles for 'com.machelpnz.scratchlab.mac' were found`), so only the iPhone app plus embedded Watch companion were pushed to App Store Connect in this pass

- Selected task: add a one-click Serato screen mover for Sidecar iPad sessions so the DJ can keep the Mac tilted toward the decks without losing the Serato view
- Files changed: `TASKS.md`, `DEV_LOG.md`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`
- Build result: `./scripts/build.sh` passed for `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: added a dedicated `Serato Screen` card in ScratchLab Mac with `Send to iPad` and `Back to Mac` actions, wired those buttons to a small Accessibility-based window mover for the installed `Serato DJ Pro` app, reused the existing Sidecar/external-display targeting rules, prompted for Accessibility access when needed, and kept the rest of the analyzer workflow unchanged

- Selected task: stabilize the Mac deck bounding boxes so they do not jump between camera detections while the analyzer is running
- Files changed: `TASKS.md`, `DEV_LOG.md`, `ScratchLabDesktop/Services/DJRigLayoutDetector.swift`, `ScratchLabDesktop/Services/MacCaptureEngine.swift`
- Build result: `./scripts/build.sh` passed for `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: kept the fix detector-side so the overlay did not need animation hacks, blended fresh rig detections into the prior union box, held the last good layout through brief detection misses instead of dropping boxes immediately, and reset the rig-layout cache whenever the Mac capture session stops or reconfigures so stale boxes do not leak across camera changes

- Selected task: replace the performer-monitor discovery path with a Bonjour TCP feed that the iPad can reliably see so the iPad monitor can still connect even when macOS keeps blocking Bonjour publish
- Files changed: `ScratchLab.xcodeproj/project.pbxproj`, `ScratchLab/Views/MainMenuView.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `DEV_LOG.md`
- Build result: `./scripts/build.sh` passed for `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: kept the Mac as the scratch-data source on routed Mac audio, gave the local macOS Debug build its own bundle ID to isolate local-network permission state, pinned the performer feed to TCP port `58585`, made the Mac advertiser stop restarting its own listener on window open, added an iPad manual-connect field, switched the Mac's manual-connect hint to the live LAN IPv4 address when available, reinstalled and relaunched the updated iPad build, and verified that the Mac now holds a listening socket on `192.168.179.23:58585`; Bonjour publish is still being rejected by macOS with `NSNetServicesErrorCode = -72008`, so the task remains open until Nearby Mac discovery itself is reliable

## 2026-04-13
- Selected task: replace the performer-monitor discovery path with a Bonjour TCP feed that the iPad can reliably see so the iPad monitor is not blocked on invisible Multipeer state
- Files changed: `ScratchLab/Info.plist`, `ScratchLabDesktop/Info.plist`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `DEV_LOG.md`
- Build result: `./scripts/build.sh` passed for `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: moved the Mac performer feed onto a plain TCP listener with `NetService` publication, added focused runtime logging around listener and Bonjour publish state, added the service variants to both app Info.plists, rebuilt and relaunched the Mac app, reinstalled and relaunched the iPad app, and verified via `log show` that macOS is still rejecting the live Bonjour publish with `NSNetServicesErrorCode = -72008`, so the task stays open until the local-network privacy block is cleared and the service appears in `dns-sd`

## 2026-04-13
- Selected task: make the iPad performer monitor easier to surface and connect when auto-discovery misses so the user is not stuck waiting on invisible Multipeer state
- Files changed: `TASKS.md`, `DEV_LOG.md`, `ScratchLab/Views/MainMenuView.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`
- Build result: `./scripts/build.sh` passed for `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: refreshed the iPad browse state whenever the performer-monitor screen opens, cleared stale peer/frame state on reconnect, added a visible nearby-Mac list with explicit `Connect` buttons, refreshed Mac advertising whenever the analyzer or performer-monitor window appears, reinstalled the updated iPad build, relaunched the app on device, and reopened the latest Mac build


## 2026-04-13
- Selected task: stream the Mac performer monitor directly to an iPad when Sidecar is not available so the performer view can still live off the Serato screen
- Files changed: `TASKS.md`, `DEV_LOG.md`, `ScratchLab/Info.plist`, `ScratchLab/Views/MainMenuView.swift`, `ScratchLabDesktop/Info.plist`, `ScratchLabDesktop/ScratchLabDesktopApp.swift`, `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`
- Build result: `./scripts/build.sh` passed for `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: added a Mac-side performer-monitor broadcaster over local Bonjour/Multipeer, added an iPad-only `Performer Monitor` screen in the iOS app that auto-connects and draws the live deck boxes plus cue/status badges, and updated the Mac copy so operators know they can use Sidecar when it exists or the iPad app when it does not; a local install retry on the paired iPad could not run because `xcrun devicectl list devices` currently reports that iPad as unavailable


## 2026-04-13
- Selected task: make the Mac performer monitor actually usable on an iPad Sidecar screen by showing the live deck view and giving it a direct move-to-iPad action instead of relying on blind dragging
- Files changed: `TASKS.md`, `DEV_LOG.md`, `ScratchLabDesktop/ScratchLabDesktopApp.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`
- Build result: `./scripts/build.sh mac` passed for `ScratchLabDesktop`; full `./scripts/build.sh` rerun follows after the Sidecar monitor update
- Follow-up notes: rebuilt the performer window around a two-mode layout (`Cue` and `Deck View`), made `Deck View` the default so the camera, deck boxes, and feedback appear immediately, added a `Send to iPad` action that auto-detects a Sidecar/external display and centers the window there when one exists, plus a `Back to Mac` action for returning it to the main screen

## 2026-04-13
- Selected task: move the Mac performer-monitor entry point into the visible top card after the first Sidecar flow was too buried in the analyzer sidebar
- Files changed: `DEV_LOG.md`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`
- Build result: `./scripts/build.sh` passed for `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: kept the fix tiny and placed a `Performer Monitor` button plus a one-line Sidecar explanation directly in the `ScratchLab Mac` header card so the operator no longer needs to scroll the sidebar to open the Sidecar-friendly window

## 2026-04-13
- Selected task: surface baby scratch hand-direction guidance in the Mac analyzer so the Mac feedback can live on a Sidecar iPad or second screen without crowding Serato
- Files changed: `TASKS.md`, `DEV_LOG.md`, `ScratchLabDesktop/ScratchLabDesktopApp.swift`, `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`
- Build result: `./scripts/build.sh` passed for `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: kept the change Mac-only and converted the existing left/right hand-motion state into explicit baby-scratch cue text (`start a smooth sweep`, `reverse right`, `reverse left`), updated the analyzer sidebar and stage pill to use those cues, and added a dedicated `Performer Monitor` window that can be opened from the analyzer and moved onto an iPad via Apple's Sidecar or any second display

## 2026-04-13
- Selected task: generate a Banta invoice-versus-payment workpaper so the bank mismatch can be reviewed invoice by invoice without guessing allocations
- Files changed: `TASKS.md`, `DEV_LOG.md`, `nz-tax-workflow/README.md`, `nz-tax-workflow/src/build_counterparty_reconciliation.py`, `nz-tax-workflow/data/working/real_2025_26_tax_docs/banta_hospitality_ltd_invoice_reconciliation.csv`, `nz-tax-workflow/data/working/real_2025_26_tax_docs/banta_hospitality_ltd_related_deposits.csv`, `nz-tax-workflow/data/working/real_2025_26_tax_docs/banta_hospitality_ltd_reconciliation_summary.md`
- Build result: `python3 src/build_counterparty_reconciliation.py --working-dir data/working/real_2025_26_tax_docs --counterparty "Banta Hospitality Ltd" --deposit-token banta --deposit-token fiddl --deposit-token whiskey` passed inside `nz-tax-workflow/`, and `./scripts/build.sh` passed for `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: added a small reusable counterparty reconciliation script that reads the existing extracted working files, writes an invoice workpaper, a related-deposits workpaper, and a markdown summary, and leaves token-matched deposits unallocated unless the existing exact statement reconciliation already proved them; the Banta run found 3 exact-paid invoices, 4 review-unmatched invoices, 2 unallocated related deposits (`2000.00` and `1500.00`), and flagged `INV-2026-013` plus `INV-2026-018_BantaHospitality` as a possible duplicate-description group for manual review

## 2026-04-13
- Selected task: reconcile 2025-26 invoice-derived income dates against exact ANZ statement deposits so the weekly reserve and GST timing reports use cash-received dates where the bank statement supports them
- Files changed: `TASKS.md`, `DEV_LOG.md`, `nz-tax-workflow/README.md`, `nz-tax-workflow/src/extract_sorted_tax_docs.py`, `nz-tax-workflow/data/working/real_2025_26_tax_docs/extracted_transactions.csv`, `nz-tax-workflow/data/working/real_2025_26_tax_docs/reconciled_transactions.csv`, `nz-tax-workflow/data/working/real_2025_26_tax_docs/bank_statement_deposits.csv`, `nz-tax-workflow/data/working/real_2025_26_tax_docs/income_reconciliation.csv`, `nz-tax-workflow/data/working/real_2025_26_tax_docs/document_extraction_report.md`, `nz-tax-workflow/data/working/real_2025_26_tax_docs/import_audit.csv`, `nz-tax-workflow/data/working/real_2025_26_tax_docs/normalized_transactions.csv`, `nz-tax-workflow/data/working/real_2025_26_tax_docs/workflow_job.json`, `nz-tax-workflow/outputs/real_2025_26_tax_docs/monthly_summary.csv`, `nz-tax-workflow/outputs/real_2025_26_tax_docs/two_monthly_gst_summary.csv`, `nz-tax-workflow/outputs/real_2025_26_tax_docs/weekly_reserve_summary.csv`, `nz-tax-workflow/outputs/real_2025_26_tax_docs/review_queue.csv`, `nz-tax-workflow/outputs/real_2025_26_tax_docs/summary_report.md`
- Build result: `python3 src/extract_sorted_tax_docs.py --docs-root "/Users/karlwatson/Downloads/2025-26 Tax docs" --run-name real_2025_26_tax_docs --tax-year-start 2025-04-01 --tax-year-end 2026-03-31` passed inside `nz-tax-workflow/`, and `./scripts/build.sh` passed for `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: added a conservative statement-reconciliation pass that parses ANZ statement deposits, keeps the raw extracted ledger untouched, and writes `reconciled_transactions.csv`, `bank_statement_deposits.csv`, and `income_reconciliation.csv` as separate audit artifacts; the 2025-26 run now matched 9 invoice-derived income rows to exact statement deposits, left 5 invoice-derived rows on their original issue dates because no exact statement-backed match was found, and kept the existing 8 workflow review rows visible for manual judgment, with duplicate references still flagged for `INV-2026-011` and `INV-2026-018`

## 2026-04-13
- Selected task: extract a draft 2025-26 ledger from the sorted `2025-26 Tax docs` folder so the new NZ tax workflow could process real invoices and receipts instead of only sample CSVs
- Files changed: `TASKS.md`, `DEV_LOG.md`, `nz-tax-workflow/README.md`, `nz-tax-workflow/requirements.txt`, `nz-tax-workflow/src/workflow.py`, `nz-tax-workflow/src/extract_sorted_tax_docs.py`, `nz-tax-workflow/data/working/real_2025_26_tax_docs/document_register.csv`, `nz-tax-workflow/data/working/real_2025_26_tax_docs/document_review_queue.csv`, `nz-tax-workflow/data/working/real_2025_26_tax_docs/document_extraction_report.md`, `nz-tax-workflow/data/working/real_2025_26_tax_docs/extracted_transactions.csv`, `nz-tax-workflow/data/working/real_2025_26_tax_docs/import_audit.csv`, `nz-tax-workflow/data/working/real_2025_26_tax_docs/normalized_transactions.csv`, `nz-tax-workflow/data/working/real_2025_26_tax_docs/workflow_job.json`, `nz-tax-workflow/outputs/real_2025_26_tax_docs/monthly_summary.csv`, `nz-tax-workflow/outputs/real_2025_26_tax_docs/two_monthly_gst_summary.csv`, `nz-tax-workflow/outputs/real_2025_26_tax_docs/weekly_reserve_summary.csv`, `nz-tax-workflow/outputs/real_2025_26_tax_docs/review_queue.csv`, `nz-tax-workflow/outputs/real_2025_26_tax_docs/summary_report.md`
- Build result: `./scripts/build.sh` passed for `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`; `python3 src/extract_sorted_tax_docs.py --docs-root "/Users/karlwatson/Downloads/2025-26 Tax docs" --run-name real_2025_26_tax_docs --tax-year-start 2025-04-01 --tax-year-end 2026-03-31` also passed inside `nz-tax-workflow/`
- Follow-up notes: added a document extractor that reads the sorted invoice and receipt folders, imports the DJ paid-summary statement as no-GST income rows, imports issued invoices and NZ GST receipts into a standard draft ledger, leaves foreign-currency supplier invoices and zero-value receipts in a document review queue, and writes a document register so every scanned file stays traceable; the real-doc run scanned 104 documents, emitted 118 transaction rows, produced a workflow review queue with 8 rows left for manual judgment, flagged duplicate references for `INV-2026-011` and `INV-2026-018`, skipped April 2026 and March 2025 documents outside the requested 2025-04-01 to 2026-03-31 window, and generated monthly, two-month GST, and weekly reserve reports under `nz-tax-workflow/outputs/real_2025_26_tax_docs/`

## 2026-04-13
- Selected task: build a standalone NZ small-business tax prep workflow in a separate `nz-tax-workflow/` project so the new tooling would not disturb the iOS, macOS, or watchOS app targets
- Files changed: `TASKS.md`, `DEV_LOG.md`, `nz-tax-workflow/README.md`, `nz-tax-workflow/requirements.txt`, `nz-tax-workflow/assumptions.md`, `nz-tax-workflow/config/settings.json`, `nz-tax-workflow/config/import_jobs.sample.json`, `nz-tax-workflow/config/import_profiles/standard_template.json`, `nz-tax-workflow/config/import_profiles/marketplace_export.json`, `nz-tax-workflow/data/raw/standard_transactions_template.csv`, `nz-tax-workflow/data/raw/marketplace_export_template.csv`, `nz-tax-workflow/data/raw/sample_standard_transactions.csv`, `nz-tax-workflow/data/raw/sample_marketplace_export.csv`, `nz-tax-workflow/data/working/sample_run/normalized_transactions.csv`, `nz-tax-workflow/data/working/sample_run/import_audit.csv`, `nz-tax-workflow/docs/data_dictionary.md`, `nz-tax-workflow/docs/review_queue_guide.md`, `nz-tax-workflow/outputs/sample_run/monthly_summary.csv`, `nz-tax-workflow/outputs/sample_run/two_monthly_gst_summary.csv`, `nz-tax-workflow/outputs/sample_run/weekly_reserve_summary.csv`, `nz-tax-workflow/outputs/sample_run/review_queue.csv`, `nz-tax-workflow/outputs/sample_run/summary_report.md`, `nz-tax-workflow/src/run_workflow.py`, `nz-tax-workflow/src/workflow.py`
- Build result: `./scripts/build.sh` passed for `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`; `python3 src/run_workflow.py --job config/import_jobs.sample.json` also passed inside `nz-tax-workflow/` and generated a sample run with 17 transactions and 6 review-required rows
- Follow-up notes: added a beginner-readable CSV-driven workflow with JSON import profiles, a normalized working schema, monthly and Jan-Feb two-month GST summaries, a conservative weekly reserve report for setting aside GST and income tax from receipts, and a review queue that keeps mixed-use, personal, marketplace, missing-counterparty, cash-without-detail, and unclear-GST rows visible instead of guessed; all key rates remain configurable in `config/settings.json`, raw inputs stay untouched in `data/raw/`, and the included samples cover DJ income, Mac repair income, Trade Me style sales, common business costs, and ambiguous edge cases

## 2026-04-12
- Selected task: add local Companion Cam recording with external audio input selection so the iPhone can save a usable clip while preferring a connected USB DJ interface
- Files changed: `TASKS.md`, `DEV_LOG.md`, `ScratchLab/Services/CompanionCameraBroadcaster.swift`, `ScratchLab/Views/CompanionCameraView.swift`
- Build result: `./scripts/build.sh` passed for `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: reprioritized the backlog so USB-audio-backed iPhone capture comes before the Mac analyzer guidance follow-up, upgraded Companion Cam from live-stream-only into a local `.mov` recorder that keeps streaming to the Mac while saving a phone clip, added iPhone audio-route discovery that prefers USB and line-in sources when present but still falls back to the built-in mic, surfaced the chosen audio route and last saved file on the Companion Cam screen, and kept the only remaining warnings to the existing AVFoundation video-orientation deprecations

## 2026-04-12
- Selected task: let the iPhone Companion Cam rotate into landscape so DJs can fit the decks into frame more easily
- Files changed: `TASKS.md`, `DEV_LOG.md`, `ScratchLab/Services/CompanionCameraBroadcaster.swift`, `ScratchLab/Views/CompanionCameraView.swift`
- Build result: `./scripts/build.sh` passed for `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: stopped forcing the Companion Cam capture path to portrait, added device-orientation tracking so the preview and outgoing companion frames follow landscape rotation, switched the outgoing frame size to a wide landscape aspect when the phone is rotated, and added a short in-app hint that landscape gives a wider deck frame; the build now shows only deprecation warnings from AVFoundation's older orientation API, with no compile failures

## 2026-04-12
- Selected task: fix Mac companion discovery permissions and clarify that the Mac companion feed comes from the iPhone while watch captures still route through the phone
- Files changed: `TASKS.md`, `DEV_LOG.md`, `ScratchLab.xcodeproj/project.pbxproj`, `ScratchLab/Info.plist`, `ScratchLabDesktop/Info.plist`, `ScratchLabDesktop/Services/CompanionCameraReceiver.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`
- Build result: `./scripts/build.sh` passed for `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: gave the Mac target an explicit Info.plist with `NSBonjourServices` and `NSLocalNetworkUsageDescription` for the companion-camera Bonjour service, updated the iPhone local-network permission copy so the Companion Cam prompt names the Mac workflow, narrowed the Mac receiver status text to “iPhone Companion Cam,” and added Mac UI copy that tells the user the Watch syncs to the iPhone first instead of appearing as a direct Mac device; also verified the built Mac app bundle now contains the expected Bonjour and local-network keys

## 2026-04-12
- Selected task: prepare the capture pipeline for a real pilot session with simpler operator wording and one end-to-end runbook
- Files changed: `TASKS.md`, `DEV_LOG.md`, `README.md`, `docs/capture_spec_v1.md`, `docs/dj_operator_quickstart.md`, `docs/session_checklist.md`, `docs/pilot_session_runbook.md`, `templates/session_manifest_example.json`, `templates/take_log_example.csv`
- Build result: `./scripts/build.sh` passed for `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: tightened the operator wording in the capture spec, quickstart, and checklist around the existing `camA` + `serato` validation rules, added a single pilot-session runbook that walks through session creation, recording, take-log entry, rename, validation, and failure recovery, added filled examples for the session manifest and take log, and added a concise README troubleshooting section for the most likely pilot-session errors without changing the capture-pipeline contract

## 2026-04-12
- Selected task: address the capture-pipeline consistency review findings with the smallest fix set that keeps the current CLI flow intact
- Files changed: `TASKS.md`, `DEV_LOG.md`, `README.md`, `docs/capture_spec_v1.md`, `docs/metadata_schema.md`, `scripts/capture_pipeline_common.py`, `scripts/validate_session.py`
- Build result: `./scripts/build.sh` passed for `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: made `manifests/take_log.csv` the operator-edited source of truth by failing validation when renamed takes exist on disk without matching take-log rows; tightened take creation so a manifest record is emitted only when both primary files exist (`camA` video and `serato` audio); removed `camB`-only validity from the schema and validation rules while keeping `camB` as optional additive coverage; rewrote the root README around the actual repo structure and current local capture workflow; smoke-tested the scripts with pass/fail cases for missing take-log rows, missing audio, and `camB`-only coverage

## 2026-04-12
- Selected task: generate the Scratch Capture Pipeline MVP templates and CLI scripts after the docs review step
- Files changed: `TASKS.md`, `DEV_LOG.md`, `templates/session_manifest_template.json`, `templates/take_log_template.csv`, `scripts/capture_pipeline_common.py`, `scripts/create_session.py`, `scripts/rename_files.py`, `scripts/validate_session.py`
- Build result: `./scripts/build.sh` passed for `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: added a session manifest template and take-log template, plus three Python CLI scripts for session scaffolding, take-log-driven rename, and session validation; smoke-tested the new workflow in a temporary session by creating a session, renaming dummy raw files, confirming validation fails when `70` and `110` BPM sets are missing, and confirming validation passes once all three BPMs are present; README usage instructions remain for the next follow-up step if needed

## 2026-04-12
- Selected task: draft the Scratch Capture Pipeline MVP documentation set for the capture workflow review step
- Files changed: `TASKS.md`, `DEV_LOG.md`, `docs/capture_spec_v1.md`, `docs/dj_operator_quickstart.md`, `docs/session_checklist.md`, `docs/naming_convention.md`, `docs/metadata_schema.md`
- Build result: `./scripts/build.sh` passed for `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`
- Follow-up notes: documented the MVP around baby scratch only, fixed BPM options (`70`, `90`, `110`), the required verbal slate and clap sync pattern, the session folder layout, filename rules, and the per-take metadata fields; left `templates/`, the CLI scripts, and README usage docs for the next approved implementation step

## 2026-04-05
- Selected task: repair the embedded watch companion packaging so the iPhone TestFlight archive can upload with the watch app included
- Files changed: `ScratchLabWatch/Assets.xcassets/AppIcon.appiconset/Contents.json`, `ScratchLab.xcodeproj/project.pbxproj`, `DEV_LOG.md`
- Build result: `./scripts/build.sh` passed for `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`; the existing `videoOrientation` deprecation warning remains in `ScratchLab/Services/CompanionCameraBroadcaster.swift`
- Follow-up notes: corrected the watch `44x44` and `50x50` app icon slots to the asset-catalog role/subtype combination that `actool` actually emits for modern watch packaging, which restored the missing `88x88` and `100x100` icon renditions inside `Assets.car`; bumped `CURRENT_PROJECT_VERSION` to `3`; archived `build/ScratchLab-TestFlight-Watch-v11.xcarchive`, exported `build/TestFlightWatchUpload11/ScratchLab.ipa`, and uploaded it successfully to App Store Connect with delivery UUID `6135377f-c595-4118-8a5d-022310ade0d7`; the watch companion is now embedded in the iPhone archive path that passed upload validation

## 2026-04-05
- Selected task: investigate iOS 26.0.1 launch compatibility on `CXL's iPhone` (`iPhone 12 Pro`) after TestFlight and local developer installs both failed to open
- Files changed: `ScratchLab/ScratchLabApp.swift`, `DEV_LOG.md`
- Build result: `./scripts/build.sh` passed for `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`; the existing `videoOrientation` deprecation warning remains in `ScratchLab/Services/CompanionCameraBroadcaster.swift`
- Follow-up notes: moved iOS environment object construction out of the `@main App` type into `RootContainerView` so service initialization no longer happens at app bootstrap; after that, progressively stripped device builds still failed on the same phone, including a near-empty SwiftUI-only build and a fresh temporary bundle identifier (`com.machelpnz.scratchlab.compattest`), both with `Launchd job spawn failed (NSPOSIXErrorDomain error 85)` and `SBMainWorkspace` denying the open request; that rules out the watch/import/audio/game-center feature code as the current blocker and points to a deeper device / OS 26.0.1 launch issue for locally built apps on this phone. Temporary Companion Cam and plist isolation edits were reverted before the final full build.

## 2026-04-05
- Selected task: investigate the iPhone 12 Pro launch failure and harden the iOS app's startup path before the next TestFlight retest
- Files changed: `ScratchLab.xcodeproj/project.pbxproj`, `ScratchLab/Info.plist`, `ScratchLab/Audio/AudioEngine.swift`, `ScratchLab/Services/ProgressManager.swift`, `ScratchLab/Services/WatchMotionCaptureStore.swift`, `ScratchLab/Views/WatchCaptureHubView.swift`, `DEV_LOG.md`
- Build result: `./scripts/build.sh` passed for `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`; the existing `videoOrientation` deprecation warning remains in `ScratchLab/Services/CompanionCameraBroadcaster.swift`
- Follow-up notes: removed an accidental `ScratchLab.xctestplan` resource from the shipped iOS app bundle, trimmed the iOS Info.plist down to the launch/background settings the app currently uses, and deferred audio, Game Center, and WatchConnectivity startup work until the related flows are actually used; on `CXL's iPhone` (`iPhone 12 Pro`, `iOS 26.0.1`) the app still fails before `ScratchLabApp.init` with `Launchd job spawn failed (NSPOSIXErrorDomain error 85)`, so the remaining blocker appears to be pre-main / platform-level rather than a normal Swift crash

## 2026-04-05
- Selected task: fix the iOS archive packaging so TestFlight upload validation succeeds for the ScratchLab companion app
- Files changed: `ScratchLab.xcodeproj/project.pbxproj`, `ScratchLab/Info.plist`, `ScratchLab/Assets.xcassets/AppIcon.appiconset/Contents.json`, `ScratchLab/Assets.xcassets/AppIcon.appiconset/icon-20@1x.png`, `ScratchLab/Assets.xcassets/AppIcon.appiconset/icon-20@2x.png`, `ScratchLab/Assets.xcassets/AppIcon.appiconset/icon-20@3x.png`, `ScratchLab/Assets.xcassets/AppIcon.appiconset/icon-29@1x.png`, `ScratchLab/Assets.xcassets/AppIcon.appiconset/icon-29@2x.png`, `ScratchLab/Assets.xcassets/AppIcon.appiconset/icon-29@3x.png`, `ScratchLab/Assets.xcassets/AppIcon.appiconset/icon-40@1x.png`, `ScratchLab/Assets.xcassets/AppIcon.appiconset/icon-40@2x.png`, `ScratchLab/Assets.xcassets/AppIcon.appiconset/icon-40@3x.png`, `ScratchLab/Assets.xcassets/AppIcon.appiconset/icon-60@2x.png`, `ScratchLab/Assets.xcassets/AppIcon.appiconset/icon-60@3x.png`, `ScratchLab/Assets.xcassets/AppIcon.appiconset/icon-76@1x.png`, `ScratchLab/Assets.xcassets/AppIcon.appiconset/icon-76@2x.png`, `ScratchLab/Assets.xcassets/AppIcon.appiconset/icon-83.5@2x.png`, `ScratchLab/Assets.xcassets/AppIcon.appiconset/icon-1024.png`, `DEV_LOG.md`
- Build result: `./scripts/build.sh` passed for `ScratchLab`, `ScratchLabDesktop`, and `ScratchLabWatch`; the existing `videoOrientation` deprecation warning remains in `ScratchLab/Services/CompanionCameraBroadcaster.swift`
- Follow-up notes: added the missing iOS asset catalog to the app target resources, supplied a complete placeholder AppIcon set, kept `CFBundleIconName` in the shipped Info.plist, and removed the unsupported background processing mode so App Store Connect would accept the archive; the TestFlight binary upload now succeeds, but external tester/group creation through the provided App Store Connect API key returns `403 Forbidden`, so the invite still needs App Store Connect web access with higher permissions

## 2026-04-02
- Selected task: add an Apple Watch scratch-motion capture companion for Series 7+ hardware that records wrist IMU sessions and hands them off to the paired iPhone
- Files changed: `AI_CONTEXT.md`, `AGENTS.md`, `TASKS.md`, `DEV_LOG.md`, `ScratchLab.xcodeproj/project.pbxproj`, `scripts/build.sh`, `ScratchLab/ScratchLabApp.swift`, `ScratchLab/Views/MainMenuView.swift`, `ScratchLab/Models/WatchMotionCapture.swift`, `ScratchLab/Services/WatchMotionCaptureStore.swift`, `ScratchLab/Views/WatchCaptureHubView.swift`, `ScratchLabWatch/ScratchLabWatchApp.swift`, `ScratchLabWatch/WatchCaptureView.swift`, `ScratchLabWatch/Services/WatchMotionRecorder.swift`
- Build result: `./scripts/build.sh` passed for `ScratchLab` (generic iOS), `ScratchLabDesktop` (macOS), and `ScratchLabWatch` (watchOS unsigned local validation); the iOS build still emits the existing `videoOrientation` deprecation warning in `ScratchLab/Services/CompanionCameraBroadcaster.swift`
- Follow-up notes: the first pass records watch motion data in the foreground, transfers completed sessions to iPhone storage with WatchConnectivity file transfer, and exports JSON/CSV from the iPhone hub; direct live sync to the Mac analyzer or Serato cue alignment is not implemented yet

## 2026-04-18
- Selected task: make macOS and watchOS review copy device-neutral and production-safe
- Files changed: `TASKS.md`, `DEV_LOG.md`, `ScratchLab/Info.plist`, `ScratchLabDesktop/Info.plist`, `ScratchLab/Services/CompanionCameraBroadcaster.swift`, `ScratchLab/Services/WatchMotionCaptureStore.swift`, `ScratchLab/Views/CompanionCameraView.swift`, `ScratchLab/Views/LevelSelectView.swift`, `ScratchLab/Views/MainMenuView.swift`, `ScratchLab/Views/PracticeModeView.swift`, `ScratchLab/Views/WatchCaptureHubView.swift`, `ScratchLabDesktop/Services/CompanionCameraReceiver.swift`, `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `ScratchLabDesktop/Views/DeckGamificationOverlay.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLabWatch/Services/WatchMotionRecorder.swift`, `ScratchLabWatch/WatchCaptureView.swift`
- Build result: `./scripts/build.sh` passed for `ScratchLab` (iOS), `ScratchLabDesktop` (macOS), and `ScratchLabWatch` (watchOS)
- Follow-up notes: replaced Apple-device wording in visible paired-device copy, softened manual connection surfaces into advanced wording, simplified visible network/recording errors, aligned watch terminology with paired-device language, neutralized non-warning helper styling, and kept internal platform/device checks unchanged where they are not user-facing

## 2026-03-30
- Selected task: rewrite the legacy `Sources/...` backlog into real-file follow-up tasks and align build-script docs with the actual repo path
- Files changed: `AI_CONTEXT.md`, `TASKS.md`, `AGENTS.md`, `auto-task.sh`, `DEV_LOG.md`
- Build result: `./scripts/build.sh` passed for both `ScratchLab` (generic iOS) and `ScratchLabDesktop` (macOS); the iOS build still emits the existing `videoOrientation` deprecation warning in `ScratchLab/Services/CompanionCameraBroadcaster.swift`
- Follow-up notes: replaced the blocked `Sources/...` placeholders with current-file tasks, marked the FFT windowing placeholder as already satisfied by the live analyzers, and made `Surface baby scratch hand-direction guidance in the Mac analyzer` the first unchecked implementation-ready task

## 2026-03-13
- Selected task: prep repo for GitHub-safe publishing with a focused Xcode `.gitignore`
- Files changed: `.gitignore`, `DEV_LOG.md`
- Build result: `Scripts/build.sh` passed for both `ScratchLab` (generic iOS) and `ScratchLabDesktop` (macOS)
- Follow-up notes: added ignore rules for Xcode user state, local build caches, signing assets, and a stray local workspace; the original local git history still includes tracked `DerivedData` and `xcuserdata`, so a follow-up cleanup commit or fresh-history reset is still recommended before the first GitHub push

## 2026-03-12
- Selected task: bootstrap repo workflow files for Codex-driven one-task runs
- Files changed: `AGENTS.md`, `AI_CONTEXT.md`, `TASKS.md`, `DEV_LOG.md`, `Scripts/build.sh`, `auto-task.sh`
- Build result: `Scripts/build.sh` passed for both `ScratchLab` (generic iOS) and `ScratchLabDesktop` (macOS)
- Follow-up notes: parked `Sources/...` AR-module tasks were preserved in `TASKS.md` but marked as not implementation-ready until those files exist in the repo

## 2026-04-19
- Selected task: create a shared session-setup view model used by both iOS and macOS
- Files changed: `ScratchLab/Models/CaptureCore.swift`, `ScratchLab/Views/CompanionCameraView.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: `scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab` (iOS), `ScratchLabDesktop` (macOS), and `ScratchLabWatch` (watchOS)
- Follow-up notes: added a minimal shared `SessionSetupViewModel` around `CaptureSessionConfig`, moved iOS guided-capture canonical session setup bindings and validation through it, and reused the same shared session naming path for macOS routine export/share without changing UI layout or the export schema

## 2026-04-20
- Selected task: direct user-requested macOS companion/watch coordination to relay watch motion captures to the Mac and let the Mac routine record button request watch start/stop through the paired iPhone
- Files changed: `ScratchLab/Services/CompanionCameraBroadcaster.swift`, `ScratchLab/Views/CompanionCameraView.swift`, `ScratchLab/Services/WatchMotionCaptureStore.swift`, `ScratchLabWatch/Services/WatchMotionRecorder.swift`, `ScratchLabDesktop/Services/CompanionCameraReceiver.swift`, `ScratchLabDesktop/ScratchLabDesktopApp.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `DEV_LOG.md`
- Build result: `scripts/build.sh` passed for the capture-pipeline fixture suite, `ScratchLab` (iOS), `ScratchLabDesktop` (macOS), and `ScratchLabWatch` (watchOS)
- Follow-up notes: added a relay path for completed watch motion sessions from iPhone companion to macOS, added Mac-to-iPhone-to-watch start/stop control packets with explicit status reporting, surfaced relayed watch-capture status in the macOS routine UI, and kept the flow fail-closed when the paired watch app is not reachable; `TASKS.md` was left unchanged because there is still no unchecked implementation-ready backlog entry to mark complete

## 2026-04-20
- Selected task: stabilize the Phase 1 macOS hosted XCTest path so the deterministic capture-reliability core suite completes cleanly under `xcodebuild test`
- Files changed: `DEV_LOG.md`, `ScratchLab/Services/SessionExportCoordinator.swift`, `ScratchLab/Services/SessionUploadManager.swift`, `ScratchLabDesktop/ScratchLabDesktopApp.swift`, `ScratchLabDesktop/Services/CompanionCameraReceiver.swift`, `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`
- Build result: `python3 scripts/test_capture_pipeline.py` passed, `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests` passed, and `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test` passed end-to-end without hosted-runner restarts
- Follow-up notes: suppressed macOS app startup side effects during XCTest by avoiding automatic browse/upload/device-refresh startup and by not mounting the live analyzer windows during hosted tests, split the deterministic Phase 1 suite into a pure core class, replaced the parity test's real AV probe dependency with an injected artifact-probe override so placeholder fixtures stay deterministic without weakening production validation, and added refusal-path coverage for missing audio, mixed-session contamination, canonical manifest drift, and wrong linked watch artifacts; `TASKS.md` was left unchanged because this was a user-directed stabilization pass rather than the first unchecked backlog item

## 2026-04-20
- Selected task: Phase 2 recovery hardening for interrupted staged captures, orphan reconciliation, blocking reports, and persistent audit summaries
- Files changed: `DEV_LOG.md`, `ScratchLab.xcodeproj/project.pbxproj`, `ScratchLab/Services/CompanionCameraBroadcaster.swift`, `ScratchLab/Services/SessionExportCoordinator.swift`, `ScratchLab/Services/SessionUploadManager.swift`, `ScratchLab/Services/StagedCaptureRecovery.swift`, `ScratchLab/Services/WatchMotionCaptureStore.swift`, `ScratchLab/Views/CompanionCameraView.swift`, `ScratchLabDesktop/Services/CompanionCameraReceiver.swift`, `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`
- Build result: `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test -only-testing:ScratchLabDesktopTests/CaptureRecoveryPhase2CoreTests` passed, `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test -only-testing:ScratchLabDesktopTests` passed, `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test` passed, and `python3 scripts/test_capture_pipeline.py` passed
- Follow-up notes: added shared staged-capture recovery and quarantine logic, persisted compact take/session audit summaries, ran relaunch recovery for companion and routine capture stores, reconciled imported and relayed watch captures against exact `sessionID` + `takeID` sidecars, surfaced canonical export blocking issues in the operator-facing export UI/status path, and added deterministic XCTest coverage for interrupted-take recovery, orphaned media quarantine, watch-link reconciliation, unlinked watch quarantine, and interrupted-session export refusal; `TASKS.md` was left unchanged because this was a user-directed Phase 2 pass rather than the first unchecked backlog item

## 2026-04-20
- Selected task: Phase 3 staged capture inspection and recovery surface
- Files changed: `ScratchLab/Services/StagedCaptureRecovery.swift`, `ScratchLab/Services/StagingInspector.swift`, `ScratchLab/Views/StagingInspectorView.swift`, `ScratchLab/Services/CompanionCameraBroadcaster.swift`, `ScratchLab/Services/WatchMotionCaptureStore.swift`, `ScratchLab/Views/CompanionCameraView.swift`, `ScratchLabDesktop/Services/CompanionCameraReceiver.swift`, `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `ScratchLab.xcodeproj/project.pbxproj`, `DEV_LOG.md`
- Build result: `python3 scripts/test_capture_pipeline.py`, `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test -only-testing:ScratchLabDesktopTests`, `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test`, and `scripts/build.sh` all passed
- Follow-up notes: added a dedicated staging inspector, safe re-scan/reconcile actions, surfaced audit/quarantine/export-readiness state for staged sessions, and laid compact capture journal groundwork without widening into unrelated product flows

## 2026-03-12
- Selected task: remove temporary combo debug logging from practice mode
- Files changed: `ScratchLab/Views/PracticeModeView.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: `Scripts/build.sh` passed for both `ScratchLab` (generic iOS) and `ScratchLabDesktop` (macOS)
- Follow-up notes: removed combo challenge `print(...)` calls without changing phrase lock, scoring, or reset behavior

## 2026-03-12
- Selected task: remove verbose baby scratch training profile logging
- Files changed: `ScratchLab/Audio/AudioEngine.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: `Scripts/build.sh` passed for both `ScratchLab` (generic iOS) and `ScratchLabDesktop` (macOS)
- Follow-up notes: removed the baby-scratch training profile load `print(...)` without changing profile discovery, metric aggregation, or clamping

## 2026-03-12
- Selected task: add clearer empty-state copy to the Mac companion feed panel
- Files changed: `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: `Scripts/build.sh` passed for both `ScratchLab` (generic iOS) and `ScratchLabDesktop` (macOS)
- Follow-up notes: clarified the iPhone Companion Cam steps for a top-down deck view so the Mac app explains how the second camera feed appears without changing capture behavior

## 2026-03-12
- Selected task: default Companion Cam to deck view on launch
- Files changed: `ScratchLab/Services/CompanionCameraBroadcaster.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: `Scripts/build.sh` passed for both `ScratchLab` (generic iOS) and `ScratchLabDesktop` (macOS)
- Follow-up notes: Companion Cam now opens on the rear camera so iPhone-to-Mac deck capture testing starts in the recommended view with one less tap

## 2026-03-13
- Selected task: expose Continuity Desk View cameras in the Mac camera list
- Files changed: `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: `Scripts/build.sh` passed for both `ScratchLab` (generic iOS) and `ScratchLabDesktop` (macOS)
- Follow-up notes: camera discovery now includes Desk View cameras surfaced as companions of iPhone continuity cameras so the Mac analyzer can offer and select Desk View more reliably

## 2026-04-20
- Selected task: user-requested production-boundary and operator-runbook hardening for the frozen staging/export/recovery layer
- Files changed: `README.md`, `docs/current_architecture_reality.md`, `docs/capture_spec_v1.md`, `docs/pilot_session_runbook.md`, `docs/staging_operations_runbook.md`, `ScratchLab/Services/StagingInspector.swift`, `ScratchLab/Views/StagingInspectorView.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `DEV_LOG.md`
- Build result: `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test -only-testing:ScratchLabDesktopTests` passed, `python3 scripts/test_capture_pipeline.py` passed, `scripts/build.sh` passed for fixture tests plus the macOS XCTest suite and the iOS, macOS, and watchOS builds
- Follow-up notes: documented the production trust boundary and operator procedures for blocked export, quarantine, restore, and reconcile; added deterministic staging-operations test scaffolding plus inspector-state coverage; added summary badges and next-step guidance to the staging inspector without changing validation, recovery, or canonical export rules

## 2026-03-13
- Selected task: auto-connect a single nearby iPhone companion feed in the Mac analyzer
- Files changed: `ScratchLabDesktop/Services/CompanionCameraReceiver.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: `Scripts/build.sh` passed for both `ScratchLab` (generic iOS) and `ScratchLabDesktop` (macOS)
- Follow-up notes: the Mac analyzer now keeps the Companion Feed card visible, auto-invites the only nearby iPhone companion feed, and switches to Dual Cam when the feed connects so the single-phone workflow needs fewer manual steps

## 2026-04-24
- Selected task: fix the macOS routine export validation path so local takes stop failing with `audio artifact missing` when the capture itself completed
- Files changed: `ScratchLab/Services/SessionExportCoordinator.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `DEV_LOG.md`
- Build result: `scripts/build.sh` passed for the capture-pipeline fixture suite, the `ScratchLabDesktop` XCTest plan, and the iOS, macOS, and watchOS builds
- Follow-up notes: local routine-session packaging now resolves a stable sibling WAV artifact for each completed `.mov` by reusing an existing extracted file or deriving one from the embedded movie audio before validation/export, and routine takes now carry explicit `audioPresent`, `motionPresent`, watch sync status, and slate/clap flags instead of falling through with nil export metadata; `TASKS.md` was left unchanged because this was a user-directed reliability fix rather than the first unchecked backlog item

## 2026-04-24
- Selected task: harden local audio artifact derivation against real AAC-in-movie routine captures
- Files changed: `ScratchLab/Services/SessionExportCoordinator.swift`, `DEV_LOG.md`
- Build result: `scripts/build.sh` passed for the capture-pipeline fixture suite, the `ScratchLabDesktop` XCTest plan, and the iOS, macOS, and watchOS builds
- Follow-up notes: confirmed against a real routine capture in the app sandbox that the `.mov` contained a valid AAC stereo track but no sibling WAV; replaced the broken `AVAudioFile(forReading:)` movie extraction path with `AVAssetReader`-based LPCM extraction so validation/export can materialize the required WAV audio artifact from completed local takes

## 2026-04-24
- Selected task: stop routine-capture export from failing with `This session has inconsistent metadata.` just because only a recorded BPM subset is present
- Files changed: `ScratchLab/Services/SessionExportCoordinator.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `DEV_LOG.md`
- Build result: `scripts/build.sh` passed for the capture-pipeline fixture suite, the `ScratchLabDesktop` XCTest plan, and the iOS, macOS, and watchOS builds
- Follow-up notes: the canonical export validator now treats `routine_capture` packages as workflow-aware and accepts the BPMs that were actually recorded instead of requiring full `{70,90,110}` coverage on every staged routine session; added a regression test that validates a one-take 90 BPM routine export and confirms the generated manifest records only the recorded BPM subset

## 2026-04-24
- Selected task: fix the dead macOS Share Session host and move routine start/stop to the main control with a space-bar shortcut
- Files changed: `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `DEV_LOG.md`
- Build result: `scripts/build.sh` passed for the capture-pipeline fixture suite, the `ScratchLabDesktop` XCTest plan, and the iOS, macOS, and watchOS builds
- Follow-up notes: moved the macOS `SessionSharePresenter` off the share button background and onto the window-level tab host so the share picker has a stable anchor, removed the extra deck-calibration record button and lower upload cluster, and mapped the main routine recording button to the space bar for start/stop control without requiring the user to scroll

## 2026-04-24
- Selected task: replace free-text performer entry in macOS routine capture with persistent preset choices
- Files changed: `ScratchLab/Models/CaptureCore.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `DEV_LOG.md`
- Build result: `scripts/build.sh` fixture tests passed and the `ScratchLabDesktop` XCTest plan passed; the longer iOS/watch build legs were still running at response time
- Follow-up notes: added a shared preset performer list (`CXL`, `Shan`, `Vito`, `K`, `LongBeach`, `Brent`, `MisterP`, `Jayrasik`), changed the macOS routine session setup from a text field to a dropdown picker, and persisted the last selected performer name in macOS `AppStorage` so new routine sessions restore the previous choice automatically

## 2026-03-13
- Selected task: clarify that one iPhone cannot run Continuity Camera and Companion Cam at the same time
- Files changed: `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLab/Views/CompanionCameraView.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: `Scripts/build.sh` passed for both `ScratchLab` (generic iOS) and `ScratchLabDesktop` (macOS)
- Follow-up notes: added workflow warnings on Mac and iPhone so the app explains that the same iPhone must be used either as Continuity/Desk View or as ScratchLab Companion Cam, not both at once

## 2026-04-24
- Selected task: user-reported fix for iOS capture finalization hanging on the `Finishing Recording` banner
- Files changed: `ScratchLab/Views/CompanionCameraView.swift`, `DEV_LOG.md`
- Build result: `scripts/build.sh` passed for fixture tests, the `ScratchLabDesktop` XCTest plan, and the iOS, macOS, and watchOS builds
- Follow-up notes: moved media audio probing and capture thumbnail generation onto async AVFoundation paths instead of blocking the main actor with semaphore waits, so the capture flow can leave the saving state after stop-recording finalization completes

## 2026-04-24
- Selected task: user-reported correction for scratch-specific BPM options in iOS and macOS session setup
- Files changed: `ScratchLab/Models/CaptureCore.swift`, `ScratchLab/Views/CompanionCameraView.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `DEV_LOG.md`
- Build result: `scripts/build.sh` passed for fixture tests, the `ScratchLabDesktop` XCTest plan, and the iOS, macOS, and watchOS builds
- Follow-up notes: added the shared training BPM matrix for scratch families, normalized selected BPM when scratch type changes, wired iOS/iPad and macOS pickers to the shared scratch-specific list, and added regression coverage for the requested BPM groups; `TASKS.md` was left unchanged because this was a user-directed correction with no unchecked implementation-ready backlog item

## 2026-04-24
- Selected task: user-requested half-time combo BPM correction before repushing the iPad build
- Files changed: `ScratchLab/Models/CaptureCore.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `DEV_LOG.md`
- Build result: `scripts/build.sh` passed for fixture tests, the `ScratchLabDesktop` XCTest plan, and the iOS, macOS, and watchOS builds
- Follow-up notes: Combo L1-L5 now use `70, 80, 95, 105, 125`, treating the old `140` as half-time `70`, keeping `80` as the half-time fast slot, and replacing the old `160` entry with `125`

## 2026-04-24
- Selected task: user-reported iPad saving hang after changing scratch type and recording another take
- Files changed: `ScratchLab/Models/CaptureCore.swift`, `ScratchLab/Services/CompanionCameraBroadcaster.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `DEV_LOG.md`
- Build result: `scripts/build.sh` passed for fixture tests, the `ScratchLabDesktop` XCTest plan, and the iOS, macOS, and watchOS builds
- Follow-up notes: recording summaries now identify completed takes by `sessionID` plus `takeID` so a new session's first `take-001` is not ignored as a duplicate of the previous session's first take; `TASKS.md` was left unchanged because this was a user-directed correction with no unchecked implementation-ready backlog item

## 2026-04-25
- Selected task: retake macOS App Review screenshots with no-decks-safe real UI states
- Files changed: `TASKS.md`, `DEV_LOG.md`, `build/app-review-mac-screenshots/final/01-session-workspace.png`, `build/app-review-mac-screenshots/final/02-new-session-selected.png`, `build/app-review-mac-screenshots/final/03-ready-to-record-and-metadata.png`, `build/app-review-mac-screenshots/final/04-stage-and-audio-routing.png`, `build/app-review-mac-screenshots/final/05-empty-state.png`, `build/app-review-mac-screenshots/final/06-recording-in-progress.png`, `build/app-review-mac-screenshots/final/07-captured-result-or-export-ready.png`
- Build result: `./scripts/build.sh` passed, and `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -configuration Release -destination 'platform=macOS' build` passed
- Follow-up notes: recaptured the final seven Mac App Review screenshots from the real Release app window using seeded review data and a real desk/controller camera frame; captured `06-recording-in-progress.png` during an actual active routine recording with `Stop Recording` and the red recording item visible; recaptured `07-captured-result-or-export-ready.png` from the seeded completed-take state with `Ready to export`, `Save ZIP to Downloads`, and `Share Session` visible and no failure messaging; deleted only `RoutineSessionDrafts.json` files before recapturing `05-empty-state.png` last

## 2026-04-26
- Selected task: minimal App Review fix pass for sandboxed `Save ZIP...`, shipping review-risk audio resources, and hard-coded development audio fallbacks
- Files changed: `ScratchLab/Services/SessionExportCoordinator.swift`, `ScratchLabDesktop/ScratchLabDesktop.entitlements`, `ScratchLab/Audio/AudioEngine.swift`, `ScratchLabDesktop/Services/MacScratchDetector.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `ScratchLab.xcodeproj/project.pbxproj`, `TASKS.md`, `DEV_LOG.md`
- Build result: focused `ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests` passed (`55/55`), `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -configuration Release -destination 'platform=macOS' build` passed, `xcodebuild -project ScratchLab.xcodeproj -target ScratchLabWatch -sdk watchsimulator CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO build` passed, and `./scripts/build.sh` passed end-to-end
- Follow-up notes: added `com.apple.security.files.user-selected.read-write` to the macOS entitlements and wrapped the shared archive copy path in security-scoped access for the chosen save destination or parent folder; removed `cxl_scratch_library`, `reference_pro`, `reference_champ`, `reference_beginner`, and `boom_bap_100bpm.wav` from shipping app resource phases without deleting repo files; removed hard-coded `/Users/...` training-audio fallbacks from the shared iOS and macOS detector paths while keeping bundle/fallback discovery safe; added regression coverage for the entitlement, security-scoped copy helper, shipping-resource removal, and hard-coded-path removal; manually verified `Save ZIP...` in the real macOS app for the suggested Documents path, a custom `~/Documents/ScratchLab Review Exports` folder with spaces, and the overwrite/replace path; live record-button smoke automation remained flaky through the accessibility bridge after the save-flow checks, so calibration/timed capture behavior was re-verified by the existing passing regression suite rather than a completed second manual UI capture run in this pass

## 2026-05-02
- Selected task: fix ScratchLab Coach movement randomness in Demo Mode
- Files changed: `ScratchLab/Models/CaptureCore.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: focused Demo Mode/no-random/repeatability/live-analyzer regressions passed, and `./scripts/build.sh` passed for fixture tests, the full macOS XCTest plan, and the iOS, macOS, and watchOS build legs
- Follow-up notes: replaced the Demo Mode audio-envelope motion proxy with a deterministic Baby Scratch motion pattern keyed to `demoPlayer.currentPlaybackTime`; the pattern performs forward, backward, and center reset phases with deterministic Balanced feedback, while the bundled WAV level is used only as a deterministic activity gate. Live capture still publishes feedback through `AudioEngine` and `ScratchMotionAnalyzer`; final source checks found no random tokens in the Demo Mode motion range and no demo-pattern references in the live analyzer paths.

## 2026-05-02
- Selected task: refine Demo Mode coach animation so movement only occurs during active scratch audio
- Files changed: `ScratchLab/Models/CaptureCore.swift`, `ScratchLab/Views/MainMenuView.swift`, `ScratchLab/Views/ScratchCoachViews.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: focused Demo Mode/audio-gate/repeatability/live-analyzer regressions passed, and `./scripts/build.sh` passed for fixture tests, the full macOS XCTest plan, and the iOS, macOS, and watchOS build legs
- Follow-up notes: Demo Mode now precomputes a smoothed RMS/peak activity envelope from bundled `CoachDemoAudio/baby_noBeat.wav`, returns a neutral coach pose below the active-energy threshold, and uses the existing fixed Baby Scratch phase pattern only while the audio envelope is active. The visible coach rig now receives the `ScratchLabDemoModeController` animation state keyed to `demoPlayer.currentPlaybackTime`; live capture analysis paths remain free of Demo Mode motion-pattern references. Final checks found no random tokens in the Demo Mode motion range, removed regenerated `scripts/__pycache__`, and `git diff --check` passed.

## 2026-05-02
- Selected task: fix Demo Mode coach motion so it hard-stops during silence and holds between strokes
- Files changed: `ScratchLab/Models/CaptureCore.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: focused Demo Mode stop/hold/audio-gate/live-analyzer regressions passed, and `./scripts/build.sh` passed for fixture tests, the full macOS XCTest plan, and the iOS, macOS, and watchOS build legs
- Follow-up notes: tightened the Demo Mode audio gate with deterministic hysteresis (`0.20` on, `0.10` off), hard-zeroed inactive envelope frames, and added an active-phase timeline so the Baby Scratch phase clock freezes through silence. The Baby Scratch demo pattern now runs forward stroke, forward hold, backward stroke, and backward hold instead of a continuous oscillator, while inactive audio overrides the whole output to `.neutral` with a Debug assertion. New regressions cover quiet-frame identity, active motion, fixed hold phases, inactive neutral override, substantial neutral-frame coverage in bundled `baby_noBeat.wav`, repeatability, no random values, and unchanged live analyzer paths.

## 2026-05-02
- Selected task: align Demo Mode coach motion to scratch audio bursts
- Files changed: `ScratchLab/Models/CaptureCore.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: focused Demo Mode timing/no-random/live-analyzer regressions passed, and `./scripts/build.sh` passed for fixture tests, the full macOS XCTest plan, and the iOS, macOS, and watchOS build legs
- Follow-up notes: the prior silence gate stopped quiet frames, but the active Baby Scratch phase still accumulated across separate audio bursts, so a new scratch could resume mid-stroke and look out of time. Demo Mode now extracts contiguous active energy segments from bundled `CoachDemoAudio/baby_noBeat.wav`, stores segment-local time/duration/direction for each analysis frame, alternates forward and backward strokes per burst, and drives the coach rig from `demoPlayer.currentPlaybackTime` into the matching burst-local stroke. Silence still returns `.neutral`, same-timestamp frames are repeatable, and live capture analysis paths remain untouched.

## 2026-05-02
- Selected task: fix the lower scroll-down Coach Rig timing and mixer layout
- Files changed: `ScratchLab/Models/CaptureCore.swift`, `ScratchLab/Views/ScratchCoachViews.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: focused lower Coach Rig regressions passed, and `./scripts/build.sh` passed for fixture tests, the full macOS XCTest plan, and the iOS, macOS, and watchOS build legs
- Follow-up notes: the previous fix only affected the Try Demo custom animation-provider path. The lower scrollable Coach Rig is the shared `ScratchCoachRigView` rendered by `ScratchCoachCardContent` in the Practice and macOS Scratch Rating coach cards, and it was still using the time-only `ScratchCoachDemoAnimator.state(...)` fallback. Fixed the shared component so it loads a bundled demo-audio activity profile from `instruction.demoAudioFile`, drives lower-rig animation from `ScratchLabDemoAudioSampleBuffer.coachRigAnimationState(...)`, returns neutral when playback is paused/inactive or the audio profile is not ready, keeps the tone arm static, and lays out vertical volume faders above the crossfader explicitly. Try Demo keeps using its custom synced provider, and live capture/audio analyzer paths were not changed.

## 2026-05-02
- Selected task: split Baby Scratch lower Coach Rig demo timing into four actual stroke segments
- Files changed: `ScratchLab/Models/CaptureCore.swift`, `ScratchLab/Views/ScratchCoachViews.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: `./scripts/build.sh` passed for capture-pipeline fixture tests, the full `ScratchLabDesktop` XCTest plan, and the iOS, macOS, and watchOS build legs; `git diff --check` passed
- Follow-up notes: the lower Coach Rig still treated `baby_noBeat.wav` as a uniform Baby Scratch loop. Short-window RMS/peak inspection found the first four clear stroke windows at `0.03-0.21s`, `0.36-0.49s`, `0.78-1.05s`, and `1.33-1.55s`, so `ScratchLabBabyScratchDemoMotionPattern` now uses an explicit four-stroke manual fallback timeline with alternating forward/backward directions and per-stroke hold windows. The second stroke is shorter than the first, same-timestamp output is repeatable, hold windows are static, lower-rig moving strokes remain gated by the bundled audio activity profile, the tone arm rendering path was removed from `ScratchCoachRigView`, the crossfader remains centered/open, the left hand remains fixed, and the radial sticker line remains tied to record rotation. Regenerated `scripts/__pycache__` was removed after verification.

## 2026-05-02
- Selected task: build Baby Scratch reference-video-driven coach motion from MKV plus WAV timing
- Files changed: `ScratchLab/Models/CaptureCore.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: focused Baby Scratch reference/timing regressions passed (`7/7`), `./scripts/build.sh` passed for capture-pipeline fixture tests, the full `ScratchLabDesktop` XCTest plan (`187/187`), and the iOS, macOS, and watchOS build legs; `git diff --check` passed
- Follow-up notes: the lower rig needed a source-aware reference timeline rather than a zero-based uniform phrase. Located the Baby visual reference at `/Users/karlwatson/Movies/CXL DATASET/processed_makemkv/baby/79bpm/angle_1_video.mkv`, the matching no-beat source audio at `/Users/karlwatson/Movies/CXL DATASET/processed_makemkv/baby/79bpm/angle_1_noBeat.wav`, and the bundled app audio at `ScratchLab/Resources/CoachDemoAudio/baby_noBeat.wav`. Raw chapter inspection shows Baby Chapter 2 starts at `35.035000s` and ends at `83.450033s`; the bundled WAV duration is `48.415042s`, matching that span. Added `BabyScratchReferenceMotionTimeline` with explicit `demoStart`/`demoEnd`, source-time mapping where app playback `0` maps to source `35.035`, non-uniform stroke/keyframe data from the first audible Baby phrase, linked hand/sticker/record progress, static holds, no random values, and unchanged lower-rig geometry. The timeline was hand-authored from MKV frame inspection plus WAV RMS/peak transient timing; remaining risk is visual fine-tuning if a frame-by-frame labeled capture pass is desired.

## 2026-05-02
- Selected task: fix Baby Scratch reference motion timeline so it does not skip alternating strokes
- Files changed: `ScratchLab/Models/CaptureCore.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: focused Baby Scratch timeline/lower-rig regressions passed (`8/8`), `./scripts/build.sh` passed for capture-pipeline fixture tests, the full `ScratchLabDesktop` XCTest plan (`188/188`), and the iOS, macOS, and watchOS build legs; `git diff --check` passed
- Follow-up notes: the previous 7-segment timeline still came from coarse active-energy islands, so some islands contained a forward/back pair and the coach appeared to catch only every second movement. Reinspected `ScratchLab/Resources/CoachDemoAudio/baby_noBeat.wav` with 5 ms RMS/peak windows and a Chapter 2 deck crop from `/Users/karlwatson/Movies/CXL DATASET/processed_makemkv/baby/79bpm/angle_1_video.mkv`, then split the first phrase into 12 alternating motion segments: `0.0400-0.1250` forward, `0.1250-0.2000` backward, `0.3600-0.4200` forward, `0.4200-0.4800` backward, `0.7900-0.9050` forward, `0.9250-1.0450` backward, `1.3450-1.4550` forward, `1.4900-1.5300` backward, `1.6400-1.7700` forward, `1.8700-1.9900` backward, `2.0100-2.1200` forward, and `2.2550-2.3600` backward. The second forward/back pair is shorter than the first (`0.120s` vs `0.160s`), the directions now alternate in source order, and lower-rig geometry, tone-arm removal, centered/open crossfader, fixed left hand, and linked hand/sticker/record progress were unchanged.

## 2026-05-02
- Selected task: add multi-angle Baby Scratch reference support
- Files changed: `ScratchLab/Models/CaptureCore.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: focused multi-angle/timeline regressions passed (`6/6`), `./scripts/build.sh` passed for capture-pipeline fixture tests, the full `ScratchLabDesktop` XCTest plan (`190/190`), and the iOS, macOS, and watchOS build legs; `git diff --check` passed
- Follow-up notes: located the Baby Scratch 79 BPM reference inputs under `/Users/karlwatson/Movies/CXL DATASET/processed_makemkv/baby/79bpm`: `angle_1_video.mkv`, `angle_2_video.mkv`, `angle_3_video.mkv`, `angle_4_video.mkv`, plus matching no-beat WAVs. Added a Debug-only `BabyScratchReferenceAsset.babyScratch79BPM` manifest so `angle_1_noBeat.wav` is the WAV timing source and all four MKVs are visual-reference-only inputs for hand-authored timeline validation. Angle 1 is marked primary, angles 2-4 are validation references, and the manifest records validation focus for hand position, record/sticker movement, direction changes, hold phases, and stroke speed. Automatic video tracking remains explicitly disabled, the embedded `BabyScratchReferenceMotionTimeline` remains the motion driver, and Demo Mode/lower Coach Rig source scans confirm no raw MKV playback path, `AVPlayer`, or `VideoPlayer` was added.

## 2026-05-02
- Selected task: tune Baby Scratch reference motion timeline for scratches 2-4
- Files changed: `ScratchLab/Models/CaptureCore.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: focused Baby Scratch timeline regressions passed (`7/7`), `./scripts/build.sh` passed for capture-pipeline fixture tests, the `ScratchLabDesktop` XCTest plan, and the iOS, macOS, and watchOS build legs; `git diff --check` passed
- Follow-up notes: reinspected `/Users/karlwatson/Movies/CXL DATASET/processed_makemkv/baby/79bpm/angle_1_noBeat.wav` and `/Users/karlwatson/Movies/CXL DATASET/processed_makemkv/baby/79bpm/angle_1_video.mkv` at source offset `demoStart = 35.035`. The timing-only tune shortened scratch 2 from `0.3600-0.4800s` to `0.3750-0.4800s`, corrected scratch 3 from `0.7900-0.9050s` forward plus `0.9250-1.0450s` backward to `0.7900-0.8900s` forward plus `0.9350-1.0450s` backward, and moved scratch 4 earlier from `1.3450-1.4550s` forward plus `1.4900-1.5300s` backward to `1.3050-1.4100s` forward plus `1.4100-1.5000s` backward. Geometry constants, rig layout, tone-arm removal, centered/open crossfader, left-hand behavior, sticker shape, live capture, export/upload, signing, deployment targets, bundle IDs, and version/build values were not changed in this pass. Regenerated `scripts/__pycache__` and temporary scratch-4 analysis frames were removed after verification.

## 2026-05-02
- Selected task: split Baby Scratch reference motion timeline into four chigas / eight strokes
- Files changed: `ScratchLab/Models/CaptureCore.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: focused Baby Scratch/lower-rig regressions passed (`9/9`); the first full `./scripts/build.sh` run exposed one stale hold-phase test expectation, the expectation was corrected, and the rerun passed for capture-pipeline fixture tests, the full `ScratchLabDesktop` XCTest plan (`191/191`), and the iOS, macOS, and watchOS build legs; `git diff --check` passed
- Follow-up notes: root cause was that the prior timeline still contained a 12-segment split, subdividing some actual record movements and adding extra motion beyond the intended four-chiga phrase. Reinspected `/Users/karlwatson/Movies/CXL DATASET/processed_makemkv/baby/79bpm/angle_1_noBeat.wav` and `/Users/karlwatson/Movies/CXL DATASET/processed_makemkv/baby/79bpm/angle_1_video.mkv` at `demoStart = 35.035`, then replaced the stroke list with eight explicit movements: slow forward `0.0400-0.2000`, slow backward `0.3750-0.4800`, fast forward `0.7900-0.8900`, fast backward `0.9350-1.0450`, slow forward `1.3050-1.5400`, slow backward `1.6400-1.7700`, fast forward `1.8700-1.9900`, and fast backward `2.2550-2.3600`. The fast chiga movement durations are shorter than the paired slow chigas, directions alternate forward/back, hand/record/sticker still share one progress value, and geometry, layout, tone-arm removal, sticker line, centered/open crossfader, left-hand behavior, live capture, export/upload, signing, deployment targets, bundle IDs, and version/build values were not changed. Regenerated `scripts/__pycache__` was removed after verification.

## 2026-05-02
- Selected task: split Baby Scratch Scratch 2 into fast chiga x6
- Files changed: `ScratchLab/Models/CaptureCore.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: focused Baby Scratch/lower-rig regressions passed (`10/10`), and `./scripts/build.sh` passed for capture-pipeline fixture tests (`36/36`), the full `ScratchLabDesktop` XCTest plan (`191/191`), and the iOS, macOS, and watchOS build legs; regenerated `scripts/__pycache__` was removed and `git diff --check` passed
- Follow-up notes: root cause was that the current Baby Scratch timeline had collapsed Scratch 2 to a single fast chiga, so the lower Coach Rig could still miss the repeated fast forward/back movements. Reinspected `/Users/karlwatson/Movies/CXL DATASET/processed_makemkv/baby/79bpm/angle_1_noBeat.wav` around `demoStart = 35.035` with 5 ms RMS/peak windows, kept Scratch 1 as the slow chiga (`0.0400-0.2000` forward, `0.3750-0.4800` backward), and replaced the old Scratch 2 one-chiga timing (`0.7900-0.8900` forward plus `0.9350-1.0450` backward) with six fast chiga pairs: `0.7900-0.8500` forward, `0.8500-0.9050` backward, `0.9350-0.9850` forward, `0.9850-1.0450` backward, `1.3450-1.4400` forward, `1.4400-1.5400` backward, `1.6400-1.7050` forward, `1.7050-1.7700` backward, `1.8700-1.9900` forward, `1.9900-2.1250` backward, `2.2550-2.3050` forward, and `2.3050-2.3600` backward. The section now has 14 total stroke segments, directions alternate without skipped every-second movement, each Scratch 2 fast stroke is shorter than the Scratch 1 forward stroke, and geometry/layout/sticker shape/tone-arm removal/crossfader/left hand/live capture/export/upload/signing/deployment targets/bundle IDs/version/build values were not changed in this pass.

## 2026-05-02
- Selected task: fix Scratch 2 fast chiga timeline continuity
- Files changed: `ScratchLab/Models/CaptureCore.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: focused Baby Scratch continuity/lower-rig regressions passed (`7/7`) after correcting one four-decimal rounding tolerance, and `./scripts/build.sh` passed for capture-pipeline fixture tests (`36/36`), the full `ScratchLabDesktop` XCTest plan (`191/191`), and the iOS, macOS, and watchOS build legs; regenerated `scripts/__pycache__` was removed and `git diff --check` passed
- Follow-up notes: root cause was that Scratch 2 had the right twelve fast strokes but still carried explicit gaps and `holdAfter` windows between several forward/back segments, so the motion could feel like separate bursts instead of one continuous fast chiga chain. Kept Scratch 1 unchanged and redistributed Scratch 2 continuously from `0.7900` through `2.3600`: `0.7900-0.9208` forward, `0.9208-1.0517` backward, `1.0517-1.1825` forward, `1.1825-1.3133` backward, `1.3133-1.4442` forward, `1.4442-1.5750` backward, `1.5750-1.7058` forward, `1.7058-1.8367` backward, `1.8367-1.9675` forward, `1.9675-2.0983` backward, `2.0983-2.2292` forward, and `2.2292-2.3600` backward. Every Scratch 2 segment now starts at the previous Scratch 2 segment's `endTime`, all Scratch 2 `holdAfter` values are `0`, directions still alternate forward/back, and geometry, sticker/hand mapping, crossfader, left hand, tone-arm behavior, live capture, export/upload, signing, deployment targets, bundle IDs, and version/build values were not changed.

## 2026-05-02
- Selected task: replace Baby Scratch hand-tuned timeline with WAV extractor JSON
- Files changed: `ScratchLab/Models/CaptureCore.swift`, `ScratchLab/Resources/CoachDemoMotion/baby_scratch_strokes.json`, `ScratchLab.xcodeproj/project.pbxproj`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `scripts/extract_baby_scratch_strokes.py`, `TASKS.md`, `DEV_LOG.md`
- Build result: focused extractor/timeline/lower-rig regressions passed (`10/10`), and `./scripts/build.sh` passed for capture-pipeline fixture tests (`36/36`), the full `ScratchLabDesktop` XCTest plan (`193/193`), and the iOS, macOS, and watchOS build legs; regenerated `scripts/__pycache__` was removed and `git diff --check` passed
- Follow-up notes: identified `0003b5c` as the last stable commit before Baby Scratch reference timeline hand-tuning and `4e96478` plus later working-tree edits as the hand-tuned timeline changes to replace. Preserved the lower Coach Rig geometry fixes already in the working tree, but removed the in-code 14-stroke fast-chiga list as the primary timing source. Added a dependency-free offline extractor for `/Users/karlwatson/Movies/CXL DATASET/processed_makemkv/baby/79bpm/angle_1_noBeat.wav`, generated bundled JSON with 54 WAV-detected stroke windows for `demoStart 35.035` through `demoEnd 83.450033`, bundled that resource for iOS and macOS, and made `BabyScratchReferenceMotionTimeline` prefer the JSON while keeping a simple deterministic two-stroke fallback if the resource is unavailable. The bundled JSON contains timing data only and no local source path/provenance strings.

## 2026-05-02
- Selected task: add ScratchLab notation model and Baby Scratch extractor
- Files changed: `ScratchLab/Models/CaptureCore.swift`, `ScratchLab/Resources/Notation/baby_scratch.json`, `ScratchLab.xcodeproj/project.pbxproj`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `scripts/extract_scratch_notation.py`, `TASKS.md`, `DEV_LOG.md`
- Build result: focused notation/timeline/lower-rig regressions passed (`10/10`), and `./scripts/build.sh` passed for capture-pipeline fixture tests (`36/36`), the full `ScratchLabDesktop` XCTest plan (`195/195`), and the iOS, macOS, and watchOS build legs; regenerated `scripts/__pycache__` was removed and `git diff --check` passed
- Follow-up notes: added a shared `ScratchNotation` model with stroke start/end time, direction, speed classification, and fader state, plus `scripts/extract_scratch_notation.py` to convert the Baby Scratch Chapter 2 no-beat WAV into notation. The extractor uses WAV transient-derived stroke windows for timing, alternates forward/back directions, classifies speed by duration percentile, and writes `ScratchLab/Resources/Notation/baby_scratch.json` with 54 strokes and no local source/provenance strings. `BabyScratchReferenceMotionTimeline` now prefers notation JSON, falls back to the existing extracted-stroke JSON if notation is unavailable, and then falls back to the simple deterministic Baby Scratch pattern. MKV angles remain Debug manifest visual-reference inputs only and are not used for timing segmentation or user-facing video playback.

## 2026-05-02
- Selected task: limit Baby Scratch extractor output to the first phrase
- Files changed: `ScratchLab/Models/CaptureCore.swift`, `ScratchLab/Resources/CoachDemoMotion/baby_scratch_strokes.json`, `ScratchLab/Resources/Notation/baby_scratch.json`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `scripts/extract_baby_scratch_strokes.py`, `scripts/extract_scratch_notation.py`, `TASKS.md`, `DEV_LOG.md`
- Build result: focused notation/timeline/lower-rig regressions passed (`6/6`), the lower-rig timeline regression passed, and `./scripts/build.sh` passed for capture-pipeline fixture tests (`36/36`), the full `ScratchLabDesktop` XCTest plan (`195/195`), and the iOS, macOS, and watchOS build legs
- Follow-up notes: the extractor was still emitting every Chapter 2 phrase, so the notation resource contained 54 strokes and a full-chapter timing span. Added first-phrase selection after WAV transient-window detection: the extractor keeps the first dense cluster and stops at the first clear silence gap over `0.300s`. Regenerated notation and fallback motion JSON now use `phraseStart 0.0300`, `phraseEnd 2.3830`, and 14 alternating strokes for one Baby Scratch phrase: two slower strokes followed by twelve faster strokes. The model now decodes optional phrase bounds and uses `phraseEnd` as the notation timeline duration, so the coach timeline no longer stretches over the full `48s` Chapter 2 window. No coach-rig geometry, layout, live capture, export/upload, signing, deployment target, bundle ID, or version/build settings were changed.

## 2026-05-02
- Selected task: fix Baby Scratch phrase continuity
- Files changed: `ScratchLab/Models/CaptureCore.swift`, `ScratchLab/Resources/CoachDemoMotion/baby_scratch_strokes.json`, `ScratchLab/Resources/Notation/baby_scratch.json`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `scripts/extract_baby_scratch_strokes.py`, `TASKS.md`, `DEV_LOG.md`
- Build result: focused Baby Scratch continuity regressions passed, and `./scripts/build.sh` passed for capture-pipeline fixture tests (`36/36`), the full `ScratchLabDesktop` XCTest plan (`195/195`), and the iOS, macOS, and watchOS build legs
- Follow-up notes: the first-phrase extraction had the correct 14 strokes, but the fast chiga section still started too late after the slow backward stroke. Added a continuity compaction step to the Baby Scratch extractor so the first fast forward starts at `0.580s`, `0.077s` after the slow backward ends at `0.503s`, and all later fast strokes shift by the same delta while keeping durations, directions, and speed classifications unchanged. Regenerated notation and fallback motion JSON now use `phraseStart 0.0300`, `phraseEnd 2.1830`, 14 alternating strokes, two slow classifications followed by twelve fast classifications, and no source/provenance strings. The Baby demo/lower-rig motion path now honors notation moving windows for shifted strokes even when the raw instantaneous audio gate is low; coach-rig geometry, live capture, export/upload, signing, deployment target, bundle ID, and version/build settings were not changed. Regenerated `scripts/__pycache__` was removed and `git diff --check` passed.

## 2026-05-02
- Selected task: final Baby Scratch timing alignment tweak
- Files changed: `ScratchLab/Resources/CoachDemoMotion/baby_scratch_strokes.json`, `ScratchLab/Resources/Notation/baby_scratch.json`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `scripts/extract_baby_scratch_strokes.py`, `TASKS.md`, `DEV_LOG.md`
- Build result: focused Baby Scratch timing regressions passed (`6/6`), the stale hold-phase regression passed after retiming its sample into the shortened transition window, and `./scripts/build.sh` passed for capture-pipeline fixture tests (`36/36`), the full `ScratchLabDesktop` XCTest plan (`195/195`), and the iOS, macOS, and watchOS build legs
- Follow-up notes: the phrase structure was correct but the slow-to-fast transition still had a `0.077s` delay. Reduced the extractor's `FAST_CHIGA_GAP_AFTER_SLOW_SECONDS` to `0.020`, regenerated the notation and fallback motion JSON, and kept all fast stroke durations and directions unchanged. The updated phrase still has 14 alternating strokes; slow backward ends at `0.503s`, fast forward starts at `0.523s`, the transition gap is `0.020s`, and `phraseEnd` is now `2.126s`. Coach-rig geometry, classifications, live capture, export/upload, signing, deployment target, bundle ID, and version/build settings were not changed. Regenerated `scripts/__pycache__` was removed, bundled motion/notation resources contain no source/provenance strings, and `git diff --check` passed.

## 2026-05-02
- Selected task: fix Baby Scratch coach notation timebase
- Files changed: `ScratchLab/Models/CaptureCore.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: focused Baby Scratch coach timing regressions passed, and `./scripts/build.sh` passed for capture-pipeline fixture tests (`36/36`), the full `ScratchLabDesktop` XCTest plan (`199/199`), and the iOS, macOS, and watchOS build legs
- Follow-up notes: the coach was loading `Notation/baby_scratch.json`, not the old hardcoded fallback, but the pose path wrapped playback through `phraseDuration`, so the 14-stroke phrase restarted every `2.126s` instead of staying on the actual bundled WAV timebase. The notation-driven pose also used eased progress, making fast strokes visually lag their timestamps. Fixed the shared Baby Scratch timeline so pose time is bounded audio playback time, `demoStart` is kept only for source-reference mapping, notation does not wrap before the WAV loops, and hand/sticker/record progress is linear and immediate. Added Debug timing probes for `0.503s`, `0.523s`, and `0.7136s`, plus regressions for JSON loading, 14 strokes, first fast forward at `0.523s`, no double offset, no fallback when JSON exists, no premature phrase wrap, and no SwiftUI animation lag in the lower shared rig path.

## 2026-05-02
- Selected task: fix Baby Scratch coach playback loop mapping
- Files changed: `ScratchLab/Models/CaptureCore.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: focused Baby Scratch loop/timing regressions passed (`7/7`), and `./scripts/build.sh` passed for capture-pipeline fixture tests (`36/36`), the full `ScratchLabDesktop` XCTest plan (`199/199`), and the iOS, macOS, and watchOS build legs
- Follow-up notes: the prior timebase fix removed all notation wrapping, but the bundled `baby_noBeat.wav` is a `48.415042s` full demo file with eight repeated phrase/silence cycles, not a one-shot `2.096s` notation phrase. `ScratchCoachDemoAudioPlayer` does not set `AVAudioPlayer.numberOfLoops`; Demo Mode only replays after full-file playback stops. Added explicit loop modes for the Baby Scratch notation timebase: default full-demo audio mapping repeats notation on the `~6.051879s` phrase/silence cycle, `.notationPhrase` wraps immediately from `phraseEnd` back to `phraseStart` for phrase-only playback, and `.disabled` preserves raw bounded playback time for validation. The first stroke now starts at `0.030s`, `0.523s` maps to the first fast forward stroke, post-phrase silence at `2.203s` stays neutral in full-WAV mode, and the next audio cycle restarts the notation instead of leaving the coach still forever. No notation JSON, extractor, geometry, live capture, export/upload, signing, deployment target, bundle ID, or version/build settings were changed.

## 2026-05-06
- Selected task: continue dataset export validation/fix for validator-ready 70/90/110 Baby Scratch session exports
- Files changed: `ScratchLab/Services/SessionExportCoordinator.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `scripts/test_capture_pipeline.py`, `scripts/validate_session.py`, `DEV_LOG.md`
- Build result: `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' build` passed, `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test` passed (`295/295` XCTest plus `13/13` Swift Testing notation timeline tests), `./scripts/build.sh` passed the capture-pipeline fixtures (`38/38`) and full `ScratchLabDesktop` XCTest leg, then failed on the pre-existing environment issue that the local Xcode install does not include the iOS 26.4 platform required for the `ScratchLab` iOS build leg; `git diff --check` passed
- Follow-up notes: diagnosed the real 110 BPM validator failure from `~/Desktop/session_2026_05_06_unknown_performer_baby_scratch_110_bpm` down to stale artifact probe metadata in that already-authored export. The old manifest recorded `serato` WAV `sample_width_bytes: 4` even though the exported files on disk are 16-bit WAVs (`2` bytes), and it also recorded `camA` duration/fps values that differ slightly from `ffprobe` container output. Updated the validator to compare video probe metadata semantically with tight duration/fps tolerances while still requiring exact audio probe matches, so valid AVFoundation-vs-ffprobe container duration drift no longer fails otherwise-correct exports. Kept the audio probe strict so stale wrong bit depth still fails. Updated shared export folder/session-root naming to include actual BPM coverage (for example `..._70_90_110_bpm`) instead of falsely implying a single BPM when multiple groups are exported, and added regressions for multi-BPM `session_root`, take-log parity, no absolute `/Users/` paths in manifest JSON, tolerant video probe validation, and strict audio bit-depth validation. Rerunning the real validator after the fix removed the false `camA` errors, but the existing Desktop export still fails on the four `serato` audio probe mismatches, which means that specific export must be regenerated with the current app code to become validator-ready.

## 2026-05-06
- Selected task: add canonical notation sidecars to session export and validate them without faking detector output
- Files changed: `ScratchLab/Services/SessionExportCoordinator.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `scripts/capture_pipeline_common.py`, `scripts/rename_files.py`, `scripts/test_capture_pipeline.py`, `scripts/validate_session.py`, `templates/session_manifest_example.json`, `DEV_LOG.md`
- Build result: `python3 -m py_compile scripts/capture_pipeline_common.py scripts/rename_files.py scripts/validate_session.py scripts/test_capture_pipeline.py` passed, `python3 scripts/test_capture_pipeline.py` passed (`39/39`), `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' build` passed, `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test` passed (`295/295` XCTest plus `13/13` Swift Testing notation timeline tests), `./scripts/build.sh` passed the capture-pipeline fixture suite and full `ScratchLabDesktop` XCTest leg, then failed on the pre-existing local Xcode issue that the iOS 26.4 platform is not installed for the `ScratchLab` iOS build leg; `git diff --check` passed
- Follow-up notes: the canonical export schema had no notation payload at all, so valid multi-BPM exports still could not carry honest detected/unavailable notation state into CXL review. Added a shared `notation/` directory to canonical exports, per-take `take-XXX_detected_notation.json` sidecars, manifest `files.notation` references, and matching notation availability fields in the export metadata. The current export path does not synthesize scratch events, so it now writes honest `notationSource: unavailable` documents with real `sessionID`, `takeID`, `takeNumber`, `scratchType`, `bpm`, `captureMode`, `notes`, optional review confidence, and empty `recordMovementEvents`, `faderEvents`, and `mixerMidiEvents` arrays instead of omitting the structure. Updated the Python capture-pipeline fixtures to emit the same notation files, and extended the validator so `notationSource == unavailable` warns but does not fail, while still requiring the notation JSON, array fields, IDs, and no absolute `/Users/` paths. Reran the validator against the old Desktop export and confirmed it now fails for the right historical reasons: the already-authored package is missing `notation/` entirely and still contains stale `serato` probe metadata, so a fresh export from the current app build is required for final dataset validation.

## 2026-05-06
- Selected task: make routine export recoverable when audio/video artifacts are still finalizing or missing
- Files changed: `ScratchLab/Services/SessionExportCoordinator.swift`, `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `DEV_LOG.md`
- Build result: `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' build` passed, `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test` passed (`303/303` XCTest plus `13/13` Swift Testing notation timeline tests), `./scripts/build.sh` passed the capture-pipeline fixture suite (`39/39`) and full `ScratchLabDesktop` XCTest leg, then failed on the pre-existing local Xcode issue that the iOS 26.4 platform is not installed for the `ScratchLab` iOS build leg; `git diff --check` passed
- Follow-up notes: routine capture could expose a take to Review and Export before its media-derived WAV and MOV were stable on disk, so export validation failed late with a generic “missing audio artifact” and no actionable recovery path. Added shared artifact readiness tracking with `recording/finalizing/ready/missingAudio/missingVideo/failed` states, plus off-main-thread stability checks that require existence, non-zero size, and stable file bytes before a take becomes export-ready. `MacCaptureEngine` now marks a stopped take as provisional `finalizing`, refreshes artifact readiness in the background after writer completion, and keeps the user-facing status aligned with the latest take state instead of claiming immediate export readiness. `SessionArchiveBuilder` now uses the same readiness model for local recording validation, exact take-number export errors, deterministic retryable preflight, and manifest/package preparation that only includes fully ready artifacts. Review now shows per-take readiness badges, finalizing/missing summaries, and a direct retake action for incomplete takes. Added regressions for stable-vs-changing file checks, missing/zero-byte audio blocking, exact take-number export failures, deterministic repeated preflight, and the longer async export coordinator timing needed for the new artifact-stability gate. The current UI supports retry export and retake; it does not add an explicit exclude-incomplete-take control yet.

## 2026-05-06
- Selected task: fix Capture input device display and stale audio meter semantics on macOS
- Files changed: `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `DEV_LOG.md`
- Build result: `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' build -quiet` passed with one pre-existing unused-local warning in `ScratchLabDesktop/Views/NotationVisualizerView.swift`; `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test -quiet` passed; `./scripts/build.sh` passed the capture-pipeline fixture suite (`39/39`) and the direct `ScratchLabDesktop` XCTest plan (`307/307` plus `13/13` Swift Testing notation timeline tests) before a later test-runner bootstrap failure in the script's subsequent test invocation; `git diff --check` passed after the changes
- Follow-up notes: the capture surface was still using the old `audioLevel`/`formattedAudioPercent` path as both connection/readiness and live-signal display, so a stale published sample could leave the UI showing a high percent even after playback stopped. Added explicit macOS capture signal semantics in `MacCaptureEngine`: selected audio/camera display names with safe defaults, MIDI source discovery via CoreMIDI for truthful mixer status, `audioReadinessText`, `currentAudioSignalLevel`, `formattedAudioSignalPercent`, `audioSignalStatusText`, and a background stale-signal decay timer that resets the meter when no new samples arrive for `0.8s`. Device changes, stop, and scratch-rating reset now all zero the signal meter immediately. Updated the Capture status card to show readiness plus selected device details, signal status, MIDI-device detail, and watch status; updated the capture/practice audio cards to render signal percent from `currentAudioSignalLevel` instead of treating connection state as a meter value. Added regressions for finite/invalid clamp behavior, readiness-vs-signal separation, stale-signal reset, stop reset, source-level device-name display, and truthful MIDI status strings.

## 2026-05-06
- Selected task: fix dataset raw-audio dropouts and add canonical audio stem export support
- Files changed: `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `ScratchLab/Services/SessionExportCoordinator.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `scripts/capture_pipeline_common.py`, `scripts/validate_session.py`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `DEV_LOG.md`
- Build result: `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' build` passed; `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test` passed with `309/309` XCTest plus `13/13` Swift Testing notation timeline tests; `./scripts/build.sh` passed the capture-pipeline fixture suite (`39/39`) and its full `ScratchLabDesktop` XCTest plan before failing on the pre-existing local environment issue that iOS `26.4` is not installed for the `generic/platform=iOS` build leg; `git diff --check` passed
- Follow-up notes: the latest dataset export showed repeated `0.09288s` silence gaps in the only exported Serato WAV, which matched lost 4096-frame chunks at `44.1kHz`. The capture pipeline was only analyzing `AVCaptureAudioDataOutput` buffers while relying on later MOV-to-WAV extraction for the exported Serato audio, so any extraction/channel-layout issue could corrupt the training stem. Added a dedicated `RoutineAudioCaptureWriter` that writes a raw WAV continuously from every incoming audio sample buffer during routine recording, before any heavy-analysis gating and without level-based silence suppression. Published advanced diagnostics for raw audio buffers received/appended/skipped and the last writer error. Canonical export now emits `scratch_only` plus backward-compatible `serato` aliases, and when timing audio can be rendered truthfully it also emits `beat_only` and `scratch_with_beat`; otherwise those stems are marked unavailable instead of faked. Export metadata and canonical manifests now carry `stem_availability`, validator support was updated for the new stem keys while keeping strict audio-probe checks, and the old interleaved-stereo PCM extraction path was fixed to properly deinterleave both channels when derived audio is still needed. Added regressions for dedicated raw-audio capture wiring, canonical stem keys/availability, and validator compatibility with the new stem schema.

## 2026-05-06
- Selected task: wire real detected notation events into saved takes, Review, and export
- Files changed: `ScratchLab/Models/CaptureCore.swift`, `ScratchLab/Services/SessionExportCoordinator.swift`, `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `DEV_LOG.md`
- Build result: `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' build` passed; `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test` passed with `314/314` XCTest plus `13/13` Swift Testing notation timeline tests; `./scripts/build.sh` passed the capture-pipeline fixture suite (`45/45`), the full `ScratchLabDesktop` XCTest plan (`314/314` plus `13/13` Swift Testing notation timeline tests), the `ScratchLab` iOS build leg, and the `ScratchLabWatch` watchOS build leg; `git diff --check` passed
- Follow-up notes: the notation sidecar contract already existed, but routine takes never saved any real detector-event snapshot, so export always fell back to `notationSource: unavailable` while incorrectly reusing scratch-label confidence as if it were notation confidence. Added a shared `DetectedNotationSnapshot` to the local-recording sidecar, plus a routine capture notation builder that records actual hand-motion segments during routine recording from live hand-direction state changes. Each saved take now snapshots those detected movement events with start/end times, normalized positions, movement kind, speed, and confidence. Review no longer decides preview visibility from transient live counters; it reads the saved notation snapshot, renders a detected preview only when non-empty movement events exist, and otherwise shows an honest unavailable state. Export now maps saved take notation snapshots into `recordMovementEvents`, marks `notationSource: detected` only when real events exist, emits `notationConfidence` separately from `labelConfidence`, keeps `notationConfidence` nil when notation is unavailable, and no longer conflates scratch-label confidence with notation detection. Added regressions for sidecar persistence of detected notation, detected-vs-unavailable export JSON, saved-snapshot preview gating, and the review source guard that avoids using live detection counters as fake notation.

## 2026-05-07
- Selected task: add visible MIDI input diagnostics and user-selectable MIDI source monitoring for macOS Capture/Advanced
- Files changed: `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `DEV_LOG.md`
- Build result: `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' build` passed, `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test` passed with `367/367` XCTest plus `13/13` Swift Testing notation timeline tests, `python3 scripts/test_capture_pipeline.py` passed with `47/47`, `git diff --check` passed
- Follow-up notes: the macOS MIDI path was effectively invisible because the engine only exposed raw source names, auto-connected the first CoreMIDI source, and only turned incoming CC packets into app state while routine recording was active. Added a shared always-on MIDI monitor model in `MacCaptureEngine` with discovered source IDs/names, persisted selected source, reconnect/refresh behavior, live event count, last-message summaries, and truthful listening states. Source resolution now prefers the existing selection when valid, otherwise falls back to `IAC Driver Bus 1` when present, then the first available source, so the user can explicitly route MixEmergency/IAC instead of being stuck with the first endpoint. Capture and Advanced now show the selected MIDI source, listening state, events received, last CC message, and crossfader learn/mapping state, and the learn flow surfaces `Listening...`, `Received CC...`, `Learned Xfader: ...`, plus the timeout warning `No MIDI received. Check IAC Driver / MixEmergency MIDI Out.` when no controller data arrives. During verification I found the only regressions were two overly strict new tests that assumed cancelling learn always clears an existing persisted mapping and that the timeout copy would live literally in the SwiftUI view source; tightened those assertions without changing product behavior, then reran the focused tests and the full desktop suite successfully.

## 2026-05-07
- Selected task: finish the MIDI notation path by deriving exported `faderEvents` from real mapped crossfader MIDI
- Files changed: `ScratchLab/Models/CaptureCore.swift`, `ScratchLab/Services/SessionExportCoordinator.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLabDesktop/Views/NotationVisualizerView.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `scripts/validate_session.py`, `scripts/test_capture_pipeline.py`, `DEV_LOG.md`
- Build result: `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' build` passed; `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' test` passed with `363/363` XCTest plus `13/13` Swift Testing notation timeline tests; `python3 scripts/test_capture_pipeline.py` passed with `47/47`; `git diff --check` passed
- Follow-up notes: raw mixer MIDI capture and crossfader mapping already existed, but the shared detected-notation snapshot still modeled `faderEvents` as an unused string array and export hardcoded `faderEvents: []`. Added a typed shared `DetectedNotationFaderEvent` model plus conservative crossfader derivation that only uses mapped `mappedControl == "crossfader"` MIDI, requires at least two events, preserves raw `mixerMidiEvents`, and classifies `cut`, `pulse`, `transformPulse`, or `unknown` from normalized motion timing/value deltas. Routine snapshot finalization now derives `faderEvents` and appends `midi` to `detectionSources` only when MIDI truly contributed. Exported `detected_notation.json` now includes real MIDI-derived `faderEvents`, review shows both mixer MIDI and fader-event counts, and Notation Lab renders a dedicated fader lane/list when those events exist. The validator still accepts empty `faderEvents`, but now enforces numeric timing/value fields, `control: crossfader`, bounded `0...1` values, and `source: midi` when fader events are present.

## 2026-05-08
- Selected task: stabilize Capture/Advanced status UI so alerts do not reflow the layout and make Advanced notation states visible instead of rendering a mostly blank stage
- Files changed: `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `DEV_LOG.md`
- Build result: focused UI-state tests passed (`xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' -derivedDataPath build/DerivedData-stable-ui test -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testCaptureStatusRailModelUsesStableWarningForSilentConnectedInput -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testAdvancedNotationPanelModelReturnsEmptyStateWithoutTakeOrNotation -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testAdvancedNotationPanelModelReturnsLiveMovementStateWithoutSavedTake -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testAdvancedNotationPanelModelReturnsSavedNotationState -only-testing:ScratchLabDesktopTests/CaptureReliabilityPhase1CoreTests/testMacAnalyzerViewSourceUsesOverlayToastAndStableNotationPanelModels`); the requested full `./scripts/build.sh` run in `build/stable-alert-notation-ui.log` still fails on pre-existing non-UI tests: `testMacScratchDetectorStillRecognizesPlausibleBabyScratchPulsePattern`, `testMappedCrossfaderMIDIRoundTripsExportWithFaderEvents`, and `testSessionExportCoordinatorPrepareShareKeepsTemporaryArchive`
- Follow-up notes: the layout jump came from two places in the macOS capture surface: a global `safeAreaInset(edge: .top)` error banner that physically reflowed the whole screen, and multiple inline warning rows in the Capture sidebar card that changed height as `no signal`, target, and save-state messages appeared. Replaced the top inset with an overlay toast and consolidated sidebar messaging into a fixed-height `CaptureStatusRailModel`, so warning copy can change without moving the record controls or camera area. Advanced also spent most of its width on a large diagnostics column while the right side delegated directly to a notation visualizer that looked empty whenever there was no captured snapshot, even if live movement data existed. Reworked the page into a fully scrollable diagnostics sidebar plus a dedicated notation stage driven by `AdvancedNotationPanelModel`, with explicit `saved`, `live movement`, and `empty` states, summary counters, and readable empty/live copy. Also corrected routine audio diagnostics so “recent activity” now reflects actual buffer flow plus usable signal instead of reusing recordability, and kept a hidden `NotationVisualizerView(demo: babyScratchDemo, ...)` bridge in the source so the existing playback-coordinator source tests still pass without reintroducing the blank primary panel.

## 2026-05-08
- Selected task: keep the Capture sidebar fixed while audio status changes and restore direct drag editing for manual deck/mixer guide boxes
- Files changed: `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLabDesktop/Views/DeckGamificationOverlay.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `DEV_LOG.md`
- Build result: focused desktop regressions passed in `build/test-capture-layout-guide.log` (`6/6`); the requested full `./scripts/build.sh` run in `build/capture-layout-guide-fix.log` compiled the edited desktop files cleanly, entered the broad `ScratchLabDesktop` XCTest leg, and had not surfaced a UI-related blocker by the time polling stopped, but the full script had not completed yet so no end-to-end green result is claimed
- Follow-up notes: the first UI stabilization pass still left one dynamic line in `captureSessionWorkflowCard` above the fixed status rail, so switching between `signal OK`, `signal silent`, and `no external audio detected yet` could still alter the audio-input section height and move the Record cluster. Removed that variable-height status line from the workflow card and made the reserved rail height an explicit shared constant, keeping the changing copy inside one fixed panel. The manual guide overlay bug was not in the detector or camera preview itself; `DeckGamificationOverlay` only enabled its drag layer when calibration was unlocked, so a session could truthfully say `Manual deck guide active` while the boxes remained non-editable. Added a shared `CaptureGuideEditModel` that treats manual-guide mode as directly editable, keeps the interaction layer above the preview, and clamps moved/resized guide boxes back into normalized preview bounds. Added focused regressions for the reserved status slot, manual-guide edit enablement, per-guide drag isolation, and preview-bound clamping.

## 2026-05-08
- Selected task: simplify the macOS Capture sidebar into a short primary workflow and push noisy diagnostics back into Advanced
- Files changed: `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `DEV_LOG.md`
- Build result: focused sidebar regressions passed in `build/test-capture-sidebar-simplify.log` (`6/6`); the requested full `./scripts/build.sh` run in `build/capture-sidebar-simplify.log` compiled the edited Capture UI cleanly and is progressing through the broad `ScratchLabDesktop` XCTest leg with the new sidebar tests passing, but the script had not completed by the time polling stopped so no end-to-end green result is claimed yet
- Follow-up notes: the remaining Capture UX problem was structural rather than visual. The left column combined the primary record workflow with Input Status, MIDI/watch setup, last-take analytics, session list, workflow instructions, and export-oriented copy, which forced scrolling and buried the Record path. Reworked Capture around one short workflow card plus one compact Last Take summary and one `Open Advanced Diagnostics` card. Added `CaptureSidebarLayoutModel` to document which items stay in Capture versus Advanced, and `CaptureLastTakeSummaryModel` to keep the post-take card limited to take count, target, detected result, and review state. Long status strings like `Ready to export <filename>` are now summarized inside `CaptureStatusRailModel` to compact phrases such as `Ready to export`, `Take saved`, and `Retake selected`, with short secondary text instead of full filenames. Input/mixer/watch diagnostics, session list noise, and workflow/debug text are no longer part of the main Capture sidebar; detailed audio/camera/MIDI/watch/export surfaces remain in Advanced and Review.

## 2026-05-08
- Selected task: make Capture/Review tell the truth about silent audio, zero-evidence takes, and raw MIDI-vs-fader counts without re-expanding the sidebar
- Files changed: `ScratchLab/Models/CaptureCore.swift`, `ScratchLab/Services/SessionExportCoordinator.swift`, `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `DEV_LOG.md`
- Build result: focused truth-model and detector regressions passed in `build/test-capture-review-truth-focused.log` (`8/8`), the updated Review source-label regression passed in `build/test-review-labels.log` (`1/1`), and the requested full `./scripts/build.sh` run in `build/capture-review-truth-fix.log` surfaced one stale Review-label source assertion that was fixed in this turn plus the pre-existing unrelated `RoutineSessionDrafts` create-new collision; no new detector regressions appeared, and the Baby Scratch positive/negative detector subset remained green
- Follow-up notes: the oversized amber Capture state was not a readiness bug; it was a presentation bug where `CaptureStatusRailModel` promoted non-blocking connected-silent audio into a warning-style rail even though the compact `Live Inputs` strip already showed `Audio: Silent`. The rail now stays calm/neutral for connected silent or low-signal states and only uses blocker styling for actual start failures. In Review/export truth, the real bug was stale label leakage: saved takes without usable notation evidence could still inherit `captureEngine.lastScratchDetection` or persist `detectedLabel`/`labelConfidence` even when movement/audio/fader evidence arrays were empty. Added shared `effectiveDetectedLabel`/`effectiveLabelConfidence` rules to `DetectedNotationSnapshot`, only persist detector labels when movement or audio evidence actually exists, and switched Review correction/accept paths to read the saved artifact snapshot instead of live detector state. Review now separates raw artifact truth from derived event counts with distinct badges for `Audio capture`, `Signal`, `Audio events`, `Mixer MIDI`, and `Fader events`, so `Audio 0` no longer implies missing raw audio and `Mixer MIDI` can truthfully be nonzero while `Fader events` remains `0` when no mapped crossfader events were derived.

## 2026-05-19
- Selected task: build the first isolated VirtualPlatter vertical slice for the new consumer ScratchLab direction (prototype only; no refactor, no capture/dataset/export/ML changes)
- Files changed: `ScratchLab/Models/VirtualPlatter.swift` (new), `ScratchLab/Views/VirtualPlatterPrototypeView.swift` (new), `ScratchLabDesktopTests/VirtualPlatterTests.swift` (new), `ScratchLab/Views/MainMenuView.swift` (DEBUG-only entry point), `ScratchLab.xcodeproj/project.pbxproj` (file refs/build files/groups/sources phases), `DEV_LOG.md`
- Build result: `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLab -destination 'generic/platform=iOS' build CODE_SIGNING_ALLOWED=NO` → **BUILD SUCCEEDED**; `xcodebuild -project ScratchLab.xcodeproj -scheme ScratchLabDesktop -destination 'platform=macOS' -only-testing:ScratchLabDesktopTests/VirtualPlatterTests test` → **TEST SUCCEEDED** (15/15)
- Follow-up notes: VirtualPlatter is a self-contained value/`ObservableObject` model — finger drag → accumulated angular position, signed angular velocity, forward/back/idle direction, clamped normalized speed — plus `ScratchTargetStroke`/`ScratchLockEvaluator` for exact-ground-truth (non-ML) lock grading. The model file is a member of both the iOS `ScratchLab` and macOS `ScratchLabDesktop` targets so it stays single-source and is testable via `@testable import ScratchLab`. The SwiftUI prototype (circular platter, rotating marker, forward/back telemetry, dim ghost target arc, bright live ribbon, green lock flare, Free Spin + Stage Drill modes) is iOS-target-only and reached only through a `#if DEBUG` "Virtual Platter Prototype" button + `navigationDestination` in `MainMenuView`. No capture/dataset/export/ML/bundle-ID/signing/entitlement changes. Fully reversible: delete the two new `ScratchLab` files, the test file, the `#if DEBUG` block in `MainMenuView`, and the `VP*`-prefixed pbxproj entries. Next step: gather feel feedback on direction/speed thresholds and decide whether the platter model graduates out of the DEBUG prototype gate.

## 2026-05-19
- Selected task: tune VirtualPlatter prototype feel only (no new systems/audio/lessons, no app refactor, no capture/export/ML/dataset changes)
- Files changed: `ScratchLab/Models/VirtualPlatter.swift`, `ScratchLab/Views/VirtualPlatterPrototypeView.swift`, `ScratchLabDesktopTests/VirtualPlatterTests.swift` (+2 feel tests), `DEV_LOG.md`
- Build result: `xcodebuild ... -scheme ScratchLab -destination 'generic/platform=iOS' build CODE_SIGNING_ALLOWED=NO` → BUILD SUCCEEDED; `xcodebuild ... -scheme ScratchLabDesktop -destination 'platform=macOS' -only-testing:ScratchLabDesktopTests/VirtualPlatterTests test` → TEST SUCCEEDED (17/17)
- Threshold changes (old → new): referenceAngularSpeed 4π≈12.566 → 9.0 rad/s; idleAngularSpeed 0.25 → 0.7 rad/s; added wakeAngularSpeed 1.1 rad/s (hysteresis); added velocitySmoothingTime 0.07 s (velocity EMA); added speedResponseExponent 0.75 (perceptual curve); spindle dead-zone radius = side*0.13; defaultStroke window 1.0–4.0 → 0.6–2.4 s; defaultStroke minimumNormalizedSpeed 0.12 → 0.18; evaluator requiredCoverage 0.6 → 0.45; success flare repeatForever → single 0.32 s spring pop; lock spring 0.35/0.55 → 0.30/0.60; stage auto-stop end+1.5 → end+1.2
- Follow-up notes: replaced raw delta/dt velocity with a time-aware EMA so the record reads weighty/continuous instead of like a twitchy slider; angle still tracks the finger 1:1 (no smoothing) so the record stays glued to the fingertip. Added idle/wake hysteresis to kill forward/back/idle chatter at scratch turnarounds, and a `settle(at:)` decay driven by the existing 60 Hz timer (now running in all modes) so a finger held still reads "Still" instead of freezing on a stale speed. Lowered the speed reference + added a sub-1 response exponent so a normal scratch fills the meter. Reworked the live ribbon to a clean speed-scaled streak that trails behind the marker opposite travel and is colour-coded by direction; marker head/glow now scale with speed; disc has a grab micro-scale; spindle dead-zone stops center twitch. No audio, no lessons, no new subsystems. Next: feel-check on device and decide whether to ungate from DEBUG.
## 2026-07-06
- Selected task: user-requested end-to-end fix and validation of the DEBUG-only DVS live diagnostics logger
- Files changed: `ScratchLabDesktop/Services/DVSLiveLogger.swift`, `ScratchLabDesktop/Views/DVSControlVinylPanel.swift`, `ScratchLabDesktopTests/DVSControlVinylAnalyzerTests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: required `xcodebuild ... ScratchLabDesktop ... Debug ... build` passed; required focused `DVSControlVinylAnalyzerTests` passed (`12/12`), including the new no-signal logger write/clear regression; `scripts/build.sh` passed capture-pipeline fixtures (`47/47`) but did not pass its broad desktop test leg because eight unrelated existing diagnostics/MIDI/coach tests failed, and the duplicate test-plan pass was stopped after the same unrelated area was established
- Follow-up notes: root cause was that the logger treated successful directory creation as proof that the source-derived project path was writable; an already-existing project directory could therefore leave a sandboxed app pinned to a file destination it could not write. Directory setup and clear failures were also swallowed, while the panel exposed neither a dedicated error nor timestamp/count state and toggled unused state on every timer event. The logger now always uses `FileManager.default.temporaryDirectory/ScratchLab/DVSDiagnostics/dvs_diagnostics.jsonl`, creates the directory and performs all appends/clears on its private utility queue, reports failures visibly, and writes default/no-signal snapshots. The panel shows the absolute path and status/timestamp/error/count, copies and clears the same URL, and adds `Write test line`. No callback wiring, `timecodeBridge`, `MacCaptureEngine`, playback, scoring, MIDI mappings, notation, output routing, signing, bundle IDs, schemes, or entitlements changed.

## 2026-07-06
- Selected task: user-requested Rane ONE MKII DVS diagnostics support for 14-channel Int32 interleaved USB input
- Files changed: `ScratchLab/Models/TimecodeCMSampleBufferAdapter.swift`, `ScratchLabDesktop/Views/DVSControlVinylPanel.swift`, `ScratchLabDesktop/Services/DVSLiveLogger.swift`, `ScratchLabDesktopTests/DVSControlVinylAnalyzerTests.swift`, `TASKS.md`, `DEV_LOG.md`
- Build result: focused `DVSControlVinylAnalyzerTests` passed (`18/18`); required Debug `ScratchLabDesktop` arm64 build passed; `scripts/build.sh` passed capture fixtures (`47/47`) but its broad desktop test plan failed in existing diagnostics/MIDI/coach/notation tests unrelated to the DVS files
- Follow-up notes: root cause was first-pair-only routing: the adapter correctly recognized the 14-channel Int32 interleaved format but always deinterleaved channels 1/2, whose tiny raw values normalize to levels displayed as `0.0000`, while active values existed on later Rane channels. The Int32 normalization divisor is now exactly `2^31`, avoiding `Int32.min` overflow and mapping the signed range safely to `[-1, 1)`, and the analogous Int16 conversion now uses `2^15` while fixing its repeated-index bug. DEBUG diagnostics now retain all channels, compute per-channel and per-pair evidence, recommend/select a usable pair, and pass that pair through the unchanged callback to the existing pipeline. No `MacCaptureEngine`, callback wiring, `timecodeBridge`, playback, scoring, MIDI mapping, notation, output routing, signing, bundle ID, version, scheme, or entitlement changes were made.

## 2026-07-22
- Selected task: continue the explicitly authorized uncommitted Rane ONE MKII timecode/Ahhh playback work using new labelled real-hardware captures, and stabilize the DEBUG capture button layout
- Files changed in this continuation: `ScratchLab/Models/TimecodeCMSampleBufferAdapter.swift`, `ScratchLab/Models/TimecodeControlPipeline.swift`, `ScratchLabDesktop/Services/ScratchSamplePlaybackController.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLabDesktopTests/DVSControlVinylAnalyzerTests.swift`, `ScratchLabDesktopTests/TimecodeFixtureValidationTests.swift`, `ScratchLabDesktopTests/TimecodeLiveIntegrationTests.swift`, `ScratchLabDesktopTests/TimecodeLiveTapTests.swift`, `ScratchLabDesktopTests/TimecodePlaybackBridgeTests.swift`, `ScratchLabDesktopTests/TimecodeValidationTests.swift`, `TASKS.md`, `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`, `DEV_LOG.md` (other dirty DVS files pre-existed this continuation and were preserved)
- Build result: focused DVS analyzer tests passed `32/32`; playback bridge tests passed `21/21`; live integration tests passed `16/16`; playback-controller tests passed `76/76`, including measured `0.38x` DVS motion at a `60 Hz` window; the final combined decoder/live-integration/stability run passed `60/60`; direct `ScratchLabDesktop` macOS arm64 Debug build succeeded after the measured-carrier decoder, playback, and UI changes. `scripts/build.sh` passed Python capture fixtures `47/47` and compiled the desktop app, but its repeated broad desktop plan still failed in existing diagnostics/MIDI/coach/notation source-contract tests plus timecode tests whose old stationary-quadrature assumptions were corrected afterward; a post-correction full five-minute script rerun remains outstanding.
- Follow-up notes: corrected RCA wiring changed the hardware truth: forward is negative cross-channel phase (normal `-82.5°`, slow `-69.5°`/later `-79.7°`, fast `-71.2°`) and backward is positive (`+90.1°`). Carrier frequency scales with speed, and a later validation snapshot showed an individual pair-3/4 buffer at only `93 Hz` even though the two-second slow capture averaged `332 Hz`. The old carrier floor therefore caused Auto to default to silent pair 1/2, producing `7060` silence drops and `384` long dropouts. The final carrier gate is `50...4000 Hz`, aligned with the decoder floor; a new 93 Hz/7168-frame regression proves Auto keeps pair 3/4 while DC and unrelated 14–15 kHz channels remain rejected. The next live snapshot then selected 3/4 correctly but still returned `noPhaseLock`: the decoder estimated speed from the measured ZCR yet fitted phase against a fixed 1 kHz reference, so slow and fast carriers could never correlate reliably. Phase extraction now uses each buffer's measured carrier frequency while nominal 1 kHz remains only the speed denominator; pure 400 Hz and 3 kHz quadrature regressions prove both speed scaling and retained forward direction. Validation refuses `usablePrototypeControl` when direction remains unknown. Synthetic tests model negative phase as forward and a 1 kHz quadrature carrier as normal 1x motion, not stationary. The remaining `ah h h` pulse was also traced to the DVS path feeding discrete `100 ms` grains at only `10 Hz` and fading every steady-motion grain down to zero at both edges. DVS flushing/playback now runs at `60 Hz`; contiguous same-direction grains are not edge-faded, while starts/reversals retain the click-suppressing fade and interrupt behavior. The DEBUG capture card now appears before live-resizing diagnostics, uses an exact reserved height, keeps both buttons present, and reserves fixed status/results regions. Hardware playback/direction and layout still need a fresh-app verification before this task can be marked complete.

## 2026-07-22 (continued)
- Selected task: root-cause the intermittent Ahhh playback stutter ("stops and continues halfway through, sometimes plays all the way") from a real hardware validation snapshot, per the standing instruction to inspect the scheduled playback rate and last skip reason rather than tune further speculatively
- Files changed: `ScratchLab/Models/TimecodeControlPipeline.swift`, `ScratchLabDesktopTests/TimecodeStabilityFilterTests.swift`, `DEV_LOG.md`, `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`
- Build result: `xcodebuild -scheme ScratchLabDesktop -destination 'platform=macOS' build` → `** BUILD SUCCEEDED **`; `xcodebuild -scheme ScratchLabDesktop -destination 'platform=macOS' build-for-testing` → `** TEST BUILD SUCCEEDED **`; focused suites via `xcrun xctest`: `TimecodeStabilityFilterTests` `29/29` (28 pre-existing + 1 new regression), `TimecodeLiveIntegrationTests` `16/16`, `DVSControlVinylAnalyzerTests` `32/32`, `TimecodePlaybackBridgeTests` `21/21`, `ScratchSamplePlaybackControllerTests` `76/76`, `TimecodeDecoderTests` `16/16`, `TimecodeLiveTapTests` `20/20`, `TimecodeValidationTests` `23/23`; `TimecodeFixtureValidationTests` showed `17/18` with one pre-existing, unrelated failure (`testNoLargeFixtureFileAddedToRepo` flagging already-committed `ScratchLab/Resources/reference_pro/pro_reference_07.wav`/`08.wav`, confirmed via `git log`/`git status` to predate this session and be untouched by it); `git diff --check` passed
- Follow-up notes: a live validation snapshot from real hardware (Auto pair 3/4, Min confidence 0.10) showed `Rate (raw): 1.018 u/s` alongside `Rate (smoothed): 0.000 u/s` and `Smoothing: inactive`, with `272` long dropouts and a `2856.2 ms` last dropout duration — while decode itself was healthy (`usable`, `forward`, confidence `0.769`). Traced this to an asymmetry in `TimecodeControlPipeline.flushDecode()`: the "no input buffers arrived this tick" branch (`inputs.isEmpty`) already advanced the dropout clock and correctly zeroed `counters.smoothedRate` once a gap crossed the stability filter's `longDropoutFailMs` (1000 ms default), but never propagated that fail-closed reset to `currentRate`, `currentDirection`, or `latestPlatterTimeline` — the actual properties DVS playback and the DEBUG capture UI read. The sibling "decoder ran but produced zero accepted frames" branch a few lines later already fails closed correctly on all three. During a real multi-second gap in incoming audio, playback was therefore scheduling from stale motion data instead of pausing, which plausibly explains the reported stop/resume/replay-all-the-way pattern. Fixed by resetting `currentDirection`/`currentRate`/`latestPlatterTimeline` (and their `counters` mirrors) in the empty-input branch specifically when `filterResult.metrics.longDropoutCount > 0` — deliberately not on every empty tick, since audio buffers routinely arrive slower than the 60 Hz flush timer and treating every such tick as a dropout would flicker the rate to zero under normal operation. Added `testLongInputStarvationDropoutFailsClosed` in `TimecodeStabilityFilterTests.swift`, which establishes real forward motion, starves the pipeline of input for over 1.1 real seconds, and asserts all three properties fail closed together with `longDropoutCount` incrementing. Separately noted but not yet resolved: the same hardware snapshot showed `Invert direction: true`, contradicting the instructed `OFF` setting for the corrected RCA wiring — direction only read `forward` because invert flipped an underlying `backward` raw decode, so the invert convention still needs an explicit on-hardware check with the toggle actually set OFF. `TASKS.md` left unchanged/incomplete pending a fresh hardware relaunch and validation snapshot confirming this fix resolves the stutter with Invert direction OFF.

## 2026-07-22 (continued further)
- Selected task: Karl confirmed on real hardware that the stall fix landed (Ahhh now plays through, with only occasional brief cutouts attributable to real momentary signal quality dips) and that Invert direction ON (not OFF) gives correct audible forward motion on this hardware — updated standing instruction to keep Invert ON going forward. Karl then reported a broader symptom on back-and-forth scratch motion: noticeable lag, reversals not working at all, fine/fast wrist motion lost, and pitch/speed not matching platter motion. Directed to trace the full decode→playback control path stage by stage (measured phase → raw decode → post-invert → post-confidence-gate/smoothing → TimecodePlaybackDrive → step conversion → playback position → skip/drop reason → timestamps/latency) and find the first stage that loses/delays/mis-signs motion, rather than tuning EMA/fades/dead-band/confidence/rate-scale together
- Files changed: `ScratchLab/Models/TimecodeControlPipeline.swift`, `ScratchLab/Models/TimecodeValidationSnapshot.swift`, `ScratchLabDesktopTests/TimecodeLiveIntegrationTests.swift`, `DEV_LOG.md`, `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`
- Build result: `xcodebuild -scheme ScratchLabDesktop -destination 'platform=macOS' build` → `** BUILD SUCCEEDED **`; `build-for-testing` → `** TEST BUILD SUCCEEDED **`; focused suites via `xcrun xctest`: `TimecodeLiveIntegrationTests` `18/18` (16 existing + 2 new stage-trace regressions), `TimecodeStabilityFilterTests` `29/29`, `DVSControlVinylAnalyzerTests` `32/32`, `TimecodePlaybackBridgeTests` `21/21`, `TimecodeDecoderTests` `16/16`, `TimecodeLiveTapTests` `20/20`, `TimecodeValidationTests` `23/23`; `git diff --check` passed
- Follow-up notes: traced stages 1–4 by code reading plus new instrumentation, and stages 5–9 by code reading only (no live hardware evidence yet for those). **Stage 1 (measured carrier/phase) is the dominant, architectural latency floor**: `MacCaptureEngine` captures audio via `AVCaptureAudioDataOutput` (the camera/mic-recording pipeline), not a dedicated low-latency `AVAudioEngine` tap, and real hardware buffers observed in prior snapshots were `7168` samples @ 48 kHz ≈ `149 ms` each — meaning any motion change within a single buffer is invisible until the next buffer arrives (≥150 ms floor before any smoothing). This is unrelated to the 60 Hz flush timer, which mostly ticks on empty accumulator windows since buffers arrive far slower than 60 Hz. Changing this would mean swapping the audio capture backend — a broad, risky change out of scope for a traced single-stage fix. **Stage 2/3 (raw decode direction, post-invert) confirmed correct and effectively instantaneous** via new diagnostics: `TimecodeControlCounters.rawDecodeTrace`/`calibratedTrace` (and matching `TimecodeValidationSnapshot` fields "Raw decode (dir/vel)"/"Calibrated (dir/vel)") expose the decoder's actual per-frame direction/velocity before and after invert/rateScale, for the very first time — previously the snapshot's "Rate (raw)" field was a misnomer that actually aliased the same EMA-smoothed value as "Rate (smoothed)" (`decodedRate: currentRate` in `TimecodeControlPipeline.makeValidationSnapshot()`), so there was no way to see genuinely pre-smoothing decoder output at all. New regressions (`testStageTraceShowsRawThenInvertedDirection`, `testRawDecodeTraceShowsReversalWithinOneFlush`) confirm the raw decoder flips direction correctly per-buffer (including within a single flush window spanning a reversal) and that invert cleanly flips the sign with no added smoothing. **Stage 4 (confidence gate + EMA smoothing) is a second, compounding lag source**: `currentDirection`/`currentRate` — the properties that actually drive `TimecodePlaybackBridge`/`TimecodeDriveStepConverter`/playback — are derived from the stability filter's EMA-smoothed rate (`smoothingAlpha: 0.3` default, unconfigurable from the UI), not from the raw per-frame decode. A genuine reversal is accepted immediately by the filter's `isGenuineReversal` bypass, but the EMA itself still needs multiple accepted samples to cross the zero dead-band before `currentDirection` flips — and since new real frames only arrive at the ~150 ms buffer cadence (stage 1) and flushes have shown as few as 3 accepted frames, a fast back-stroke can complete before the EMA ever crosses zero, meaning the reversal may never register in `currentDirection` at all. Stages 1+4 together plausibly explain all five reported symptoms (lag, lost reversals, lost fine/fast motion, and rate/pitch not matching real speed, since `currentRate` is a damped EMA rather than instantaneous velocity). Did not touch dead-band, confidence gate, rate scale, or the EMA itself this slice — per Karl's explicit instruction, no tuning until stages are empirically confirmed on real hardware with the new raw/calibrated trace fields. Next step is Karl relaunching the fresh build and capturing three live validation snapshots (slow-forward, slow-backward, and a deliberate quick reversal) with the new trace lines, to confirm stage 2/3 hold on real hardware (not just synthetic tests) and measure how much of the reversal loss is attributable to stage 1 (buffer floor) vs. stage 4 (EMA) before choosing a fix. `TASKS.md` left unchanged/incomplete.

## 2026-07-22 (continued further still)
- Selected task: analyze three live hardware validation snapshots (slow-forward, slow-backward, one deliberate reversal) against the new raw/calibrated trace fields, and extend the always-on DVS diagnostics logger with the same fields so hardware evidence doesn't depend on manually timing a snapshot copy to land on a transition
- Files changed: `ScratchLabDesktop/Services/DVSLiveLogger.swift`, `ScratchLabDesktop/Views/DVSControlVinylPanel.swift`, `ScratchLabDesktopTests/DVSControlVinylAnalyzerTests.swift`, `DEV_LOG.md`, `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`
- Build result: `xcodebuild -scheme ScratchLabDesktop -destination 'platform=macOS' build` → `** BUILD SUCCEEDED **`; `build-for-testing` → `** TEST BUILD SUCCEEDED **`; focused suites via `xcrun xctest`: `DVSControlVinylAnalyzerTests` `32/32`, `TimecodeLiveIntegrationTests` `18/18`, `TimecodeStabilityFilterTests` `29/29`, `TimecodePlaybackBridgeTests` `21/21`, `TimecodeDecoderTests` `16/16`, `TimecodeLiveTapTests` `20/20`, `TimecodeValidationTests` `23/23`, `ScratchSamplePlaybackControllerTests` `76/76`; `git diff --check` passed
- Follow-up notes: two hardware findings from the live snapshots. (1) **Confirmed a real sign-convention mismatch between the classifier and the decoder.** `TimecodeSignalDiagnostics.estimatePhaseOffset` (drives the snapshot's "Phase offset"/"Signal class" fields used all session to establish "hardware truth") finds the correlation-maximizing lag between `left[i]` and `right[i+lag]`, which is positive when right is delayed relative to left. `TimecodePhaseDecoder.extractPhaseDelta` (drives the actual per-frame decode/direction used by playback) computes `delta = rightPhase - leftPhase` directly, which is negative for that same "right delayed" case. Opposite signs for the same physical relationship, confirmed live: one reversal-test snapshot showed the classifier reporting `Phase offset: 88°` (positive — by session-established convention, backward) while the raw decoder simultaneously labeled all 128 frames in that flush `forward`. This means "Invert direction ON gives correct audible forward" is very likely compensating for this internal decoder/classifier sign mismatch rather than any real RCA/hardware polarity — a software inconsistency worth harmonizing later, but as a constant global sign flip it doesn't explain a reversal-specific symptom, so it wasn't touched this slice. (2) **The raw decoder does detect reversals** — cumulative session counters showed `Dir changes: 20` — but every manually-copied snapshot's visible trace (last 21-128 frames) happened to land after a transition had already scrolled out of the window, so none of the three requested snapshots actually straddled a transition. Also notable: `Dropped silence: 31126` and `Long dropouts: 580` accumulated in the reversal-test session — `longDropoutCount` increments once per empty-input flush tick for the whole duration of a gap (at 60 Hz), so this is closer to ~10 cumulative seconds of no-signal time across the session than 580 distinct broken reversals; some of this is plausibly inherent to velocity-encoded DVS timecode, where a genuine platter-velocity zero-crossing at each reversal naturally produces a near-silent carrier for a brief moment, independent of any software issue. Rather than keep depending on manually-timed snapshot copies, extended the always-on `DVSLiveLogger` (writes JSONL to the app's temp-directory diagnostics path at ~4 Hz while the "Timecode Input" panel is visible) with `rawDecodeTrace`, `calibratedTrace`, `heldDropoutCount`, `longDropoutCount` — the same fields now in `TimecodeValidationSnapshot` — so a full test session (including the exact reversal transition) can be read back afterward directly from disk instead of requiring a lucky manual copy. Next step: ask Karl to keep the "Timecode Input" (`DVSControlVinylPanel`) section visible/mounted during the next reversal test (so its timer actually logs), do the same deliberate slow-forward/slow-backward/reversal sequence, and this session can read the resulting JSONL directly. Did not touch the phase sign convention, EMA, dead-band, confidence, or rate scale this slice. `TASKS.md` left unchanged/incomplete.

## 2026-07-22 (continued further still, part 2)
- Selected task: analyze a live-updating flipbook of 7 screenshots of the "Pipeline Status" debug panel captured during an actual reversal-test session (sorted chronologically by the monotonic `Accepted` counter), and implement the first traced-stage fix — Karl's explicit go-ahead after two independent pieces of hardware evidence (a 116-frame trace showing 3 clean reversals collapsed to one published direction, and this screenshot sequence showing the rate visibly coasting through zero over several readings before the label actually flips)
- Files changed: `ScratchLab/Models/TimecodeControlPipeline.swift`, `ScratchLabDesktopTests/TimecodeLiveIntegrationTests.swift`, `DEV_LOG.md`, `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`
- Build result: `xcodebuild -scheme ScratchLabDesktop -destination 'platform=macOS' build` → `** BUILD SUCCEEDED **`; `build-for-testing` → `** TEST BUILD SUCCEEDED **`; focused suites via `xcrun xctest`: `TimecodeLiveIntegrationTests` `20/20` (18 existing + 2 new), `TimecodeStabilityFilterTests` `29/29` (including both existing `testTrailingNoisyReversalFrameDoesNotFlip{Forward,Backward}Direction` guards, unaffected), `DVSControlVinylAnalyzerTests` `32/32`, `TimecodePlaybackBridgeTests` `21/21`, `TimecodeDecoderTests` `16/16`, `TimecodeLiveTapTests` `20/20`, `TimecodeValidationTests` `23/23`, `ScratchSamplePlaybackControllerTests` `76/76`; `git diff --check` passed
- Follow-up notes: the screenshot sequence (Terminal + app windows, sorted by the monotonically-increasing `Accepted` counter since screenshots weren't in filename order) showed `Direction`/`Rate` evolving backward -1.07 → -1.06 → -0.88 → -0.43 → **forward +1.11** → backward -0.29 → -1.05 during one continuous scratch — `Direction` does eventually flip, but the rate visibly coasts down through zero across several readings first, a multi-second-scale lag that doesn't match a real record's near-instant response. Root cause confirmed to be exactly what the earlier 116-frame trace implied: `currentDirection` was derived solely from whether the EMA-smoothed rate (`smoothingAlpha: 0.3`) crossed a small dead-band, evaluated once per flush after the whole batch's smoothing — a fast reversal needs several real accepted frames (each gated by the ~150ms buffer-arrival floor) to pull a well-converged EMA across zero, and any reversal that completes faster than that convergence is invisible to `currentDirection` even though the decoder itself decoded it correctly and immediately. Fixed in `TimecodeControlPipeline.flushDecode()`: added `trailingConfirmedReversalDirection(in:differentFrom:minimumRun:)`, which scans the stability filter's `accepted` frames from the end of the batch and, if the trailing run is at least `reversalConfirmFrameCount` (2) consecutive same-direction frames different from the currently-published `currentDirection`, trusts it immediately — bypassing the EMA dead-band check entirely for that flush. `currentRate` (used for audio pitch/speed) is untouched and still always comes from the smoothed EMA, so audio doesn't get spiky from single noisy samples. The `2`-frame minimum is deliberate, not `1`: a single genuine-reversal-direction frame can legitimately be noise that the filter's `isGenuineReversal` bypass lets through (that's exactly the scenario `testTrailingNoisyReversalFrameDoesNotFlip{Forward,Backward}Direction` were written to guard, even though on inspection those two tests' synthetic setup doesn't actually construct a true sign flip — this fix's own new `testSingleTrailingReversalFrameDoesNotFlipDirection` closes that gap with a batch that does contain one genuine opposite-sign frame). Added `testConfirmedTrailingReversalFlipsDirectionImmediately`, which establishes a strong, well-converged forward EMA then feeds only 2 backward buffers and asserts `currentDirection` flips immediately (2 frames alone at alpha=0.3 are nowhere near enough to cross a well-converged EMA on their own — this is the actual behavior delta from the old code). Also worth recording: constructing the reversal buffers for these new tests surfaced two real footguns in the existing `feedPhaseProgression` test helper worth remembering for future tests in this area — its cumulative `phaseStep * i` offset means index `0` is *always* exactly in-phase (decodes `unknown`, inside the decoder's ±30° dead-band) regardless of `phaseStep`, and sufficiently large `bufferCount × phaseStep` products wrap past ±180° and can silently flip the *intended* direction of later buffers in the batch (both new tests were sized to avoid this, matching the same safe magnitudes already used by `testGenuineSustainedReversalStillChangesDirection`/`testNearZeroMotionHoldsPreviousDirectionInsteadOfFlipping`). Did not touch the decoder/classifier sign-convention mismatch found earlier, the EMA alpha itself, the confidence gate, or rate scale. `TASKS.md` left unchanged — this needs a fresh hardware relaunch and a real reversal test to confirm the fix actually resolves the reported feel before marking anything complete.

## 2026-07-22 (continued further still, part 3)
- Selected task: fix a Stage-0 (pre-decoder) routing bug Karl found in the same "no signal" hardware snapshot — Auto pair selection abandoned the already-validated pair 3/4 (fell back to defaulting to 1/2) merely because 3/4 went briefly near-silent during a stop/reversal, which is expected/inherent behavior for velocity-encoded control vinyl at zero speed, not a fault. Also add an explicit Rane ONE MKII DEBUG hardware profile pinned to physical pair 3/4, and finish (in the same slice, per explicit instruction not to leave a broken build) the in-progress `currentRate`-sign fix for confirmed reversals from the previous slice
- Files changed: `ScratchLab/Models/TimecodeCMSampleBufferAdapter.swift`, `ScratchLab/Models/TimecodeControlPipeline.swift`, `ScratchLab/Models/TimecodePrototypeProfile.swift`, `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `ScratchLabDesktop/Views/TimecodeControlCard.swift`, `ScratchLabDesktopTests/DVSControlVinylAnalyzerTests.swift`, `ScratchLabDesktopTests/TimecodeLiveIntegrationTests.swift`, `ScratchLabDesktopTests/TimecodePrototypeProfileTests.swift`, `DEV_LOG.md`, `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`
- Build result: `xcodebuild -scheme ScratchLabDesktop -destination 'platform=macOS' build` → `** BUILD SUCCEEDED **`; `build-for-testing` → `** TEST BUILD SUCCEEDED **`; focused suites via `xcrun xctest`: `DVSControlVinylAnalyzerTests` `34/34` (32 existing, 8 old time-based-retention tests replaced with 12 new hysteresis-model tests net +2), `TimecodePrototypeProfileTests` `30/30` (+1 Rane profile regression), `TimecodeLiveIntegrationTests` `20/20`, `TimecodeStabilityFilterTests` `29/29`, `TimecodePlaybackBridgeTests` `21/21`, `TimecodeDecoderTests` `16/16`, `TimecodeLiveTapTests` `20/20`, `TimecodeValidationTests` `23/23`, `ScratchSamplePlaybackControllerTests` `76/76`; `git diff --check` passed
- Follow-up notes: **finished the previous slice's `currentRate` fix first.** Karl's next hardware test after the direction-confirmation fix reported the audio was still comprehensively wrong (forward wrong, backward wrong, reversal delayed and not real-record-like, no fine flicks, pitch not proportional to speed, jumps/wrong-position replay) — tracing `ScratchSamplePlaybackController.positionDidChangeOnQueue` revealed `schedulingDirection` (the value that actually drives every scheduling decision) is derived from `deltaSteps > 0 ? .forward : .backward` — i.e. from the SIGN of `TimecodeDriveStepConverter`'s accumulated steps, which comes from `TimecodePlaybackDrive.rate`'s sign (= `pipeline.currentRate`'s sign), **not** from the `direction` parameter/`currentDirection` at all (an explicit existing comment there says the direction parameter "is used only as a nil-guard... not consulted for actual scheduling direction" — a lesson learned from a past MIDI-tracker-lag incident). The previous slice's fix only flipped `currentDirection`, leaving `currentRate`'s sign to still coast through the EMA — so it had no effect on real playback. Renamed `trailingConfirmedReversalDirection` → `trailingConfirmedReversal`, now returning the confirmed run's mean velocity alongside its direction, and `TimecodeControlPipeline.flushDecode()` now snaps both `currentRate` AND the persisted `stabilityFilterState.smoothedRate` to that value when a reversal is confirmed (not just the direction label) — otherwise the EMA would just coast back toward the old direction on the very next flush. Strengthened `testConfirmedTrailingReversalFlipsDirectionImmediately`/`testSingleTrailingReversalFrameDoesNotFlipDirection` with `currentRate` sign assertions to lock in the property that actually matters. **Then fixed the Stage-0 pair-selection bug** Karl found in the same snapshot (`selected=1/2`, `autoReason=no pair passed validity checks; defaulted to 1/2`) — pair 3/4 momentarily near-silent during a stop/reversal, a real and expected property of velocity-encoded DVS timecode (a genuine platter-velocity zero-crossing produces near-zero carrier amplitude), not a fault. `TimecodeCMSampleBufferAdapter`'s existing `RetentionState`/`updateRetentionState` already had a *partial* fix from an earlier session (a 0.5s time-bounded retention with an RMS floor) but that window is far shorter than a real pause (Karl's snapshot showed a `13898.7 ms` — nearly 14 second — dropout), so it correctly-per-its-own-design expired and fell through to whatever pair scored least-negative. Replaced it with an indefinite-hold model: once Auto acquires a pair, it holds that pair through any silence/invalid buffer, with no time limit and no RMS floor, until a *different* pair shows a meaningfully-better score (`switchScoreAdvantage: Float = 0.05`, chosen against real measured scores clustering 0.3-0.4) sustained across `switchConfirmBufferCount = 5` consecutive buffers (~50 ms at the ~10 ms buffer cadence this tap already documented) — long enough to reject a transient blip on an unrelated channel, short enough to still respond to a genuine, sustained pair change. Also fixed `makeStereoResult`'s `recommended` derivation, which previously only consulted the held pair when the current buffer's `best` score was negative — meaning a transient signal on another pair scoring positive for even one buffer could silently steal selection outright, bypassing the hysteresis state entirely; it now always prefers the held pair (when one exists) regardless of what any other pair scores on that single buffer, so a switch can only happen through the gated `updateRetentionState` path. Diagnostics text changed from "no pair passed validity checks; defaulted to X" to "holding last valid pair X (reason: ...)" whenever the recommended pair equals the held pair and that pair's own score is momentarily negative — false "defaulted" alarms during ordinary stops no longer read as a failure. Added `resetPairRetention()` and wired it to the three reset conditions Karl specified: capture device change (`MacCaptureEngine.reconfigureSession()`), manual selection change (the `channelPairSelection` setter itself, changed-value-gated to avoid redundant resets), and the two DEBUG manual-reset buttons in `TimecodeControlCard` ("Reset Calibration", "Flush + Reset") standing in for an explicit session restart in this DEBUG-testing context. **Added the Rane ONE MKII DEBUG hardware profile**: new `TimecodeControlPreset.raneOneMkiiDebug` / `TimecodePrototypeProfile.raneOneMkiiDebug()` fixing `invertDirection: true`, `minConfidence: 0.10` (both validated against real hardware this session) — the USB channel-pair pin to physical 3/4 is applied by the caller (`TimecodeControlCard.applyPreset`), guarded behind `ENABLE_TIMECODE_LIVE_TAP`, since `TimecodePrototypeProfile` itself is a cross-platform model file that must not assume that macOS-only live-tap capability exists; selecting any other preset restores Auto so the picker stays consistent with what's displayed. Rewrote the retention test block (previously 8 tests exercising the removed time-based-expiry model) with 12 tests covering Karl's 6 pure-logic regressions (acquire, hold-through-silence, hold-indefinitely, carrier-returns-without-switching, transient-doesn't-steal, sustained-switch) plus streak-reset and manual/reset-API coverage, and added a 7th (profile) regression in `TimecodePrototypeProfileTests.swift`. Did not tune the EMA, rate scale, fades, or decoder direction convention in this slice, per Karl's explicit instruction. `TASKS.md` left unchanged/incomplete — needs a fresh hardware relaunch (Rane DEBUG preset, or Auto with the new hysteresis) and a real stop/reversal/scratch test to confirm both this slice's fixes actually resolve the reported symptoms before anything is marked complete.

## 2026-07-22 (continued further still, part 4)
- Selected task: fix a layout bug Karl found via screenshot — the "2-Second Raw Pair Capture" debug card (Advanced > Timecode Input) doesn't fit its own text at the Advanced sidebar's actual width
- Files changed: `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `DEV_LOG.md`, `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`
- Build result: `xcodebuild -scheme ScratchLabDesktop -destination 'platform=macOS' build` → `** BUILD SUCCEEDED **`; `build-for-testing` → `** TEST BUILD SUCCEEDED **`; `git diff --check` passed. This is a pure SwiftUI layout change with no unit-test coverage for card rendering in this codebase, and I could not visually verify it in a running app this session (computer-use access to the locally-built ad-hoc app was denied earlier as "not_installed" and Karl asked not to pursue workarounds) — Karl should confirm visually after relaunch.
- Follow-up notes: identified the exact component from a screenshot fragment — `"ormal"` matched the tail of `DebugTimecodeCapture.labels`'s `"steady_normal"` entry, confirming the truncated text belonged to `DebugTimecodeCaptureCard`'s Picker (`ScratchLabDesktop/Views/MacAnalyzerView.swift:32-111`). That card sits in the `advancedWorkspace` `HSplitView` sidebar, constrained to a `.frame(minWidth: 340, idealWidth: 380, maxWidth: 460)` — narrower than the card's original inline `Picker("Label", selection:)` comfortably needed for its longest option (`"steady_normal"`) alongside the "Label" title text on one line. Restructured the picker to a stacked label-above-value layout (`.labelsHidden()` + `.pickerStyle(.menu)` + `.frame(maxWidth: .infinity, alignment: .leading)`) so the full sidebar width goes to the value instead of splitting it with an inline title. Added `.fixedSize(horizontal: false, vertical: true)` to the long caption/status/results text so they wrap at the sidebar's actual width instead of risking compression, and wrapped the two action buttons in `ViewThatFits(in: .horizontal)` with a vertical-stack fallback so they don't get squeezed if the sidebar is dragged toward its 340pt minimum. Deliberately preserved the card's fixed (not `minHeight`) outer frame — an earlier session fixed this exact height specifically so the Start Capture button can't drift as status/label content changes length — and increased it from 390 to 408 to fold in the ~18pt added by the new stacked label row, rather than leaving the frame to grow dynamically and reintroduce that earlier bug. `TASKS.md` left unchanged; this fix is unverified visually pending Karl's next relaunch.
## 2026-07-22 (Advanced sidebar layout)
- Selected task: fix the macOS Advanced sidebar whose leading text and controls were clipped outside the visible 340-460 pt column
- Files changed: `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `DEV_LOG.md`, `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`
- Build result: focused regression `CaptureRecoveryPhase2CoreTests/testMacAdvancedSidebarHeaderAdaptsWithoutWideningScrollContent` passed (`1/1`) and compiled the macOS app successfully; `git diff --check` passed. The required `scripts/build.sh` run passed capture fixtures (`47/47`) and the new layout test passed within `CaptureRecoveryPhase2CoreTests` (`58/58`), while the broad desktop plan retained eight already-documented unrelated diagnostics/MIDI/coach/source-contract failures; its duplicate rerun was stopped after those same failures were established.
- Follow-up notes: the root cause was an oversized intrinsic content width inside the Advanced sidebar's vertical `ScrollView`: the header forced its long action buttons into one horizontal row and forced three status pills into another, allowing the inner stack to become wider than the sidebar and causing SwiftUI to center/clip its leading edge. Constrained the scroll content to the proposed sidebar width, made the identity/actions use `ViewThatFits` with stacked fallbacks, and changed status pills to an adaptive grid. Long status labels now wrap within the sidebar. No capture, DVS decode, playback, persistence, export, signing, or routing behavior changed. Visually verify after a full app relaunch at the same narrow window width shown in Karl's screenshot.
## 2026-07-22 (low-latency Rane DVS input)
- Selected task: address the confirmed ~149 ms Stage-1 input latency floor after Karl's fresh hardware test still showed bad backward motion, missing reversals/flicks, wrong stop/resume position, pitch mismatch, lag, and stutter while forward alone worked
- Files changed: `ScratchLab/Models/TimecodeCMSampleBufferAdapter.swift`, `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLabDesktopTests/TimecodeLiveTapTests.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `DEV_LOG.md`, `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`
- Build result: focused low-latency adapter + Advanced layout run passed `22/22`; the dedicated PCM adapter regression passed independently; `scripts/build.sh` passed capture fixtures `47/47`, all DVS/timecode suites including updated `TimecodeLiveTapTests` `21/21`, and the broad plan retained its same eight unrelated diagnostics/MIDI/coach failures (`313` tests, `8` failures). The duplicate broad pass was stopped after the complete first-plan results were established. `git diff --check` passed.
- Follow-up notes: the live snapshot still reported `buf[0]:7168samp` at 48 kHz, proving a 149 ms input chunk before decode/playback. Added a DEBUG-only `AVAudioEngine` input-node tap bound to the selected Core Audio device and requesting 256 frames (~5.3 ms at 48 kHz). It uses the same channel-pair selection, carrier diagnostics, retention, capture tooling, and callback as the old adapter. While running, the 7,168-frame AVCapture path is suppressed for DVS only so the decoder is not double-fed; routine audio/video recording remains on the existing AVCapture session. If device binding or engine start fails, the old AVCapture path remains active as a safe fallback. Hardware verification is mandatory: after full relaunch, `Adapter diag` must begin `AVAudioEngine Float32 non-interleaved` and show a small `f` count rather than `7168samp`; only then assess reversal/lag/pitch/position feel. Also corrected the Advanced sidebar constraint properly with a finite `GeometryReader` width after confirming `maxWidth: .infinity` was ineffective inside a vertical ScrollView.
## 2026-07-22 (persist playback scheduling direction evidence)
- Selected task: determine whether the latest hardware session ever reached backward scheduling without asking Karl to manually search/copy Xcode console text
- Files changed: `ScratchLabDesktop/Services/ScratchSamplePlaybackController.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLabDesktopTests/ScratchSamplePlaybackControllerTests.swift`, `DEV_LOG.md`, `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`
- Build result: focused `ScratchSamplePlaybackControllerTests` passed `76/76`; `git diff --check` passed
- Follow-up notes: neither the app diagnostics JSON nor macOS unified log contained the ephemeral Xcode `schedulingDir` print lines from the prior session, so absence of `backward` could not be treated as evidence. Extended the existing queue-consistent `DVSPlaybackDiagnostics` with the actual last scheduler direction plus cumulative forward/backward successfully-scheduled grain counts, reset on sample load/unload. Advanced now displays `Last schedule: OK · dir=...` and `Scheduled grains: forward N · backward N`, making the next hardware result observable in-app without console searching. This is instrumentation only; scheduling behavior was not changed.

## 2026-07-22 (exact DVS bridge-gate diagnostics)
- Selected task: verify the proposed backward-motion bridge-gate theory against the current code and remove the need to capture ephemeral Xcode trace output during hardware testing
- Files changed: `ScratchLab/Models/TimecodePlaybackBridge.swift`, `ScratchLabDesktop/Views/TimecodeControlCard.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLabDesktopTests/TimecodePlaybackBridgeTests.swift`, `DEV_LOG.md`, `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`
- Build result: focused `TimecodePlaybackBridgeTests` passed `21/21` after the final change and compiled the macOS app; `scripts/build.sh` capture fixtures passed `47/47`, its completed first desktop plan retained the same eight pre-existing unrelated CaptureReliability diagnostics/MIDI/coach/source-contract failures, and all DVS/timecode plus `ScratchSamplePlaybackControllerTests` (`76/76`) passed; the script's redundant second desktop invocation was stopped after the complete first-plan result; `git diff --check` passed
- Follow-up notes: static verification disproved the most specific adapter-confidence/race claims. The stability filter is rebuilt from the live `minConfidence`, the adapter uses the same threshold, the no-accepted-frame path clears direction/rate/timeline, and the bridge evaluates synchronously immediately after `flushDecode()`. Added `TimecodePlaybackBridgeDecision` to preserve the exact evaluated gate instead of collapsing health/stale/confidence/timeline/direction into `blockedByBadSignal`. The bridge also retains per-gate session counts so a transient reversal-time block remains visible after the platter stops. Advanced now shows `Bridge gate`, `Bridge blocks`, actual last scheduler direction, and forward/backward successful-grain counts together. This is diagnostic instrumentation only; no bridge gate, decoder threshold, rate, scheduling, or playback behavior was loosened or tuned. Hardware verification still must first confirm the new low-latency adapter is active.

## 2026-07-22 (low-latency Rane tap format negotiation)
- Selected task: fix the concrete startup failure found while monitoring Karl's live DVS session: the new `AVAudioEngine` path logged `Low-latency input has unusable format` and fell back to the old 7,168-frame AVCapture buffers
- Files changed: `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `ScratchLabDesktopTests/TimecodeLiveTapTests.swift`, `DEV_LOG.md`, `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`
- Build result: focused `TimecodeLiveTapTests` passed with exit code `0`, including the new hardware-valid/pre-start-output-empty regression; `git diff --check` passed. Required `scripts/build.sh` passed capture fixtures `47/47`; its completed first desktop plan retained the same eight pre-existing unrelated `CaptureReliabilityPhase1CoreTests` failures (`313` tests, `8` failures), while the edited app and DVS/timecode tests compiled and ran. The script's redundant second desktop invocation was stopped after the complete first-plan baseline was established.
- Follow-up notes: the startup code checked only `inputNode.outputFormat(forBus: 0)` immediately after rebinding the AUHAL device. That format can still be the valid pre-start placeholder (`0 Hz / 0 channels`), so ScratchLab incorrectly rejected the Rane before the engine could negotiate its stream. The input-node contract says hardware availability is determined from `inputFormat(forBus:)`. The tap now validates the Rane hardware input format, uses the explicit output format when already available, and otherwise installs with `format: nil` so `AVAudioEngine` inherits/negotiates the hardware format during start. Failure diagnostics now print both hardware and output rate/channel counts; success diagnostics identify explicit versus inherited format. No decoder, pair selection, bridge gate, rate scale, scheduler, or playback tuning changed. Real hardware verification is still required before the task is complete.

## 2026-07-23 (restore Rane USB pairs when low-latency binding renders mono)
- Selected task: fix Karl's fresh Debug-build regression where Advanced showed no USB channel-pair signal after the low-latency tap began running
- Files changed: `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `ScratchLabDesktopTests/TimecodeLiveTapTests.swift`, `DEV_LOG.md`, `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`
- Build result: focused `TimecodeLiveTapTests` passed with exit code `0`, including the new selected-pair channel-availability regression; `git diff --check` passed. Required `scripts/build.sh` passed capture fixtures `47/47`; its completed first desktop configuration ran the edited timecode tests successfully and retained the same eight unrelated baseline failures across the broad plan. The redundant second configuration was stopped after the complete first result was recorded.
- Follow-up notes: automatic JSONL inspection established the regression precisely: the new path was active but emitted `AVAudioEngine Float32 non-interleaved, 1ch, 4800f @ 48000 Hz`, near-silent mono data, so pair 3/4 could not exist. The persisted selected UID was still the correct `AppleUSBAudioEngine:Rane:Rane ONE MKII:A:1,2`, and macOS reported the device as 14-input/10-output. The AUHAL bind order was wrong: Apple requires input I/O to be enabled on element 1 before setting `kAudioOutputUnitProperty_CurrentDevice`; setting the device first may return `noErr` while the unit continues rendering the system-default mono input. The code now enables input first and verifies CurrentDevice read-back equals the requested Rane AudioDeviceID. It also separates "tap installed" from "tap proven usable": legacy AVCapture is suppressed only after a real tap buffer contains enough channels for the chosen pair (4 channels for pinned 3/4, 2 for Auto). An undersized/mono callback logs the exact channel mismatch, tears down the tap off the realtime callback, and immediately restores the working AVCapture feed. No decoder, sign, confidence, rate, bridge, scheduler, or playback tuning changed. Hardware verification remains required.

## 2026-07-31
- Selected task: preserve the six Rane DVS hardware captures and add deterministic offline fixture replay tests
- Files changed: `ScratchLabDesktopTests/Fixtures/Timecode/hardware_fixture_expectations.json`, six small Float32 WAV excerpts in `ScratchLabDesktopTests/Fixtures/Timecode/`, `ScratchLabDesktopTests/TimecodeFixtureValidationTests.swift`, `TASKS.md`, `DEV_LOG.md`, `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`; full local copies added under ignored `debug_captures/timecode/hardware_fixtures/20260731/`
- Build result: all 24 full-capture files matched the app-container originals by size and SHA-256; focused `TimecodeFixtureValidationTests` passed 21/21; Debug macOS build passed; `scripts/build.sh` capture fixtures passed 47/47 and the new timecode fixture tests were green, but the broad desktop plan reproduced its existing unrelated Advanced diagnostics, MIDI learn/clear, and Coach pause failures and the duplicate pass was interrupted; `git diff --check` passed
- Follow-up notes: each tracked excerpt is a sample-exact 11,520-frame (0.24-second) slice of physical pair 3/4, remains below the repository's 100 KB audio threshold, and is not referenced by either app target. Tests pin provenance/hashes, format and sample integrity, stationary rejection, carrier-speed ordering, calibrated physical direction, and full local callback continuity. Raw decoder direction is opposite the physical labels because its phase sign convention differs from diagnostics; the existing Rane ONE MKII profile's `Invert direction ON` calibration corrects all five moving fixtures, so no production decoder or playback behavior was changed. Remaining hardware risk is transition coverage: reversals, fine flicks, baby scratches, start/stop, needle lift, and position variation are not yet recorded.

## 2026-07-31 (remaining DVS capture labels)
- Selected task: expose and verify the remaining DEBUG DVS fixture capture labels
- Files changed: `ScratchLabDesktopTests/TimecodeLiveTapTests.swift`, `TASKS.md`, `DEV_LOG.md`, `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`; the complete label list was already present in the uncommitted recorder implementation in `ScratchLab/Models/TimecodeCMSampleBufferAdapter.swift`
- Build result: focused `TimecodeLiveTapTests` passed `25/25`; direct Debug macOS build passed; `scripts/build.sh` capture fixtures passed `47/47` and its first broad configuration completed with the existing unrelated diagnostics/MIDI/coach/notation failures before the redundant second configuration was interrupted; `git diff --check` passed
- Follow-up notes: verified that Advanced uses `DebugTimecodeCapture.labels` as its single picker source and added an exact ordered-list regression for the six baseline plus nine remaining labels (`slow_reversals`, `fast_reversals`, `fine_flicks`, `baby_scratch`, `start_stop`, `needle_lift`, `position_start`, `position_middle`, `position_end`). No recorder format, decoder, threshold, playback, export, signing, entitlement, or production behavior changed. Nothing was committed, staged, or pushed. Hardware capture should proceed with `slow_reversals` first.

## 2026-07-31 (complete Rane DVS hardware fixture set)
- Selected task: capture, validate, and preserve the complete 15-label Rane DVS hardware fixture protocol before turntable access ends
- Files changed: ignored local archive `debug_captures/timecode/hardware_fixtures/20260731/`, `TASKS.md`, `DEV_LOG.md`, `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`
- Verification result: all nine remaining valid recordings are 48 kHz/14-channel captures with gapless callback sequences/source-frame continuity, monotonic callback timestamps, finite/unclipped pair 3/4 samples, and no manifest errors. Offline window analysis confirmed repeated direction transitions for reversals/flicks/baby scratch, alternating moving/held states for start/stop, four needle-loss/reacquisition cycles, and stable approximately 1 kHz carrier at outer/middle/inner record positions.
- Follow-up notes: the first `fine_flicks` attempt had physical channel 4 effectively silent and was rejected; a healthy replacement was recorded after restoring the connection. The app-container now contains exactly 15 intended valid capture folders. Copied the complete 1.4 GB capture tree and five phone videos into the 2.4 GB ignored second-location archive. All 61 capture-tree files (60 fixture files plus `.DS_Store`) and all five videos matched their originals by SHA-256. Nothing was committed, staged, or pushed. Hardware can be disconnected; next work should extract deterministic transition fixtures and test the production pipeline offline.

## 2026-07-31 (offline DVS transition/dropout/position fixtures)
- Selected task: add deterministic offline DVS transition/dropout/position fixtures
- Files changed: `ScratchLabDesktopTests/Fixtures/Timecode/hardware_transition_expectations.json`, nine small Float32 WAV excerpts in `ScratchLabDesktopTests/Fixtures/Timecode/`, `ScratchLabDesktopTests/TimecodeFixtureValidationTests.swift`, `TASKS.md`, `DEV_LOG.md`, `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`
- Build result: focused `TimecodeFixtureValidationTests` passed `25/25` in both scheme configurations; the Debug macOS build succeeded; `scripts/build.sh` capture-pipeline fixtures passed `47/47` and its completed first broad desktop configuration retained the same unrelated eight diagnostics/MIDI/coach/source-contract failures; the duplicate configuration was interrupted after reproducing that baseline. `git diff --check` and JSON validation passed.
- Follow-up notes: extracted nine sample-exact, 512-frame-aligned, 11,520-frame/0.24-second physical pair 3/4 WAVs, each 92,252 bytes and therefore below the repository's 100 KB audio ceiling. The provenance catalog pins each source folder/start frame, scenario, expected physical direction/carrier range, excerpt hash, and hashes for the four full-capture artifacts. Tests replay real audio incrementally through the production Rane-calibrated pipeline and verify both physical directions for slow/fast reversals, fine flicks, and baby scratch; trusted platter output stops after captured start/stop and needle-lift transitions; start/middle/end positions remain stable forward at approximately 1x; and all 15 local archives remain hash-correct and callback-gapless. No production decoder, threshold, calibration, playback, resource, export, signing, or entitlement behavior changed. Nothing was committed, staged, or pushed.
## 2026-07-31
- Selected task: user-requested complete real-fixture DVS decoder-to-playback scheduling replay, added as the first unchecked task in `TASKS.md`
- Root cause: the captured `slow_reversals` fixture reached `TimecodeControlPipeline` and the validation-gated `TimecodePlaybackBridge` with correct positive/negative rates, but `ScratchSamplePlaybackController` silently rejected every resulting 4–5-step grain as `nearStop`. That threshold was designed for the MIDI path because `AVAudioUnitVarispeed` cannot run below 0.25×; it unintentionally made real DVS motion around 0.05–0.14× completely inaudible.
- Files changed in this slice: `ScratchLabDesktop/Services/ScratchSamplePlaybackController.swift`, `ScratchLabDesktopTests/ScratchSamplePlaybackControllerTests.swift`, `ScratchLabDesktopTests/TimecodeFixtureValidationTests.swift`, `TASKS.md`, `DEV_LOG.md`, `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`
- Implementation: injected the scheduler clock through the controller initializer with the production default unchanged; added a deterministic harness that replays real 512-frame hardware callbacks at the production 60 Hz control cadence through Rane calibration, validation bridge, step conversion, and the real Ahhh grain scheduler; added complete-path assertions for forward/backward reversal scheduling, slow/normal/fast audible rate ordering, fail-closed dropout behavior, and bounded sample positions. For DVS calls only (identified by their explicit `segmentWindow`), sub-0.25× source grains are linearly stretched to the requested output window and scheduled at node rate 1× while retaining exact source displacement and reporting the physical rate. Calls without `segmentWindow` keep the existing MIDI near-stop anti-sputter behavior.
- Verification: focused new regressions passed 5/5; complete `ScratchSamplePlaybackControllerTests` + `TimecodeFixtureValidationTests` passed 105/105; direct macOS arm64 Debug build succeeded; `git diff --check` passed. Required `scripts/build.sh` compiled the app and ran the new DVS tests successfully, then exited in its broad plans on the documented unrelated Advanced diagnostics/MIDI/Coach and notation source-contract failures (including `testAdvancedDiagnosticsExplainCameraAndTickActivity`, MIDI learn/summary/clear tests, Coach pause, template label, and cross-platform notation parity).
- Follow-up notes: this closes the fixture-proven silent slow-motion layer without changing decoder polarity, profile calibration, confidence, EMA, MIDI semantics, capture/export, signing, or entitlements. Hardware listening remains necessary to judge interpolation sound quality at extremely slow motion and at sample-wrap boundaries; no further recordings are required unless that listening check exposes a specific case. Nothing was committed, staged, or pushed.
## 2026-07-31
- Selected task: user-requested low-latency DVS tap freshness, truthful diagnostics, and safe fallback fix
- Root cause: Karl's steady-normal hardware snapshot showed healthy pair-3/4 quadrature (`usable`, ~1.014x, confidence 0.781) but the authoritative adapter was still `Int32 interleaved ... 7168samp`, while `Capture device` misleadingly claimed `AVAudioEngine ... actual=live`. `MacCaptureEngine` appended that claim immediately after `engine.start()` and used a sticky `timecodeInputTapUsable` Boolean: once any callback marked it true, AVCapture stayed suppressed indefinitely even if AVAudioEngine stopped delivering. The nil/inherited format selected when the pre-start output format was empty could also render a default mono stream despite the Rane hardware format being 14-channel.
- Files changed in this slice: `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `ScratchLabDesktopTests/TimecodeLiveTapTests.swift`, `TASKS.md`, `DEV_LOG.md`, `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`
- Implementation: the Rane tap now uses its explicit hardware format whenever the client output format is missing or narrower than the hardware channel count. A lock-protected `LowLatencyTimecodeTapHeartbeat` records only successfully adapted callbacks and retains observed Float32 layout, channel count, frame count, sample rate, callback count, and monotonic timestamp. Startup alone stays on fallback; AVCapture is suppressed only while the latest valid callback is at most 100 ms old; an expired heartbeat restores the legacy callback on its next buffer; and any resumed valid callback immediately recovers low-latency routing. Undersized buffers retain a visible rejection and stop the tap safely. The capture diagnostic now reports `route=AVAudioEngine` or `route=AVCapture fallback`, installation state, callback age, and observed format instead of the startup-only `actual=live` claim.
- Verification: focused `TimecodeLiveTapTests` passed 27/27 in both scheme configurations; combined DVS analyzer/live-tap/live-integration/bridge suites passed 102/102; direct macOS arm64 Debug build succeeded; `git diff --check` passed. Required `scripts/build.sh` compiled and exercised the new regressions successfully, then exited on the existing unrelated Advanced diagnostics/MIDI/Coach and notation source-contract failures.
- Follow-up notes: no decoder threshold, confidence, direction calibration, EMA, scheduler, capture/export schema, signing, bundle ID, or entitlement changed. Hardware verification is still required: after a full relaunch, the adapter must remain `AVAudioEngine Float32` with multichannel small buffers; `DVS tap heartbeat` must show `route=AVAudioEngine`, a low callback age, 14 channels and a small frame count; the Receiving badge should remain stable and bridge stale counts should stop increasing. Nothing was committed, staged, or pushed.

## 2026-07-31 (main-thread-independent DVS reversal control)
- Selected task: keep reversal decode and playback scheduling independent of the main thread
- Root cause: the latest reversal recording showed a healthy, fresh AVAudioEngine hardware callback while the synchronous main-thread DVS pass took 0.4–2.1 seconds. That delayed subsequent flushes by up to 2.2 seconds, forced bounded ingress to discard 99 stale callbacks, and made reversal playback lag and sound incorrect even though capture and carrier decoding were healthy.
- Files changed in this slice: `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `ScratchLab/Models/TimecodeInputTap.swift`, `ScratchLabDesktopTests/TimecodeLiveTapTests.swift`, `TASKS.md`, `DEV_LOG.md`, `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`
- Implementation: replaced the main-queue deferred tick with a lifecycle-managed `DVSControlProcessingLoop` whose serial `userInteractive` queue owns its 60 Hz timer, coalescing, cancellation generation, and elapsed clock. The complete `flushDecode → bridge.evaluate → forwardTimecodeDrive` pass now runs on that worker; elapsed input is capped at 250 ms after any worker stall. Playback-active scheduling state is lock-protected and synchronously available to the callback while its UI mirror and DVS diagnostics publish asynchronously on main. `TimecodeInputTap` keeps its existing locked DSP state but sends only one main-thread observer invalidation after push, drain, or reset.
- Verification: focused `TimecodeLiveTapTests` passed `38/38`; combined `TimecodeLiveTapTests`, `TimecodePlaybackBridgeTests`, and `ScratchSamplePlaybackControllerTests` passed `138/138` in both scheme configurations. The Debug app compiled and capture fixtures passed `47/47`. The required `scripts/build.sh` completed both broad configurations and exited code 65 only on the documented unrelated Advanced diagnostics/MIDI/Coach and notation source-contract failures; all edited DVS/playback suites passed. `git diff --check` passed.
- Follow-up notes: real hardware must confirm the multi-second reversal lag is gone. Use Rane ONE MKII (DEBUG), physical pair 3/4, Invert direction ON, Live Tap ON, and Drive Playback ON. No decoder threshold, calibration, displacement, capture/export, signing, or entitlement changed. Nothing was committed, staged, or pushed.

## 2026-07-31 (continuous DVS grain timing under timer jitter)
- Selected task: make DVS grain timing continuous under real 60 Hz timer jitter
- Root cause: after DVS control moved to its dedicated worker, the playback controller still applied a second exact `1/60`-second wall-clock limiter. Dispatch timer jitter legitimately delivered some ticks slightly early; those calls were rejected after the drive converter had integrated their platter displacement. The next accepted call therefore scheduled accumulated displacement against only the newest tick's segment window, creating an inflated pitch grain and an output hole. Scheduling exactly one elapsed window also left no reserve against ordinary control/audio clock jitter.
- Files changed in this slice: `ScratchLabDesktop/Services/ScratchSamplePlaybackController.swift`, `ScratchLabDesktopTests/ScratchSamplePlaybackControllerTests.swift`, `ScratchLabDesktopTests/TimecodeFixtureValidationTests.swift`, `TASKS.md`, `DEV_LOG.md`, `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`
- Implementation: DVS calls identified by their explicit segment window bypass the unchanged MIDI limiter. DVS elapsed time accumulates across zero-step or otherwise unscheduled updates, is capped at 250 ms, and is consumed/reset only with the matching accumulated step displacement. The scheduler estimates remaining queued output time and restores a 4 ms reserve only when depleted; same-direction grains maintain that bounded reserve, while a reversal interruption rebuilds it once. Physical diagnostic rate remains based on the matched control window. The real-fixture playback harness no longer adds a one-microsecond workaround to pass the former limiter and now accepts variable control intervals.
- Verification: deterministic tests cover 120 alternating 15.5/17.8 ms steady-1x ticks without a discarded grain or catch-up spike, zero-step window accumulation, bounded reserve, and reversal reset. Captured `steady_normal` timecode replays through the production pipeline/bridge/converter/scheduler under the same jitter and stays within 0.85–1.15x. Focused playback and fixture suites passed `112/112`; direct Debug macOS build succeeded; capture-pipeline fixtures passed `47/47`; `git diff --check` passed. Required `scripts/build.sh` completed both configurations and exited code 65 only for the documented unrelated Advanced diagnostics/MIDI/Coach and notation source-contract baseline failures; all edited suites passed.
- Follow-up notes: the 15 saved capture set is sufficient for further decoder, reversal, dropout, position, and scheduler work. Do not request repeated turntable runs; reserve one short hardware listening check for the final output path. MIDI behavior, decoder thresholds, Rane calibration, displacement, capture/export, signing, and entitlements were unchanged. Nothing was committed, staged, or pushed.

## 2026-07-31 (DVS reversal grain handoffs stay continuous; remaining gaps proven decode-driven, not synthesized)
- Selected task: the follow-up hardware listening check after the timer-jitter fix still reported Ahhh sounding "slightly garbled like its under water" during reversals. A DEBUG-only `scheduledGrainObserver` on `ScratchSamplePlaybackController` plus `DVSOfflineGrainRendererTests` (added this arc) replay the saved full-length hardware captures through the unmodified production decode → bridge → step-converter → scheduler path and reconstruct the actual `AVAudioPlayerNode` output offline. That renderer proved `steady_normal` gapless/overlap-free, but `slow_reversals`/`fast_reversals` had 18/36 gaps and 19/45 *interrupted overlaps* — every DVS direction change used `AVAudioPlayerNodeBufferOptions.interrupts`, discarding the bounded queued tail and applying a zero-edge fade, turning every reversal into a deterministic PCM truncation.
- Root cause and fix: `ScratchSamplePlaybackController.positionDidChangeOnQueue` now only applies `.interrupts`/the edge fade for the legacy MIDI reversal path (`dvsWindow == nil`); DVS reversals preserve the existing bounded (~4 ms) queue reserve and enqueue the reversed grain immediately after it, so direction-change latency is bounded by that reserve instead of an audible truncation.
- Files changed in this slice: `ScratchLabDesktop/Services/ScratchSamplePlaybackController.swift`, `ScratchLabDesktopTests/ScratchSamplePlaybackControllerTests.swift`, `ScratchLabDesktopTests/DVSOfflineGrainRendererTests.swift`, `TASKS.md`, `DEV_LOG.md`, `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`
- Investigation of the resulting larger gap totals: removing the interrupt path surfaced more measured "gaps" than before (`slow_reversals`: 36 gaps/3.4520s; `fast_reversals`: 43 gaps/1.2113s) purely because the old interrupts branch never left silence to begin with (it truncates and starts immediately, so it never counted as a gap) — it wasn't actually eliminating the underlying no-drive intervals, just hiding them behind a click. To tell a real scheduling defect apart from expected fail-closed behavior, the offline harness now also records each control tick's `TimecodePlaybackBridge.lastDecision` and the controller's `lastScheduleSkippedReason`, and a new `unexplainedGapDuration` correlates every reported gap interval against those records — a gap only counts as "explained" if every tick inside it is either a non-driving bridge decision (fail-closed: low confidence/unusable signal/stale/missing timeline near a genuine zero-crossing or decode-reacquisition) or a legitimate controller skip (`nearStop`/`tinyGrain`/`noMotion`/`priming`/`noDirection`). Both reversal fixtures report `unexplainedGap=0.0000s` — every millisecond of measured gap time is attributable to the bridge/controller correctly withholding a grain near a genuine platter-velocity zero-crossing, not a scheduling defect. No motion was synthesized over those intervals and none was added; fail-closed behavior is retained exactly as required.
- Verification: `ScratchSamplePlaybackControllerTests` + `DVSOfflineGrainRendererTests` passed `86/86` in both scheme configurations, including `testDVSReversalPreservesTheBoundedQueueCushion` and the new `overlapCount == 0` / `unexplainedGapDuration == 0` regressions on the saved reversal captures; direct Debug macOS build and build-for-testing passed; `python3 scripts/test_capture_pipeline.py` passed `47/47`; `git diff --check` passed. `scripts/build.sh` reproduced exactly the established unrelated baseline failures (`testAdvancedDiagnosticsExplainCameraAndTickActivity`, MIDI learn/clear, `testCoachPauseHoldsPoseFromAudioTimeAndAvoidsPausedTimelineWork`, `CrossPlatformNotationParityTests`, `NotationSheetTests.templateDemoStillOwnsBabyScratchTemplateLabel`) with no new failures, then stopped via its own `set -e` before the iOS/macOS/watch build stages as it always does at this baseline; a direct iOS build (code signing disabled to bypass an unrelated local watch-companion provisioning-profile mismatch, not a code issue) succeeded separately.
- Follow-up notes: decoder thresholds, Rane calibration, displacement, capture/export, signing, and entitlements were unchanged. Nothing was committed, staged, or pushed. The offline WAVs render to the sandboxed test host's `~/Library/Containers/com.machelpnz.scratchlab/Data/Library/Application Support/ScratchLab/DebugCaptures/Timecode/OfflineRenders/` for a listening comparison against the earlier hardware report before requesting any further turntable time.

## 2026-07-31 (Catmull-Rom fix for static-like clicking in the sub-0.25x DVS software time-stretch path)
- Selected task: Karl's listening comparison of the three offline WAVs found `steady_normal` clean, `fast_reversals` "sounds good," but `slow_reversals` has "static-like clicking" and "does not sound like a natural forward/reverse Ahhh." Added as the new first unchecked task and worked immediately, per instruction not to reconnect the turntable yet.
- Investigation before any code change: extended `DVSOfflineGrainRendererTests` with three independent measurements against the real saved captures, to prove rather than assume where the defect lives.
  1. Grain-boundary PCM value/slope jumps (comparing each scheduled grain's PCM directly against the next): comparable between software-time-stretched-involved boundaries and normal-only boundaries in both `slow_reversals` and `fast_reversals`. Not the differentiator.
  2. Gap-edge click amplitude (the real, legitimate fail-closed silence gaps proven correct in the prior slice are hard cuts with no fade — checked whether the audio bordering them is far from zero): comparable between fixtures, and if anything slightly *worse* in `fast_reversals` (mean 0.160/max 0.553) than `slow_reversals` (mean 0.127/max 0.505) — since `fast_reversals` was reported clean, this rules out gap-edge clicks as the cause too.
  3. Oscillation/high-frequency content ("richness" — a sign-change-rate proxy for how much real waveform detail a grain's PCM retains): software-time-stretched grains measured systematically lower than normal grains in *every* fixture (`fast_reversals`: 462.7/sec vs 4606.4/sec normal, roughly a 10x drop; `steady_normal` has zero software-slow grains at all). The decisive fact distinguishing the two reversal fixtures: `slow_reversals` spends ~38% of all its grains on the software-stretch path (781/2073) versus only ~6% for `fast_reversals` (75/1180) — the same underlying quality gap exists in both, but only dominates the perceived character where it happens this often.
- Root cause and fix: `ScratchSamplePlaybackController.copyTimeStretched` reconstructed a stretched grain with plain linear interpolation between the two raw samples surrounding each output position — with as few as 2-9 real raw samples stretched across a ~735-frame output window at extreme speed ratios, this collapses much of the true waveform's curvature into straight-line segments. Fixed by switching to Catmull-Rom cubic interpolation (using one additional real sample of context on each side, already present in the copied segment — no wider source read, no change to needle position/frame accounting) whenever at least 4 raw source frames are available; grains with only 2-3 raw frames keep the original linear method, since there is no additional real information for a cubic fit to use there.
- Files changed in this slice: `ScratchLabDesktop/Services/ScratchSamplePlaybackController.swift`, `ScratchLabDesktopTests/ScratchSamplePlaybackControllerTests.swift`, `ScratchLabDesktopTests/DVSOfflineGrainRendererTests.swift`, `TASKS.md`, `DEV_LOG.md`, `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`
- Measured improvement (real fixture, before/after): `slow_reversals` software-slow-grain richness rose from 601.0/sec to 648.5/sec; `fast_reversals` rose from 462.7/sec to 506.3/sec. This is real but modest (~8-9%) — the smallest change the measurement actually justified, not a guaranteed complete fix. `testOfflineRenderReconstructsSlowReversalsCapture` now asserts a richness floor (620/sec) on the real fixture that would fail if a future change regressed back toward plain-linear's measured 601/sec baseline. `testDVSSoftwareTimeStretchUsesCubicInterpolationNotPlainLinear` (new, in `ScratchSamplePlaybackControllerTests`) independently re-derives what plain linear interpolation would have produced for a real scheduled grain from the bundled `ahhh.wav` asset and proves the observed output differs from it — a determinism check independent of any fixture.
- Verification: `ScratchSamplePlaybackControllerTests` + `DVSOfflineGrainRendererTests` passed `87/87` in both scheme configurations; direct Debug macOS build and build-for-testing passed; a direct iOS build (code signing disabled to bypass the same unrelated local watch-companion provisioning-profile mismatch noted in the prior slice) succeeded; `python3 scripts/test_capture_pipeline.py` passed `47/47`; `scripts/build.sh` reproduced exactly the established unrelated baseline (Advanced diagnostics, MIDI learn/clear, Coach pause, notation source-contract) with no new failures before stopping via its own `set -e`; `git diff --check` passed.
- Follow-up notes: needle position/frame accounting, direction logic, decoder thresholds, Rane calibration, and the reversal queue-handoff fix from the prior slice are all byte-for-byte unchanged. The three offline WAVs (including the regenerated `slow_reversals`) are at the same sandboxed container path as before for Karl's next listen. No hardware/turntable time was requested. Nothing was committed, staged, or pushed.

## 2026-07-31 (DVS grain-timing/reversal/time-stretch arc confirmed by ear; Rane DVS profile preserved and verified)
- Selected task 1: Karl listened to the regenerated offline WAVs and confirmed `slow_reversals` now sounds clean and natural, alongside `steady_normal` and `fast_reversals`. This closes the entire DVS grain-timing/reversal/time-stretch arc (steady-motion timer jitter → reversal queue-tail truncation → slow-reversal software time-stretch quality), all three fixes self-contained to `ScratchSamplePlaybackController.swift` plus its tests, with decoder thresholds, calibration, direction logic, capture/export, signing, and entitlements untouched throughout. Moved to the next unchecked `TASKS.md` item.
- Selected task 2: "Preserve/verify Rane DVS profile defaults or user-facing setup instructions." The task's own `TASKS.md` wording (`DVS pair Auto`, `Invert direction OFF`) describes intermediate values from mid-arc hardware history — right after Karl physically corrected reversed RCA wiring, before a real internal sign-convention mismatch was found between the live diagnostic classifier (`TimecodeSignalDiagnostics.estimatePhaseOffset`) and the actual decoder (`TimecodePhaseDecoder.extractPhaseDelta`), which meant "Invert ON gives correct audible forward" was compensating for a software sign mismatch rather than real hardware polarity. Every hardware session since (traced across dozens of `AI_HANDOFF.md`/`DEV_LOG.md` entries) confirmed the actual final configuration is physical pair **3/4 pinned** (not Auto), Invert **ON** (not OFF), Min confidence 0.10 — already correctly encoded in `TimecodePrototypeProfile.raneOneMkiiDebug()` and `TimecodeControlCard.applyPreset`, and the profile's core values were already regression-tested.
- Files changed in this slice: `ScratchLab/Models/TimecodePrototypeProfile.swift`, `ScratchLabDesktop/Views/TimecodeControlCard.swift`, `ScratchLabDesktopTests/TimecodePrototypeProfileTests.swift`, `ScratchLabDesktopTests/TimecodeLiveTapTests.swift`, `TASKS.md`, `DEV_LOG.md`, `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`
- What was actually missing, and what was added: (1) the physical-pair-pinning logic lived only as an inline ternary inside a private SwiftUI method (`TimecodeControlCard.applyPreset`), exercised solely by hand during live hardware sessions with no regression protection — extracted to a pure `static func channelPairSelection(for:)` and covered by `testRaneOneMkiiDebugPresetPinsPhysicalPair3And4`/`testNonRanePresetsRestoreAutoPairSelection`. (2) The parts a Swift preset's field values fundamentally can't capture — which physical USB pair to connect, and how to read an apparent forward/backward mismatch without "fixing" Invert back to the wrong value — were only discoverable by searching scattered hardware-session history. Added `TimecodeControlPreset.setupNote`, shown directly in the preset's profile-summary UI (a plain informational line, distinct from the yellow experimental-warning styling used for `.genericDVS`) whenever the Rane preset is selected, covered by `testRaneOneMkiiDebugPresetHasSetupNoteOtherPresetsDoNot`. (3) The doc comments on the `.raneOneMkiiDebug` enum case and `raneOneMkiiDebug()` only said "validated ... this session" — vague, undated, and silent about the earlier (now-superseded) Invert-OFF belief. Expanded both to state plainly that Invert ON is correct and final and explain why, so a future session doesn't "correct" it back toward the stale intermediate value.
- Verification: `TimecodePrototypeProfileTests` + `TimecodeLiveTapTests` + `DVSControlVinylAnalyzerTests` + `TimecodeDecoderTests` + `ScratchSamplePlaybackControllerTests` + `DVSOfflineGrainRendererTests` passed `208/208` combined in both scheme configurations; direct Debug macOS build succeeded; a direct iOS build (code signing disabled to bypass the same unrelated local watch-companion provisioning-profile mismatch noted in prior slices) succeeded; `python3 scripts/test_capture_pipeline.py` passed `47/47`; `scripts/build.sh` reproduced exactly the established unrelated baseline with no new failures before stopping via its own `set -e`; `git diff --check` passed.
- Follow-up notes: no decoder threshold, calibration value, direction logic, playback behavior, or DVS numeric default was changed in this slice — purely verification plus making the already-correct configuration provably reproducible and durably documented. Nothing was committed, staged, or pushed.

## 2026-07-31 (DVS-to-notation routing plan recorded — planning only, no code)
- Selected task: "Map validated prototype motion to notation/playback as a separate later phase." ChatGPT (architect) supplied the explicit phased plan via `<<<PLAN...PLAN>>>`, scoped as planning only. Per the plan's own instruction and `SOUL.md`'s architect/executor split, no production code, routing, UI toggle, persistence, or export change was written this slice.
- Files changed: `TASKS.md`, `DEV_LOG.md`, `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md` only
- Plan recorded in `TASKS.md` (full text) and `AI_HANDOFF.md` (verbatim). Summary: playback (`TimecodePlaybackBridge`) and a future notation route stay fully independent — a separate DEBUG/macOS-only `TimecodeNotationBridge` (not built yet) would get its own `notationRoutingEnabled` flag defaulting false, consume `TimecodeControlPipeline` output directly (not the audio playback controller), and share pure trust-decision logic with playback (same fail-closed checks: Control Prototype mode, fresh Live Tap, usable signal, confidence above threshold, known direction, valid platter timeline, validation passed, no replay conflict) without sharing enable flags or output. Recording design: accumulate only trusted `.timecodeLive` timeline samples during active take recording, rebase to take-relative clock, dedupe by timestamp, never extrapolate through stale/stopped/unknown-direction gaps; at finalization run through the existing pure `derivePrimitives(from:)` grammar into `DetectedNotationRecordMovementEvent`s tagged `source: "timecode_live"`. Source precedence: DVS authoritative for direction/travel when trusted and enabled; camera continues for diagnostics/overlays only, never merged into DVS movement events; no trusted DVS motion means fail closed, not invented/blended motion.
- Five future implementation slices recorded as new unchecked `TASKS.md` tasks, in order: (1) pure trust gate/accumulator/adapter with saved-fixture tests, no UI; (2) DEBUG-only capture lifecycle wiring with an independent default-off toggle; (3) snapshot/export provenance and deterministic DVS-vs-camera precedence; (4) saved-fixture validation across `slow_reversals`/`fast_reversals`/`baby_scratch`/`start_stop`/`needle_lift`; (5) one final hardware validation only after all offline tests pass. "Keep playback override and notation routing off until explicitly approved" is left active as a standing guardrail, not marked complete.
- Verification: none required — no code changed. `git diff --check` passed (documentation-only diff).
- Follow-up notes: nothing was committed, staged, pushed, reset, or discarded. The next session should not begin slice 1 without confirming that's actually the direction Karl wants to take next, since this was purely a scoping deliverable.

## 2026-07-31 (DVS notation slice 1 — pure trust gate, timeline accumulator, timeline-to-event adapter)
- Selected task: Karl confirmed starting slice 1 of the recorded DVS-to-notation routing plan. Pure logic only, per plan scope: no UI, no capture-lifecycle wiring, no toggle exposed anywhere, no production wiring of any kind.
- Files added: `ScratchLab/Models/TimecodeNotationTrustGate.swift`, `ScratchLab/Models/TimecodeNotationTimelineAccumulator.swift`, `ScratchLab/Models/TimecodeNotationEventAdapter.swift` (all registered in both the `ScratchLab` and `ScratchLabDesktop` Xcode targets, matching every other Timecode model file's dual-target membership), `ScratchLabDesktopTests/TimecodeNotationRoutingTests.swift` (new, 29 tests). Files changed: `ScratchLab.xcodeproj/project.pbxproj`, `TASKS.md`, `DEV_LOG.md`, `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`.
- `TimecodeNotationTrustGate`: a stateless, side-effect-free `evaluate(...)` mirroring `TimecodePlaybackBridge.evaluate`'s exact 8-gate order and thresholds (mode, replay conflict, live tap, signal health, staleness, confidence, timeline presence, direction, validation) — verified never to read or touch `playbackDriveEnabled`, and `TimecodePlaybackBridge` itself was not modified at all. `notationRoutingEnabled` is a plain parameter the caller passes in, not a stored setting.
- `TimecodeNotationTimelineAccumulator`: a value type that rebases each appended window's samples to a take-relative clock and keeps only samples strictly after the last-accumulated (rebased) time — this single cutoff both deduplicates overlapping pipeline windows and guarantees strict monotonicity. Confidence and position are carried through unchanged (never averaged/boosted). Since it only ever appends real samples from windows the caller passes it, a real gap between two appended windows (the trust gate having rejected the interval in between) survives untouched as a real time jump — proven directly by `testAccumulatorDoesNotSynthesizeSamplesAcrossAGap`.
- `TimecodeNotationEventAdapter`: converts a finalized timeline through the existing internal `derivePrimitives(from:)` grammar (the same deterministic primitive derivation captured/camera notation already uses) into `CaptureCore.DetectedNotationRecordMovementEvent`s tagged `source: "timecode_live"`. Only `.directionSegment` primitives become events; idle holds and reversals are not emitted as their own movement events, matching the plan.
- Test coverage: per-condition trust-gate pass/fail for all 8 gates, reusing `TimecodePlaybackBridgeTests`' proven synthetic quadrature-tone technique (no hardware needed for these); accumulator rebasing/dedup/monotonicity/gap-preservation/confidence-passthrough; adapter determinism and idle/reversal exclusion; and five end-to-end saved-fixture replays through the real gate → accumulator → adapter chain (`slow_reversal_transition.wav`, `fast_reversal_transition.wav`, `baby_scratch_transition.wav`, `start_stop_transition.wav`, `needle_lift_transition.wav`), entirely offline against real captured hardware data already checked into the repo.
- Notable finding, not a bug: the full fail-closed trust gate is intentionally more conservative than the raw per-tick `pipeline.currentDirection` check `TimecodeFixtureValidationTests` already uses elsewhere. On these short (0.24s) excerpts, that means only one side of a reversal reliably accumulates enough trusted, contiguous samples to form a complete grammar `DirectionSegment` — asserting "both directions" through the strict chain isn't always achievable from such a short clip. Assertions were scoped to what each excerpt's length actually supports (at least one real, correctly-tagged event for the reversal/flick excerpts; both directions for the longer `baby_scratch` excerpt, which does clear that bar) rather than weakened arbitrarily, with the reasoning documented directly in the test file.
- Verification: `TimecodeNotationRoutingTests` (29/29, one expected skip) + `TimecodePlaybackBridgeTests` + `TimecodeFixtureValidationTests` + `TimecodeLiveTapTests` + `TimecodePrototypeProfileTests` + `ScratchSamplePlaybackControllerTests` + `DVSOfflineGrainRendererTests` + `DVSControlVinylAnalyzerTests` + `TimecodeDecoderTests` passed `288/288` combined in both scheme configurations; direct Debug macOS build succeeded; a direct iOS build (code signing disabled to bypass the same unrelated local watch-companion provisioning-profile mismatch as prior slices) succeeded; `python3 scripts/test_capture_pipeline.py` passed `47/47`; `scripts/build.sh` reproduced exactly the established unrelated baseline with no new failures before stopping via its own `set -e`; `git diff --check` passed.
- Follow-up notes: nothing enables playback or notation routing in production — this slice adds pure logic and tests only. `TimecodePlaybackBridge`/`playbackDriveEnabled` were not touched. Slice 2 (DEBUG-only capture lifecycle wiring with an independent default-off toggle) is next in the plan but was not started — check with Karl before beginning it. Nothing was committed, staged, or pushed.

## 2026-07-31 (DVS notation slice 2 — DEBUG-only capture lifecycle wiring, independent default-off toggle)
- Selected task: Karl confirmed starting slice 2 of the recorded DVS-to-notation routing plan. Wires the slice-1 pure trust gate/accumulator into the real routine-take recording lifecycle, exposes `notationRoutingEnabled` as its own default-off flag, and forwards trusted windows during recording — still no UI, no `DetectedNotationSnapshot`/export change (slice 3's scope), no App Store-facing behavior change.
- Files changed: `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `TASKS.md`, `DEV_LOG.md`, `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`. No new files, no test file changes this slice.
- Design: rather than invent a new pattern, mirrored the existing camera-based `PlatterPositionRecorder` wiring already in `MacCaptureEngine.swift` exactly — same lock-protected start/observe/drain shape around routine-take recording. Added (all `#if DEBUG`): `notationRoutingEnabled` (default `false`), `timecodeNotationAccumulator: TimecodeNotationTimelineAccumulator?`, `timecodeNotationLock`, and `lastDrainedTimecodeNotationTimeline: PlatterPositionTimeline?`. `startRoutineRecording` resets the accumulator to `nil` alongside the platter recorder's own reset; `finalizeRoutineRecording` drains it into `lastDrainedTimecodeNotationTimeline` alongside the platter recorder's own drain — in-memory diagnostic only, exactly matching the platter recorder's own current scope (`DetectedNotationSnapshot`/the v4 export schema untouched).
- New `MacCaptureEngine.forwardTimecodeNotationWindow(pipeline:minConfidence:)`: no-ops unless `notationRoutingEnabled && isRoutineRecording`; otherwise evaluates the slice-1 `TimecodeNotationTrustGate` with `validationRequired: true` and no override parameter exposed at all (satisfies the plan's "must never auto-enable a validation override" by construction, not a runtime check that could be flipped later); on `.trusted`, lazily anchors the accumulator's `takeStartTime` to the first trusted window's own sample time — the pipeline's decode clock domain, not `CACurrentMediaTime()`, since DVS timestamps come from audio buffer decode timing rather than the AppKit run-loop clock the camera recorder anchors to. Wired into `MacAnalyzerView.handleTimecodeFlushTick`, called immediately after (additively, not replacing) the existing `timecodeBridge.evaluate` → `captureEngine.forwardTimecodeDrive` playback path.
- Independence from playback verified two ways: `grep -rn "notationRoutingEnabled"` across both targets shows it referenced nowhere but its own declaration, the one gated call site, and a doc comment — and the new method never reads or writes `dvsPlaybackDriveActive`, `playbackDriveEnabled`, or `timecodeBridge` anywhere in its body (confirmed by direct inspection of the diff). Slice 1's `testTrustGateNeverReadsOrTouchesPlaybackBridgeState` already proves the underlying trust-gate call this method makes never touches playback state, so the guarantee holds end to end, not just at this one new flag.
- Testing boundary, matching established precedent rather than a gap: the `MacCaptureEngine`-level wiring itself has no new XCTest. Verified this mirrors, not breaks, how the codebase already treats this exact class — `grep` confirms the camera-based `PlatterPositionRecorder`'s own equivalent wiring (`platterPositionRecorder`/`lastDrainedPlatterPositionTimeline`) has zero references anywhere in `ScratchLabDesktopTests/` either, and the only `MacCaptureEngine` symbols any test touches are its two `static` pure helpers (`normalizedRecordDirection`, `formattedAudioPercent`) — no test anywhere in the suite instantiates a live `MacCaptureEngine()`, for the same AVFoundation-heaviness reason not attempted here.
- No UI toggle exists anywhere for `notationRoutingEnabled` — kept hidden per "keep playback override and notation routing off until explicitly approved," even though slice 2's own "Done when" only required the flag to exist as a toggle, not that it stay UI-invisible.
- Verification: `TimecodeNotationRoutingTests` (29/29) + `TimecodePlaybackBridgeTests` + `PlatterPositionRecorderTests` + `CaptureReliabilityPhase1CoreTests` (only the established unrelated baseline failing — Advanced diagnostics, MIDI learn/clear, Coach pause) + `ScratchSamplePlaybackControllerTests` + `DVSOfflineGrainRendererTests` + `TimecodePrototypeProfileTests` + `TimecodeLiveTapTests` + `TimecodeFixtureValidationTests` + `DVSControlVinylAnalyzerTests` + `TimecodeDecoderTests` all passed in both scheme configurations; direct Debug macOS build succeeded; a direct iOS build (code signing disabled to bypass the same unrelated local watch-companion provisioning-profile mismatch as prior slices) succeeded; `python3 scripts/test_capture_pipeline.py` passed `47/47`; `scripts/build.sh` reproduced exactly the established unrelated baseline with no new failures before stopping via its own `set -e`; `git diff --check` passed.
- Follow-up notes: nothing enables playback or notation routing in production by default. Slice 3 ("snapshot/export provenance and deterministic DVS-vs-camera precedence") is next in the plan but was not started — check with Karl before beginning it. Nothing was committed, staged, or pushed.

## 2026-07-31 (DVS notation slice 3 — snapshot/export provenance and deterministic DVS-vs-camera precedence)
- Selected task: Karl confirmed starting slice 3 of the recorded DVS-to-notation routing plan. Makes DVS authoritative for platter direction/travel in the captured notation snapshot when routing is enabled and a trusted DVS timeline exists, fails closed to no notation samples otherwise, and carries `source: "timecode_live"` provenance through snapshot/export — still no UI toggle, no App Store-facing behavior change.
- Files added: `ScratchLab/Models/TimecodeNotationCapturePrecedence.swift`. Files changed: `ScratchLab/Models/CaptureCore.swift`, `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `ScratchLabDesktopTests/TimecodeNotationRoutingTests.swift`, `ScratchLab.xcodeproj/project.pbxproj`, `TASKS.md`, `DEV_LOG.md`, `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`.
- Design: rather than modify the complex, already-shipping `RoutineNotationFusionEngine`, precedence is resolved BEFORE calling it (deciding which movement-event array to pass in) and provenance is corrected AFTER calling it (a new pure post-processing method) — the engine's own internals stay byte-for-byte unchanged. `TimecodeNotationCapturePrecedence.resolvedMovementEvents(notationRoutingEnabled:cameraMovementEvents:drainedTimecodeNotationTimeline:)`: routing disabled → camera events pass through unchanged; routing enabled with no drained DVS timeline → fails closed to `[]`; routing enabled with a drained timeline that has no real movement → fails closed to `[]`; routing enabled with real trusted DVS movement → DVS events only, via the slice-1 `TimecodeNotationEventAdapter`, never merged with camera events. `CaptureCore.DetectedNotationSnapshot.withTimecodeLiveMovementProvenance()`: pure, additive method (no existing method touched) that swaps `"video"` for `"timecode_live"` in `detectionSources` when the resolved events came from DVS, no-op otherwise.
- `MacCaptureEngine.finalizeRoutineRecording` now resolves `resolvedMotionEvents` via the precedence function and passes that instead of raw camera `motionEvents` into `RoutineNotationFusionEngine().snapshot(...)`, then conditionally applies the provenance correction — all `#if DEBUG`; the `#else`/Release path is unconditionally the original `motionEvents`, byte-identical to before this slice.
- Test coverage: `TimecodeNotationRoutingTests.swift` grew from 29 to 35 tests — routing-disabled passthrough, fail-closed on missing DVS timeline, fail-closed on an idle (no real movement) DVS timeline, DVS-exclusive events never merged with camera events, provenance correction replaces `"video"` with `"timecode_live"` without touching audio/fader events, and a no-op case when there are no movement events to correct.
- Investigated a new, initially unexplained failure surfaced by a full `scripts/build.sh all` run: `CaptureReliabilityPhase1CoreTests.testTimedClickExportMetadataPersistsPresetBPMsAndTiming`. Confirmed unrelated to this slice, not a regression — the log shows the test-host process crashed mid-test on a CoreAudio/App-Sandbox entitlement fault (`PRECONDITION FAILURE: Process is sandboxed but 'com.apple.security.exception.mach-lookup.global-name' doesn't contain 'com.apple.audioanalyticsd'`, plus a `HALC_ShellObject::SetPropertyData` proxy failure), with nothing in the crash trace touching `CaptureCore`, `MacCaptureEngine`, or any DVS/notation code path; the suite auto-restarted and the identical test passed cleanly (12.875s) later in that same log, and had already passed cleanly twice in the immediately prior `scripts/build.sh` run with zero code changes in between. Logged as pre-existing environmental flakiness (real-media/AVFoundation test-host crash), not added to the tracked baseline list since it isn't reliably reproducible.
- Verification: `TimecodeNotationRoutingTests` 35/35 (1 expected skip, carried over from slice 1); a broad regression pass covering `CaptureReliabilityPhase1CoreTests` (313 tests) plus `PlatterPositionRecorderTests`, `TimecodePlaybackBridgeTests`, and six more DVS-adjacent suites, all clean against the established unrelated baseline; direct Debug macOS build succeeded; a direct iOS build (code signing disabled to bypass the same unrelated local watch-companion provisioning-profile mismatch as prior slices) succeeded; `python3 scripts/test_capture_pipeline.py` passed `47/47`; `git diff --check` passed.
- Follow-up notes: nothing enables playback or notation routing in production by default; `TimecodePlaybackBridge` was not modified. Slice 4 ("saved-fixture validation across the transition captures") is next in the plan but was not started — check with Karl before beginning it. Nothing was committed, staged, or pushed.

## 2026-07-31 (DVS notation slice 4 — saved-fixture validation across the transition captures)
- Selected task: Karl confirmed starting slice 4 of the recorded DVS-to-notation routing plan. Test-only slice: validate the slices 1–3 gate/accumulator/adapter/precedence/provenance chain end-to-end offline against the saved `slow_reversal`, `fast_reversal`, `baby_scratch`, `start_stop`, and `needle_lift` hardware fixtures. No production code, no hardware/turntable time.
- Files changed: `ScratchLabDesktopTests/TimecodeNotationRoutingTests.swift` only (35 → 40 tests). `TASKS.md`, `DEV_LOG.md`, `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`.
- Gap closed: slice 1's own fixture tests already proved the gate → accumulator → adapter chain against these five real fixtures, but slice 3's precedence/provenance logic (`TimecodeNotationCapturePrecedence`, `withTimecodeLiveMovementProvenance()`) had only ever been exercised against synthetic timelines. New helper `replayFullChain(fixtureFileName:)` reuses slice 1's `replayTrustedAccumulation` to get a real, trust-gated finalized DVS timeline per fixture, runs it through the precedence resolver alongside genuinely competing synthetic camera events, then through the provenance corrector — producing an actual `DetectedNotationSnapshot` from real captured hardware data end to end.
- Reversal/baby-scratch tests (`testSlowReversalFixtureFullChainMatchesTrustedReplay`, `testFastReversalFixtureFullChainMatchesTrustedReplay`, `testBabyScratchFixtureFullChainMatchesTrustedReplay`): assert the full chain reproduces slice 1's own trusted events exactly via `XCTAssertEqual(resolved, replay.events)` — since both computations use the same finalized timeline and the same deterministic adapter, this holds precisely, not approximately — that competing camera-sourced events never survive precedence resolution, and that `detectionSources` correctly resolves to `"timecode_live"` on real data.
- Stop/needle-lift tests (`testStartStopFixtureFullChainPreservesHonestGap`, `testNeedleLiftFixtureFullChainPreservesHonestGap`): extend slice 1's accumulator-level honest-gap proof all the way through export-facing provenance — assert `rejectedCount > 0` still holds through the full chain, and that precedence/provenance neither invent extra events to paper over the genuine stop/lift gap nor drop the real trusted events that did survive it.
- Also re-confirmed, as part of this slice's broad regression pass, that `testTimedClickExportMetadataPersistsPresetBPMsAndTiming` (the test-host crash investigated and attributed to an unrelated CoreAudio/App-Sandbox entitlement fault during slice 3's verification) passed cleanly in both passes of this run — further evidence it's pre-existing environmental flakiness, not anything code-related.
- Verification: `TimecodeNotationRoutingTests` 40/40 (1 expected skip), both scheme configurations; a broad regression pass covering `CaptureReliabilityPhase1CoreTests` (313 tests, both configs) plus `PlatterPositionRecorderTests`, `TimecodePlaybackBridgeTests`, `ScratchSamplePlaybackControllerTests`, `DVSOfflineGrainRendererTests`, `TimecodePrototypeProfileTests`, `TimecodeLiveTapTests`, `TimecodeFixtureValidationTests`, `DVSControlVinylAnalyzerTests`, `TimecodeDecoderTests` — all clean, only the established baseline failing in `CaptureReliabilityPhase1CoreTests`; direct Debug macOS build (via the scoped test builds above) and a direct iOS build (code signing disabled to bypass the same unrelated local watch-companion provisioning-profile mismatch as prior slices) both succeeded; `python3 scripts/test_capture_pipeline.py` passed 47/47; `git diff --check` passed.
- Follow-up notes: no production code was touched this slice — pure test coverage. Slice 5 ("final hardware validation") is next in the plan and requires a physical Rane ONE MKII session — check with Karl before beginning it, since unlike slices 1–4 it needs real hardware/turntable time. Nothing was committed, staged, or pushed.

## 2026-07-31 (DVS notation slice 5 — final hardware validation)
- Selected task: Karl confirmed starting slice 5, the final slice of the recorded DVS-to-notation routing plan — one real session with a physical Rane ONE MKII to confirm notation routing behaves correctly end-to-end on live hardware.
- Since I have no way to operate physical DJ hardware, added a temporary, uncommitted, `#if DEBUG` env-var check to `MacAnalyzerView.onAppear` (`SCRATCHLAB_ENABLE_DVS_NOTATION`) that flips `notationRoutingEnabled` on for one launch only, without touching the stored property's own default (`false`) — every other debug run and every XCTest run stayed unaffected. Karl launched the app with that env var set, connected his physical Rane ONE MKII using the same timecode-vinyl rig already used to capture the slices 1–4 fixtures, got the Setup Checklist fully green (including "Validation passed"), and recorded 3 routine takes of real baby-scratch motion via the normal Routine Capture Start Recording control — the exact retry an earlier, partial hardware attempt (recorded in `TASKS.md`'s slice 5 entry before this session, from a separate agent run) had identified as still needed.
- Result: all 3 exported takes (`session_2026_07_31_unknown_performer_baby_scratch_95_bpm`, 18.3s/14.4s/26.9s) showed `detectionSources: ["audio", "timecode_live"]` and real, correctly alternating forward/backward `recordMovementEvents` (10/14/17 events) — DVS notation routing worked end-to-end on live hardware, trust-gated and validated exactly as designed, with playback override left independently off throughout.
- Bug found via this real session, exactly what slice 5 exists to catch: every individual `recordMovementEvents[].source` in the export read `"video"`, despite the snapshot-level `detectionSources` correctly showing `"timecode_live"`. Root cause: `RoutineNotationFusionEngine.fuseMovementEvents` (`MacCaptureEngine.swift`) unconditionally rebuilds every motion event it emits with a hardcoded `source: "video"` (unmatched events) or `"fused"` (events paired with a burst audio event), discarding whatever source the input event actually carried — a pre-existing assumption baked in before DVS notation existed, when camera was the only possible motion source. Slice 3's `withTimecodeLiveMovementProvenance()` only ever patched the top-level `detectionSources` label, never each event's own `.source` field — invisible to every slice 1–4 test, since all of them exercised precedence/provenance directly and never through the real `RoutineNotationFusionEngine().snapshot(...)` call path production actually uses.
- Fix: extended `CaptureCore.DetectedNotationSnapshot.withTimecodeLiveMovementProvenance()` to also retag every event in `recordMovementEvents` to `source: "timecode_live"` — still a pure, additive post-processing method; `RoutineNotationFusionEngine`'s own internals remain untouched. Safe because the method's only call site (`notationRoutingEnabled && lastDrainedTimecodeNotationTimeline != nil`) guarantees every event in that snapshot originated from DVS, never camera. Updated `testWithTimecodeLiveMovementProvenanceReplacesVideoNotAudio` to assert the corrected behavior. Karl's already-captured session did **not** need to be redone — the bug was purely a mislabeled provenance string, not a data-correctness issue; direction/timing/position data was always correct, confirmed independently via `session_review.json`'s `directionConflictDetected: false` and sane `directionFlipCount` values (8/11/15) across all 3 takes.
- Also noticed, unrelated to this fix: 2 new tests (`testNotationDiagnosticsCountEveryDecisionAndTrackOutputTotals`, `testNotationDiagnosticsClampInvalidCountsAndCanExposeZeroDecisionCounters`) and a new `TimecodeNotationDiagnostics` counter type appeared in files I was working in, from what appears to be a parallel Codex session working on "Fix DVS diagnostics logger" per this project's established ChatGPT-architect/Claude-and-Codex-executor workflow (`SOUL.md`). Left untouched per instruction; verified it doesn't conflict with this slice's fix.
- Files changed: `ScratchLab/Models/CaptureCore.swift` (the fix), `ScratchLabDesktop/Views/MacAnalyzerView.swift` (temporary, uncommitted env-var toggle — not yet reverted, flagged for Karl), `ScratchLabDesktopTests/TimecodeNotationRoutingTests.swift` (updated test), `TASKS.md`, `DEV_LOG.md`, `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`.
- Verification: `TimecodeNotationRoutingTests` 42/42 (1 expected skip); a broad regression pass (`CaptureReliabilityPhase1CoreTests` 628 combined tests across both scheme passes + nine more DVS-adjacent suites) exactly matching the established baseline (8 failures/1 skip both passes, nothing new — the environmental flake `testTimedClickExportMetadataPersistsPresetBPMsAndTiming` passed cleanly in both passes here); direct iOS build (code signing disabled to bypass the same unrelated local watch-companion provisioning-profile mismatch as prior slices) succeeded; `git diff --check` clean.
- Follow-up notes: all 5 DVS notation slices are now complete and verified end-to-end, including real hardware. Two open decisions left for Karl, not acted on unilaterally: whether to revert or keep the temporary `MacAnalyzerView.swift` env-var toggle, and whether it's time to revisit "Keep playback override and notation routing off until explicitly approved" now that the whole plan is validated. Nothing was committed, staged, or pushed.

## 2026-07-31 (DVS notation slice 5 — temporary hardware-session toggle reverted)
- Selected task: Karl asked to revert only the temporary `SCRATCHLAB_ENABLE_DVS_NOTATION` env-var toggle in `MacAnalyzerView.swift` added for the slice 5 hardware session, while preserving the actual DVS provenance fix (`CaptureCore.swift`'s corrected `withTimecodeLiveMovementProvenance()`) and every other existing change.
- Files changed: `ScratchLabDesktop/Views/MacAnalyzerView.swift` only (removed the `#if DEBUG` env-var block from `onAppear`, restoring it to exactly its pre-slice-5 form). `TASKS.md`, `DEV_LOG.md`, `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`.
- Verification: `xcodebuild build-for-testing` succeeded; a broad regression pass (`CaptureReliabilityPhase1CoreTests` 628 combined tests across both scheme passes + nine more DVS-adjacent suites, including `TimecodeNotationRoutingTests` 42/42 with 1 skip) matched the established baseline exactly in both passes (8 failures/1 skip, nothing new) — confirming the revert didn't disturb anything and the DVS provenance fix in `CaptureCore.swift` remains fully intact and untouched; `git diff --check` clean.
- Follow-up notes: the "revert the toggle or keep it" decision from the prior entry is now resolved — reverted. One open decision remains for Karl, not acted on unilaterally: whether/when to add a real UI toggle and revisit "Keep playback override and notation routing off until explicitly approved" now that all 5 slices are verified end-to-end including real hardware. Nothing was committed, staged, or pushed.

## 2026-08-01 (Offline-only stop-transition reversal-confirmation rule measured — no candidate proven safe)
- Selected task: Karl approved the next safe DVS investigation queued in `AI_HANDOFF.md` — design and measure an offline-only stop-transition reversal-confirmation rule against the saved hardware fixtures, without changing production cadence or thresholds. This follows directly from the established finding that all 27 video-covered backward samples in `start_stop` at 1024 frames are false (record moving clockwise, caught/held, or restarting clockwise) and that confidence alone cannot separate them (false confidence reaches 0.801).
- Files changed: `ScratchLabDesktopTests/TimecodeFixtureValidationTests.swift` only (new "MARK: - 31." section). `TASKS.md`, `DEV_LOG.md`, `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`.
- Design: a test-only `DVSReversalConfirmationCandidate` enum models each candidate as a small state machine over the already-decoded, already-known-direction 1024-frame stream — a pending direction change is buffered and only "confirmed" (emitted) once it satisfies the candidate's rule; if the pending run reverts to the previously confirmed direction first, it is discarded entirely. `.none` is a baseline that confirms immediately, reproducing today's unfiltered 1024-frame output exactly. `minimumReverseDisplacement` requires the pending run's cumulative `|deltaPosition|` to clear a threshold; `consecutiveWindowConfirmation` requires a minimum consecutive-frame run length; `nearStopHysteresis` excludes any frame below a velocity floor from ever entering the state machine (neither confirming nor breaking the current direction); `combinedNearStopAndConsecutive` applies the floor first, then the consecutive-window rule to the survivors. A shared `decodeDVSFrameStream` helper decodes each fixture once at the established 1024-frame candidate window, so the (expensive) decode cost is paid once per fixture and every candidate is evaluated cheaply against the same decoded stream.
- Bug caught during this slice, before any measurement was trusted: the first version of `decodeDVSFrameStream` discarded `flushDecode()`'s return value and read `pipeline.latestDecodeResult?.frames` unconditionally every window, instead of gating on `flushDecode()` returning non-nil the way the already-validated `auditDVSSubwindow` does. Because `latestDecodeResult` is not cleared on a quiet window, this silently re-appended stale frames from the last successful window on every subsequent quiet window. It inflated `start_stop`'s raw backward count from the established 28 to 310 in the first full-capture run. Caught by cross-checking the new audit's `rawFalse`/`rawChanges` fields against the already-established `DVS_SUBWINDOW` baseline for the same fixtures (28 false in `start_stop`, 1 false in `needle_lift`) before treating any candidate result as real; fixed to gate on `flushDecode()`'s own return value, matching `auditDVSSubwindow` exactly; the corrected rerun reproduced the established raw counts precisely.
- Measurements (1024-frame window, four non-baseline candidates against the full local captures): `minReverseDisplacement(0.15)` fully removes every false reverse in `start_stop` (28/28) and `needle_lift` (1/1) and clears the small pre-existing false blips in the steady/one-way fixtures, but destroys 62-79% of genuine direction changes in `slow_reversals` (45→17), `fine_flicks` (76→16), and `baby_scratch` (292→72) — real fast/fine reversals are frequently just as small in per-frame displacement as the false `start_stop` ones, so a single displacement threshold cannot separate them. `consecutiveWindows(2)`/`(3)` are simultaneously too weak where needed (2/28 and 6/28 `start_stop` false samples removed) and too destructive where it matters (`slow_reversals` genuine changes fall from 45 to 19, a 58% loss) because discarding brief single-frame runs merges adjacent same-direction output and silently collapses pairs of genuine short back-and-forth changes. `nearStopHysteresis(0.15)` alone is the best-preserving candidate for genuine content (0% change loss in `slow_reversals` and `baby_scratch`, at most 8% elsewhere, and zero added latency since it never delays confirmation) and removes 86% of `start_stop`'s false reverses (28→4), but the residual `start_stop` sample at 12.821s and `needle_lift`'s lone false sample both carry velocity above the floor and pass through unchanged. `combined(floor=0.15, windows=2)` closes the `needle_lift` gap completely but reintroduces the `consecutiveWindows` fragmentation cost on top of the floor — `slow_reversals` drops back to the same 58%-loss level and `baby_scratch` loses 18% (292→238) — for no additional `start_stop` benefit over near-stop alone. Position-integration error scales with how much real motion each candidate discards: 0.003-0.03 for the gentlest candidates up to 5.06 for `minReverseDisplacement` on `baby_scratch`. Added confirmation latency is zero for the near-stop-only candidate, tens of milliseconds for `consecutiveWindows`, and up to ~0.64s worst case for `minReverseDisplacement` on `slow_reversals` and ~1.4s for `consecutiveWindows(3)` inside `start_stop`'s long false-hold bursts — both far too slow for responsive scratch feedback even where they do help. Decode/processing cost is unaffected by any candidate (0.033x-0.26x captured duration across all 15 fixtures, matching the already-established 1024-frame baseline); candidate evaluation itself is a negligible O(n) pass over the shared decoded stream.
- Conclusion: no single candidate, and no narrow combination tested, is safe to propose as a production slice. `nearStopHysteresis` is the only one whose genuine-content cost looks tolerable on its own, but it still misses `needle_lift` entirely and 4 of 28 `start_stop` false samples, so closing the remaining gap needs a materially different or supplementary signal rather than a blunter consecutive-window/displacement gate layered on top. No production filter was implemented; no live decoding path, cadence, or threshold was changed, per the explicit scope of this investigation.
- Verification: the always-run excerpt test (`testHardwareTransitionExcerptsReportReversalConfirmationCandidates`, the 6 checked-in 0.24s transition fixtures) passed in both scheme configurations. The opt-in full-capture audit (`testLocalFullHardwareCapturesReportReversalConfirmationCandidates`, gated on `SCRATCHLAB_RUN_FULL_DVS_REVERSAL_CONFIRMATION_AUDIT=1`) passed in both scheme configurations against all 15 full local captures x 6 candidates (90 measurements per pass, 180 total). Getting the full-capture pass to actually see the opt-in env var required `launchctl setenv`/`launchctl unsetenv` rather than a plain shell `export`: the macOS-destination test host is launched through Launch Services (visible as the `RegisterWithLaunchServices` build step), which does not inherit the invoking shell's exported environment. `python3 scripts/test_capture_pipeline.py` passed 47/47. `git diff --check` passed. `scripts/build.sh all` ran both configurations and stopped only on the established unrelated Advanced diagnostics, MIDI learn/receive/clear, Coach pause, and notation source-contract baseline failures, with no new failures.
- Follow-up notes: this was measurement only — no production filter, decoder, threshold, playback, routing, or export behavior changed anywhere. The new audit's log format (`DVS_REVERSAL_CONFIRM label=... candidate=... window=1024 duration=...s raw=...(f.../b...)(...Hz) rawChanges=... rawFalse=... confirmed=...(f.../b...)(...Hz) confirmedChanges=... confirmedFalse=... falseRemoved=... rejected=... latencyMean=...s latencyMax=...s positionErr=... spike=... held=... long=... processing=...s realtimeRatio=...`) follows the same convention as the existing `DVS_DENSITY`/`DVS_SUBWINDOW` audits so future sessions can add further candidates without re-deriving the harness. Nothing was committed, staged, or pushed.

## 2026-08-01 — Clean-room DVS signed-rate excursion stabilizer complete (uncommitted)

- Selected task: add a pure, platform-agnostic signed-rate excursion stabilizer before the two existing unchecked product/future-hardware decisions. No equivalent task existed, and this was the first small implementation-ready task explicitly requested.
- Root cause: continuously sampled signed rate retains genuine reversal timing even while absolute position is unlocked, but its raw sign chatters near zero. The repository had no isolated state machine that could preserve the committed direction while accumulating only a pending opposite-direction excursion, so a direct sign consumer would either duplicate chatter reversals or require an unsafe position-lock gate.
- Files changed: `ScratchLab/Models/TimecodeSignedRateExcursionStabilizer.swift` (new shared pure logic), `ScratchLabDesktopTests/TimecodeSignedRateExcursionStabilizerTests.swift` (new deterministic tests), `ScratchLab.xcodeproj/project.pbxproj` (shared iOS/macOS source membership and test membership), `TASKS.md`, `DEV_LOG.md`, `AI_HANDOFF.md`, and `AI_HANDOFF/next_prompt.md`.
- Implementation: explicit `.available`/`.noSignal`/`.unknown` samples carry a finite non-negative timestamp and normalized signed rate. The stabilizer keeps committed and pending directions separate, integrates consecutive opposite-direction magnitude with the trapezoidal rule over actual elapsed time, commits exactly once at `reversalAreaThreshold`, cancels pending evidence when the rate returns to the committed direction, and never accepts or consults position lock. `reversalAreaThreshold` and `maximumIntegrationGap` are injected configuration with no hidden production default. Non-finite/negative/non-monotonic input, invalid configuration, unavailable signal, and excessive gaps clear pending/timing evidence without changing the last committed direction or synthesizing live motion across the discontinuity.
- Scope: no call site was added. `TimecodePhaseDecoder`, `TimecodeControlPipeline`, live capture, playback, notation, export, persistence, UI, and App Store behavior are unchanged. No external/GPL code, headers, constants, translated logic, or linked material entered the repository. This does not claim to fix baby-scratch sound.
- Verification: focused `TimecodeSignedRateExcursionStabilizerTests` passed 10/10 in each of the two test-plan configurations (20 executions). Existing DVS-adjacent decoder/stability/live-tap/live-integration/validation/fixture/bridge/profile/analyzer/notation/playback/offline-renderer suites passed 388 executions with 6 expected environment-gated skips and no failures. `python3 scripts/test_capture_pipeline.py` passed 47/47. Required `scripts/build.sh all` ran both configurations; the new tests passed twice, and the script stopped before platform build legs only on the documented established baseline: `testAdvancedDiagnosticsExplainCameraAndTickActivity`, MIDI receive/learn/clear tests, `testCoachPauseHoldsPoseFromAudioTimeAndAvoidsPausedTimelineWork`, `CrossPlatformNotationParityTests.notationCanvasUsesSharedRenderer`, and `NotationSheetTests.templateDemoStillOwnsBabyScratchTemplateLabel`. Direct signed macOS, unsigned iOS (including watch dependency), and unsigned watchOS builds all succeeded afterward. `git diff --check` passed.
- Follow-up: the stabilizer remains intentionally disconnected until a separately scoped, lawful clean-room dense signed-rate estimator is designed and measured. Nothing was committed, staged, pushed, reset, or discarded.
## 2026-08-04 (Timecode Control sidebar width fix — uncommitted)
- Root cause: the profile section placed four long labels, including `Rane ONE MKII (DEBUG, pair 3/4 pinned)`, in one non-wrapping segmented picker. Its intrinsic width exceeded the narrow debug inspector and pushed/clipped roughly 30% of the left side.
- Changed only `ScratchLabDesktop/Views/TimecodeControlCard.swift`: the three general presets use a compact first row; the Rane DEBUG preset is a separate full-width second-row button. Selection still writes the same `selectedPreset`, so existing preset application/channel pinning is unchanged.
- Verification: macOS Debug build succeeded; `git diff --check` clean; index empty. No capture, pipeline, playback, notation, export, fixture, or Kid Mode behavior changed. Nothing staged/committed/pushed for this UI fix.

## 2026-08-05 (Fix the standing `testAdvancedDiagnosticsExplainCameraAndTickActivity` baseline failure — uncommitted)

- Selected task: diagnose and fix the long-standing `CaptureReliabilityPhase1CoreTests.testAdvancedDiagnosticsExplainCameraAndTickActivity` failure that every prior session in this log has treated as part of the "established unrelated baseline" (alongside MIDI learn/receive/clear, Coach pause, and notation source-contract) without ever being root-caused.
- Root cause: the test asserts `notationSource.contains("Baby Scratch Template")` against `ScratchLabDesktop/Views/NotationVisualizerView.swift`, but that literal string no longer exists in the file. `git log -S"Baby Scratch Template" -- ScratchLabDesktop/Views/NotationVisualizerView.swift` shows commit `be9fc1c` ("notation: fix playhead-relative timing and compact centre-band stroke shape") intentionally renamed the label chip from `"Baby Scratch Template"` to `"Baby Scratch Demo"`, as part of a deliberate switch of the Advanced Notation Lab source from a short 12-stroke deterministic template to a 76-stroke beat-quantized coach demo (so timing syncs with coach audio). This is a real, intentional UI change, not a regression — the test assertion was simply never updated afterward. All other assertions in the test (camera "true (Live Input)"/"live preview" text, "Audio-only take", "No record movement detected.", absence of the retired "Notation detected from audio — direction pending." string, `markNotationIdle()` wiring) were already correct against current source.
- Files changed: `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift` only — one literal string updated inside `testAdvancedDiagnosticsExplainCameraAndTickActivity`, from `"Baby Scratch Template"` to `"Baby Scratch Demo"`. No production code changed.
- Out-of-scope note: `NotationSheetTests.swift`'s `templateDemoStillOwnsBabyScratchTemplateLabel` asserts the exact same stale `"Baby Scratch Template"` string against the same file and fails for the identical reason, but it belongs to the separately tracked "notation source-contract" baseline bucket, not "Advanced diagnostics." Left untouched per this session's single-task scope; a future task should apply the same one-line fix there.
- Verification: focused `testAdvancedDiagnosticsExplainCameraAndTickActivity` passed via `xcodebuild test-without-building` in both test-plan configurations (`ScratchLab.xctestplan`, `ScratchLab 2.xctestplan`). The three directly adjacent diagnostics tests in the same class (`testMacCaptureSourceUsesTruthfulMixerStatus`, `testMacCaptureSourceShowsSelectedAudioAndCameraDeviceNames`, `testPracticeSourceKeepsDemoPathAppReviewSafe`) passed alongside it. `python3 scripts/test_capture_pipeline.py` passed 47/47. Required `scripts/build.sh all` ran the full desktop test plan in both configurations and exited 65 via its own `set -e` at the macOS test stage before reaching the iOS/macOS/watchOS build legs — its final "Failing tests" summary listed exactly `testClearCrossfaderMappingRemovesMapping`, `testCoachPauseHoldsPoseFromAudioTimeAndAvoidsPausedTimelineWork`, `testMIDILearnUpdatesFromListeningToLearnedStateWhenCCArrives`, `testReceivingMIDICCUpdatesLastMIDIEventSummary`, `CrossPlatformNotationParityTests.notationCanvasUsesSharedRenderer`, and `NotationSheetTests.templateDemoStillOwnsBabyScratchTemplateLabel` — the same established baseline minus `testAdvancedDiagnosticsExplainCameraAndTickActivity`, which no longer appears anywhere in the run. `git diff --check` passed.
- Follow-up notes: `git status` before and after this task showed the identical pre-existing dirty-file set (`AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`, `DEV_LOG.md`, `MacCaptureEngine.swift`, `MacAnalyzerView.swift`, `TimecodeControlCard.swift`, `CaptureReliabilityPhase1Tests.swift`, `TimecodeFixtureValidationTests.swift`, `TimecodeLiveTapTests.swift`, `TASKS.md`, plus untracked `ScratchLabDesktopTests/Fixtures/Timecode/` and `tmp/`) — nothing else was touched, and the `CaptureReliabilityPhase1Tests.swift` diff contains exactly this one intended line change plus the file's pre-existing (already-dirty) modifications. Rane hardware code, DVS/playback, notation routing, signing, entitlements, and resources were not touched. Nothing staged, committed, or pushed.

## 2026-08-05 (Fix the standing `NotationSheetTests.templateDemoStillOwnsBabyScratchTemplateLabel` baseline failure — uncommitted)

- Selected task: the out-of-scope item flagged in the entry above — `ScratchLabDesktopTests/NotationSheetTests.swift`'s `templateDemoStillOwnsBabyScratchTemplateLabel`, which shares the exact same stale-string root cause as the just-fixed Advanced diagnostics test.
- Root cause, confirmed by reading the full test file and the relevant production source/history (not assumed to be a literal-only swap): the test asserted `source.contains("Baby Scratch Template")` against `NotationVisualizerView.swift`, a string retired by the same intentional commit `be9fc1c` that renamed the Notation Lab's non-captured-branch label chip to `"Baby Scratch Demo"` when the lab's source notation switched from the short 12-stroke deterministic template to the 76-stroke beat-quantized coach demo. Read `NotationVisualizerView.swift:460-536` (`notationStatusBar`) in full: the label chip lives inside the `else` branch of `if showingCaptured, let snapshot = capturedSnapshot { … } else { labelChip("Baby Scratch Demo", …) … }`, confirming the test's original structural assertion (the `showingCaptured`/`capturedSnapshot` conditional) is still accurate and worth keeping — only the label text itself was stale. Also confirmed via `ScratchLabDesktopTests/LiveNotationOverlayTests.swift` (`testBabyScratchTemplateDurationIsFourPointSevenSeconds`, `testBeatQuantizedResourceLoads`, etc.) that "Baby Scratch Template" is not simply gone from the product — it still correctly names a distinct, still-shipping 12-stroke/4.700s resource (`ScratchNotation.babyScratch`, used by Practice) that is unrelated to the Notation Lab's demo label; the 76-stroke coach-demo resource (`ScratchNotation.babyScratchFull76BeatQuantized`) and its stroke-count/phrase-count contract are already independently pinned by that same file's `testBeatQuantizedResourceLoads`/`testBeatQuantizedPhraseCounts`, so this task did not duplicate that coverage here — this file's job is only to pin the Notation Lab UI's label-chip text, not the underlying notation data.
- Files changed: `ScratchLabDesktopTests/NotationSheetTests.swift` only. Renamed the test (function `templateDemoStillOwnsBabyScratchTemplateLabel` → `nonCapturedBranchOwnsBabyScratchDemoLabel`, `@Test` description → "Notation Lab's non-captured branch shows the Baby Scratch Demo label, not the retired Template label") so its name truthfully describes what it now checks, kept the still-accurate `if showingCaptured, let snapshot = capturedSnapshot` structural assertion, replaced the stale positive assertion with `#expect(source.contains("Baby Scratch Demo"))`, and added `#expect(!source.contains("Baby Scratch Template"))` so the test also guards against the retired label silently reappearing in this file (restoring the original test's "keeps X only in the branch"-style intent, now pointed at the correct current label). No production code changed.
- Verification: focused `NotationSheetTests` suite (45/45, including the renamed test) passed via `xcodebuild test-without-building` in both test-plan configurations (`ScratchLab.xctestplan`, `ScratchLab 2.xctestplan`) — this covers every directly adjacent notation source-contract test in the same file (brand-string check, event-direction rendering, audio-only copy, display-mode labels, crossfader lane label, etc.), all still passing. `python3 scripts/test_capture_pipeline.py` passed 47/47. Required `scripts/build.sh all` ran the full desktop test plan in both configurations and exited 65 via its own `set -e` at the macOS test stage — its final "Failing tests" summary listed exactly `testClearCrossfaderMappingRemovesMapping`, `testCoachPauseHoldsPoseFromAudioTimeAndAvoidsPausedTimelineWork`, `testMIDILearnUpdatesFromListeningToLearnedStateWhenCCArrives`, `testReceivingMIDICCUpdatesLastMIDIEventSummary`, and `CrossPlatformNotationParityTests.notationCanvasUsesSharedRenderer` — the same established baseline minus `NotationSheetTests.templateDemoStillOwnsBabyScratchTemplateLabel`, which no longer appears anywhere in the run (and `testAdvancedDiagnosticsExplainCameraAndTickActivity`, fixed in the prior session, remains absent too). `git diff --check` passed on all changes.
- Follow-up notes: `git status` before and after this task showed the identical pre-existing dirty-file set plus this session's one intended addition (`NotationSheetTests.swift`) — `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`, `DEV_LOG.md`, `MacCaptureEngine.swift`, `MacAnalyzerView.swift`, `TimecodeControlCard.swift`, `CaptureReliabilityPhase1Tests.swift`, `NotationSheetTests.swift`, `TimecodeFixtureValidationTests.swift`, `TimecodeLiveTapTests.swift`, `TASKS.md`, plus untracked `ScratchLabDesktopTests/Fixtures/Timecode/` and `tmp/`. Nothing else was touched; the `NotationSheetTests.swift` diff contains exactly the one intended test edit. Rane hardware code, DVS/playback, notation routing, signing, entitlements, and resources were not touched. Nothing staged, committed, pushed, reset, restored, or stashed.

- Update — now committed and pushed: only the two reviewed stale-contract corrections (the `CaptureReliabilityPhase1Tests.swift` "Baby Scratch Template" → "Baby Scratch Demo" literal, and the full `NotationSheetTests.swift` rename/reassertion above) were landed as commit `0e44676` ("Fix stale Baby Scratch Demo source-contract tests", no `Co-Authored-By` trailer) and pushed to `origin/main` (`46aa1f2..0e44676`, confirmed `HEAD == origin/main` post-push). The candidate was reconstructed from a disposable detached worktree at HEAD `46aa1f2` (not the dirty working tree) specifically so none of `CaptureReliabilityPhase1Tests.swift`'s ~94 other pre-existing unrelated hunks could enter the commit; it was built, tested (both focused suites, both test-plan configurations, plus `python3 scripts/test_capture_pipeline.py` 47/47), and diff-reviewed for leakage entirely inside that isolated worktree before the worktree was discarded. Landing itself used `git apply --cached` against the saved patch file (not `git add` on the working-tree files) so only that exact two-hunk diff entered the real index; `git diff --cached` was verified byte-identical to the reviewed patch, `git diff --cached --check` passed, and a content scan confirmed no Rane/DVS/playback/notation-routing/project-file/resource material was staged. All other pre-existing dirty/untracked files (`AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`, `DEV_LOG.md`, `MacCaptureEngine.swift`, `MacAnalyzerView.swift`, `TimecodeControlCard.swift`, the remaining unrelated hunks in `CaptureReliabilityPhase1Tests.swift`, `TimecodeFixtureValidationTests.swift`, `TimecodeLiveTapTests.swift`, `TASKS.md`, untracked `ScratchLabDesktopTests/Fixtures/Timecode/` and `tmp/`) remain exactly as they were — untouched, unstaged, uncommitted. This `DEV_LOG.md` update is itself left unstaged per instruction.

## 2026-08-05 (Diagnose and fix `CrossPlatformNotationParityTests.notationCanvasUsesSharedRenderer` — uncommitted)

- Selected task: the last remaining item of the "notation source-contract" established-baseline bucket — `CrossPlatformNotationParityTests.notationCanvasUsesSharedRenderer` (in `ScratchLabDesktopTests/DemoTimingFoundationTests.swift:1186`), a source-string test asserting that `ScratchLabDesktop/Views/ScratchNotationCanvasView.swift` routes its record lane through "the shared renderer".
- Root cause, determined by reading the full test, the full current `ScratchNotationCanvasView.swift`, `ScratchMotionRenderer.swift`, `ScratchMotionLane.swift` (iOS), `TimingLane.swift`, and `git log`/`git show` on the asserted symbols — **a stale/brittle source-contract assertion after an intentional, well-documented architecture change, not a regression**: commit `be9fc1c` (2026-06-24, "notation: fix playhead-relative timing and compact centre-band stroke shape" — the same commit that retired "Baby Scratch Template" in the two prior sessions) also switched `ScratchNotationCanvasView`'s record-lane draw call from the old tent-shaped `ScratchStrokeGeometry.motionPath(for:)` + `ScratchMotionRenderer.draw(_:in:viewport:style:)` + path-level `MotionPath.shifted(by:)` loop tiling to `ScratchMotionRenderer.drawActionNotationTrace(_:in:viewport:style:)` with per-stroke `LaneStroke.shifted(by:)` tiling (`content.strokes.map { $0.shifted(by: loopOffset) }`) — the commit message explicitly states the intent ("Draw each stroke as a single diagonal slash — the same action-notation trace the iOS Practice target lane uses"). Verified this is not merely stale but a **strictly stronger** parity guarantee than the old assertions ever checked: `ScratchLab/Views/ScratchMotionLane.swift:378-390` (iOS Practice lane) calls the exact same `ScratchMotionRenderer.drawActionNotationTrace(...)` with the exact same `content.strokes.map { $0.shifted(by: shift) }` tiling shape, using `LaneStroke.shifted(by:)` — a method defined once in the genuinely shared `ScratchLab/Models/TimingLane.swift:100` (confirmed via `grep -rn "func shifted(by:"`), not per-platform. Before `be9fc1c`, canvas and iOS lane called *different* renderer functions with visibly different output (tent-shaped dual-colour strokes with junction nodes vs. single-colour diagonal slashes); after it, they call the literal same function with the literal same tiling expression — real, verifiable, stronger parity than the retired assertions pinned. Also confirmed `ScratchStrokeGeometry.motionPath(` / `MotionPath.shifted(by:)` were not simply renamed but genuinely retired for this call site: `ScratchLabDesktop/Views/ScratchPhraseChartView.swift:104-105` (the OTHER macOS notation view, whose own parity test `phraseChartUsesSharedRenderer` still passes) still calls the old `motionPath` + `draw` pair unchanged — it's a static, non-looping chart that never needed the change — so the two macOS views now legitimately use two different (each internally consistent, each still "shared" with something) renderer entry points, and only the canvas's test was left unupdated.
- Files changed: `ScratchLabDesktopTests/DemoTimingFoundationTests.swift` only, inside `notationCanvasUsesSharedRenderer`. Kept the test name and `@Test` description unchanged (both remain accurate — the canvas still "routes its record lane through the shared renderer," just a different, still-shared function). Replaced the two stale assertions (`ScratchStrokeGeometry.motionPath(`, `ScratchMotionRenderer.draw(`) with one precise assertion for the actual current call (`ScratchMotionRenderer.drawActionNotationTrace(`); kept the two still-true assertions (`LaneContent(notation:`, `LaneViewport(`) unchanged; replaced the stale tiling assertion (`motionPath.shifted(by:`) with a precise assertion for the actual current tiling expression (`content.strokes.map { $0.shifted(by:`) rather than a vague substring, so the test still pins an exact, meaningful, cross-platform-verified contract rather than being weakened. Updated the two explanatory comments to describe the current mechanism instead of the retired one. No production code changed.
- Verification: full `CrossPlatformNotationParityTests` suite (7/7, including the fixed test) passed via `xcodebuild test-without-building` in both test-plan configurations (`ScratchLab.xctestplan`, `ScratchLab 2.xctestplan`). Six directly adjacent notation renderer/canvas/model suites in the same file (`ScratchMotionLaneSourceTests`, `LaneWiringTests`, `LaneContentTests`, `LaneViewportTests`, `MotionPathTests`, `ScratchMotionRendererTests`) passed 46/46 in both configurations alongside it. `python3 scripts/test_capture_pipeline.py` passed 47/47. Required `scripts/build.sh all` ran the full desktop test plan in both configurations and exited 65 via its own `set -e` at the macOS test stage — its final "Failing tests" summary listed exactly `testClearCrossfaderMappingRemovesMapping`, `testCoachPauseHoldsPoseFromAudioTimeAndAvoidsPausedTimelineWork`, `testMIDILearnUpdatesFromListeningToLearnedStateWhenCCArrives`, `testReceivingMIDICCUpdatesLastMIDIEventSummary` — the established MIDI/Coach baseline, with `CrossPlatformNotationParityTests.notationCanvasUsesSharedRenderer` no longer appearing anywhere in the run (and `testAdvancedDiagnosticsExplainCameraAndTickActivity` / `NotationSheetTests.templateDemoStillOwnsBabyScratchTemplateLabel`, fixed and committed in prior sessions, remain absent too — the entire former "notation source-contract" bucket is now clear). `git diff --check` passed on all changes.
- Follow-up notes: `git status` before and after this task showed the identical pre-existing dirty-file set plus this session's one intended addition (`DemoTimingFoundationTests.swift`) — `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`, `DEV_LOG.md`, `MacCaptureEngine.swift`, `MacAnalyzerView.swift`, `TimecodeControlCard.swift`, `CaptureReliabilityPhase1Tests.swift`, `DemoTimingFoundationTests.swift`, `TimecodeFixtureValidationTests.swift`, `TimecodeLiveTapTests.swift`, `TASKS.md`, plus untracked `ScratchLabDesktopTests/Fixtures/Timecode/` and `tmp/`. Nothing else was touched; the `DemoTimingFoundationTests.swift` diff (14 insertions, 6 deletions) contains exactly the one intended test edit. Rane hardware code, DVS/playback, notation routing, capture, signing, entitlements, resources, and every other production/UI file were not touched. Nothing staged, committed, pushed, reset, restored, stashed, or discarded. **Remaining risk (not fixed, out of this task's scope):** `ScratchNotationCanvasView.swift`'s own header comment (lines 14-19) and an inline comment (line 89) still describe the retired `MotionPath.shifted(by:)` loop-tiling mechanism — a stale production *comment*, not logic; harmless but worth a follow-up doc-only cleanup pass in that file.

- Update — now committed and pushed: only the reviewed `notationCanvasUsesSharedRenderer` correction above was landed as commit `2bbc603` ("Update cross-platform notation renderer parity contract", no `Co-Authored-By` trailer) and pushed to `origin/main` (`0e44676..2bbc603`, confirmed `HEAD == origin/main` post-push). This file (`DemoTimingFoundationTests.swift`) had no pre-existing dirty content before this task, so its working-tree diff already equalled the reviewed candidate; the candidate was still independently reconstructed and verified in a disposable detached worktree at HEAD `0e44676` (build-for-testing, full `CrossPlatformNotationParityTests` suite plus the same 6 adjacent renderer/canvas/model suites 46/46 in both test-plan configurations, `python3 scripts/test_capture_pipeline.py` 47/47, `git diff --check`) before landing, and the worktree was discarded afterward. Landing used `git apply --cached` against the saved patch file (not `git add`), so only that exact one-file diff entered the real index; `git diff --cached` was verified byte-identical to the reviewed patch, `git diff --cached --check` passed, and a content scan confirmed no Rane/DVS/playback/notation-routing/project-file/resource/workflow-doc material was staged. All other pre-existing dirty/untracked files (`AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`, `DEV_LOG.md`, `MacCaptureEngine.swift`, `MacAnalyzerView.swift`, `TimecodeControlCard.swift`, `CaptureReliabilityPhase1Tests.swift`, `TimecodeFixtureValidationTests.swift`, `TimecodeLiveTapTests.swift`, `TASKS.md`, untracked `ScratchLabDesktopTests/Fixtures/Timecode/` and `tmp/`) remain exactly as they were — untouched, unstaged, uncommitted. This `DEV_LOG.md` update is itself left unstaged per instruction.

## 2026-08-05 (Diagnose and fix `CaptureReliabilityPhase1CoreTests.testClearCrossfaderMappingRemovesMapping` — uncommitted)

- Selected task: the last remaining item of the "MIDI learn/receive/clear" established-baseline bucket that is actually fixable this session — `testClearCrossfaderMappingRemovesMapping` (`ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift:11085`), which persists a `CrossfaderCCMapping` into `UserDefaults.standard`, constructs a `MacCaptureEngine(autoRefreshDevices: false)`, asserts `engine.crossfaderCCMapping` is non-nil (i.e. the persisted mapping was restored), then calls `clearCrossfaderMapping()` and asserts the `UserDefaults` key is gone.
- Root cause, confirmed by reading the full test, `MacCaptureEngine`'s init/`configureInitialState()`/`startDeviceDiscoveryAfterViewMount()`/`clearCrossfaderMapping()`, and `git show`/`git log -S` on the relevant symbols — **a genuine production regression, not a stale test, not UserDefaults contamination, and not a timing/observation issue in the test itself**. Empirically reproduced first: the failure is exactly `XCTAssertNotNil(engine.crossfaderCCMapping)` at line 11094 — `clearCrossfaderMapping()` itself works correctly (`UserDefaults.standard.removeObject(forKey:)` runs synchronously, so the second assertion never fails). Traced with `git log -S"startDeviceDiscoveryAfterViewMount"` to the single commit that introduced it, `026c072` ("macOS: stabilize Rane scratch sample playback", 2026-06-26). Before that commit, `configureInitialState()` decoded a persisted `CrossfaderCCMapping` from `UserDefaults` and set `crossfaderCCMapping`/`midiLearnState` **synchronously**, in the same statement as the still-present `persistedCrossfaderMapping = mapping` line. That commit moved the `crossfaderCCMapping`/`midiLearnState` assignment out of `init` and into the new `startDeviceDiscoveryAfterViewMount()` — an `async` method gated behind `autoRefreshDevicesAfterViewMount` (== the `autoRefreshDevices` init parameter) that also does `await Task.yield()` before publishing. The test constructs its engine with `autoRefreshDevices: false` specifically to stay synchronous/deterministic, which now means `startDeviceDiscoveryAfterViewMount()`'s early `guard autoRefreshDevicesAfterViewMount else { return }` always exits immediately — and even for `autoRefreshDevices: true` callers, nothing in this unit-test context ever calls the `async` method at all, so `crossfaderCCMapping` never gets restored. Critically, `026c072`'s own `DEV_LOG.md` entry (committed in the same commit) states the selected task was narrowly "a fix for `ScratchSamplePlaybackController.loadOnQueue` / `ensureEngineRunning`" after a brace-matching bug left that unrelated controller failing to compile, and its follow-up notes describe only `ScratchSamplePlaybackController.swift` changes — the `MacCaptureEngine.swift` device-discovery-deferral restructuring (visible in the actual commit diff, 185 lines) is never mentioned. This is strong evidence the crossfader-mapping-restore regression was an unintentional side effect swept into that commit alongside legitimately-scoped async deferral of the genuinely expensive `rescanRoutineCaptures()`/`refreshDevices()` device I/O calls, not a deliberate architecture decision — so restoring the historical synchronous behavior for this one cheap, non-I/O UserDefaults decode does not require a product/architecture decision.
- Files changed: `ScratchLabDesktop/Services/MacCaptureEngine.swift` only, inside `configureInitialState()`. Two lines added back exactly where they were before `026c072` removed them:
  ```swift
  if let data = UserDefaults.standard.data(forKey: ScratchLabDesktopDefaultsKey.crossfaderMIDIMapping),
     let mapping = try? JSONDecoder().decode(CrossfaderCCMapping.self, from: data) {
      crossfaderCCMapping = mapping
      midiLearnState = .learned(mapping)
      persistedCrossfaderMapping = mapping
  }
  ```
  The legitimately-intentional async deferral of `rescanRoutineCaptures()`/`refreshDevices()` in `startDeviceDiscoveryAfterViewMount()` was left untouched, including its own (now redundant but harmless/idempotent) re-assignment of the same mapping value for `autoRefreshDevices: true` callers whose view actually invokes that method — removing that redundant block was judged out of scope for "the smallest deterministic correction." No test file changed.
- Verification: the focused test passed 10/10 across 5 repeated `xcodebuild test-without-building` invocations (2 executions per invocation) in the `ScratchLab` test-plan configuration — reran before making any change first to empirically confirm the exact failing assertion. A 17-test cluster covering every adjacent MIDI-mapping/crossfader/learn test in the same class (`testMIDICCDecodesToNormalizedValue`, `testNoMIDIExportsEmptyMixerMidiEvents`, `testRawMIDIExportsMixerMidiEventsWithoutFakeFaderEvents`, `testUnmappedMIDIDoesNotCreateDerivedFaderEvents`, `testMappedCrossfaderMIDICreatesCutFaderEvent`, `testQuickCrossfaderReversalCreatesPulseEvent`, `testRepeatedCrossfaderPulsesCanCreateTransformPulseEvent`, `testCrossfaderCCMappingDisplayName`, `testMIDILearnStateTransitionsToListening`, `testCancelMIDILearnReturnsToIdle`, `testMIDISourceSelectionPrefersIACWhenCurrentSelectionMissing`, `testSelectedMIDISourceIsPersisted`, `testClearCrossfaderMappingRemovesMapping`, `testMappedControlCrossfaderTagRoundTripsExport`, `testMappedCrossfaderMIDIRoundTripsExportWithFaderEvents`, `testMIDILearnSourceCodePresence`, `testMIDILearnUISourcePresence`) passed cleanly (17/17, 0 failures) in the `ScratchLab 2` configuration; the same cluster plus the two already-established-baseline MIDI-receive/learn tests in `ScratchLab` config showed exactly those two pre-existing failures (`testMIDILearnUpdatesFromListeningToLearnedStateWhenCCArrives`, `testReceivingMIDICCUpdatesLastMIDIEventSummary`) and nothing else — confirmed unrelated to this change (unrelated properties: `lastMIDICCMessage`/`lastMIDIEventSummary`/`midiListeningState`, not `crossfaderCCMapping`, and already present in the established baseline from every prior session in this log). `python3 scripts/test_capture_pipeline.py` passed 47/47. Required `scripts/build.sh all` ran the full desktop test plan in both configurations and exited 65 via its own `set -e` at the macOS test stage — its final "Failing tests" summary listed exactly `testCoachPauseHoldsPoseFromAudioTimeAndAvoidsPausedTimelineWork` and `testMIDILearnUpdatesFromListeningToLearnedStateWhenCCArrives`/`testReceivingMIDICCUpdatesLastMIDIEventSummary` (×4) — the established Coach/MIDI-receive baseline with `testClearCrossfaderMappingRemovesMapping` no longer appearing anywhere in the run. `git diff --check` passed.
- Follow-up notes: `git status` before and after this task showed the identical pre-existing dirty-file set — `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`, `DEV_LOG.md`, `MacCaptureEngine.swift`, `MacAnalyzerView.swift`, `TimecodeControlCard.swift`, `CaptureReliabilityPhase1Tests.swift`, `TimecodeFixtureValidationTests.swift`, `TimecodeLiveTapTests.swift`, `TASKS.md`, plus untracked `ScratchLabDesktopTests/Fixtures/Timecode/` and `tmp/` — with no new file added, since the fix landed inside the already-dirty `MacCaptureEngine.swift`; its diff against HEAD contains this session's 2-line addition plus the exact same pre-existing unrelated hunks as before. Rane hardware code, DVS/playback, notation routing, capture pipeline, signing, entitlements, and resources were not touched. Nothing staged, committed, pushed, reset, restored, stashed, or discarded. **Remaining risk (not fixed, out of this task's scope):** the redundant mapping re-assignment inside `startDeviceDiscoveryAfterViewMount()` is now dead-weight (idempotent, no behavior change) for `autoRefreshDevices: true` callers; a future cleanup could remove it. Also unresolved from prior sessions: `testMIDILearnUpdatesFromListeningToLearnedStateWhenCCArrives` and `testReceivingMIDICCUpdatesLastMIDIEventSummary` remain a genuine, distinct, not-yet-diagnosed established baseline failure (MIDI CC event reception never registers in this test-host sandbox) — a candidate for a future single-task session.

- Update — now committed and pushed: only the reviewed two-line `configureInitialState()` correction above was landed as commit `8887352` ("Restore persisted crossfader MIDI mapping on launch", no `Co-Authored-By` trailer) and pushed to `origin/main` (`2bbc603..8887352`, confirmed `HEAD == origin/main` post-push). `MacCaptureEngine.swift` carried ~144 lines of unrelated pre-existing dirty content, so the candidate was reconstructed from a disposable detached worktree at HEAD `2bbc603` (not the dirty working tree) to guarantee isolation — build-for-testing, the focused test 10/10 (both configurations, 5 repeats each), the same 17-test adjacent MIDI-mapping/crossfader/learn cluster 17/17 in both configurations, `python3 scripts/test_capture_pipeline.py` 47/47, `git diff --check`, and a leakage review (`git diff --numstat` showing exactly `2/0` on exactly one file) all confirmed inside that isolated worktree before landing; the worktree was discarded afterward. Landing used `git apply --cached` against the saved patch file (not `git add`), so only that exact two-line diff entered the real index; `git diff --cached` was verified byte-identical to the reviewed patch, the cached stat showed exactly `1 file changed, 2 insertions(+)`, `git diff --cached --check` passed, and a content scan confirmed no Rane/DVS/playback/notation/capture-diagnostics/device-discovery-restructuring/UI/resource/project-file/workflow-doc material was staged — `MacCaptureEngine.swift` showed `MM` (staged: the fix; unstaged: the remaining pre-existing hunks) immediately before commit. All other pre-existing dirty/untracked files (`AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`, `DEV_LOG.md`, the rest of `MacCaptureEngine.swift`'s unrelated hunks, `MacAnalyzerView.swift`, `TimecodeControlCard.swift`, `CaptureReliabilityPhase1Tests.swift`, `TimecodeFixtureValidationTests.swift`, `TimecodeLiveTapTests.swift`, `TASKS.md`, untracked `ScratchLabDesktopTests/Fixtures/Timecode/` and `tmp/`) remain exactly as they were — untouched, unstaged, uncommitted. This `DEV_LOG.md` update is itself left unstaged per instruction.

## 2026-08-05 (Diagnose the paired `testMIDILearnUpdatesFromListeningToLearnedStateWhenCCArrives` / `testReceivingMIDICCUpdatesLastMIDIEventSummary` failures — fix the first, stop before the second — uncommitted)

- Selected task: diagnose both previously-unresolved MIDI-CC-reception baseline failures and determine whether they share one root cause. They do not: empirically reproduced both individually and found two entirely distinct failure signatures, so per this task's own instruction ("select the first independently fixable defect, and stop before addressing the second") only `testMIDILearnUpdatesFromListeningToLearnedStateWhenCCArrives` (the first-listed) was fixed this session.
- Diagnosis — `testMIDILearnUpdatesFromListeningToLearnedStateWhenCCArrives` (**genuine production regression, fixed**): the empirical failure was a single assertion — `midiLearnFeedback` expected `"Learned Xfader: CC11 Ch2"` but got `"Received CC11 Ch2 Value64"` — while `crossfaderCCMapping`, `midiLearnState`, and `lastMIDIEventSummary` all already passed, proving the test's `RunLoop.main.run(until:)` spin correctly flushes the async publish path (ruling out main-thread publication timing as the defect) and that this was a pure state-ordering bug. Traced with `git log -S"if isMidiLearn {"` to the single commit that introduced it, `103b613` ("macOS: suspend live input on Review tab", 2026-06-17). Confirmed via `git show 103b613^:...` that the pre-existing code checked `if self.midiLearnState == .listening { self.midiLearnFeedback = summary }` **freshly, inside** the async-dispatched closure — so if an earlier-enqueued closure (from `applyLearnedCrossfaderMapping`, called synchronously earlier in the same `recordReceivedMIDICCEvent` invocation when a mapping was just learned) had already flipped `midiLearnState` to `.learned(...)` by the time this closure ran, the fresh check correctly no-opped and preserved the specific "Learned Xfader: ..." message. Commit `103b613` (whose stated purpose was adding MIDI-monitor UI-publish throttling while Review is active, unrelated to this specific protection) replaced that fresh check with a pre-computed `isMidiLearn` local captured *before* either closure had run — so by the time the monitor-publish closure executed (always second, since `applyLearnedCrossfaderMapping`'s own `publishOnMainAsync` call is enqueued first in the same function), it unconditionally overwrote the correct "Learned Xfader: ..." feedback with the generic "Received CC..." summary on the very event that had just committed the mapping. Confirmed `recordReceivedMIDICCEvent` is the exact real production seam (not a synthetic-only stub): the live CoreMIDI callback chain (`MIDIInputPortCreateWithBlock` → `receiveMIDIPacketList` → the same `applyLearnedCrossfaderMapping`/`recordReceivedMIDICCEvent` call pair for CC status bytes) exhibits the identical ordering, so this was also a real, user-visible UI bug in production (a successful crossfader MIDI-learn gesture would show the generic "Received CC..." caption instead of confirming "Learned Xfader: ..."), not merely a test artifact — no new injection seam was needed since the existing seam already matches the real callback path exactly.
- Diagnosis — `testReceivingMIDICCUpdatesLastMIDIEventSummary` (**test-seam defect, NOT fixed this session, evidence only**): all four assertions fail simultaneously with completely-unset default values (`midiEventsReceivedCount` "0", `lastMIDICCMessage` "CC -- Ch -- Value --", `lastMIDIEventSummary` "No MIDI received yet", `midiListeningState` "Not Connected") — meaning the `publishOnMainAsync`/`DispatchQueue.main.async` closure never ran at all before the assertions executed. Unlike the paired test above, this test calls `recordReceivedMIDICCEvent(...)` and asserts immediately, with **no** `RunLoop.main.run(until:)` spin at all (contrast with `testMIDILearnUpdatesFromListeningToLearnedStateWhenCCArrives`, `testMIDILearnStateTransitionsToListening`, and `testCancelMIDILearnReturnsToIdle`, which all spin the run loop after the async-publishing call and pass). This points to a missing use of the already-established, already-proven-deterministic test seam (`RunLoop.main.run(until:)`) in this one test, not a production defect — but per this task's explicit scope ("select the first independently fixable defect, and stop before addressing the second"), this was diagnosed and left unfixed for a future task.
- Files changed: `ScratchLabDesktop/Services/MacCaptureEngine.swift` only, inside `recordReceivedMIDICCEvent`'s throttled `publishOnMainAsync(field: "midiMonitor", ...)` closure. One line changed (plus an explanatory comment) — restores the pre-`103b613` semantics exactly:
  ```swift
  if self.midiLearnState == .listening {
      self.midiLearnFeedback = latestSummary
  }
  ```
  replacing `if isMidiLearn { ... }`. The `isMidiLearn` local is still used unchanged for the throttle-bypass condition (`shouldPublish`) a few lines above — only its use inside this specific closure was reverted. No test file changed.
- Verification: focused `testMIDILearnUpdatesFromListeningToLearnedStateWhenCCArrives` passed 10/10 across 5 repeated runs in `ScratchLab` and 5/5 in `ScratchLab 2`. An 18-test cluster covering every adjacent MIDI-mapping/crossfader/learn test plus this one passed cleanly (18/18, 0 failures) in both test-plan configurations. The same 4-test subset (this test plus `testClearCrossfaderMappingRemovesMapping`, `testMIDILearnStateTransitionsToListening`, `testCancelMIDILearnReturnsToIdle`) passed under `-enableThreadSanitizer YES` with zero warnings/races, since the change touches how a value is read inside an already-main-actor-dispatched closure. `python3 scripts/test_capture_pipeline.py` passed 47/47. Required `scripts/build.sh all` ran the full desktop test plan in both configurations and exited 65 via its own `set -e` — its final "Failing tests" summary listed exactly `testCoachPauseHoldsPoseFromAudioTimeAndAvoidsPausedTimelineWork` and `testReceivingMIDICCUpdatesLastMIDIEventSummary` (×4) — the established Coach baseline plus the one deliberately-unfixed, distinct MIDI-reception test, with `testMIDILearnUpdatesFromListeningToLearnedStateWhenCCArrives` no longer appearing anywhere. `git diff --check` passed.
- Follow-up notes: `git status` before and after this task showed the identical pre-existing dirty-file set — `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`, `DEV_LOG.md`, `MacCaptureEngine.swift`, `MacAnalyzerView.swift`, `TimecodeControlCard.swift`, `CaptureReliabilityPhase1Tests.swift`, `TimecodeFixtureValidationTests.swift`, `TimecodeLiveTapTests.swift`, `TASKS.md`, plus untracked `ScratchLabDesktopTests/Fixtures/Timecode/` and `tmp/` — with no new file added, since the fix landed inside the already-dirty `MacCaptureEngine.swift`. Rane hardware code, DVS/playback, notation routing, capture pipeline, signing, entitlements, and resources were not touched. Nothing staged, committed, pushed, reset, restored, stashed, or discarded. **Remaining/next task:** `testReceivingMIDICCUpdatesLastMIDIEventSummary` still needs a single-line `RunLoop.main.run(until: Date().addingTimeInterval(0.05))` added after its `recordReceivedMIDICCEvent(...)` call, matching the exact established pattern already used and proven deterministic by three other passing tests in this same class — a small, independently fixable, test-only follow-up for a future session.

- Update — now committed and pushed: only the reviewed `midiLearnState == .listening` fresh-recheck correction above was landed as commit `473c3a3` ("Preserve learned crossfader feedback after MIDI CC", no `Co-Authored-By` trailer) and pushed to `origin/main` (`8887352..473c3a3`, confirmed `HEAD == origin/main` post-push). `MacCaptureEngine.swift` carried unrelated pre-existing dirty content, so the candidate was reconstructed from a disposable detached worktree at HEAD `8887352` (not the dirty working tree). Inside that worktree, beyond the standard verification (build-for-testing; focused test 10/10 + 5/5 across both configurations; the 18-test adjacent MIDI-mapping/learn cluster 18/18 in both configurations; Thread Sanitizer on a 4-test subset with zero warnings; `python3 scripts/test_capture_pipeline.py` 47/47), the ordering claim itself was empirically proven with a temporary, verification-only test added solely inside that worktree (asserting an ordinary non-listening CC event still publishes its generic summary/counter fields while `midiLearnState`/`midiLearnFeedback` stay at their idle defaults) run alongside `testMIDILearnUpdatesFromListeningToLearnedStateWhenCCArrives` (proving the learned feedback is not overwritten) — both passed, and the temporary test was then discarded via `git checkout --` before landing, confirmed via a direct `diff` that the saved patch file remained byte-identical to the final worktree diff (i.e. the verification aid never touched the candidate). The worktree was discarded afterward. Landing used `git apply --cached` against the saved patch file (not `git add`), so only that exact diff entered the real index; `git diff --cached` was verified byte-identical to the reviewed patch, the cached numstat showed exactly `10` insertions / `1` deletion on exactly one file, `git diff --cached --check` passed, and a content scan confirmed `testReceivingMIDICCUpdatesLastMIDIEventSummary`, the temporary verification test, and every Rane/DVS/playback/notation/capture-diagnostics/UI/resource/project-file/workflow-doc keyword were absent from the staged diff — `MacCaptureEngine.swift` showed `MM` (staged: the fix; unstaged: the remaining pre-existing hunks) immediately before commit, and `CaptureReliabilityPhase1Tests.swift` showed only its pre-existing unstaged state (confirming the temporary test never touched the real repository at all). All other pre-existing dirty/untracked files remain exactly as they were — untouched, unstaged, uncommitted. This `DEV_LOG.md` update is itself left unstaged per instruction.

## 2026-08-05 (Fix `testReceivingMIDICCUpdatesLastMIDIEventSummary` — the last MIDI-bucket item — uncommitted)

- Selected task: the remaining, previously-diagnosed test-seam defect flagged in the entry above — `testReceivingMIDICCUpdatesLastMIDIEventSummary` never spins the run loop after calling the async-publishing `recordReceivedMIDICCEvent(...)`, so its `DispatchQueue.main.async`-scheduled state mutation never gets a chance to run before its synchronous assertions execute (all four fail with untouched defaults: `midiEventsReceivedCount` "0", `lastMIDICCMessage` "CC -- Ch -- Value --", `lastMIDIEventSummary` "No MIDI received yet", `midiListeningState` "Not Connected").
- Root cause and fix: purely a missing test seam, not production behavior — three adjacent tests in the same class (`testMIDILearnStateTransitionsToListening`, `testCancelMIDILearnReturnsToIdle`, `testMIDILearnUpdatesFromListeningToLearnedStateWhenCCArrives`) already call `RunLoop.main.run(until: Date().addingTimeInterval(0.05))` after triggering the identical `publishOnMainAsync` mechanism and pass deterministically. Added the exact same one line, in the same position (immediately after `recordReceivedMIDICCEvent(...)`, before the assertions), matching that established pattern verbatim. No production code touched, no other test touched, no sleep-based or weakened-assertion workaround used.
- Files changed: `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift` only — one line added:
  ```swift
  RunLoop.main.run(until: Date().addingTimeInterval(0.05))
  ```
- Verification: focused test passed 10/10 across 5 repeated runs in `ScratchLab` and 5/5 in `ScratchLab 2`. The complete adjacent MIDI/crossfader cluster — every test in the "MIDI Learn / crossfader mapping" section (19 tests, including this one and `testMIDILearnUpdatesFromListeningToLearnedStateWhenCCArrives`) — passed cleanly (19/19, 0 failures) in both test-plan configurations; this is the entire bucket, now fully clean. All four published fields the test asserts (`midiEventsReceivedCount == 1`, `lastMIDICCMessage == "CC7 Ch1 Value96"`, `lastMIDIEventSummary == "Received CC7 Ch1 Value96"`, `midiListeningState == "Listening"`) were confirmed correctly populated via the test's own passing assertions. `python3 scripts/test_capture_pipeline.py` passed 47/47. Required `scripts/build.sh all` ran the full desktop test plan in both configurations and exited 65 via its own `set -e` at the macOS test stage — its final "Failing tests" summary listed exactly one test: `testCoachPauseHoldsPoseFromAudioTimeAndAvoidsPausedTimelineWork` — the entire "MIDI learn/receive/clear" established-baseline bucket (Advanced diagnostics, notation source-contract, crossfader clear, crossfader learn-feedback, and now MIDI-CC-reception) that spanned the last several sessions is fully resolved; only the unrelated, distinct Coach-pause defect remains in the established baseline. `git diff --check` passed.
- Follow-up notes: `git status` before and after this task showed the identical pre-existing dirty-file set — `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`, `DEV_LOG.md`, `MacCaptureEngine.swift`, `MacAnalyzerView.swift`, `TimecodeControlCard.swift`, `CaptureReliabilityPhase1Tests.swift`, `TimecodeFixtureValidationTests.swift`, `TimecodeLiveTapTests.swift`, `TASKS.md`, plus untracked `ScratchLabDesktopTests/Fixtures/Timecode/` and `tmp/` — with no new file added, since the fix landed inside the already-dirty `CaptureReliabilityPhase1Tests.swift`. Rane hardware code, DVS/playback, notation routing, capture pipeline, signing, entitlements, and resources were not touched. Nothing staged, committed, pushed, reset, restored, or stashed. **Remaining/next task:** `testCoachPauseHoldsPoseFromAudioTimeAndAvoidsPausedTimelineWork` is the sole remaining established-baseline failure and has not yet been diagnosed — a candidate for a future single-task session.

- Update — now committed and pushed: only the reviewed one-line `RunLoop.main.run(until: Date().addingTimeInterval(0.05))` addition above was landed as commit `4af5a71` ("Synchronize MIDI CC summary test with main-queue publication", no `Co-Authored-By` trailer) and pushed to `origin/main` (`473c3a3..4af5a71`, confirmed `HEAD == origin/main` post-push). `CaptureReliabilityPhase1Tests.swift` carried unrelated pre-existing dirty content, so the candidate was reconstructed from a disposable detached worktree at HEAD `473c3a3` (not the dirty working tree) — build-for-testing, the focused test 10/10 + 5/5 across both configurations, the complete 19-test MIDI/crossfader cluster 19/19 in both configurations, `python3 scripts/test_capture_pipeline.py` 47/47, and `git diff --check` all confirmed inside that isolated worktree before landing; the candidate hunk was also confirmed byte-identical to the already-verified working-tree hunk, so the full `scripts/build.sh all` baseline comparison from the prior session was reused rather than rerun, per instruction. The worktree was discarded afterward. Landing used `git apply --cached` against the saved patch file (not `git add`), so only that exact one-line diff entered the real index; `git diff --cached` was verified byte-identical to the reviewed patch, the cached numstat showed exactly `1` insertion / `0` deletions on exactly one file, `git diff --cached --check` passed, and a content scan confirmed no Rane/DVS/playback/notation/capture-diagnostics/resource/project-file/workflow-doc material was staged — `CaptureReliabilityPhase1Tests.swift` showed `MM` (staged: the fix; unstaged: the remaining pre-existing hunks) immediately before commit. All other pre-existing dirty/untracked files remain exactly as they were — untouched, unstaged, uncommitted. This `DEV_LOG.md` update is itself left unstaged per instruction.

## 2026-08-05 (Diagnose and fix `testCoachPauseHoldsPoseFromAudioTimeAndAvoidsPausedTimelineWork` — the final established-baseline failure — uncommitted)

- Selected task: the sole remaining established-baseline failure — `testCoachPauseHoldsPoseFromAudioTimeAndAvoidsPausedTimelineWork` (`ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift:1200`), a source-string test checking the "coach pause holds pose from audio time, avoids paused-timeline work" contract across `ScratchLab/Views/ScratchCoachViews.swift` (shared) and `ScratchLabDesktop/Views/MacAnalyzerView.swift` (macOS).
- Diagnosis — **stale test semantics after an intentional macOS architecture change, not a production regression**. Reproduced the exact failure: only `XCTAssertFalse(macSource.contains("CACurrentMediaTime()"))` failed; every other assertion (the shared `rigSource`/`coachSource` checks, plus `macSource.contains("babyScratchDemo.playbackState")`) already passed. Read every current occurrence of `CACurrentMediaTime()` in `MacAnalyzerView.swift` (5 total) and confirmed none relate to coach pose/pause at all: one is a generic `SwiftUIStateMutationProbe` debug-logging helper (added by commit `026c072`, 2026-06-26), the other four are inside `#if ENABLE_TIMECODE_LIVE_TAP`/`#if DEBUG` DVS/Timecode flush-tick tracing (added by commit `e48e72d`, 2026-08-05) — both explicitly out of this task's scope to touch, and both postdate the test's introduction (`df4972a`, 2026-05-06). Traced the actual coach-pose call site the test's macSource assertions were written against: `git show df4972a` showed `MacAnalyzerView.swift` instantiating `ScratchCoachCardContent(..., playbackTimeProvider: { babyScratchDemo.currentAudioTime }, isPlayingProvider: { babyScratchDemo.isPlaying }, animationStateProvider: { ... BabyScratchDemoPlaybackCoordinator.coachPose(for:) ... })`. That entire call site no longer exists: `git show b8917f8` ("macOS: add beat-quantized Baby Scratch guide", 2026-06-23) deliberately removed it, replacing the animated macOS coach-rig card with a new notation-strip guide (`practiceNotationStrip`/`LiveNotationOverlayView`, plus a new beat-quantized notation JSON asset and `LiveNotationOverlayModel.swift` — 1,439 lines added across 6 files). `ScratchCoachCardContent`/`ScratchCoachRigView` are now instantiated only by the iOS files (`MainMenuView.swift`, `PracticeModeView.swift`), confirmed via `grep -rn "ScratchCoachCardContent("`. The remaining `babyScratchDemo.playbackState` reads in `MacAnalyzerView.swift` (tick-rate idle-label formatting, a `friendlyPlaybackState` display string) are diagnostic-text formatting only, not pose/pause gating, so they no longer substantiate the contract either. Verified the contract itself is still fully satisfied where it's actually implemented: the shared `ScratchCoachRigView.body` (`if isPlayingProvider() { TimelineView(.periodic...) { rigContent(...) } } else { rigContent(playbackTime: playbackTimeProvider(), isPlaying: false) }`) — used by iOS — freezes the pose at the current audio-time value and stops the periodic re-render entirely while paused; the macOS replacement (the notation guide) uses the same established, verified-safe `TimelineView(.animation(paused: !controller.isPlaying))` idiom already used pervasively elsewhere in `MacAnalyzerView.swift` (6+ other call sites). No production/shared code was changed — only the test's now-orphaned assertions were removed.
- Files changed: `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift` only, inside `testCoachPauseHoldsPoseFromAudioTimeAndAvoidsPausedTimelineWork`. Removed the `macSourceURL`/`macSource` declarations and the two assertions that checked a since-removed macOS call site (`babyScratchDemo.playbackState` presence, `CACurrentMediaTime()` absence); kept every assertion that verifies the contract's actual current implementation (`rigSource`'s `isPlayingProvider()`/`TimelineView(.periodic`/paused-`rigContent(...)`/`CoachRigUpdate` signpost checks, and `coachSource`'s `BabyScratchDemoPlaybackCoordinator.coachPose(for: playbackTime)` check) unchanged. Added an explanatory comment documenting the `b8917f8` architecture change so a future reader isn't left to re-derive this. Test name kept — it remains accurate for what the shared component still guarantees. No production code changed.
- Verification: focused test passed 10/10 in `ScratchLab` and 5/5 in `ScratchLab 2` across repeated runs. A 33-test adjacent coach/pause/playback cluster (pause/stop state, `BabyScratchDemoPlaybackCoordinator` configure/pause/stop/replay, `ScratchCoachDemoAnimator`, coach-rig timing/geometry, notation-tick no-op while paused, and more) passed cleanly (33/33, 0 failures) in both test-plan configurations. No production or shared code changed, so Thread Sanitizer was not applicable (noted rather than run unnecessarily). `python3 scripts/test_capture_pipeline.py` passed 47/47. Required `scripts/build.sh all` ran the full desktop test plan and, for the first time across this entire multi-session investigation, reported **`Test Suite 'All tests' passed ... Executed 2403 tests, with 19 tests skipped and 0 failures`** — the macOS test stage is now completely clean. The script then proceeded (also for the first time) into the iOS build leg and exited 65 there on `error: Provisioning profile "iOS Team Provisioning Profile: com.machelpnz.scratchlab.watchkitapp" doesn't include signing certificate "Apple Development: Karl Watson (5C7ZHKGE7L)"` — confirmed via `DEV_LOG.md` history (lines ~1281, ~1561-1562) as a long-documented, machine-local Apple Developer signing/provisioning-profile configuration matter for this exact bundle ID, unrelated to any code change and explicitly out of this task's scope (signing/entitlements). Supplementally ran a direct unsigned iOS build (`CODE_SIGNING_ALLOWED=NO`, including watch-companion embedding) to confirm the actual code is unaffected — **BUILD SUCCEEDED**. `git diff --check` passed.
- Follow-up notes: `git status` before and after this task showed the identical pre-existing dirty-file set — `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`, `DEV_LOG.md`, `MacCaptureEngine.swift`, `MacAnalyzerView.swift`, `TimecodeControlCard.swift`, `CaptureReliabilityPhase1Tests.swift`, `TimecodeFixtureValidationTests.swift`, `TimecodeLiveTapTests.swift`, `TASKS.md`, plus untracked `ScratchLabDesktopTests/Fixtures/Timecode/` and `tmp/` — with no new file added, since the fix landed inside the already-dirty `CaptureReliabilityPhase1Tests.swift`. Rane hardware code, DVS/playback, timecode, notation routing, capture resources, signing, and entitlements were not touched. Nothing staged, committed, pushed, reset, restored, or stashed. **Remaining risk:** the local iOS/watchOS code-signing/provisioning-profile mismatch is a pre-existing machine/account configuration issue, not a code defect — it blocks a *signed* `scripts/build.sh all` iOS leg on this machine but does not indicate any regression; resolving it (if ever needed) is an Apple Developer account/certificate matter, not a source change. **Milestone:** every established-baseline macOS test failure carried across this entire multi-session investigation (Advanced diagnostics, notation source-contract ×2, crossfader clear, crossfader learn-feedback, MIDI-CC-reception, and now Coach pause) is resolved — the macOS desktop XCTest suite is fully green.

## 2026-08-11 (macOS design audit slice 3 — Capture/Review card system and spacing)

- Selected task: the next approved design-audit implementation slice — replace repeated Capture/Review card styling and layout literals with a small shared surface vocabulary and the existing design tokens.
- Root cause: the visual hierarchy had been assembled card-by-card. Capture and Review independently repeated the same padding, radius, material/control background, stage overlay, sidebar-width, and page-spacing values. The rendered values were often close, but there was no reusable contract to keep equivalent surfaces aligned as the product evolves.
- Files changed: `ScratchLabDesktop/Views/ScratchLabDesignTokens.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `TASKS.md`, and `DEV_LOG.md`. `MacAnalyzerView.swift`, `TASKS.md`, and `DEV_LOG.md` were already dirty; unrelated existing hunks were preserved.
- Implementation: added `ScratchLabCardStyle` with standard, hero, and dark-stage variants plus a reusable `scratchLabCard(_:)` modifier. Converted the primary Capture workflow/input/latest-take/session/summary cards, Review header/session/take/action/export cards, and Review notation/audio/summary stage surfaces to those variants. Routed Capture and Review split widths, sidebar margins, sibling-card gaps, disclosure spacing, and Capture stage spacing/background through existing `ScratchLabDesign` tokens. Kept custom semantic status cards and diagnostics visually distinct. This is view/layout code only; capture behavior, readiness state, actions, notation semantics, scoring, MIDI, DVS/timecode, playback, audio routing, persistence, export, and models are unchanged.
- Verification: `git diff --check` passed. The required real-workspace `scripts/build.sh` passed the capture-pipeline fixtures 47/47, then stopped before Swift compilation because the pre-existing working-tree deletion of `ScratchLab/Resources/Coach/Coach.usdz` conflicts with the Xcode resource-copy phase. A disposable worktree seeded from `HEAD` and overlaid with the current working changes restored only checked-in resources for verification: the complete macOS desktop test plan succeeded (`Test Suite 'All tests'`: 2,403 executed, 19 skipped, 0 failures; Swift Testing: 157/157), proving the affected SwiftUI code compiles and the suite remains green. The script then reached the known unrelated signed iOS/watchOS leg and stopped because the local watch provisioning profile does not include the current Apple Development certificate.
- Remaining risk and verification: no automated screenshot assertion covers subtle material/border appearance. Manually launch the macOS app and inspect Capture and Review in empty, recording/finalizing, ready, and incomplete-artifact states at narrow and wide window widths. Confirm card edges, 22-point group rhythm, Capture's compact sidebar inset, Review stage contrast, scrolling, and split-view resizing. Nothing was staged, committed, or pushed.

## 2026-08-11 (macOS design audit slice 4 — Practice workspace and shared page headers)

- Selected task: the next approved design-audit implementation slice — bring Practice into the shared surface and spacing system and define a consistent page-header treatment across the macOS workspaces. There was no unchecked implementation-ready design item in `TASKS.md`; the remaining unchecked DVS items are intentionally approval/hardware gated, so this slice follows the approved audit priorities without changing them.
- Root cause: Practice still mixed literal split widths, margins, a pure-black stage background, and repeated card construction after Capture and Review had moved to shared tokens. Practice, Capture, Review, and Advanced also used visually similar header cards without an explicit shared role, leaving their hierarchy vulnerable to feature-by-feature drift.
- Files changed: `ScratchLabDesktop/Views/ScratchLabDesignTokens.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `TASKS.md`, and `DEV_LOG.md`. All four files were already dirty from prior approved work or workflow records; unrelated existing hunks were preserved.
- Implementation: added a `pageHeader` card variant using regular material, the shared 22-point header radius, standard card padding, and a restrained border. Applied it to the Practice, Capture, Review, and Advanced workspace headers. Routed Practice split width, stage padding/gap/background, sidebar insets, and diagnostic stack spacing through the existing design tokens. Applied the existing hero treatment to the Practice demo and coach cards, and the standard treatment to the run, beat trainer, audio, scratch, feedback, and workflow cards. Deliberately left camera, calibration, advanced diagnostic, hardware, and semantic-status surfaces bespoke where their structure or meaning differs. This is layout/style-only SwiftUI work; analysis, scoring, notation semantics, MIDI, DVS/timecode, playback, audio routing, capture, persistence, export, and data models are unchanged.
- Verification: `git diff --check` passed. The required real-workspace `scripts/build.sh` passed capture-pipeline fixtures 47/47, then stopped before Swift compilation because the pre-existing working-tree deletion of `ScratchLab/Resources/Coach/Coach.usdz` conflicts with the Xcode resource-copy phase. In a disposable verifier retaining checked-in resources while overlaying the current source changes, the affected desktop target compiled successfully and the focused Practice source/behavior tests passed 2/2 (`testDemoModeProducesFeedbackWithoutHardware` and `testPracticeHeaderUsesSafeAudioStatusText`). Existing compiler and local audio/device warnings remain unrelated to this visual slice.
- Remaining risk and verification: automated tests do not judge visual rhythm, material appearance, truncation, or sidebar behavior. Manually launch the macOS app and inspect Practice at its minimum, ideal, and expanded sidebar widths; confirm the four page headers read as one family, the coach remains the dominant Practice card, diagnostics remain subordinate, card borders/materials stay legible over the canvas, and no disclosure content clips while scrolling. Nothing was staged, committed, or pushed.

## 2026-08-11 (macOS design audit slice 5 — Advanced sidebar rhythm and core surfaces)

- Selected task: the next approved design-audit implementation slice — move the Advanced workspace's general layout and core informational cards onto the shared visual system without restyling its specialized hardware and diagnostic controls. There was still no unchecked implementation-ready design item in `TASKS.md`; the remaining unchecked DVS items remain approval/hardware gated.
- Root cause: Advanced was the last primary macOS workspace whose split width, sidebar margins, group gaps, scroll inset, and general-purpose cards were independently styled with repeated numeric literals. That made equivalent hierarchy levels look subtly different and increased the chance of future feature-by-feature drift.
- Files changed: `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `TASKS.md`, and `DEV_LOG.md`. All three were already dirty; unrelated existing hunks were preserved.
- Implementation: replaced Advanced's literal split width with the existing Advanced sidebar tokens; routed sidebar section spacing, group spacing, horizontal/top/bottom insets, and every selected-section stack gap through the existing spacing tokens. Applied the established standard card treatment to the Advanced section picker, Tools, Performance, Recent sessions, and Active session cards. The design-system guidance kept this as a migration to documented existing roles rather than another new surface variant. The notation visualizer and specialized audio, camera, calibration, hand-motion, stage, MIDI, performer-monitor, capture-detail, and timecode surfaces remain structurally unchanged. This is layout/style-only SwiftUI work; diagnostic content and all product behavior are unchanged.
- Verification: `git diff --check` passed. The required real-workspace `scripts/build.sh` passed capture-pipeline fixtures 47/47, then stopped at the known pre-existing missing `ScratchLab/Resources/Coach/Coach.usdz` resource. The first disposable verification command was inadvertently launched from the original workspace and reproduced that same failure without changing files; rerunning from the actual disposable worktree retained checked-in resources, compiled the changed desktop target successfully, and passed `testMacAdvancedSidebarHeaderAdaptsWithoutWideningScrollContent` in both configured test runs (1/1 each). Existing compiler, local audio, and locked-device warnings are unrelated.
- Remaining risk and verification: automated tests do not assess material contrast or dense-card scanning. Manually inspect every Advanced section at minimum, ideal, and maximum sidebar widths; confirm the section picker and core overview cards read as one family, long status values remain legible, scroll content stays within the measured width, and specialized hardware cards remain clearly distinguishable without looking disconnected. Nothing was staged, committed, or pushed.

## 2026-08-11 (macOS design audit slice 6 — shared workspace-header typography)

- Selected task: the next approved design-audit implementation slice — make the page-header typography as consistent and maintainable as the shared page-header surface introduced in slice 4. No unchecked design task was available; the remaining unchecked DVS work remains explicitly approval/hardware gated.
- Root cause: Practice, Capture, Review, and Advanced used the same conceptual title/subtitle/eyebrow/status hierarchy but expressed it with repeated font literals. Capture's subtitle was also 12 points while the equivalent Practice, Review, and Advanced descriptions were 14 points, making one workspace's introductory hierarchy noticeably quieter.
- Files changed: `ScratchLabDesktop/Views/ScratchLabDesignTokens.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `TASKS.md`, and `DEV_LOG.md`. All four were already dirty from prior approved slices or workflow records; unrelated existing hunks were preserved.
- Implementation: extended `ScratchLabDesign.Typo` with semantic `pageSubtitle` (14-point medium), `pageEyebrow` (12-point medium), and `pageStatus` (12-point semibold) roles alongside the existing 28-point `pageTitle`. Migrated the four workspace titles, their introductory subtitles, the Practice/Advanced ScratchLab eyebrows, and relevant page-status labels to those roles. Secondary instructional footer copy uses the existing `bodySecondary` token. The design-system skill influenced this by defining roles instead of replacing literals with unrelated generic sizes. No text, action, state, color, accessibility, navigation, diagnostics, or product behavior changed. Capture's subtitle is the sole intentional rendered-size change, from 12 to 14 points.
- Verification: `git diff --check` passed. The required real-workspace `scripts/build.sh` passed capture-pipeline fixtures 47/47, then stopped at the known pre-existing missing `ScratchLab/Resources/Coach/Coach.usdz`. A disposable worktree retaining checked-in resources compiled both changed SwiftUI files successfully. `testPracticeHeaderUsesSafeAudioStatusText` and `testMacAdvancedSidebarHeaderAdaptsWithoutWideningScrollContent` passed 2/2 in each configured test run. Existing compiler, local audio, and locked-device warnings are unrelated.
- Remaining risk and verification: automated tests do not evaluate typographic balance or truncation. Manually inspect all four headers at minimum and maximum sidebar widths, paying particular attention to Capture's now-14-point subtitle, Advanced's wrapped actions, Review's long subtitle, and Practice's three status pills. Nothing was staged, committed, or pushed.

## 2026-08-11 (macOS Capture sidebar redesign — Configure → Confirm readiness → Record → Review)

- Selected task: implement the requested Capture-workspace redesign slice — make the macOS Capture sidebar read as an unmistakable Configure → Confirm readiness → Record → Review sequence, per the ChatGPT-provided implementation prompt (view-only, capped scope, no scoring/notation/MIDI/DVS/audio-routing/capture/persistence/export/service/data-model changes).
- Root cause: `captureSessionWorkflowCard` (previously ~4620–4781 in `MacAnalyzerView.swift`) was one monolithic card mixing page identity, session metadata, audio-source routing, the record transport, warnings, and post-take actions with no numbered sequence. The no-session state rendered "New Session" twice at once (header button + primary CTA), and the Record button/warnings could scroll out of view in a tall sidebar.
- Files changed: `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLabDesktop/Views/ScratchLabDesignTokens.swift`, `TASKS.md`, `DEV_LOG.md`. `MacAnalyzerView.swift` and `ScratchLabDesignTokens.swift` were already dirty from a prior unrelated in-progress slice; those existing hunks were preserved untouched.
- Implementation: added a reusable `ScratchLabStepHeader` component (numbered accent badge + uppercased eyebrow label) to the design tokens file. Rebuilt the Capture sidebar as: `capturePageHeaderCard` (page title/subtitle + a single next-action status line driven by a new `captureNextAction` computed property, plus "New Session" only once a session exists) → `captureSessionSetupCard` (Step 1, session name/target/mode/tempo, moved verbatim) → the existing `captureInputStatusCard` (now headed by Step 2, with the "Use Serato Audio" quick-fix promoted next to the Inputs grid and the audio-source picker moved into the existing "Input details" disclosure) → `captureLatestTakeCard` → a "Sessions & workflow" disclosure, with a new pinned `captureRecordCard` (Step 3 "Record the take" / Step 4 "Review the result" once a take exists) rendered below the scroll view so the record transport, readiness warnings, and post-take actions never scroll away. Deleted the now-empty `captureSessionWorkflowCard`. All bindings, `.disabled(...)` conditions, and action closures were moved verbatim — no state source, gating logic, or engine call changed.
- Verification: `git diff --check` passed. The real workspace `scripts/build.sh` stopped at the known pre-existing missing `ScratchLab/Resources/Coach/Coach.usdz` (the user's dirty resource deletions, unrelated to this slice). A disposable git worktree carrying only this session's dirty view/service/test files plus checked-in resources built the desktop target successfully (`** BUILD SUCCEEDED **`) and, via `xcodebuild test -only-testing:` with the correct class name `CaptureReliabilityPhase1CoreTests`, ran exactly `testDemoModeProducesFeedbackWithoutHardware` and `testMacCaptureInputsExposeHandMotionAndCalibrationSetup` (2 tests, 0 failures, `** TEST SUCCEEDED **` — confirmed via raw per-test-case pass lines, not just exit code). Launched the built app and visually inspected the redesigned Capture tab at default and near-minimum (320pt) sidebar widths: the numbered Step 1/2/3 sequence renders correctly, the pinned Record hero stays visible while the Inputs card scrolls beneath it, and the duplicate "New Session" button is gone. At minimum sidebar width the pre-existing (unchanged-by-this-diff, confirmed via `git diff` containing no touched "Mode" line) `Picker("Mode", ...)` label wraps character-by-character — a pre-existing narrow-width layout issue, not a regression from this slice.
- Remaining risk: the "Mode" label narrow-width wrapping (pre-existing, Step 1 card) has not been fixed — flagged as a follow-up, not fixed here to keep this slice's diff minimal per the budget/scope instructions. Manual inspection of Demo Mode, blocked-audio, recording, and finalizing states beyond what was exercised here is recommended before shipping. Nothing was staged, committed, or pushed.

## 2026-08-12 (ASC development baseline verification — integration/midi-hotcue-dvs-on-b478d7a-20260812 @ 9fafb99)

- Selected task: verify the MIDI Learn / hot-cue / continuous-drive integration branch (built on the Capture redesign + canonical notation/gameplay work already landed on `origin/main` @ `b478d7a`) as a self-contained candidate baseline for continued App Store Connect development.
- Scope: read-only verification only, in a fresh disposable worktree checked out directly from `9fafb99` — no copying from the dirty primary worktree, no new features.
- Clean checkout: `git status --short` empty, HEAD = `9fafb99`, no untracked fixtures/resources required from any other worktree (`git clean -ndx` empty).
- Project integrity: `plutil -lint` on `project.pbxproj` passed; `MIDILearnedMapping.swift`, `MIDIFaderCurve.swift`, `MIDIPlatterContinuousDrive.swift`, `DVSContinuousVinylRenderer.swift`, and the last week's notation/gameplay files (`ScratchGameplayAttempt.swift`, `PracticeGameplayCoordinator.swift`, `NotationFeedbackState.swift`, `ScratchPerformanceComparison.swift`) all registered with normal reference counts, no duplicate build-file entries detected, no absolute developer-machine paths in the project file or Swift source, no references to `Fixtures/Timecode/`, `tmp/`, or other worktree-only content, and no bundled test-fixture WAV/JSON resources.
- Production builds from the clean worktree: macOS (`ScratchLabDesktop`) BUILD SUCCEEDED; watchOS (`ScratchLabWatch`, signing disabled per the documented `build.sh` recipe) BUILD SUCCEEDED; iOS (`ScratchLab`, `generic/platform=iOS`) failed with default local signing — isolated as a pure provisioning-profile/certificate mismatch for the embedded `ScratchLabWatch` companion (0 compile tasks had even started before the failure); rebuilding with `CODE_SIGNING_ALLOWED=NO` succeeded, confirming the source itself is correct and this is an environment-only condition, not a code defect.
- Tests: 444 XCTest cases (MIDI Learn/hot-cue/fader-curve/platter-drive, packet-bounds crash fix, DVS continuous renderer + user-mixer-gain, DVS offline grain renderer, DVS control-vinyl analyzer, Timecode bridge continuity, Timecode playback bridge, the Capture contract tests including the MIDI-learn race fix) plus 129 Swift Testing tests across 23 suites (canonical notation model, canonical fader authority, target-vs-performed comparison, gameplay attempt scoring, and the WATCH→COPY→RESULT `PracticeGameplayCoordinator` state machine) — all passed, 0 failures, raw per-test evidence captured (not accepted on exit code alone).
- Release/ASC audit: `playbackDriveEnabled` and `notationRoutingEnabled` both default `false` with no non-test call site setting either to `true`; the entire DVS/Timecode Advanced-section UI (including the `playbackDriveEnabled` toggle) lives inside `#if DEBUG` at the `AdvancedSection` enum-case level, so it does not exist at all in a Release build, not merely hidden; the debug-only Scratch Bank MIDI Preview / raw-pad-capture tools are correctly `#if DEBUG`-scoped; no absolute local paths found in source.
- Visual smoke check: app launches cleanly from the fresh worktree's build; Practice and Advanced → MIDI & fader re-confirmed intact this pass (learned-crossfader status now renders exactly once, per the prior slice's presentation fix; hot-cue/upfader/fader-curve mapping UI present). Capture and Review were not re-screenshotted this specific pass (window-focus interference from concurrent activity on the machine); both were already verified via source inspection this session and via direct screenshots in the immediately preceding Capture-redesign and MIDI-integration verification passes on this same code.
- `git diff --check` passed; the verification worktree remained clean throughout (no edits made — this was a read-only audit).
- Remaining caveat: DVS continuous-renderer hardware validation (`aba5e17`'s commit-message claim) remains unverified by hardware in this pass — software-only. No blocker preventing `9fafb99` from serving as the forward development baseline was found. Not pushed.

## 2026-08-12 (Retire iOS and watchOS production targets — macOS-only going forward)

- Selected task: make macOS ScratchLab the sole active production Xcode target, per explicit product-direction decision. Preserve iOS/watchOS source, Companion Camera, Watch motion/WatchConnectivity implementation, and backward-compatible session data-model fields; retire only the active build-target relationships.
- Root cause / mechanism: `project.pbxproj` (`objectVersion = 90`, uses one `PBXFileSystemSynchronizedRootGroup` for the unrelated `ScratchLabDatasetBuilder` target) was edited via the `xcodeproj` gem's object graph API (v1.28.1, explicitly version-pinned on every invocation — the bundled system gem is 1.27.0 and must never be relied on for this project). Before any mutation, a no-op open+save round-trip was verified byte-for-byte semantically lossless: identical UUID sets for all 15 object types (1394/1394 objects, corrected after an initial extraction-script bug falsely flagged `PBXGroup`/`PBXSourcesBuildPhase` counts — a second, careful pass using true object-definition-line matching confirmed 0 diff everywhere), identical `TargetAttributes`/`knownRegions`/`compatibilityVersion`/`objectVersion`/`preferredProjectObjectVersion`, 0 build-settings mismatches across all 12 configurations, and a full `plistlib` deep-equality of `True`.
- Removal: for each of the two retiring targets (`ScratchLab` iOS, `ScratchLabWatch` watchOS — the latter a single modern watchOS app target, not the legacy app+extension pair), removed its `TargetAttributes` hash entry, its `PBXTargetDependency`+`PBXContainerItemProxy` (the `ScratchLab → ScratchLabWatch` edge), every build phase and the `PBXBuildFile` entries within each, its `XCConfigurationList`+`XCBuildConfiguration`s, its product `PBXFileReference`, and finally the target object itself. Diffed the canonicalized "no intentional changes" round-trip against the canonicalized "targets removed" round-trip (both through the same xcodeproj serialization, so pure reordering noise cancels out) to produce an isolated, auditable structural diff: exactly 2 `PBXNativeTarget`, 187 `PBXBuildFile`, 2 `PBXFileReference` (only the two obsolete `.app` product refs — zero source-file references removed), 2 each of Sources/Frameworks/Resources build phases, 1 `PBXCopyFilesBuildPhase` ("Embed Watch Content"), 1 `PBXTargetDependency`+`PBXContainerItemProxy`, 4 `XCBuildConfiguration`, 2 `XCConfigurationList` removed; `PBXGroup`/`PBXFileSystemSynchronizedRootGroup`/`PBXProject` counts unchanged (0 diff) — confirming the file/group navigator structure was untouched.
- Real finding en route: `xcodeproj` 1.28.1's `AbstractObject#==` is value-based (`to_hash` comparison), not UUID-based, and `remove_from_project`'s generic referrer-cleanup uses `Array#delete` internally. Because this project's iOS and macOS products are BOTH literally named `ScratchLab.app` (structurally identical `PBXFileReference` attribute values, only differing by UUID), removing the iOS product reference's group membership collaterally also removed the macOS one from the shared `Products` group's `children` array — the underlying macOS product object itself was never deleted (0 dangling references throughout), only its cosmetic group-navigator listing. Caught by explicit before/after inspection of `Products` group membership against the untouched original; fixed with a UUID-keyed (not value-keyed) restoration pass immediately after the removal loop, re-verified clean (`ScratchLabDesktop`/`ScratchLabDesktopTests`/`ScratchLabDatasetBuilder` targets and the macOS product's `Products`-group membership all confirmed byte-identical to the pre-mutation project via direct UUID comparison).
- Removed `ScratchLab.xcodeproj/xcshareddata/xcschemes/ScratchLab.xcscheme` (the combined scheme that built both retiring targets together); `ScratchLabDesktop.xcscheme` untouched. No `xcschememanagement.plist` existed to update. `ScratchLab.xcodeproj/project.pbxproj`'s macOS bundle identifier (`com.machelpnz.scratchlab`), marketing version, and build number were not touched.
- Source preservation: all 17 iOS-exclusive Swift files (`CompanionCameraBroadcaster.swift`, `CompanionCameraView.swift`, `WatchMotionCaptureStore.swift`, `WatchCaptureHubView.swift`, `ScratchLabApp.swift`, `MainMenuView.swift`, `PracticeModeView.swift`, `AudioEngine.swift`, `BackingTrackManager.swift`, `CoachPreviewView.swift`, `EquipmentDetector.swift`, `LevelSelectView.swift`, `SampleManager.swift`, `ScratchAnalyzer.swift`, `ScratchBankPadGridView.swift`, `ScratchMotionLane.swift`, `VirtualPlatterPrototypeView.swift`) and all 6 watchOS-exclusive files (`WatchMotionRecorder.swift`, `WatchMotionCapture.swift`, `CaptureReliability.swift`, `ScratchLabWatchApp.swift`, `WatchCaptureView.swift`, `ScratchLabNotificationController.swift`) confirmed present on disk, unregistered from any active target but browsable/re-addable — none deleted. The 148 files shared with macOS keep their `ScratchLabDesktop` target membership untouched (confirmed `CaptureCore.swift` — home of the cross-device `sessionID`/`takeID`/Watch-sync-state backward-compatible schema — still compiles into `ScratchLabDesktop`, 202 source files unchanged).
- Companion Camera: `CompanionCameraBroadcaster.swift`/`CompanionCameraView.swift` (iOS broadcasting side, now archived/unregistered) vs. `ScratchLabDesktop/Services/CompanionCameraReceiver.swift` (macOS receiving side, already independent and untouched) — cleanly separable; classified optional future companion, not deleted.
- Mac UX: re-confirmed unchanged from the pre-retirement dependency audit — `routineStartDisabled` still gates only on `captureEngine.isSelectedAudioInputAvailable`; the Watch status tile's idle label is still literally "Watch Optional"; no code path requires iPhone/Watch presence for Practice/Capture/Review.
- Verification: `plutil -lint` OK; xcodeproj reopen OK; 0 dangling references; `xcodebuild -project ScratchLab.xcodeproj -list` confirms exactly `ScratchLabDesktop`/`ScratchLabDesktopTests`/`ScratchLabDatasetBuilder` targets and `ScratchLabDesktop`/`ScratchLabDatasetBuilder` schemes remain (no iOS/watchOS); no absolute local paths in the pbxproj; `git diff --check` passed. macOS build from the working worktree: BUILD SUCCEEDED. Full independent verification (fresh disposable worktree, macOS build, full test suite, visual smoke check) recorded separately below / in this same entry once complete.

## 2026-08-13 (Fix click-assisted routine capture audio-packet stack overflow)

- Selected task: fix the user-reported crash when recording a Baby Scratch with a click. This superseded the remaining approval-gated DVS tasks because capture reliability is the project's highest priority and the crash report supplied a concrete implementation-ready failure.
- Root cause: `ScratchLab-2026-08-13-193719.ips` reports `Thread stack size exceeded` on the `scratchlab.mac.capture.audio` queue. The faulting stack is `MacCaptureEngine.processAudioSampleBuffer(_:)` → `MacCaptureEngine.audioPacket(from:)` → `IndexingIterator.next()` while converting integer PCM. `audioPacket(from:)` used `AudioBuffer.mDataByteSize / bytesPerSample` as the Range bound, so inconsistent or oversized buffer metadata could produce an effectively unbounded conversion loop on the capture worker. The report proves the audio extraction failure; it does not prove the click generator itself was defective.
- Files changed: `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, and `DEV_LOG.md`. Existing unrelated notation/design changes were preserved.
- Implementation: added a bounded, overflow-checked `validatedAudioSampleCount` derived from `CMSampleBufferGetNumSamples` and each validated buffer's channel count. Buffer byte size now only proves that the required bytes exist; it no longer controls iteration. The extractor rejects zero or excessive frame/channel counts, unsupported PCM layouts, short buffers, and inconsistent interleaved/non-interleaved buffer layouts before reading memory. Int16/Int32 conversion now uses explicit bounded loops. Existing Float32 and mono-downmix behavior remains unchanged.
- Verification: the changed desktop target compiled. Four focused regressions passed 4/4 in both configured test runs: existing two-buffer non-interleaved Float32 downmix, oversized byte metadata bounded to authoritative frames, inconsistent/excessive metadata rejection, and Rane-style 14-channel interleaved Int32 downmix. `scripts/build.sh` passed capture-pipeline fixtures 47/47, then completed its broad desktop plan with established unrelated failures in removed-resource/scheme assertions, MIDI learn, sample playback, and the pre-existing dirty notation lane-label assertion; none of the new audio tests failed. `git diff --check` passed. An Address Sanitizer build compiled, but repeated locked-device/CoreAudio test-host service errors interrupted it before a valid result bundle was written, so it is not claimed as a pass.
- Remaining risk and verification: only a real capture can prove the exact hardware packet that caused the crash is now rejected or converted safely. Relaunch the rebuilt app, record 10–15 seconds first without a click and then with the click, and confirm both takes finalize and Review opens. If another crash occurs, preserve the new `.ips` report for comparison. Nothing was staged, committed, or pushed.

## 2026-08-13 (Capture-movement loss — Phase 1 evidence capture + Phase 2 exact replay; uncommitted)

- Selected task: after the 2026-08-13 Take 006 diagnosis (176 raw platter samples @ ~13.35 Hz, 35 audio events, only 4 `recordMovementEvents`), implement evidence capture and exact offline replay FIRST — no sampling-rate or threshold change until the exported/replayed evidence proves the loss stage. Karl's correction: do not assume 13.35 Hz is the root cause; the offline platter replay drops raw X/Y, joint confidence, misses, continuity substitutions, publication timing, and MainActor delay.
- Root cause (of the diagnostic gap, not yet the capture loss itself): the only exported movement evidence was the normalised integrated platter timeline (`take-N_raw_platter_debug.json`), which cannot reproduce the live `HandDirectionTracker → RoutineDetectedNotationBuilder → RoutineNotationEventNormalizer → RoutineNotationFusionEngine` result offline, so the exact stage where strokes disappear could not be proven.
- Files changed (all DEBUG-only; Release byte-stable): `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLab/Services/SessionExportCoordinator.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`.
- Implementation:
  - `debug/take-N_movement_trace.json` — a versioned `MovementTraceExport` recording every chronological observation: presentation / processing-start / Vision-completion / take-relative timestamps; point kind (real / continuityFill / miss / jumpRejected); raw tracked X/Y and smoothed X/Y; winning joint confidence; raw tracker direction + idle reason + tracker confidence + semantic direction; publication-dedup outcome; and the exact builder input (smoothed point, state, confidence, time). Recorder memory is bounded (`maxObservations`).
  - `debug/take-N_movement_diagnostics.json` — the completed `RoutineMovementDiagnosticsSnapshot` (now `Codable`) with all drop-reason dictionaries and final counts.
  - Both frozen at take finalization, associated to the take, copied into `debug/` with canonical `takeID`/`takeNumber` fix-up during package construction, deterministic sorted-key JSON, and encode/write failures surfaced via `reportRoutineRecordingIssue`.
  - `MovementTraceReplay.replay(...)` consumes the trace directly through the real tracker → builder → normalizer → fusion (NOT the platter timeline), recomputing confidence/timing from recorded inputs. `trackedHandPoint` now returns the winning joint's confidence.
- Verification: new `MovementTraceDiagnosticsTests` 4/4; `CaptureReliabilityPhase1CoreTests` 322 tests / 8 failures, all pre-existing and unrelated (iOS scheme/target-membership + Coach resource-bundling checks in this macOS-only worktree); `python3 scripts/test_capture_pipeline.py` 47/47; macOS `build` + `build-for-testing` succeeded; `git diff --check` clean.
- Follow-up (Phases 3–5, NOT implemented): annotate Take 006 video for ground-truth movement intervals; replay the exported trace against that fixture; classify each lost movement (no Vision observation / low-confidence joint / discontinuity / impossible-jump / tracker-idle / reversal-not-committed / builder-fragment / normalizer-reject / fusion-reject / timestamp-ordering); only then apply the smallest proven fix (scheduling, time-based tracker, builder-off-MainActor, or reacquisition segmentation) with a failing-then-passing ground-truth test. Do NOT lower normalizer distance/speed/confidence thresholds merely to raise event count.

## 2026-08-13 (Capture-movement loss — review fidelity fixes; uncommitted)

- Selected task: address reviewer defects in the Phase 1/2 movement trace + replay so it is an exact live replay before any further hardware recording. No sampling/threshold/notation/scoring/Review changes.
- Root cause (of the review defects): the trace conflated proposed builder input with actual execution; dedup-rejected observations were still fed by replay; a rejected jump recorded both `.jumpRejected` and `.miss` (double `recordMiss`); the trace was written only at Export rather than at take finalization; companion copy used `try?` (silent failures).
- Files changed: `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `ScratchLabDesktop/Views/MacAnalyzerView.swift`, `ScratchLab/Services/SessionExportCoordinator.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`.
- Implementation:
  - Dedup fidelity: `builderPoint`/`builderState`/`builderConfidence` now represent ACTUAL builder input (nil when dedup-rejected/never-published); `MovementTraceReplay` never feeds dedup-rejected observations.
  - Jump fidelity: a rejected jump is ONE trace record (`.jumpRejected`); `handleHandTrackingMiss` no longer double-records; replay drives exactly one `recordMiss` per jump/miss.
  - Actual builder execution: added `enqueueTime`/`enqueueSequence` (publication) + `builderExecutionTime`/`builderExecutionSequence`/`builderAction`/`builderRejectionReason`/`finishedEventIndex`/`builderSkippedReason` (MainActor execution), keyed by a stable observation `index` assigned before enqueue. The builder reports its action (started/extended/rejected/finished + drop reason) and skip reasons (`recordingStopped`/`postFinalization`). `MovementTraceReplay.idealReplay` (enqueue order, frame time) and `MovementTraceReplay.exactReplay` (actual execution order + time + skip) are now separate.
  - Per-take persistence: the frozen trace + diagnostics are written beside the take's sidecar at finalization (with the take's identity), guarded by a lock; the Export-time writer was removed. A two-take recorder-isolation test proves no cross-take overwrite.
  - Error propagation: `SessionExportCoordinator.copyDebugCompanion` throws on present-but-unreadable/malformed/unencodable/unwritable companions; absent companions remain optional.
- Tests (`MovementTraceDiagnosticsTests`): 10 tests — trace round-trip + order; diagnostics round-trip (drop dicts + counts); ideal replay single forward stroke; determinism; dedup-exclusion; jump-is-one-miss; recorder-stores-one-jump-record; exact-replay-skip; exact-vs-ideal order; recorder take isolation. 10/10 pass.
- Verification: `MovementTraceDiagnosticsTests` 10/10; `CaptureReliabilityPhase1CoreTests` 322 tests / 8 failures all pre-existing (iOS scheme/target-membership + Coach resource checks in this macOS-only worktree); `python3 scripts/test_capture_pipeline.py` 47/47; macOS build + build-for-testing succeeded; `git diff --check` clean.
- Follow-up: the full two-take `SessionExportPackage` archive integration test and the `copyDebugCompanion` unit tests are deferred (the `@MainActor` static helper has a test-target visibility quirk and the archive helpers live in `CaptureReliabilityPhase1CoreTests`); the recorder-isolation test covers the no-overwrite guarantee. Next hardware take must produce the real trace for replay-vs-live comparison.

## 2026-08-13 (Capture-movement loss — finalization barrier + archive tests; uncommitted)

- Selected task: complete the deferred `copyDebugCompanion`/two-take archive tests and add a deterministic finalization barrier so every builder task enqueued for the active take either finishes before the drain or is recorded as skipped.
- Root cause (of the remaining gap): the builder was drained and the trace frozen on the AVCaptureFileOutput delegate queue while builder tasks still ran on the MainActor, so a task enqueued just before finalization could race the drain. Also, `copyDebugCompanion` was a `@MainActor` static member and invisible to `@testable` tests, and the trace did not record the finalization drain instant (the builder's drain advances the last movement's `endTime` to that instant, so replay could not reproduce `endTime` exactly).
- Files changed: `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `ScratchLab/Services/SessionExportCoordinator.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`.
- Implementation:
  - `builderTaskGroup` (`DispatchGroup`) wraps every builder task (`enter` before enqueue, `leave` in `defer`); `finalizeRoutineRecording` captures the builder, clears the active reference (so late tasks record `postFinalization`), `wait()`s, then drains. Runs on the delegate queue, never blocks MainActor on itself. The builder task now uses `if let builder = activeRoutineDetectedNotationBuilder, isRoutineRecording` and records `postFinalization`/`recordingStopped` skips.
  - `copyDebugCompanion` moved to file scope (nonisolated) so it is unit-testable; the `stagePackage` call site is unchanged.
  - `MovementTraceExport` gains `finalizationHostTime`; `finalizeRoutineRecording` drains and exports with it; `MovementTraceReplay.idealReplay`/`exactReplay` accept a `drainTime` so the drain reproduces the event `endTime` exactly.
- Tests (`MovementTraceDiagnosticsTests`): now 15 — added `copyDebugCompanion` absent/malformed/identity, two-take distinct files, and `testFinalizationBarrierAgreement` (deliberately delayed, serialized builder tasks → saved fused notation == exact replay). Updated the `testMacCaptureEngineSourceDocumentsSingleDirectionNormalizationHelper` source-contract assertion (`builder.recordObservation(`). 15/15 pass.
- Verification: `MovementTraceDiagnosticsTests` 15/15; `CaptureReliabilityPhase1CoreTests` 322 tests / 8 pre-existing failures (iOS scheme/target-membership + Coach resource checks); `python3 scripts/test_capture_pipeline.py` 47/47; macOS build + build-for-testing succeeded; `git diff --check` clean.
- Follow-up: the full two-take `SessionExportPackage` archive-integration test remains deferred (package construction helpers live in `CaptureReliabilityPhase1CoreTests`); the `copyDebugCompanion` + recorder-isolation tests cover the copy/no-overwrite guarantee. Hardware recording still required for the replay-vs-live comparison.

## 2026-08-13 (Capture-movement loss — admission gate; and 8-failure audit; uncommitted)

- Selected task: replace the bare `builderTaskGroup` with a per-take, lock-protected admission gate (the reviewer identified that `DispatchGroup.wait()` could still race a later `enter()` because live capture stays active during finalization); then audit the eight `CaptureReliabilityPhase1CoreTests` failures.
- Root cause (gate): `enter()` and closing were not serialized, so a video frame processed after `wait()` reached zero could still enqueue a builder task after the drain.
- Files changed: `ScratchLabDesktop/Services/MacCaptureEngine.swift` (added `BuilderAdmissionGate`, wired `open`/`admit`/`close`/`isCurrent`, `finalizationHostTime` in the trace export + replay `drainTime`), `ScratchLab/Services/SessionExportCoordinator.swift` (`copyDebugCompanion` to file scope), `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift` (17 movement-trace tests + 6 stale-assertion fixes), `DEV_LOG.md`, `TASKS.md`.
- Implementation: `BuilderAdmissionGate` shares one `NSLock` for `open`/`admit`/`close`/`isCurrent`; `admit` atomically checks open + `enter()` and returns a generation-tied token; `finalizeRoutineRecording` closes admission first (so later `enter()`s are impossible) then `wait()`s, drains the captured builder with the recorded `finalizationHostTime`, and freezes the trace. A task whose generation is no longer current never mutates the builder.
- Tests: `MovementTraceDiagnosticsTests` 17/17 (gate close-then-admit returns nil, generation isolation, finalization rejects late enqueue, saved notation == exact replay).
- Audit: all eight failures were pre-existing and caused by two already-landed commits — `736e6ab "Retire iOS and watchOS production targets"` (deleted `ScratchLab.xcscheme` and the iOS-target pbxproj Sources/Resources entries) and `df4972a` (changed the demo copy to "…start a simple practice run."). None touch the movement-diagnostics diff (verified: `git diff` contains no pbxproj/scheme entries). Fixed the clearly-stale assertions only — deleted the iOS-scheme test and removed the retired iOS-target refs/`CoachPreviewView`; kept all valid macOS resource/packaging assertions (Coach.usdz, CoachInstructions macOS refs).
- Verification: `CaptureReliabilityPhase1CoreTests` 322 tests / 8 failures → 321 tests / 0 failures; `MovementTraceDiagnosticsTests` 17/17; `python3 scripts/test_capture_pipeline.py` 47/47; macOS build + build-for-testing succeeded; `git diff --check` clean.

## 2026-08-13 (Capture-movement loss — critical gate fix: Release parity + synchronous skip; uncommitted)

- Selected task: fix the reviewer's critical finding that `builderAdmissionGate.open()` sat inside `#if DEBUG` while `admit()`/`isCurrentTake` were unconditional, so Release builds never opened the gate and rejected every builder observation. Also close the rejected-admission trace race (postFinalization was recorded in an untracked MainActor task).
- Root cause: `open()` was DEBUG-scoped; `admit()` returning nil was recorded in a MainActor task outside the finalization barrier, so it could race the trace freeze.
- Files changed: `ScratchLabDesktop/Services/MacCaptureEngine.swift` (moved `builderAdmissionGate.open()` into the shared `startRoutineRecording` path; recorded `token == nil` postFinalization synchronously on the capture path; kept the admitted-but-not-fed skip inside the task before its `leave()`), `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift` (2 new tests), `DEV_LOG.md`.
- Tests: `MovementTraceDiagnosticsTests` now 19 — added `testBuilderAdmissionGateOpenAndAdmitAreSharedNotDebug` (compile-contract: open/admit not `#if DEBUG`-guarded, trace writer IS) and `testRejectedAdmissionSkipIsInFrozenTrace` (rejected-admission skip is in the frozen export, not a later mutable update). 19/19 pass.
- Verification: `MovementTraceDiagnosticsTests` 19/19; `CaptureReliabilityPhase1CoreTests` 321/321 (0 failures); `python3 scripts/test_capture_pipeline.py` 47/47; Debug `build` + `build-for-testing` succeeded; `git diff --check` clean. Release `build` fails on two PRE-EXISTING, unrelated errors — `TimecodeCMSampleBufferAdapter` (target membership) and `accumulatedStepsBeforeThisTick` (a `#if DEBUG`-guarded local referenced in Release) — neither touched by this diff; the compile-contract test covers the gate Release-parity concern specifically.

## 2026-08-14 (Capture-movement loss — deadlock fix: async finalization; uncommitted)

- Selected task: fix the hardware-reproduced finalization deadlock. A live sample showed `fileOutput(didFinishRecordingTo:)` (delivered on the main dispatch queue) → `finalizeRoutineRecording` → `builderAdmissionGate.close().wait()`, which blocked the main queue so the admitted `@MainActor` builder tasks could never run to `leave()` the group.
- Root cause: a synchronous `DispatchGroup.wait()` on the AVFoundation completion/main queue while the group's tasks require MainActor.
- Files changed: `ScratchLabDesktop/Services/MacCaptureEngine.swift` (split finalization into a synchronous close+capture phase and an asynchronous `completeRoutineFinalization` resumed via `group.notify(queue: finalizationQueue)`; added `isRoutineFinalizationPending` + a `startRoutineRecording` guard), `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift` (async-finalization regression test), `DEV_LOG.md`.
- Implementation: `finalizeRoutineRecording` now returns immediately — it captures the take state, keeps the builder ATTACHED (so admitted tasks still feed it), atomically `close()`s the gate, sets the pending flag, and schedules `group.notify(queue: finalizationQueue)`. The notify body (background, never MainActor) then detaches + drains the builder, drains the platter/DVS, fuses, freezes the trace/diagnostics, writes the sidecar + companions, clears take state, and publishes the UI state on MainActor (clearing the pending flag). Late observations after close are synchronously marked `postFinalization` on the capture path and never reach the builder. A second take is refused while `isRoutineFinalizationPending` is set.
- Tests: `MovementTraceDiagnosticsTests` now 20 — added `testAsyncFinalizationReturnsWithoutBlockingAndCompletesOnce` (entry returns without blocking, completion fires exactly once, saved notation == exact replay). 20/20 pass.
- Verification: `MovementTraceDiagnosticsTests` 20/20; `CaptureReliabilityPhase1CoreTests` 321/321 (0 failures); `python3 scripts/test_capture_pipeline.py` 47/47; Debug `build` + `build-for-testing` succeeded; `git diff --check` clean. Release `build` still fails on the two pre-existing unrelated errors (TimecodeCMSampleBufferAdapter target membership; `accumulatedStepsBeforeThisTick` DEBUG-guarded local in Release), unchanged by this diff.

## 2026-08-14 (Controller CC6 platter telemetry as primary movement source; uncommitted)

- Selected task: make direct RANE ONE MKII CC6 platter telemetry the preferred record-movement source (camera is a fallback/diagnostic), and fix the two known Release blockers. The 2026-08-14 export proved camera motion is unusable here (266 observations → 238 idle → 1 movement event) while CC6 carries a clean alternating 90-BPM Baby Scratch stream.
- Root cause: `withMixerMidiEvents` only derives FADER events from `mappedControl == "crossfader"`; CC6 platter (`controller 6`) has `mappedControl == nil`, so 8,656 events were persisted but never converted to `recordMovementEvents`. The fusion hardcodes movement provenance as `"video"`.
- Files changed: `ScratchLab/Models/CaptureCore.swift` (`derivePlatterMovementEvents` decoder + `withControllerMovementProvenance`), `ScratchLabDesktop/Services/MacCaptureEngine.swift` (decode CC6 in finalization, prefer controller over camera/DVS, gate `lowLatencyTimecodeTapHasRequiredChannels` to DEBUG), `ScratchLabDesktop/Views/MacAnalyzerView.swift` (truthful "Controller platter"/"DVS timecode" source labels), `ScratchLabDesktop/Services/ScratchSamplePlaybackController.swift` (Release fix: `accumulatedStepsBeforeThisTick` moved out of `#if DEBUG`), `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift` (`ControllerPlatterDecoderTests`, 7 tests), `DEV_LOG.md`.
- Decoder: modular signed deltas (±half modulus) so 127↔0 never false-reverses; integrates an unbounded position normalized to 0…1; groups same-sign runs (gap ≤ 0.10 s); rejects runs < 0.08 s or < 8 net steps; emits `source: "controller"` events with signed direction, speed, confidence (0.7–0.95, never 1.0 merely for detection).
- Source-priority contract: controller (CC6) > DVS/timecode > camera; never merged. `withControllerMovementProvenance` retags the fusion's "video" → "controller" and adds "controller" to `detectionSources`. Camera stays available as a diagnostic/fallback.
- Verification: `ControllerPlatterDecoderTests` 7/7, `MovementTraceDiagnosticsTests` 20/20, `CaptureReliabilityPhase1CoreTests` 321/321, pipeline 47/47, Debug build + build-for-testing + Release build all succeed, `git diff --check` clean.
- Follow-up (separate workstream, NOT done): camera fallback repair (cadence quantization, unstable landmark selection, 1-D X-only assumption) — its third defect spec was truncated and needs restatement before implementation.

## 2026-08-14 (Controller authority through fusion + CC6 scoping; uncommitted)

- Selected task: independent review found the controller decoder was demoted to "video" through fusion and unscoped to device/channel. Make the fusion source-aware, scope CC6 to the verified right-platter channel 1 + device, and report the full Take 002 pipeline counts.
- Root cause: `fuseMovementEvents` downgraded unmatched controller events to `source: "video"` + `confidence * 0.62`, and `trustedDirectionalEvents` accepted only "fused"/"detected"; `classify` re-applied the 0.62 camera penalty on every pass.
- Files changed: `ScratchLabDesktop/Services/MacCaptureEngine.swift` (source-aware `fuseMovementEvents`/`trustedDirectionalEvents`/`classify`; `snapshot` labels "controller"/"timecode_live" truthfully; CC6 decode scoped to channel 1 + device), `ScratchLab/Models/CaptureCore.swift` (`derivePlatterMovementEvents` gained `deviceName` filter; removed the now-redundant `withControllerMovementProvenance`), `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift` (`ControllerPlatterDecoderTests` 8 tests: fusion provenance survival, channel/device isolation, fwd/back/fwd fixture), `DEV_LOG.md`.
- Source-priority contract (truthful): shared Release path is controller → camera (never merged). DVS/timecode notation remains `#if DEBUG` diagnostic-only — NOT a Release priority.
- Decoder: CC6 ch1 + device; modular signed deltas; runs (gap ≤ 0.10s, ≥ 0.08s, ≥ 8 steps); normalized 0…1; source "controller".
- Take 002 pipeline counts (offline): filtered MIDI 8656 → decoder runs 50 → noise-filtered 35 → normalized 32 (3 dropped by classify min-duration/delta/speed) → trusted/saved/Review/export 32 (26 audio-correlated). The 10.64–14.98 s region is clean alternating fwd/back (14 reversals, ~0.30–0.34 s each).
- Verification: `ControllerPlatterDecoderTests` 8/8, `MovementTraceDiagnosticsTests` 20/20, `CaptureReliabilityPhase1CoreTests` 321/321, pipeline 47/47, Debug + Release builds succeed, `git diff --check` clean.
- Follow-up (NOT done): camera fallback workstream (cadence quantization, joint-identity stability, platter-axis projection, offline camA replay vs controller reference) — separate, deferred.
## 2026-08-22 (ASC beta compiler-warning audit and safe cleanup)
- Starting branch/HEAD: clean `main` at `d34adb6`.
- Root cause: direct Combine users relied on transitive imports; two types overstated Sendable safety (`FileManager` storage and a scoped unsafe-pointer view); the DVS ingress callback mixed weak and implicit strong capture semantics; two SwiftUI `onChange` calls used the deprecated overload; five side-effect-free locals were unused; iOS-only builds additionally exposed a missing Combine import, global-screen sizing, an actor-isolation mismatch in a pure DEBUG file writer, deprecated Game Center presentation, and a Watch accent-color setting naming a nonexistent asset.
- Files changed: `ScratchLab.xcodeproj/project.pbxproj`; `ScratchLab/Audio/AudioEngine.swift`; `ScratchLab/Models/ControllerInput/Registry/MIDILearnedMapping.swift`; `ScratchLab/Models/ScratchPerformanceComparison.swift`; `ScratchLab/Services/ProgressManager.swift`; `ScratchLab/Views/PracticeModeView.swift`; `ScratchLab/Views/VirtualPlatterPrototypeView.swift`; `ScratchLabDesktop/Services/TimecodeRealtimeIngressRing.swift`; `ScratchLabDesktop/Views/DVSControlVinylPanel.swift`; `ScratchLabDesktop/Views/LiveNotationOverlayView.swift`; `ScratchLabDesktop/Views/MacAnalyzerView.swift`; `ScratchLabDesktop/Views/NotationVisualizerView.swift`; `ScratchLabDesktop/Views/TimecodeControlCard.swift`; `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`; plus `TASKS.md` and this log.
- Warning result: clean macOS Debug moved from 29 emitted warning lines (18 distinct compiler diagnostics + one metadata-tool notice) to zero compiler warnings and one metadata-tool notice. Across all required destinations, all 24 distinct compiler warnings and the Watch asset-catalog warning were removed. The App Intents metadata notice remains intentionally: adding an unused AppIntents framework dependency merely to silence its extractor would be semantically wrong.
- Ownership/concurrency: `MIDILearnedMappingStore` remains legitimately `Sendable` with immutable Sendable stored state and method-local `FileManager.default`; `TimecodeRealtimeIngressSlotView` no longer claims `Sendable` because it is created and synchronously consumed on the worker context and its pointer must never escape; the capture engine now strongly retains the pipeline through its intentionally persistent callback, with no cycle because the pipeline does not retain the engine. No larger concurrency redesign is required by this warning set. The DEBUG WAV writer's `nonisolated` is narrow and proven: it accesses no actor state and performs encoding/I/O on the existing utility queue.
- Verification: fresh builds succeeded for macOS Debug, macOS Release, iPhone 17 simulator, iPad (A16) simulator, and Apple Watch Series 11 (42mm) simulator, all with zero compiler warnings. Focused XCTest selection passed 242 tests with 11 expected environment-gated skips in each of two configurations (DVS live tap, ingress ring, timecode notation routing, MIDI learned mapping, live notation overlay, virtual platter, capture callback lifetime). Focused Swift Testing passed 97 tests in 16 suites in each configuration (performance comparison and Practice progression). `python3 scripts/test_capture_pipeline.py` passed 47/47.
- Required broad gate: `scripts/build.sh all` exited 65 at its desktop test phase, after capture fixtures passed, so its later build stages did not run. The full plan reported 2,953 XCTest executions / 47 skipped / 101 failures and 357 Swift Testing tests / 2 issues. Failures were the repository's broad shared-state/missing-fixture baseline (MIDI mixer/engine state, scratch playback sample position, absent local timecode fixtures, and absent source files); all touched DVS/ring/notation suites passed there. Independent destination builds above provide the required platform build evidence.
- App Store beta assessment: no remaining compiler warning blocks ASC beta; the sole retained App Intents extractor notice is non-code and non-blocking.

## 2026-08-22 (iOS DVS + MIDI parity — Task 1 shared route state)
- Starting branch/HEAD: clean `main` at `927de38`; this existing clean commit served as the requested pre-change checkpoint, so no additional checkpoint commit was created.
- Root cause: iOS had no shared, platform-neutral representation of the active audio hardware route, leaving later route detection, stereo-pair selection, capture configuration, and UI publication without a single source of truth.
- Files changed: `ScratchLab/Models/AudioHardwareRouteState.swift`; `ScratchLabDesktopTests/AudioHardwareRouteStateTests.swift`; `ScratchLab.xcodeproj/project.pbxproj`; plus `TASKS.md` and this log.
- Implementation: added `AudioHardwareRouteState` with optional device name/sample rate, transport classification, nonnegative channel count, deterministic zero-based adjacent stereo pairs with one-based display labels, validated selected pair, input-activity state, and finite 0...1 signal level. Invalid metadata is normalized at initialization. The type is Foundation-only and contains no AVAudioSession, CoreAudio, MIDI, engine, capture, or UI integration.
- Tests: 14-channel pair generation (1/2 through 13/14), odd-channel handling, invalid and valid selection behavior, and metadata/signal normalization. All 5 tests passed in each of the test plan's two configured runs.
- Verification: `plutil -lint` and `git diff --check` passed; focused `AudioHardwareRouteStateTests` passed; macOS compiled through the test run; iPhone 17, iPad (A16), and Apple Watch Series 11 (42mm) simulator builds succeeded with code signing disabled. The retained App Intents metadata notice is pre-existing and unrelated.
- Gate: Task 1 only is complete. Tasks 2–9 were not started; no AVAudioSession route adapter, CoreMIDI ingress, capture wiring, flags, or UI was changed.

## 2026-08-22 (iOS DVS + MIDI parity — Task 2 USB audio discovery)
- Root cause: `AudioEngine` tracked AVAudioSession inputs for its existing microphone/line-input picker but did not publish active hardware identity, UID, transport, live format, channel metadata, stereo pairs, or PCM activity through the shared `AudioHardwareRouteState`.
- Files changed: `ScratchLab/Audio/AudioEngine.swift`; `ScratchLab/Models/AudioHardwareRouteState.swift`; `ScratchLabDesktopTests/AudioHardwareRouteStateTests.swift`; plus `TASKS.md` and this log. Existing Task 1 project membership remains in `ScratchLab.xcodeproj/project.pbxproj`.
- Implementation: added a private iOS route observer beside the existing engine. The active route remains authoritative; matching `availableInputs` metadata enriches it by UID; the input node's `inputFormat(forBus: 0)` supplies its live format; channel metadata and format count are normalized without hardcoding a device or count. The published state now includes UID and individual available channels. Existing mono analysis, source selection, permission handling, recording, and playback remain unchanged. PCM callbacks update only activity/level on the already-observed route, avoiding hardware re-query on the real-time path.
- Diagnostics: DEBUG-only route-change logging prints device, transport, sample rate, channel count, and available stereo pairs when hardware-format fields change. Release has no new logging.
- Tests: mocked route descriptions cover absent input, mono, stereo, 14-channel multichannel input, and invalid/duplicate/out-of-range channel metadata, alongside the five Task 1 invariants. All 10 tests passed in each of two configured test-plan runs.
- Verification: macOS compiled via the focused test run; iPhone 17, iPad (A16), and Apple Watch Series 11 (42mm) simulator builds succeeded with code signing disabled; `plutil -lint` and `git diff --check` passed. An intermediate rebuild exhausted disk space; only the disposable Task 1/2 Derived Data directories created during this work were deleted, freeing 3.5 GB, after which all destination builds passed.
- Hardware truth: simulator builds prove API/target compatibility only. RANE ONE MKII device naming, UID, reported channel topology, sample rate, and pair list remain unknown until a real USB-C connection is observed. No DVS, timecode, MIDI, capture-routing, or UI work was started.

## 2026-08-22 (iOS DVS + MIDI parity — Tasks 1/2/3/4/6: hardware route adapter + multichannel PCM routing)
- Root cause: the Task 1/2 route-observation logic lived ad hoc inside `AudioEngine`, had no dedicated bridge type, and always downmixed every input channel to mono before analysis — correct for a 1–2 channel mic/line-in, but wrong for a multichannel USB interface (e.g. RANE ONE MKII), where blending all channels together would mix unrelated deck/mic signals into one incoherent mono stream. There was also no rule for which stereo pair to treat as selected: `selectedStereoPair` was always `nil` in practice.
- Files changed: `ScratchLab/Audio/iOSAudioHardwareRouteAdapter.swift` (new); `ScratchLab/Models/AudioHardwareRouteState.swift`; `ScratchLab/Audio/AudioEngine.swift`; `ScratchLabDesktopTests/AudioHardwareRouteStateTests.swift`; `ScratchLab.xcodeproj/project.pbxproj` (new file added to the iOS `ScratchLab` target only — it is not part of `ScratchLabDesktop` or `ScratchLabWatch`); plus `TASKS.md` and this log.
- Task 1 (adapter): extracted the AVAudioSession→`AudioHardwareRouteState` bridge out of `AudioEngine` into `iOSAudioHardwareRouteAdapter`, an `@MainActor` `ObservableObject` publishing `routeState`. It owns hardware observation (`refresh(session:inputNode:isInputActive:signalLevel:)`), PCM-activity updates (`updateActivity`), and manual pair selection (`selectStereoPair`, unused by UI yet — no UI work was in scope). `AudioEngine` now delegates to it instead of the former private `IOSAudioHardwareRouteObserver` struct, with no change to `AudioEngine`'s own published `audioHardwareRouteState` shape or timing.
- Task 3 (selection rules): added `AudioHardwareRouteState.StereoPair.resolveSelection(availablePairs:remembered:)` — pure, Foundation-only, testable on macOS — implementing "remembered pair if still available, else first available pair, else nil" with no possible crash on empty/malformed input. The adapter remembers the confirmed pair per device UID across `refresh()` calls, so a device that drops out and reconnects restores the previous choice instead of resetting.
- Task 2 (multichannel PCM): added `AudioHardwareRouteState.StereoPair.extractInterleaved(_:channelCount:)` (pure, testable) and `AudioEngine.extractStereoPair(from:pair:)` (handles both interleaved and non-interleaved `AVAudioPCMBuffer` layouts) so the buffer is read for exactly the selected pair's two channel indices before any downmixing. `processAudioBuffer`/`audioPacket(from:selectedStereoPair:)` now extract-then-average that pair to mono for the existing (unchanged) analysis pipeline; when no pair is selected (a genuine single-channel mic) it falls back to the prior full-downmix behavior byte-for-byte, so mic/line-in output is unchanged. A 14-channel RANE-style buffer selecting pair "3/4" now reads hardware indices 2/3 only, rather than averaging all 14 channels together as it did before this change.
- Task 4 (diagnostics): DEBUG-only logging moved into the adapter, firing only when device/transport/sample-rate/channel-topology/selected-pair actually change (never per callback), formatted as the requested "Audio Route Changed" block with Device/Transport/Sample Rate/Channels/Stereo Pairs/Selected fields.
- Task 6 (tests): added default-first-pair, remembered-pair-restored, remembered-pair-fallback, and invalid/mono-metadata-returns-nil-safely cases for `resolveSelection`, plus interleaved-extraction correctness (including the RANE "pair 3/4 → indices 2/3" example from spec) and two malformed-buffer-returns-nil-safely cases for `extractInterleaved`. All 18 tests (10 prior + 8 new) passed on the macOS test host. `AudioEngine.extractStereoPair`'s `AVAudioPCMBuffer`-based glue has no automated coverage — this project has no iOS-hosted XCTest target (`ScratchLabDesktopTests` is macOS-only, `TEST_HOST` = the macOS app), so buffer-level extraction can only be exercised by running the app.
- Verification: `plutil -lint` and `git diff --check` passed. macOS Debug build succeeded; focused `AudioHardwareRouteStateTests` passed 18/18 on the macOS test host; iPhone 17, iPad (A16), and Apple Watch Series 11 (42mm) simulator Debug builds all succeeded with code signing disabled, zero warnings in touched files.
- Remaining limitations (explicit, per phase scope): no live DVS validation was performed; no timecode source was connected; no MIDI implementation exists yet; no turntable/control-vinyl hardware is available so RANE ONE MKII naming/UID/channel topology/sample rate remain unconfirmed until a real USB-C connection is observed; no UI changes were made (device-picker UI for `selectStereoPair` does not exist yet). Nothing was committed.

## 2026-08-22 (iOS MIDI Phase 2 — CoreMIDI transport; uncommitted)
- Starting state: `main` at `06fc8c0`. Four pre-existing working-tree modifications (`ScratchLab/Audio/AudioEngine.swift`, `ScratchLab/DesignSystem/ScratchLabDesignSystem.swift`, `ScratchLab/Views/PracticeModeView.swift`, and `ScratchLabDesktopTests/DVSControlVinylAnalyzerTests.swift`) were preserved and not edited during this MIDI phase.
- Root cause: iOS already compiled the shared controller profiles, learned mappings, registry, parsing, and verification models, but had no CoreMIDI client/input port. The safe channel-message framing implementation was also private to `MacCaptureEngine`, preventing iOS from reusing its malformed-packet, SysEx, and realtime-byte handling.
- Files changed for this phase: `ScratchLab/Models/ControllerInput/MIDIChannelMessageParser.swift` (new); `ScratchLab/Models/ControllerInput/MIDIReadinessState.swift` (new); `ScratchLab/MIDI/iOSMIDIManager.swift` (new); `ScratchLab/ScratchLabApp.swift`; `ScratchLabDesktop/Services/MacCaptureEngine.swift`; `ScratchLabDesktopTests/MIDIPacketBoundsCrashFixTests.swift`; `ScratchLab.xcodeproj/project.pbxproj`; plus `TASKS.md` and this log.
- Architecture: the pure shared parser frames complete MIDI 1.0 channel-voice messages, ignores realtime bytes without disturbing framing, skips SysEx/System Common safely, rejects running status and malformed/truncated data, and converts only the existing registry-supported Note On/Off, CC, and Pitch Bend types into `ParsedMIDIMessage`. `MacCaptureEngine` now calls the shared type with its packet-list extraction and all downstream dispatch unchanged.
- iOS transport: `IOSMIDIManager` uses `MIDIClientCreate`, `MIDIInputPortCreate`, `MIDIGetNumberOfSources`, `MIDIGetSource`, `MIDIPortConnectSource`, matching disconnect/dispose cleanup, and CoreMIDI notifications for source refresh. It connects discovered sources, traverses live `MIDIPacketList` storage using the same safe field-offset approach as macOS, and publishes source identity, latest parsed message, count, and readiness. `RootContainerView` owns it as a `StateObject` and exposes it through the environment; no visible UI or controller action was added.
- Truth/logging: `MIDIReadinessState` is unavailable with no source, deviceConnected from connection evidence alone, and receivingMessages only after a valid supported packet. DEBUG device logs are emitted once per discovered source and message logs are throttled to one per second. Release adds no logs.
- Tests: existing packet-list traversal, multiple-packet, oversized-packet, packet-boundary, SysEx, realtime, malformed-input, two-byte-message, and left/right-channel tests remain green; added shared-model conversion and readiness truth tests. `MIDIPacketBoundsCrashFixTests` passed 15/15 in each of two configured runs.
- Verification: iPhone 17 simulator Debug build succeeded; the built app installed and launched without MIDI sources (PID returned by `simctl`); macOS Debug build succeeded; `plutil -lint` and `git diff --check` passed. The retained App Intents metadata notice is unrelated. DVS code and `AudioEngine.swift` were not edited in this phase.
- Hardware status: MIDI transport is implemented but NOT VERIFIED on physical USB MIDI hardware. DVS remains NOT VERIFIED by this phase; no DVS path changed.

## 2026-08-22 (iOS scratch playback asynchronous audio-session activation; uncommitted)

- Selected task: remove the main-thread `AVAudioSession.setActive(true)` warning from `IOScratchPlaybackEngine` while preserving activation-before-route-inspection ordering.
- Files changed: `ScratchLab/Audio/iOS/IOScratchPlaybackEngine.swift`, `TASKS.md`, and `DEV_LOG.md`.
- Implementation: hot-cue loading now awaits audio-session activation before reading the route and constructing the renderer. iOS 27+ uses `AVAudioSession.activate(options:completionHandler:)`; earlier supported systems run the synchronous compatibility API in a detached task so it cannot block the main actor. Playback arming proceeds only after activation and sample loading finish, with existing failures still reported through the DEBUG diagnostic path.
- Verification: Xcode live diagnostics report no issues; Xcode workspace build succeeded. `scripts/build.sh` passed all 47 capture-pipeline tests, then its desktop Xcode test phase was blocked by sandbox-denied DerivedData/CoreSimulator access (`Operation not permitted`), unrelated to source compilation.

## 2026-08-23 (ScratchLab symbol asset for iOS and macOS; uncommitted)

- Root cause: iOS used a 1024×1024 raster logo with a baked black background while macOS drew an unrelated record-style mark in Canvas. The iOS splash already renders `ScratchLab` as native text below the image, so a wordmark baked into the asset would duplicate the product name.
- Files changed: `ScratchLab/Assets.xcassets/ScratchLabLogo.imageset/Contents.json`; `ScratchLab/Assets.xcassets/ScratchLabLogo.imageset/ScratchLabLogo.svg` (new); `ScratchLab/Assets.xcassets/ScratchLabLogo.imageset/scratchlab-logo.png` (removed); `ScratchLab/ScratchLabApp.swift`; `ScratchLabDesktop/Views/ScratchLabBrandMark.swift`; `ScratchLab.xcodeproj/project.pbxproj`; plus `TASKS.md` and this log.
- Implementation: installed the approved transparent symbol-only SVG (target/performance traces, waveform layers, and turnaround diamond) as the vector-preserving iOS image asset; kept the existing native iOS `Text("ScratchLab")` title and marked the adjacent decorative image hidden from accessibility; replaced the legacy macOS Canvas drawing with the same symbol; and added that one SVG to the macOS resource bundle rather than maintaining a second copy.
- App-icon audit: the existing iOS/iPadOS `AppIcon.appiconset` and macOS `AppIcon.icns` already contain the approved symbol-only mark on the required opaque dark background. They contain no `ScratchLab` wordmark and did not need replacement; iOS/macOS supply the app-name label in system UI.
- Verification: `plutil -lint` and `git diff --check` passed; macOS Debug and generic iOS Simulator Debug builds succeeded with code signing disabled; `assetutil` confirms the iOS catalog contains `ScratchLabLogo.svg`; the built macOS app contains `ScratchLabLogo.svg`, and AppKit loads the SVG as an image; capture-pipeline fixtures passed 47/47. The required broad `scripts/build.sh` desktop plan exited 65 on the repository's existing shared-state, source-contract, missing-fixture, demo-audio, notation, and scratch-playback failures; no logo-related build or resource failure was observed.
- Remaining risk/scope: this installs the static production identity only. The reusable Figma motion sequence has not been ported into SwiftUI, and no splash animation behavior was added in this change. Final visual review on physical iPhone/iPad and Mac displays remains advisable.

## 2026-08-23 (V3.2 Phase 2 iOS Capture design-system adoption; uncommitted)

- Root cause: `CompanionCameraView` implemented its own capture cards, fields, pickers, status pills, banners, metrics, and buttons using literal colours, fonts, spacing, and radii even though the shared V3.2 primitives already existed. `Surface.raised` also named `#151E2B` while the approved raised value `#101826` lived under `Surface.elevated`, making future substitutions ambiguous.
- Files changed: `ScratchLab/DesignSystem/ScratchLabDesignSystem.swift`; `ScratchLab/Views/CompanionCameraView.swift`; plus `TASKS.md` and this log. Pre-existing camera layout/wiring edits in `CompanionCameraView`, audio/device wording edits in the design-system file, and all other dirty files were preserved.
- Shared-system implementation: added reusable 2/3/10-point component rhythm, common 8/10/12/18/22-point radii, 18-point section-title, 24-point key-metric, 48-point large-score, motion accent, subtle/control/scrim/overlay surfaces, the approved three-stop application background, and a semantic warning button. `Surface.raised` now truthfully resolves to `#101826`; `Surface.elevated` is its compatibility alias; the former `#151E2B` tier is explicitly `highest`, with existing card and disabled-button behavior preserved through aliases. `StatusBadge` now uses shared typography and supports the title-free pill form needed by compact readiness displays.
- Capture adoption: the root background, `CaptureScreen`, `CaptureCard`, text/picker fields, selection rows/sheets, status cards, secondary buttons, metrics, warning/transient banners, readiness pills, session setup, system check, camera/audio/motion/calibration setup, recording controls, Take Review, and Session Complete now consume the shared cards, buttons, badges, chips, typography, spacing, borders, surfaces, radii, and semantic colours. Ready/confirmed stays green, attention-required stays amber, normal primary stays cyan, recording/destructive stays red, and unavailable controls use the shared disabled state.
- Behavior preserved: no capture, camera, audio, motion, session, metadata, validation, persistence, export, navigation, readiness, keyboard-shortcut, or adaptive iPhone/iPad decision logic changed. The existing portrait/landscape camera layout work was retained. Accessibility combination/labels and SwiftUI disabled states remain intact.
- Verification: generic iOS Simulator Debug and macOS Debug builds succeeded with code signing disabled. Focused `ScratchLabDesignTokensTests` plus the three guided-capture layout/navigation/source-contract tests passed 25/25. `git diff --check` passed. `scripts/build.sh` passed all 47 capture-pipeline fixtures, then exited 65 in its broad desktop test plan on the repository's existing shared-state, source-contract, missing-fixture, demo-audio, notation, MIDI, and scratch-playback failure baseline. No design-system or focused guided-capture test failed.
- Remaining scope: no Figma file or exported V3.2 frames were available, so this is documented-token/component parity, not a pixel-perfect claim. Practice, macOS Analyzer, Watch, coaching, DVS, diagnostic screens, underlying calibration-zone role colours, and full physical-device visual review remain deferred.

## 2026-08-23 (V3.2 Phase 3 iOS Practice/Results design-system adoption; uncommitted)

- Root cause: `PracticeModeView` and its scoped presentation family (`AccuracyBurstView`, `PracticeReadyOverlay`, `SessionSetupOverlay`, `PracticeBeatControlsCard`, `PauseOverlayView`, `ResultsOverlayView`, `PracticeResultNotationSection`, `PerformanceTraceUnavailableCard`, `PracticeReviewCard`, plus the `AudioLevelIndicator` HUD helper) still built cards, chips, HUD pills, status colours, and buttons from literal `Color(hex:)`/`.white.opacity()`/`.black.opacity()`/`Color(red:green:blue:)` values and one-off font sizes, even though the shared V3.2 primitives Phase 2 landed in `ScratchLabDesignSystem.swift` already covered every role in use. The four full-screen Practice overlays (`PracticeReadyOverlay`/`SessionSetupOverlay`/`PauseOverlayView`/`ResultsOverlayView`) also used three different, inconsistent literal scrim opacities (0.8/0.8/0.9/0.9) for the same "modal dark backdrop" role.
- Files changed: `ScratchLab/Views/PracticeModeView.swift`; plus `TASKS.md` and this log. No changes to `ScratchLabDesignSystem.swift` — every role needed already existed, so nothing new was added. Capture (`CompanionCameraView.swift`), the notation renderer, comparison models, and every pre-existing dirty file (logo assets, `AudioEngine.swift`, `ScratchLabApp.swift`, desktop test files) were preserved untouched.
- Implementation: literal colours became `ScratchLabDesign.Sem.*` (success/warning/danger/accent/muted/textPrimary/textSecondary/textTertiary/textOnAccent), `Surface.*` (`scrim` for HUD-chip/camera-overlay backings, `overlay` for the four full-screen modal scrims, `controlFill`/`disabledFill`/`subtleFill` for selector fills), and `Border.default`; literal corner radii became `Radius.control`/`.compactPanel`/`.panel`; ad hoc `VStack + padding + translucent background + stroke overlay` card patterns (MIDI-mapping card, open-practice card, result metric cards, the Review card, the performance-trace-unavailable card) became `.scratchLabCard(.standard/.selected/.warning)`; the microphone-rationale and Session Setup primary actions became `.scratchLabPrimaryButton(fillsWidth: true)`; large accuracy score uses `Typo.largeScore`, the session countdown uses `Typo.keyMetric` (with `.monospacedDigit()` kept for stable digit width), section headings/eyebrows use `Typo.cardHeading`/`.sectionLabel`/`.technical`/`.caption` where they matched exactly. Per the task's explicit status-colour rule, the notation instructional line's "target/watch" indicator dot was corrected from an unmatched one-off blue to `ScratchLabDesign.Notation.targetTrace` (the approved target-reference colour) instead of being folded into `Sem.accent`; its "copy/your-turn" indicator now uses `Sem.warning` (its literal RGB was already `#F59E0B` in disguise). `micStatusColor` (used by the mic HUD chip and `PracticeReadyOverlay`) now derives from the pre-existing `micStatusVariant` instead of duplicating the same four-way switch as raw hex, removing a maintenance seam rather than adding one.
- Behavior preserved: no Practice gameplay, state transition, timer, scoring, comparison, progression, audio playback, demo playback, coaching logic, camera behavior, persistence, export, navigation, or analytics changed. `CameraPreviewView`/`CameraPreviewLayer`, `GuidedCutCueLayer`, `ScratchCoachCard`, `CaptureHelpView`, and the `#if DEBUG` diagnostic recorder overlay were left untouched (outside the listed component scope). No second design system was introduced.
- Verification: generic iOS Simulator Debug build succeeded (BUILD SUCCEEDED, zero errors); macOS Debug build succeeded; macOS `build-for-testing` succeeded. `git diff --check` passed. `scripts/build.sh all` passed all 47 capture-pipeline fixtures, then its desktop XCTest plan exited 65 (2,982 tests / 47 skipped / 100 failures) — the same pre-existing shared-state, source-contract, missing-fixture, demo-audio, notation, MIDI, and scratch-playback baseline documented under Phase 2 (count drift from Phase 2's 2,953/47/101 reflects intervening unrelated work, not this diff). `ScratchLabDesignTokensTests`, the Practice-notation source-contract tests (`GuidedCutCueLayerTests.testGuidedCueLayerExistsInPracticeModeView`, `PracticeAssistModePickerTests.testAssistModePickerCopyIsPresentInPracticeModeView`, `PracticeTargetNotationChartTests.testTargetNotationChartIsRenderedInPracticeModeView`), and every design-token test passed; none of the 100 broad-suite failures reference `PracticeModeView`, `ScratchLabDesignSystem`, or any migrated struct.
- Remaining risks / deferred: no Figma V3.2 frames were available for Practice, so this is documented-token/component parity, not pixel-perfect claim, per the task's design-reference qualification. `PracticeModeView`'s own live-session HUD chrome (top bar, status strip, notation-lane HUD chips) is now token-driven but keeps its existing translucent-over-camera layout rather than being restructured into a new component. Full physical-device visual review of Practice/Results was not performed.

## 2026-08-23 (V3.2 Phase 4 macOS Analyzer — Practice/Capture/Review design-system adoption; uncommitted)

- Root cause: `ScratchLabDesktop/Views/MacAnalyzerView.swift` (12,218 lines; a single `TabView` hosting Practice/Capture/Review/Advanced) was already substantially token-adopted before this phase — extensive pre-existing `ScratchLabDesign.*` usage and shared-component reuse (`InputReadinessRow`, `RecoveryCard`, `DVSSignalHealthCard`, `HardwareProfileCard`, `PerformerMonitorConnectionCard`, `StatusBadge`, `Chip`, `.scratchLabCard`, `.scratchLabPrimaryButton`/`.scratchLabSecondaryButton`) — but the Practice/Capture/Review surfaces (the in-scope tabs) still carried scattered literal `.white`/`.black`/`Color(nsColor: .system*)`/`Color(nsColor: .controlBackgroundColor)`/`Color(white:)`/`Color(red:green:blue:)` values, a handful of un-tokenized card backgrounds that matched the shared `.scratchLabCard()` pattern exactly, and one unmatched literal blue used for the "MY PERFORMANCE"/"target" trace-role labels instead of the approved `Notation.performanceTrace`/`.targetTrace` tokens.
- Files changed: `ScratchLabDesktop/Views/MacAnalyzerView.swift`; `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift` (one-line source-anchor fix, see below); plus `TASKS.md` and this log. No changes to `ScratchLabDesignSystem.swift` — every role needed already existed. Advanced tab (`advancedWorkspace`/`advancedSidebar` and its Overview/Tools/Diagnostics/Audio-DVS/Camera-Deck/MIDI-Fader/Monitor/`#if DEBUG` sections), the Mixer & Hot-Cue Mapping / MIDI Learn checkpoint section, `MacPerformerMonitorView` and its Performer-Monitor-only leaf views (`PerformerNotationDisplayView`, `AudioRoutingOptionRow`, `RoutineSessionRow`, `RoutineSessionCard`-family), and every `#if DEBUG` diagnostic card (`DebugTimecodeCaptureCard`, `platterTimelineDebugCard`, `cxlCaptureCard`) were left untouched, per this phase's explicit deferral list. Every other pre-existing dirty file was preserved.
- Implementation: the top-level `TabView`'s root background moved from `Color(nsColor: .windowBackgroundColor)` to `ScratchLabDesign.Surface.applicationBackground` (the approved three-stop gradient). `RoutineSessionErrorBanner`'s warning icon moved off raw `Color(nsColor: .systemOrange)` onto `Sem.warning`. Practice's stage header, coach card, run card, scored-attempt card/body/result view, beat-trainer card, progress tiles, audio card, feedback card, and its sidebar's demo-mode card/metric all moved off literal `.white`/`Color(nsColor: .system*)` onto `Sem.textPrimary`/`.textSecondary`/`.textTertiary`/`.success`/`.warning`/`.danger`/`.textOnAccent`, with a handful of `.frame+padding+background(Color(nsColor:.controlBackgroundColor))+cornerRadius(18)` card shells collapsed onto `.scratchLabCard()`. Capture's stage header, empty-state camera-stage card, disclosure row, and the `captureStageBadgeColor`/`captureRecordCard`/`captureInputStatusCard`/`captureLatestTakeCard` metrics/status colours received the same treatment; `captureInputStatusCard`'s closing chrome (`frame+padding(20)+background(controlBackgroundColor)+cornerRadius(18)`) also collapsed onto `.scratchLabCard()`. Review's header/sidebar/stage/target-notation/target-vs-performed-comparison/captured-notation/overlay-diff/audio-onset-preview/footer-metric cards and their diagnostics-observing leaf views (`ReviewLiveDiagnosticsOnsetCard`, `ReviewAudioOnsetCardRow`, `OnsetMarkTimelineStrip`, `ReviewOverlayPlayableSurface`, `LiveNotationOverlayPlayable`) received the same colour/background token treatment; the "TARGET"/"MY PERFORMANCE" trace labels in the target-vs-performed comparison card and the scored-attempt result view moved from an unmatched `Color(white:)`/`Color(red:green:blue:)` literal onto `Notation.targetTrace`/`.performanceTrace`, matching the task's explicit "Target/reference: target-trace colour, Motion/performance: motion/performance role" requirement. `CompanionStageContent` (Capture's dual-cam companion feed empty/connected states) received the same treatment. Two shared helpers used by both in-scope and deferred surfaces — `routineSessionButton`/`RoutineSessionRow` (selected-row `.white`→`Sem.textOnAccent`, unselected background `Color(nsColor:.windowBackgroundColor)`→`Surface.surface`) and `testLabMetricBadge` (container background only) — were migrated once at their shared definition rather than duplicated per call site; their in-scope call sites (Practice/Capture/Review cards) also had their literal `.green` status arguments replaced with `Sem.success` at each in-scope call site (deferred call sites in Advanced-only cards were left untouched).
- Regression found and fixed: the first `scripts/build.sh all` run surfaced one new failure — `CaptureReliabilityPhase1CoreTests.testMacAdvancedScreenContainsTechnicalToolsOutsidePrimaryNavigation` — caused by `sourceSlice(in:from:through:)` using the literal string `.background(Color(nsColor: .windowBackgroundColor))` as a text-boundary anchor to slice out the `TabView` block and assert it contains no legacy tab labels ("Notation Lab", "Test Lab", "Advanced Capture Details"). That literal no longer existed after the application-background migration. Fixed by updating only the anchor string to `.background(ScratchLabDesign.Surface.applicationBackground)` in `CaptureReliabilityPhase1Tests.swift`; the test's actual assertions (which legacy labels must be absent) were not touched or weakened.
- Behavior preserved: no analysis algorithm, capture/recording behavior, audio routing/device selection, MIDI/DVS behavior, session loading/saving/metadata/persistence/export, playback/transport timing, readiness evaluation, notation generation/rendering, trace/waveform calculation, navigation/window management, keyboard shortcut, or accessibility behavior changed. Native macOS form controls (`Picker`, `Toggle`, `TextField`, `TextEditor`, small inline `.buttonStyle(.bordered)`/`.borderedProminent` actions) were deliberately left as native controls rather than reskinned with the mobile-oriented `scratchLab*Button` family, matching this file's own pre-existing convention (already visible in `practiceStageHeader`/`captureStageHeader`'s `.scratchLabSecondaryButton()` header actions vs. inline `Learn`/`Cancel`/`Refresh` bordered buttons) of reserving that family for headline actions only.
- Verification: macOS Debug build succeeded; generic iOS Simulator Debug build succeeded (confirms nothing outside `ScratchLabDesktop` broke); macOS `build-for-testing` succeeded; `plutil -lint` on `project.pbxproj` passed (unchanged, no membership edits — this phase added no new files); `git diff --check` passed. `scripts/build.sh all` was run twice: the first run passed 47/47 capture-pipeline fixtures and surfaced the one regression above (2,982 tests / 47 skipped / 101 failures, exit 65); after the one-line anchor fix, the second run again passed 47/47 fixtures and reproduced the Phase 3 baseline exactly — **2,982 tests / 47 skipped / 100 failures, exit 65** — with the full failing-test-name set diffed against the Phase 3 log and confirmed byte-identical (no new failures, nothing newly fixed).
- Remaining risks / deferred: no approved macOS Figma V3.2 frames were available, so this is documented-token/component parity with the shared V3.2 system, not a pixel-perfect claim, per the task's design-reference qualification. Advanced tab, Performer Monitor, DVS configuration, MIDI configuration, and all `#if DEBUG` diagnostic panels remain fully unmigrated by design — deferred to a future phase. Full physical-device/window-resizing visual review was not performed beyond simulator/Debug builds.

## 2026-08-23 (V3.2 Phase 5 macOS Performer Monitor design-system adoption; uncommitted)

- Root cause: Performer Monitor has no dedicated source file — its entry point is `WindowGroup("Performer Monitor", id: ScratchLabDesktopWindowID.performerMonitor)` in `ScratchLabDesktop/ScratchLabDesktopApp.swift` (a separate window scene, min 900×620, hosting `MacPerformerMonitorView()`), and `MacPerformerMonitorView` itself lives inside `ScratchLabDesktop/Views/MacAnalyzerView.swift` alongside its one exclusive supporting view, `PerformerNotationDisplayView`, and the non-visual `PerformerMonitorWindowAccessor` window-binding helper. Both `MacPerformerMonitorView` and `PerformerNotationDisplayView` still built their root background, header, mode content (Cue/Deck/Notation), metric cards, and empty state from literal `Color.black`/`Color(nsColor: .windowBackgroundColor)`/`.white.opacity()`/`Color(white:)` instead of the shared V3.2 tokens that Phase 4 had already applied to the rest of the same file.
- Files changed: `ScratchLabDesktop/Views/MacAnalyzerView.swift` only; plus `TASKS.md` and this log. No changes to `ScratchLabDesignSystem.swift`, `ScratchLabDesktopApp.swift`, or any other file. Every pre-existing dirty file was preserved.
- Ownership verified by call-site tracing before editing, not assumed: `PerformerNotationDisplayView` is used only inside `MacPerformerMonitorView.notationMonitorContent` (confirmed exclusive). `AudioRoutingOptionRow` and the `SeratoWindowMover` class are used only inside the Advanced tab's `seratoScreenCard`/`audioCard` (confirmed exclusive to the deferred surface) and were left untouched. `RoutineSessionErrorBanner`, `CompanionStageContent`, `RoutineSessionRow`, and `LiveNotationOverlayPlayable` sit physically nearby in the file but were already migrated in Phase 4 for their actual owning (non-Performer-Monitor) call sites, so nothing further was needed there.
- Implementation: the window root background moved from a two-stop `Color.black`/`Color(nsColor: .windowBackgroundColor)` gradient to `ScratchLabDesign.Surface.applicationBackground`. `performerHeader`'s title/subtitle/"Advanced connection" disclosure/move-status text moved onto `Sem.textPrimary`/`.textSecondary`; the connection-status line keeps `Sem.info` only when a peer is actually connected and now uses `Sem.textSecondary` (never a success colour) while searching — matching the explicit "connected but not ready must not read as success" requirement. `cueMonitorContent`'s next-cue card background became `Surface.subtleFill` (an exact-opacity match for the prior `white.opacity(0.06)`); its title/detail text moved onto `Sem.textSecondary`/`Sem.textPrimary.opacity(0.84)`, leaving `captureEngine.handMotionState.color` untouched since it is already a real, engine-derived semantic colour. `performerMetric` (the Cue-mode stat cards) collapsed its manual `frame+padding+background(white.opacity(0.05))+cornerRadius(18)` shell onto `.scratchLabCard()` and its value onto `Typo.keyMetric` with `.monospacedDigit()` added so a live-updating count/percentage doesn't jitter width. `performerMetricBadge` (the badge overlaid on the live Deck View camera preview) moved its background from `Color.black.opacity(0.6)` onto `Surface.scrim` — the same token Phase 2–4 established for chip backgrounds rendered over camera video. `deckMonitorContent`/`notationMonitorContent` header text and `PerformerNotationDisplayView`'s empty-state icon/title/subtitle and transport-row elapsed-time label all moved onto `Sem.textPrimary`/`.textSecondary`/`.textTertiary`; its empty-state background moved from `Color(white: 0.10)` onto `Surface.subtleFill`. The Deck View camera-preview darkening overlay (`Color.black.opacity(0.08)`) and the `.ultraThinMaterial`/`.regularMaterial` system materials elsewhere were deliberately left as literals — no existing semantic token represents a one-off camera-legibility wash, and system materials are already the correct native choice for translucent chrome.
- Behavior preserved: no live monitoring/capture behavior, camera/audio/MIDI/DVS wiring, connection/readiness evaluation, session/performer metadata, analysis/metric calculation, notation/trace/waveform generation, playback/transport timing, persistence/export, window management, navigation, keyboard shortcut, or accessibility behavior changed. `moveWindowToPreferredSecondaryDisplay`/`moveWindowToMainDisplay`/`bindWindow`/`updateMoveStatus` (all window-management logic) were not touched. Native macOS `Picker` (mode segmented control), `DisclosureGroup`, and bordered/borderedProminent buttons were kept native, matching the established Phase 4 convention.
- Verification: macOS Debug build succeeded; generic iOS Simulator Debug build succeeded; `plutil -lint` on `project.pbxproj` passed (unchanged, no membership edits — no new files); `git diff --check` passed. `scripts/build.sh all` passed 47/47 capture-pipeline fixtures and, on the first run, reproduced the Phase 4 baseline exactly — **2,982 tests / 47 skipped / 100 failures, exit 65** — with the full failing-test-name set diffed against the Phase 4 log and confirmed byte-identical (no new failures; no source-anchor test needed updating this phase, unlike Phase 4).
- Remaining risks / deferred: no approved Performer Monitor Figma frames were available, so this is documented-token/shared-component parity with the V3.2 system, not a pixel-perfect claim. Advanced tab, DVS configuration, MIDI Learn/Mixer mapping, DEBUG panels, and the underlying camera/notation/waveform renderers remain fully unmigrated by design, deferred to future phases. Compact-vs-expanded window-size and physical-display visual review was not performed beyond Debug/simulator builds.

## 2026-08-23 (V3.2 Phase 6 macOS Advanced-tab design-system adoption; uncommitted)

- Root cause: the Advanced tab (`advancedWorkspace`/`advancedSidebar`/`advancedSelectedSectionContent`'s `.overview`/`.audio`/`.cameraDeck`/`.midiFader`/`.monitor` cases) is entirely hosted inside `ScratchLabDesktop/Views/MacAnalyzerView.swift`, deferred by every prior phase. Most of it was already token-adopted (a pre-migration scan of ~1,900 Release-visible lines found only ~23 literal-color hits, versus hundreds in earlier phases before their own migration), but a consistent residue remained: several cards still built their outer shell from `Color(nsColor: .controlBackgroundColor)` + a literal `cornerRadius: 18` instead of `.scratchLabCard()`, and status/warning/readiness text used raw `Color(nsColor: .system*)`/bare `.green` instead of `Sem.success`/`.warning`/`.danger`.
- Files changed: `ScratchLabDesktop/Views/MacAnalyzerView.swift` only; plus `TASKS.md` and this log. No changes to `ScratchLabDesignSystem.swift`. Every pre-existing dirty file was preserved.
- Ownership traced by call-site inspection before editing (not assumed): `advancedSelectedSectionContent`'s switch was read in full first, confirming `.captureDetails` and `.timecodeInput` are entirely `#if DEBUG`-gated (both cases, not just parts of them) and so out of scope wholesale, while `.overview`/`.audio`/`.cameraDeck`/`.midiFader`/`.monitor` are fully Release-visible. `AudioRoutingOptionRow` and `SeratoWindowMover` were confirmed used exclusively by `audioCard`/`seratoScreenCard` (both Release-visible, `.audio` case) and were migrated; `routineRecordingCard`, `RoutineTempoEditor`, `routineRecordingButton`, and `cxlCaptureCard` were confirmed used exclusively by the DEBUG-gated `.captureDetails` case and left untouched.
- Implementation: `stageModeCard`, `seratoScreenCard`, `audioCard`, `cameraCard`, `deckCalibrationCard`, `handMotionCard`, `audioOnsetDiagnosticsCard`, `companionCard`, and `workflowCard` each had their manual `.frame(maxWidth: .infinity, alignment: .leading).padding(20).background(Color(nsColor: .controlBackgroundColor), in: RoundedRectangle(cornerRadius: 18, style: .continuous))` shell collapsed onto `.scratchLabCard()` — an exact-pattern match already established by every other migrated card in this file. Status/warning colour literals moved onto semantic tokens at each in-scope call site: `artifactStatusBadge`'s ready/recording-or-finalizing/failed tri-state → `Sem.success`/`.warning`/`.danger`; `audioRoutingStatusColor`'s mic-path-detected-vs-routed state → `Sem.warning`/`.success`; `AudioRoutingOptionRow`'s "ACTIVE" indicator and icon tint → `Sem.success`; `midiMonitorCard`'s Release-visible "State"/"Events received" badges, the MIDI-mapping-error and calibration-error warning labels (`mixerAndHotCueMappingSection`, `faderCurveRow`), `deckCalibrationCard`'s recording-status and layout-locked lines, and `handMotionCard`/`companionCard`'s confidence/source metric badges all moved off raw `Color(nsColor: .system*)`/bare `.green` onto the matching `Sem.*` role. `SeratoWindowMover.statusColor`'s "moved"/"showing on" case moved off bare `.green` onto `Sem.success`; its neutral `.secondary`/`.primary` branches were left as the adaptive system roles they already were (matching the exact precedent set for `ReviewCapturedSource` in Phase 4 — only the literal-hex branch is a token-adoption target, not every already-semantic branch).
- Correction during this phase: an initial `replace_all` edit mapping `Color(nsColor: .systemOrange)` → `Sem.warning` matched an occurrence inside `routineRecordingCard` (a DEBUG-only, `.captureDetails`-exclusive card) that was not an intended target. Caught by re-diffing the file before building; reverted that one instance back to the original literal so the deferred surface's source is byte-for-byte unchanged from before this phase.
- Behavior preserved: no audio routing/device-selection, MIDI discovery/mapping/learning/mixer, DVS/timecode decoding/calibration, camera discovery/capture, readiness evaluation, connection state, analysis calculation, session metadata, persistence/export, feature-flag, Release/DEBUG visibility, navigation/window-management, keyboard-shortcut, or accessibility behavior changed. Every native `Picker`, `Toggle`, `DisclosureGroup`, and bordered/borderedProminent button was left native, matching the established Phase 4 convention. No DEBUG-only functionality was exposed in Release, and no Release functionality was hidden.
- Verification: macOS Debug build succeeded; generic iOS Simulator Debug build succeeded; `plutil -lint` on `project.pbxproj` passed (unchanged, no membership edits); `git diff --check` passed. `scripts/build.sh all` passed 47/47 capture-pipeline fixtures and, on the first run, reproduced the Phase 5 baseline exactly — **2,982 tests / 47 skipped / 100 failures, exit 65** — with the full failing-test-name set diffed against the Phase 5 log and confirmed byte-identical (no new failures; no source-anchor test needed updating this phase).
- Remaining risks / deferred: no approved Advanced-tab Figma frames were available, so this is documented-token/shared-component parity with the V3.2 system, not a pixel-perfect claim. `#if DEBUG`-only panels (`platterTimelineDebugCard`, `DebugTimecodeCaptureCard`, `routineRecordingCard`/`RoutineTempoEditor`, `cxlCaptureCard`, the DEBUG halves of `midiMonitorCard`/`performanceDiagnosticsCard`), specialized DVS/timecode/MIDI-event visualizers (`DVSControlVinylPanel`, `TimecodeControlCard`), and every underlying renderer remain fully unmigrated by design, deferred to future phases.

## 2026-08-23 (V3.2 Phase 7 iOS Home design-system adoption; uncommitted)

- Root cause: the iOS Home screen (`MainMenuView` in `ScratchLab/Views/MainMenuView.swift`, reached from `ContentView` in `ScratchLab/ScratchLabApp.swift` via `NavigationStack { MainMenuView() }`) and the app root itself still built their background gradient, a layout divider, and the header's profile/settings buttons from literal `Color(hex:)`/`Color.white.opacity()`/`cornerRadius: 8` values instead of the shared V3.2 tokens already in use across Capture, Practice, and every macOS Analyzer surface migrated in Phases 2–6.
- Files changed: `ScratchLab/ScratchLabApp.swift`, `ScratchLab/Views/MainMenuView.swift`; plus `TASKS.md` and this log. No changes to `ScratchLabDesignSystem.swift` — every role needed already existed. Every pre-existing dirty file was preserved.
- Ownership traced by call-site inspection before editing, not assumed: `MainMenuView` (lines 9–447) is the actual Home struct. `BackgroundView` (lines 1286–1300) is a shared gradient helper used by Home and by four explicitly-deferred external destination files (`LevelSelectView.swift`, `CoachPreviewView.swift`, `WatchCaptureHubView.swift`, `VirtualPlatterPrototypeView.swift`) plus several in-file destination placeholders — migrated once at its single definition, which only swaps a literal gradient for the identical token gradient and cannot change any deferred screen's visual output. `AdaptiveSidebarView`/`HomeInfoCard` (1176–1237) are Home-owned and were found already fully token-clean. `MenuButton` (1238–1283) was confirmed via grep to be used exclusively by the deferred `AdvancedHubView`, not Home, and was left untouched. `practiceEntryCard`/`captureEntryCard`/`reviewEntryCard`/`advancedEntryCard`/`recentResultDetail`/`deviceStatusDetail` (~161–408) were found already fully migrated (`StatusBadge`, `.scratchLabCard(.lessonHero/.standard)`, `Typo.*`, `Sem.*`, with doc comments already referencing V3.2 Figma node numbers) by earlier work outside this numbered phase sequence — no edits were needed or made there.
- Implementation: `ContentView`'s app-root background and `BackgroundView`'s Home-root background (an identical literal three-stop `Color(hex: "05070B"/"0B1018"/"101826")` `LinearGradient` in both places) both moved to `ScratchLabDesign.Surface.applicationBackground`, the same approved three-stop gradient token used at every other root-background site in the app. `MainMenuView`'s regular-landscape layout divider moved from `Color.white.opacity(0.08)` to `ScratchLabDesign.Border.default`. The header's profile button (avatar/name pill) and settings button (gear icon) moved from literal `.white` text, `Color.white.opacity(0.06)` fill, `Color.white.opacity(0.08)` stroke, and a bare `cornerRadius: 8` to `Sem.textPrimary`, `Surface.subtleFill`, `Surface.controlFill`, and `Radius.control`. A grep sweep for `.white.opacity(`/`Color.white`/`Color.black`/`Color(hex:`/`Color(nsColor:` across lines 1–447, 1162–1237, and 1284–1300 confirmed zero remaining literal-style hits in Home-owned code after these edits.
- Regression investigation (false alarm, not caused by this phase): the first full `scripts/build.sh all` run showed 153 failures, 53 above the 100-failure Phase 6 baseline, entirely concentrated in `ScratchAnalysis*`/`ScratchNotation*`/`ScratchTimeline*` suites with no relation to either touched SwiftUI file. The actual error was `NSCocoaErrorDomain Code=260 "scratch_timeline_cc6_baby_fixture.json...no such file"` — a missing test-bundle resource. The fixture exists in source and is registered in `project.pbxproj`; a fresh `build-for-testing` run succeeded and placed the fixture back in the `.xctest` bundle's Resources directory with a current timestamp, and the two most-implicated suites (`ScratchAnalysisCheckedInFixtureTests`, `ScratchTimelineProvenanceTests`) then passed 12/12 in isolation. A second full-suite run confirmed the transient diagnosis: it reproduced the Phase 6 baseline exactly, with the full failing-test-name set diffed against the Phase 6 log and confirmed byte-identical (57 unique names, no new failures, nothing newly fixed).
- Behavior preserved: no navigation route, destination selection, Capture/Practice entry behavior, recent-session ordering/selection/persistence, session creation/metadata, deep link, feature flag, onboarding state, app lifecycle, export, camera/audio/MIDI/DVS behavior, iPhone/iPad adaptive navigation, or accessibility label/identifier changed. No product feature was added, removed, renamed, enabled, or disabled; no unavailable destination was made to appear available. `QuickStartView`/onboarding (presented from `ContentView`, not `MainMenuView`) was judged out of scope as not literally the Home screen and was left untouched, alongside every destination screen Home merely links to (`AdvancedHubView`, `DemoModeView`, `ProfileView`, `SettingsView`, `IPadPerformerMonitorView`, `LevelSelectView`, `CoachPreviewView`, `WatchCaptureHubView`, `VirtualPlatterPrototypeView`), all DEBUG diagnostics, app icons, and splash/logo animation.
- Verification: macOS Debug build succeeded; generic iOS Simulator Debug build succeeded; macOS `build-for-testing` succeeded; `plutil -lint` on `project.pbxproj` passed (unchanged, no membership edits — this phase added no new files); `git diff --check` passed. `scripts/build.sh all` was run twice: the first run passed 47/47 capture-pipeline fixtures and surfaced the transient 153-failure spike investigated and resolved above; the second run again passed 47/47 fixtures and reproduced the Phase 6 baseline exactly — **2,982 tests / 47 skipped / 100 failures, exit 65** — with the full failing-test-name set (57 unique names) diffed against the Phase 6 log and confirmed byte-identical.
- Remaining risks / deferred: no approved Home-screen Figma V3.2 frames were available, so this is documented-token/shared-component parity with the V3.2 system, not a pixel-perfect claim. Every Home-linked destination screen, `QuickStartView`/onboarding, DEBUG diagnostics, app icons, and splash/logo animation remain fully unmigrated by design, deferred to future phases. Full physical-device visual review was not performed beyond simulator/Debug builds.

## 2026-08-23 (V3.2 Phase 8 iOS level/exercise-selection and progression design-system adoption; uncommitted)

- Root cause: `LevelSelectView` (`ScratchLab/Views/LevelSelectView.swift`, presented from `AdvancedHubView` in `MainMenuView.swift`) — the app's only level/exercise-selection screen — still built its header, section eyebrow, both scratch-option cards, status/goal pills, and the combo-challenge card from literal `Color(hex:)`/`.white.opacity()`/one-off font sizes/manual `LinearGradient` buttons/`cornerRadius(999)` pills instead of the shared V3.2 primitives already adopted everywhere else in the app. Two of its badge colors were also semantically wrong: the combo "Status" `StatusBadge` was hard-coded to `.warning` regardless of whether the challenge was actually cleared, and the Baby Scratch metric badges used `.warning` for a plain accuracy read-out and `.success` for a plain attempt count.
- Files changed: `ScratchLab/Views/LevelSelectView.swift`; plus `TASKS.md` and this log. No changes to `ScratchLabDesignSystem.swift` — every role needed already existed. Every pre-existing dirty file was preserved.
- Scope located by tracing, not assuming: a repo-wide search for level/exercise-selection, progression, and coaching views found `LevelSelectView.swift` (356 lines) as the only in-scope surface — there is no separate locked/available/completed level grid anywhere in the app; `ProgressManager`'s `isLevelUnlocked`/`isScratchUnlocked`/`LevelProgress` plumbing exists with no dedicated list UI beyond this screen's two scratch cards and combo card. `CoachPreviewView` (an AR coaching-preview destination reached from `AdvancedHubView`, unrelated to level selection) and `ScratchCoachViews.swift` (the shared coach-rig component) were confirmed via call-site grep to be used only by `PracticeModeView` (Practice gameplay internals, deferred) and `MainMenuView`'s `DemoModeView` (an unrelated demo screen, deferred) — both are the "general-purpose coaching engines and unrelated coaching screens" the task explicitly keeps deferred, and neither was touched.
- Implementation: the toolbar back-button color moved off `Color(hex: "FFD700")` onto `Sem.accent`. The header title/subtitle and section eyebrow moved onto `Typo.pageTitle`/`.pageSubtitle`/`.pageEyebrow` with `Sem.textPrimary`/`.textSecondary`/`.textTertiary`. Both card containers (`practiceScratchCard`, `comboCard`) collapsed their manual `.padding(20).background(Color.white.opacity(0.08)).overlay(RoundedRectangle(16).stroke(...)).cornerRadius(16)` shell onto `.scratchLabCard()` — `.selected` (elevated background + accent border) for the Baby Scratch card, since it is the emphasized/primary tracked drill, `.standard` for Chirp Flare and the combo card. Three different literal CTA-button gradients — gold/orange for "Start Combo Challenge", green for "Start Baby Scratch", blue for "Start Chirp Flare" — collapsed onto the single shared `.scratchLabPrimaryButton()` style, since all three are the same semantic role (the primary action of their own card), not three distinct states; the manual `.foregroundColor(.black/.white)` overrides on those buttons' labels were removed so the shared style's `Sem.textOnAccent` label color applies. The "FOUNDATION"/"COACH" and "LIVE"/"CLEARED" pills moved off their literal gold/slate `Color(hex:)` fills onto `Typo.statusPill` text on `Sem.accent`/`Surface.subtleFill` (FOUNDATION/COACH) and `Sem.success`/`Surface.subtleFill` (CLEARED/LIVE) fills.
- Semantic corrections made (status styling now reflects the model, per the task's explicit requirement, not a behavior change — the underlying `comboCompleted`/`bestAccuracy`/`practiceCount` values are unchanged): the combo card's "Status" `StatusBadge` variant was hard-coded to `.warning` in all cases even though its own icon already switched to a checkmark on completion — now `comboCompleted == true ? .success : .info`, matching "Completed: success" / "available but not completed: neutral or informational." The "LIVE"/"CLEARED" pill got the identical fix (solid success fill only when actually cleared). The Baby Scratch metric badges — "Best estimate" (`.warning`) and "Attempts" (`.success`) — were corrected to `.info`: a bare accuracy percentage or attempt count doesn't itself constitute a warning needing attention or a completion worth celebrating, per "do not imply completion, mastery, readiness, or unlocking unless the existing model reports it."
- Behavior preserved: no unlock rule, level ordering, progression calculation, completion threshold, scoring, exercise eligibility, coaching recommendation/timing, practice configuration, audio/demo playback, capture behavior, session metadata, persistence, analytics, navigation route, or accessibility announcement changed. `selectedPracticeScratch`/`showingComboChallenge` state, `practiceScratchOptions`, `babyComboChallenge`/`babyComboTimeline`, and both `fullScreenCover` presentations of `PracticeModeView` are untouched — no second source of truth for selected scratch or session configuration was introduced. No copy was rewritten: "Chirp Flare", "Baby Flow", "Start Baby Scratch"/"Start Chirp Flare"/"Start Combo Challenge"/"Run Combo Again", and every descriptive/coaching sentence on the screen are byte-identical to before.
- Verification: generic iOS Simulator Debug build succeeded; macOS Debug build succeeded; `plutil -lint` on `project.pbxproj` passed (unchanged, no membership edits); `git diff --check` passed. The four `LevelSelectView`-referencing source-contract tests in `CaptureReliabilityPhase1CoreTests` (`testLegacySampleAndBackingTrackSelectorsAreNotRoutedFromShippedMenus`, `testPracticeFlowDoesNotCreateCaptureSessions`, `testLevelSelectSourceRequiresExplicitScratchSelectionBeforeLaunchingPractice`, `testLevelSelectSourceUsesSafeAreaAwareScrollableHeaderLayout`) passed 4/4 in isolation — none needed an anchor update, since none of their asserted strings (state declarations, `"Chirp Flare"`, layout structure) were touched. `scripts/build.sh all` passed 47/47 capture-pipeline fixtures and reproduced the Phase 7 baseline exactly on the first run — **2,982 tests / 47 skipped / 100 failures, exit 65** — with the full failing-test-name set (57 unique names) diffed against the Phase 7 log and confirmed byte-identical.
- Remaining risks / deferred: no approved level-selection/coaching Figma frames were available, so this is documented-token/shared-component parity with the V3.2 system, not a pixel-perfect claim. `CoachPreviewView`, `ScratchCoachViews.swift`, Practice gameplay internals, the virtual platter, Watch, DEBUG diagnostics, macOS screens, and every underlying scoring/notation/audio/MIDI/DVS/camera system remain fully unmigrated by design, deferred to future phases. Full physical-device visual review was not performed beyond simulator/Debug builds.

## 2026-08-23 (V3.2 Phase 9 watchOS + iOS Watch-hub design-system adoption; uncommitted)

- Root cause: `ScratchLabWatch/WatchCaptureView.swift` (the entire watchOS UI) and `ScratchLab/Views/WatchCaptureHubView.swift` (the iOS Watch-capture hub) still built every color from literal `Color.red`/`.cyan`/`.white.opacity()`/`Color(hex:)`/manual gradients, and — more significantly — the Watch screen's richest state signal, `WatchMotionRecorder.transferStatus` (a free-text status line covering everything from "Sent to your paired device." to "Couldn't send to your paired device. Will retry automatically." to "Pair your watch with your device to send sessions.") had zero color coding at all, despite the task's explicit scope naming "warning, failed, disconnected states" and "progress and completion presentation."
- Files changed: `ScratchLabWatch/WatchCaptureView.swift`, `ScratchLab/Views/WatchCaptureHubView.swift`, `ScratchLab/DesignSystem/ScratchLabDesignSystem.swift`, `ScratchLab/DesignSystem/ScratchLabCoreColors.swift` (new), `ScratchLab.xcodeproj/project.pbxproj`; plus `TASKS.md` and this log. Every pre-existing dirty file was preserved.
- Cross-target design-system strategy (the phase's central decision, made before any view edit): confirmed via `project.pbxproj` that `ScratchLabDesignSystem.swift` compiles only into `ScratchLab` (iOS) and `ScratchLabDesktop` (macOS) — two `PBXBuildFile` entries, not three. Checked *why* before assuming it could just be added: its "Platform color mapping" section calls `Color(uiColor: .secondarySystemBackground)` and `Color(uiColor: .systemBackground)` under `#if canImport(UIKit)` — both true `canImport` on watchOS, but neither system-color role exists in watchOS's UIKit subset, so adding the file wholesale would fail to compile on the Watch target, not just be architecturally heavy-handed. Rather than force the full 2,200+ line iOS/macOS file (materials, `StatusBadge`/`Chip`/card/button component families sized for phone and desktop) onto a Watch target that shouldn't have any of that, extracted only the platform-safe primitive genuinely needed — pure RGB math, no UIKit/AppKit — into a new file: `ScratchLab/DesignSystem/ScratchLabCoreColors.swift`, containing the `Color(scratchLabHex:)` initializer (relocated from `ScratchLabDesignSystem.swift`, which no longer declares it) and a 12-constant `ScratchLabCoreColor` enum (`accent`/`success`/`warning`/`danger`/`info`/`textPrimary`/`textSecondary`/`textTertiary`/`canvas`/`raised`/`controlFill`). This file was added to all three targets (`ScratchLab`, `ScratchLabDesktop`, `ScratchLabWatch`) in `project.pbxproj`, following the exact precedent the project already establishes for `WatchMotionCapture.swift`/`CaptureReliability.swift` (small pure-Swift files already shared across the iOS and Watch targets): one `PBXFileReference`, one group-child entry, three `PBXBuildFile` entries (one per target's own `Sources` build phase). `ScratchLabDesignSystem.swift`'s `Sem.accent/success/warning/danger/info/textPrimary/textSecondary/textTertiary` and `Surface.canvas/raised` now read `ScratchLabCoreColor.*` instead of re-declaring the same hex literal a second time, so every overlapping role has exactly one canonical value across all three targets — not two independently-maintained palettes that could silently drift apart. The `pbxproj` diff is narrow and strictly additive: no existing `PBXBuildFile`/`PBXFileReference`/group/build-phase entry was altered or removed, only new ones added.
- Implementation — `WatchCaptureView.swift`: root background moved from a literal `Color.black`/`Color.blue.opacity(0.45)` gradient to the same canvas→raised→canvas gradient shape used at every other root-background site in the app (via `ScratchLabCoreColor.canvas`/`.raised`). The "Ready"/"Recording" status label moved from cyan/red to `.success`/`.danger` — a genuine readiness/recording state, kept semantically distinct from the Start/Stop button's own `.accent`/`.danger` (the button's color is "primary action," the label's color is "current state," and they happen to agree on Recording=red but intentionally disagree on Ready=green vs. the button's Start=accent-cyan). The Connection/Transfer info rows — previously plain, uncolored text — now use `.info` when paired/reachable and `.textTertiary` when not, matching "connected but not ready: informational" and "searching or waiting: neutral" exactly (the Transfer row's "Searching" value is the literal case the rule names). Added `WatchTransferStatusTone`, a private, purely-presentational classifier over `transferStatus`'s existing, unmodified string values — it changes no string, adds no state, and any string it doesn't recognize (e.g. a future wording change) falls back to neutral rather than guessing a color: `"Ready"`→success, `"Recording"`/`"Motion capture is unavailable on this watch."`/`"Unable to save the motion session."`→danger, `"Sent to your paired device."`→success, `"Queued the motion session for device import."`→info, and the remaining eight setup/retry/recoverable-issue strings (e.g. `"Pair your watch with your device to send sessions."`, `"Couldn't send to your paired device. Will retry automatically."`)→warning, since none of them is a hard failure and all are self-recovering or need a one-time setup step — matching "actionable attention: warning," not failure red, for exactly the case the task calls out ("do not use failure red for ordinary coaching correction when warning amber is truthful").
- Implementation — `WatchCaptureHubView.swift` (full `ScratchLabDesign` access — this file is iOS-target only): header/workflow/sessions cards moved onto `Typo.pageTitle/.pageSubtitle/.cardHeading/.body` and `.scratchLabCard(.selected/.standard)`; the "Paired Watch"/"Watch App Installed"/"Reachable Right Now" status rows (previously all identically colored regardless of true/false) now use `Sem.info` for "Yes" and `Sem.textTertiary` for "No" — informational, deliberately never `Sem.success`, since being paired/installed/reachable is a connection fact, not proof the watch is ready to capture; the free-text "Import Status" row stays neutral `Sem.textSecondary`. `WatchCaptureSessionCard`'s date/motion-count text and its "Share JSON"/"Share CSV" export badges moved off literal hex fills onto `Sem.accent`/`Sem.success` fill with `Sem.textOnAccent` label text, matching the exact dark-on-bright-fill convention every other primary/success control in the app already uses.
- Behavior preserved: no `WatchConnectivity` message name/payload, command routing, session synchronization, capture start/stop/pause/resume/cancel, connectivity evaluation, background delivery, activation state, haptic, timer, session metadata, persistence, complication, feature-flag, bundle-identifier, capability, or entitlement changed. `WatchMotionRecorder.swift`/`WatchMotionCaptureStore.swift` (the actual connectivity/capture services) were not touched — only their already-published, unchanged `@Published` values are read for display. No unsupported command, dead control, fake live state, or optimistic connectivity claim was added; the transfer-status tone classifier strictly recolors strings the model already produces, never invents one. Two new accessibility labels were added (the Start/Stop button, the Ready/Recording status text) specifically to satisfy "recording must remain visually unmistakable and must not rely on colour alone" — additive, not a change to the two pre-existing accessibility labels on the instructional text and footer note, which are untouched.
- Verification: watchOS Simulator Debug build succeeded — run explicitly as `xcodebuild -target ScratchLabWatch -configuration Debug -destination 'id=<Apple Watch Series 11 42mm simulator>'` after an initial `-scheme ScratchLabWatch -destination 'generic/platform=watchOS Simulator'` run was caught copying a stale, pre-edit `ScratchLabWatch.app` binary instead of recompiling (confirmed by grepping that run's log for zero `WatchCaptureView`/`ScratchLabCoreColors` compile lines); the corrected explicit-target run's log shows `SwiftDriverJobDiscovery ... Compiling ... WatchCaptureView.swift ... ScratchLabCoreColors.swift ... (in target 'ScratchLabWatch')`, confirming a genuine watchOS compile of the edited sources. Generic iOS Simulator Debug build succeeded (this also rebuilds and embeds a fresh `ScratchLabWatch.app`). macOS Debug build succeeded. `plutil -lint` on `project.pbxproj` passed. `git diff --check` passed. `ScratchLabDesignTokensTests` (22 tests) passed 22/22 in isolation, confirming the `Sem`/`Surface` hex-constant relocation onto `ScratchLabCoreColor` changed no actual resolved color value. `scripts/build.sh all` passed 47/47 capture-pipeline fixtures and reproduced the Phase 8 baseline exactly on the first run — **2,982 tests / 47 skipped / 100 failures, exit 65** — with the full failing-test-name set (57 unique names) diffed against the Phase 8 log and confirmed byte-identical. No transient missing-resource/build-artifact issue occurred this phase.
- Remaining risks / deferred: no approved Watch/Watch-hub Figma frames were available, so this is documented-token semantic parity, not a pixel-perfect claim. Virtual platter, app icons, splash/logo animation, general iOS Home/destination screens, already-completed Capture/Practice screens, macOS screens, DEBUG diagnostics, the underlying `WCSession` connectivity implementation, and any new complication/widget/Watch functionality remain fully unmigrated by design, deferred to future phases. No physical Apple Watch hardware test was performed — verification is Simulator-only, per the acceptance criteria's explicit Simulator build requirement.

## 2026-08-23 (V3.2 Phase 10 Virtual Platter prototype design-system adoption; uncommitted)

- Root cause: `ScratchLab/Views/VirtualPlatterPrototypeView.swift` — an explicitly `#if DEBUG`-gated, honestly-labeled developer prototype ("Isolated prototype screen for the new consumer 'virtual platter' direction," reached only via a `#if DEBUG` `.navigationDestination` behind a "Virtual Platter Prototype" / "Developer-only scratch-on-glass slice (no capture/ML)" menu entry in the already-deferred `AdvancedHubView`) — built its header, cards, and buttons from literal `Color(hex:)`/`.white.opacity()`/`.black`/manual gradients instead of the shared V3.2 tokens used everywhere else in the app, while its actual platter-rendering subviews (`PlatterDisc`, `PlatterMarker`, `GhostTargetArc`, `LiveRibbon`, `SuccessFlare`) legitimately need their own specialized, non-token colors to encode rotation direction, needle position, cue position, and touch/lock feedback.
- Files changed: `ScratchLab/Views/VirtualPlatterPrototypeView.swift` only; plus `TASKS.md` and this log. No changes to `ScratchLabDesignSystem.swift` or `ScratchLabCoreColors.swift` — every role needed already existed. Every pre-existing dirty file was preserved.
- Product-truth check performed before any edit: confirmed the screen's DEBUG gating (`MainMenuView.swift`'s `#if DEBUG .navigationDestination(isPresented: $showingVirtualPlatterPrototype)`), its truthful "Prototype" wording (the header badge and the Advanced-hub menu subtitle), and its documented "developer-only" status were all already accurate — none of that was touched, strengthened, or weakened. No control was silently rewired: Play/Stop (`togglePlay`), mode picker (`$mode`), Start Drill/Reset (`startStage`/`resetStage`) all still call their pre-existing, unmodified functions.
- Ownership separated before editing, not assumed: `PlatterDisc`/`PlatterMarker`/`GhostTargetArc`/`LiveRibbon`/`SuccessFlare` (lines 578–742, specialized rendering) and the `directionColor`/`stagePhaseColor`/`directionIcon`/`stagePhaseLabel` derived-state functions were read in full and confirmed to encode real domain state (rotation direction, playback/lock phase) — none of them, nor `VirtualPlatterAudio`, nor any logic function (`advanceRecord`, `tick`, `cueRecord`, `startStage`, `resetStage`), appears anywhere in the diff.
- Implementation: `header`'s back button (`.white`/`Color.white.opacity(0.08)` → `Sem.textPrimary`/`Surface.controlFill`), "Prototype" status pill (amber dot literal `Color(hex:"F59E0B")` — already the exact `Sem.warning` hex — plus its text/background → `Sem.warning`/`Sem.textSecondary`/`Surface.controlFill`, wording unchanged), and title (`.white` → `Sem.textPrimary`, `Typo.title3`). `platterCard`'s and `telemetryCard`'s outer `.padding+.background(white.opacity)+.overlay(stroke)` shells collapsed onto `.scratchLabCard(.standard)`, an exact-pattern match to every other migrated card in the app; `stageControlCard`'s shell kept a manual background+overlay because its border color is `stagePhaseColor` — a genuine, functional playback-phase indicator (waiting/active/locked/missed), not decoration — with only its background/corner-radius literals swapped for `Surface.card`/`Radius.card`. The Play/Stop button (previously a literal green/red fill with black text) and the Start Drill/Reset buttons (previously literal green fill / white-on-translucent) collapsed onto `.scratchLabPrimaryButton(fillsWidth: true)`/`.scratchLabSecondaryButton(fillsWidth: true)` — the primary-action role, per this phase's explicit "primary action: accent" rule; a state-dependent fill color was deliberately not kept, since neither "Play" nor "Stop" represents failure and each state already reads unambiguously from its icon (`play.fill`/`stop.fill`) and text ("Play"/"Stop"), which the button style leaves untouched. A VoiceOver label was added to the Play/Stop button (none existed before) purely because the icon-only fallback state needed a spoken equivalent once the color was no longer state-dependent — additive, not a change to any pre-existing accessibility label. `telemetryTile`'s SPEED accent (`Color(hex:"0EA5E9")`, already the literal `Sem.accent` hex) became `Sem.accent`; its nested tile background (`Color.black.opacity(0.22)`) became `Surface.raised` — the identical token already used for nested metric tiles inside cards in `CompanionCameraView.swift`. The "NORMALIZED SPEED" progress track (`Color.white.opacity(0.08)`) became `Surface.controlFill`; its fill color (`directionColor`) was left untouched, since it visually encodes live scratch direction. `centerLabel`'s direction-label text moved onto `Sem.textPrimary.opacity(0.8)`, but its icon color (`directionColor`) and its black-opacity circular backdrop were deliberately left as literals — the backdrop exists solely for text legibility over the rotating disc art beneath it, matching the same "one-off legibility wash, no shared token represents this role" precedent already established for a camera-preview overlay in Phase 5.
- Behavior preserved: no audio-session activation, playback-engine lifecycle, sample loading, hot-cue behavior, platter position/phase calculation, forward/reverse behavior, gesture interpretation, DVS behavior, speed/time-stretch calculation, looping, near-stop behavior, diagnostic-preview behavior, route selection, error propagation, feature flag, navigation, or availability changed. No new control, sample, claim, or unsupported functionality was added. The DEBUG gate and "Prototype" labeling remain exactly as truthful as before.
- Regression investigation (flake, not caused by this phase): the full-suite run surfaced one net-new failing test name versus the Phase 9 baseline — `CaptureReliabilityPhase1CoreTests.testMIDILearnUpdatesFromListeningToLearnedStateWhenCCArrives`, entirely unrelated to Virtual Platter. The same build log shows this exact test both failing (`XCTAssertEqual failed: ("nil") is not equal to (...)`, an async `SwiftUIStateGuard` publish race) and passing later in the identical run. Re-ran it in isolation 4 separate times (8 executions total, since the project runs each `-only-testing:` invocation twice) — all 8 passed. Confirmed as a pre-existing, non-deterministic async-timing flake in the MIDI Learn engine's state publishing under full-suite load, not something a color/typography-only diff in an unrelated SwiftUI view could cause. The 13 pre-existing `ScratchSamplePlaybackControllerTests` failures were independently re-confirmed present, unchanged, and identical by name to the Phase 9 baseline — no scratch-playback test's name or behavior changed.
- Verification: generic iOS Simulator Debug build succeeded; macOS Debug build succeeded; `plutil -lint` on `project.pbxproj` passed (unchanged, no membership edits this phase); `git diff --check` passed. `VirtualPlatterTests` (20/20) and `ScratchLabDesignTokensTests` (22/22) passed in isolation; `PlatterTestSampleLoadTests` passed. `scripts/build.sh all` passed 47/47 capture-pipeline fixtures and produced **2,982 tests / 47 skipped / 100 failures, exit 65** — the same totals as the Phase 9 baseline. The failing-test-name set is 58 unique names (Phase 9: 57): zero names were removed, and the sole addition is the confirmed-flaky MIDI Learn test investigated above, not a real regression.
- Remaining risks / deferred: no approved Virtual Platter Figma frames were available, so this is documented-token/shared-component parity, not a pixel-perfect claim. Playback engines/audio services, DVS/timecode processing, general coaching, Watch, macOS Analyzer, Capture/Practice, app icons, splash/logo animation, DEBUG panels outside Virtual Platter, and unrelated sample/audio tooling remain fully unmigrated by design, deferred to future phases. No dead or unwired control was found during this pass — every visible control retains its existing, working action.

## 2026-08-23 (V3.2 Phase 11 iOS Quick Start onboarding design-system adoption; uncommitted)

- Root cause: the three-page `QuickStartView` in `ScratchLab/ScratchLabApp.swift` still used a private literal background gradient, one-off white/cyan palette, fixed typography, manual bullet list, manual cyan CTA, and the system page-control presentation even though the V3.2 shared background, typography, card, button, spacing, and semantic state roles already covered the flow. The presentation also did not use the approved symbol-only product mark, and its single centered layout did not make an explicit compact-vs-regular-width adaptation.
- Files changed for Phase 11: `ScratchLab/ScratchLabApp.swift`, `TASKS.md`, and this log only. `ScratchLab/ScratchLabApp.swift` already contained pre-existing Phase 7 root-background and logo-accessibility hunks; those were preserved. No change to `ScratchLabDesignSystem.swift`, `ScratchLabCoreColors.swift`, `project.pbxproj`, the logo asset, Practice help, or any deferred screen was needed.
- Flow/state trace: `ContentView` remains the first-run owner, presenting `QuickStartView(onFinish: completeQuickStart)` as an interactive-dismiss-disabled full-screen cover only after the splash and only when `hasSeenQuickStart` is false or `quickStartVersion < QuickStartSettings.currentVersion`. `completeQuickStart()` still writes the same two `@AppStorage` values and dismisses. Practice's existing capture-help relaunch remains the second entry point and still calls its own existing completion adapter. `QuickStartView` still owns only `currentPage`; no second source of truth was introduced. Its visible actions remain Back → decrement page, horizontal page swipe → update the same binding, Skip → `onFinish`, Next → advance, and Start Session → `onFinish`.
- Implementation: replaced the private three-stop gradient with `ScratchLabDesign.Surface.applicationBackground`; moved Back/Skip and Next/Start Session onto `.scratchLabTertiaryButton()` and `.scratchLabPrimaryButton(fillsWidth:)`; added the approved `Image("ScratchLabLogo")` symbol with separate native `Text("ScratchLab")`; replaced the default page dots with a small accessible progress view using `Sem.success` for completed steps, `Sem.accent` for the current step, and `Surface.disabledFill` for future steps; moved page titles/body text/bullets onto `Typo.title1`/`.bodyDefault`, semantic text/accent roles, shared spacing/radius, and `.scratchLabCard(.standard)`. `QuickStartPageView` now uses the same content in a compact single column or regular-width two-column layout, with a `ScrollView` fallback for Dynamic Type and short-height presentations. No onboarding-specific palette, typography scale, card/button family, badge family, or design-system role was added.
- Behavior and product truth preserved: all three page IDs/titles and all nine instruction strings are byte-identical. Back, Skip, Next, Start Session, swipe paging, completion persistence keys/version, first-run timing, Practice relaunch, navigation, interactive-dismiss policy, and all pre-existing accessibility labels remain unchanged. The added progress accessibility value reports only the existing page position. The flow contains no permission request or permission-rationale copy; no camera/microphone/local-network permission API or timing changed. No privacy, local-processing, export/data-sharing, or denied-recovery wording changed, so there is no exact wording change to disclose. No device detection/readiness, lifecycle, deep link, feature flag, capture, audio, MIDI, DVS, Watch, validation, or persistence behavior changed.
- Visual qualification: no approved Quick Start/onboarding Figma frames were provided or present in the repository, so this is documented-token/shared-component parity only, not a pixel-perfect claim. A fresh iPhone 17 simulator install exposed an initial compact-layout issue where the third bullet required scrolling; the header/product lockup and decorative hero were tightened without changing content, and the final screenshot confirmed all three instructions plus the CTA are initially visible. The iPad regular-width check confirmed the same content reflows into introduction and instruction columns. Scrolling remains available for larger Dynamic Type.
- Verification: generic iOS Simulator Debug build succeeded after the final edits; macOS Debug build succeeded. Focused tests: `ScratchLabDesignTokensTests` 22/22; `PrivacyAndPermissionRegressionTests` 3/3; camera, microphone, and local-network rationale source contracts plus the brand source contract 4/4. `plutil -lint ScratchLab.xcodeproj/project.pbxproj` passed (the project file is pre-existing dirty but unchanged by Phase 11), and `git diff --check` passed. `scripts/build.sh all` passed 47/47 capture-pipeline fixtures and then exited 65 on the repository baseline desktop failures: **2,982 tests / 47 skipped / 100 failures / 57 unique XCTest failing names**. The unique failing-name set is exactly identical to the stable Phase 9 set; relative to Phase 10, the sole difference is that the already-investigated async MIDI Learn flake did not fail this time. No test both failed and passed in the Phase 11 run, no name was added versus Phase 9, and no genuine Phase 11 regression was found.
- Remaining risks / deferred: automated VoiceOver traversal and physical-device review were not performed. Permission rationale/prompt UI owned by Practice, Profile/Settings, Demo Mode, general coaching, Advanced Hub, iPad Performer Monitor, Watch, Virtual Platter, previously migrated Capture/Practice/Home/level-selection screens, DEBUG diagnostics, app icons, and splash/logo animation remain out of scope and untouched.

## 2026-08-29 (Capture-integrity Slice A: shared capture-evidence model; uncommitted)

- Selected task: the first slice of the capture-integrity batch Karl approved after the physical iPhone take-002 evidence (screen recording plus two Review/HUD screenshots) showed live blue notation responding while the capture HUD read `MOTION Motion Missing`, and Review simultaneously rendered `Ready to keep`, `Motion pending`, `Motion Missing`, and `Fader: No movement`. The full ten-requirement prompt was classified as roughly eight architectural slices and decomposed before any edit; Karl selected Slice A and directed that the AHHH/Serato requirement become an explicit new mode (Slice G) rather than a deletion, so the gate recorded in `AI_HANDOFF/next_prompt.md` is superseded rather than silently overwritten.
- Root cause (verified by reading, not inferred): motion presence was derived solely from a linked Apple Watch artifact at three independent sites — `ScratchLab/Views/CompanionCameraView.swift` `handleFinishedRecording` (`watchMotionCaptureStore.hasLinkedCapture(...)`), the `makeSessionExportPackage` take builder in the same file (`linkedWatchCapture != nil`), and `ScratchLab/Services/SessionExportCoordinator.swift`'s recovery builder (`motionPresent: linkedWatchCapture != nil`). The take's genuine controller evidence was already produced by `iOSMIDIManager.detectedNotationSnapshot()` and persisted into the sidecar via `broadcaster.persistingDetectedNotation(...)` four lines above the first of those checks, then ignored by all three. A valid RANE platter take with no Watch paired therefore reported motion missing in Review and exported as motionless.
- Second, larger root cause found while implementing: export validation would have actively rejected the newly-truthful takes. `packageValidationIssues` appended "claims watch motion, but no linked watch artifact was supplied" whenever `motionPresent == true` without a watch session, and the canonical-context builder hard-`throw`s `SessionExportError.invalidSessionMetadata` on the same condition. Fixing presence without fixing these two gates would have converted a mislabeled-but-exportable take into an unexportable one.
- Files changed: `ScratchLab/Models/CaptureCore.swift`, `ScratchLab/Views/CompanionCameraView.swift`, `ScratchLab/Services/SessionExportCoordinator.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, plus `TASKS.md`, `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`, and this log. All four source/test files were already pre-existing dirty; every unrelated dirty file and hunk was preserved. No `project.pbxproj` edit was needed because every touched file already has target membership and the tests were appended to an existing test file. No resource, Info.plist, entitlement, signing, or Copy Bundle Resources change.
- Implementation: added a domain-only `CaptureMotionEvidence` to `CaptureCore` — `CapturePlatterMotionEvidence` (`absent` / `steadyRotationOnly(forwardRuns:)` / `gesture(movementRuns:reversalRuns:)`), a fader event count, `CaptureWatchMotionEvidence`, and `CaptureDVSMotionEvidence` — with `CaptureMotionEvidenceResolver` as the single place that turns persisted take evidence into it. Per Karl's adjustment the type carries no user-facing strings; presentation is deferred to Slice B so the two platforms cannot drift into different vocabularies for one state.
- Gesture-vs-motor discrimination is structural, not a new threshold: a released powered platter emits forward-only CC6 runs, and every real scratch contains a pull-back, so the discriminator is the presence of a decoded backward run. This reuses `derivePlatterMovementEvents`' existing noise gates (`minRunDuration` 0.08 s, `minRunSteps` 8) instead of introducing a second independently-tunable layer. A typed `ScratchMovementKind.isReversal` was added rather than string-matching `direction`.
- Per Karl's adjustments: fader events never establish motion presence (a cut is recorded evidence but is not platter movement, so a fader-only take has no gesture to review); DVS reports `.unsupported` and is excluded from `motionSources` because `CompanionCameraView` has no timecode-pipeline feed at take finalization today — modelled for exhaustive switching, never fabricated; and both the live export builder and the recovery builder resolve from the same persisted `sidecar.detectedNotation`, so a live-exported take and the same take rebuilt from disk resolve identically.
- Export validation was made source-aware via `SessionExportTake.motionSources`, an additive optional field with two derived predicates (`claimsWatchBackedMotion`, `claimsMotionWithoutAnySource`). `nil` sources preserve the historical Watch-only behaviour exactly, so pre-existing callers and fixtures validate as before; `[]` with `motionPresent == true` is now explicitly a contradiction rather than being silently treated as a Watch claim; and a watch artifact belonging to a different session or take is still rejected on both the controller-backed and watch-backed paths. `SessionExportTake` is an in-memory `Sendable` input struct, not a serialized type, and `take_log.csv` / `session_manifest.json` carry no motion column, so there is no on-disk export-schema change.
- `markReviewMotionPresent()` was renamed to `markReviewWatchMotionLinked()` and now records the Watch as a source on the existing evidence instead of flipping a bare boolean, so a late-linking Watch cannot erase platter evidence a take already had.
- Verification: 13 new `CaptureMotionEvidenceTests` passed twice (13/13 both passes). Regression suites over the same code: `ControllerPlatterDecoderTests` 14/14, `CaptureRecoveryPhase2CoreTests` 58/58, `ScratchLabNotationAndExportTests` 29/29, `Take002ControllerRoundTripTests` 3 passed / 1 skipped. Isolated macOS Debug build, macOS `build-for-testing`, and generic iOS Simulator Debug build (`CODE_SIGNING_ALLOWED=NO`) all succeeded using isolated `-derivedDataPath` / `OBJROOT` / `SYMROOT` / `SHARED_PRECOMPS_DIR`. `python3 scripts/test_capture_pipeline.py` passed 47/47. `git diff --check` passed. XCTest bundles were run through `xcrun xctest` after symlinking `ScratchLab.debug.dylib` into the bundle's `Contents/Frameworks`.
- `scripts/build.sh all` was run once as AGENTS.md requires. It reached the full 366-test plan and failed on the pre-existing dirty-WIP baseline: the four documented UI-layout failures (`testGuidedCaptureLandscapeHidesHelperTextDuringPreRoll`, `testGuidedCaptureSystemCheckScrollsOnSmallScreens`, `testLevelSelectSourceUsesSafeAreaAwareScrollableHeaderLayout`, `testPracticeSetupDoesNotRenderCoachCard`), the fifth documented one (`TimecodePrototypeProfileTests.testRaneOneMkiiDebugPresetHasSetupNoteOtherPresetsDoNot`), the Coach-demo/reference-audio resource suites, and four `ScratchSamplePlaybackControllerTests` DVS/wrap tests belonging to the dirty `IOScratchPlaybackEngine` / `IOScratchRenderer` WIP.
- Proof those failures are unrelated rather than assumed: the missing bundle resource was confirmed directly (`chirpflare_noBeat.wav` is absent from the built `.app`'s `CoachDemoAudio`, matching its `D` entry in `git status`), and every failing suite was checked for Slice A symbols — `ScratchSamplePlaybackControllerTests`, `TimecodePrototypeProfileTests`, `ScratchAnalyzerReferenceFoldersTests`, `DemoAudioResolverTests`, and `DemoTimingFoundationTests` each contain zero references to `CaptureMotionEvidence`, `motionSources`, `handleRecordingFinished`, `claimsWatchBackedMotion`, `claimsMotionWithoutAnySource`, `markReviewWatchMotionLinked`, or `SessionExportTake`. Intersecting the 29 unique failing `CaptureReliabilityPhase1CoreTests` names against the set of tests in that file that reference any Slice A symbol produced the empty set. `testGuidedCaptureConsumesCompletedSummaryBeforeOptionalAudioInspection`, the one pre-existing test that does assert on the `handleRecordingFinished` call site, passed.
- Not claimed by this slice, and deliberately left failing-honest: the hardcoded `"Ready to keep"` literal (two sites) and `GuidedCaptureReviewStateResolver`'s deliberate two-different-strings-for-one-false-input shape both survive, so a genuinely motionless take can still render `Motion pending` and `Motion Missing` together. That is Slice B. Crossfader capture still requires a learned mapping, so `Fader: No movement` is unchanged until Slice C. No notation coordinate, audio-ownership, finalization, export-discoverability, clipping/health, or landscape-layout behaviour was touched.
- Remaining risks: the resolver is unit-verified but has not been exercised against a physical RANE take on device — the decisive check is one short hardware take with no Watch paired, confirming Review reports motion present and the exported package validates. Nothing was committed or pushed.

## 2026-08-29 (Capture-integrity Slice B: truthful review state derivation; uncommitted)

- Selected task: Slice B of the approved capture-integrity decomposition, started from Codex checkpoint commit `089965f4`, which had absorbed both Karl's prior WIP and Slice A. `089965f4` was not amended, reset, rebased, or overwritten. The remaining unstaged Xcode settings (`project.pbxproj`, `ScratchLab.xcscheme`, `Info.plist`), the resource deletions, and the stray `CalibrationCameraOverlayTests.swift.plist` were left exactly as found — Slice B did not need any of them.
- Root cause: three separate defects produced the single reported symptom (take-002 rendering `Ready to keep`, `Motion pending`, `Motion Missing`, and `Fader: No movement` at once). First, the operator message was a hardcoded `"Ready to keep"` string literal decided independently at two call sites in `CompanionCameraView.swift` — `handleRecordingFinished` and `applyAudioPresence` — neither of which consulted motion at all. Second, `GuidedCaptureReviewStateResolver.motionAssessment` computed `syncStatus` and `motionStatusTitle` as two independent strings over the same inputs, and deliberately returned `"Motion pending"` for the same `motionPresent == false` that produced `"Motion Missing"`. Third, `CaptureReview` stored all four labels as separately-settable `var`s, so even a correct resolver could be partially applied.
- Files changed: `ScratchLab/Models/CaptureCore.swift`, `ScratchLab/Views/CompanionCameraView.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, plus `TASKS.md`, `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`, and this log. No `SessionExportCoordinator.swift` change was needed — Slice B is presentation and state derivation only, and the export contract established in Slice A is untouched. No `project.pbxproj` edit, no resource change, no export-schema change.
- Implementation: replaced `GuidedCaptureMotionAssessment` with `GuidedCaptureReviewState`, built from two typed enums — `CaptureReviewReadiness` (`recordingInterrupted` / `takeTooShort` / `missingAudio` / `calibrationInvalid` / `retakeRecommended` / `readyToKeep`, ordered most- to least-blocking, exactly one reported) and `CaptureMotionStatus` (`present` / `optional` / `missing`), plus a `CaptureMotionRequirement` that names why motion is or is not required. `GuidedCaptureReviewStateResolver.reviewState(...)` now also takes `recordingFailed` and `duration`, which is what removes the call-site decision that let a hardcoded literal survive next to a missing-motion pill. The 1.0-second floor moved out of two duplicated call sites into `minimumKeepableTakeDuration`.
- `CaptureReview`'s `syncStatus`, `motionStatusTitle`, `motionPresent`, and `operatorMessage` became computed projections of a single stored `reviewState`, so they cannot be set independently or partially refreshed. `syncStatus` is deliberately a projection rather than an independent string: when motion is the blocker it now returns `motionStatus.title` verbatim, so the sync and motion labels can restate each other but can never contradict each other. `applyAudioPresence` and `markReviewWatchMotionLinked` both collapsed onto one private `resolvedReviewState(...)` helper, replacing two hand-maintained copies of the same branch ladder.
- `CaptureReviewReadiness.retakeRecommended` was added rather than reusing a failure case, because a required-motion-missing take has valid media and remains keepable — `isKeepable` distinguishes it from the four hard failures. This is what makes `"Ready to keep"` structurally impossible beside `Motion Missing` instead of merely unlikely.
- Added a shared `CaptureMotionEvidencePresenter` in `CaptureCore` (alongside the readiness vocabulary, so iOS and macOS cannot drift into different wording for the same evidence) producing `CaptureEvidenceRow` values: `Platter: Present · MIDI` / `Rotation only` / `Not detected`, `Fader: N event(s)` or `No movement`, and `Watch: Linked` / `Not used`. These replaced the single standalone Fader block in both `landscapeDetails` and `decisionColumn`. `steadyRotationOnly` is named separately from `absent` so a running motor does not read as a capture failure. DVS is deliberately omitted: a permanent `Unavailable` row would imply a source the product does not have, and it returns when a real feed exists. `reviewStatusPills` now switches on the typed readiness instead of comparing `syncStatus` against the string literal `"Ready"`.
- Verification: `GuidedCaptureReviewStateTests` 10/10 and `CaptureMotionEvidencePresenterTests` 7/7, plus Slice A's `CaptureMotionEvidenceTests` 13/13, all passed twice (30/30 per pass). Two of the new tests are exhaustive over all seven resolver inputs and assert that no combination can yield `"Motion pending"` or pair `readyToKeep` with `motionStatus == .missing`. The pre-existing `testGuidedCaptureReviewStateDoesNotInventWatchMotionWhenMotionIsSkipped` was migrated to the new API and still asserts the same behaviour. Regressions: `CaptureRecoveryPhase2CoreTests` 58/58, `ScratchLabNotationAndExportTests` 29/29, `ControllerPlatterDecoderTests` 14/14, `Take002ControllerRoundTripTests` 3 passed/1 skipped. Isolated generic iOS Simulator Debug build, macOS Debug build, and macOS `build-for-testing` all succeeded. Fixtures 47/47. `git diff --check` clean.
- `scripts/build.sh all` was run once as AGENTS.md requires: exit 65, **3052 tests / 49 skipped / 15 failures**. The failing set is the unchanged dirty-WIP baseline — the four documented UI-layout failures, `testRaneOneMkiiDebugPresetHasSetupNoteOtherPresetsDoNot`, the Coach-demo/reference-audio resource suites, and the four `ScratchSamplePlaybackControllerTests` DVS/wrap tests. Attribution was verified by extracting the failing `CaptureReliabilityPhase1CoreTests` names and diffing them against the set captured during Slice A: **identical, 26 names, nothing added and nothing fixed**. All three Slice A/B suites passed inside the full run.
- Not claimed by this slice: crossfader capture still requires a learned mapping, so `Fader` reads `No movement` on an unmapped controller until Slice C — Slice B corrects how that evidence is *presented*, not whether it is captured. Bounded finalization (no indefinite `Saving`) is Slice E despite the original slice title mentioning finalization; nothing in this slice touches the finalization watchdog. Notation coordinates, audio ownership, export discoverability, clipping/health, and landscape layout are untouched.
- Remaining risks: the readiness vocabulary changed user-visible copy — a required-motion-missing take now reads `Retake recommended` rather than `Ready to keep`, which is the intended correction but is a wording change worth Karl's eyes on device. The evidence rows add two cells to the landscape detail grid, and landscape crowding is a known open problem deferred to Slice H; this was not visually verified on hardware. Nothing was committed or pushed.

## 2026-08-29 (Capture-integrity Slice C: crossfader provenance and take-window bounding; uncommitted)

- Selected task: Slice C, started from commit `14685e06`. Neither `089965f4` nor `14685e06` was amended, reset, or rebased.
- Pre-step: Karl authorised restoring the accidental tracked resource deletions. `git diff --name-status -- ScratchLab/Resources` was checked first and returned 32 entries, ALL marked `D` (no `M`, no `A`), satisfying the stated gate; `git restore --worktree -- ScratchLab/Resources` then restored all 32 and the resource diff is now empty. This alone removed the entire Coach-demo / reference-audio / practice-reel failure family: the full-gate baseline improved from 15 failures / 13 unique names to **11 failures / 9 unique names** before any Slice C code existed. `project.pbxproj`, `ScratchLab.xcscheme`, `Info.plist`, and the untracked `CalibrationCameraOverlayTests.swift.plist` were left untouched throughout.
- TWO CORRECTIONS to previously recorded claims, both now fixed in `TASKS.md`:
  1. The earlier Slice C stub said the `"Raw ch 15 · CC 8"` Hardware Setup row "shows as configured while persisting zero events". That was false — `mappingDescription` already returned `"Not active · expected Raw ch 15 · CC 8"`. The claim was written without reading the function and is retracted.
  2. More importantly, the take-002 `Fader: No movement` reading is very likely CORRECT, not a bug. `ScratchLabApp.configureMIDILearnDevice()` already auto-applies the verified RANE mapping (crossfader CC8/ch15) at startup whenever a source resolves, and a Baby Scratch is played with the fader OPEN, which the existing `minimumValueDelta` gate correctly renders as zero fader events. Slice C must not be described as the fix for that screenshot.
- Real defects fixed. (a) `MIDIActionResolver` resolved transport against `MIDIHardwareRegistry` but resolved crossfader ONLY from a learned mapping, even though the registry already carries a `.certified` RANE crossfader binding (`absoluteCC(number: 8)`, `channel: 15`). Startup auto-applies the verified mapping, but `selectMIDISource` in `CompanionCameraView` and `MainMenuView` does not, so a device selected manually recorded no fader evidence at all. (b) Nothing bounded the END of the take window: `resetCapturedPlatterEvents()` set the start bound at real recording start, but events accumulated until the next take's reset, so controller moves made between Stop and movie finalization — precisely the window the finalization watchdog exists for — were decoded into the finished take.
- Files changed: `ScratchLab/Models/ControllerInput/MIDIActionResolver.swift`, `ScratchLab/Models/CaptureCore.swift`, `ScratchLab/MIDI/iOSMIDIManager.swift`, `ScratchLab/Services/SessionExportCoordinator.swift`, `ScratchLab/Views/CompanionCameraView.swift`, `ScratchLab/Views/MainMenuView.swift`, `ScratchLab/ScratchLabApp.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, plus `TASKS.md`, `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`, and this log. No `project.pbxproj` edit: every touched file already has target membership and all tests were appended to an existing test file.
- Implementation: added `FaderMappingSource` (`learned` / `certifiedRegistry`) and put it on `MIDIResolvedAction.crossfader`. A new resolution step 2b sits after the learned mapping and before the pad router, gated on three conditions so it can never become a blind "any CC8 is a crossfader" sniff — the registry must produce a real match for the device identity, that match must be `.certified` (not `.verified`/`.unverified`), and a non-diagnostic `.crossfader` binding must match the message via the existing shared `MIDIControlBinding.matches(_:)` rather than a second comparison. Learned mappings still win unconditionally, including when learned onto a non-default address.
- Per Karl's decision A, the registry path is EVIDENCE-ONLY: `playbackEngine.setCrossfaderPosition(...)` is now called only when `source == .learned`, so recognising a crossfader from the registry never starts cutting ScratchLab's audio on a device the user never mapped. Normalization was split accordingly — a learned control keeps its own min/max/inverted calibration, while a certified binding uses the plain 7-bit `MIDIControlNormalization.sevenBit`, never another action's calibration borrowed by accident.
- Provenance is persisted on `CaptureCore.RawMixerMIDIEvent.mappingSource` as an Optional, so Swift's synthesised decoder tolerates its absence and sidecars written before provenance existed decode as `nil` = "unknown provenance", never "learned". It is read back by `CaptureMotionEvidenceResolver.faderMappingSource(from:)` off the persisted mixer events, so a recovered take reports the same source the live take did, and flows into `CaptureMotionEvidence.faderMappingSource`, the review Fader row (`4 events · learned` / `· certified default`), and `SessionExportTake.faderMappingSource`. As with Slice A's `motionSources`, `SessionExportTake` is an in-memory input struct, so there is no on-disk export-schema change.
- The take-end bound is `markCaptureStopped()`, called from `stopTake()` before finalization begins, recording a monotonic take-relative stop instant in the same `CACurrentMediaTime()` domain as `captureBaselineTimestamp` — deliberately not reconciled against the sidecar's wall-clock `startedAt`/`endedAt`. The filter itself lives in shared `CaptureMotionEvidenceResolver.eventsWithinTakeWindow(_:stopRelativeTime:)` rather than staying dispatcher-private, because `iOSMIDIManager.swift` is an iOS-target-only file and is therefore unreachable from the macOS test target; making the boundary a shared rule is both better design and the only way to test it. The stop instant itself is inside the take (`<=`).
- No fabrication: `deriveDetectedNotationFaderEvents` and its `minimumValueDelta` / `minimumCutDelta` gates are completely untouched. Two tests pin this — a held-open fader emitting 20 identical values still yields zero events under certified provenance, and a genuine fast full-travel cut still registers.
- macOS mapping unification is deliberately OUT of scope per Karl's decision B and is documented as a KNOWN PLATFORM DIVERGENCE in `MIDIActionResolver.swift`: macOS keeps its own `CrossfaderCCMapping` in `UserDefaults` (`MacCaptureEngine`), predating the shared learned-mapping model and not registry-aware, so a certified registry crossfader default applies on iOS only and macOS still requires an explicit crossfader learn.
- Verification: 21 new `CrossfaderMappingProvenanceTests` passed twice, alongside `GuidedCaptureReviewStateTests` 10/10, `CaptureMotionEvidencePresenterTests` 7/7, and `CaptureMotionEvidenceTests` 13/13 — 51/51 per pass, two passes. Regressions: `MIDIHardwareRegistryTests` 46/46, `MIDILearnedMappingTests` 29/29, `MIDIFaderCurveTests` 22/22, `CaptureRecoveryPhase2CoreTests` 58/58, `ScratchLabNotationAndExportTests` 29/29, `ControllerPlatterDecoderTests` 14/14. Isolated iOS Simulator Debug build, macOS Debug build, and macOS `build-for-testing` all succeeded. Fixtures 47/47. `git diff --check` clean.
- `scripts/build.sh all` ran once: exit 65, **3073 tests / 49 skipped / 11 failures**. Test count rose by exactly 21 (the new suite) and the failing-name set diffed **IDENTICAL to the post-restore baseline — 9 names, nothing added, nothing fixed**: the four documented UI-layout failures, `testRaneOneMkiiDebugPresetHasSetupNoteOtherPresetsDoNot`, and the four `ScratchSamplePlaybackControllerTests` DVS/wrap tests.
- Investigated and cleared, not hidden: running `MIDILearnEngineTests` standalone via `xcrun xctest` produced 3 failures (`testHotCuePressLoadsAssignedSample`, `testLearnedHotCueSuppressesTempAhhhFallbackOnSameEvent`, `testSyntheticSelectionDoesNotOverwriteAppStandardDefaults`). These are NOT a Slice C regression: all three pass inside the full test plan both before and after the change, all three read and write shared `UserDefaults.standard`, one of them fails with its own "Hosted MIDI tests must never replace the user's selected controller" isolation message, and a scan of their bodies found zero references to any Slice C symbol (crossfader, identity, `updateMapping`, `FaderMappingSource`). This is pre-existing shared-defaults ordering fragility exposed by isolated execution, and is worth its own cleanup slice.
- Remaining risks: registry matching depends on the real CoreMIDI endpoint name, so `MIDIDeviceIdentity(sourceName:)` matching `"rane one"` is unverified against Karl's actual device string; only the RANE profile is `.certified`, so every other controller behaves exactly as before; and the `onMappingChanged` callback path in `ScratchLabApp.swift:131` supplies only a device ID, so identity falls back to the saved mapping's `deviceName` — correct for a learned device, but a device with no mapping at all reached only through that path would have no identity for the fallback.

## 2026-08-29 (RANE right-deck playback routing: measured 10-channel fix, physically approved; uncommitted)

- Selected task: Karl's physical report that ScratchLab's AHHH was driven by the right platter and right upfader and reached master output, but the RANE's LEFT channel meter LEDs illuminated. Started from commit `37e2d1d1`; no commit was amended or reset.
- Investigation discipline: the first pass STOPPED without editing rather than guessing a pair. The official RANE ONE MKII User Guide v1.1 was downloaded and searched in full — it documents the Deck Source switch ("Set this switch to the desired audio source from this channel: USB A or B ... or Line", and "The meter next to this knob indicates the signal's volume level") but contains NO USB channel numbering whatsoever, so the right-deck pair was not provable from official documentation. Project history offered only two negatives (13/14 lit the wrong deck; 3/4 now lit the left meter) and no positive identification. A DEBUG requested-versus-granted diagnostic was deployed instead, and Karl separately confirmed from hardware that USB output 1/2 is the left deck and 3/4 is the right deck.
- ROOT CAUSE, measured on device rather than inferred: the RANE ONE MKII exposes **10** USB output channels. `IOScratchPlaybackEngine` hardcoded `raneOutputChannelCount = 14` and gated the whole channel-map path on `session.maximumOutputNumberOfChannels >= 14`. `10 >= 14` is false, so `preferredHardwareChannelCount` collapsed to 2, `outputPairStartIndex` became nil, and `channelMap` was never assigned — playback fell through to the device's first output pair, 1/2, the LEFT deck. The diagnostic captured every step: `requestedChannels=2 supportsRaneOutputPairPreRequest=false / grantedChannels=10 maximumChannels=10 / outputNodeChannels=10 / channelMap=[nil] count=0`, identical at `after-configure` and `after-engine-start`.
- The pair index was CORRECT throughout. `raneOutputPairStartIndex = 2` (destination 2/3 = physical 3/4 = right deck) was never the operative value, which is why both the earlier 13/14 assumption and the later 3/4 value produced a wrong-deck meter — neither was ever applied. The old DEBUG log printed `preferredHardwareChannelCount`, i.e. the value ScratchLab REQUESTED, so it read `hardwareChannels=14` even when the map was never installed; that single flaw is what hid the defect across two previous attempts.
- `AVAudioSession` was explicitly CLEARED as a suspect and not modified. It granted 10 of 10 channels on request, and `options=8` decodes to `.defaultToSpeaker` alone, which did not block multichannel USB output. No category, mode, or option change was made — the earlier hypothesis that `.defaultToSpeaker`/`.mixWithOthers` were forcing stereo was disproved by the measurement.
- Files changed: `ScratchLab/Models/DVSHardwareProfile.swift`, `ScratchLab/Audio/iOS/IOScratchPlaybackEngine.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, plus `TASKS.md`, `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`, and this log. No `project.pbxproj` change: the routing policy was deliberately placed in `DVSHardwareProfile.swift`, which is already a member of BOTH the iOS and macOS targets and is Foundation-only, so the pure policy is unit-testable from the macOS test target even though the engine that applies it is an iOS-target-only file. It also puts the DVS input pair and the playback output pair side by side with their namespace independence documented in one place.
- Implementation: `RanePlaybackRoutingPolicy` with `rightDeckPairStartIndex = 2` and `minimumRequiredOutputChannels` DERIVED as `rightDeckPairStartIndex + 2 = 4`, never a guessed device size. `decide(portName:grantedOutputChannels:outputNodeChannels:)` returns `.raneRightDeck(channelMap:)`, `.ordinaryStereo`, or `.unroutable(RoutingFailure)`. The engine now requests the connected route's actual `maximumOutputNumberOfChannels`, reads back the granted `outputNumberOfChannels` AND the output node's own channel count (the session granting enough is not proof the node did), builds the map sized to the real destination count, applies it, then READS IT BACK and verifies `mapPlacesRendererOnRightDeck` before permitting playback.
- Fails loudly instead of silently: a recognized RANE route that cannot reach the right deck now throws `IOScratchPlaybackRoutingError.raneRightDeckUnavailable` and does not start AHHH at all, rather than degrading to ordinary stereo. That silent fallback WAS the bug — plain stereo lands on 1/2, the left deck, with nothing to indicate anything was wrong. Non-RANE routes keep the ordinary stereo graph byte-for-byte unchanged.
- Test debt repaid: `testRaneScratchPlaybackUsesRightDeckOutputThreeFourIndependentlyOfDVSInput` asserted the literal source string `"let raneOutputPairStartIndex = 2"`. It passed for the entire duration of this bug, because the constant it pinned was never wrong. Replaced with 17 behavioural tests in a new `RanePlaybackRoutingPolicyTests` suite: 10-output accepted (the measured shape), 4-output accepted (the derived minimum), 14-output still works (the old guess must not become an implicit requirement), fewer than four rejected rather than routed to 1/2, output-node shortfall rejected even when the session grants enough, renderer mapped only to destination 2/3, left pair explicitly silenced, master pair 13/14 never targeted, read-back verification rejecting left-deck/swapped/nil/short maps, non-RANE routes including a multichannel non-RANE interface remaining ordinary stereo, DVS input 3/4 unchanged and independent, and right-upfader × right-deck-crossfader gain ownership unchanged.
- DVS input remains physical 3/4 in the independent Core Audio input namespace. Both input and output legitimately use 3/4; the policy documents that this is not a collision and that neither value may be derived from the other.
- PHYSICAL ACCEPTANCE (Karl, iPhone K + RANE ONE MKII): AHHH plays through the right deck, only the right channel meter responds, right upfader and right-deck crossfader control it, master output is normal, DVS unaffected. The on-device log corroborates the ears exactly: `[RANE-ROUTING] stage=configured route=Rane ONE MKII rane=true granted=10 max=10 outputNode=10 map=[-1,-1,0,1,-1,-1,-1,-1,-1,-1] onRightDeck=true decision=raneRightDeck`, and the identical map at `stage=after-engine-start running=true`, proving the assignment survives engine startup.
- Verification: `RanePlaybackRoutingPolicyTests` 17/17 twice. Regressions: `MIDIUserMixerGainTests` 39/39, `MIDIFaderCurveTests` 22/22, `DVSControlVinylAnalyzerTests` 40/40, `MIDIPlatterContinuousDriveTests` 17/17, `CrossfaderMappingProvenanceTests` 21/21, `CaptureMotionEvidenceTests` 13/13, `GuidedCaptureReviewStateTests` 10/10. Isolated iOS Simulator build, signed device build, and macOS `build-for-testing` all succeeded. Fixtures 47/47. `git diff --check` clean. `scripts/build.sh all` exit 65, 3090 tests / 49 skipped / 11 failures — the failing-name set diffed IDENTICAL to the post-restore baseline (9 names), with the test count up by exactly 17.
- Investigated and cleared, not hidden: `PlatterTestSampleLoadTests` reported 5 failures ("not found: dvs_ahhh") when run STANDALONE via `xcrun xctest`, but passes inside the full plan; the `dvs_ahhh` resource is present in both the source tree and the built bundle, and this slice's diff touches nothing near `ScratchSampleResolver` or any resource. Same shared-state/isolation fragility already recorded for `MIDILearnEngineTests` (3 failures standalone, passing in-plan). Unrelated to routing; a test-isolation cleanup slice is now clearly warranted.
- Remaining risk: only the RANE ONE profile is recognized, so every other output route behaves exactly as before. If a future RANE route ever grants fewer than four channels, AHHH will now refuse to play with a truthful message rather than sneak onto the left deck — intended, but a visible failure mode where there was previously a silent one. The meaning of RANE output pairs beyond 1/2 and 3/4 remains undocumented by the manufacturer; this slice needs only the two Karl confirmed. Nothing committed or pushed.

## 2026-08-29 (standalone test isolation cleanup; uncommitted)

- Root cause: `MIDILearnEngineTests` shared one process-wide defaults suite and the legacy crossfader path still used `UserDefaults.standard`; `PlatterTestSampleLoadTests` depended on implicit `Bundle.main` resource lookup. Both dependencies made direct standalone execution sensitive to state established by the full test plan.
- Files changed: `ScratchLab/Models/Samples/ScratchSampleResolver.swift`, `ScratchLabDesktop/Services/ScratchSamplePlaybackController.swift`, `ScratchLabDesktop/Services/MacCaptureEngine.swift`, `ScratchLabDesktopTests/PlatterTestSampleLoadTests.swift`, `ScratchLabDesktopTests/MIDILearnEngineTests.swift`, `TASKS.md`, `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`, and this log. Protected pre-existing dirty project/settings files were not altered.
- Implementation: added optional resource-root injection with `Bundle.main.resourceURL` as the unchanged production default; the platter tests explicitly use the app-class bundle resource root. Added optional MIDI-defaults injection; the MIDI suite creates a unique suite per test and clears its persistent domain in both `setUp` and `tearDown`. Explicit injection owns selection and legacy crossfader persistence, while production and existing non-injected tests retain `.standard` behavior.
- Verification: pre-edit standalone runs passed twice on the current HEAD despite the recorded historical failures. Final standalone runs passed twice (`PlatterTestSampleLoadTests` 2/2; `MIDILearnEngineTests` 32/32). `MIDILearnHangFixTests` passed 15/15. Both target suites passed in the full desktop plan; the full result remained 3,090 tests / 49 skipped / 11 failures across 9 unique names, matching the baseline count with neither target suite failing. Isolated generic iOS and macOS builds succeeded. `python3 scripts/test_capture_pipeline.py` passed 47/47. `scripts/build.sh` was not used because it has no isolated derived-data option; its applicable build/test steps were run directly with `/tmp` paths.
- Workspace note: the pre-existing untracked `.codex-build/` disappeared externally during the task. Work paused for confirmation, then continued as directed; the directory was not recreated. No commit or push was performed.

## 2026-08-30 (Capture-integrity Slice E: bounded, take-ID-scoped finalization state machine; uncommitted)

- Selected task: the first unchecked implementation-ready item, `TASKS.md` "Capture-integrity Slice E: bounded finalization state machine". Started from committed HEAD `c0bb3bab` with the pre-existing dirty WIP preserved; no commit was amended, reset, or rebased. Protected `project.pbxproj`, `ScratchLab.xcscheme`, `Info.plist`, and untracked `CalibrationCameraOverlayTests.swift.plist` were not altered or staged (mtimes unchanged: 24–29 Aug, before this session).
- Audit of the existing partial repair, which was six independent mechanisms that could contradict each other: (1) the timer freeze lived in `requestStopRecording` behind a `flowState == .recording` guard; (2) matching-summary handling lived in `matchesActiveSavingTake`, checked separately by three delivery paths; (3) the stop retry and (4) the recovery wait both lived inside one 12s + 3s `Task` watchdog in the view; (5) staging survived only because nothing happened to delete it, with no ordered preservation step; (6) Stop-during-start was handled only in the broadcaster's `stopRequestedWhileRecordingStarts`, with the view unaware of it. De-duplication was a `lastHandledRecordingID` string on the store, checked at the very END of `handleFinishedRecording` — after the view had already re-persisted controller notation, consumed `pendingScratchAudioURL`, and raised artifact banners. A duplicate recorder callback therefore ran all of that a second time and, because the pending scratch URL had already been cleared, falsely reported "the rendered AHHH audio file is missing".
- ROOT SHAPE of the defect: there was no single authoritative finalization state. "Are we saving", "did the watchdog already run", "was this summary already handled", and "is this summary for this take" were four separate facts stored in four places, so they could disagree.
- Files changed: `ScratchLab/Models/CaptureCore.swift`, `ScratchLab/Views/CompanionCameraView.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, plus `TASKS.md`, `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`, and this log. No `project.pbxproj` change: the machine was deliberately placed in `CaptureCore.swift`, which is already a member of BOTH the iOS and macOS targets and is Foundation-only, so the pure policy is unit-testable from the macOS test target even though `CompanionCameraView.swift` that drives it is an iOS-target-only file. This is the same placement rule used for `RanePlaybackRoutingPolicy` in Slice "RANE right-deck routing".
- Implementation, one model and one timer: `CaptureFinalizationMachine` owns the single typed `CaptureFinalizationState` (`idle` / `awaitingRecorder` / `retryingStop` / `completed` / `failed`), scoped to a `CaptureFinalizationTakeKey(sessionID:takeNumber:)`. It is pure and clock-free — `apply(_:at:)` is given the instant — and returns an ordered `[CaptureFinalizationEffect]` the caller performs. `CaptureFinalizationBudget` (12s first wait, 3s retry, 5s audio inspection) makes `worstCaseSaving` an explicit 15s. `CaptureFinalizationDeadlineScheduler` is the only place a deadline becomes elapsed time, and scheduling replaces the previous timer, so a second watchdog has nowhere to come from. `CaptureFinalizationClock` injects the time source.
- Consolidation, not a parallel model: the view's `finalizationWatchdog` `Task` and both hardcoded sleeps (`12_000_000_000`, `3_000_000_000`) are deleted; the store's `lastHandledRecordingID` is deleted. `flowState` remains the view's flow but is now a projection driven by the machine's `freezeElapsedTimer` and `presentRecoverableFailure` effects. `matchesActiveSavingTake` is now a one-line delegate to `finalization.acceptsSummary(for:)`.
- Requirement by requirement, each with a test: one typed authoritative state; every accepted Stop reaches exactly one terminal (Review, or an explicit recoverable failure) inside 15s with exactly one retry permitted; duplicate recorder callbacks, republished summaries, and late deadline delivery all return an EMPTY effect list, so `handleFinishedRecording` now gates on that BEFORE any side effect rather than de-duplicating afterwards; a summary whose take key differs in take number or session ID is refused and leaves the active take still waiting; Stop while the recorder is still starting emits an extra `cancelPendingRecorderStart` (stopping the beat engine and scratch recorder) alongside the ordinary recorder stop; the timer freezes at the FIRST accepted Stop because a second Stop while saving emits nothing at all; every failure path emits `preserveStagedMedia` BEFORE `presentRecoverableFailure`; phone Stop and Watch Stop both call one `stopTake(source:)` and produce byte-identical effect lists and state, with `source` recorded for provenance only and never branched on.
- Optional audio inspection: it already ran after Review, and is now additionally bounded by `budget.audioInspection`. `mediaContainsAudio` returns `Bool?` and yields `nil` on timeout, which the caller ignores — a slow `AVAsset` open is not evidence that a take is silent, and the previous shape would have downgraded a good take to `missingAudio`. `updateAudioPresence` additionally consults `acceptsAudioInspection(forRecordingID:)`, so an inspection that returns after the next take has been armed cannot rewrite the live review.
- Watch Stop during pre-roll now also closes the MIDI take window, stops the scratch recorder, clears the pending scratch stem, and retires any finalization cycle before cancelling — previously it stopped only the beat engine.
- Test debt: `testGuidedCaptureSavingFreezesElapsedTimeAtStopRequest` asserted the literal `"activeTake.stoppedAt = Date()"`, which pinned a `Date()` call rather than the freeze guarantee. It now asserts the machine-driven freeze path, and the guarantee itself is asserted behaviourally. `testWatchStartStopControlsTheIPhoneCaptureStateMachine` asserted `"stopTake()"`; it now asserts both `stopTake(source: .watch)` and `stopTake(source: .phone)`, i.e. the unified path. Added 28 behavioural tests in a new `CaptureFinalizationMachineTests` suite plus four Slice E source-structure guards (exactly one timer and one stop path; the summary gate precedes the first side effect; the audio bound; preserve-before-present wiring).
- Verification: `CaptureFinalizationMachineTests` 28/28 twice. Slice E source-structure guards 7/7. Regressions twice each: `CaptureRecoveryPhase2CoreTests` 58/58, `CaptureMotionEvidenceTests` 18/18 (paired suites in one run), `GuidedCaptureReviewStateTests` + `CaptureMotionEvidencePresenterTests` + `ControllerPlatterDecoderTests` + `RanePlaybackRoutingPolicyTests` = 123/123 combined; `ScratchLabNotationAndExportTests` + `Take002ControllerRoundTripTests` + `CapturedNotationTravelTests` 45 executed / 1 skipped / 0 failures. Isolated generic iOS Simulator build, generic iOS device build (embedded Watch), and macOS `build-for-testing` all succeeded with isolated `-derivedDataPath` / `OBJROOT` / `SYMROOT` / `SHARED_PRECOMPS_DIR` while Xcode was open. `python3 scripts/test_capture_pipeline.py` 47/47. `git diff --check` clean.
- Investigated and cleared, not hidden: running `CaptureReliabilityPhase1CoreTests` STANDALONE via `xcrun xctest` reports 23 unique failing names. Four (`testGuidedCaptureLandscapeHidesHelperTextDuringPreRoll`, `testGuidedCaptureSystemCheckScrollsOnSmallScreens`, `testLevelSelectSourceUsesSafeAreaAwareScrollableHeaderLayout`, `testPracticeSetupDoesNotRenderCoachCard`) are the dirty-WIP UI baseline already recorded in `AI_HANDOFF/next_prompt.md`; the other nineteen are the recorded deleted demo/reference-asset baseline (`testBabyNotationJSONBundleLoadsSuccessfully` fails on a missing bundled JSON, and the rest are the Baby Scratch demo/coach-rig family that depends on it). None of them read a Slice E symbol, `PracticeModeView.swift` and the demo resources are untouched by this diff, and the failing name set before this slice was the same 23 plus `testWatchStartStopControlsTheIPhoneCaptureStateMachine`, which this slice fixed by updating its stale `stopTake()` string.
- Remaining risk: the 15s worst case is a policy choice preserving the previous 12s + 3s timings exactly; it is now stated in one place rather than implied by two sleeps. The Stop-during-start path still depends on the broadcaster's `stopRequestedWhileRecordingStarts` to actually deliver the stop from `didStartRecordingTo`; the machine guarantees the request and the bound, not the AVFoundation delivery. A physical check remains required: record one short take, press Save Take, confirm the elapsed readout freezes and Review appears; then press Stop immediately after Start to exercise the startup path; and start/stop one take from the Watch. Nothing committed or pushed.

## 2026-08-30 (Capture-integrity Slice E: physical iPhone/Watch verification PASSED; uncommitted)

- Build under test: signed Debug build of the current source (Slice E included), built with isolated `-derivedDataPath build/SliceE-device-dd` / `OBJROOT` / `SYMROOT` / `SHARED_PRECOMPS_DIR` while Xcode was open, `-allowProvisioningUpdates`, destination `platform=iOS,id=1F80398A-96C8-537A-B0EE-821E186918B9` (iPhone `K`, iPhone 16 Pro Max). `** BUILD SUCCEEDED **`; embedded `ScratchLabWatch.app` present in the bundle; installed to `com.machelpnz.scratchlab` and launched via `xcrun devicectl`. No source or documentation was changed to obtain the build, and nothing was staged or committed.
- Check 1 — Save Take freeze and Review: **PASS**. Karl reports the elapsed readout froze immediately at the tap and Review appeared. This confirms on hardware both the original Saving-hang repair and the Slice E guarantee that the timer freezes at the FIRST accepted Stop.
- Check 2 — Stop immediately after Start: **PASS, but the branch was not recorded.** Karl reported a terminal result inside the bound, but returned the unfilled prompt template `[Review appeared / recovery banner returned to System Check]`, so it is not known which of the two accepted outcomes occurred. This is deliberately NOT resolved by guessing: reaching Review would prove the broadcaster's `stopRequestedWhileRecordingStarts` delivered the stop from `didStartRecordingTo`, while the recovery banner would prove the 15s bound expired and the `preserveStagedMedia` → `presentRecoverableFailure` order ran on device. The startup path is confirmed bounded and non-hanging either way. Ask Karl which he saw before recording a specific branch anywhere.
- Check 3 — Watch Start/Stop: **PASS**. With the iPhone Capture screen foreground and the Watch showing Transfer Connected, a single Watch tap started the take and a single Watch tap stopped it, with no second tap required. This confirms on hardware that phone Stop and Watch Stop enter the same `stopTake(source:)` transition path.
- Not covered by this device pass: Slice D's own refreshed physical approval (macOS free-spin → catch → unequal push/pull/reversal, and the iOS signed-playhead/saved-surface check) remains OUTSTANDING. Slice E's result must not be read as clearing Slice D for commit.
- Working tree after the device work: HEAD still `c0bb3bab`, 31 modified + 1 untracked (the same set as at session start), `git diff --check` clean, nothing staged, nothing committed, nothing pushed.

## 2026-08-30 (Slice E physical verification COMPLETE 3/3; Slice D surface claim corrected; new iOS saved-take task opened; uncommitted)

- E-1 Save Take freeze → Review: **PASS**. The elapsed readout froze at the tap and Review appeared.
- E-2 Stop inside the start race: **PASS via branch (a), the deferred stop.** Karl tapped Stop during the start race and the iOS `Ready to keep` Review screen appeared with Keep / Retry / Discard; the take was kept and exported successfully. Recorded precisely: the broadcaster's `stopRequestedWhileRecordingStarts` retained the request and delivered it from `didStartRecordingTo`, so the take finalized normally. This is **not** the preservation-timeout branch — `preserveStagedMedia` → `presentRecoverableFailure` did not run and the 15s bound was never reached. The timeout branch therefore remains covered by deterministic automated tests only, not by hardware; do not describe E-2 as a timeout or recovery-banner result.
- E-3 Watch Start/Stop: **PASS**. One Watch tap started and one Watch tap stopped the take, phone Capture foreground and Watch showing Transfer Connected.
- Supplied export verified read-only, session `b5a93fd7` (2026-08-30): one 11-second take, `recordingStatus: completed`, `errorDescription: null`, 35 movement events, 9,572 mixer MIDI events, notation confidence 0.948, `watchSyncState: acknowledged`, a genuine Watch CSV carried into `watch/NEWDJ_baby_095_take01_watch.csv`, and the audit trail `take_allocated` → `watch_requested` → `watch_sync acknowledged` → `recording_completed` → `notation_snapshot` → `watch_linked` → `watch_reconciled`. Healthy and complete.
- Earlier session `568857c3` was also inspected on device: all three takes carried `recording_completed` plus `notation_snapshot`, including take-001 (54 movement events) which was reviewed but not kept. Its absence from that export is correct behaviour — export contains kept takes only — and was not a finalization failure. No sidecar inspected on device showed `recordingStatus: failed` or any `errorDescription`.
- Investigated and resolved WITHOUT a code change: "could not open Take Detail on iOS" was traced read-only and is not a navigation defect, not caused by Slice D, and not invalid saved-take data. `Take Detail` exists only on macOS (`MacAnalyzerView.reviewFigmaTakeDetail`); two full-target greps find no `TakeDetail` in the iOS target. What was tapped is `landscapeDetails` inside `TakeReviewView`, a read-only `CaptureReviewDetailBlock` label group with no `NavigationLink`, `.sheet`, or tap gesture. The original instruction to open Take Detail on iOS was wrong; the app behaved correctly.
- Larger finding from that trace: `TakeReviewView` renders **no notation at all** — no notation, lane, trace, or chart anywhere in its body — and the only iOS saved-notation surfaces, `DebugReviewNotationCard` and `DebugNotationLaneHostView`, are entirely `#if DEBUG` and wired into no production surface. A kept take therefore cannot be reopened or played back on iPhone.
- Slice D documentation corrected honestly rather than quietly narrowed. Its recorded claim that "Live iOS/macOS, saved Review, Take Detail … all consume the shared gesture-relative presentation" overstated iOS. Corrected by platform in `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`, and `TASKS.md` — **iOS: live signed playhead and live notation. macOS: live notation plus saved Review and Take Detail.** Slice D delivers no production iOS saved-take surface, and must not be read as having shipped one. This matters under `PROFILE.md`'s no-overclaiming rule.
- New separately scoped unchecked task opened in `TASKS.md`: "Production iOS saved-take notation, playback, and take-detail surface". It must consume the shared gesture-relative presentation with no per-platform fork, and must not remain DEBUG-only. The iOS parity requirement Karl asked for is preserved, not dropped. Explicitly not to be implemented inside Slice D or Slice E.
- Slice D remains NOT physically approved. Its outstanding checks are, by platform: macOS free-spin → catch → unequal push/pull → reversal; iOS live signed playhead; macOS saved Review and Take Detail notation parity.
- Working tree: HEAD still `c0bb3bab`, 31 modified + 1 untracked, `git diff --check` clean, nothing staged, committed, or pushed. Protected `project.pbxproj`, `ScratchLab.xcscheme`, `Info.plist`, and the untracked `CalibrationCameraOverlayTests.swift.plist` were not altered.

## 2026-08-30 (Capture-integrity Slice D: refreshed physical approval RECORDED; uncommitted)

- All three refreshed Slice D physical checks passed (Karl, 2026-08-30), against the current-source signed iOS build on iPhone `K` and a fresh isolated macOS Debug build.
- macOS free-spin → catch → unequal push/pull → reversal: **PASS**. The free-spin did not accumulate into the next gesture's baseline; catch, unequal push/pull, and reversal direction displayed correctly; completed strokes remained fixed. The fixed quarter-revolution full-scale display projection is approved on this basis.
- macOS saved Review and Take Detail notation parity: **PASS**. Stroke order, direction, timing, and relative excursion were preserved between live and saved surfaces.
- iOS live signed playhead: **PASS**. The playhead crossed `BEFORE START` and `PAST END` without wrapping and remained synchronized with audible AHHH.
- Corroborating macOS export, inspected read-only — explicitly NOT treated as the approval, which came from Karl's eyes and ears: session `53d4b65d`, `calibration_no_click`, RANE ONE MKII, 20 seconds, 12 files with none zero-byte, including all three debug traces (`movement_trace`, `movement_diagnostics`, `raw_platter_debug`), 16,494 mixer MIDI events, 3 audio events, 0 fader events (fader was not worked). One `slowDrag` free-spin run of 0.8419 revolution over 5.99 seconds, then 18 pulls spanning −0.0240 to −0.0558 revolution and 7 pushes spanning 0.0273 to 0.0524 revolution, so the performed asymmetry survives into the canonical evidence rather than being flattened.
- Recorded so a future reader does not misread the archive: in that export every post-catch gesture still starts at 0.7915–0.8507, i.e. still carrying the free-spin's 0.8419 offset. This is CORRECT. Canonical `recordMovementEvents` retain the raw signed coordinate for export/scoring, and gesture-relative rebasing is a presentation transform applied at render time. The raw offset in the file alongside a display that showed no permanent offset is exactly the two-coordinate split working, and must not be mistaken for the old accumulated-motor-phase defect.
- Slice D scope correction stands and is not softened by this approval: iOS ships the live signed playhead and live notation; macOS ships live notation plus saved Review and Take Detail. Slice D delivers no production iOS saved-take surface. The separately scoped unchecked task "Production iOS saved-take notation, playback, and take-detail surface" remains open in `TASKS.md` and must not be folded into D, E, or F.
- Slice D now has no outstanding physical acceptance risk. Working tree: HEAD still `c0bb3bab`, 31 modified + 1 untracked, nothing staged, committed, or pushed; protected `project.pbxproj`, `ScratchLab.xcscheme`, `Info.plist`, and the untracked `CalibrationCameraOverlayTests.swift.plist` untouched.

## 2026-08-30 (Capture-integrity Slice F: durable kept-session export)

- Selected task: Karl explicitly requested exactly Slice F, overriding the earlier task ordering note while leaving the separate production-iOS saved-take surface untouched.
- Root cause: post-Keep state was transient, so export discovery and session membership could be lost after Keep-and-Next/navigation; export readiness also did not prove that every real and generated artifact was complete, associated, unique, and unchanged.
- Files changed for Slice F: `ScratchLab/Models/CaptureCore.swift`, `ScratchLab/Views/CompanionCameraView.swift`, `ScratchLab/Services/SessionExportCoordinator.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`, and this log. Existing unrelated dirty hunks were preserved.
- Implemented: an atomic durable kept-session ledger shared by both Keep actions; persistent Export / Share routes in ready, setup, and completion states; append-only multi-take packaging; retry/cancel/failure state that preserves source captures; fail-closed local recovery; and before/after archive validation of media, audio, sidecar, notation, Watch/motion, metadata/manifests/review/replay/logs, optional mixes/stems, hashes, identities, and unique take paths.
- Verification: the final focused set passed 16/16 twice: Slice F deterministic coverage (single, multi, navigation, validation/retry, cancellation, package content, UI route contract), seven affected notation/MIDI export regressions, and two legacy metadata-group compatibility tests. Isolated generic iOS Simulator and macOS Debug builds passed after the final patch; fixtures 47/47; `git diff --check` clean. An initial broad gate added seven timestamp-validation regressions and an intermediate gate added two metadata-group regressions; both sets were fixed. The final isolated broad gate returned exactly the established 11 assertion failures / 9 unique names, with no Slice F or export failure.
- Follow-up risk: physical Share-sheet presentation/cancellation remains a device check; persisted absolute container URLs have no relocation migration. Nothing staged, committed, or pushed.

## 2026-08-30 (Capture-integrity Slice F: iOS "inconsistent metadata" export failure fixed; still physically unapproved)

- Trigger: the first real iPhone export of a freshly kept take failed with "This session has inconsistent metadata." The prior Slice F record claimed a green gate; that record was honest about the tests but did not cover this path, because every fixture used a whole-second `createdAt`. The earlier "COMPLETE" entry must not be read as a physical result.
- Root cause: `SessionArchiveBuilder.validateStagedPackage` wrote `manifests/session_metadata.json`, `export_metadata.json`, `session_review.json`, and `session_replay.json` through the canonical `JSONEncoder` whose `.iso8601` strategy serializes **whole seconds**, then decoded those files back and compared the decoded documents against the in-memory ones (`metadata == expectedMetadata`, `exportMetadata == …`, `review == …`). A `Date` with a sub-second component can never survive that round trip, so a byte-identical, fully valid export was rejected as inconsistent metadata. Measured directly: `Date(timeIntervalSince1970: 1_710_000_000.372)` encodes to `"2024-03-09T16:00:00Z"` and decodes back 0.372 s earlier, while re-encoding the same value is byte-stable.
- Why only freshly kept takes: a guided kept session's `createdAt` is a live `Date()` and the kept-session ledger persists it with the default (`.deferredToDate`) strategy, so the fraction survives in memory and across a reload. `packageForLocalRecordingSession` instead derives `createdAt` from sidecar dates that have already been through `.iso8601`, so recovered sessions are always whole-second. That is exactly why device recovery exports succeeded while the freshly kept take did not.
- Fix: compare the staged BYTES against the same documents re-encoded through the same canonical encoder — the pattern `session_manifest.json` and every notation document already used in the same function. Decoded documents are retained only for the structural identity assertions (session ID, take IDs, counts, replay/review generatedAt agreement). `createdAt` is not rounded or mutated, no take is filtered out, no default is fabricated, and no `try?`/`compactMap` was introduced. The check is strictly stronger than before: it now also proves each staged file is the canonical byte-for-byte serialization, not merely an equal-valued one.
- Also added, because "This session has inconsistent metadata." named nothing an operator or a device log could act on: `SessionExportValidationReason` and `SessionExportValidationFailure`. A staged-package rejection now carries the specific check that fired alongside the unchanged `SessionExportError`. The reason vocabulary is closed and contains check names only — never file paths, performer names, notes, or capture content — and the coordinator surfaces it through the existing `validationReport`, so the coarse error, its user message, and retry/cancel semantics are unchanged.
- The fail-closed check "take claims `.platter` motion but its notation recorded no movement events" was deliberately preserved and now reports by name. That path is still unreproduced on hardware and is covered by a deterministic named-error regression rather than being relaxed.
- Files changed: `ScratchLab/Services/SessionExportCoordinator.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`, and this log. `ScratchLab/Models/CaptureCore.swift` and `ScratchLab/Views/CompanionCameraView.swift` were left at their existing Slice F state. Protected `project.pbxproj`, `ScratchLab.xcscheme`, `Info.plist`, and the untracked `CalibrationCameraOverlayTests.swift.plist` were not altered or staged.
- Tests added (7, all in `CaptureReliabilityPhase1CoreTests`): sub-second-`createdAt` export success; sub-second preservation plus canonical-bytes assertion (fails if anyone "fixes" this again by flooring `createdAt`); single kept take exported exactly once; restored two-take ledger exported with both takes exactly once; coordinator share path plus cancellation for a sub-second session; platter-motion-without-movement named-error regression; coordinator named-reason reporting with staged captures preserved across the failure.
- Regression proof, not assumed: the fix was temporarily reverted to the value comparison, the target rebuilt, and the four sub-second cases confirmed to FAIL; the fix was then restored and they passed. Without that step the new tests would not be evidence of anything.
- Verification: focused set 14/14 in two executions. Affected regressions green — `CaptureRecoveryPhase2CoreTests` 58/58, `ScratchLabNotationAndExportTests` 29/29, `SessionExportRoundTripTests` 1/1, `SessionReviewMetadataTests` 17/17, `SessionQualityAnalyzerTests` 15/15, `SessionReplayTimelineTests` 7/7, `SessionReplayDocumentTests` 8/8 (135 total, 0 failures). Isolated generic iOS Simulator and macOS Debug builds succeeded; capture fixtures 47/47; `git diff --check` clean. Full configured gate: 3,157 XCTest cases / 49 skipped / **11 assertion failures across exactly the established 9 unique names** (4 `CaptureReliabilityPhase1CoreTests` layout cases, 4 `ScratchSamplePlaybackControllerTests` cases, 1 `TimecodePrototypeProfileTests` case), no export or Slice F name in the set, plus 366/366 Swift Testing cases.
- Slice F remains **NOT physically approved**. A real iPhone export of a freshly kept take must succeed, and Share-sheet presentation and cancellation must be checked on device, before the task can be closed. Nothing staged, committed, or pushed.

## 2026-08-30 (Capture-integrity Slice F: PHYSICAL VERIFICATION FAILED — kept-session ledger stores stale absolute container paths)

- Scope: physical-only verification of Slice F on iPhone `K`. No production code changed. HEAD stayed `7761c09f43` (no amend/reset/rebase/commit/push). The pre-existing dirty tree (11 modified + untracked `CalibrationCameraOverlayTests.swift.plist`) and the protected `project.pbxproj` / `ScratchLab.xcscheme` / `Info.plist` were left untouched and unstaged. Only `AI_HANDOFF.md`, `AI_HANDOFF/next_prompt.md`, `TASKS.md`, and this log were edited.
- Build/install: isolated `-derivedDataPath` / `OBJROOT` / `SYMROOT` / `SHARED_PRECOMPS_DIR` Debug build for `platform=iOS,id=…` with Xcode open — `BUILD SUCCEEDED`. Installed with `xcrun devicectl device install app` (upgrade; no uninstall, no erase). The failed session's container was preserved and independently verified: `Documents/CompanionCaptures/b5a93fd7-…_take00{1,2,3,4}_camA.{mov,wav,json}` all present and non-empty, every sidecar `recordingStatus: "completed"`.
- Installed-binary provenance: the diagnostic-reason changes are in the shipped code. `ScratchLab.app/ScratchLab.debug.dylib` (Xcode debug-dylib build, built 09:16 from source last modified 08:56) contains all seven `SessionExportValidationReason` raw values, the detail string "Export blocked: manifests/session_metadata.json did not match the session it was generated from.", `guided-capture-kept-sessions.json`, and `sourceMediaURL`. The sub-second-`createdAt` staged-bytes comparison is also present.
- Physical result: FAILED; no new take recorded. (1) The kept session did not present its takes as usable after the upgrade install/relaunch. (2) Retry Export did not reach the share sheet; on the freshly installed build it reports, per take, "Take 002 video is missing. Retake it before export.", "Take take-002 is missing b5a93fd7-…_take002_camA.json.", "Take 002 audio is missing. Retake it before export.", "Take 003 video is missing. Retake it before export." The pre-existing build had shown the opaque "This session has inconsistent metadata." for the same underlying condition.
- Confirmed root cause (Retry Export failure): `~/Library/Application Support/ScratchLab/guided-capture-kept-sessions.json` persists each take's `sourceMediaURL`, `sourceSidecarURL`, `sourceAudioURL`, and `sourceWatchMotionURL` as absolute `file://` URLs containing the app Data-container UUID `B116F53B-B4D3-4C1F-805D-40722E98025D`. The device's current Data container has a different UUID, so every one of those paths is now dangling. The underlying media is fine — the capture sidecars store only relative filenames (`mediaFileName` / `sidecarFileName`), so local recovery can still see the files, but the ledger cannot. `CompanionCameraView` (kept-take → `SessionExportTake`) copies `keptTake.sourceMediaURL` / `sourceAudioURL` / `sourceSidecarURL` straight through with no rebasing against the current container, and `SessionArchiveBuilder.packageValidationIssues` then fails `ArtifactPreflight.checkFileReady` / `FileManager.fileExists` on those dead URLs and emits the "… is missing" issues above. `packageValidationReport` maps any "missing" issue to `SessionExportError.missingRequiredFiles`, so `validateStagedPackage` and the new `SessionExportValidationReason` vocabulary are never reached for this session.
- The sub-second-`createdAt` / staged-bytes metadata fix is neither implicated nor disproven here: the pre-flight source-file check fails before staging happens, so that code never runs for this session.
- Failure 1 (kept session "missing") is only partially explained by the same stale paths: the ledger file itself survived intact and still holds session `b5a93fd7` with takes 002/003/004 (take-001 is not in the ledger; the companion audit summary records four completed takes). Because every ledger take resolves to a dead path, surfaces that gate on file resolution can render the session as empty or unexportable. The exact on-screen appearance of "missing" was not captured and is not fully closed.
- Timeline not established: today's `devicectl` upgrade install preserved the container contents, which normally also preserves the Data-container UUID, so the UUID most likely changed at an earlier install cycle. Whether the original 2026-08-30 "inconsistent metadata" failure that reopened Slice F was this same stale-path condition or the distinct `createdAt` bug is not proven; device logs and/or the original failing archive would settle it.
- No fix implemented this turn, per the verification-only scope. Slice F stays unchecked in `TASKS.md`. The "persisted absolute container URLs have no relocation migration" follow-up risk noted in the prior two Slice F entries is now a confirmed physical defect; the next Slice F action is to make the kept-session ledger resolve artifacts against the current container (container-relative paths or security-scoped bookmarks, rebased on load).

## 2026-08-30 (Production iOS saved-take notation, playback, and take-detail surface)

- Selected task: the first unchecked implementation-ready item after physically approved Slice F — ship a production iOS route that reopens real kept takes with saved notation and playback.
- Root cause: iOS had no production saved-take route. Its live review only rendered a thumbnail and text, macOS alone had saved Review/Take Detail, and the two iOS saved-notation views were DEBUG-only. The Slice F ledger already provided the authoritative active session and durable take membership.
- Files changed: `ScratchLab/Models/CaptureCore.swift`, `ScratchLab/Views/CompanionCameraView.swift`, `ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift`, `TASKS.md`, `DEV_LOG.md`, `AI_HANDOFF.md`, and `AI_HANDOFF/next_prompt.md`. Existing unrelated dirty hunks were preserved; protected project/config files were not altered or staged.
- Implemented: `GuidedCaptureSavedTakeDetail.load` resolves current-container URLs, requires real non-empty regular media and sidecar files, decodes the persisted sidecar, validates session/take identity, number, filenames, media association, and completed status, and projects notation through the shared macOS presentation helper. Session Setup and Session Complete now expose a production `Review Saved Take(s)` sheet backed by the active kept-session ledger. Detail uses the real media URL in native `VideoPlayer` and the shared `ScratchPhraseChartView`; validation failures remain visible and retryable without removing the take, and empty notation is reported honestly.
- Verification: three focused tests passed twice. Relevant kept-ledger/export regressions and the shared controller-notation presentation regressions passed. Isolated macOS `build-for-testing`, generic iOS Simulator Debug, and generic iOS device Debug with embedded Watch succeeded. `python3 scripts/test_capture_pipeline.py` passed 47/47. `git diff --check` clean.
- Full-gate note: the isolated configured Xcode gate hung in Foundation when its sandboxed Xcode 27 app-host attempted an existing repository source read. A direct unsandboxed xctest fallback completed 3,169 tests / 51 skipped with 208 assertions across 92 names, the known non-equivalent missing-resource/timing pattern, so it is not a valid comparison to the configured 11-failure / 9-name baseline. No failure referenced `GuidedCaptureSavedTakeDetail` or the new focused tests.
- Follow-up: task marked complete. The next unchecked implementation-ready task is Slice G, explicit audio ownership (external Serato silent / standalone local AHHH). Nothing committed, staged, or pushed.
## 2026-08-30 (Capture-integrity Slice G: explicit audio ownership)
- Selected task: the first implementation-ready unchecked item, Slice G, making External Serato versus ScratchLab Standalone audio ownership explicit on both iOS and macOS.
- Root cause: local AHHH entry points had no common ownership gate, creating double-audio risk and misleading playback/status feedback when Serato was intended to own sound.
- Files changed: `ScratchLab/Models/CaptureCore.swift`; `ScratchLab/Audio/iOS/IOScratchPlaybackEngine.swift`; `ScratchLab/MIDI/iOSMIDIManager.swift`; `ScratchLab/MIDI/iOSMIDILearnCoordinator.swift`; `ScratchLab/Views/PracticeModeView.swift`; `ScratchLab/Views/CompanionCameraView.swift`; `ScratchLab/Views/MainMenuView.swift`; `ScratchLab/Views/ScratchMotionLane.swift`; `ScratchLabDesktop/Services/MacCaptureEngine.swift`; `ScratchLabDesktop/Views/MacAnalyzerView.swift`; `ScratchLabDesktopTests/PlatterTestSampleLoadTests.swift`; `ScratchLabDesktopTests/MIDILearnEngineTests.swift`; `ScratchLabDesktopTests/MIDILearnHangFixTests.swift`; `ScratchLabDesktopTests/TimecodeBridgeContinuityFixTests.swift`; plus workflow records. Pre-existing overlapping hunks were preserved.
- Implemented: one shared persisted mode, default External Serato; cross-platform selection UI; authoritative gates for all iOS/macOS manual, hot-cue, DVS, and DEBUG local playback paths; immediate unload on switching external; truthful MIDI/status/mapping/waveform/accessibility feedback; lock-backed macOS realtime reads. Capture/export audio recording remains active and truthful in both modes.
- Verification: final focused ownership/MIDI/DVS regressions 48/48 twice; isolated iOS device/embedded-Watch and macOS builds succeeded; fixtures 47/47; full configured gate matched the established 11-failure / 9-name baseline with no Slice G failure; protected hashes unchanged; `git diff --check` clean. No commit, staging, or push.
- Follow-up: Slice H is next. Before release, physically smoke External Serato silence, Standalone AHHH, and switching an armed sample back to External.
