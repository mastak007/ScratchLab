# Next prompt - read-only receipt-slice commit-isolation audit

The receipt repair and Take 006 durable iPhone-to-Mac-sidecar transfer are verified; this does NOT mean the full authoring hardware protocol passed or any recording is a valid reference. No Take 007 is authorized.

## Current verified checkpoint

- HEAD d3ecf2a69bde57f7d6ac681b6dbdd37347c83e97; feature/ios-capture-camera-ux ahead 2; all work remains unstaged and uncommitted. Receipt changes share files with earlier work, so the conditional checkpoint commit was refused.
- Root cause: WCSessionFile.fileURL escaped into async work before durable ownership; the delegate now synchronously moves into Application Support/ScratchLab/IncomingWatchTransfers and async work uses only the staged URL. Recovery, safe naming, duplicate preservation and failure diagnostics are covered without new project membership.
- Mac/iPhone K 1.0.1 (22), compatible unchanged Watch 1.0.1 (21). Take 005 used the old phone build and remains missing-transfer evidence, alongside 003/004. D6 layout was hardware-verified on Take 005.
- Take 006: sessionID 41949897-5458-449d-9280-65508a4f6600; captureID D3687AE4-9476-40AD-BB00-0A7AE6DE29BC; file scratch-motion-2026-09-04T22-40-06Z-D3687A.json. Start command 59432ebb-90fa-433a-84e7-2b4e52f0e9de; stop command 87a8dc08-95d3-4da9-bf65-98c0dd388cee.
- Durable phone quarantine bytes, identical Mac motion, relay/reconciliation and both exact take-006 sidecar links passed. 835 ordered samples over 8.300746 seconds; about 2 s stop-to-phone and 5 s stop-to-link within the unchanged 90 s. The transient staging entry was consumed before inspection, not directly observed.
- Take 006 full checklist did NOT pass: zero fader samples, all 5,195 MIDI events Ch2/CC6; review blocks missing trustworthy calibrated fader evidence, not merely zero cuts. Its DEBUG row was cropped. Do not invent a fresh in-take OPEN observation or silently reuse Take 005's diagnostics.
- Existing verification: exact focused Watch/iPhone/relay/reconciliation selectors 123/123 per configuration; staging 18/18. Full serial gate passed Python 82 and all three platform builds. Available full bundle: 3,938 unique cases; 7,904 parameter-expanded runs = 7,800 passed, 104 skipped, 0 failed across two configurations. Focused log retained but its recorded bundle is now absent. Exact commands, destinations and paths are in DEV_LOG.md's final checkpoint entry.
- Tests must use a separate test-host bundle/container, not production ScratchLab. TEST_RUNNER_CFFIXED_USER_HOME alone was overridden by the sandbox. Existing successful gate used the temporary test-only bundle override described in DEV_LOG.md; do not launch that gate during this read-only audit.

## Exact next numbered prompt

1. Re-read AGENTS.md, SOUL.md, PROFILE.md, AI_CONTEXT.md, TASKS.md and the current DEV_LOG.md/AI_HANDOFF.md checkpoint. Inspect git status --short --branch, HEAD and the index first. This task is read-only: no source/documentation edits, staging, commits, pushes, resets, cleaning, stashing, discarding, builds, tests, deployment, app relaunch or recording.
2. Produce a hunk-level ownership/dependency map for the receipt repair in ScratchLab/Services/WatchMotionCaptureStore.swift, ScratchLab/Services/StagedCaptureRecovery.swift and ScratchLabDesktopTests/CaptureReliabilityPhase1Tests.swift. Identify exactly which test hunks are receipt-specific versus the earlier source-assertion, DEBUG-route and crossfader-buffer work. Inspect dependencies at HEAD without modifying either tree or index.
3. Separately map the receipt/checkpoint portions of TASKS.md, DEV_LOG.md, AI_HANDOFF.md and AI_HANDOFF/next_prompt.md. Keep D6's layout changes and the larger untracked authoring/calibration work separate. Do not extract whole files that contain mixed work or treat existing project.pbxproj membership/build-number changes as newly authorized.
4. Return the smallest proposed commit boundary, its required dependencies, and any additional permission needed to isolate mixed files or add a test file with project membership. Confirm the recorded exact verification evidence that would apply; name any gate that would need rerunning against a genuinely isolated candidate. Do not execute it or substitute a broad/wrong-target selector.
5. Preserve Takes 001-006, all Watch/relay artifacts, both old phone motion files and the existing calibration. Preserve the documented older test-host metadata mutations and later normal relay-summary refreshes; do not attempt restoration. No artifact may be promoted to valid/canonical/approved/training-eligible, and detector output is not human ground truth. Do not copy ScratchBook GPL source/code/data.
6. Stop with the proposed boundary and complete git status for Karl's decision. No commit is authorized by this audit. Do not implement D5, final-live-batch, quarantine/retry, Mac durable acknowledgement, schema/timeout changes, TTM/Tear or a fader workaround. Do not record Take 007 or approve/package/publish/install a reference or enable training.

## Risks still separate

Final live-batch identity clears before the final send; quarantined captures are excluded from reconnect retries; Mac acknowledgement precedes proven durable retention; nested transfer state can remain pending after a valid link; D5 session reuse/local review numbering is unresolved; fresh take-scoped OPEN fader evidence and broader authoring phases are incomplete. Any future physical test requires a new explicit scope. Nothing in this prompt expands recording or publication authority.
